---
phase: 05
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/constants/theme.ts
  - src/constants/translations.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04]

must_haves:
  truths:
    - "Every UI-SPEC §3 (theme tokens) addition exists as an exported constant in src/constants/theme.ts"
    - "Every UI-SPEC §10 (copywriting) RU+EN string pair exists in src/constants/translations.ts"
    - "Existing tokens (COLORS.success, SIZES.padding, SIZES.borderRadius, SIZES.iconSize) are preserved verbatim — no breaking changes to call sites"
    - "The legacy `adminUsers: 'Администраторы' / 'Administrators'` key is preserved AS-IS (per RESEARCH §Pitfall 8); new screen header uses `adminUsersTitle`"
    - "Dual-role delete labels `deleteBrokerProfile` and `deleteLogisticsProfile` exist for QuickActionSheet's role-explicit delete rows (per CONTEXT §D-04 + RESEARCH §Pitfall 11)"
    - "RU and EN translation blocks contain identical key sets"
  artifacts:
    - path: "src/constants/theme.ts"
      provides: "COLORS.moderation, COLORS.role, COLORS.destructive/warning/successFg/textTertiaryStrong, SIZES.spacing*/radius*/minTapTarget/badgeHeight/chipHeight/bottomSheetHandle*, TYPOGRAPHY"
      contains: "moderation: {"
    - path: "src/constants/translations.ts"
      provides: "~72 RU+EN keys per UI-SPEC §10 + dual-role delete keys"
      contains: "adminUsersTitle:"
  key_links:
    - from: "src/constants/theme.ts"
      to: "Phase 6 UserStatusBanner (consumer)"
      via: "COLORS.moderation.* import"
      pattern: "moderation: \\{"
    - from: "src/constants/translations.ts"
      to: "every Phase 5 component"
      via: "useLanguage().t.<key>"
      pattern: "severityFeatureLimited:|reasonSpam:|errCannotModerateSelf:|deleteBrokerProfile:|deleteLogisticsProfile:"
---

<objective>
Wave 1 foundation #1 — extend the design tokens (`src/constants/theme.ts`) with the full Phase 5 palette, spacing scale, and typography object specified verbatim in UI-SPEC §3 + §11; and add ~72 RU+EN translation keys (UI-SPEC §10 set plus the two dual-role delete labels required by RESEARCH §Pitfall 11) to `src/constants/translations.ts`.

Purpose: lock the visual contract before any component is built; resolve the `adminUsers` key collision flagged in RESEARCH Open Question 6; provide the role-explicit delete labels that QuickActionSheet (Plan 05-06) emits for users with both broker AND logistics profiles.
Output: `theme.ts` and `translations.ts` carry every token + string Phase 5 needs; downstream components import without reaching outside these files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-admin-moderation-ui-mobile/05-CONTEXT.md
@.planning/phases/05-admin-moderation-ui-mobile/05-UI-SPEC.md
@.planning/phases/05-admin-moderation-ui-mobile/05-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing token shape — back-compat MUST be preserved -->

From src/constants/theme.ts (current state — DO NOT BREAK):
```typescript
export const COLORS = {
  background: '#0F1115',
  cardBackground: '#181B21',
  textPrimary: '#FFFFFF',
  textSecondary: '#9CA3AF',
  textTertiary: '#6B7280',
  accent: '#3B82F6',
  border: '#2A2F3A',
  searchBackground: '#232730',
  success: '#22C55E',
};
export const SIZES = { padding: 16, borderRadius: 12, iconSize: 24 };
```

From src/constants/translations.ts (current admin block — RESEARCH §Pitfall 8 — `adminUsers` MUST be preserved):
```typescript
adminPanel: 'Панель администратора',
manageAdmins: 'Управление админами',
pendingRequests: 'Заявки на рассмотрение',
adminUsers: 'Администраторы',  // KEEP THIS — used by Profile menu
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend theme.ts with COLORS.moderation, COLORS.role, new top-level color tokens, expanded SIZES, and new TYPOGRAPHY export</name>
  <files>src/constants/theme.ts</files>
  <read_first>
    - src/constants/theme.ts (existing source — to preserve every existing key untouched)
    - .planning/phases/05-admin-moderation-ui-mobile/05-UI-SPEC.md (§3 Color, §11 Token Additions Summary — locked palette + sizes + typography)
    - .planning/phases/05-admin-moderation-ui-mobile/05-PATTERNS.md (§src/constants/theme.ts — back-compat constraint)
  </read_first>
  <behavior>
    - All existing keys (background, cardBackground, textPrimary, textSecondary, textTertiary, accent, border, searchBackground, success, padding, borderRadius, iconSize) remain at their current values — zero breaking changes
    - COLORS.moderation contains exactly 4 nested keys (active, featureLimited, blockedReview, permaBanned) each with bg + fg + border
    - COLORS.role contains exactly 4 nested keys (admin, broker, seller, logistics) each with bg + fg
    - COLORS top-level adds: destructive, warning, successFg, textTertiaryStrong
    - SIZES adds: spacingXs/Sm/Md/Lg/Xl/2xl, radiusSm/Md/Pill, minTapTarget, badgeHeight, chipHeight, bottomSheetHandleWidth, bottomSheetHandleHeight
    - TYPOGRAPHY exported as a separate const with: body, bodyStrong, label, labelStrong, heading, display
  </behavior>
  <action>
Replace the entire contents of `src/constants/theme.ts` with the following EXACT contents. Every value is locked by UI-SPEC §11 and MUST NOT be modified.

```typescript
export const COLORS = {
  // --- Existing core (preserved verbatim — DO NOT change) ---
  background: '#0F1115', // Very dark blue-grey, sleek
  cardBackground: '#181B21', // Slightly lighter blue-grey
  textPrimary: '#FFFFFF',
  textSecondary: '#9CA3AF', // Cool grey
  textTertiary: '#6B7280',
  accent: '#3B82F6', // Corporate Blue (Business-like, Trustworthy)
  border: '#2A2F3A',
  searchBackground: '#232730',
  success: '#22C55E', // KEEP — existing call sites depend on this

  // --- Phase 5 additions (UI-SPEC §11) ---
  textTertiaryStrong: '#D1D5DB', // stronger neutral for reason chips
  successFg: '#4ADE80', // aligned with moderation.active.fg
  destructive: '#EF4444', // explicit token (was ad-hoc before)
  warning: '#F59E0B', // amber — superadmin, destructive warnings in modals

  // Severity palette — Phase 6 UserStatusBanner imports this verbatim
  moderation: {
    active: { bg: 'rgba(34, 197, 94, 0.15)', fg: '#4ADE80', border: '#4ADE80' },
    featureLimited: { bg: 'rgba(245, 158, 11, 0.15)', fg: '#FBBF24', border: '#FBBF24' },
    blockedReview: { bg: 'rgba(239, 68, 68, 0.15)', fg: '#F87171', border: '#F87171' },
    permaBanned: { bg: 'rgba(156, 163, 175, 0.15)', fg: '#9CA3AF', border: '#6B7280' },
  },

  // Role badge palette
  role: {
    admin: { bg: 'rgba(245, 158, 11, 0.15)', fg: '#F59E0B' },
    broker: { bg: 'rgba(59, 130, 246, 0.15)', fg: '#3B82F6' },
    seller: { bg: 'rgba(6, 182, 212, 0.15)', fg: '#22D3EE' },
    logistics: { bg: 'rgba(167, 139, 250, 0.15)', fg: '#A78BFA' },
  },
};

export const SIZES = {
  // --- Existing (preserved verbatim) ---
  padding: 16,
  borderRadius: 12,
  iconSize: 24,

  // --- Phase 5 additions (UI-SPEC §11) ---
  spacingXs: 4,
  spacingSm: 8,
  spacingMd: 16, // alias for padding; use spacingMd in new code
  spacingLg: 24,
  spacingXl: 32,
  spacing2xl: 48,
  radiusSm: 8, // chips, inputs, buttons
  radiusMd: 12, // alias for borderRadius; cards
  radiusPill: 999, // badges
  minTapTarget: 44, // minimum touch zone
  badgeHeight: 22, // severity badge height
  chipHeight: 32, // filter chip height
  bottomSheetHandleWidth: 36,
  bottomSheetHandleHeight: 4,
};

export const TYPOGRAPHY = {
  body: { fontSize: 14, fontWeight: '400' as const, lineHeight: 20 },
  bodyStrong: { fontSize: 14, fontWeight: '600' as const, lineHeight: 20 }, // badge labels
  label: { fontSize: 16, fontWeight: '400' as const, lineHeight: 24 },
  labelStrong: { fontSize: 16, fontWeight: '600' as const, lineHeight: 24 }, // primary row text
  heading: { fontSize: 20, fontWeight: '600' as const, lineHeight: 26 },
  display: { fontSize: 28, fontWeight: '600' as const, lineHeight: 34 },
};
```

The `as const` on fontWeight values is REQUIRED — React Native StyleSheet types reject plain `string` for fontWeight; only the literal union (`'normal' | 'bold' | '100' | ... | '900'`) is accepted.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/constants/theme.ts 2>&1 | grep -E "^src/constants/theme.ts" | wc -l | xargs -I {} test {} -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "background: '#0F1115'" src/constants/theme.ts` returns 1 (back-compat)
    - `grep -c "success: '#22C55E'" src/constants/theme.ts` returns 1 (back-compat)
    - `grep -c "padding: 16" src/constants/theme.ts` returns 1 (back-compat)
    - `grep -c "borderRadius: 12" src/constants/theme.ts` returns 1 (back-compat)
    - `grep -c "moderation:" src/constants/theme.ts` returns 1
    - `grep -c "featureLimited:" src/constants/theme.ts` returns 1
    - `grep -c "blockedReview:" src/constants/theme.ts` returns 1
    - `grep -c "permaBanned:" src/constants/theme.ts` returns 1
    - `grep -c "role:" src/constants/theme.ts` returns 1
    - `grep -c "destructive: '#EF4444'" src/constants/theme.ts` returns 1
    - `grep -c "warning: '#F59E0B'" src/constants/theme.ts` returns 1
    - `grep -c "successFg: '#4ADE80'" src/constants/theme.ts` returns 1
    - `grep -c "textTertiaryStrong: '#D1D5DB'" src/constants/theme.ts` returns 1
    - `grep -c "spacingXs:" src/constants/theme.ts` returns 1
    - `grep -c "spacing2xl: 48" src/constants/theme.ts` returns 1
    - `grep -c "radiusPill: 999" src/constants/theme.ts` returns 1
    - `grep -c "minTapTarget: 44" src/constants/theme.ts` returns 1
    - `grep -c "bottomSheetHandleWidth: 36" src/constants/theme.ts` returns 1
    - `grep -c "export const TYPOGRAPHY" src/constants/theme.ts` returns 1
    - `grep -c "labelStrong:" src/constants/theme.ts` returns 1
    - `grep -c "as const" src/constants/theme.ts` returns 6 (one per font weight)
    - `npx tsc --noEmit` does not introduce a new error in `src/constants/theme.ts` (existing call sites still typecheck)
  </acceptance_criteria>
  <done>theme.ts contains all UI-SPEC §11 tokens; existing call sites continue to typecheck.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ~72 RU+EN translation keys to translations.ts (UI-SPEC §10 + dual-role delete labels)</name>
  <files>src/constants/translations.ts</files>
  <read_first>
    - src/constants/translations.ts (existing file — to find the RU and EN block boundaries; preserve `adminUsers` key per RESEARCH §Pitfall 8)
    - .planning/phases/05-admin-moderation-ui-mobile/05-UI-SPEC.md (§10 Copywriting Contract — exhaustive RU+EN string list)
    - .planning/phases/05-admin-moderation-ui-mobile/05-PATTERNS.md (§src/constants/translations.ts — RU/EN ordering parity convention)
    - .planning/phases/05-admin-moderation-ui-mobile/05-CONTEXT.md (D-04 typed confirmation + RESEARCH §Pitfall 11 — dual-role delete requires explicit role labels)
  </read_first>
  <behavior>
    - Existing `adminUsers: 'Администраторы' / 'Administrators'` key is NOT renamed and NOT removed
    - New `adminUsersTitle` key is added (RU: 'Пользователи', EN: 'Users') for the repurposed AdminManagementScreen header
    - All other keys from UI-SPEC §10 are added under both RU and EN
    - Dual-role delete keys `deleteBrokerProfile` and `deleteLogisticsProfile` are added in BOTH RU and EN — these label the two distinct delete rows the QuickActionSheet renders when a target user holds BOTH broker AND logistics profiles (RESEARCH §Pitfall 11)
    - The set of keys present in the RU object equals the set in the EN object (strict parity)
    - Keys are grouped under a `// Phase 5 — Admin Moderation UI` comment block in BOTH RU and EN, with identical ordering
  </behavior>
  <action>
Locate the existing RU and EN admin blocks in `src/constants/translations.ts` (search for `adminUsers:` to find them). Append the Phase 5 keys at the END of EACH block (RU first, then EN later in the file), preceded by a banner comment.

Use the EXACT key set below. Every key MUST appear in BOTH the RU object and the EN object with the strings shown.

**Banner comment (insert in BOTH RU and EN blocks at the start of the addition):**
```
    // ---- Phase 5 — Admin Moderation UI (UI-SPEC §10) ----
```

**Insert the following keys in BOTH the RU object and the EN object, in this exact order. The RU object gets the RU values; the EN object gets the EN values.**

Screen titles + headers:
- `adminUsersTitle`: 'Пользователи' / 'Users'
- `adminModerationTitle`: 'Модерация' / 'Moderation'
- `adminUserDetailTitle`: 'Карточка пользователя' / 'User Details'

Filter chips — AdminManagementScreen:
- `filterAdminsOnly`: 'Только админы' / 'Admins only'
- `filterAllUsers`: 'Все пользователи' / 'All users'

Filter chips — AdminModerationScreen — Role group:
- `roleFilterAll`: 'Все роли' / 'All roles'
- `roleFilterBuyer`: 'Покупатели' / 'Buyers'
- `roleFilterSeller`: 'Продавцы' / 'Sellers'
- `roleFilterBroker`: 'Брокеры' / 'Brokers'
- `roleFilterLogistics`: 'Логистика' / 'Logistics'
- `roleFilterAdmin`: 'Админы' / 'Admins'

Filter chips — State group:
- `stateFilterAll`: 'Все статусы' / 'All states'
- `stateFilterActive`: 'Активен' / 'Active'
- `stateFilterFeatureLimited`: 'Огр. функции' / 'Limited'
- `stateFilterBlocked`: 'Заблокирован' / 'Blocked'
- `stateFilterBanned`: 'Забанен' / 'Banned'

Severity names + descriptions (for SeverityPicker radio cards):
- `severityFeatureLimited`: 'Ограничить функции' / 'Feature-limited'
- `severityFeatureLimitedDesc`: 'Пользователь может заходить и смотреть, но не может размещать, заказывать или связываться.' / 'User can log in and browse, but cannot list, order, or message.'
- `severityBlockedWithReview`: 'Заблокировать с апелляцией' / 'Blocked with review'
- `severityBlockedWithReviewDesc`: 'Вход заблокирован. Пользователь увидит инструкцию написать в поддержку.' / 'Sign-in blocked. User sees instructions to contact support for appeal.'
- `severityPermanentlyBanned`: 'Бессрочный бан' / 'Permanent ban'
- `severityPermanentlyBannedDesc`: 'Пользователь полностью заблокирован без права апелляции. Необратимое действие.' / 'User fully blocked with no appeal path. Irreversible.'

Reason categories:
- `reasonSpam`: 'Спам' / 'Spam'
- `reasonPolicyViolation`: 'Нарушение правил' / 'Policy violation'
- `reasonFraud`: 'Мошенничество' / 'Fraud'
- `reasonOther`: 'Другое' / 'Other'

Action names (quick-action sheet rows + modal titles):
- `actionSuspend`: 'Приостановить' / 'Suspend'
- `actionUnsuspend`: 'Восстановить' / 'Unsuspend'
- `actionRevokeRole`: 'Отозвать роль' / 'Revoke role'
- `actionDeleteProfile`: 'Удалить профиль' / 'Delete profile'
- `actionEditProfile`: 'Редактировать профиль' / 'Edit profile'

Dual-role delete labels (QuickActionSheet renders these as two distinct rows when target holds BOTH broker AND logistics profiles — see RESEARCH §Pitfall 11; Plan 05-06 implements):
- `deleteBrokerProfile`: 'Удалить профиль брокера' / 'Delete broker profile'
- `deleteLogisticsProfile`: 'Удалить профиль логистики' / 'Delete logistics profile'

Form field labels:
- `fieldSeverity`: 'Степень' / 'Severity'
- `fieldReason`: 'Причина' / 'Reason'
- `fieldNote`: 'Примечание (необязательно)' / 'Note (optional)'
- `fieldNotePlaceholder`: 'До 500 символов…' / 'Up to 500 characters…'
- `fieldRoleToRevoke`: 'Какую роль отозвать' / 'Which role to revoke'
- `fieldRoleToDelete`: 'Какой профиль удалить' / 'Which profile to delete'
- `fieldCompanyName`: 'Название компании' / 'Company name'
- `fieldPhoneNumber`: 'Телефон' / 'Phone number'
- `fieldTelegram`: 'Telegram' / 'Telegram'
- `fieldCoverageAreas`: 'Регионы обслуживания' / 'Coverage areas'
- `fieldTimelines`: 'Сроки' / 'Timelines'

Primary CTAs (modal-specific):
- `modalConfirm`: 'Подтвердить' / 'Confirm'
- `modalCancel`: 'Отмена' / 'Cancel'
- `confirmSuspend`: 'Применить ограничение' / 'Apply restriction'
- `confirmUnsuspend`: 'Восстановить доступ' / 'Restore access'
- `confirmRevokeRole`: 'Отозвать роль' / 'Revoke role'
- `confirmDeleteProfile`: 'Удалить профиль' / 'Delete profile'
- `confirmEditProfile`: 'Сохранить изменения' / 'Save changes'
- `unsuspendUser`: 'Восстановить пользователя' / 'Unsuspend user'

Typed-confirmation copy:
- `typedConfirmTitle`: 'Подтвердите необратимое действие' / 'Confirm irreversible action'
- `typedConfirmHint`: 'Введите email пользователя {email} для подтверждения' / 'Type the user\'s email {email} to confirm'
- `typedConfirmInputPlaceholder`: 'email@example.com' / 'email@example.com'
- `typedConfirmMismatch`: 'Email не совпадает' / 'Email doesn\'t match'
- `typedConfirmWarningHeading`: 'Это действие нельзя отменить' / 'This action cannot be undone'
- `typedConfirmWarningBodyDelete`: 'Профиль провайдера будет удалён навсегда. История заказов сохранится.' / 'The provider profile will be permanently deleted. Order history is preserved.'
- `typedConfirmWarningBodyRevoke`: 'Пользователь потеряет роль. Прошлые записи сохранятся, но роль больше не будет отображаться.' / 'The user will lose this role. Past records are preserved but the role will no longer appear.'
- `typedConfirmWarningBodyPermaBan`: 'Пользователь будет забанен без возможности апелляции.' / 'User will be permanently banned with no appeal path.'

Empty states:
- `emptyUsersTitle`: 'Нет пользователей' / 'No users found'
- `emptyUsersBody`: 'Измените фильтр или потяните вниз, чтобы обновить.' / 'Change the filter or pull down to refresh.'
- `emptySearchTitle`: 'Ничего не найдено' / 'No matches'
- `emptySearchBody`: 'Попробуйте другой email или UID.' / 'Try a different email or UID.'
- `searchPromptTitle`: 'Начните поиск' / 'Start searching'
- `searchPromptBody`: 'Введите email или Firebase UID, чтобы найти пользователя.' / 'Type an email or Firebase UID to find a user.'
- `searchEmailOrUid`: 'Поиск: email или Firebase UID' / 'Search: email or Firebase UID'
- `emptyHistoryTitle`: 'Действий модерации нет' / 'No moderation history'
- `emptyHistoryBody`: 'С этим пользователем ещё не было действий модерации.' / 'No moderation actions have been taken on this user yet.'

Error mappings (one per ModerationError.code — consumed by Plan 05-04 moderationErrorKeyMap):
- `errCannotModerateSelf`: 'Нельзя модерировать самого себя.' / 'You cannot moderate your own account.'
- `errLastAdmin`: 'Нельзя заблокировать последнего активного администратора.' / 'Cannot suspend the last active admin.'
- `errRoleNotAssigned`: 'У пользователя нет этой роли.' / 'User does not have this role.'
- `errInvalidField`: 'Недопустимое поле для этой роли.' / 'Field not allowed for this role.'
- `errNoChanges`: 'Не указаны изменения.' / 'No changes were provided.'
- `errInvalidRoleForDelete`: 'Можно удалить только профиль брокера или логиста.' / 'Only broker or logistics profiles can be deleted.'
- `errUserNotFound`: 'Пользователь не найден.' / 'User not found.'
- `errRateLimited`: 'Слишком много действий. Подождите несколько минут.' / 'Too many actions. Wait a few minutes.'
- `errAlreadyAtSeverity`: 'Пользователь уже в этом статусе.' / 'User is already at this severity.'
- `errNotSuspended`: 'Пользователь не заблокирован.' / 'User is not suspended.'
- `errAccountSuspended`: 'Действие заблокировано: ваш аккаунт ограничен.' / 'Action blocked: your account is restricted.'
- `errNetwork`: 'Ошибка сети. Проверьте подключение.' / 'Network error. Check your connection.'
- `errGeneric`: 'Не удалось выполнить действие.' / 'Action failed.'

Success toasts:
- `toastSuspended`: 'Пользователь ограничен' / 'User restricted'
- `toastUnsuspended`: 'Доступ восстановлен' / 'Access restored'
- `toastRoleRevoked`: 'Роль отозвана' / 'Role revoked'
- `toastProfileDeleted`: 'Профиль удалён' / 'Profile deleted'
- `toastProfileEdited`: 'Профиль обновлён' / 'Profile updated'

Sticky summary card copy:
- `summaryHistoryCount`: '{count} действий' / '{count} actions'
- `summaryRolesLabel`: 'Роли' / 'Roles'
- `summaryNoRoles`: 'Нет специальных ролей' / 'No special roles'
- `summaryMemberSinceLabel`: 'В сервисе с' / 'Member since'

Capability preview (for SeverityPicker description bottom line):
- `capabilityBrowseOnly`: 'Может смотреть, но не может: {list}' / 'Can browse, cannot: {list}'
- `capabilityFullyBlocked`: 'Вход невозможен. Все функции заблокированы.' / 'Sign-in blocked. All features disabled.'
- `capabilityPermaBlocked`: 'Вход невозможен навсегда.' / 'Sign-in blocked permanently.'
- `capItemCreateListing`: 'размещать объявления' / 'list cars'
- `capItemCreateOrder`: 'заказывать услуги' / 'place orders'
- `capItemContactSeller`: 'писать продавцам' / 'message sellers'
- `capItemWithdrawFunds`: 'выводить средства' / 'withdraw funds'

Implementation steps:
1. Open `src/constants/translations.ts`
2. Locate the RU object's admin block (search for `adminUsers:`); append the banner comment + all keys above (with RU values) at the end of the RU object — INSIDE the closing `}` brace of the RU literal
3. Locate the EN object similarly (search for `adminUsers:` second occurrence); append the banner comment + all keys above (with EN values) at the end of the EN object in the same order
4. Do NOT touch any existing key. Do NOT rename `adminUsers`.
5. After the edit, the count of new keys added per block must be exactly the same (parity).

Notes on syntax:
- Use single quotes around string values (matches existing convention)
- Escape apostrophes in EN strings with `\'` (e.g. `'You\'re...'`)
- For `{email}`, `{count}`, `{list}` interpolation tokens — they are LITERAL substrings in the values; consumers will use `string.replace('{email}', value)` at render time
  </action>
  <verify>
    <automated>node -e "const T = require('./src/constants/translations.ts').TRANSLATIONS || require('./src/constants/translations.ts'); const ru = Object.keys(T.RU || {}); const en = Object.keys(T.EN || {}); const missing = ru.filter(k => !en.includes(k)).concat(en.filter(k => !ru.includes(k))); if (missing.length > 0) { console.error('MISSING parity:', missing); process.exit(1); }" 2>&1 || echo "If node-eval fails (TS module), fall back to grep parity"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "adminUsers:" src/constants/translations.ts` returns exactly 2 (one RU, one EN — preserved verbatim, NOT renamed)
    - `grep -c "adminUsersTitle:" src/constants/translations.ts` returns 2 (new key, RU + EN parity)
    - `grep -c "adminModerationTitle:" src/constants/translations.ts` returns 2
    - `grep -c "adminUserDetailTitle:" src/constants/translations.ts` returns 2
    - `grep -c "severityFeatureLimited:" src/constants/translations.ts` returns 2
    - `grep -c "severityBlockedWithReview:" src/constants/translations.ts` returns 2
    - `grep -c "severityPermanentlyBanned:" src/constants/translations.ts` returns 2
    - `grep -c "reasonSpam:" src/constants/translations.ts` returns 2
    - `grep -c "actionSuspend:" src/constants/translations.ts` returns 2
    - `grep -c "actionUnsuspend:" src/constants/translations.ts` returns 2
    - `grep -c "actionRevokeRole:" src/constants/translations.ts` returns 2
    - `grep -c "actionEditProfile:" src/constants/translations.ts` returns 2
    - `grep -c "actionDeleteProfile:" src/constants/translations.ts` returns 2
    - `grep -c "deleteBrokerProfile:" src/constants/translations.ts` returns 2 (new — dual-role delete label, RU + EN parity)
    - `grep -c "deleteLogisticsProfile:" src/constants/translations.ts` returns 2 (new — dual-role delete label, RU + EN parity)
    - `grep -c "errCannotModerateSelf:" src/constants/translations.ts` returns 2
    - `grep -c "errLastAdmin:" src/constants/translations.ts` returns 2
    - `grep -c "errRateLimited:" src/constants/translations.ts` returns 2
    - `grep -c "errNetwork:" src/constants/translations.ts` returns 2
    - `grep -c "errGeneric:" src/constants/translations.ts` returns 2
    - `grep -c "typedConfirmTitle:" src/constants/translations.ts` returns 2
    - `grep -c "typedConfirmWarningBodyDelete:" src/constants/translations.ts` returns 2
    - `grep -c "typedConfirmWarningBodyRevoke:" src/constants/translations.ts` returns 2
    - `grep -c "typedConfirmWarningBodyPermaBan:" src/constants/translations.ts` returns 2
    - `grep -c "emptyHistoryTitle:" src/constants/translations.ts` returns 2
    - `grep -c "searchPromptTitle:" src/constants/translations.ts` returns 2
    - `grep -c "summaryHistoryCount:" src/constants/translations.ts` returns 2
    - `grep -c "capabilityBrowseOnly:" src/constants/translations.ts` returns 2
    - `grep -c "Phase 5 — Admin Moderation UI" src/constants/translations.ts` returns 2 (one per language block)
    - `npx tsc --noEmit` reports no NEW TypeScript errors in `src/constants/translations.ts` beyond pre-existing
  </acceptance_criteria>
  <done>translations.ts contains every Phase 5 key in both RU and EN with strict parity; existing keys (incl. `adminUsers`) are unchanged; dual-role delete labels are present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → repo | Pure constants — no runtime trust boundary; no user input flows through these files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | translation interpolation tokens (`{email}`, `{count}`, `{list}`) | mitigate | Tokens are LITERAL substrings — consumers use `String.prototype.replace`, not template eval. No code execution path. |
| T-05-02-02 | Information Disclosure | EN copy reveals product moderation severity names | accept | Same labels are visible in the UI itself; no leak beyond what the UI shows |
| T-05-02-03 | Spoofing | New theme tokens duplicate names with existing tokens (`success` vs `successFg`) | mitigate | Both tokens preserved with distinct keys; existing call sites of `COLORS.success` continue to resolve to the original `#22C55E`; new code uses `COLORS.successFg` |
| T-05-02-04 | Tampering | dual-role delete labels accidentally deviate from action names, causing admin confusion about which profile is being deleted | mitigate | Both `deleteBrokerProfile` and `deleteLogisticsProfile` explicitly name the target role in BOTH languages; QuickActionSheet (Plan 05-06) renders them as two SEPARATE rows so the admin's tap unambiguously selects the role payload |
</threat_model>

<verification>
- `theme.ts` contains all Phase 5 tokens; tsc reports no new errors
- `translations.ts` RU + EN have strict key parity; `adminUsers` legacy key preserved
- `deleteBrokerProfile` and `deleteLogisticsProfile` exist in both languages
- No existing call site of `COLORS.success`, `SIZES.padding`, `SIZES.borderRadius`, `SIZES.iconSize` breaks
</verification>

<success_criteria>
- All UI-SPEC §11 token additions present in theme.ts
- All UI-SPEC §10 RU+EN keys present in translations.ts with parity
- Dual-role delete keys (`deleteBrokerProfile`, `deleteLogisticsProfile`) present with parity
- Banner comment `// ---- Phase 5 — Admin Moderation UI (UI-SPEC §10) ----` appears in both RU and EN blocks
- Legacy `adminUsers` key preserved (RESEARCH §Pitfall 8)
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-moderation-ui-mobile/05-02-SUMMARY.md`
</output>
