---
phase: 09-backend-read-time-toctou-enforcement
plan: 05
type: execute
wave: 2
depends_on:
  - 01
  - 02
  - 04
files_modified:
  - ../backend-services/carEx-services/src/payments/confirmBooking.js
  - ../backend-services/carEx-services/__tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js
autonomous: true
requirements:
  - LENF-03
must_haves:
  goal: |
    Non-active listings disappear from all public reads without any denormalized
    flag mutation, listing-detail GET returns a status-aware thin payload to
    non-admin viewers, and cart-add + confirm-booking re-verify listing status
    inside the same transaction with refund-first-throw-second semantics.
  truths:
    - "Listing status flipped to suspended/archived/deleted between create-payment-intent and confirm-booking causes confirm-booking to abort the Mongoose transaction with a 409 listing_not_available response (D-12, D-13, ROADMAP Criterion #3)"
    - "Step 4 (Car refetch inside session.withTransaction) chains BOTH bypass flags: .setOptions({ includeAllUsers: true, includeAllListingStatuses: true }) — the existing seller-cascade hook AND the Plan 02 listing-status hook are both bypassed so the lookup succeeds for any seller AND any listing status (D-12, Pitfall 1)"
    - "Step 4 checks three conditions in order (D-13): (1) seller.moderationStatus.state === 'active' (v1.0 existing); (2) seller.sellerStatus === 'APPROVED' (v1.0 existing); (3) car.status === 'active' (Phase 9 NEW). Any failure routes through refundAndThrow."
    - "When listing is non-active inside the transaction, refundAndThrow is called with errorBody = { error: 'listing_not_available', listingStatus, reasonCategory, banner } — the helper issues stripe.refunds.create BEFORE throwing ListingNotAvailableError (D-14 refund-first-throw-second invariant, Plan 01)"
    - "stripe.refunds.create receives idempotencyKey: `refund-${paymentIntentId}` so withTransaction auto-retries do not double-refund (Plan 01 helper, Pitfall 3, RESEARCH A3)"
    - "Existing 7-case __tests__/enforcement/confirmBooking.transaction.test.js suite still passes after the step-4 extension (D-15 regression — v1.0 production path preserved)"
    - "Concurrent admin.suspendListing + buyer confirm-booking — exactly one outcome (refund-abort OR booking-then-suspend), never both succeed (Pitfall 9 race coverage)"
    - "Server.js confirm-booking error-mapping branch (wired in Plan 04) is exercised end-to-end: ListingNotAvailableError thrown from confirmBooking.js step 4 → caught at server.js:1049+ → 409 with full D-11 body shape including refundId + refundFailed"
    - "confirmBooking.js imports ListingNotAvailableError from its CANONICAL neighbor module './refundAndThrow' (W-7 — same canonical source used by server.js per Plan 04)"
    - "End-of-Plan-05 `refundAndThrow(` call-count in confirmBooking.js is EXACTLY 4 (3 v1.0 + 1 Phase 9 LENF-03) per B-3"
    - "confirmBooking.listingTOCTOU.test.js scaffold (Plan 01) transitions from RED to GREEN — 6 LENF-03 confirm-booking cases pass"
  artifacts:
    - path: "../backend-services/carEx-services/src/payments/confirmBooking.js"
      provides: "step 4 listing-status assertion inside session.withTransaction — refunds + aborts on non-active listing via the Plan 01 refundAndThrow helper"
      contains: "car.status !== 'active'"
    - path: "../backend-services/carEx-services/__tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js"
      provides: "6 GREEN integration tests covering LENF-03 confirm-booking half + race + route + regression"
  key_links:
    - from: "src/payments/confirmBooking.js step 4 Car refetch"
      to: "Plan 02 hide hook + Phase 1/3 seller-cascade hook"
      via: ".setOptions({ includeAllUsers: true, includeAllListingStatuses: true }).session(session)"
      pattern: "includeAllListingStatuses: true.*includeAllUsers: true|includeAllUsers: true.*includeAllListingStatuses: true"
    - from: "src/payments/confirmBooking.js step 4 listing-status assertion"
      to: "Plan 01 refundAndThrow + ListingNotAvailableError (canonical neighbor module — W-7)"
      via: "await refundAndThrow(stripe, paymentIntentId, { error: 'listing_not_available', listingStatus, reasonCategory, banner })"
      pattern: "refundAndThrow\\(stripe, paymentIntentId, \\{[\\s\\S]*?error: 'listing_not_available'"
    - from: "src/payments/confirmBooking.js (require)"
      to: "src/payments/refundAndThrow.js (canonical source for ListingNotAvailableError — W-7)"
      via: "const { refundAndThrow, ListingNotAvailableError, ProviderSuspendedError } = require('./refundAndThrow');"
      pattern: "require\\(['\\\"]\\./refundAndThrow['\\\"]"
---

<objective>
LENF-03 part B: Add the third condition to confirmBooking.js step 4 — verify
`car.status === 'active'` inside the existing v1.0 `session.withTransaction()`
callback. On failure, route through the `refundAndThrow` helper (Plan 01)
which issues `stripe.refunds.create` with idempotency-key BEFORE throwing
`ListingNotAvailableError`. The thrown error aborts the Mongo transaction
(rolls back order writes + listingStatus flip) and is mapped by the route
handler (Plan 04) to a 409 with the D-11 body shape including `refundId` and
`refundFailed`.

Purpose: deliver the confirm-booking half of ROADMAP Criterion #3 ("a status
change between cart-add and confirm-booking aborts the booking inside the
transaction and refunds the Stripe charge before throwing"). This is the
TOCTOU close on the create-payment-intent → confirm-booking window opened
by Plan 04's early-gate-only protection.

Output: extended `confirmBooking.js` step 4 + GREEN `confirmBooking.listing
TOCTOU.test.js`. After this plan, ALL Phase 9 scaffolds are GREEN and the
phase is feature-complete.

**Class-identity safety (W-7):** confirmBooking.js imports
`ListingNotAvailableError` from the canonical neighbor module
`./refundAndThrow` — the SAME canonical source that server.js (Plan 04)
imports from. This ensures the class object referenced by `throw new
ListingNotAvailableError(...)` here matches the class referenced by
`if (err instanceof ListingNotAvailableError)` in server.js, so the route-
level error mapping works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-backend-read-time-toctou-enforcement/09-CONTEXT.md
@.planning/phases/09-backend-read-time-toctou-enforcement/09-RESEARCH.md
@.planning/phases/09-backend-read-time-toctou-enforcement/09-VALIDATION.md
@.planning/phases/09-backend-read-time-toctou-enforcement/09-PATTERNS.md
@.planning/phases/09-backend-read-time-toctou-enforcement/09-01-PLAN.md
@.planning/phases/09-backend-read-time-toctou-enforcement/09-02-PLAN.md
@.planning/phases/09-backend-read-time-toctou-enforcement/09-04-PLAN.md

<interfaces>
<!-- From Plan 01 (Wave 0): -->
```
src/payments/refundAndThrow.js exports:
  - refundAndThrow(stripe, paymentIntentId, errorBody):
       1. stripe.refunds.create({ payment_intent }, { idempotencyKey: `refund-${paymentIntentId}` })
       2. Discriminate: errorBody.error === 'listing_not_available' → throw new ListingNotAvailableError({ ...errorBody, refundId, refundFailed })
                       else                                          → throw ProviderSuspendedError with Object.assign of enriched body
  - ListingNotAvailableError class — CANONICAL SOURCE (W-7); confirmBooking.js MUST import from this module directly, not via re-export.
  - ProviderSuspendedError class (moved here from confirmBooking.js for circular-require resolution)
```

<!-- From Plan 02 (Wave 1): -->
```
src/models/Car.js pre(/^find/) listing-status hide hook:
  Default behavior: filter status: 'active'.
  Bypass: setOptions({ includeAllListingStatuses: true }).
  Plan 05 step 4 read MUST chain BOTH bypass flags (Pitfall 1).
```

<!-- From Plan 04 (Wave 2): -->
```
server.js confirm-booking error-mapping (lines ~1049+):
  - `if (err instanceof ListingNotAvailableError)` branch lands ABOVE ProviderSuspendedError branch
  - Returns 409 with D-11 body: { error: 'listing_not_available', listingStatus, reasonCategory, banner, refundId, refundFailed }
  - ListingNotAvailableError is imported from CANONICAL `./src/payments/refundAndThrow` (W-7)
```

<!-- From src/payments/confirmBooking.js post-Plan-01 state: -->
```
- 3 v1.0 refund call sites already use refundAndThrow(stripe, paymentIntentId, { error: 'provider_suspended', providerUid })
- ProviderSuspendedError moved to refundAndThrow.js; re-exported here for back-compat
- Imports already include: refundAndThrow, ListingNotAvailableError, ProviderSuspendedError (from './refundAndThrow' — W-7 canonical neighbor module)
- Step c (Car refetch + seller re-check) still at lines ~189-207 — Plan 05 modifies this in place
- End-of-Plan-01 `refundAndThrow(` call-count is EXACTLY 3 (B-3); Plan 05 makes it 4.
```

<!-- LISTING_STATUS_POLICY in src/moderation/listingCapabilities.js: -->
```
LISTING_STATUS_POLICY[status].banner returns { titleKey, bodyKey, severity }
for non-active statuses; null for active.
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add listing-status assertion to confirmBooking.js step 4 (D-12 / D-13) — final call-count == 4 per B-3, canonical require path per W-7</name>
  <files>
    ../backend-services/carEx-services/src/payments/confirmBooking.js
  </files>
  <read_first>
    - ../backend-services/carEx-services/src/payments/confirmBooking.js (current post-Plan-01 state — confirm 3 v1.0 sites now use refundAndThrow; the `refundAndThrow(` call-count is EXACTLY 3 at start of Plan 05 (B-3); verify step c is at lines ~189-207; verify imports include LISTING_STATUS_POLICY OR add it; locate the exact insertion point AFTER the seller re-check `if (!sellerUser || ...) await refundAndThrow(...)` block and BEFORE `car.listingStatus = 'booked'`)
    - ../backend-services/carEx-services/src/payments/refundAndThrow.js (Plan 01 — verify ListingNotAvailableError discriminator works on errorBody.error === 'listing_not_available'; CANONICAL source per W-7)
    - ../backend-services/carEx-services/src/moderation/listingCapabilities.js (LISTING_STATUS_POLICY — source for banner field)
    - ../backend-services/carEx-services/src/models/Car.js (Plan 02 — confirm hide hook is live; step 4 needs BOTH bypass flags)
    - ../backend-services/carEx-services/server.js (Plan 04 — verify `ListingNotAvailableError` is imported from canonical `./src/payments/refundAndThrow`; Plan 05 must match this canonical require path for class-identity safety per W-7)
    - .planning/phases/09-backend-read-time-toctou-enforcement/09-PATTERNS.md §5 (Pattern Assignment — exact step 4 modification: 3 changes per D-12/D-13/D-14) + §S-3 (chained bypass flags inside withTransaction — Phase 8 has 8 such call sites)
    - .planning/phases/09-backend-read-time-toctou-enforcement/09-RESEARCH.md §Pattern 4 (verbatim target shape for step 4 + the new assertion block) + §Pitfall 1 (hide hook hides in-flight listing) + §Pitfall 3 (idempotencyKey already handled by helper)
    - .planning/phases/09-backend-read-time-toctou-enforcement/09-CONTEXT.md §D-12 (fold into step 4) + §D-13 (three ordered conditions) + §D-14 (helper contract) + §D-15 (regression risk)
    - .planning/phases/09-backend-read-time-toctou-enforcement/09-01-SUMMARY.md (Plan 01 outputs — verify helper signature AND D-15 Regression Coverage Map per B-4)
    - .planning/phases/09-backend-read-time-toctou-enforcement/09-04-SUMMARY.md (Plan 04 outputs — verify route-error-map branch is live + canonical require path)
  </read_first>
  <action>
PRE-EDIT (W-7 class-identity pre-flight): Confirm confirmBooking.js already imports `ListingNotAvailableError` from `./refundAndThrow` (Plan 01 added this). Verify:
```bash
grep -cE "ListingNotAvailableError.*require\\(['\\\"]\\./refundAndThrow['\\\"]\\)|require\\(['\\\"]\\./refundAndThrow['\\\"]\\).*ListingNotAvailableError|\\{[^}]*ListingNotAvailableError[^}]*\\}\\s*=\\s*require\\(['\\\"]\\./refundAndThrow['\\\"]" ../backend-services/carEx-services/src/payments/confirmBooking.js
```
This grep MUST return >= 1. If it returns 0, Plan 01 did not ship the canonical import or it has drifted — STOP, surface the issue, and fix Plan 01's import line before adding the new assertion. The canonical require path (W-7) ensures the class object thrown by this file matches the class checked by `instanceof` in server.js.

Modify the existing step c (Car refetch + seller re-check + car flip) in `src/payments/confirmBooking.js`. The exact location is around lines 189-207 in the pre-Plan-01 source; verify the post-Plan-01 line range by reading the file before editing (Plan 01 removed the inline refundThenThrow function which may have shifted line numbers).

Three concrete changes per PATTERNS §5:

CHANGE 1 — Extend the Car refetch's bypass flags (D-12, Pitfall 1):
- Before (post-Plan-01 source): `const car = await Car.findById(carId).setOptions({ includeAllUsers: true }).session(session);`
- After: `const car = await Car.findById(carId).setOptions({ includeAllUsers: true, includeAllListingStatuses: true }).session(session);`
- Rationale: WITHOUT `includeAllListingStatuses: true`, the Plan 02 hide hook returns null for any non-active listing, the existing `if (!car) throw new Error('car_not_found')` fires, and the response is 404 instead of 409 with refund (RESEARCH §Pitfall 1 — the canonical failure mode this assertion exists to prevent). PATTERNS §S-3 confirms this exact pattern across 8 Phase 8 listingService call sites.

CHANGE 2 — Add the NEW listing-status assertion block IMMEDIATELY AFTER the existing seller-not-active check (`if (!sellerUser || ...) await refundAndThrow(...)`) and BEFORE `car.listingStatus = 'booked'`:

```
if (car.status !== 'active') {
  const banner = LISTING_STATUS_POLICY[car.status]?.banner ?? null;
  await refundAndThrow(stripe, paymentIntentId, {
    error: 'listing_not_available',
    listingStatus: car.status,
    reasonCategory: car.moderationReason,
    banner,
  });
}
```

Key invariants:
- The assertion is the THIRD ordered check (D-13): (1) seller.moderationStatus.state === 'active' [existing], (2) seller.sellerStatus === 'APPROVED' [existing], (3) car.status === 'active' [NEW]. The first two are the existing seller-not-active check; this is the third. Order matters because the seller checks are cheaper (no extra Car field lookup) and any seller failure already produces a refund — checking listing status third means a non-active listing whose seller is also non-active routes through the provider_suspended refund path (preserving v1.0 semantics for the seller dimension).
- `await refundAndThrow(...)` is critical — without `await`, the throw escapes synchronously and the Stripe refund never runs (v1.0 D-11 refund-first-throw-second invariant). The existing seller-cascade call site is also `await refundAndThrow(...)` — match the pattern exactly.
- `errorBody.error === 'listing_not_available'` triggers the helper's discriminator branch (Plan 01) which throws `ListingNotAvailableError` (NOT `ProviderSuspendedError`). The server.js error-map at the route layer (Plan 04) catches `ListingNotAvailableError` BEFORE `ProviderSuspendedError`, producing the 409 listing_not_available response with refundId + refundFailed.

CHANGE 3 — Add `LISTING_STATUS_POLICY` require at top of file (if not already present from Plan 01's earlier require list):
- `const { LISTING_STATUS_POLICY } = require('../moderation/listingCapabilities');`
- Verify Plan 01's modifications by grepping; if Plan 01 added it for any reason, do not duplicate. If absent, add it adjacent to the existing `require('./refundAndThrow')` line.

DO NOT modify the seller-cascade `if (!sellerUser || ...)` block. DO NOT modify the buyer re-check at step b (uses refundAndThrow with `error: 'buyer_suspended'` — unrelated path). DO NOT touch step a (paymentIntent retrieve) or step d (order create + car flip). DO NOT modify any other refund call site — Plan 01 already converted the 3 v1.0 sites; this plan adds the 4th site only.

After modification, the step c block should have this structure (high-level shape):
```
const car = await Car.findById(carId).setOptions({ includeAllUsers: true, includeAllListingStatuses: true }).session(session);
if (!car) { throw new Error('car_not_found'); }

const sellerUser = await User.findOne(...).setOptions({ includeAllUsers: true }).session(session).lean();
if (!sellerUser || sellerUser.moderationStatus?.state !== 'active' || sellerUser.sellerStatus !== 'APPROVED') {
  await refundAndThrow(stripe, paymentIntentId, { error: 'provider_suspended', providerUid: car.sellerId });
}

// Phase 9 LENF-03 — third TOCTOU dimension: listing status (D-13).
if (car.status !== 'active') {
  const banner = LISTING_STATUS_POLICY[car.status]?.banner ?? null;
  await refundAndThrow(stripe, paymentIntentId, {
    error: 'listing_not_available',
    listingStatus: car.status,
    reasonCategory: car.moderationReason,
    banner,
  });
}

car.listingStatus = 'booked';
// ... rest unchanged ...
```

**B-3 end-of-Plan-05 call-count contract:** After this edit, `grep -cE "await refundAndThrow\\(|^[[:space:]]*refundAndThrow\\(" ../backend-services/carEx-services/src/payments/confirmBooking.js` MUST return EXACTLY 4 (3 v1.0 buyer/provider/seller call sites preserved from Plan 01 + 1 new LENF-03 listing-not-active call site added in this task). The end-of-Plan-01 count was 3 (per Plan 01 B-3 acceptance); this task adds the 4th. Acceptance criterion below enforces the literal `== 4` count.

D-15 regression risk: After this modification, run `npx jest __tests__/enforcement/confirmBooking.transaction.test.js -x` LOCALLY before completing the task. All 7 existing v1.0 cases MUST still pass. The new assertion only affects the listing-status TOCTOU path (which is novel — no v1.0 test exercises it), so existing cases should be untouched. If any existing case fails, the bypass-flag extension is the most likely culprit — verify the bypass chain order doesn't change Mongoose's option-merge behavior.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && npx jest __tests__/enforcement/confirmBooking.transaction.test.js -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "includeAllListingStatuses: true" ../backend-services/carEx-services/src/payments/confirmBooking.js` returns 1 (exactly one — only step c's Car refetch needs both bypass flags; the other lookups in step b read User, not Car)
    - `grep -cE "setOptions\\(\\{ includeAllUsers: true, includeAllListingStatuses: true \\}\\)|setOptions\\(\\{ includeAllListingStatuses: true, includeAllUsers: true \\}\\)" ../backend-services/carEx-services/src/payments/confirmBooking.js` returns 1 (chained pair on step c per D-12, order-agnostic)
    - The literal `if (car.status !== 'active')` appears exactly once in confirmBooking.js: `grep -cE "if \\(car\\.status !== 'active'\\)" ../backend-services/carEx-services/src/payments/confirmBooking.js` returns 1
    - The new assertion uses `refundAndThrow` (NOT throwing a raw error directly): `awk "/if \\(car\\.status !== 'active'\\)/,/^  \\}/" ../backend-services/carEx-services/src/payments/confirmBooking.js | grep -c "refundAndThrow"` returns 1
    - The new assertion's errorBody has `error: 'listing_not_available'`: `awk "/if \\(car\\.status !== 'active'\\)/,/^  \\}/" ../backend-services/carEx-services/src/payments/confirmBooking.js | grep -c "error: 'listing_not_available'"` returns 1
    - LISTING_STATUS_POLICY is required: `grep -c "LISTING_STATUS_POLICY" ../backend-services/carEx-services/src/payments/confirmBooking.js` returns >= 2 (1 require + 1 usage)
    - **B-3 FIX (end-of-Plan-05 call-count == 4):** `grep -cE "await refundAndThrow\\(|^[[:space:]]*refundAndThrow\\(" ../backend-services/carEx-services/src/payments/confirmBooking.js` returns EXACTLY 4 (3 v1.0 + 1 new Phase 9 LENF-03 site). The historical sequence: Plan 01 left this count at 3; Plan 05 adds the 4th. This count contract is the load-bearing acceptance for Plan 05's mutation.
    - **W-7 FIX (canonical require path):** `grep -cE "require\\(['\\\"]\\./refundAndThrow['\\\"]\\).*ListingNotAvailableError|\\{[^}]*ListingNotAvailableError[^}]*\\}\\s*=\\s*require\\(['\\\"]\\./refundAndThrow['\\\"]" ../backend-services/carEx-services/src/payments/confirmBooking.js` returns >= 1 (the import line uses the canonical neighbor module path `./refundAndThrow`, NOT a self-re-export or external path). This matches the canonical require path that Plan 04 uses in server.js (`./src/payments/refundAndThrow`) — both resolve to the same Node module instance, so class identity for `instanceof` is preserved across the throw → catch boundary.
    - The new assertion runs AFTER the seller re-check (D-13 ordering): `awk '/if \\(!sellerUser/,/if \\(car\\.status !== /' ../backend-services/carEx-services/src/payments/confirmBooking.js | wc -l | xargs -I {} test {} -gt 0` succeeds (the awk range from seller-check to listing-check returns at least 1 line, proving the seller check precedes the listing check in source order)
    - The new assertion runs BEFORE the car.listingStatus = 'booked' line: `awk "/if \\(car\\.status !== 'active'\\)/,/car\\.listingStatus = 'booked'/" ../backend-services/carEx-services/src/payments/confirmBooking.js | wc -l | xargs -I {} test {} -gt 0` succeeds
    - Phase 3 regression: `cd ../backend-services/carEx-services && npx jest __tests__/enforcement/confirmBooking.transaction.test.js -x` exits 0 (all 7 v1.0 cases GREEN)
    - File loads under Node: `node -e "require('../backend-services/carEx-services/src/payments/refundAndThrow'); require('../backend-services/carEx-services/src/payments/confirmBooking')"` exits 0 (no circular-require errors)
  </acceptance_criteria>
  <done>Step c extends to a third ordered check on car.status; bypass flags chained on Car refetch; refundAndThrow invoked with listing_not_available errorBody; canonical require path for ListingNotAvailableError matches server.js (W-7); end-of-Plan-05 refundAndThrow( call-count == 4 (B-3); Phase 3 regression suite still 7/7 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill confirmBooking.listingTOCTOU.test.js with 6 GREEN LENF-03 confirm-booking cases (TOCTOU + race + route + regression smoke)</name>
  <files>
    ../backend-services/carEx-services/__tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js
  </files>
  <read_first>
    - ../backend-services/carEx-services/__tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js (Plan 01 scaffold with 6 test.todo entries — fill bodies)
    - ../backend-services/carEx-services/__tests__/enforcement/confirmBooking.transaction.test.js (PATTERNS §12 — DIRECT TEMPLATE; copy verbatim including ServiceOrder loose-schema registration at top, Stripe mock factory, MongoMemoryReplSet harness, seedHappyPath helper)
    - ../backend-services/carEx-services/src/payments/confirmBooking.js (post-Task-1 state — the source under test)
    - ../backend-services/carEx-services/src/payments/refundAndThrow.js (Plan 01 — confirm import path for ListingNotAvailableError class)
    - ../backend-services/carEx-services/src/moderation/listingCapabilities.js (LISTING_STATUS_POLICY for banner-equality assertions)
    - ../backend-services/carEx-services/server.js (Plan 04 — confirm route-error-map branch for ListingNotAvailableError; lift the confirm-booking route signature for the route-level supertest case 6)
    - ../backend-services/carEx-services/__tests__/_helpers/mongoReplSet.js (REUSE — fixture)
    - .planning/phases/09-backend-read-time-toctou-enforcement/09-VALIDATION.md (rows 09-LENF03-B-a..d + race + route + regression — exact case names + expected behaviors)
    - .planning/phases/09-backend-read-time-toctou-enforcement/09-PATTERNS.md §12 (test file structure to copy)
    - .planning/phases/09-backend-read-time-toctou-enforcement/09-RESEARCH.md §Pitfall 9 (concurrent suspend + confirm race — exactly one outcome)
  </read_first>
  <action>
Convert the test.todo entries in `confirmBooking.listingTOCTOU.test.js` into 6 GREEN tests. Copy the test-file structure of `__tests__/enforcement/confirmBooking.transaction.test.js` verbatim (PATTERNS §12 — exact analog). The shape is locked:

- `jest.mock('stripe', () => { ... })` factory at top (Shared Pattern S-7 — exposes `__paymentIntentsRetrieveMock` and `__refundsCreateMock`)
- Register `ServiceOrder` as a loose `{ strict: false }` schema BEFORE `require('../../src/payments/confirmBooking')` — `mongoose.model('ServiceOrder', new mongoose.Schema({}, { strict: false }), 'service_orders');` (PATTERNS §12)
- Imports: `mongoose`, `stripe` (factory), `startReplSet`/`stopReplSet`, `User`, `Car`, `confirmBooking`, `ListingNotAvailableError`, `ProviderSuspendedError`
- `beforeAll` starts replica set; `afterAll` stops it
- `beforeEach` deletes Users and Cars (with bypass flags chained), resets Stripe mocks, mocks `__paymentIntentsRetrieveMock.mockResolvedValue({ status: 'succeeded' })` and `__refundsCreateMock.mockResolvedValue({ id: 're_mock_123' })`
- `seedHappyPath` helper creates buyer + provider + seller + active car ready to confirm

Required 6 GREEN cases:

1. **LENF-03-B-a — "listing flipped to suspended between create-payment-intent and confirm-booking → 409 + refundId + no orders + car NOT flipped to booked"**
   - Call `seedHappyPath()` → returns `{ buyerUid, carId, paymentIntentId, providerUid, sellerUid }`
   - Flip the listing status: `await Car.updateOne({ _id: carId }, { $set: { status: 'suspended', moderationReason: 'spam' } });` (raw updateOne does not fire pre-find hooks, so this is safe)
   - Call `await expect(confirmBooking(stripeFactory(), { paymentIntentId, carId, buyerUid })).rejects.toBeInstanceOf(ListingNotAvailableError);`
   - Catch the error and assert: `err.listingStatus === 'suspended'`, `err.reasonCategory === 'spam'`, `err.refundId === 're_mock_123'`, `err.refundFailed === false`, `err.banner === LISTING_STATUS_POLICY.suspended.banner`
   - Assert no orders created: `expect(await mongoose.model('ServiceOrder').countDocuments({})).toBe(0);`
   - Assert car NOT flipped: `const car = await Car.findById(carId).setOptions({ includeAllUsers: true, includeAllListingStatuses: true }).lean(); expect(car.listingStatus).not.toBe('booked');`

2. **LENF-03-B-b — "Stripe refund called BEFORE the throw (refund-first-throw-second invariant)"**
   - Set up a side-effect counter to track invocation order: `let order = 0; let refundOrder = null; __refundsCreateMock.mockImplementation(() => { refundOrder = ++order; return Promise.resolve({ id: 're_mock_123' }); });`
   - Call confirmBooking, catch the throw, mark `const throwOrder = ++order;` in the catch block
   - Assert `refundOrder !== null && refundOrder < throwOrder`
   - Alternative using `jest.fn.mock.invocationCallOrder`: assert `__refundsCreateMock.mock.invocationCallOrder[0]` is defined AND less than any artificially-tracked throw order

3. **LENF-03-B-c — "Refund failure: stripe.refunds.create throws → response body has refundFailed: true, refundId: null"**
   - `__refundsCreateMock.mockRejectedValueOnce(new Error('stripe down'));`
   - Same seed + flip-to-suspended
   - `await expect(confirmBooking(...)).rejects.toMatchObject({ refundFailed: true, refundId: null, error: 'listing_not_available' });`

4. **LENF-03-B-d — "withTransaction retry does NOT issue a second refund (idempotencyKey was passed)"**
   - Same seed + flip
   - Catch the throw
   - `expect(__refundsCreateMock.mock.calls[0][1]).toEqual({ idempotencyKey: \`refund-${paymentIntentId}\` });` — asserts the helper passed the key from Plan 01
   - Note: This case asserts the KEY was passed; full retry simulation is harder to provoke without monkey-patching Mongoose's withTransaction (which is out of scope for Phase 9). Stripe-side dedup is verified by the manual reconciliation check in 09-VALIDATION.md.

5. **PITFALL 9 RACE — "Concurrent (event-loop-sequenced) admin.suspend + buyer.confirm — buyer ALWAYS loses (refund + abort fires correctly)" (W-5 fix — option (a) rename + ack of JS single-threaded ordering)**
   - Use `Promise.allSettled([adminSuspend(), confirmBooking(...)])` — admin suspend can be a direct `Car.updateOne({ _id: carId }, { $set: { status: 'suspended' } })` (mimics Phase 8 admin action)
   - **Acknowledge the JS event-loop ordering reality in a code comment inside the test:** "JS is single-threaded. With Promise.allSettled, admin.suspend (single Mongo write) typically completes before confirmBooking finishes its multi-step transactional setup, so the admin always wins this race in practice. The buyer ALWAYS loses → confirmBooking rejects with ListingNotAvailableError + refund fires. This case proves the LOSING path produces the correct refund-abort outcome — it does NOT exercise the booking-then-suspend branch (that branch is documented in Pitfall 9 / RESEARCH and remains v1.0 behavior; explicit coverage would require monkey-patching withTransaction ordering, which is out of scope for Phase 9)."
   - Assertion: `expect(results[0].status).toBe('fulfilled');` (admin succeeded) AND `expect(results[1].status).toBe('rejected');` (buyer rejected) AND `expect(results[1].reason).toBeInstanceOf(ListingNotAvailableError);` AND `expect(__refundsCreateMock).toHaveBeenCalledTimes(1);` (exactly one refund issued for the losing buyer)
   - Repeat 5 iterations to flush out timing flakiness (existing test pattern at confirmBooking.transaction.test.js case 6 — D-13 race in Phase 3). Each iteration MUST produce the same outcome (admin wins, buyer loses, refund fires).
   - Note: option (b) — exercising the booking-then-suspend branch — was considered but rejected for Phase 9 scope (would require monkey-patching withTransaction); the v1.0 contract for that branch (both succeed, no refund) is documented in RESEARCH §Pitfall 9 and is preserved without explicit Phase 9 coverage. Phase 11 may revisit if Pitfall 9 coverage gains priority.

6. **ROUTE-LEVEL — "POST /api/payments/confirm-booking on suspended listing returns 409 listing_not_available with D-11 body (full route stack including server.js error-map)"**
   - Build a thin Express app with the confirm-booking route handler (extract from server.js or mount inline including the Plan 04 `ListingNotAvailableError` error-mapping branch)
   - Seed + flip to suspended
   - `supertest(app).post('/api/payments/confirm-booking').send({ paymentIntentId, carId, buyerUid })`
   - `expect(res.status).toBe(409); expect(res.body).toEqual({ error: 'listing_not_available', listingStatus: 'suspended', reasonCategory: 'spam', banner: LISTING_STATUS_POLICY.suspended.banner, refundId: 're_mock_123', refundFailed: false });`

ADDITIONAL REGRESSION SMOKE (NOT counted as one of the 6, but recommended): At the END of the file in an `afterAll` describe block OR a separate `describe.skip` for manual run, document that the existing `__tests__/enforcement/confirmBooking.transaction.test.js` 7-case suite MUST still pass — this is enforced by Task 1's verify command + the plan-level verification block below, not by this test file itself.

All 6 cases MUST pass with `npx jest __tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js -x`.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && npx jest __tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd ../backend-services/carEx-services && npx jest __tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js -x` exits 0
    - Test summary >= 6 passing / 0 failing / 0 todo: `npx jest __tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js --json 2>/dev/null | grep -cE '"numPassingTests":[6-9]|"numPassingTests":[0-9][0-9]'` matches
    - File registers ServiceOrder loose schema before requiring confirmBooking: `grep -c "mongoose.model('ServiceOrder'" ../backend-services/carEx-services/__tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js` returns >= 1
    - File uses the Stripe mock factory: `grep -c "jest.mock\\('stripe'" ../backend-services/carEx-services/__tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js` returns 1
    - File asserts ListingNotAvailableError thrown: `grep -c "ListingNotAvailableError" ../backend-services/carEx-services/__tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js` returns >= 2
    - File asserts idempotencyKey passed: `grep -cE "idempotencyKey.*refund-|expect.*calls\\[0\\]\\[1\\]" ../backend-services/carEx-services/__tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js` returns >= 1
    - File contains the race case (Promise.allSettled): `grep -c "Promise.allSettled" ../backend-services/carEx-services/__tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js` returns >= 1
    - **W-5 FIX (race-test rename + acknowledgement):** The race case (case 5) test name uses the phrase "buyer ALWAYS loses" OR "event-loop-sequenced" OR equivalent making clear that the JS event-loop ordering means admin always wins. Verified by `grep -cE "buyer ALWAYS loses|event-loop|JS is single-threaded|admin always wins" ../backend-services/carEx-services/__tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js` returning >= 1. AND the test asserts the refund-abort branch fires correctly (exactly 1 refund call, ListingNotAvailableError thrown, no orders created). The booking-then-suspend branch is NOT required by this acceptance — it's explicitly out of scope per the W-5 option (a) choice.
    - File asserts banner equality against LISTING_STATUS_POLICY (not hardcoded): `grep -c "LISTING_STATUS_POLICY" ../backend-services/carEx-services/__tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js` returns >= 2
    - File asserts no orders created on TOCTOU abort: `grep -cE "countDocuments\\(\\{\\}\\)|ServiceOrder.*toBe\\(0\\)" ../backend-services/carEx-services/__tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js` returns >= 1
    - File asserts car NOT flipped to 'booked' on TOCTOU abort: `grep -cE "listingStatus.*not.toBe\\('booked'\\)|listingStatus.*toBe\\('active'\\)" ../backend-services/carEx-services/__tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js` returns >= 1
    - File reuses the MongoMemoryReplSet harness: `grep -c "_helpers/mongoReplSet" ../backend-services/carEx-services/__tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js` returns >= 1
    - Phase 3 regression preserved: `cd ../backend-services/carEx-services && npx jest __tests__/enforcement/confirmBooking.transaction.test.js -x` exits 0 (7/7 GREEN — D-15 contract enforced)
    - Plan 02 + Plan 03 + Plan 04 regression preserved: `cd ../backend-services/carEx-services && npx jest __tests__/listing-enforcement/ -x` exits 0 (5/5 scaffolds GREEN — full Phase 9 wave complete)
  </acceptance_criteria>
  <done>6 LENF-03 confirm-booking cases GREEN; Phase 3 regression 7/7 GREEN; race case (W-5) acknowledges JS event-loop ordering — buyer always loses, refund-abort path verified; route-level supertest proves Plan 04 wiring works end-to-end; all 5 Phase 9 scaffolds are now GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (POST /api/payments/confirm-booking) | Buyer presses Confirm after Stripe charge. Listing status may have changed between create-payment-intent (Plan 04 gate) and now. |
| API → MongoDB (Car.findById inside session.withTransaction) | Snapshot isolation in transaction; assertion runs at commit-time. |
| API → Stripe (refunds.create from refundAndThrow helper) | Outbound TLS; fires INSIDE the txn callback but OUTSIDE the Mongo write path. Idempotency-key-protected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | T (Tampering with time-of-check) — listing status flipped mid-checkout | `confirmBooking.js` step c (third ordered check on car.status) | mitigate | New assertion `if (car.status !== 'active')` inside `session.withTransaction()` runs after seller checks. Detects suspend/archive/delete between create-payment-intent and confirm-booking. Test case 1 + 5 (race) cover the canonical scenarios. ASVS V11.1. |
| T-09-05-02 | T+I (Tampering + Information disclosure) — refund order leak: Mongo committed then Stripe refund failed (charged-no-order-no-refund) | `refundAndThrow` invariant + step c await ordering | mitigate | `await refundAndThrow(...)` is INSIDE the txn callback. Helper performs Stripe call FIRST (D-11 / Plan 01 helper body); the THROW that aborts the transaction comes after the refund attempt. Test case 2 asserts `refundOrder < throwOrder` ordering. Stripe outage during refund leaves `refundFailed: true` for ops reconciliation (test case 3). ASVS V11.1, V10.1. |
| T-09-05-03 | R (Repudiation) — double refund on withTransaction auto-retry | `refundAndThrow` idempotencyKey | mitigate | Plan 01 helper passes `idempotencyKey: \`refund-${paymentIntentId}\`` to `stripe.refunds.create`. Test case 4 asserts the key is passed. Stripe-side dedup ensures retries return the SAME refund object on subsequent attempts. RESEARCH §Pitfall 3 + Open Question A3. ASVS V11.1. |
| T-09-05-04 | T (Tampering) — Plan 02 hide hook hides in-flight listing → step c lookup returns null → 404 not 409 with refund | `confirmBooking.js` step c bypass flags | mitigate | Chain BOTH `includeAllUsers: true` AND `includeAllListingStatuses: true` on the Car refetch. Without the second flag, the Plan 02 hide hook silently 404s a suspended listing inside the transaction and the buyer is charged with no refund. Acceptance criterion enforces literal `setOptions({ includeAllUsers: true, includeAllListingStatuses: true })` line. PATTERNS §S-3, RESEARCH §Pitfall 1. ASVS V11.1. |
| T-09-05-05 | T (Tampering) — concurrent admin.suspendListing + buyer confirm-booking can produce inconsistent state | `confirmBooking.js` + Phase 8 admin suspendListing transaction | mitigate | **W-5 fix (option (a)):** In the JS event-loop-sequenced reality (single-threaded runtime + Promise.allSettled in tests), the admin's single-write `Car.updateOne` completes before confirmBooking finishes its transactional setup, so the admin always wins the race in practice. The buyer's confirm-booking always sees the suspended state inside its transaction and routes through `refundAndThrow` — refund issued, ListingNotAvailableError thrown, no orders created (test case 5 verifies). The booking-then-suspend branch (Pitfall 9 — both succeed, no refund) is v1.0-inherited behavior preserved by Mongo snapshot isolation; explicit Phase 9 coverage would require monkey-patching withTransaction ordering and is out of scope. Phase 11 may revisit. ASVS V11.1. |
| T-09-05-06 | V7 (Error handling) — ListingNotAvailableError not mapped at route → 500 fallthrough | server.js error-mapping (wired in Plan 04) + confirmBooking.js step c throw | mitigate | Plan 04 wired `if (err instanceof ListingNotAvailableError)` ABOVE `ProviderSuspendedError` (Pitfall 10). Test case 6 (route-level supertest) exercises the full chain: confirmBooking.js throws → server.js catches → 409 listing_not_available with D-11 body. **W-7 class identity preserved by single CANONICAL export from `src/payments/refundAndThrow.js`:** confirmBooking.js requires `ListingNotAvailableError` from `./refundAndThrow` (neighbor module); server.js requires it from `./src/payments/refundAndThrow` (same canonical path); both resolve to the same Node module instance, so the class object is referentially equal — `instanceof` works at the throw → catch boundary. Acceptance grep enforces the canonical require path in both files. ASVS V7.1. |
| T-09-05-07 | R (Repudiation) — v1.0 Phase 3 contract regression after step c extension | `confirmBooking.js` v1.0 paths (buyer, provider, seller) | mitigate | D-15 regression — Phase 3's `__tests__/enforcement/confirmBooking.transaction.test.js` 7-case suite is re-run as part of Task 1 verify command AND Task 2 acceptance. Any case failing produces a hard stop. The new assertion is BELOW the seller check, so v1.0 seller-cascade paths fire BEFORE listing-status assertion — v1.0 behavior preserved. Acceptance criterion `grep -c "await refundAndThrow(" = 4` enforces the 3+1 call-site count (3 v1.0 + 1 Phase 9) per B-3. ASVS V11.1. |
| T-09-05-08 | D (DoS via additional bypass flag in Mongoose options) | Mongoose option merge behavior | accept | The bypass flag chain `{ includeAllUsers: true, includeAllListingStatuses: true }` is a plain JS object passed to `.setOptions()`. Both flags are read independently by their respective `pre(/^find/)` hooks via `this.getOptions().FLAG_NAME` — no interaction. Mongoose option merge is a shallow Object.assign per documentation; no DoS surface. |
</threat_model>

<verification>
1. `cd ../backend-services/carEx-services && npx jest __tests__/listing-enforcement/confirmBooking.listingTOCTOU.test.js -x` exits 0 (6 GREEN cases)
2. Phase 3 regression: `cd ../backend-services/carEx-services && npx jest __tests__/enforcement/confirmBooking.transaction.test.js -x` exits 0 (7/7 GREEN — D-15 contract enforced)
3. Full Phase 9 wave: `cd ../backend-services/carEx-services && npx jest __tests__/listing-enforcement/ -x` exits 0 (all 5 scaffolds GREEN — Phase 9 feature-complete)
4. Phase 8 listing-moderation regression: `cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/ -x` exits 0
5. Full backend suite gate: `cd ../backend-services/carEx-services && npm test` exits 0 (phase gate before `/gsd-verify-work`)
6. `grep -c "if (car.status !== 'active')" ../backend-services/carEx-services/src/payments/confirmBooking.js` returns 1 (single Phase 9 assertion site, grep-isolatable)
7. **B-3 FINAL CALL-COUNT:** `grep -cE "await refundAndThrow\\(|^[[:space:]]*refundAndThrow\\(" ../backend-services/carEx-services/src/payments/confirmBooking.js` returns EXACTLY 4 (3 v1.0 + 1 new Phase 9 site — final total call-count contract at end of Plan 05)
8. **W-7 canonical require path:** `grep -cE "require\\(['\\\"]\\./refundAndThrow['\\\"]" ../backend-services/carEx-services/src/payments/confirmBooking.js` returns >= 1 (confirmBooking imports from canonical neighbor module, matching server.js's canonical require path).
</verification>

<success_criteria>
- Step c (Car refetch + checks) extends to a third ordered listing-status assertion (D-13)
- Chained bypass flags `includeAllUsers + includeAllListingStatuses` prevent Pitfall 1 silent 404
- refundAndThrow called with listing_not_available errorBody routes through ListingNotAvailableError throw path → server.js 409 (Plan 04 wiring)
- ListingNotAvailableError imported from canonical neighbor module `./refundAndThrow` (W-7) — class identity matches server.js
- End-of-Plan-05 `refundAndThrow(` call-count in confirmBooking.js is EXACTLY 4 (B-3) — 3 v1.0 + 1 Phase 9 LENF-03 site
- All four failure points in confirm-booking (buyer / provider / seller / Phase-9 listing) share the D-14 helper — one refund codepath, one set of regression cases
- Concurrent admin.suspend + buyer.confirm produces refund-abort outcome (W-5 case 5 — buyer always loses in JS event-loop reality; booking-then-suspend branch acknowledged as out-of-scope, preserved as v1.0 contract)
- Phase 3 regression suite 7/7 still GREEN (D-15 contract enforced)
- All 5 Phase 9 scaffolds GREEN at end of plan — Phase 9 feature-complete
</success_criteria>

<output>
After completion, create `.planning/phases/09-backend-read-time-toctou-enforcement/09-05-SUMMARY.md` recording:
- Exact line numbers where the new assertion landed in confirmBooking.js
- **B-3 confirmation:** `refundAndThrow(` call-count in confirmBooking.js is EXACTLY 4 at end of Plan 05 (3 v1.0 + 1 Phase 9 LENF-03 site); historical sequence: Plan 01 left it at 3, Plan 05 added the 4th
- **W-7 confirmation:** confirmBooking.js imports `ListingNotAvailableError` from canonical `./refundAndThrow` (same canonical source path that server.js uses per Plan 04); class identity preserved across throw → catch boundary
- Phase 3 regression result (must be 7/7 GREEN)
- **W-5 race-test disposition:** case 5 renamed per option (a) — JS event-loop ordering means admin always wins; refund-abort branch verified; booking-then-suspend branch acknowledged as v1.0 contract, out of Phase 9 scope. Flakiness rate over 5 iterations of the renamed race case.
- Note for /gsd-verify-work + Phase 11 LIST-SECURITY review: all 3 LENF requirement IDs (LENF-01/02/03) now have GREEN integration coverage; ServiceOrder pause-not-cancel (WR-03) remains deferred and surfaces in 09-VERIFICATION.md as a known gap for future phase; the W-4 null-Car-at-create-payment-intent hardening forward-pointer is recorded in 09-04-SUMMARY.md and 09-VERIFICATION.md for Phase 11 to action; the W-5 booking-then-suspend branch coverage is similarly recorded as a Phase 11 candidate.
- Any deviations from PATTERNS §5 / §12 verbatim with rationale
</output>
</content>
</invoke>