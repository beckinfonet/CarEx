---
phase: 12-notification-domain-in-app-center
plan: 06
type: execute
wave: 4
depends_on: ["12-02", "12-04", "12-05"]
files_modified:
  - "[MOB] src/services/notifications/NotificationService.ts"
  - "[MOB] src/context/NotificationContext.tsx"
  - "[MOB] src/types/navigation.ts"
  - "[MOB] App.tsx"
  - "[MOB] src/screens/NotificationsScreen.tsx"          # placeholder default-export; real screen lands in 12-08 (Wave 5)
  - "[MOB] src/screens/NotificationSettingsScreen.tsx"   # placeholder default-export; real screen lands in 12-10 (Wave 5)
  - "[MOB] src/constants/translations.ts"
  - "[MOB] src/services/notifications/__tests__/NotificationService.test.ts"
  - "[MOB] src/context/__tests__/NotificationContext.test.tsx"
autonomous: true
requirements: [NCEN-01, NCEN-02, NCEN-03, NCEN-04, NPRF-07, NI18N-03]
must_haves:
  truths:
    - "NotificationService wraps the backend endpoints via the shared apiClient (not AuthService)"
    - "NotificationProvider exposes unreadCount, feed, loadMore, markRead, markAllRead, and subscription CRUD"
    - "The context clears its cache when the logged-in user changes"
    - "Both new screens are registered in the navigator and RootStackParamList"
    - "The linking config carries BOTH a CarDetails (watch) deeplink AND a SearchResults (saved-search/new_match) deeplink"
    - "All new RU/EN notification strings exist with parity"
  artifacts:
    - path: "[MOB] src/services/notifications/NotificationService.ts"
      provides: "Thin apiClient verb+path wrappers for the notification API"
      exports: ["NotificationService"]
    - path: "[MOB] src/context/NotificationContext.tsx"
      provides: "NotificationProvider + useNotifications"
      exports: ["NotificationProvider", "useNotifications"]
    - path: "[MOB] src/types/navigation.ts"
      provides: "Notifications + NotificationSettings routes"
      contains: "Notifications: undefined"
    - path: "[MOB] App.tsx"
      provides: "linking config with CarDetails + SearchResults deeplink paths; provider + screen registration"
      contains: "SearchResults"
  key_links:
    - from: "[MOB] NotificationContext.tsx"
      to: "NotificationService"
      via: "fetch unread count + feed"
      pattern: "NotificationService\\."
    - from: "[MOB] App.tsx"
      to: "NotificationProvider"
      via: "wraps NavigationContainer, placed after FavoritesProvider"
      pattern: "NotificationProvider"
    - from: "[MOB] App.tsx linking.config.screens"
      to: "SearchResults route"
      via: "saved-search/new_match deeplink path carrying filter params"
      pattern: "SearchResults"
---

<objective>
Build the mobile notification foundation: `NotificationService.ts` (shared apiClient, sibling of ModerationService — NOT AuthService), `NotificationContext.tsx` (provider + hook, FavoritesContext auto-clear pattern), the two new routes in `navigation.ts`, the provider + screen registration in `App.tsx`, the SearchResults deep-link entry in `App.tsx`'s `linking.config.screens` (so saved-search/new_match notifications can deep-link to filtered results), and all new RU/EN strings in `translations.ts`. Fill the Wave-0 NotificationService + NotificationContext tests.

Purpose: Everything in Wave 4 (screens, controls, badges) consumes this context and service. Centralizing it here gives downstream parallel plans a stable contract and avoids App.tsx/navigation.ts merge conflicts. Adding the SearchResults deeplink path here (12-06 already owns App.tsx) is what lets a new_match notification tap (12-08) land on filtered results — closing Success Criterion #1 / the saved-search half of NCEN-03.
Output: service + context + nav + provider wiring + SearchResults linking entry + translations + 2 filled tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-notification-domain-in-app-center/12-PATTERNS.md
@.planning/phases/12-notification-domain-in-app-center/12-UI-SPEC.md
@src/services/moderation/ModerationService.ts
@src/context/FavoritesContext.tsx
@src/types/navigation.ts

<repo_note>
ALL files live in THIS (mobile) repo. Tests: `npx jest <path>`. Consumes the backend /api/notifications router from 12-04 (mounted in 12-05).
</repo_note>

<interfaces>
NotificationService (PATTERNS §S5 — apiClient verb+path wrappers; DELETE with body uses config.data; isAbortError suppression for signal reads):
  getFeed(cursor?, {signal}?) → { items, nextCursor }   GET /api/notifications?cursor=
  getUnreadCount() → { count }                           GET /api/notifications/unread-count
  markRead(id) → ...                                     PATCH /api/notifications/:id/read
  markAllRead() → ...                                    PATCH /api/notifications/read-all
  createSubscription(body) → ...                         POST /api/notifications/subscriptions
  listSubscriptions() → ...                              GET /api/notifications/subscriptions
  updateSubscription(id, body) → ...                     PATCH /api/notifications/subscriptions/:id
  deleteSubscription(id) → ...                           DELETE /api/notifications/subscriptions/:id
  MOB-01 guardrail (CLAUDE.md): notification/subscription HTTP stays OFF AuthService.ts.

NotificationContext (PATTERNS §S6 — FavoritesContext prevUidRef auto-clear; throw-if-outside hook):
  exposes: unreadCount, feed, loading, refresh, loadMore, markRead, markAllRead + subscription CRUD passthroughs.
  prevUidRef sentinel clears unreadCount/feed on user.localId change (FavoritesContext:55-63).
  useNotifications throws "useNotifications must be used within a NotificationProvider" if used outside.

App.tsx (PATTERNS §[MOB] App.tsx): insert <NotificationProvider> AFTER <FavoritesProvider> (needs useAuth) and it must wrap NavigationContainer (so badges/screens read context). Register <Stack.Screen name="Notifications" .../> and name="NotificationSettings" (:109-132). The screen components are built in Wave 5 (12-08/12-10) — this plan creates minimal placeholder default-export screen files at `src/screens/NotificationsScreen.tsx` and `src/screens/NotificationSettingsScreen.tsx` so App.tsx compiles immediately; 12-08/12-10 overwrite them with the real screens. NOTE these in the SUMMARY as placeholders, not finished work.

App.tsx linking config (App.tsx:79-87 — currently `Home: ''` and `CarDetails: 'listing/:carId'` ONLY): ADD a `SearchResults` entry to `linking.config.screens` so saved-search/new_match notifications can deep-link to filtered results (closes the saved-search half of NCEN-03 + Success Criterion #1). The SearchResults route already takes `{ initialQuery: string; initialFilters?: {...} }` (navigation.ts:25). React Navigation's linking maps URL query params into route params; define the path so it carries the saved-search criteria. Use:
  SearchResults: 'search'
and rely on React Navigation parsing `?initialQuery=...&...` query string into params (the in-app tap path in 12-08 navigates directly with a typed params object — the URL form is the web/deep-link fallback parity). Document the exact deeplink shape the BACKEND produces in the 12-03 emit service: a new_match notification's `data.deeplink` is a `carex://search?...` / `https://www.carexmarket.com/search?...` URL carrying makeId/modelId + price/year filter criteria so it round-trips into `SearchResults` params. App.tsx owns the route registration + linking entry; the backend owns the deeplink string format (12-03); the tap handler that resolves new_match → SearchResults vs watch → CarDetails lives in 12-08.

navigation.ts: add `Notifications: undefined;` and `NotificationSettings: undefined;` to RootStackParamList. (SearchResults already exists with `{ initialQuery, initialFilters? }`.)

translations.ts (UI-SPEC §Copywriting — add ALL listed strings to BOTH RU and EN; KGS som not ruble): notifications, notificationSettings, watch/watching CTAs, save-search CTA/toast/undo, markAllRead, muteAll, category toggles, cadence instant/daily-coming-soon, list headers, empty-state heading/body, feed/subscription error lines, daily-disabled hint, delete-confirm copy.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NotificationService + translations + navigation route types</name>
  <files>[MOB] src/services/notifications/NotificationService.ts, [MOB] src/constants/translations.ts, [MOB] src/types/navigation.ts, [MOB] src/services/notifications/__tests__/NotificationService.test.ts</files>
  <read_first>
    - src/services/moderation/ModerationService.ts (lines 8-21 split contract; 311-462 wrappers; 383-399 DELETE-with-body config.data; 35-39 isAbortError)
    - src/services/http/client.ts (apiClient interceptors — Bearer inject + 401/403 normalize)
    - src/constants/translations.ts (RU block head + EN block head — add new keys in BOTH, parallel)
    - .planning/phases/12-notification-domain-in-app-center/12-UI-SPEC.md §Copywriting Contract (every string)
    - .planning/phases/12-notification-domain-in-app-center/12-PATTERNS.md §S5, §S8
  </read_first>
  <behavior>
    - NotificationService.getFeed passes cursor as a param and propagates an AbortSignal; abort errors re-throw (isAbortError) rather than logging.
    - markRead/markAllRead/createSubscription/deleteSubscription hit the documented paths; DELETE subscription uses config.data if a body is needed.
    - translation-parity test stays green with the new keys (RU/EN set-equality).
  </behavior>
  <action>
    Implement NotificationService.ts as an object export of thin apiClient wrappers per the `<interfaces>` paths (mirror ModerationService.ts exactly: try/catch → console.error + re-throw, isAbortError suppression on getFeed). Add the two routes to RootStackParamList in navigation.ts. Add EVERY UI-SPEC copywriting string to BOTH the RU and EN blocks in translations.ts (parallel keys; KGS som, regional KG/RU tone per audience memory; no Russia-specific regulatory terms). Fill NotificationService.test.ts: mock apiClient, assert each method calls the right verb+path, assert getFeed forwards the signal, assert MOB-01 intent (notification HTTP is in NotificationService, not AuthService).
  </action>
  <verify>
    <automated>npx jest src/services/notifications/__tests__/NotificationService.test.ts __tests__/translation-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass.
    - `grep -c "notification\|subscription\|watch" src/services/AuthService.ts` == 0 (MOB-01 guardrail held).
    - `grep -n "Notifications: undefined\|NotificationSettings: undefined" src/types/navigation.ts` shows both routes.
    - `grep -c "apiClient\." src/services/notifications/NotificationService.ts` >= 6 (wrappers present).
    - New notification keys exist in BOTH RU and EN blocks of translations.ts (parity test green confirms set-equality).
    - `grep -n "сом\|som\|KGS" src/constants/translations.ts` confirms KGS currency wording in a notification string (not ruble).
  </acceptance_criteria>
  <done>NotificationService mirrors ModerationService, both routes typed, all strings added with RU/EN parity, MOB-01 held, tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NotificationContext + App.tsx provider/screen wiring + SearchResults deeplink</name>
  <files>[MOB] src/context/NotificationContext.tsx, [MOB] App.tsx, [MOB] src/screens/NotificationsScreen.tsx, [MOB] src/screens/NotificationSettingsScreen.tsx, [MOB] src/context/__tests__/NotificationContext.test.tsx</files>
  <read_first>
    - src/context/FavoritesContext.tsx (lines 28-63 auto-clear prevUidRef; 34-53 hydrate-on-mount cancellable effect; 93-97 throw-if-outside hook)
    - App.tsx (lines 79-87 linking config — CarDetails: 'listing/:carId'; lines 89-144 provider stack + Stack.Navigator screen registration; FavoritesProvider position)
    - src/types/navigation.ts (SearchResults: { initialQuery, initialFilters? } — the deeplink target params)
    - src/services/notifications/NotificationService.ts (from Task 1 — the data source)
    - .planning/phases/12-notification-domain-in-app-center/12-PATTERNS.md §S6, §[MOB] NotificationContext, §[MOB] App.tsx
  </read_first>
  <behavior>
    - useNotifications throws when used outside a NotificationProvider.
    - unreadCount and feed reset to 0 / [] when user.localId changes (auto-clear), but NOT on first mount (prevUidRef skip-on-mount).
    - refresh fetches unread count + first feed page; loadMore appends the next cursor page; markRead decrements unread.
    - App compiles: both screens are registered and import-resolvable; the linking config exposes BOTH a CarDetails and a SearchResults deeplink path.
  </behavior>
  <action>
    Implement NotificationContext.tsx: provider that `useAuth()`s, fetches unreadCount + feed via NotificationService, exposes the `<interfaces>` surface, and uses the prevUidRef sentinel (FavoritesContext:55-63) to clear cache on uid change. Export `useNotifications` with the throw-if-outside guard. In App.tsx, insert `<NotificationProvider>` AFTER `<FavoritesProvider>` so it can `useAuth` and so it wraps the NavigationContainer (badges/screens consume it). Register `<Stack.Screen name="Notifications" component={NotificationsScreen} />` and `name="NotificationSettings" component={NotificationSettingsScreen}`. ADD a `SearchResults: 'search'` entry to `linking.config.screens` (App.tsx:81-86) alongside the existing `Home` and `CarDetails: 'listing/:carId'` entries — this is the deep-link path saved-search/new_match notifications resolve to (12-08 routes new_match taps here; the backend builds the matching `carex://search?...` deeplink in 12-03). Do NOT remove or alter the existing CarDetails entry. Create minimal placeholder screen files at `src/screens/NotificationsScreen.tsx` and `src/screens/NotificationSettingsScreen.tsx` (trivial default-export components) so App.tsx compiles now — Wave-5 plans 12-08/12-10 overwrite them with the real screens (note this in the SUMMARY so executors know they are placeholders, not finished work). Fill NotificationContext.test.tsx: render the provider, assert badge derives from unreadCount (NCEN-01), assert auto-clear on uid change, assert the hook throws outside the provider.
  </action>
  <verify>
    <automated>npx jest src/context/__tests__/NotificationContext.test.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "App.tsx|NotificationContext" || echo "no TS errors in touched files"</automated>
  </verify>
  <acceptance_criteria>
    - NotificationContext.test.tsx passes (RED→GREEN; was todo in 12-02).
    - Test proves unreadCount drives badge state (NCEN-01) and the cache clears on uid change.
    - `grep -n "prevUidRef" src/context/NotificationContext.tsx` confirms the auto-clear sentinel.
    - `grep -n "NotificationProvider" App.tsx` shows it placed AFTER FavoritesProvider and wrapping NavigationContainer.
    - `grep -c "Stack.Screen name=\"Notifications\"\|Stack.Screen name=\"NotificationSettings\"" App.tsx` == 2.
    - `grep -n "SearchResults:" App.tsx` shows the new SearchResults entry inside linking.config.screens (NCEN-03 saved-search deeplink path), AND `grep -n "CarDetails: 'listing/:carId'" App.tsx` confirms the existing watch deeplink is untouched.
    - `grep -n "src/screens/NotificationsScreen\|src/screens/NotificationSettingsScreen" App.tsx` (imports resolve) and both placeholder files exist.
    - `npx tsc --noEmit` reports no new errors in App.tsx / NotificationContext.tsx (placeholders keep it compilable).
  </acceptance_criteria>
  <done>Provider exposes the full surface, auto-clears on user switch, both screens registered, linking config carries both CarDetails and SearchResults deeplinks, App compiles, context test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile → /api/notifications | apiClient injects the Bearer idToken; the backend (12-04) scopes by token uid |
| notification.data.deeplink → linking config | Deeplink built server-side (12-03); routed only through the declared CarDetails/SearchResults linking paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06-01 | Info disclosure | cross-user cache leak | mitigate | prevUidRef auto-clear wipes unreadCount/feed on user switch (FavoritesContext pattern) — prior user's data never shown |
| T-12-06-02 | Info disclosure | AuthService bloat | mitigate | MOB-01 guardrail: notification HTTP isolated in NotificationService; grep gate == 0 on AuthService |
| T-12-06-03 | Tampering | uid in requests | accept | uid is never sent from the client; the backend derives it from the verified token (handled in 12-04) |
| T-12-06-04 | Tampering | deeplink routing target | mitigate | linking.config.screens whitelists only CarDetails + SearchResults paths; a deeplink cannot resolve to an arbitrary screen |
</threat_model>

<verification>
- NotificationService.test.ts, NotificationContext.test.tsx, translation-parity.test.ts all pass.
- `grep -c "notification\|subscription\|watch" src/services/AuthService.ts` == 0.
- App.tsx compiles with both screens registered, the provider after FavoritesProvider, and the SearchResults deeplink entry added alongside CarDetails.
</verification>

<success_criteria>
- NCEN-01: badge derives from context unread count.
- NCEN-02/04: feed + read-state passthroughs exposed by context.
- NCEN-03: linking config carries the SearchResults (saved-search/new_match) deeplink path AND the CarDetails (watch) path — the routing targets 12-08's tap handler resolves to.
- NPRF-07: context works regardless of push (REST only).
- NI18N-03: all new strings RU/EN parity, KGS som.
</success_criteria>

<output>
After completion, create `.planning/phases/12-notification-domain-in-app-center/12-06-SUMMARY.md`
</output>
