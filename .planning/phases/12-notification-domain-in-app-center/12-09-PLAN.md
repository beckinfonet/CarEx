---
phase: 12-notification-domain-in-app-center
plan: 09
type: execute
wave: 5
depends_on: ["12-06"]
files_modified:
  - "[MOB] src/components/notifications/WatchButton.tsx"
  - "[MOB] src/components/notifications/SaveSearchBar.tsx"
  - "[MOB] src/screens/CarDetailsScreen.tsx"
  - "[MOB] src/screens/SearchResultsV2.tsx"
  - "[MOB] src/components/notifications/__tests__/WatchButton.test.tsx"
autonomous: true
requirements: [NSUB-01, NSUB-02, NSUB-03, NSUB-04, NCEN-06]
must_haves:
  truths:
    - "A labeled bell Watch control on CarDetails creates a watch subscription opting into all 4 events"
    - "The Watch subscription keys on car._id || car.id || carId, never bare car.id"
    - "A sticky 'Notify me about new matches' bar on results creates a Saved Search from current filters at instant cadence"
    - "Saving a search shows a toast with Undo; the filter labels are mapped to canonical criteria with ObjectId ids"
  artifacts:
    - path: "[MOB] src/components/notifications/WatchButton.tsx"
      provides: "Labeled bell pill, distinct from the Favorite heart"
    - path: "[MOB] src/components/notifications/SaveSearchBar.tsx"
      provides: "Sticky save-search bar with criteria mapping + toast-with-Undo"
  key_links:
    - from: "[MOB] WatchButton.tsx"
      to: "NotificationService.createSubscription (watch)"
      via: "one tap, events all 4, key car._id||car.id||carId"
      pattern: "car._id"
    - from: "[MOB] SaveSearchBar.tsx"
      to: "NotificationService.createSubscription (saved_search)"
      via: "RU-label filters mapped to canonical criteria"
      pattern: "createSubscription"
---

<objective>
Build the subscription-creation controls (NCEN-06): the labeled `WatchButton` (Bell pill, distinct from the Favorite heart) on `CarDetailsScreen` below the hero (D-01/D-02/D-03/D-04), and the sticky `SaveSearchBar` ("Notify me about new matches") on `SearchResultsV2` with one-tap create + toast-with-Undo and the load-bearing RU-label→canonical-criteria mapping (D-08/D-09, Pitfall 4). Fill the Wave-0 WatchButton test.

Purpose: The two conversion moments — Watch a car, Save a search — that populate subscriptions the backend matches against.
Output: 2 components + CarDetails/SearchResultsV2 edits + filled WatchButton test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-notification-domain-in-app-center/12-UI-SPEC.md
@.planning/phases/12-notification-domain-in-app-center/12-PATTERNS.md
@.planning/phases/12-notification-domain-in-app-center/12-CONTEXT.md
@src/components/BottomBar.tsx

<repo_note>
ALL files live in THIS (mobile) repo. Tests: `npx jest <path>`. Consumes NotificationService.createSubscription (12-06).
</repo_note>

<interfaces>
WatchButton (UI-SPEC §Color/§Iconography, PATTERNS §[MOB] WatchButton; BottomBar.tsx:25-35 labeled-pill analog + :68-70 active fill):
  - Labeled `Bell` + text: inactive "Отслеживать"/"Watch", active "Вы отслеживаете"/"Watching" (t keys from 12-06). Accent border + accent icon; active fill `rgba(59,130,246,0.1)`. NOT a Heart variant (D-01 sibling discipline). minTapTarget 44px.
  - One tap → `NotificationService.createSubscription({ kind:'watch', carId, events:['price_drop','booked','sold','back_available'], cadence:'instant' })` (D-03).
  - KEY on `car._id || car.id || carId` — NEVER bare `car.id` (D-04, project memory car_id_field_unreliable).
  - Mount on CarDetailsScreen BELOW the hero image, above the spec block (D-02) — separate from the heart (:612) and the CTA stack (:787-898).

SaveSearchBar (UI-SPEC §Color, PATTERNS §[MOB] SaveSearchBar; mounts in SearchResultsV2 header :115-167):
  - Sticky bar, visible only when filters are active. Accent emphasis. CTA "Сообщать о новых вариантах"/"Notify me about new matches".
  - One tap → create Saved Search from current filters at instant cadence (D-09), then toast-with-Undo (success "Будем сообщать…", Undo "Отменить"); no confirm sheet. Toast auto-dismisses ~4s; Undo tappable for its duration; anchored above BottomBar; non-blocking.
  - CRITERIA MAPPING (Pitfall 4 — load-bearing): useHomeListings.activeFilters uses RU label keys ('Цена','Год','Пробег','Топливо','КПП'). Map to canonical before POST:
      { makeId: selectedMake?.id, modelId: selectedModel?.id, priceMin: activeFilters['Цена']?.min, priceMax: activeFilters['Цена']?.max, yearMin: activeFilters['Год']?.min, yearMax: activeFilters['Год']?.max, bodyType: selectedCategory?.name }
    makeId/modelId are ObjectId strings (match Car.makeId/modelId). Then `createSubscription({ kind:'saved_search', criteria, cadence:'instant' })`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WatchButton component + CarDetails mount</name>
  <files>[MOB] src/components/notifications/WatchButton.tsx, [MOB] src/screens/CarDetailsScreen.tsx, [MOB] src/components/notifications/__tests__/WatchButton.test.tsx</files>
  <read_first>
    - src/components/BottomBar.tsx (lines 25-35 labeled pill + 68-70 active fill — the styling analog)
    - src/screens/CarDetailsScreen.tsx (hero/title block, the heart at :612, the CTA stack :787-898 — where WatchButton mounts BELOW hero, separate from both)
    - src/components/notifications/__tests__/WatchButton.test.tsx (the 12-02 scaffold — fill its todos)
    - .planning/phases/12-notification-domain-in-app-center/12-UI-SPEC.md §Color (Watch vs Favorite disambiguation), §Interaction Contract (Watch tap/key)
    - project memory car_id_field_unreliable
  </read_first>
  <behavior>
    - WatchButton renders a labeled Bell pill (text + icon), visually distinct from the Heart favorite (blue vs red, label vs icon-only, pill vs bare icon).
    - One tap creates a watch subscription with events = all four and cadence instant (D-03).
    - The subscription carId resolves via `car._id || car.id || carId` and is NEVER bare `car.id` (D-04) — the test asserts the fallback order, including the case where car._id is undefined but carId is defined.
    - After a successful watch, the button shows the active "Watching" state.
  </behavior>
  <action>
    Implement WatchButton.tsx as its own component (NOT a Heart variant): labeled Bell pill (BottomBar pill styling, accent border/icon, active fill rgba(59,130,246,0.1)), inactive/active labels via t. One tap calls `NotificationService.createSubscription({ kind:'watch', carId: car._id || car.id || carId, events:['price_drop','booked','sold','back_available'], cadence:'instant' })`. Reflect active state. Mount on CarDetailsScreen BELOW the hero image, above the spec block (D-02), clearly separate from the heart (:612) and the CTA stack (:787-898). Fill WatchButton.test.tsx: assert all four events on create, assert the car-id fallback order (including car._id undefined → uses carId), assert it is not a Heart variant.
  </action>
  <verify>
    <automated>npx jest src/components/notifications/__tests__/WatchButton.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - WatchButton.test.tsx passes (RED→GREEN; was todo in 12-02).
    - Test proves carId resolves via `car._id || car.id || carId` and never bare car.id (case: car._id undefined, carId set → uses carId).
    - Test proves the watch create payload has events length 4 + cadence 'instant'.
    - `grep -n "car._id || car.id || carId" src/components/notifications/WatchButton.tsx` matches (D-04).
    - `grep -n "Bell" src/components/notifications/WatchButton.tsx` and absence of `Heart` import confirm sibling discipline (D-01).
    - `grep -n "WatchButton" src/screens/CarDetailsScreen.tsx` confirms the mount.
  </acceptance_criteria>
  <done>Watch is a labeled bell pill below the hero, creates an all-4-events instant watch keyed on the safe car-id fallback, distinct from the heart; test green.</done>
</task>

<task type="auto">
  <name>Task 2: SaveSearchBar + SearchResultsV2 mount (criteria mapping + toast-with-Undo)</name>
  <files>[MOB] src/components/notifications/SaveSearchBar.tsx, [MOB] src/screens/SearchResultsV2.tsx</files>
  <read_first>
    - src/screens/SearchResultsV2.tsx (lines 115-167 header / FilterChipRow region — where the sticky bar mounts; how activeFilters/selectedMake/selectedModel/selectedCategory are available)
    - src/hooks/useHomeListings.ts (lines 95-122 — activeFilters RU label keys: 'Цена','Год','Пробег','Топливо','КПП' — the mapping source)
    - src/components/BottomBar.tsx (accent pill styling)
    - .planning/phases/12-notification-domain-in-app-center/12-UI-SPEC.md §Interaction Contract (Save-search tap/visibility/toast), §Copywriting (CTA/toast/Undo)
    - .planning/phases/12-notification-domain-in-app-center/12-RESEARCH.md §Pitfall 4 (RU-keyed filters), §Pitfall 5 (ObjectId)
  </read_first>
  <action>
    Implement SaveSearchBar.tsx: sticky accent bar visible only when filters are active, CTA text via t. On tap, map the current filters to canonical criteria per the `<interfaces>` mapping (RU label keys → priceMin/priceMax/yearMin/yearMax + makeId/modelId ObjectId strings + bodyType — Pitfall 4/5), then `NotificationService.createSubscription({ kind:'saved_search', criteria, cadence:'instant' })` (D-09, instant default). Show a toast-with-Undo (success + Undo strings via t; auto-dismiss ~4s; Undo deletes the just-created subscription via NotificationService.deleteSubscription; anchored above BottomBar; non-blocking). No confirm sheet (D-09). On create failure, show the subscription-action error line and roll back optimistic UI. Mount SaveSearchBar in the SearchResultsV2 header region (D-08), visible whenever filters are active.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "SaveSearchBar|SearchResultsV2" || echo "no TS errors in SaveSearchBar/SearchResultsV2"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports no errors in SaveSearchBar.tsx / SearchResultsV2.tsx.
    - `grep -n "activeFilters\['Цена'\]\|activeFilters\[\"Цена\"\]" src/components/notifications/SaveSearchBar.tsx` shows the RU-label→canonical mapping (Pitfall 4).
    - `grep -n "makeId" src/components/notifications/SaveSearchBar.tsx` shows ObjectId id passthrough (Pitfall 5).
    - `grep -n "cadence: 'instant'" src/components/notifications/SaveSearchBar.tsx` confirms instant default (NSUB-03/D-09).
    - `grep -n "deleteSubscription\|Undo\|Отменить" src/components/notifications/SaveSearchBar.tsx` confirms the Undo affordance.
    - `grep -n "SaveSearchBar" src/screens/SearchResultsV2.tsx` confirms the mount.
  </acceptance_criteria>
  <done>Sticky save-search bar maps RU filters to canonical ObjectId criteria, creates an instant saved search, and offers a toast-with-Undo; mounted in the results header.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile → POST /api/notifications/subscriptions | Subscription criteria from the client; backend zod-validates + casts ObjectId (12-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09-01 | Tampering | save-search criteria | mitigate | Client maps to canonical fields + ObjectId ids; backend re-validates with zod .strict() and casts (12-04). Name-string injection cannot match (Pitfall 5) |
| T-12-09-02 | Tampering | watch car-id | mitigate | car._id || car.id || carId fallback prevents the bare-car.id undefined class of bug; backend re-reads the Car at emit time |
| T-12-09-03 | Info disclosure | subscription ownership | accept | uid attached server-side from the token (12-04); client never sets uid |
</threat_model>

<verification>
- WatchButton.test.tsx passes; tsc clean on all touched files.
- Watch keys on car._id||car.id||carId; save-search maps RU filters to ObjectId criteria.
- Both controls mounted in their designated regions.
</verification>

<success_criteria>
- NSUB-01: create Saved Search from filter criteria, default instant.
- NSUB-02: Watch a car (all 4 events).
- NSUB-03: instant cadence on create.
- NSUB-04: watch keys on car._id||car.id||carId.
- NCEN-06: Watch control on CarDetails + "Notify me about new matches" on results.
</success_criteria>

<output>
After completion, create `.planning/phases/12-notification-domain-in-app-center/12-09-SUMMARY.md`
</output>
