---
phase: 12-notification-domain-in-app-center
plan: 07
type: execute
wave: 4
depends_on: ["12-05"]
files_modified:
  - "[MOB] src/context/LanguageContext.tsx"
  - "[MOB] src/context/__tests__/LanguageContext.test.tsx"
autonomous: true
requirements: [NI18N-02]
must_haves:
  truths:
    - "The user's language hydrates from AsyncStorage on mount (default RU when absent)"
    - "Changing language persists to AsyncStorage and to PUT /api/users/:uid when a user is logged in"
    - "No backend write fires before auth is ready"
  artifacts:
    - path: "[MOB] src/context/LanguageContext.tsx"
      provides: "Persistent LanguageContext (AsyncStorage + backend write)"
      contains: "AsyncStorage"
  key_links:
    - from: "[MOB] LanguageContext.tsx"
      to: "AsyncStorage + PUT /api/users/:uid"
      via: "setLanguage persists both"
      pattern: "AsyncStorage.setItem"
---

<objective>
Make `LanguageContext` persistent (NI18N-02): hydrate from AsyncStorage on mount (default RU), and on `setLanguage` persist to AsyncStorage AND `PUT /api/users/:uid` (guarded on a logged-in user). Currently in-memory only (`LanguageContext.tsx:15`). This is a single-file, independent change so it runs parallel to 12-06 in Wave 3.

Purpose: Closes the verified persistence gap so the backend can render server-side notifications (Phase 13) in the user's chosen language, and the choice survives restart.
Output: LanguageContext.tsx with persistence + a new test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-notification-domain-in-app-center/12-PATTERNS.md
@.planning/phases/12-notification-domain-in-app-center/12-RESEARCH.md
@src/context/LanguageContext.tsx
@src/context/FavoritesContext.tsx

<repo_note>
ALL files live in THIS (mobile) repo. Tests: `npx jest <path>`. The PUT /api/users/:uid language whitelist is added in 12-05 (backend); this plan consumes it.
</repo_note>

<interfaces>
LanguageContext today (LanguageContext.tsx:14-24): language held in useState only; no AsyncStorage; does NOT useAuth; sits BELOW StripeProvider in App.tsx:98 (Pitfall 6).

Target behavior (PATTERNS §[MOB] LanguageContext, RESEARCH §Pitfall 6):
  - Hydrate from AsyncStorage key (e.g. '@carex_language') on mount; default 'RU' when absent (cancellable effect like FavoritesContext:34-53).
  - setLanguage(lang): set state, AsyncStorage.setItem, and — ONLY when a user is logged in — PUT /api/users/:uid with { language: lang }.
  - Do NOT fire the backend write before auth is ready / when no user (guard on user?.localId). LanguageContext does not currently useAuth and sits below StripeProvider — DO NOT reorder the provider stack (that risks the no-breaking-changes constraint). Thread the user/uid in via a callback or a setUserUid setter the AuthContext calls, OR read uid lazily at setLanguage time from AsyncStorage userData (whichever avoids reordering App.tsx). Document the chosen approach in the SUMMARY.
  - Backend write uses the apiClient (shared client, Bearer injected) — NOT AuthService bloat; a small PUT helper is acceptable, or reuse an existing user-update method already present in AuthService (updateBackendUser) since language is a user-profile field (this is allowed — language is NOT a notification concern, so MOB-01 does not apply here).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist language to AsyncStorage + backend</name>
  <files>[MOB] src/context/LanguageContext.tsx, [MOB] src/context/__tests__/LanguageContext.test.tsx</files>
  <read_first>
    - src/context/LanguageContext.tsx (lines 14-24 — current in-memory implementation)
    - src/context/FavoritesContext.tsx (lines 34-53 — hydrate-on-mount cancellable effect pattern)
    - src/services/AuthService.ts (find updateBackendUser or the existing user-profile PUT method to reuse for the language write — language is a profile field, MOB-01 does not apply)
    - .planning/phases/12-notification-domain-in-app-center/12-RESEARCH.md §Pitfall 6 (persistence regression risks)
    - .planning/phases/12-notification-domain-in-app-center/12-PATTERNS.md §[MOB] LanguageContext.tsx
  </read_first>
  <behavior>
    - On mount with a stored language 'EN', the context initializes to 'EN'.
    - On mount with no stored value, the context defaults to 'RU'.
    - setLanguage('EN') writes AsyncStorage AND calls the backend PUT when a user is logged in.
    - setLanguage when NO user is logged in writes AsyncStorage but does NOT call the backend (no crash before auth).
  </behavior>
  <action>
    Add AsyncStorage hydrate-on-mount (default 'RU' when absent; cancellable effect mirroring FavoritesContext:34-53). In setLanguage, persist to AsyncStorage and, guarded on a logged-in user, persist to the backend via the existing user-profile update path (reuse AuthService.updateBackendUser or an apiClient PUT to /api/users/:uid — language is a profile field, not a notification concern). DO NOT reorder the App.tsx provider stack; obtain the uid via a callback/lazy read of stored userData rather than adding useAuth to a provider that sits below StripeProvider (RESEARCH Pitfall 6). Guard against firing a backend write before auth is ready. Fill LanguageContext.test.tsx with the four behaviors (mock AsyncStorage + the backend write).
  </action>
  <verify>
    <automated>npx jest src/context/__tests__/LanguageContext.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - LanguageContext.test.tsx passes.
    - Test proves hydrate-from-AsyncStorage ('EN' stored → context 'EN'; absent → 'RU').
    - Test proves setLanguage with no logged-in user does NOT call the backend (no crash-before-auth).
    - `grep -n "AsyncStorage" src/context/LanguageContext.tsx` shows both getItem (hydrate) and setItem (persist).
    - `git diff App.tsx` is empty (provider stack NOT reordered).
  </acceptance_criteria>
  <done>Language hydrates from and persists to AsyncStorage, writes to the backend only when logged in, and the provider stack is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile → PUT /api/users/:uid | language value sent to the backend; backend enum-validates (12-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07-01 | Tampering | language value | mitigate | Backend whitelist enum-validates ['RU','EN'] (12-05); client only sends one of two known values |
| T-12-07-02 | DoS | write-before-auth | mitigate | Backend write guarded on logged-in user; no call fires before auth is ready (Pitfall 6) |
</threat_model>

<verification>
- LanguageContext.test.tsx passes.
- AsyncStorage hydrate + persist present; no App.tsx reorder.
- Backend write guarded on logged-in user.
</verification>

<success_criteria>
- NI18N-02: LanguageContext persists language to backend + AsyncStorage (was in-memory only).
</success_criteria>

<output>
After completion, create `.planning/phases/12-notification-domain-in-app-center/12-07-SUMMARY.md`
</output>
