---
phase: 12-notification-domain-in-app-center
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - "[MOB] src/services/notifications/__tests__/NotificationService.test.ts"
  - "[MOB] src/context/__tests__/NotificationContext.test.tsx"
  - "[MOB] src/components/notifications/__tests__/WatchButton.test.tsx"
  - "[MOB] __tests__/translation-parity.test.ts"
autonomous: true
requirements: [NCEN-01, NPRF-07, NSUB-04, NI18N-02, NI18N-03]
must_haves:
  truths:
    - "Mobile Wave-0 test scaffolds exist for NotificationService, NotificationContext, and WatchButton"
    - "The existing mobile translation-parity test still passes and will auto-cover new notification keys"
  artifacts:
    - path: "[MOB] src/context/__tests__/NotificationContext.test.tsx"
      provides: "Scaffold proving badge derives from context unread count"
    - path: "[MOB] src/components/notifications/__tests__/WatchButton.test.tsx"
      provides: "Scaffold locking the car._id || car.id || carId watch key"
  key_links:
    - from: "[MOB] WatchButton.test.tsx"
      to: "car._id || car.id || carId key contract"
      via: "test.todo asserting key fallback order"
      pattern: "car._id"
---

<objective>
Create the mobile-side Wave-0 jest scaffolds (NotificationService, NotificationContext, WatchButton) and confirm the existing RU/EN translation-parity test is green so new notification keys added later are auto-covered by set-equality (NI18N-02/03). Mirrors the Phase 5 Wave-0 scaffold pattern in this repo.

Purpose: Give the Wave-2/Wave-3 mobile plans runnable `<automated>` verify targets that import their not-yet-built modules (compile-time wiring check), and lock the `car._id || car.id || carId` watch-key contract (NSUB-04, project memory `car_id_field_unreliable`) at the scaffold layer before any implementation.
Output: 3 new mobile test scaffolds + a confirmed-green existing parity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-notification-domain-in-app-center/12-VALIDATION.md
@.planning/phases/12-notification-domain-in-app-center/12-PATTERNS.md
@.planning/codebase/TESTING.md
@__tests__/translation-parity.test.ts
@src/services/moderation/ModerationService.ts

<repo_note>
ALL files in this plan live in THIS (mobile) repo. Run tests with `npx jest <path>`.
</repo_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold NotificationService + NotificationContext tests</name>
  <files>[MOB] src/services/notifications/__tests__/NotificationService.test.ts, [MOB] src/context/__tests__/NotificationContext.test.tsx</files>
  <read_first>
    - src/services/moderation/__tests__/ (the analog Wave-0 scaffolds from Phase 5 — mirror their import-the-target shape)
    - src/services/moderation/ModerationService.ts (the service-split contract the NotificationService scaffold will assert)
    - src/context/__tests__/ if any analog exists; otherwise src/context/FavoritesContext.tsx (auto-clear pattern :55-63 the context test will exercise)
    - .planning/phases/12-notification-domain-in-app-center/12-VALIDATION.md §Per-Task Verification Map
  </read_first>
  <action>
    Create two scaffolds. NotificationService.test.ts: `import { NotificationService } from '../NotificationService'` and list test.todo for getFeed (cursor passthrough), getUnreadCount, markRead, markAllRead, createSubscription, deleteSubscription — plus one test.todo asserting MOB-01 guardrail intent (no notification methods on AuthService). NotificationContext.test.tsx (react-test-renderer, jest preset already configured): import `NotificationProvider` + `useNotifications` from '../NotificationContext' and list test.todo for: unreadCount derives badge state (NCEN-01), feed renders (NPRF-07 fallback functional with no-op push), and auto-clear on uid change (mirrors FavoritesContext prevUidRef). Bodies are test.todo; the imports are the load-bearing wiring check. Keep the file collectible even though the target modules do not yet exist (match the Phase 5 scaffold collectibility approach).
  </action>
  <verify>
    <automated>npx jest src/services/notifications/__tests__/NotificationService.test.ts src/context/__tests__/NotificationContext.test.tsx --listTests | grep -c notifications</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --listTests` lists both new scaffold files.
    - `grep -n "from '../NotificationService'" src/services/notifications/__tests__/NotificationService.test.ts` matches.
    - `grep -n "useNotifications\|NotificationProvider" src/context/__tests__/NotificationContext.test.tsx` matches.
    - `grep -c "test.todo" src/services/notifications/__tests__/NotificationService.test.ts` >= 6.
    - A test.todo references the unread-count-derives-badge behavior (NCEN-01).
  </acceptance_criteria>
  <done>Both scaffolds exist, are discovered by jest, import their target modules, and enumerate VALIDATION behaviors as test.todo.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold WatchButton test (lock the watch-key contract) and confirm parity test green</name>
  <files>[MOB] src/components/notifications/__tests__/WatchButton.test.tsx, [MOB] __tests__/translation-parity.test.ts</files>
  <read_first>
    - __tests__/translation-parity.test.ts (existing — set-equality + non-empty-leaf harness; confirm it is green and understand how new keys are auto-covered)
    - src/components/notifications (will not exist yet — the scaffold imports the future WatchButton)
    - project memory car_id_field_unreliable (the `car._id || car.id || carId` rule)
    - .planning/phases/12-notification-domain-in-app-center/12-UI-SPEC.md §Interaction Contract (Watch key row)
  </read_first>
  <action>
    Create WatchButton.test.tsx: `import WatchButton from '../WatchButton'` and list test.todo entries for: renders labeled Bell pill (not a Heart variant — D-01 sibling discipline), one tap creates a watch subscription opting into all 4 events (D-03), and — the load-bearing one — the subscription keys on `car._id || car.id || carId` and NEVER bare `car.id` (NSUB-04, D-04). Add an explicit test.todo string mentioning the fallback order so the contract is grep-visible. Then run the existing `__tests__/translation-parity.test.ts` to confirm it is green NOW (baseline); do NOT add notification keys here (translations.ts edits land in Wave 2 plan 12-06) — this task only confirms the harness will auto-cover them via set-equality. Make a trivial whitespace-only touch ONLY if the test framework requires the file in files_modified; otherwise leave translation-parity.test.ts unmodified and rely on its existing green state (record the baseline run in the SUMMARY).
  </action>
  <verify>
    <automated>npx jest __tests__/translation-parity.test.ts && npx jest src/components/notifications/__tests__/WatchButton.test.tsx --listTests | grep -c WatchButton</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/translation-parity.test.ts` exits 0 (existing parity harness green — baseline confirmed).
    - `npx jest --listTests` lists WatchButton.test.tsx.
    - `grep -n "car._id" src/components/notifications/__tests__/WatchButton.test.tsx` shows the watch-key contract is scaffolded (NSUB-04 lock).
    - `grep -n "import WatchButton from '../WatchButton'" src/components/notifications/__tests__/WatchButton.test.tsx` matches (wiring import present).
    - `grep -c "test.todo" src/components/notifications/__tests__/WatchButton.test.tsx` >= 3.
  </acceptance_criteria>
  <done>WatchButton scaffold exists with the car._id||car.id||carId contract locked as a todo; the existing parity test is confirmed green as the baseline that will auto-cover new keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (test-only plan) | No runtime trust boundary crossed; scaffolds exercise client-side contracts only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | WatchButton car-id key | mitigate | Scaffold locks `car._id || car.id || carId` so implementation cannot silently regress to bare `car.id` (prevents the prod booking-status class of bug) |
| T-12-02-02 | Info disclosure | NotificationService (MOB-01) | mitigate | Scaffold asserts notification HTTP stays off AuthService — prevents auth-client bloat / accidental exposure |
</threat_model>

<verification>
- `npx jest --listTests` discovers all 3 new mobile scaffolds.
- Existing `__tests__/translation-parity.test.ts` is green (baseline).
- Each scaffold imports its target module and locks its contract as test.todo.
</verification>

<success_criteria>
- NCEN-01: context-derived unread badge behavior scaffolded.
- NSUB-04: watch-key fallback contract locked at scaffold layer.
- NPRF-07: in-app-functional-with-stub behavior scaffolded.
- NI18N-02/03: parity harness confirmed green to auto-cover new keys.
</success_criteria>

<output>
After completion, create `.planning/phases/12-notification-domain-in-app-center/12-02-SUMMARY.md`
</output>
