---
phase: 12-notification-domain-in-app-center
plan: 10
type: execute
wave: 5
depends_on: ["12-06"]
files_modified:
  - "[MOB] src/screens/NotificationSettingsScreen.tsx"
  - "[MOB] src/screens/__tests__/NotificationSettingsScreen.test.tsx"
  - "[MOB] src/screens/ProfileScreen.tsx"
autonomous: true
requirements: [NPRF-01, NPRF-02, NPRF-03, NPRF-04, NSUB-03]
must_haves:
  truths:
    - "NotificationSettingsScreen exposes master mute + per-category toggles"
    - "The screen lists saved searches and watched cars, each editable and deletable inline"
    - "Saved-search cadence selector shows Instant selected and Daily disabled (coming soon)"
    - "Quiet-hours + daily-cap controls are present (plumbing only)"
    - "A 'Notification settings' row in ProfileScreen reaches the screen"
  artifacts:
    - path: "[MOB] src/screens/NotificationSettingsScreen.tsx"
      provides: "Settings + subscription management screen"
    - path: "[MOB] src/screens/ProfileScreen.tsx"
      provides: "Notification settings menu row"
  key_links:
    - from: "[MOB] ProfileScreen.tsx"
      to: "NotificationSettings route"
      via: "menu-row navigate"
      pattern: "NotificationSettings"
    - from: "[MOB] NotificationSettingsScreen.tsx"
      to: "useNotifications subscription CRUD"
      via: "list/edit-cadence/delete"
      pattern: "useNotifications"
---

<objective>
Build the `NotificationSettingsScreen` (NPRF-01/02): master mute + per-category toggles, quiet-hours controls (plumbing only), daily-cap control (plumbing only), a "My saved searches" list (cadence-editable: Instant selected / Daily disabled per D-10) and a "My watched cars" list (per-event toggles per D-03), each row inline-editable and deletable with an `Alert.alert` confirm (D-11). Add the "Notification settings" row to `ProfileScreen` (D-12). Overwrites the placeholder NotificationSettingsScreen created in 12-06.

Purpose: The preferences + subscription-management surface. Quiet-hours/daily-cap are persisted but NOT enforced (enforcement is Phase 14); the Daily cadence is shown-but-disabled (delivery is Phase 14).
Output: settings screen + ProfileScreen row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-notification-domain-in-app-center/12-UI-SPEC.md
@.planning/phases/12-notification-domain-in-app-center/12-PATTERNS.md
@.planning/phases/12-notification-domain-in-app-center/12-CONTEXT.md
@src/screens/ProfileScreen.tsx

<repo_note>
ALL files live in THIS (mobile) repo. Tests: `npx jest <path>`. Consumes useNotifications subscription CRUD (12-06). Quiet-hours/daily-cap persist to User.notificationPrefs via the backend (12-01 schema); the cadence Daily option is disabled per D-10.
</repo_note>

<interfaces>
NotificationSettingsScreen (PATTERNS §[MOB] NotificationSettingsScreen; ProfileScreen.tsx:141-175 menu-group analog). Order per D-11:
  1. Master mute (RN Switch) — "Отключить все уведомления"/"Mute all notifications" (NPRF-01). No confirm (reversible).
  2. Per-category toggles (RN Switch) — saved-search "Сохранённые поиски", watch "Отслеживаемые авто" (NPRF-01).
  3. Quiet-hours controls — present, default 22:00–08:00 (D-16); plumbing only, persisted to User.notificationPrefs.quietHours; NOT enforced (Phase 14) (NPRF-03).
  4. Daily-cap control — present, default 3 (D-16); plumbing only, persisted to User.notificationPrefs.dailyCap; NOT enforced (Phase 14) (NPRF-04).
  5. "My saved searches" list ("Мои поиски") — each row shows criteria summary + cadence selector (Instant selected accent; Daily greyed "Раз в день — скоро" with the daily-disabled hint on tap, D-10/NSUB-03) + inline delete (Alert.alert confirm: "Удалить поиск?"…, destructive red).
  6. "My watched cars" list ("Мои авто") — each row shows car + per-event toggles (D-03) + inline delete (Alert.alert confirm: "Перестать отслеживать?"…).
  All strings via useLanguage().t (added in 12-06). Theme tokens per S8. minTapTarget 44px on every control.

ProfileScreen (PATTERNS §[MOB] ProfileScreen; self :141-150 menu-row pattern + admin rows :157-163):
  Add a "Notification settings" row (`Bell` + `ChevronRight`) → navigation.navigate('NotificationSettings'). Label "Настройки уведомлений"/"Notification settings" (distinct from MoreMenu "Notifications" feed entry, D-12).

Persistence: master mute / category toggles / quiet-hours / daily-cap write to User.notificationPrefs via the existing user-profile update path (reuse AuthService.updateBackendUser or apiClient PUT /api/users/:uid — these are user-profile fields; MOB-01 only forbids notification *router* calls on AuthService). Subscription list/edit/delete go through useNotifications (NotificationService).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NotificationSettingsScreen (mute, categories, quiet-hours, daily-cap, lists)</name>
  <files>[MOB] src/screens/NotificationSettingsScreen.tsx, [MOB] src/screens/__tests__/NotificationSettingsScreen.test.tsx</files>
  <read_first>
    - src/screens/ProfileScreen.tsx (lines 141-175 — menu-group View + rows + Switch conventions to mirror)
    - src/context/NotificationContext.tsx (subscription CRUD surface from 12-06)
    - src/services/AuthService.ts (updateBackendUser — for persisting notificationPrefs as profile fields)
    - .planning/phases/12-notification-domain-in-app-center/12-UI-SPEC.md §Copywriting (all settings labels + delete-confirm copy), §Interaction Contract (cadence selector), §Color
    - .planning/phases/12-notification-domain-in-app-center/12-CONTEXT.md D-10/D-11/D-16
  </read_first>
  <behavior>
    - Given a saved-search subscription, the cadence selector renders Instant as selected and Daily as disabled/non-selectable; pressing the Daily option does NOT change the subscription cadence (it surfaces the "coming soon" hint instead) — D-10/NSUB-03.
    - The screen renders a master-mute Switch plus saved-search and watch category Switches (NPRF-01).
    - Inline delete on a subscription row triggers an Alert.alert destructive confirm (NPRF-02).
  </behavior>
  <action>
    Implement NotificationSettingsScreen.tsx (overwriting the 12-06 placeholder) in the D-11 order: master mute Switch, per-category Switches (saved-search/watch), quiet-hours controls (default 22:00–08:00, persisted to notificationPrefs, NOT enforced — plumbing only, NPRF-03), daily-cap control (default 3, persisted, NOT enforced — plumbing only, NPRF-04), "My saved searches" list (cadence selector: Instant selected accent / Daily disabled "Раз в день — скоро" with the daily-disabled hint on tap per D-10; inline delete via Alert.alert with the destructive-confirm copy), "My watched cars" list (per-event toggles per D-03; inline delete via Alert.alert). Persist mute/category/quiet-hours/daily-cap via the user-profile update path (updateBackendUser); subscription list/edit/delete via useNotifications. All strings via t; theme tokens; 44px tap targets. Daily cadence MUST NOT be selectable (D-10) — show the coming-soon hint instead. Fill NotificationSettingsScreen.test.tsx (react-test-renderer): mock useNotifications (seed one saved_search subscription) + the user-profile update path; render the screen; assert the Daily cadence control is rendered disabled/non-interactive and that pressing it does NOT call updateSubscription with cadence 'daily' (Daily-disabled assertion, D-10); assert the master-mute Switch is present (NPRF-01).
  </action>
  <verify>
    <automated>npx jest src/screens/__tests__/NotificationSettingsScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - NotificationSettingsScreen.test.tsx passes (render/behavior — not a typecheck gate).
    - The test proves the Daily cadence option is disabled/non-selectable and pressing it does NOT set cadence 'daily' (D-10 Daily-disabled assertion).
    - `grep -c "Switch" src/screens/NotificationSettingsScreen.tsx` >= 3 (master mute + 2 category toggles, NPRF-01).
    - `grep -n "Alert.alert" src/screens/NotificationSettingsScreen.tsx` shows the delete-confirm flow (NPRF-02, D-11).
    - `grep -n "скоро\|coming soon\|disabled" src/screens/NotificationSettingsScreen.tsx` confirms the Daily cadence is shown-but-disabled (D-10) — Daily is not selectable.
    - `grep -n "quietHours\|22:00\|dailyCap" src/screens/NotificationSettingsScreen.tsx` confirms quiet-hours + daily-cap plumbing (NPRF-03/04).
    - `grep -n "useNotifications" src/screens/NotificationSettingsScreen.tsx` confirms subscription list/edit/delete wiring.
  </acceptance_criteria>
  <done>Settings screen has mute + category toggles + quiet-hours + daily-cap (plumbing) + editable/deletable saved-search and watched-car lists with Daily disabled.</done>
</task>

<task type="auto">
  <name>Task 2: ProfileScreen "Notification settings" row</name>
  <files>[MOB] src/screens/ProfileScreen.tsx</files>
  <read_first>
    - src/screens/ProfileScreen.tsx (lines 141-150 menu-row pattern; 157-163 admin rows — where to add the new row)
    - .planning/phases/12-notification-domain-in-app-center/12-CONTEXT.md D-12 (label disambiguation from the MoreMenu feed entry)
    - .planning/phases/12-notification-domain-in-app-center/12-UI-SPEC.md §Iconography (Bell + ChevronRight)
  </read_first>
  <action>
    Add a "Notification settings" row to ProfileScreen following the existing menu-row pattern (`Bell` icon + title + `ChevronRight`) that calls `navigation.navigate('NotificationSettings')`. Use the t key for "Настройки уведомлений"/"Notification settings" (distinct from the MoreMenu "Notifications" feed label, D-12). Place it among the existing settings rows; do not disturb the admin rows.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep "ProfileScreen" || echo "no TS errors in ProfileScreen"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports no errors in ProfileScreen.tsx.
    - `grep -n "NotificationSettings" src/screens/ProfileScreen.tsx` shows the navigate target (D-12).
    - `grep -n "Bell" src/screens/ProfileScreen.tsx` confirms the icon for the new row.
    - The row uses the t key for "Notification settings" (distinct from the MoreMenu feed label).
  </acceptance_criteria>
  <done>ProfileScreen has a Notification settings row reaching NotificationSettingsScreen, labeled distinctly from the feed entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile → PUT /api/users/:uid (notificationPrefs) | Preference values from the client; backend stores on User |
| mobile → /api/notifications/subscriptions (edit/delete) | Subscription mutations; backend scopes by token uid (12-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10-01 | Elevation / Info disclosure | subscription edit/delete | mitigate | Edit/delete go through the uid-scoped router (12-04); a user can only mutate their own subscriptions (IDOR-safe server-side) |
| T-12-10-02 | DoS / user-trust | mute + daily-cap | mitigate | Master mute + per-category toggles + daily-cap plumbing give the user spam controls (NPRF-04 enforcement Phase 14) |
| T-12-10-03 | Tampering | notificationPrefs values | accept | Plumbing-only in P12; values bounded by UI controls; enforcement (and any server-side bounds) is Phase 14 |
</threat_model>

<verification>
- tsc clean on NotificationSettingsScreen.tsx and ProfileScreen.tsx.
- Mute + 2 category toggles, quiet-hours + daily-cap plumbing, editable/deletable lists, Daily disabled.
- ProfileScreen row navigates to NotificationSettings.
</verification>

<success_criteria>
- NPRF-01: master mute + per-category toggles.
- NPRF-02: list/edit-cadence/delete subscriptions.
- NPRF-03: quiet-hours controls present (plumbing).
- NPRF-04: daily-cap control present (plumbing).
- NSUB-03: cadence selector — Instant selected, Daily disabled.
</success_criteria>

<output>
After completion, create `.planning/phases/12-notification-domain-in-app-center/12-10-SUMMARY.md`
</output>
