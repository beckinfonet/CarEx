---
phase: 12-notification-domain-in-app-center
plan: 08
type: execute
wave: 5
depends_on: ["12-06"]
files_modified:
  - "[MOB] src/screens/NotificationsScreen.tsx"
  - "[MOB] src/screens/__tests__/NotificationsScreen.test.tsx"
  - "[MOB] src/components/notifications/NotificationFeedItem.tsx"
  - "[MOB] src/components/notifications/NotificationBadge.tsx"
  - "[MOB] src/components/MoreMenu.tsx"
  - "[MOB] src/components/BottomBar.tsx"
autonomous: true
requirements: [NCEN-01, NCEN-02, NCEN-03, NCEN-04, NCEN-05, NPRF-07]
must_haves:
  truths:
    - "NotificationsScreen shows a reverse-chron cursor-paginated feed"
    - "Tapping a watch notification marks it read and navigates to CarDetails; tapping a new_match notification navigates to SearchResults with the saved-search filters parsed from data.deeplink"
    - "A 'mark all read' action exists and read vs unread are visually distinct"
    - "A first-time empty state guides the user"
    - "The BottomBar More button shows a red dot and the MoreMenu Notifications item shows a 9+ count"
  artifacts:
    - path: "[MOB] src/screens/NotificationsScreen.tsx"
      provides: "Cursor feed screen with empty state + mark-all-read"
    - path: "[MOB] src/components/notifications/NotificationBadge.tsx"
      provides: "Red dot + 9+ count badge"
  key_links:
    - from: "[MOB] NotificationsScreen.tsx"
      to: "useNotifications feed/loadMore/markRead"
      via: "FlatList + onEndReached + tap"
      pattern: "useNotifications"
    - from: "[MOB] NotificationsScreen.tsx tap handler"
      to: "SearchResults (new_match) / CarDetails (watch) routes"
      via: "parse notification.data.deeplink → typed navigation.navigate"
      pattern: "SearchResults"
    - from: "[MOB] BottomBar.tsx / MoreMenu.tsx"
      to: "NotificationBadge"
      via: "unreadCount from context"
      pattern: "unreadCount"
---

<objective>
Build the in-app notification center feed (NCEN-02/03/04/05): `NotificationsScreen` (reverse-chron cursor feed, pull-to-refresh, tap→markRead+deeplink-routing, empty state, mark-all-read), `NotificationFeedItem` (per-category icon, read/unread styling), `NotificationBadge` (red dot + 9+ count), and the badge surfaces on `BottomBar` (red dot on More) and `MoreMenu` (Notifications item + count) per D-05/D-06/D-07.

Purpose: The user-facing feed and its entry points — the standalone center that works with `fcm.send` as a no-op stub (NPRF-07). Overwrites the placeholder NotificationsScreen created in 12-06.
Output: feed screen + 2 components + MoreMenu/BottomBar edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-notification-domain-in-app-center/12-UI-SPEC.md
@.planning/phases/12-notification-domain-in-app-center/12-PATTERNS.md
@src/components/MoreMenu.tsx
@src/components/BottomBar.tsx
@src/context/NotificationContext.tsx

<repo_note>
ALL files live in THIS (mobile) repo. Tests: `npx jest <path>`. Consumes useNotifications (12-06) and the deep-link config in App.tsx (CarDetails: 'listing/:carId' for watch events AND SearchResults: 'search' for saved-search/new_match events — both added/declared in 12-06).
</repo_note>

<interfaces>
NotificationFeedItem (UI-SPEC §Iconography + §Color): per-category lucide icon — saved-search `Search`, price_drop `TrendingDown`, booked `Lock`, sold `BadgeCheck`/`CircleCheck`, back_available `RotateCcw`. Unread = accent (`COLORS.accent` #3B82F6) left indicator + 600-weight title (`textPrimary`); read = `textSecondary`. minTapTarget 44px. Renders title/body from titleKey/bodyKey via useLanguage().t (notification rows store keys, not text).

NotificationBadge (UI-SPEC §Color, no analog): two modes — red dot (`COLORS.destructive` #EF4444, unread>0) for BottomBar; "9+"-capped count bubble (`COLORS.accent`, radiusPill, ~18-22px) for MoreMenu item. Both read unreadCount from useNotifications.

NotificationsScreen (PATTERNS §[MOB] NotificationsScreen): FlatList reverse-chron, RefreshControl (pull-to-refresh), onEndReached→loadMore, tap→markRead THEN deeplink-routing. Header "Mark all read" action. Empty state: `BellOff` icon + UI-SPEC copy (heading "Пока тихо"/"Nothing yet", body "Сохраните поиск или начните отслеживать авто…"/"Save a search or watch a car…"). Feed-load error: pull-to-refresh error line (non-blocking; feed stays mounted — NPRF-07 never dead-ends). Theme tokens per S8.

Tap → deeplink routing (NCEN-03 — BOTH families; data.deeplink is built server-side in 12-03):
  - Parse `notification.data.deeplink` (a `carex://...` string). A tiny resolver maps it to a typed navigation.navigate call (do NOT eval arbitrary targets — only the two known prefixes):
    • `carex://listing/:carId` (WATCH events: price_drop/booked/sold/back_available) → `navigation.navigate('CarDetails', { carId })`.
    • `carex://search?<criteria>` (NEW_MATCH saved-search hits) → parse the query string (makeId, modelId, priceMin, priceMax, yearMin, yearMax, bodyType + optional initialQuery) and `navigation.navigate('SearchResults', { initialQuery: <query||''>, initialFilters: { makeId, modelId, priceMin, priceMax, yearMin, yearMax, bodyType } })` (only the present keys). This is THE saved-search half of NCEN-03 / Success Criterion #1 — a new_match tap lands on filtered results, not a single car.
  - Fallback: if data carries a bare `carId` (legacy) route to CarDetails; if neither prefix matches, no-op (do not crash). Prefer the prefix check on the deeplink string. The SearchResults route params shape is `{ initialQuery: string; initialFilters?: {...} }` (navigation.ts:25); the linking entry `SearchResults: 'search'` (12-06) provides URL parity for the web/deep-link path.

MoreMenu (PATTERNS §[MOB] MoreMenu): add `{ id, name: t.notifications, route: 'Notifications' }` to MENU_ITEMS (:31-41), a `Bell` case in getIcon (:16-25), overlay the 9+ NotificationBadge on that grid item (:76-89).

BottomBar (PATTERNS §[MOB] BottomBar): overlay the red-dot NotificationBadge on the More TouchableOpacity (:29-35) when unreadCount>0 (consume useNotifications).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NotificationBadge + NotificationFeedItem</name>
  <files>[MOB] src/components/notifications/NotificationBadge.tsx, [MOB] src/components/notifications/NotificationFeedItem.tsx</files>
  <read_first>
    - src/components/BottomBar.tsx (lines 25-35 labeled pill + 68-70 active fill — styling reference)
    - src/components/MoreMenu.tsx (grid item pattern :76-89 — where the count badge overlays)
    - src/constants/theme.ts (COLORS.accent, COLORS.destructive, radiusPill, minTapTarget, iconSize)
    - .planning/phases/12-notification-domain-in-app-center/12-UI-SPEC.md §Color (accent reserved list), §Iconography, §Typography
  </read_first>
  <behavior>
    - NotificationBadge dot mode renders nothing when unreadCount === 0, a red dot when > 0.
    - NotificationBadge count mode caps the display at "9+" for counts >= 10 and shows the exact number for 1-9.
    - NotificationFeedItem renders the correct lucide icon per kind/eventType and applies unread (accent indicator + 600 title) vs read (textSecondary) styling.
  </behavior>
  <action>
    Implement NotificationBadge.tsx with two render modes (dot via `COLORS.destructive`, count bubble via `COLORS.accent`, radiusPill, ~18-22px, "9+" cap). Implement NotificationFeedItem.tsx mapping kind/eventType → lucide icon (icon table in `<interfaces>`), unread/read styling per UI-SPEC, title/body rendered from keys via useLanguage().t, 44px tap target, no hardcoded hex (theme tokens only). Add a test (NotificationBadge.test.tsx) proving the 9+ cap and the dot-hidden-at-zero behavior.
  </action>
  <verify>
    <automated>npx jest src/components/notifications/__tests__/NotificationBadge.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - NotificationBadge.test.tsx passes (9+ cap; dot hidden at 0).
    - `grep -c "#" src/components/notifications/NotificationFeedItem.tsx` — no raw hex literals (theme tokens only; permitted neutrals like #000 excluded).
    - `grep -n "TrendingDown\|Lock\|BadgeCheck\|CircleCheck\|RotateCcw\|Search" src/components/notifications/NotificationFeedItem.tsx` shows the per-category icons.
    - `grep -n "COLORS.destructive" src/components/notifications/NotificationBadge.tsx` (red dot) and `grep -n "COLORS.accent" src/components/notifications/NotificationBadge.tsx` (count) both match.
  </acceptance_criteria>
  <done>Badge renders dot + capped count correctly; feed item renders per-category icon with read/unread styling using theme tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NotificationsScreen feed (cursor, tap→markRead+deeplink-routing, empty state, mark-all-read)</name>
  <files>[MOB] src/screens/NotificationsScreen.tsx, [MOB] src/screens/__tests__/NotificationsScreen.test.tsx</files>
  <read_first>
    - src/context/NotificationContext.tsx (feed, loadMore, markRead, markAllRead, refresh surface from 12-06)
    - App.tsx (lines 79-87 linking config — CarDetails: 'listing/:carId' AND SearchResults: 'search' both declared in 12-06; how deeplinks resolve)
    - src/types/navigation.ts (SearchResults: { initialQuery, initialFilters? } — the new_match navigation target; CarDetails: { carId } — the watch target)
    - a FlatList-based feed screen analog (e.g. AdminModerationScreen if present) for RefreshControl + onEndReached conventions
    - an existing react-test-renderer screen test (e.g. under src/screens/__tests__ or __tests__/) for the navigation-prop mock + fireEvent/press conventions
    - .planning/phases/12-notification-domain-in-app-center/12-UI-SPEC.md §Copywriting (empty state, error lines), §Interaction Contract (feed/read state rows)
    - .planning/phases/12-notification-domain-in-app-center/12-08-PLAN.md §interfaces (the tap→deeplink-routing resolver spec)
  </read_first>
  <behavior>
    - Tapping a WATCH notification (data.deeplink `carex://listing/abc123`) calls markRead(id) THEN navigation.navigate('CarDetails', { carId: 'abc123' }).
    - Tapping a NEW_MATCH notification (data.deeplink `carex://search?makeId=AAA&modelId=BBB&priceMax=15000`) calls markRead(id) THEN navigation.navigate('SearchResults', { initialQuery: '', initialFilters: { makeId: 'AAA', modelId: 'BBB', priceMax: 15000 } }) — lands on FILTERED RESULTS, not a single car (NCEN-03 saved-search half / Success Criterion #1).
    - An empty feed renders the BellOff empty state (NCEN-05).
    - A feed-load error renders the error line but keeps the list mounted (NPRF-07).
  </behavior>
  <action>
    Implement NotificationsScreen.tsx (overwriting the 12-06 placeholder): FlatList of NotificationFeedItem, reverse-chron from context feed, RefreshControl→refresh, onEndReached→loadMore (cursor pagination NCEN-02), tap→markRead(id) THEN deeplink-routing per the §interfaces resolver: parse `notification.data.deeplink`; `carex://listing/:carId` → navigate('CarDetails', { carId }); `carex://search?<criteria>` → parse the query string (makeId/modelId/priceMin/priceMax/yearMin/yearMax/bodyType + optional initialQuery, numeric fields coerced to Number) → navigate('SearchResults', { initialQuery: query||'', initialFilters: {<present keys>} }); unknown prefix → no-op (do not crash). Header "Mark all read" action (NCEN-04). Empty state with `BellOff` + UI-SPEC copy when feed is empty (NCEN-05). Feed-load error → non-blocking pull-to-refresh error line; feed stays mounted (NPRF-07). All strings via useLanguage().t; theme tokens per S8. Fill NotificationsScreen.test.tsx (react-test-renderer): mock useNotifications (seed one watch + one new_match row) and the navigation prop; assert the watch tap → markRead + navigate('CarDetails', {carId}); assert the new_match tap → markRead + navigate('SearchResults', { initialFilters: { makeId, modelId, priceMax } }); assert the empty state renders BellOff when feed is [].
  </action>
  <verify>
    <automated>npx jest src/screens/__tests__/NotificationsScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - NotificationsScreen.test.tsx passes (render/behavior — not a typecheck gate).
    - The test proves a NEW_MATCH tap calls navigate('SearchResults', ...) with initialFilters parsed from the deeplink (NCEN-03 saved-search half / Success Criterion #1: a new_match tap lands on filtered results).
    - The test proves a WATCH tap calls navigate('CarDetails', { carId }) (watch routing preserved).
    - The test proves markRead is called before navigate on tap (NCEN-04).
    - The test proves the empty feed renders the BellOff empty state (NCEN-05).
    - `grep -n "onEndReached\|loadMore" src/screens/NotificationsScreen.tsx` shows cursor pagination wiring (NCEN-02).
    - `grep -n "SearchResults" src/screens/NotificationsScreen.tsx` and `grep -n "CarDetails" src/screens/NotificationsScreen.tsx` confirm both routing targets.
    - `grep -n "RefreshControl" src/screens/NotificationsScreen.tsx` shows pull-to-refresh.
  </acceptance_criteria>
  <done>Feed paginates; a watch tap marks read + routes to CarDetails and a new_match tap marks read + routes to SearchResults with parsed filters (render test green); mark-all-read works, empty state guides first-timers, errors never dead-end.</done>
</task>

<task type="auto">
  <name>Task 3: Wire badges into MoreMenu + BottomBar</name>
  <files>[MOB] src/components/MoreMenu.tsx, [MOB] src/components/BottomBar.tsx</files>
  <read_first>
    - src/components/MoreMenu.tsx (lines 16-25 getIcon; 31-41 MENU_ITEMS; 76-89 grid render)
    - src/components/BottomBar.tsx (lines 29-35 the More TouchableOpacity)
    - src/components/notifications/NotificationBadge.tsx (from Task 1)
    - src/context/NotificationContext.tsx (unreadCount from useNotifications)
    - .planning/phases/12-notification-domain-in-app-center/12-CONTEXT.md D-05/D-06/D-07
  </read_first>
  <action>
    MoreMenu: add a Notifications grid item (`{ id, name: t.notifications, route: 'Notifications' }`) with a `Bell` case in getIcon, navigating to NotificationsScreen; overlay the count-mode NotificationBadge (9+ cap) on that item using unreadCount from useNotifications (D-06/D-07). BottomBar: overlay the dot-mode NotificationBadge on the More button when unreadCount>0 (D-07), consuming useNotifications. Do not restructure navigation — reuse the existing BottomBar/MoreMenu surfaces (D-05; a global tab navigator is explicitly deferred).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "MoreMenu|BottomBar" || echo "no TS errors in MoreMenu/BottomBar"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports no errors in MoreMenu.tsx / BottomBar.tsx.
    - `grep -n "route: 'Notifications'" src/components/MoreMenu.tsx` shows the new grid item (D-06).
    - `grep -n "NotificationBadge" src/components/MoreMenu.tsx` (count) and `src/components/BottomBar.tsx` (dot) both match.
    - `grep -n "useNotifications" src/components/BottomBar.tsx` confirms the badge derives from context unread count (D-07).
    - No new bottom-tab navigator introduced (`git diff App.tsx` empty for this plan).
  </acceptance_criteria>
  <done>MoreMenu has the Notifications entry with a 9+ count; BottomBar More shows a red dot when unread>0; both derive from context; no nav restructure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notification.data.deeplink → navigation | Deeplink built server-side (12-03); resolved by a prefix whitelist (carex://listing/, carex://search) to typed CarDetails/SearchResults navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08-01 | Tampering | deeplink routing | mitigate | Deeplink is server-built (12-03) and resolved only via a two-prefix whitelist (carex://listing/ → CarDetails, carex://search → SearchResults); unknown prefixes no-op; the screen never evals arbitrary navigation targets |
| T-12-08-02 | Info disclosure | feed contents | accept | Feed is uid-scoped by the backend (12-04); the screen renders only what the token-scoped API returns |
| T-12-08-03 | DoS | unbounded feed | mitigate | Cursor pagination (limit-clamped server-side); loadMore appends incrementally |
</threat_model>

<verification>
- NotificationBadge.test.tsx passes; tsc clean on all touched files.
- NotificationsScreen.test.tsx passes: watch tap → CarDetails, new_match tap → SearchResults(filters), markRead-before-navigate, empty state.
- MoreMenu count + BottomBar dot both derive from context unreadCount.
</verification>

<success_criteria>
- NCEN-01: red dot (BottomBar) + 9+ count (MoreMenu) from context.
- NCEN-02: reverse-chron cursor feed.
- NCEN-03: a watch tap deep-links to CarDetails AND a new_match (saved-search) tap deep-links to SearchResults with the saved-search filters (both routing families proven by the render test).
- NCEN-04: mark read on open + mark-all-read + read/unread distinct.
- NCEN-05: onboarding empty state.
- NPRF-07: feed fully functional with no-op push; errors never dead-end.
</success_criteria>

<output>
After completion, create `.planning/phases/12-notification-domain-in-app-center/12-08-SUMMARY.md`
</output>
