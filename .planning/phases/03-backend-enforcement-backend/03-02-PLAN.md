---
phase: 03-backend-enforcement-backend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ../backend-services/carEx-services/src/security/attachAuthIfPresent.js
  - ../backend-services/carEx-services/src/security/requireNotSuspended.js
autonomous: true
requirements: [ENF-01, ENF-04]
must_haves:
  truths:
    - "attachAuthIfPresent sets req.auth = { uid, email, claims } when a valid Bearer token is present, calls next() with no 401 when no Authorization header is present, and returns 401 when Bearer is present but invalid/malformed"
    - "requireNotSuspended(capability) returns 404 user_not_found when caller uid cannot be resolved to a User"
    - "requireNotSuspended returns 403 { error: 'account_suspended', status, reasonCategory, note } when caller.moderationStatus.state is blocked_with_review or permanently_banned"
    - "requireNotSuspended with feature_limited caller returns 403 only when requiredCapability is in user.moderationStatus.restrictedFeatures, else next()"
    - "requireNotSuspended falls back to req.body.sellerId || req.body.buyerUid || req.params.uid only when req.auth is absent, and logs console.warn on the fallback branch"
    - "requireNotSuspended issues User.findOne with .setOptions({ includeAllUsers: true }) so the pre-hook from Plan 03-01 does not hide the caller from their own self-lookup"
  artifacts:
    - path: "../backend-services/carEx-services/src/security/attachAuthIfPresent.js"
      provides: "Soft fork of verifyIdToken — no header = next() instead of 401"
      exports: ["attachAuthIfPresent"]
    - path: "../backend-services/carEx-services/src/security/requireNotSuspended.js"
      provides: "Factory middleware that gates routes by caller moderationStatus + capability"
      exports: ["requireNotSuspended"]
  key_links:
    - from: "requireNotSuspended"
      to: "User.findOne({firebaseUid: callerUid}).setOptions({includeAllUsers: true})"
      via: "self-lookup bypass of Plan 03-01 pre(/^find/) hook"
      pattern: "includeAllUsers: true"
    - from: "requireNotSuspended feature_limited branch"
      to: "user.moderationStatus.restrictedFeatures.includes(requiredCapability)"
      via: "capability check (D-01 + Phase 1 D-26 denormalized restrictedFeatures)"
      pattern: "restrictedFeatures.*includes"
    - from: "attachAuthIfPresent"
      to: "ensureInitialized() from firebaseAdmin"
      via: "admin.auth().verifyIdToken(token, true)"
      pattern: "verifyIdToken\\(.*true\\)"
---

<objective>
Create two middleware modules: `attachAuthIfPresent.js` (a soft fork of Phase 1's strict `verifyIdToken.js` that calls `next()` on missing Bearer instead of 401) and `requireNotSuspended.js` (a factory middleware that blocks non-active callers with 403 `account_suspended` and selectively blocks `feature_limited` callers by capability token).

Purpose: Implements ENF-01 (write-route gating by moderation state) and ENF-04 (capability-map-driven selective gating for `feature_limited` severity). Provides the per-request guard that Plan 03-03 will compose onto the five ROADMAP-named routes.
Output: Two new security modules under `../backend-services/carEx-services/src/security/`.

D-03 dual-accept mode: `attachAuthIfPresent` exists specifically because the mobile app has not yet wired Bearer for these five routes (Phase 4 work). When no `Authorization` header is present, `requireNotSuspended` falls back to `req.body.sellerId || req.body.buyerUid || req.params.uid` with a deprecation warning. Phase 6 QUAL-03 removes both the fork and the fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-backend-enforcement-backend/03-CONTEXT.md
@.planning/phases/03-backend-enforcement-backend/03-PATTERNS.md
@.planning/phases/01-schema-security-baseline-backend/01-CONTEXT.md

<interfaces>
From ../backend-services/carEx-services/src/security/verifyIdToken.js (existing — the source of the fork):
```
const { ensureInitialized } = require('./firebaseAdmin');
async function verifyIdToken(req, res, next) {
  const header = req.header('authorization') || req.header('Authorization') || '';
  const match = header.match(/^Bearer (.+)$/);
  if (!match) {
    return res.status(401).json({ error: 'unauthenticated', message: 'Missing or invalid idToken' });
  }
  try {
    const admin = ensureInitialized();
    const decoded = await admin.auth().verifyIdToken(match[1], true);
    req.auth = { uid: decoded.uid, email: decoded.email, claims: decoded };
    return next();
  } catch (err) {
    return res.status(401).json({ error: 'unauthenticated', message: 'Missing or invalid idToken' });
  }
}
module.exports = { verifyIdToken };
```

From ../backend-services/carEx-services/src/security/requireAdmin.js (existing — shape analog for requireNotSuspended):
```
const AdminUser = require('../models/AdminUser');
async function requireAdmin(req, res, next) {
  if (!req.auth || !req.auth.email) {
    return res.status(403).json({ error: 'unauthorized', message: 'Admin access required' });
  }
  const admin = await AdminUser.findOne({ email: req.auth.email.toLowerCase() }).lean();
  if (!admin) {
    return res.status(403).json({ error: 'unauthorized', message: 'Admin access required' });
  }
  req.admin = { uid: req.auth.uid, role: admin.role, email: admin.email };
  return next();
}
module.exports = { requireAdmin };
```

From ../backend-services/carEx-services/src/models/User.js (Phase 1):
- `moderationStatus` subdoc: `{ state, severity, reasonCategory, note, setByAdminUid, setAt, restrictedFeatures[], lastActionId }`
- state enum: `'active' | 'feature_limited' | 'blocked_with_review' | 'permanently_banned'`
- Note: `restrictedFeatures` is denormalized from STATUS_POLICY at suspend time (Phase 2 D-18). The middleware reads it directly — do NOT re-resolve via STATUS_POLICY.
</interfaces>

<capability_tokens>
Exact capability strings (Phase 1 D-26 — must be identity-equal to the values Plan 03-03 passes at mount):
- `create_listing` — POST /api/cars
- `create_order` — POST /api/payments/create-payment-intent, POST /api/payments/confirm-booking
- `update_profile` — PUT /api/brokers/:uid, PUT /api/logistics/:uid
</capability_tokens>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create attachAuthIfPresent middleware (soft fork of verifyIdToken)</name>
  <files>../backend-services/carEx-services/src/security/attachAuthIfPresent.js</files>
  <read_first>
    - ../backend-services/carEx-services/src/security/verifyIdToken.js (the source — fork starts here)
    - ../backend-services/carEx-services/src/security/firebaseAdmin.js (exports ensureInitialized())
    - .planning/phases/03-backend-enforcement-backend/03-PATTERNS.md section "src/security/attachAuthIfPresent.js" (the three diffs from verifyIdToken)
    - .planning/phases/03-backend-enforcement-backend/03-CONTEXT.md D-03, D-04
  </read_first>
  <behavior>
    - No Authorization header at all → next() is called; req.auth is undefined; no response written.
    - Valid Bearer token → admin.auth().verifyIdToken(token, true) succeeds → req.auth = { uid, email, claims: decoded } → next().
    - Bearer header present but token is malformed/expired/revoked → verifyIdToken throws → respond 401 { error: 'unauthenticated', message: 'Missing or invalid idToken' }.
    - Header present but not matching /^Bearer (.+)$/ → next() (treat as no-Bearer — do NOT 401). Per PATTERNS.md "Phase 3 fork" change #1: the !match branch changes from 401 to next().
    - Per PATTERNS.md "Phase 3 fork" change #2: when Bearer IS matched but verifyIdToken(token, true) throws, keep the 401.
    - TODO comment present citing QUAL-03 / Phase 6 removal trigger.
  </behavior>
  <action>
    Create `../backend-services/carEx-services/src/security/attachAuthIfPresent.js` by copying verifyIdToken.js and applying these three diffs (per 03-PATTERNS.md "Phase 3 fork"):

    1. Rename the exported function from verifyIdToken to attachAuthIfPresent. Export shape: `module.exports = { attachAuthIfPresent };` (named export, same idiom as verifyIdToken.js). Grep-visible so the fork is obvious.

    2. No-header / non-Bearer → next() (the load-bearing change):
       Replace:
       ```
       if (!match) {
         return res.status(401).json({ error: 'unauthenticated', message: 'Missing or invalid idToken' });
       }
       ```
       With:
       ```
       if (!match) {
         return next();  // dual-accept: no Bearer -> requireNotSuspended falls back to body/params uid
       }
       ```

    3. Keep 401 on Bearer-present-but-bad (no change — the existing catch block stays as-is with the same JSON shape).

    4. Add TODO comment at the top of the file (after the `const { ensureInitialized } = ...` import, before the function definition):
       ```
       // TODO(QUAL-03, Phase 6): remove this module; revert all five gated routes to strict
       // verifyIdToken once mobile wires Bearer on every call. See 03-CONTEXT.md D-03/D-04.
       ```

    Do NOT:
    - Modify verifyIdToken.js (admin routes depend on its strict behavior per D-04).
    - Add any logging on the no-header branch (requireNotSuspended emits the deprecation warning — single log source).
    - Change the 401 response shape when Bearer-present-but-bad (stays `{ error: 'unauthenticated', message: 'Missing or invalid idToken' }`).
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services &amp;&amp; grep -n "attachAuthIfPresent" src/security/attachAuthIfPresent.js &amp;&amp; grep -c "return next()" src/security/attachAuthIfPresent.js &amp;&amp; grep -n "TODO(QUAL-03" src/security/attachAuthIfPresent.js &amp;&amp; grep -c "status(401)" src/security/attachAuthIfPresent.js &amp;&amp; node -e "const { attachAuthIfPresent } = require('./src/security/attachAuthIfPresent'); console.log(typeof attachAuthIfPresent === 'function' ? 'ok' : 'fail');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `../backend-services/carEx-services/src/security/attachAuthIfPresent.js`.
    - `grep -n "module.exports = { attachAuthIfPresent }" src/security/attachAuthIfPresent.js` returns 1 match.
    - `grep -c "return next()" src/security/attachAuthIfPresent.js` returns at least 2 (one on no-match, one on Bearer-success path).
    - `grep -c "status(401)" src/security/attachAuthIfPresent.js` returns exactly 1 (only in the catch block — not on !match).
    - `grep -n "TODO(QUAL-03" src/security/attachAuthIfPresent.js` returns 1 match.
    - `grep -n "verifyIdToken(match\[1\], true)" src/security/attachAuthIfPresent.js` returns 1 match (the Firebase SDK call preserved from the source).
    - `node -e "const { attachAuthIfPresent } = require('./src/security/attachAuthIfPresent'); console.log(typeof attachAuthIfPresent === 'function' ? 'ok' : 'fail');"` prints `ok`.
    - `../backend-services/carEx-services/src/security/verifyIdToken.js` was NOT modified (diff empty against HEAD for that file).
  </acceptance_criteria>
  <done>attachAuthIfPresent.js exists, exports the named function, returns next() on !match, returns 401 on Bearer-present-but-bad, carries the TODO(QUAL-03) comment. verifyIdToken.js is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create requireNotSuspended factory middleware</name>
  <files>../backend-services/carEx-services/src/security/requireNotSuspended.js</files>
  <read_first>
    - ../backend-services/carEx-services/src/security/requireAdmin.js (shape analog — factory-less, but same req/res/next + .lean() + single User lookup discipline)
    - ../backend-services/carEx-services/src/models/User.js (firebaseUid index + moderationStatus shape)
    - ../backend-services/carEx-services/src/moderation/capabilities.js (confirm the capability strings used by Plan 03-03 exist in STATUS_POLICY.feature_limited.capabilities.blocked)
    - .planning/phases/03-backend-enforcement-backend/03-PATTERNS.md section "src/security/requireNotSuspended.js"
    - .planning/phases/03-backend-enforcement-backend/03-CONTEXT.md D-01, D-03, D-04, D-15
    - .planning/phases/01-schema-security-baseline-backend/01-CONTEXT.md D-11 (moderationStatus shape), D-12 (restrictedFeatures denormalization)
  </read_first>
  <behavior>
    - Exported as a factory: `requireNotSuspended(requiredCapability)` returns an Express middleware `async (req, res, next) => ...`.
    - Resolves caller uid as: `req.auth?.uid ?? req.body?.sellerId ?? req.body?.buyerUid ?? req.params?.uid`.
    - When req.auth is absent AND the fallback branch supplies a uid → `console.warn('[requireNotSuspended] deprecated body-uid fallback used', { route: req.originalUrl, uid: callerUid })` is emitted exactly once per request.
    - If no uid resolves (all four sources empty) → responds 404 `{ error: 'user_not_found' }`.
    - Looks up User via `User.findOne({ firebaseUid: callerUid }).select('moderationStatus firebaseUid').setOptions({ includeAllUsers: true }).lean()`. The `includeAllUsers: true` is MANDATORY — without it, Plan 03-01's pre-hook would hide the caller's own User document from this lookup when the caller is suspended (self-hide = false negative = bypass).
    - User not found → 404 `{ error: 'user_not_found' }`.
    - User found, `moderationStatus?.state === 'active'` (or moderationStatus missing entirely, default active) → `req.callerUser = user;` then next().
    - User found, state === 'feature_limited':
      - If requiredCapability is truthy AND `user.moderationStatus.restrictedFeatures.includes(requiredCapability)` → 403 `{ error: 'account_suspended', status: 'feature_limited', reasonCategory, note }`.
      - Otherwise → `req.callerUser = user;` then next().
    - User found, state === 'blocked_with_review' OR 'permanently_banned' → 403 `{ error: 'account_suspended', status: <state>, reasonCategory, note }` regardless of requiredCapability.
    - Response shape exactly matches D-15: `{ error: 'account_suspended', status, reasonCategory, note }` — no extra fields (mobile depends on this for banner copy).
  </behavior>
  <action>
    Create `../backend-services/carEx-services/src/security/requireNotSuspended.js` with the following structure (shape copied from requireAdmin.js; factory wrapper added per D-01):

    1. Imports:
       ```
       const User = require('../models/User');
       ```

    2. Factory export:
       ```
       function requireNotSuspended(requiredCapability) {
         return async function requireNotSuspendedMiddleware(req, res, next) {
           try {
             // Dual-accept uid resolution (D-03) — strict Bearer first, body/params fallback only if req.auth absent.
             let callerUid = req.auth?.uid;
             if (!callerUid) {
               callerUid = req.body?.sellerId || req.body?.buyerUid || req.params?.uid;
               if (callerUid) {
                 // TODO(QUAL-03, Phase 6): remove fallback + this warning once mobile wires Bearer.
                 // eslint-disable-next-line no-console
                 console.warn('[requireNotSuspended] deprecated body-uid fallback used', {
                   route: req.originalUrl,
                   uid: callerUid,
                 });
               }
             }
             if (!callerUid) {
               return res.status(404).json({ error: 'user_not_found' });
             }

             // includeAllUsers bypass is REQUIRED — caller's own User doc would be hidden by
             // the Plan 03-01 pre(/^find/) hook when they are suspended. Without this the
             // middleware would 404 instead of 403 on a suspended caller (false-negative bypass).
             const user = await User.findOne({ firebaseUid: callerUid })
               .select('moderationStatus firebaseUid')
               .setOptions({ includeAllUsers: true })
               .lean();

             if (!user) {
               return res.status(404).json({ error: 'user_not_found' });
             }

             const status = user.moderationStatus || { state: 'active' };
             const { state, reasonCategory = null, note = null, restrictedFeatures = [] } = status;

             if (state === 'active') {
               req.callerUser = user;
               return next();
             }

             if (state === 'feature_limited') {
               if (requiredCapability && restrictedFeatures.includes(requiredCapability)) {
                 return res.status(403).json({
                   error: 'account_suspended',
                   status,
                   reasonCategory,
                   note,
                 });
               }
               // feature_limited but this capability is allowed -> continue
               req.callerUser = user;
               return next();
             }

             // blocked_with_review | permanently_banned -> always 403
             return res.status(403).json({
               error: 'account_suspended',
               status: state,
               reasonCategory,
               note,
             });
           } catch (err) {
             return next(err);
           }
         };
       }

       module.exports = { requireNotSuspended };
       ```

    Note on the 403 body: the `status` field in the response must be the string state value (e.g., 'blocked_with_review'), NOT the whole moderationStatus subdoc. Fix the snippet's `status` on feature_limited branch to send the string `'feature_limited'` explicitly to match D-15 — the example code above accidentally sends the whole status object; use `status: state` consistently.

    Correction: use `status: state` on BOTH 403 branches. Final branches must be:
    - feature_limited blocked-capability 403 → `{ error: 'account_suspended', status: state, reasonCategory, note }` where `state === 'feature_limited'`.
    - non-feature_limited 403 → `{ error: 'account_suspended', status: state, reasonCategory, note }`.

    Do NOT:
    - Omit the `includeAllUsers: true` setOptions call — skipping it creates a bypass (Plan 03-01 hook hides suspended User from self-lookup).
    - Re-resolve capabilities via STATUS_POLICY — always read `user.moderationStatus.restrictedFeatures` directly (Phase 1 D-12 denormalization).
    - Hard-code the capability strings here — requiredCapability is received as a factory argument.
    - Log the fallback warning more than once per request.
    - Use `verifyAdminByUid` or any Phase 1 legacy admin helper — this is a fresh self-lookup on the User collection.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services &amp;&amp; grep -n "function requireNotSuspended" src/security/requireNotSuspended.js &amp;&amp; grep -c "includeAllUsers: true" src/security/requireNotSuspended.js &amp;&amp; grep -n "deprecated body-uid fallback" src/security/requireNotSuspended.js &amp;&amp; grep -n "account_suspended" src/security/requireNotSuspended.js &amp;&amp; grep -n "user_not_found" src/security/requireNotSuspended.js &amp;&amp; grep -n "restrictedFeatures.includes" src/security/requireNotSuspended.js &amp;&amp; node -e "const { requireNotSuspended } = require('./src/security/requireNotSuspended'); const mw = requireNotSuspended('create_listing'); console.log(typeof mw === 'function' ? 'ok' : 'fail');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `../backend-services/carEx-services/src/security/requireNotSuspended.js`.
    - `grep -n "module.exports = { requireNotSuspended }" src/security/requireNotSuspended.js` returns 1 match.
    - `grep -c "includeAllUsers: true" src/security/requireNotSuspended.js` returns exactly 1 (on the User.findOne chain).
    - `grep -c "deprecated body-uid fallback" src/security/requireNotSuspended.js` returns 1 (the console.warn message).
    - `grep -c "account_suspended" src/security/requireNotSuspended.js` returns at least 2 (both 403 branches).
    - `grep -c "user_not_found" src/security/requireNotSuspended.js` returns at least 2 (missing uid + missing user branches).
    - `grep -c "req.auth" src/security/requireNotSuspended.js` returns at least 1 (Bearer-first uid resolution).
    - `grep -c "req.body" src/security/requireNotSuspended.js` returns at least 1 (dual-accept fallback).
    - `grep -c "restrictedFeatures" src/security/requireNotSuspended.js` returns at least 1 (feature_limited capability check).
    - `grep -c "STATUS_POLICY" src/security/requireNotSuspended.js` returns 0 (capability is read from denormalized restrictedFeatures per Phase 1 D-12, not re-resolved from the policy).
    - `node -e "const { requireNotSuspended } = require('./src/security/requireNotSuspended'); const mw = requireNotSuspended('create_listing'); if (typeof mw !== 'function' || mw.length !== 3) process.exit(1); console.log('ok');"` prints `ok`.
  </acceptance_criteria>
  <done>requireNotSuspended.js exists as a factory that resolves caller uid (Bearer first, body/params fallback with warn), self-looks-up via User.findOne with includeAllUsers bypass, and returns the correct 403/404/next() matrix per D-01/D-15.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → attachAuthIfPresent | Untrusted Authorization header; dual-accept means absence is NOT a failure. |
| client body → requireNotSuspended uid fallback | req.body.sellerId / req.body.buyerUid / req.params.uid are spoofable; acceptable only because dual-accept is transitional (D-03). |
| requireNotSuspended → User collection | Self-lookup; must bypass Plan 03-01 pre-hook via includeAllUsers, else suspended caller self-hides. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | S (Spoofing) | requireNotSuspended body-uid fallback | accept (transitional) | D-03 documents the spoofability window; Phase 6 QUAL-03 removes it. Deprecation warning on every fallback use gives ops visibility of the attack surface during Phase 3-5. |
| T-03-02-02 | S (Spoofing) | Bearer path in attachAuthIfPresent | mitigate | `admin.auth().verifyIdToken(token, true)` with `checkRevoked=true` — copied verbatim from Phase 1 D-05. Revoked tokens fail. |
| T-03-02-03 | I (Info Disclosure) | Self-lookup hidden by pre-hook | mitigate | MANDATORY `.setOptions({ includeAllUsers: true })` on User.findOne. Without it, a suspended caller's User doc is hidden → middleware 404s instead of 403s → bypass. Acceptance criterion enforces presence of the exact string. |
| T-03-02-04 | E (Elevation of privilege) | feature_limited gating | mitigate | Capability check reads denormalized `restrictedFeatures` (Phase 1 D-12). Admin's suspend handler (Phase 2) writes this array at suspend time via `resolveRestrictedFeatures()` — middleware trusts the User doc. If an attacker mutates their own restrictedFeatures, they'd only hurt themselves (it's their own denylist). |
| T-03-02-05 | D (DoS) | User.findOne per gated write | accept | Index-backed by `firebaseUid unique` from Phase 1 User schema. Per-request cost is a single indexed read. |
| T-03-02-06 | R (Repudiation) | Deprecation warning log | accept | console.warn writes to Railway logs; gives ops retroactive ability to enumerate body-uid fallback callers before Phase 6 strict cutover. |
</threat_model>

<verification>
- Phase 3 ROADMAP Success Criterion #1 (403 on user-write for suspended user, five gated routes) is PROVEN by Plan 03-06 `requireNotSuspended.middleware.test.js` + `acceptance.test.js` Block 1, which import `attachAuthIfPresent` and `requireNotSuspended` directly.
- Phase 3 ROADMAP Success Criterion #4 (feature_limited capability selectivity) is PROVEN by the same test files — `feature_limited` + blocked capability → 403; `feature_limited` + unblocked capability → 200.
- Plan 03-03 composes these two middlewares onto the five gated routes in `server.js`.
</verification>

<success_criteria>
- Two new files exist at `../backend-services/carEx-services/src/security/attachAuthIfPresent.js` and `requireNotSuspended.js`.
- `attachAuthIfPresent` returns `next()` on no-Bearer and on non-Bearer headers, returns 401 on Bearer-present-but-bad, and does NOT mutate `verifyIdToken.js`.
- `requireNotSuspended` is a factory; the returned middleware enforces the D-01 403/404/next() matrix with the MANDATORY `includeAllUsers: true` on its self-lookup.
- Both modules are require-clean (`node -e "require(...)"` does not throw).
- No changes to `server.js`, admin routes, or `verifyIdToken.js` in this plan (Plan 03-03 does the composition).
</success_criteria>

<output>
After completion, create `.planning/phases/03-backend-enforcement-backend/03-02-SUMMARY.md`.
</output>
