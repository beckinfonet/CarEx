---
phase: 03-backend-enforcement-backend
plan: 06
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05]
files_modified:
  - ../backend-services/carEx-services/__tests__/enforcement/requireNotSuspended.middleware.test.js
  - ../backend-services/carEx-services/__tests__/enforcement/hideOnFind.test.js
  - ../backend-services/carEx-services/__tests__/enforcement/confirmBooking.transaction.test.js
  - ../backend-services/carEx-services/__tests__/enforcement/ordersDeprecated.test.js
  - ../backend-services/carEx-services/__tests__/enforcement/acceptance.test.js
autonomous: true
requirements: [ENF-01, ENF-02, ENF-03, ENF-04]
must_haves:
  truths:
    - "Every ROADMAP Phase 3 Success Criterion (#1, #2, #3, #4) maps to at least one Jest test that passes against the real implementation from Plans 03-01..05"
    - "requireNotSuspended.middleware.test.js covers the 6-case matrix from CONTEXT.md D-16 (Bearer+active, body-fallback+active, suspended, feature_limited+blocked, feature_limited+unblocked, user-not-found)"
    - "hideOnFind.test.js proves zero-data-mutation on the owned doc across suspend/unsuspend round-trip for Car, Broker, and LogisticsPartner"
    - "confirmBooking.transaction.test.js covers happy path + buyer-suspended + provider-suspended + seller-suspended + refund-API-failure + concurrent-suspend race"
    - "acceptance.test.js has four integration blocks each explicitly mapping to ROADMAP Success Criteria #1-4"
    - "ordersDeprecated.test.js asserts POST /api/orders returns exactly 410 with the D-12 body"
  artifacts:
    - path: "../backend-services/carEx-services/__tests__/enforcement/requireNotSuspended.middleware.test.js"
      provides: "Middleware unit matrix"
    - path: "../backend-services/carEx-services/__tests__/enforcement/hideOnFind.test.js"
      provides: "pre(/^find/) hook behavior matrix for all three models"
    - path: "../backend-services/carEx-services/__tests__/enforcement/confirmBooking.transaction.test.js"
      provides: "Transaction atomicity + refund-first-throw-second + race test"
    - path: "../backend-services/carEx-services/__tests__/enforcement/ordersDeprecated.test.js"
      provides: "410 Gone assertion"
    - path: "../backend-services/carEx-services/__tests__/enforcement/acceptance.test.js"
      provides: "End-to-end capstone mapping to all 4 ROADMAP success criteria"
  key_links:
    - from: "Plan 03-02 requireNotSuspended"
      to: "Plan 03-06 requireNotSuspended.middleware.test.js"
      via: "jest matrix + firebase-admin mock"
      pattern: "describe.*requireNotSuspended"
    - from: "Plan 03-01 pre(/^find/) hooks"
      to: "Plan 03-06 hideOnFind.test.js"
      via: "MongoMemoryReplSet + direct model calls"
      pattern: "Car.find|Broker.find|LogisticsPartner.find"
    - from: "Plan 03-04 confirmBooking service"
      to: "Plan 03-06 confirmBooking.transaction.test.js"
      via: "Stripe mock + race test"
      pattern: "refunds.create.*toHaveBeenCalled"
---

<objective>
Create the Phase 3 enforcement test suite under `../backend-services/carEx-services/__tests__/enforcement/`. Five test files provide unit coverage for each Phase 3 module plus an end-to-end acceptance test that maps 1:1 to the four ROADMAP Phase 3 Success Criteria.

Purpose: This is the verifier's source of truth. Each ROADMAP criterion becomes an executable test that Phase 3 work has delivered the behavior, and the verifier reads this file to run the criteria.
Output: Five new test files, one new directory (`__tests__/enforcement/`), reusing the existing `__tests__/_helpers/mongoReplSet.js` harness from Phase 2 Plan 02-01.

Scope discipline: These tests exercise the Phase 3 modules via their public interfaces — the middleware factories, the service function, the model hooks — without booting the full server.js. The acceptance test builds a minimal Express app with ONLY the five gated routes + their minimal handlers (per PATTERNS.md) so tests don't have to mock S3, Twilio, MongoDB connection URIs, etc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-backend-enforcement-backend/03-CONTEXT.md
@.planning/phases/03-backend-enforcement-backend/03-PATTERNS.md
@.planning/phases/03-backend-enforcement-backend/03-01-PLAN.md
@.planning/phases/03-backend-enforcement-backend/03-02-PLAN.md
@.planning/phases/03-backend-enforcement-backend/03-03-PLAN.md
@.planning/phases/03-backend-enforcement-backend/03-04-PLAN.md
@.planning/phases/03-backend-enforcement-backend/03-05-PLAN.md

<interfaces>
From ../backend-services/carEx-services/__tests__/_helpers/mongoReplSet.js (Phase 2 Plan 02-01):
```
const { startReplSet, stopReplSet } = require('../_helpers/mongoReplSet');
let rs;
beforeAll(async () => { rs = await startReplSet(); });
afterAll(async () => { await stopReplSet(rs); });
```

From ../backend-services/carEx-services/__tests__/moderation/acceptance.test.js (Phase 2 analog to mirror):
- jest.mock('firebase-admin', ...) block with __verifyIdTokenMock escape hatch.
- MongoMemoryReplSet startup in beforeAll.
- Three-block integration style (this plan's acceptance.test.js is FOUR blocks, one per ROADMAP criterion).

From ../backend-services/carEx-services/__tests__/moderation/requireAdmin.middleware.test.js:
- Minimal Express app with a single test route.
- supertest for HTTP assertions.
- Pattern to mirror for requireNotSuspended.middleware.test.js.

From ../backend-services/carEx-services/__tests__/moderation/suspend.test.js:
- Direct-service test (no HTTP layer) that uses MongoMemoryReplSet + direct User.updateOne.
- Pattern to mirror for hideOnFind.test.js (query-middleware test — no HTTP layer needed).
</interfaces>

<mock_templates>
**firebase-admin mock** (copy verbatim to every test that uses attachAuthIfPresent with a Bearer branch):
```
jest.mock('firebase-admin', () => {
  const verifyIdTokenMock = jest.fn();
  const mock = {
    credential: { cert: jest.fn(() => ({})) },
    initializeApp: jest.fn(),
    auth: jest.fn(() => ({ verifyIdToken: verifyIdTokenMock })),
  };
  mock.__verifyIdTokenMock = verifyIdTokenMock;
  return mock;
});
```

**stripe mock** (copy verbatim to confirmBooking tests):
```
jest.mock('stripe', () => {
  const paymentIntentsRetrieveMock = jest.fn();
  const refundsCreateMock = jest.fn();
  const stripeMock = () => ({
    paymentIntents: { retrieve: paymentIntentsRetrieveMock },
    refunds: { create: refundsCreateMock },
  });
  stripeMock.__paymentIntentsRetrieveMock = paymentIntentsRetrieveMock;
  stripeMock.__refundsCreateMock = refundsCreateMock;
  return stripeMock;
});
```
</mock_templates>

<criterion_mapping>
| ROADMAP Success Criterion | Proving Test File | Proving Block |
|---------------------------|-------------------|---------------|
| #1 (403 on 5 gated routes for blocked_with_review user) | acceptance.test.js | Block 1 |
| #1 (matrix per-state per-capability) | requireNotSuspended.middleware.test.js | all 6 cases |
| #2 (GET /api/cars hides on suspend, restores on unsuspend, zero mutation) | hideOnFind.test.js + acceptance.test.js | full Car matrix + Block 2 |
| #2 (same for Broker + LogisticsPartner) | hideOnFind.test.js | Broker + Logistics matrices |
| #3 (concurrent admin.suspend during confirm-booking → refund + no order) | confirmBooking.transaction.test.js + acceptance.test.js | race test + Block 3 |
| #3 (buyer, provider, seller suspended paths) | confirmBooking.transaction.test.js | cases 2-4 |
| #3 (refund-API-failure surfaced) | confirmBooking.transaction.test.js | case 5 |
| #4 (feature_limited blocks create_listing, allows create_order if unblocked) | requireNotSuspended.middleware.test.js + acceptance.test.js | feature_limited cases + Block 4 |
| D-12 (POST /api/orders 410 Gone) | ordersDeprecated.test.js | single test |
</criterion_mapping>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create requireNotSuspended + hideOnFind + ordersDeprecated unit tests</name>
  <files>
    ../backend-services/carEx-services/__tests__/enforcement/requireNotSuspended.middleware.test.js
    ../backend-services/carEx-services/__tests__/enforcement/hideOnFind.test.js
    ../backend-services/carEx-services/__tests__/enforcement/ordersDeprecated.test.js
  </files>
  <read_first>
    - ../backend-services/carEx-services/__tests__/moderation/requireAdmin.middleware.test.js (shape analog for middleware matrix)
    - ../backend-services/carEx-services/__tests__/moderation/suspend.test.js (shape analog for direct-model tests)
    - ../backend-services/carEx-services/__tests__/_helpers/mongoReplSet.js (startReplSet/stopReplSet contract)
    - .planning/phases/03-backend-enforcement-backend/03-PATTERNS.md sections for each test file
    - .planning/phases/03-backend-enforcement-backend/03-CONTEXT.md D-16, D-17
  </read_first>
  <behavior>
    **requireNotSuspended.middleware.test.js** (6-case matrix per D-16):
    1. Active user + valid Bearer → 200, `req.callerUser.firebaseUid === seededUid`.
    2. Active user + no Bearer + `req.body.sellerId === seededUid` → 200, console.warn called once with `[requireNotSuspended] deprecated body-uid fallback used` in message. Use jest.spyOn(console, 'warn') + restore in afterEach.
    3. Suspended user (`state='blocked_with_review'`, reasonCategory='policy_violation', note='spammed listings') + Bearer → 403 `{ error: 'account_suspended', status: 'blocked_with_review', reasonCategory: 'policy_violation', note: 'spammed listings' }`.
    4. feature_limited user with `restrictedFeatures: ['create_listing']`, route `requireNotSuspended('create_listing')` → 403 `{ error: 'account_suspended', status: 'feature_limited', reasonCategory, note }`.
    5. feature_limited user with `restrictedFeatures: ['create_listing']`, route `requireNotSuspended('create_order')` → 200 (capability not blocked).
    6. Non-existent firebaseUid + Bearer pointing at it → 404 `{ error: 'user_not_found' }`.

    **hideOnFind.test.js** (D-16 + ROADMAP #2):
    Car matrix:
    - Seed active seller (`moderationStatus.state='active'`, `sellerStatus='APPROVED'`) + one Car → `Car.find({})` returns 1 result.
    - Update seller `moderationStatus.state='blocked_with_review'` → `Car.find({})` returns []; re-read the car from DB directly via `Car.findById(id).setOptions({ includeAllUsers: true })` → car exists with `listingStatus` and `active` fields unchanged (proves zero mutation per ROADMAP #2).
    - Restore `state='active'` → `Car.find({})` returns 1 again, same _id.
    - Set `sellerStatus='NONE'` → `Car.find({})` returns [].
    - Bypass: `Car.find({}).setOptions({ includeAllUsers: true })` returns 1 regardless of state (suspended or NONE).
    - `Car.findById(carOfSuspendedSeller)` (no bypass) returns null (proves D-09 `/^find/` covers findById).

    Broker matrix (same shape, field = `brokerStatus`, join = `ownerUid`).
    LogisticsPartner matrix (same shape, field = `logisticsStatus`).

    **ordersDeprecated.test.js**:
    Single test: minimal Express app with ONLY `app.post('/api/orders', (req,res) => res.status(410).json({error:'deprecated', message:'Use POST /api/payments/confirm-booking which now creates orders atomically'}))` → supertest POST any body → expect 410 + exact body.
  </behavior>
  <action>
    Create the `__tests__/enforcement/` directory and the three files.

    **`requireNotSuspended.middleware.test.js`** (template from PATTERNS.md §"__tests__/enforcement/requireNotSuspended.middleware.test.js"):
    - Top-of-file: `jest.mock('firebase-admin', ...)` per mock_templates above.
    - Requires: `express`, `supertest`, `../_helpers/mongoReplSet`, `firebase-admin`, `../../src/models/User`, `../../src/security/attachAuthIfPresent`, `../../src/security/requireNotSuspended`.
    - beforeAll: `rs = await startReplSet(); process.env.FIREBASE_SERVICE_ACCOUNT_JSON = JSON.stringify({project_id:'test'}); app = express(); app.use(express.json()); app.post('/test-create-listing', attachAuthIfPresent, requireNotSuspended('create_listing'), (req,res)=>res.json({ok:true, callerUid:req.callerUser?.firebaseUid})); app.post('/test-create-order', attachAuthIfPresent, requireNotSuspended('create_order'), (req,res)=>res.json({ok:true}));`
    - afterAll: stopReplSet.
    - beforeEach: `await User.deleteMany({}); admin.__verifyIdTokenMock.mockReset();`
    - Each of the 6 cases is its own `test(...)` block. Case 2 spies on console.warn with `const warnSpy = jest.spyOn(console, 'warn').mockImplementation(()=>{})` and restores in afterEach.
    - For Bearer cases: `admin.__verifyIdTokenMock.mockResolvedValue({ uid: seededUid, email: 'x@y.z' })` then `request(app).post('/test-...').set('Authorization', 'Bearer faketoken').send({})`.
    - For body-fallback case: no Authorization header, body includes `sellerId: seededUid`.

    **`hideOnFind.test.js`** (template from PATTERNS.md §"__tests__/enforcement/hideOnFind.test.js"):
    - No firebase-admin mock needed (no HTTP layer).
    - Requires: `../_helpers/mongoReplSet`, `../../src/models/User`, `../../src/models/Car`, `../../src/models/Broker`, `../../src/models/LogisticsPartner`.
    - beforeAll/afterAll: start/stop replSet.
    - beforeEach: `await User.deleteMany({}); await Car.deleteMany({}); await Broker.deleteMany({}); await LogisticsPartner.deleteMany({});`
    - Three `describe` blocks: Car, Broker, LogisticsPartner. Each with the full matrix from <behavior>.
    - For the zero-mutation assertion: after updating seller.moderationStatus.state, re-fetch the car via `Car.findById(id).setOptions({ includeAllUsers: true })` and `expect(car.listingStatus).toBe('active')` and any other denormalized field to still equal its pre-suspension value.

    **`ordersDeprecated.test.js`** (template from PATTERNS.md §"__tests__/enforcement/ordersDeprecated.test.js"):
    - NO database needed — just Express + supertest.
    - Build a 10-line test that exactly mirrors the template in PATTERNS.md. Use a fresh Express app inside the test (not from server.js) to avoid booting the full backend.

    Do NOT:
    - Import `server.js` in any of these three tests (pulls in Mongo URI, Twilio, S3, Stripe initializers — heavyweight).
    - Skip the mongoReplSet harness for requireNotSuspended or hideOnFind — they both hit real Mongo for the User/Car/Broker/LogisticsPartner collections.
    - Couple test order — each `test()` seeds its own state in beforeEach.
    - Leave console.warn spies installed between tests (afterEach restore mandatory).
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services &amp;&amp; npx jest __tests__/enforcement/requireNotSuspended.middleware.test.js __tests__/enforcement/hideOnFind.test.js __tests__/enforcement/ordersDeprecated.test.js --runInBand 2&gt;&amp;1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under `../backend-services/carEx-services/__tests__/enforcement/`.
    - `npx jest __tests__/enforcement/requireNotSuspended.middleware.test.js --runInBand` exits 0.
    - `npx jest __tests__/enforcement/hideOnFind.test.js --runInBand` exits 0.
    - `npx jest __tests__/enforcement/ordersDeprecated.test.js --runInBand` exits 0.
    - `grep -c "test(" __tests__/enforcement/requireNotSuspended.middleware.test.js` returns at least 6 (one per case).
    - `grep -c "includeAllUsers" __tests__/enforcement/hideOnFind.test.js` returns at least 2 (bypass-verification cases).
    - `grep -c "'blocked_with_review'" __tests__/enforcement/requireNotSuspended.middleware.test.js` returns at least 1.
    - `grep -c "feature_limited" __tests__/enforcement/requireNotSuspended.middleware.test.js` returns at least 2 (blocked-capability case + unblocked-capability case).
    - `grep -c "status(410)" __tests__/enforcement/ordersDeprecated.test.js` returns at least 1.
    - `grep -c "deprecated body-uid fallback" __tests__/enforcement/requireNotSuspended.middleware.test.js` returns at least 1 (asserts the warn message).
  </acceptance_criteria>
  <done>Three unit test files exist, all three pass `npx jest --runInBand`, and the per-file grep assertions confirm the D-16 matrix coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create confirmBooking.transaction.test.js (atomicity + refund + concurrent-suspend race)</name>
  <files>../backend-services/carEx-services/__tests__/enforcement/confirmBooking.transaction.test.js</files>
  <read_first>
    - ../backend-services/carEx-services/__tests__/moderation/suspend.test.js (session.withTransaction assertion patterns)
    - ../backend-services/carEx-services/__tests__/moderation/deleteProviderProfile.test.js (rollback-on-error patterns)
    - ../backend-services/carEx-services/src/payments/confirmBooking.js (Plan 03-04 — entry point, ProviderSuspendedError shape, return value)
    - ../backend-services/carEx-services/src/moderation/service.js (suspend function — used directly for race test)
    - .planning/phases/03-backend-enforcement-backend/03-PATTERNS.md section "__tests__/enforcement/confirmBooking.transaction.test.js"
    - .planning/phases/03-backend-enforcement-backend/03-CONTEXT.md D-11, D-13, D-16, D-17
  </read_first>
  <behavior>
    Six cases per D-16:

    1. **Happy path**: active buyer + active provider (broker) + active seller + PI status 'succeeded' → `confirmBookingService({...})` returns `{ car, orders }` where `car.listingStatus === 'booked'`, `orders.length >= 1`, each order has populated `providerSnapshot`. Assertion: `stripeMock.__refundsCreateMock` was NOT called.

    2. **Provider suspended mid-window**: seed active state, then between `create-payment-intent` and `confirmBookingService` call, directly update the provider User's `moderationStatus.state='blocked_with_review'`. Invoke `confirmBookingService(...)`. Expect it to throw with `err instanceof ProviderSuspendedError`, `err.message === 'provider_suspended'`, `err.providerUid === suspendedProviderUid`, `err.refundId` is a truthy string (mock returns `{id: 're_mock_123'}`). Assert `refundsCreateMock` called exactly once with `{payment_intent: paymentIntentId}`. Assert no ServiceOrder rows created (`ServiceOrder.countDocuments({stripePaymentIntentId: paymentIntentId}) === 0`). Assert car.listingStatus is still 'active' (or whatever the pre-test state was).

    3. **Buyer suspended mid-window**: same shape as case 2 but suspend the buyer User instead. Assert same rollback shape + `err.providerUid === buyerUid`.

    4. **Seller (car owner) suspended mid-window**: same shape, seller suspended. `err.providerUid === sellerUid`.

    5. **Refund API failure**: provider is suspended (so refund triggers) AND `refundsCreateMock.mockRejectedValueOnce(new Error('stripe down'))`. Expect thrown err has `err.refundFailed === true`, `err.refundId === null`. Assert `console.error` was called (use spy).

    6. **Concurrent-suspend race (D-13)**: run `Promise.all([ moderationService.suspend({...providerUid, severity:'blocked_with_review'}), confirmBookingService({...provider is providerUid}) ])`. Exactly one of two outcomes MUST obtain:
       - **Outcome A**: suspend wins the race → confirmBookingService throws ProviderSuspendedError, refund fired, zero ServiceOrder rows, car not booked.
       - **Outcome B**: confirmBooking wins the race → confirmBookingService resolves successfully with `{car, orders}`, car.listingStatus='booked', orders created, refund NOT fired, provider User ends up suspended (suspend committed afterward).
       Never both-succeed-and-leave-booked-order-for-suspended-provider (the forbidden state). Assert via a `try/catch` wrapper that checks the outcome shape and rejects any inconsistent intermediate state.

    Additional idempotency test:
    7. **Retry same paymentIntentId on already-booked car**: seed a Car with `stripePaymentIntentId === somePi` and pre-existing ServiceOrder rows. Call `confirmBookingService({...paymentIntentId: somePi})`. Expect it returns `{car, orders}` with the EXISTING orders (same _ids). Assert `stripeMock.__paymentIntentsRetrieveMock` was NOT called (the fast-path short-circuits before Stripe).
  </behavior>
  <action>
    Create `../backend-services/carEx-services/__tests__/enforcement/confirmBooking.transaction.test.js`:

    1. Top-of-file mocks: `jest.mock('stripe', ...)` per mock_templates. No firebase-admin mock needed (no HTTP layer in this test).

    2. Requires: `mongoose`, `../_helpers/mongoReplSet`, `../../src/models/User`, `../../src/models/Car`, `../../src/models/Broker`, `../../src/models/LogisticsPartner`, `../../src/payments/confirmBooking` (imports `confirmBooking`, `ProviderSuspendedError`), `../../src/moderation/service` (for the race test — imports `suspend`).

    3. ServiceOrder schema: since Phase 3 does NOT extract ServiceOrder, the test must register it so `mongoose.model('ServiceOrder')` resolves. Easiest path: the test defines a loose `serviceOrderSchema = new mongoose.Schema({...}, {strict:false})` ONCE in beforeAll before any models are used — mirrors the Phase 2 test pattern for deleteProviderProfile.test.js. Fields needed: `stripePaymentIntentId`, `buyerUid`, `carId`, `providerUid`, `providerSnapshot`, `services[]`, `totalAmount`, `orderNumber`, `createdAt`.

    4. beforeAll: `rs = await startReplSet();` + register ServiceOrder schema.

    5. afterAll: `await stopReplSet(rs);`.

    6. beforeEach: delete all User/Car/Broker/LogisticsPartner/ServiceOrder/ModerationAction/AdminUser docs; reset `paymentIntentsRetrieveMock` and `refundsCreateMock` to default success shape (`mockResolvedValue({status:'succeeded'})`, `mockResolvedValue({id:'re_mock_123'})`).

    7. Helper `seedHappyPath()` returns `{ buyerUid, providerUid, sellerUid, carId, paymentIntentId, items }` — seeds all Users as active+APPROVED, one Broker profile, one Car.

    8. Each of the 7 cases as its own `test()` block. Case 6 (race) uses `Promise.allSettled` and custom assertion logic to classify Outcome A vs B and reject any third state.

    9. Case 7 (idempotency) pre-seeds a Car with `stripePaymentIntentId = 'pi_already_booked'` and a ServiceOrder row with the same id, calls confirmBookingService, asserts `paymentIntentsRetrieveMock` NOT called.

    Do NOT:
    - Mock confirmBookingService itself — this test exercises the real implementation from Plan 03-04.
    - Boot server.js.
    - Use real Stripe SDK (module-level jest.mock covers it).
    - Rely on test execution order (every case seeds fresh state in beforeEach).
    - Skip the race test — it's the ENF-03 / ROADMAP #3 proof.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services &amp;&amp; npx jest __tests__/enforcement/confirmBooking.transaction.test.js --runInBand 2&gt;&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `../backend-services/carEx-services/__tests__/enforcement/confirmBooking.transaction.test.js`.
    - `npx jest __tests__/enforcement/confirmBooking.transaction.test.js --runInBand` exits 0.
    - `grep -c "test(" __tests__/enforcement/confirmBooking.transaction.test.js` returns at least 7.
    - `grep -c "ProviderSuspendedError" __tests__/enforcement/confirmBooking.transaction.test.js` returns at least 3 (case 2, 3, 4 all assert instanceof).
    - `grep -c "refundsCreateMock" __tests__/enforcement/confirmBooking.transaction.test.js` returns at least 5 (all cases touch refund assertions).
    - `grep -c "Promise.allSettled" __tests__/enforcement/confirmBooking.transaction.test.js` returns at least 1 (case 6 concurrent race).
    - `grep -c "refundFailed" __tests__/enforcement/confirmBooking.transaction.test.js` returns at least 1 (case 5 refund failure assertion).
    - `grep -c "paymentIntentsRetrieveMock" __tests__/enforcement/confirmBooking.transaction.test.js` returns at least 2 (case 7 asserts NOT called for idempotency).
    - `grep -c "listingStatus" __tests__/enforcement/confirmBooking.transaction.test.js` returns at least 3 (happy path + rollback assertions).
    - `grep -c "suspend" __tests__/enforcement/confirmBooking.transaction.test.js` returns at least 2 (case 6 race test imports moderationService.suspend).
  </acceptance_criteria>
  <done>confirmBooking.transaction.test.js passes with 7 test cases covering happy path, three suspended-party paths, refund failure, concurrent race, and idempotency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create acceptance.test.js — four-block capstone mapping to ROADMAP #1-4</name>
  <files>../backend-services/carEx-services/__tests__/enforcement/acceptance.test.js</files>
  <read_first>
    - ../backend-services/carEx-services/__tests__/moderation/acceptance.test.js (Phase 2 acceptance test — exact structural twin per PATTERNS.md)
    - ../backend-services/carEx-services/__tests__/enforcement/confirmBooking.transaction.test.js (Task 2 — ServiceOrder schema registration pattern + stripe mock)
    - .planning/phases/03-backend-enforcement-backend/03-PATTERNS.md section "__tests__/enforcement/acceptance.test.js"
    - .planning/phases/03-backend-enforcement-backend/03-CONTEXT.md §Specifics (explicit criterion→test mapping)
  </read_first>
  <behavior>
    End-to-end acceptance test with four blocks. Each block names its ROADMAP criterion in the describe string so the verifier's grep can confirm coverage.

    **Structural shape** (from PATTERNS.md):
    - `jest.mock('firebase-admin', ...)` and `jest.mock('stripe', ...)`.
    - Minimal Express app built in beforeAll with ONLY the five gated routes + POST /api/orders (410) + a fake GET /api/cars that uses the `Car.find({})` hook directly (NOT the real server.js GET handler).
    - Mount: `app.post('/api/cars', attachAuthIfPresent, requireNotSuspended('create_listing'), (req,res)=>res.status(201).json({ok:true}))` and similar for the other routes. The confirm-booking route wires the real `confirmBookingService` import from Plan 03-04.
    - Standard replSet + User/Car/Broker/LogisticsPartner cleanup per test.

    **Block 1 — Criterion #1 (403 on user-write for suspended, 5 routes)**:
    - Seed a user with `moderationStatus.state='blocked_with_review'`, reasonCategory='fraud', note='test note'.
    - Mock Firebase verifyIdToken to return `{uid:seededUid, email:...}`.
    - Hit each of the 5 gated routes with Bearer + minimal body.
    - Assert every response is `403 { error: 'account_suspended', status: 'blocked_with_review', reasonCategory: 'fraud', note: 'test note' }`.
    - This is ROADMAP criterion #1 verbatim.

    **Block 2 — Criterion #2 (hide/restore + zero mutation)**:
    - Seed active seller + Car. Set `listingStatus='active'` explicitly.
    - `Car.find({})` → 1 result.
    - Update seller `moderationStatus.state='blocked_with_review'`.
    - `Car.find({})` → [].
    - Re-fetch the car via `Car.findById(id).setOptions({includeAllUsers:true})` → `listingStatus` still === 'active' (zero mutation assertion — ROADMAP criterion #2 verbatim).
    - Restore `state='active'` → `Car.find({})` → 1 result.

    **Block 3 — Criterion #3 (concurrent suspend + confirm-booking)**:
    - Seed buyer, provider, seller, car.
    - Mock Stripe PI retrieve → succeeded; refunds.create → `{id:'re_accept_123'}`.
    - Invoke `Promise.allSettled([ direct call to moderationService.suspend(providerUid,...), fetch('/api/payments/confirm-booking') via supertest ])`.
    - Assert exactly one of: (A) confirm 409 provider_suspended + zero ServiceOrder rows + refund called, OR (B) confirm 200 + orders created + refund NOT called + provider later shows suspended state.
    - Any other state → test fails.

    **Block 4 — Criterion #4 (feature_limited capability selectivity)**:
    - Seed user with `moderationStatus.state='feature_limited'`, `restrictedFeatures=['create_listing']`, reasonCategory='policy_violation'.
    - POST /api/cars with Bearer → 403 account_suspended.
    - POST /api/payments/create-payment-intent with same Bearer → 200 (capability 'create_order' not in restrictedFeatures).
    - GET /api/cars on a minimal route — NOT one of the 5 gated routes, so not even reached by requireNotSuspended; separately assert that a feature-limited user can still read.
  </behavior>
  <action>
    Create `../backend-services/carEx-services/__tests__/enforcement/acceptance.test.js`:

    1. Top-of-file: `jest.mock('firebase-admin', ...)` + `jest.mock('stripe', ...)` per mock_templates. Add a test-isolation header comment referencing the Phase 2 acceptance.test.js write-up per PATTERNS.md.

    2. Requires: express, supertest, mongoose, ../_helpers/mongoReplSet, firebase-admin, stripe, all three new Plan 03-01 models, User, attachAuthIfPresent, requireNotSuspended, the confirmBooking service, moderation service (for Block 3).

    3. beforeAll: start replSet, env setup, register ServiceOrder loose schema, build app with the 5 gated routes + /api/orders 410 + real confirm-booking route wiring confirmBookingService with the mocked Stripe instance. Use minimal inline handlers (no multer — just `(req,res)=>res.status(201).json({ok:true})` — except confirm-booking which calls the real service).

    4. afterAll: stopReplSet.

    5. beforeEach: deleteMany on all seeded collections + reset firebase-admin + stripe mocks.

    6. Four top-level `describe('ROADMAP Criterion #N: ...', () => { ... })` blocks with one or more `test(...)` cases each.

    7. Use supertest consistently (`request(app).post(path).set('Authorization','Bearer x').send(body)`).

    8. Block 1: a `.forEach` over the 5 routes + assertion matrix, not 5 separate test blocks — DRY the repetition per Phase 2 pattern.

    9. Block 3's `Promise.allSettled` race test: whichever request completes first, assert the invariant (no both-succeed state). Use `await` on `allSettled`, inspect `.status` fields.

    Do NOT:
    - Boot real server.js (avoid Twilio/S3/Stripe initializers).
    - Skip ANY of the four blocks — each one is a ROADMAP criterion proof.
    - Couple blocks (each cleans up in beforeEach).
    - Use the real Stripe SDK — the module-level mock is authoritative.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services &amp;&amp; npx jest __tests__/enforcement/acceptance.test.js --runInBand 2&gt;&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `../backend-services/carEx-services/__tests__/enforcement/acceptance.test.js`.
    - `npx jest __tests__/enforcement/acceptance.test.js --runInBand` exits 0.
    - `grep -c "ROADMAP Criterion #1" __tests__/enforcement/acceptance.test.js` returns at least 1.
    - `grep -c "ROADMAP Criterion #2" __tests__/enforcement/acceptance.test.js` returns at least 1.
    - `grep -c "ROADMAP Criterion #3" __tests__/enforcement/acceptance.test.js` returns at least 1.
    - `grep -c "ROADMAP Criterion #4" __tests__/enforcement/acceptance.test.js` returns at least 1.
    - `grep -c "account_suspended" __tests__/enforcement/acceptance.test.js` returns at least 2 (Block 1 + Block 4).
    - `grep -c "blocked_with_review" __tests__/enforcement/acceptance.test.js` returns at least 1.
    - `grep -c "feature_limited" __tests__/enforcement/acceptance.test.js` returns at least 1.
    - `grep -c "Promise.allSettled\\|Promise.all" __tests__/enforcement/acceptance.test.js` returns at least 1 (Block 3 race).
    - `grep -c "includeAllUsers" __tests__/enforcement/acceptance.test.js` returns at least 1 (Block 2 zero-mutation re-fetch).
    - `grep -c "'/api/cars'" __tests__/enforcement/acceptance.test.js` returns at least 2 (Block 1 + Block 4).
    - `grep -c "'/api/payments/create-payment-intent'" __tests__/enforcement/acceptance.test.js` returns at least 1.
    - `grep -c "'/api/payments/confirm-booking'" __tests__/enforcement/acceptance.test.js` returns at least 1.
    - `grep -c "'/api/brokers/" __tests__/enforcement/acceptance.test.js` returns at least 1.
    - `grep -c "'/api/logistics/" __tests__/enforcement/acceptance.test.js` returns at least 1.
    - Full phase test suite passes: `cd ../backend-services/carEx-services && npx jest __tests__/enforcement --runInBand` exits 0.
  </acceptance_criteria>
  <done>acceptance.test.js passes with four describe blocks each mapping to a ROADMAP Phase 3 success criterion. All five enforcement tests run green under `npx jest __tests__/enforcement --runInBand`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test mocks (firebase-admin, stripe) | Tests must not rely on real external services — mocks are authoritative. |
| test isolation | MongoMemoryReplSet shared across files means beforeEach cleanup is mandatory; forgotten cleanup causes cross-test pollution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | T (Tampering) | Flaky race test | accept | Block 3 + confirmBooking case 6 use Promise.allSettled with outcome-classification assertions — both outcomes are valid, the test fails ONLY on the forbidden third state. Non-deterministic ordering is intentional. |
| T-03-06-02 | R (Repudiation) | Test coverage of ROADMAP criteria | mitigate | Each of the four criteria has its describe string named literally "ROADMAP Criterion #N" — grep-checked in acceptance criteria so the verifier can trivially confirm coverage. |
| T-03-06-03 | I (Info Disclosure) | Leaked test state between runs | mitigate | beforeEach deletes every collection used. MongoMemoryReplSet drops all data on stopReplSet. |
| T-03-06-04 | D (DoS / slow CI) | MongoMemoryReplSet boot per file | accept | --runInBand avoids parallel replSet boots. Each file boots once in beforeAll; per-test cost is beforeEach cleanup only. |
</threat_model>

<verification>
- Every ROADMAP Phase 3 Success Criterion is PROVEN by at least one test in this suite (see <criterion_mapping>).
- `cd ../backend-services/carEx-services && npx jest __tests__/enforcement --runInBand` is THE gate — if it exits 0, Phase 3 ships.
- Existing Phase 1 + Phase 2 tests continue to pass (no edits to __tests__/moderation/ in this phase).
</verification>

<success_criteria>
- Five new test files exist under `../backend-services/carEx-services/__tests__/enforcement/`.
- `npx jest __tests__/enforcement --runInBand` exits 0 (all files green).
- Each of the 4 ROADMAP Success Criteria has at least one test in acceptance.test.js with the criterion named literally in a describe string.
- requireNotSuspended.middleware.test.js has all 6 cases from D-16.
- hideOnFind.test.js covers Car + Broker + LogisticsPartner matrices including includeAllUsers bypass + zero-mutation assertion.
- confirmBooking.transaction.test.js covers happy path + 3 suspended-party paths + refund-API-failure + concurrent-suspend race + idempotency retry.
- ordersDeprecated.test.js asserts 410 with exact D-12 body.
- No changes to Phase 1 or Phase 2 tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-backend-enforcement-backend/03-06-SUMMARY.md`.
</output>
