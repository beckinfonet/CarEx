---
phase: 03-backend-enforcement-backend
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - ../backend-services/carEx-services/server.js
autonomous: true
requirements: [ENF-01, ENF-02, ENF-04]
must_haves:
  truths:
    - "POST /api/cars, POST /api/payments/create-payment-intent, POST /api/payments/confirm-booking, PUT /api/brokers/:uid, PUT /api/logistics/:uid are each gated by attachAuthIfPresent + requireNotSuspended(<capability>) BEFORE the existing handler middleware (multer, etc.)"
    - "The capability tokens passed at mount are exactly: 'create_listing' (POST /api/cars), 'create_order' (both payment endpoints), 'update_profile' (both PUT routes)"
    - "server.js no longer defines inline carSchema/brokerSchema/logisticsPartnerSchema — the three extracted Plan 03-01 model files are required at top-of-file"
    - "The existing handler bodies on all five routes are PRESERVED unchanged; only the middleware chain is prepended"
    - "No other user-write endpoints are gated in this plan (hybrid cutover discipline per D-02)"
  artifacts:
    - path: "../backend-services/carEx-services/server.js"
      provides: "Five gated routes + three model requires replacing inline schemas"
      contains: "requireNotSuspended('create_listing')"
  key_links:
    - from: "server.js POST /api/cars route"
      to: "requireNotSuspended('create_listing') + attachAuthIfPresent"
      via: "inline per-route middleware composition"
      pattern: "app.post\\('/api/cars',.*attachAuthIfPresent.*requireNotSuspended\\('create_listing'\\)"
    - from: "server.js top-of-file require()"
      to: "src/models/Car, Broker, LogisticsPartner"
      via: "module replacement of inline schemas"
      pattern: "require\\('./src/models/(Car|Broker|LogisticsPartner)'\\)"
---

<objective>
Modify `../backend-services/carEx-services/server.js` to (a) require the three extracted model files from Plan 03-01 instead of declaring inline schemas, and (b) prepend `attachAuthIfPresent` + `requireNotSuspended(<capability>)` on exactly the five ROADMAP-named user-write routes (D-02).

Purpose: Delivers the enforcement perimeter for ROADMAP Phase 3 Success Criterion #1 (403 on user-write for suspended callers). Pairs server.js wiring with the Plan 03-01 pre(/^find/) hooks (already on the extracted models) to satisfy Criterion #2 as a side effect — once the inline schemas are deleted, every `Car.find(...)` in server.js resolves to the new hook-equipped model.

Scope discipline (D-02 hybrid cutover): exactly five routes get the gate. Do NOT also gate PUT /api/cars/:id, PATCH /api/cars/:id/status, POST /api/users/:uid/request-{seller,broker,logistics}, PUT /api/users/:uid, POST /api/users/:uid/avatar, DELETE /api/users/:uid, PATCH /api/orders/:id/status, or PATCH /api/orders/:id/services/:serviceIndex/status. Those are explicitly tracked as follow-up tickets (CONTEXT.md §Deferred Ideas).

Note: This plan does NOT rewrite POST /api/payments/confirm-booking's handler body — that's Plan 03-04's job. This plan only prepends middleware. The existing handler body stays until Plan 03-04 replaces it with a call into the shared confirmBooking service. Plans 03-03 and 03-04 both touch server.js and are therefore sequenced in Wave 2 (03-04 depends on 03-03 to avoid merge conflict in the same lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-backend-enforcement-backend/03-CONTEXT.md
@.planning/phases/03-backend-enforcement-backend/03-PATTERNS.md
@.planning/phases/03-backend-enforcement-backend/03-01-PLAN.md
@.planning/phases/03-backend-enforcement-backend/03-02-PLAN.md

<interfaces>
From Plan 03-01 (Wave 1 sibling):
```
const Car = require('./src/models/Car');                          // replaces inline mongoose.model('Car', carSchema) at server.js:133
const Broker = require('./src/models/Broker');                    // replaces inline at server.js:160
const LogisticsPartner = require('./src/models/LogisticsPartner'); // replaces inline at server.js:179
```
Each module has the pre(/^find/) hide-hook registered; once inline schemas are deleted, all existing Car/Broker/LogisticsPartner find* calls in server.js automatically pick up the hook.

From Plan 03-02 (Wave 1 sibling):
```
const { attachAuthIfPresent } = require('./src/security/attachAuthIfPresent');
const { requireNotSuspended } = require('./src/security/requireNotSuspended');
```
Usage: `app.post('/api/cars', attachAuthIfPresent, requireNotSuspended('create_listing'), <existing-middleware-and-handler>);`
</interfaces>

<canonical_line_ranges>
From 03-CONTEXT.md canonical_refs (line-stable at mapping time):
- server.js:95-133 — carSchema + `const Car = mongoose.model('Car', carSchema);` — DELETE, replace with `require('./src/models/Car')`.
- server.js:137-160 — serviceItemSchema + brokerSchema + `mongoose.model('Broker', brokerSchema, 'brokers');` — DELETE, replace with `require('./src/models/Broker')`. Keep a local `serviceItemSchema` ONLY if it is referenced by schemas OTHER than brokerSchema/logisticsPartnerSchema in server.js (it isn't, per PATTERNS.md).
- server.js:163-179 — logisticsPartnerSchema + `mongoose.model('LogisticsPartner', logisticsPartnerSchema, 'logistics_partners');` — DELETE, replace with `require('./src/models/LogisticsPartner')`.
- server.js:590-611 — PUT /api/brokers/:uid
- server.js:645-668 — PUT /api/logistics/:uid
- server.js:740-827 — POST /api/cars
- server.js:1087-1111 — POST /api/payments/create-payment-intent
- server.js:1113-1143 — POST /api/payments/confirm-booking
</canonical_line_ranges>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace inline Car/Broker/LogisticsPartner schemas with requires</name>
  <files>../backend-services/carEx-services/server.js</files>
  <read_first>
    - ../backend-services/carEx-services/server.js lines 1-30 (existing top-of-file require block — find the Phase 1 pattern at server.js:12-17 to slot new requires next to it)
    - ../backend-services/carEx-services/server.js lines 85-185 (full inline schemas to delete)
    - ../backend-services/carEx-services/src/models/Car.js, Broker.js, LogisticsPartner.js (produced by Plan 03-01 — confirm they exist and export the model correctly)
    - .planning/phases/03-backend-enforcement-backend/03-PATTERNS.md section "server.js (MODIFIED)" — "Inline schema removal" paragraph
  </read_first>
  <behavior>
    - `server.js` loads cleanly with `node --check server.js` (syntax valid).
    - `server.js` no longer contains inline `carSchema`, `brokerSchema`, or `logisticsPartnerSchema` declarations.
    - `mongoose.model('Car')`, `mongoose.model('Broker')`, `mongoose.model('LogisticsPartner')` continue to resolve to the same registered instances (now registered by the required files).
    - All existing route handlers in server.js that reference `Car`, `Broker`, `LogisticsPartner` variables continue to work — the top-of-file `const Car = require('./src/models/Car')` (and siblings) keeps the same variable names in scope.
    - If `serviceItemSchema` was ONLY used by the two inline provider schemas, its declaration is removed from server.js too (the two extracted model files each define a local clone per Plan 03-01 Task 2). Confirm via grep before deletion — if any OTHER schema in server.js references `serviceItemSchema`, keep it.
  </behavior>
  <action>
    Edit `../backend-services/carEx-services/server.js`:

    1. **Add requires at top-of-file** (after the existing `const { verifyIdToken } = require('./src/security/verifyIdToken');` line from Phase 1, around server.js:12-17). Use an Edit operation (not a full rewrite) to add these six lines as a block, alongside existing security/model requires:
       ```
       const { attachAuthIfPresent } = require('./src/security/attachAuthIfPresent');
       const { requireNotSuspended } = require('./src/security/requireNotSuspended');
       const Car = require('./src/models/Car');
       const Broker = require('./src/models/Broker');
       const LogisticsPartner = require('./src/models/LogisticsPartner');
       ```
       If the existing file already has `const Car = mongoose.model(...)` further down, we'll delete that below in step 2/3/4 — don't worry about temporary duplicate identifiers during the edit because the replacements are in the same file; plan the edits so only one final declaration of `Car`, `Broker`, `LogisticsPartner` exists in the final file.

    2. **Delete inline Car schema + registration** (server.js:95-133 approximately — verify by grepping for `carSchema = new mongoose.Schema` and the following `const Car = mongoose.model('Car', carSchema)`). Remove the entire block. No replacement lines — the top-of-file `require('./src/models/Car')` already binds `Car`.

    3. **Delete inline Broker schema + registration** (server.js:137-160): remove `brokerSchema = new mongoose.Schema(...)`, `brokerSchema.index(...)`, `mongoose.model('Broker', brokerSchema, 'brokers')`, and any `const Broker = ...` binding.

    4. **Delete inline LogisticsPartner schema + registration** (server.js:163-179): same pattern.

    5. **serviceItemSchema handling**: run `grep -n "serviceItemSchema" server.js` before deleting any occurrences. Expected state: the only references are inside the inline broker and logistics schemas being deleted in steps 3 and 4. In that case, delete the top-level `const serviceItemSchema = ...` declaration too. If any OTHER schema references `serviceItemSchema` (unlikely per PATTERNS.md), KEEP the declaration.

    Do NOT:
    - Change any route handler bodies in this task.
    - Touch the ServiceOrder schema (Phase 1 D-21 extended it; stays inline in Phase 3 per PATTERNS.md).
    - Touch the User or AdminUser or ModerationAction schemas (Phase 1 already extracted them).
    - Rearrange `app.use(...)` middleware order or the `mongoose.connect(...)` block.
    - Delete existing comments or JSDoc on adjacent code.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services &amp;&amp; node --check server.js &amp;&amp; grep -c "new mongoose.Schema" server.js | awk '{ if ($1 == 0) exit 1; print "schemas remaining: " $1 }' &amp;&amp; grep -c "carSchema = new mongoose.Schema" server.js | awk '{ if ($1 != 0) exit 1; print "ok carSchema removed" }' &amp;&amp; grep -c "brokerSchema = new mongoose.Schema" server.js | awk '{ if ($1 != 0) exit 1; print "ok brokerSchema removed" }' &amp;&amp; grep -c "logisticsPartnerSchema = new mongoose.Schema" server.js | awk '{ if ($1 != 0) exit 1; print "ok logisticsPartnerSchema removed" }' &amp;&amp; grep -n "require('./src/models/Car')" server.js &amp;&amp; grep -n "require('./src/models/Broker')" server.js &amp;&amp; grep -n "require('./src/models/LogisticsPartner')" server.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check server.js` exits 0 (no syntax errors).
    - `grep -c "carSchema = new mongoose.Schema" server.js` returns 0.
    - `grep -c "brokerSchema = new mongoose.Schema" server.js` returns 0.
    - `grep -c "logisticsPartnerSchema = new mongoose.Schema" server.js` returns 0.
    - `grep -c "mongoose.model('Car'," server.js` returns 0 (inline registration removed).
    - `grep -c "mongoose.model('Broker'," server.js` returns 0.
    - `grep -c "mongoose.model('LogisticsPartner'," server.js` returns 0.
    - `grep -n "require('./src/models/Car')" server.js` returns exactly 1 match.
    - `grep -n "require('./src/models/Broker')" server.js` returns exactly 1 match.
    - `grep -n "require('./src/models/LogisticsPartner')" server.js` returns exactly 1 match.
    - `grep -c "const Car = " server.js` returns exactly 1 match (the require-based binding).
    - `grep -c "const Broker = " server.js` returns exactly 1 match.
    - `grep -c "const LogisticsPartner = " server.js` returns exactly 1 match.
    - `git diff server.js | grep '^-.*mongoose.model(.Car' | wc -l` is at least 1 (confirming deletion happened).
  </acceptance_criteria>
  <done>server.js has three new model requires at the top, the three inline schemas are deleted, node --check passes, and all `Car`/`Broker`/`LogisticsPartner` identifiers continue to resolve.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prepend attachAuthIfPresent + requireNotSuspended on the five gated routes</name>
  <files>../backend-services/carEx-services/server.js</files>
  <read_first>
    - ../backend-services/carEx-services/server.js lines 590-611 (PUT /api/brokers/:uid)
    - ../backend-services/carEx-services/server.js lines 645-668 (PUT /api/logistics/:uid)
    - ../backend-services/carEx-services/server.js lines 740-827 (POST /api/cars — note multer usage: `upload.array('images', 25)`)
    - ../backend-services/carEx-services/server.js lines 1087-1111 (POST /api/payments/create-payment-intent)
    - ../backend-services/carEx-services/server.js lines 1113-1143 (POST /api/payments/confirm-booking — middleware chain only; do not touch handler body, Plan 03-04 owns that)
    - .planning/phases/03-backend-enforcement-backend/03-PATTERNS.md section "server.js (MODIFIED)" — the exact middleware composition lines
    - .planning/phases/03-backend-enforcement-backend/03-CONTEXT.md D-02 (exactly these five routes)
  </read_first>
  <behavior>
    - `POST /api/cars` route definition starts: `app.post('/api/cars', attachAuthIfPresent, requireNotSuspended('create_listing'), upload.array('images', 25), async (req, res) => {...existing body...});`
    - `POST /api/payments/create-payment-intent` starts: `app.post('/api/payments/create-payment-intent', attachAuthIfPresent, requireNotSuspended('create_order'), async (req, res) => {...existing body...});`
    - `POST /api/payments/confirm-booking` starts: `app.post('/api/payments/confirm-booking', attachAuthIfPresent, requireNotSuspended('create_order'), async (req, res) => {...existing body unchanged in THIS plan — Plan 03-04 replaces it...});`
    - `PUT /api/brokers/:uid` starts: `app.put('/api/brokers/:uid', attachAuthIfPresent, requireNotSuspended('update_profile'), async (req, res) => {...existing body...});`
    - `PUT /api/logistics/:uid` starts: `app.put('/api/logistics/:uid', attachAuthIfPresent, requireNotSuspended('update_profile'), async (req, res) => {...existing body...});`
    - No OTHER routes in server.js gain new middleware in this plan (no PUT /api/cars/:id, no /users/:uid/*, no /orders/:id/status, etc.).
    - `node --check server.js` passes.
    - Route handler bodies are byte-identical to pre-edit state (diff only shows the prepended middleware args).
  </behavior>
  <action>
    For each of the five routes below, perform an Edit that changes ONLY the `app.<verb>(path, ...)` argument list by inserting `attachAuthIfPresent, requireNotSuspended('<capability>'), ` immediately after the path string and before the existing next argument. Use absolute path + capability mapping:

    | Route | Capability | Middleware position |
    |-------|------------|---------------------|
    | `app.post('/api/cars', upload.array('images', 25), async (req, res) => {...})` | `create_listing` | insert between path and `upload.array(...)` |
    | `app.post('/api/payments/create-payment-intent', async (req, res) => {...})` | `create_order` | insert between path and handler |
    | `app.post('/api/payments/confirm-booking', async (req, res) => {...})` | `create_order` | insert between path and handler — do NOT touch handler body in this plan |
    | `app.put('/api/brokers/:uid', async (req, res) => {...})` | `update_profile` | insert between path and handler |
    | `app.put('/api/logistics/:uid', async (req, res) => {...})` | `update_profile` | insert between path and handler |

    Final shape for each route (example POST /api/cars):
    ```
    app.post(
      '/api/cars',
      attachAuthIfPresent,
      requireNotSuspended('create_listing'),
      upload.array('images', 25),
      async (req, res) => {
        // ...existing handler body unchanged...
      }
    );
    ```

    Do NOT:
    - Modify any other routes. Scope discipline per D-02.
    - Change the handler body (argument, body, try/catch, response shape) of any of the five routes. Middleware prepend only.
    - Reorder attachAuthIfPresent and requireNotSuspended — order matters: attachAuthIfPresent runs first to populate req.auth when Bearer is present, then requireNotSuspended resolves uid with Bearer-first precedence.
    - Add the gate to PUT /api/cars/:id or any other neighbor route (those are explicitly deferred per CONTEXT.md §Deferred Ideas).
    - Remove or rename multer's `upload.array('images', 25)` on POST /api/cars — it stays, just moves one position to the right in the arg list.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services &amp;&amp; node --check server.js &amp;&amp; grep -n "app.post('/api/cars'" server.js &amp;&amp; grep -c "requireNotSuspended('create_listing')" server.js &amp;&amp; grep -c "requireNotSuspended('create_order')" server.js &amp;&amp; grep -c "requireNotSuspended('update_profile')" server.js &amp;&amp; grep -c "attachAuthIfPresent" server.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check server.js` exits 0.
    - `grep -c "requireNotSuspended('create_listing')" server.js` returns exactly 1 (only POST /api/cars).
    - `grep -c "requireNotSuspended('create_order')" server.js` returns exactly 2 (POST /api/payments/create-payment-intent AND POST /api/payments/confirm-booking).
    - `grep -c "requireNotSuspended('update_profile')" server.js` returns exactly 2 (PUT /api/brokers/:uid AND PUT /api/logistics/:uid).
    - `grep -c "attachAuthIfPresent," server.js` returns exactly 5 (one per gated route, used as a middleware arg).
    - `grep -c "requireNotSuspended(" server.js` returns exactly 5 (total across all five gated routes).
    - `grep -n "app.post('/api/cars'" server.js` — the matched line (or subsequent 3 lines in multi-line form) contains BOTH `attachAuthIfPresent` AND `requireNotSuspended('create_listing')` AND `upload.array` — verify via `grep -A 3 "app.post('/api/cars'" server.js | grep -c "upload.array"` returns 1.
    - `git diff server.js` shows no deletions of handler body lines in the five gated routes (only additions of middleware args).
    - No other `requireNotSuspended(` or `attachAuthIfPresent,` occurrences elsewhere in server.js (scope discipline enforced by the exact count assertions above).
  </acceptance_criteria>
  <done>All five ROADMAP-named routes have attachAuthIfPresent + requireNotSuspended(capability) prepended with the correct capability token; no other routes are gated; handler bodies unchanged; node --check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client → five gated routes | attachAuthIfPresent populates req.auth only when Bearer present; requireNotSuspended enforces. |
| existing handler body → req.callerUser | Downstream handler may trust req.callerUser (set by requireNotSuspended). No handler in this plan is modified, so no new trust-in-callerUser surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | E (Elevation) | Non-gated user-write routes | accept (tracked) | CONTEXT.md §Deferred Ideas enumerates PUT /api/cars/:id, PATCH /api/cars/:id/status, /users/:uid/*, etc. as known-not-gated. Follow-up ticket. Hybrid cutover per D-02. |
| T-03-03-02 | S (Spoofing) | POST /api/cars body-uid fallback | accept (transitional) | Same as Plan 03-02 T-03-02-01; removed in Phase 6 QUAL-03. |
| T-03-03-03 | T (Tampering) | Middleware ordering | mitigate | attachAuthIfPresent MUST precede requireNotSuspended; otherwise req.auth is not set before uid resolution. Acceptance-criteria grep-A enforces multer follows requireNotSuspended on POST /api/cars, confirming order. |
| T-03-03-04 | I (Info Disclosure) | Inline schema removal | mitigate | After Task 1, all Car/Broker/LogisticsPartner find* calls in server.js resolve to Plan 03-01 models with pre-hooks active. Default hide-safely covers ROADMAP Criterion #2 for GET routes without further edits. |
| T-03-03-05 | D (DoS) | Fivefold increase in gated-route latency | accept | Each gated request adds one indexed User.findOne. Expected scale well within latency budget. |
</threat_model>

<verification>
- ROADMAP Success Criterion #1 (403 on user-write for suspended user across five routes) is PROVEN by Plan 03-06 acceptance.test.js Block 1, which posts to each of the five routes with a blocked_with_review user.
- ROADMAP Success Criterion #2 (GET /api/cars hides then restores on suspend/unsuspend) is PROVEN by Plan 03-06 hideOnFind.test.js + acceptance.test.js Block 2. Both tests depend on the Plan 03-01 hooks being the ONLY Car model — Task 1's inline-schema deletion delivers that.
- ROADMAP Success Criterion #4 (feature_limited selectivity) is PROVEN by Plan 03-06 acceptance.test.js Block 4, which hits POST /api/cars (blocked via `create_listing`) and GET /api/cars (allowed) for the same user.
</verification>

<success_criteria>
- `server.js` contains the three `require('./src/models/{Car,Broker,LogisticsPartner}')` statements at the top-of-file and no inline schemas for those three models.
- `server.js` has exactly 5 `requireNotSuspended(...)` occurrences with the capability tokens distributed per D-02.
- `server.js` has exactly 5 `attachAuthIfPresent,` occurrences, each immediately preceding the matching `requireNotSuspended(...)`.
- `node --check server.js` passes.
- No routes other than the five listed are gated.
</success_criteria>

<output>
After completion, create `.planning/phases/03-backend-enforcement-backend/03-03-SUMMARY.md`.
</output>
