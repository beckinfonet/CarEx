---
phase: 15-broadcast-new-listing-notifications
plan: 05
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - "src/screens/NotificationSettingsScreen.tsx"
  - "src/constants/translations.ts"
  - "src/screens/__tests__/NotificationSettingsScreen.test.tsx"
autonomous: true
requirements: ["Req 5"]
repo: mobile
repo_path: /Users/beckmaldinVL/development/mobileApps/carEx

must_haves:
  truths:
    - "NotificationSettingsScreen shows a third 'New Listings' toggle, defaulting ON for a user who never set it"
    - "Toggling it persists notificationPrefs.newListingEnabled via AuthService.updateBackendUser"
    - "The toggle is disabled when muteAll is on, mirroring the existing watch toggle"
    - "The RU label is 'Новые объявления', EN 'New listings'"
  artifacts:
    - path: "src/screens/NotificationSettingsScreen.tsx"
      provides: "New Listings category toggle (near-copy of the watch toggle)"
      contains: "newListingEnabled"
    - path: "src/constants/translations.ts"
      provides: "categoryNewListings RU+EN label"
      contains: "categoryNewListings"
  key_links:
    - from: "onToggleNewListing"
      to: "AuthService.updateBackendUser"
      via: "persistPrefs({ newListingEnabled })"
      pattern: "newListingEnabled"
---

<objective>
Add the third "New Listings" toggle to `NotificationSettingsScreen` (Req 5 / D-11), defaulting ON (opt-out), with the
RU "Новые объявления" / EN "New listings" label, persisting through the existing `persistPrefs` →
`AuthService.updateBackendUser` path. This is the ONLY mobile work in the phase — it lives in THIS repo (carEx),
separate from all backend plans.

Purpose: Surfaces the server-side `newListingEnabled` pref (added in 15-02) so users can opt out. Inert until the
backend PUT fix (15-02 R-02) persists prefs — but the mobile change is independent and committable now in this repo.
Output: edited `NotificationSettingsScreen.tsx`, `translations.ts`, and the existing screen test (extended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**SPLIT-REPO:** This is the carEx (mobile) repo — the current working tree. Commit HERE. The backend repo is untouched
by this plan. Mobile and backend are SEPARATE repos; this commit cannot span both.
</execution_context>

<context>
@.planning/phases/15-broadcast-new-listing-notifications/15-SPEC.md
@.planning/phases/15-broadcast-new-listing-notifications/15-RESEARCH.md

<interfaces>
<!-- Ground-truth from carEx mobile source this session. -->

NotificationSettingsScreen.tsx (the watch toggle to MIRROR exactly):
  - interface NotificationPrefs (:65-69): { muteAll?, savedSearchEnabled?, watchEnabled?, ... } — ADD newListingEnabled?: boolean.
  - state (:104-106): const [watchEnabled, setWatchEnabled] = useState<boolean>(prefs.watchEnabled ?? true);
  - handler (:227-233): const onToggleWatch = useCallback((value)=>{ setWatchEnabled(value); persistPrefs({ watchEnabled:value }); }, [persistPrefs]);
  - render row (:405-415): <View style={styles.toggleRow}><Text style={styles.toggleLabel}>{t.categoryWatchedCars}</Text>
      <Switch value={watchEnabled} disabled={muteAll} onValueChange={onToggleWatch} trackColor={{false:COLORS.border,true:COLORS.accent}}/></View>
  - persistPrefs (:193-209): merges { ...prefs, ...patch } and calls
      AuthService.updateBackendUser(user.localId, { notificationPrefs: merged }); then refreshUser().

translations.ts category keys: RU categorySavedSearches/categoryWatchedCars at :1022-1023; EN at :2089-2090.
  ADD categoryNewListings in BOTH blocks (RU 'Новые объявления', EN 'New listings').

Existing test to MIRROR/extend: src/screens/__tests__/NotificationSettingsScreen.test.tsx
  — echo-key Proxy for t, mockUpdateBackendUser spy on AuthService, react-test-renderer + act. mockUser starts
  { localId:'u-1', notificationPrefs:{} } so an absent newListingEnabled must render the toggle ON (default).

D-11: never-touched = enabled; muteAll still suppresses everything (toggle disabled under muteAll, like watch).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the New Listings toggle + categoryNewListings label (near-copy of the watch toggle)</name>
  <files>src/screens/NotificationSettingsScreen.tsx, src/constants/translations.ts</files>
  <behavior>
    - A user with notificationPrefs.newListingEnabled absent renders the toggle in the ON position (default true).
    - Toggling OFF calls AuthService.updateBackendUser with notificationPrefs containing newListingEnabled:false.
    - The toggle is disabled when muteAll is true (mirrors watch).
  </behavior>
  <read_first>
    - src/screens/NotificationSettingsScreen.tsx:65-69 (NotificationPrefs interface), :104-106 (watch state), :227-233 (onToggleWatch), :405-415 (watch render row), :193-209 (persistPrefs)
    - src/constants/translations.ts:1022-1023 (RU category labels), :2089-2090 (EN category labels)
    - 15-RESEARCH.md Code Example 4 (the exact toggle additions)
  </read_first>
  <action>
    Mirror the watch toggle three places, immediately after the Watched-cars equivalents:
      - Extend the local `NotificationPrefs` interface with `newListingEnabled?: boolean`.
      - Add state: `const [newListingEnabled, setNewListingEnabled] = useState<boolean>(prefs.newListingEnabled ?? true);`
        (the `?? true` is the never-touched=enabled default, D-11).
      - Add handler: `const onToggleNewListing = useCallback((value: boolean) => { setNewListingEnabled(value);
        persistPrefs({ newListingEnabled: value }); }, [persistPrefs]);`
      - Add the render row in the per-category group AFTER the Watched-cars row, using `t.categoryNewListings`,
        `value={newListingEnabled}`, `disabled={muteAll}`, `onValueChange={onToggleNewListing}`, same trackColor.
    In translations.ts add `categoryNewListings` to BOTH blocks: RU `'Новые объявления'`, EN `'New listings'`
    (RU-first, KGS/Kyrgyzstan audience — no Russia-specific terms; this generic label is fine). Reuse the existing
    persistPrefs path — do NOT add a new AuthService method (milestone constraint: extend, don't add networking).
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/carEx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "NotificationSettings|translations" || echo "TS-CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compiles with no new errors in the two edited files.
    - grep `newListingEnabled` appears in NotificationSettingsScreen.tsx (interface, state, handler, row); `categoryNewListings` appears in both RU and EN blocks of translations.ts.
    - The new toggle uses `disabled={muteAll}` and persists via persistPrefs (no new networking method).
  </acceptance_criteria>
  <done>Third toggle wired with categoryNewListings RU/EN labels, default ON, disabled under muteAll, persists via existing path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend NotificationSettingsScreen.test.tsx — default-ON render + persist on toggle</name>
  <files>src/screens/__tests__/NotificationSettingsScreen.test.tsx</files>
  <behavior>
    - With mockUser.notificationPrefs = {} (no newListingEnabled), the New Listings Switch renders value=true (default ON).
    - Toggling the New Listings Switch off calls AuthService.updateBackendUser with a notificationPrefs whose newListingEnabled === false.
  </behavior>
  <read_first>
    - src/screens/__tests__/NotificationSettingsScreen.test.tsx (full — mockUpdateBackendUser spy, echo-key t Proxy, act/TestRenderer harness to extend)
    - src/screens/NotificationSettingsScreen.tsx:405-415 (the rendered Switch the test queries)
  </read_first>
  <action>
    Add two tests to the existing file (reuse its mocks — do NOT create a new file):
      - "New Listings toggle defaults ON for never-set user": with mockUser.notificationPrefs = {}, render and find
        the Switch whose adjacent label key is `categoryNewListings` (echo-key proxy makes the key the visible text);
        assert its `value` prop is true.
      - "Toggling New Listings off persists newListingEnabled:false": fire onValueChange(false) on that Switch inside
        act(); assert mockUpdateBackendUser was called with (user.localId, { notificationPrefs: expect.objectContaining({ newListingEnabled: false }) }).
    Mirror how the existing watch/saved-search assertions locate their Switch + assert the persistence spy.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/carEx && npx jest src/screens/__tests__/NotificationSettingsScreen.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The two new tests PASS (default-ON render + persist-on-toggle), and the pre-existing tests in the file still PASS.
    - The persist assertion checks `newListingEnabled: false` reaches AuthService.updateBackendUser under a `notificationPrefs` object.
  </acceptance_criteria>
  <done>Screen test extended; default-ON + persist-on-toggle GREEN; existing assertions intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile toggle → PUT /api/users/:uid | Client sends a notificationPrefs patch; server (15-02) allowlists it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering (mass-assignment) | toggle persistence | mitigate (server-side) | Mobile sends only known pref keys; the server allowlist (15-02 Task 3) is the authoritative guard — mobile is a thin client, never trusted for audience/enforcement |
| T-15-09 | Spoofing (audience computed client-side) | recipient eligibility | accept | Mobile only persists the user's OWN pref; recipient resolution + enforcement is 100% server-side (15-04). No client trust for audience. |
</threat_model>

<verification>
- `npx jest src/screens/__tests__/NotificationSettingsScreen.test.tsx` GREEN (new + existing).
- `npx tsc --noEmit` clean for the edited files.
- `npm run lint` shows no NEW errors in the edited files (note: 3 pre-existing NotificationSettings lint errors are documented/deferred per STATE — do not "fix" them here).
</verification>

<success_criteria>
NotificationSettingsScreen renders a third "New Listings" toggle (RU "Новые объявления" / EN "New listings"), default ON for never-set users, disabled under muteAll, persisting newListingEnabled via the existing AuthService.updateBackendUser path — proven by the extended screen test, with no new TS/lint errors.
</success_criteria>

<output>
After completion, create `.planning/phases/15-broadcast-new-listing-notifications/15-05-SUMMARY.md`. This work is committed in the MOBILE (carEx) repo — record THIS repo's commit SHA (distinct from the backend SHAs in 15-01..15-04).
</output>
