---
phase: 15-broadcast-new-listing-notifications
plan: 04
type: execute
wave: 2
depends_on: ["15-01", "15-02", "15-03"]
files_modified:
  - "backend(carEx-services): src/notifications/notificationService.js"
autonomous: true
requirements: ["Req 1", "Req 2", "Req 3", "Req 4", "Req 6"]
repo: backend
repo_path: /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services

must_haves:
  truths:
    - "Creating a new active listing resolves a broadcast audience = DeviceToken distinct-uids, pref-eligible, actor-excluded, saved-search-excluded"
    - "Each eligible non-capped recipient gets exactly one in-app Notification row + one OS push to carex://search"
    - "A recipient at their daily broadcast cap gets the in-app row (pushSuppressed:true) but no push; cap counts since the Asia/Bishkek 08:00 boundary"
    - "A listing hidden/non-active between create and send produces zero broadcast rows/pushes; the seller never gets a broadcast for their own listing"
  artifacts:
    - path: "src/notifications/notificationService.js"
      provides: "new_listing_broadcast branch inside emit() after the saved-search pass, reusing the hide-hook'd visible Car"
      contains: "new_listing_broadcast"
  key_links:
    - from: "emit() broadcast branch"
      to: "DeviceToken.distinct('uid') + User.find pref filter"
      via: "audience resolution"
      pattern: "DeviceToken\\.distinct"
    - from: "emit() broadcast branch"
      to: "fcm.send({ uid, titleKey:'new_listing' })"
      via: "per-uid push when under cap"
      pattern: "fcm\\.send"
    - from: "cap count"
      to: "Notification.countDocuments pushSuppressed:{$ne:true}"
      via: "date-bounded count since bishkekMorningBoundary(now)"
      pattern: "pushSuppressed"
---

<objective>
Implement the broadcast branch inside `emit()` — the core of Phase 15. After the existing saved-search pass (and
after the hide-hook re-read it reuses), resolve an all-token-holders audience from `DeviceToken`, filter by the new
`newListingEnabled`/`muteAll` prefs, exclude the actor and the saved-search-matched uids (saved-search wins), enforce
a per-user daily push cap keyed on `pushSuppressed` + the Asia/Bishkek 08:00 boundary, write one in-app row per
recipient (uncapped), and send one OS push per under-cap recipient via the existing `fcm.send`. Backend-repo-only,
single file. Covers Reqs 1,2,3,4,6.

Purpose: This is the only genuinely new logic in the phase (audience query + cap count + branch wiring). Everything
else it calls (fcm.send, DeviceToken, the new fields/copy) already exists from 15-02/15-03.
Output: edited `notificationService.js`. Makes `broadcast.test.js` + the `guards.test.js` broadcast assertions GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**SPLIT-REPO:** Backend repo only — `cd /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services`
(branch `main`). Commit there. Do NOT touch the carEx working tree.
</execution_context>

<context>
@.planning/phases/15-broadcast-new-listing-notifications/15-SPEC.md
@.planning/phases/15-broadcast-new-listing-notifications/15-RESEARCH.md
@.planning/phases/15-broadcast-new-listing-notifications/15-VALIDATION.md

<interfaces>
<!-- Ground-truth from notificationService.js + collaborators this session. -->

emit() TODAY (notificationService.js:149-219):
  (a) hide-hook re-read: `const visible = await Car.findById(carId); if (!visible || visible.status !== 'active') return [];`  // lines 165-168 — NO bypass flags (grep-gated)
  (b) `const targets = await resolveTargets(event, visible, deps);`
  (c) actor-exclude: `const filtered = targets.filter((t) => t.sub && t.sub.uid && t.sub.uid !== event.actorUid);`  // line 174
  (d) write loop: `const uid = target.sub.uid;` (180), dedupeKey `${carId}:${rowEventType}` (182),
      Notification.findOne dedup, Notification.create, `if (cadence==='instant') await fcm.send({uid,title:keys.titleKey,data:target.data})`.
  returns `written`.
  Exports: module.exports = { emit, buildWatchDeeplink, buildSearchDeeplink, resolveTargets, WATCH_EVENTS };

resolveQuery helper (notificationService.js:46) unwraps a mongoose query/promise — use it for the new queries too.

Collaborators (from 15-02/15-03, now present):
  - DeviceToken.distinct('uid') → string[] of token-holding uids (uid-indexed). deps.DeviceToken || mongoose.model('DeviceToken').
  - User.find({ firebaseUid:{$in}, 'notificationPrefs.muteAll':{$ne:true}, 'notificationPrefs.newListingEnabled':{$ne:false} })
      .select('firebaseUid notificationPrefs.dailyCap language').lean()  → eligible recipients (legacy docs = enabled via $ne).
      MUST select dailyCap (R-01) AND language (so fcm.send renders RU/EN per recipient — without it lang=undefined falls back to RU and EN users get Russian copy). deps.User || mongoose.model('User').
  - Notification.countDocuments({ uid, kind:'new_listing', pushSuppressed:{$ne:true}, createdAt:{$gte: boundary} }) → today's sent broadcast pushes.
  - Notification.create([{ uid, kind:'new_listing', titleKey:'new_listing', bodyKey:'new_listing', params:{}, data, dedupeKey, pushSuppressed }]).
  - fcm.send({ uid, titleKey:'new_listing', lang, data }) — renders push_new_listing, fans out per-uid DeviceToken, never throws.

Constants/keys this branch introduces: eventType `new_listing_broadcast`; dedupeKey `${carId}:new_listing_broadcast`
  (must NOT equal `${carId}:new_match`); deeplink `carex://search`; in-app/push key literal `new_listing`.

Bishkek boundary (from digest.js): DIGEST_HOUR = 8; Asia/Bishkek fixed +06:00, no DST. bishkekMorningBoundary(now)
  = most-recent 08:00 Asia/Bishkek instant <= now (see 15-RESEARCH Pattern 4 for the exact +360min math).

uidOf widening (15-RESEARCH Pattern 2): the existing t.sub.uid reads at 174/180/198/204 must accept the new
  {uid}-shaped broadcast target OR stay isolated in a parallel branch — see Task 1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the bishkek boundary helper + cap constant plumbing; widen target reads for the {uid} shape</name>
  <files>backend(carEx-services): src/notifications/notificationService.js</files>
  <behavior>
    - bishkekMorningBoundary(now) returns the most-recent 08:00 Asia/Bishkek instant <= now (fixed +06:00, no DST).
    - The existing saved-search/watch paths still pass guards.test.js / actorExclusion.test.js / dedup.test.js (no regression).
  </behavior>
  <read_first>
    - src/notifications/notificationService.js:149-219 (emit body + the four t.sub.uid reads at 174/180/198/204)
    - src/notifications/digest.js:48,114 (DIGEST_HOUR=8 + the Asia/Bishkek boundary discipline to mirror)
    - 15-RESEARCH.md Pattern 2 (uidOf widening) + Pattern 4 (bishkekMorningBoundary math)
  </read_first>
  <action>
    Two structural prerequisites for the broadcast branch, no behavior change to existing paths yet:
      1. Add a module-local `bishkekMorningBoundary(now)` helper computing the most-recent 08:00 Asia/Bishkek
         instant <= now using the fixed +06:00 offset (no TZ lib — milestone constraint; no new cron). Reuse the
         DIGEST_HOUR value (import/derive from digest.js or define the same literal 8 with a comment pointing at
         digest.js so they share the clock — D-04). Per R-01 there is NO BROADCAST_DAILY_CAP=5 constant; the cap is
         read per-recipient from notificationPrefs.dailyCap (fallback 3).
      2. Introduce a `uidOf = (t) => t.uid || (t.sub && t.sub.uid)` helper and widen the actor-exclude (line 174) to
         `targets.filter((t) => uidOf(t) && uidOf(t) !== event.actorUid)`, plus the write-loop reads: `const uid = uidOf(target)`,
         `const cadence = (target.sub && target.sub.cadence) || 'instant'`, `kind: (target.sub && target.sub.kind) || 'new_listing'`.
         This keeps the existing saved-search/watch targets working AND prepares for {uid}-shaped broadcast targets.
         (Pitfall 4: the current `t.sub &&` short-circuit would silently DROP a {uid} target — the widening fixes that.)
    Do NOT add the broadcast branch yet (Task 2). Do NOT touch the hide-hook re-read (165-168) or add any bypass flag.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services && npx jest src/notifications/__tests__/guards.test.js src/notifications/__tests__/actorExclusion.test.js src/notifications/__tests__/dedup.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - guards/actorExclusion/dedup tests still GREEN (existing saved-search/watch paths intact after the widening).
    - `bishkekMorningBoundary` exists and, for a known `now`, returns an instant whose Asia/Bishkek local time is 08:00 and is <= now.
    - The grep gate in guards.test.js still passes (no `.setOptions(`, no bypass-flag literal).
  </acceptance_criteria>
  <done>boundary helper + uidOf widening in place; all existing emit tests GREEN; no broadcast branch yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the new_listing_broadcast branch (audience, dedup, cap, row+push, guards reused)</name>
  <files>backend(carEx-services): src/notifications/notificationService.js</files>
  <behavior>
    - audience excludes actor (Req 1); saved-search-matched uid gets new_match only (Req 2).
    - each non-capped recipient → 1 row + 1 fcm.send to carex://search (Req 3); dedupeKey `${carId}:new_listing_broadcast` ≠ new_match.
    - over-cap → row with pushSuppressed:true, no fcm.send (Req 4); cap = recipient dailyCap (default 3) counted since bishkek boundary.
    - opt-out (newListingEnabled:false / muteAll) → no row/push; legacy doc (absent field) → enabled (Req 5 query semantics).
    - hidden/non-active car → 0 broadcast rows/pushes (Req 6); branch reuses the hide-hook'd `visible` Car.
  </behavior>
  <read_first>
    - src/notifications/notificationService.js:149-219 (insert the branch AFTER the saved-search write loop, BEFORE `return written;`; reuse `visible` from 165-168 and the `written` array)
    - src/notifications/push/fcm.js:174 (send({uid,titleKey,lang,data}) contract)
    - src/models/DeviceToken.js (uid index), src/models/User.js (notificationPrefs incl. new newListingEnabled + dailyCap), src/models/Notification.js (new pushSuppressed)
    - 15-RESEARCH.md Code Example 1 (the branch skeleton) + Pitfalls 2,3,4,5,6 — ⚠ Code Example 1 / Pattern 4 still show a stale `BROADCAST_DAILY_CAP = 5`; IGNORE that constant, use per-recipient `notificationPrefs.dailyCap ?? 3` per R-01
    - src/notifications/__tests__/broadcast.test.js (the spec this makes GREEN)
  </read_first>
  <action>
    Slot a `if (eventType === 'new_listing') { ... }` broadcast branch AFTER the existing saved-search write loop and
    BEFORE `return written;`, reusing the already-hide-hook'd `visible` Car (NEVER re-fetch with a bypass flag —
    grep-gated HARD CONSTRAINT; the branch sits after the `if (!visible || visible.status !== 'active') return [];`
    guard so a hidden listing yields nothing). Steps:
      - Saved-search-wins exclusion set: `const ssUids = new Set(written.map((r) => r.uid))` (those uids just got new_match — Req 2; document that for a brand-new car a pre-existing unread new_match cannot occur per A3/D-10).
      - Audience: `const tokenUids = await resolveQuery(DeviceToken.distinct('uid'))`; candidate = tokenUids minus
        event.actorUid (Req 1 actor-exclude) minus ssUids. If empty, return `written` unchanged.
      - Eligible: `User.find({ firebaseUid:{$in:candidate}, 'notificationPrefs.muteAll':{$ne:true},
        'notificationPrefs.newListingEnabled':{$ne:false} }).select('firebaseUid notificationPrefs.dailyCap language').lean()`
        — `$ne` so legacy docs (absent field) read as enabled (Req 5); MUST select dailyCap (R-01) AND language (R-01 cap + per-recipient push localization).
      - Per recipient loop (key off firebaseUid String everywhere — Pitfall 5; never User._id):
          * dedup: `Notification.findOne({ uid, dedupeKey:`${carId}:new_listing_broadcast`, read:false })` → if exists, continue (Pitfall 3 — distinct key, no collision with new_match).
          * cap: `const cap = (u.notificationPrefs && u.notificationPrefs.dailyCap) ?? 3` (R-01 fallback 3);
            `const sentToday = await Notification.countDocuments({ uid, kind:'new_listing', pushSuppressed:{$ne:true}, createdAt:{$gte: bishkekMorningBoundary(now)} })`; `const suppress = sentToday >= cap` (Pitfall 2 — count only pushSuppressed:{$ne:true}, NOT rows).
          * ALWAYS create the in-app row: `Notification.create([{ uid, kind:'new_listing', titleKey:'new_listing',
            bodyKey:'new_listing', params:{}, data:{ deeplink:'carex://search', carId:null, searchId:null },
            dedupeKey:`${carId}:new_listing_broadcast`, pushSuppressed: suppress, digestPending:false }])` — push-free, PII-free (D-06/D-08).
          * `if (!suppress) await fcm.send({ uid, titleKey:'new_listing', lang: u.language, data })` (Req 3; D-07 deeplink). Push lang from the recipient User.language — the .select above includes `language` (unconditional; without it EN recipients get RU copy).
          * `written.push(row)`.
      - `now` = `event.now || new Date()` (injectable for the cap tests).
    Add a `KEYS_BY_EVENT['new_listing_broadcast']` entry pointing at the `new_listing` keys if the codebase routes
    in-app render keys through KEYS_BY_EVENT (Research Open Q3 — for clarity; the branch supplies the literal keys directly regardless).
    Keep the whole branch inside the existing try/catch isolation at the emit site (server.js) — it already wraps emit.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services && npx jest src/notifications/__tests__/broadcast.test.js src/notifications/__tests__/guards.test.js 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `broadcast.test.js` GREEN on all 7 named tests (audience excludes actor; saved-search wins dedup; row + push per recipient; over cap suppresses push not row; cap counts since bishkek boundary; opt-out suppresses / legacy doc enabled; broadcast dedupeKey no collision).
    - `guards.test.js` GREEN: existing grep gate still passes (no `.setOptions(`, no bypass literal) AND the new broadcast hide-hook / `new_listing_broadcast`-presence assertions pass.
    - `npm test` (full backend) shows no NEW failures vs the pre-existing baseline (the known ServiceOrder.providerSnapshot failure may remain).
  </acceptance_criteria>
  <done>broadcast branch live in emit(); broadcast.test.js + guards.test.js GREEN; hide-hook reused with zero bypass flags; full suite shows no new regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new_listing event → broadcast audience | Server resolves recipients; actor + saved-search uids excluded server-side |
| Car state at create → Car state at send | TOCTOU window: a listing may hide/sell between create and send |
| broadcast push frequency → user device | Unbounded broadcasts would spam; cap gates it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Information disclosure (self-notify) | broadcast audience | mitigate | Actor excluded from candidate uids (Task 2); verified by "audience excludes actor" |
| T-15-03 | Tampering (TOCTOU hidden listing) | hide-hook re-read | mitigate | Branch sits after `visible.status !== 'active'` guard, reuses `visible`, NO bypass flag (grep-gated) — verified by guards.test.js extension |
| T-15-07 | DoS (cap-bypass spam) | daily push cap | mitigate | Server-side per-uid count by pushSuppressed since Bishkek boundary, cap = dailyCap pref; client never trusted — verified by "over cap suppresses push not row" |
| T-15-08 | Tampering (dedupeKey collision) | broadcast vs new_match | mitigate | Distinct `${carId}:new_listing_broadcast` key — verified by "broadcast dedupeKey no collision" |
</threat_model>

<verification>
- `npx jest src/notifications/__tests__/broadcast.test.js` GREEN (all 7).
- `npx jest src/notifications/__tests__/guards.test.js` GREEN (grep gate + broadcast assertions).
- `npm test` full backend suite: no new failures beyond the documented pre-existing one.
- grep `notificationService.js`: contains `new_listing_broadcast`, `DeviceToken.distinct`, `pushSuppressed`; still contains zero `.setOptions(` / `{ includeAll...` literals.
</verification>

<success_criteria>
emit() fans out a new active listing to the DeviceToken-distinct, pref-eligible, actor-excluded, saved-search-excluded audience; each non-capped recipient gets one in-app row + one carex://search push; over-cap recipients get the row (pushSuppressed:true) but no push with the cap = their dailyCap pref counted since the Asia/Bishkek 08:00 boundary; hidden/non-active listings and the seller produce zero broadcasts — all proven by broadcast.test.js + guards.test.js with the hide-hook reused and zero bypass flags.
</success_criteria>

<output>
After completion, create `.planning/phases/15-broadcast-new-listing-notifications/15-04-SUMMARY.md` (carEx planning tree). Record the BACKEND repo commit SHA and note this is the branch Railway deploys (backend main).
</output>
