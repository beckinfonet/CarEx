---
phase: 15-broadcast-new-listing-notifications
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - "backend(carEx-services): src/notifications/__tests__/broadcast.test.js"
  - "backend(carEx-services): src/notifications/__tests__/guards.test.js"
  - "backend(carEx-services): __tests__/users-prefs.test.js"
autonomous: true
requirements: ["Req 1", "Req 2", "Req 3", "Req 4", "Req 5", "Req 6"]
repo: backend
repo_path: /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services

must_haves:
  truths:
    - "Running broadcast.test.js fails with assertion errors (RED), not import/syntax errors"
    - "guards.test.js still passes its existing grep gate AND a new assertion covering the broadcast branch"
    - "users-prefs.test.js loads, boots in-memory Mongo, and passes (GREEN) — it inlines the FIXED handler verbatim per the userLanguage.test.js pattern, serving as the executable spec 15-02 Task 3's server.js edit must match"
  artifacts:
    - path: "src/notifications/__tests__/broadcast.test.js"
      provides: "DI-style behavior contract for audience/dedup/cap/copy/opt-out (Reqs 1-5,7)"
      contains: "audience excludes actor"
    - path: "__tests__/users-prefs.test.js"
      provides: "Server-handler test for PUT /api/users/:uid notificationPrefs persistence (Req 5 / R-02)"
      contains: "persists notificationPrefs"
  key_links:
    - from: "broadcast.test.js"
      to: "notificationService.emit"
      via: "DI emit(event, { Car, Notification, DeviceToken, User, fcm, matchSavedSearches })"
      pattern: "require\\(.*notificationService"
---

<objective>
Create the RED test scaffolds for the entire Phase-15 backend surface BEFORE any implementation, satisfying the Nyquist gate from 15-VALIDATION.md. This is a backend-repo-only plan (carEx-services).

Purpose: Every implementation task in 15-02/15-03/15-04 must have an automated proof that already exists and is RED. Without these files, those tasks would have `<automated>MISSING</automated>` verifications.
Output: `broadcast.test.js` (new, covers Reqs 1-5,7 + dedupeKey isolation), `guards.test.js` (extended for Req 6 broadcast-branch coverage), `__tests__/users-prefs.test.js` (new, Req 5 / R-02 prefs persistence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**SPLIT-REPO:** All work here is in the SEPARATE backend repo at
`/Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services`
(branch `main`, clean). `cd` into that repo first; commit there, NOT in carEx.
The carEx working tree is untouched by this plan.
</execution_context>

<context>
@.planning/phases/15-broadcast-new-listing-notifications/15-SPEC.md
@.planning/phases/15-broadcast-new-listing-notifications/15-RESEARCH.md
@.planning/phases/15-broadcast-new-listing-notifications/15-VALIDATION.md

<interfaces>
<!-- Ground-truth extracted from backend source this planning session. Use directly; no exploration needed. -->

emit() signature + DI (notificationService.js:149-222, exported at :222):
  module.exports = { emit, buildWatchDeeplink, buildSearchDeeplink, resolveTargets, WATCH_EVENTS };
  async function emit(event, deps = {})   // deps: { Car, Notification, fcm, DeviceToken, User, matchSavedSearches }
  event shape for this phase: { type: 'new_listing', carId, actorUid, now? }

Notification row fields TODAY (Notification.js): uid, kind, titleKey, bodyKey, params, data{deeplink,carId,searchId},
  read(default false), channels(['in_app']), digestPending(default false), dedupeKey(String), createdAt.
  NO pushSuppressed field yet — 15-02 adds it.

User.notificationPrefs TODAY (User.js:25-34): muteAll(false), savedSearchEnabled(true), watchEnabled(true),
  quietHours{start,end}, dailyCap(Number, default 3). NO newListingEnabled — 15-02 adds it.

DeviceToken (DeviceToken.js): { uid(String), token(unique), platform }, indexed { uid: 1 }.
  Recipient source of truth = DeviceToken.distinct('uid') (D-02).

fcm.send signature (fcm.js:174): async send({ uid, titleKey, title, lang='RU', data={} })
  — broadcast calls fcm.send({ uid, titleKey:'new_listing', lang, data }).

Existing DI test scaffold to MIRROR (guards.test.js:1-30): jest.mock('../push/fcm', ...);
  in-memory Notification stub with async findOne()/create(docs)→array; emit(event, stubDeps); no live DB.

Server-handler test to MIRROR for prefs (__tests__/userLanguage.test.js): builds a minimal express app that
  reproduces the PUT /api/users/:uid handler VERBATIM, backed by mongodb-memory-server + the real User model;
  uses supertest. Does NOT boot server.js.

PUT /api/users/:uid handler TODAY (server.js:542-563): allowlist =
  { firstName, lastName, phoneNumber, telegramUsername, avatarUrl, language }; notificationPrefs is OMITTED
  (the latent bug R-02 fixes). Updates findOneAndUpdate({ firebaseUid: req.params.uid }, update).

Existing grep gate (guards.test.js:83-90): asserts source of notificationService.js never contains
  `.setOptions(` and never matches /\{\s*includeAll(Users|ListingStatuses)/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold broadcast.test.js (RED) — audience, dedup, cap, opt-out, copy, dedupeKey isolation</name>
  <files>backend(carEx-services): src/notifications/__tests__/broadcast.test.js</files>
  <read_first>
    - src/notifications/__tests__/guards.test.js (DI/stub style to MIRROR — fcm mock + in-memory Notification stub + DI emit, lines 1-30)
    - src/notifications/__tests__/dedup.test.js (dedupeKey shape assertions)
    - src/notifications/notificationService.js:149-219 (emit guard order + the t.sub.uid reads at 174/180/198/204 the broadcast branch widens)
    - src/notifications/translations.js:26-40,58-62 (in-app new_match block + push_new_match pattern — broadcast copy mirrors)
  </read_first>
  <action>
    Create a Jest DI-style test (testEnvironment node, NO live DB) mirroring guards.test.js's stub harness:
    jest.mock('../push/fcm') with a `send` spy; provide in-memory stubs for Notification (findOne/create/countDocuments),
    DeviceToken (distinct), User (find→.select→.lean), Car (findById returning an active visible doc), and a
    matchSavedSearches stub. Invoke `emit({ type:'new_listing', carId:'car-1', actorUid:'seller-1', now:<fixed Date> }, stubDeps)`.
    Write these named tests (RED — they MUST fail with assertion errors against the current code that has no broadcast branch):
      - "audience excludes actor" (Req 1): seller-1 has a token but is NEVER in the resolved broadcast recipients;
        a non-seller token-holder IS. Assert via fcm.send spy calls + Notification.create rows by uid.
      - "saved-search wins dedup" (Req 2): a uid whose matchSavedSearches produced a written new_match row gets
        exactly one notification (the new_match), NOT a second broadcast row. Build the exclusion from the
        written saved-search rows' uids (ssUids).
      - "row + push per recipient" (Req 3): each eligible non-capped uid yields exactly one Notification.create
        AND one fcm.send({ uid, titleKey:'new_listing', data:{ deeplink:'carex://search' } }).
      - "over cap suppresses push not row" (Req 4 + D-06): with countDocuments stubbed to return >= the cap,
        the broadcast row is STILL created (pushSuppressed:true) but fcm.send is NOT called for that uid.
      - "cap counts since bishkek boundary" (Req 4 / R-01): the cap value used is the recipient User's
        notificationPrefs.dailyCap (assert the eligible-user query selects dailyCap; default 3 when unset);
        the countDocuments filter is restricted to pushSuppressed:{$ne:true} and createdAt:{$gte: <bishkek 08:00 boundary>}.
      - "opt-out suppresses; legacy doc enabled" (Req 5): the User.find pref filter uses
        'notificationPrefs.newListingEnabled':{$ne:false} and 'notificationPrefs.muteAll':{$ne:true}
        (a legacy doc with the field ABSENT is treated as enabled; an explicit false is excluded).
      - "broadcast dedupeKey no collision" (Req 3/7): the broadcast row's dedupeKey === `${carId}:new_listing_broadcast`
        and is asserted NOT equal to `${carId}:new_match`.
    Use the exact identifiers the implementation will use: eventType `new_listing_broadcast`, dedupeKey
    `${carId}:new_listing_broadcast`, field `pushSuppressed`, kind `new_listing`, deeplink `carex://search`,
    pref query operators `$ne:false`/`$ne:true`. Do NOT implement emit changes here — tests only.
    Reference 15-VALIDATION.md "Per-Task Verification Map" for the canonical `-t` test names.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services && npx jest src/notifications/__tests__/broadcast.test.js 2>&1 | grep -Eq "(failing|fail|✕|expect)" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists and is parseable (no SyntaxError / module-not-found): the run reaches assertions, then FAILS them (RED), not a load error.
    - Contains the 7 named tests above with the exact `-t` strings from 15-VALIDATION.md.
    - No `.only`, no watch-mode flag, no live DB (no mongodb-memory-server in this file — pure DI stubs).
  </acceptance_criteria>
  <done>broadcast.test.js exists, loads cleanly, and every assertion is RED against current notificationService.js.</done>
</task>

<task type="auto">
  <name>Task 2: Extend guards.test.js for the broadcast branch (Req 6 hide-hook + grep gate)</name>
  <files>backend(carEx-services): src/notifications/__tests__/guards.test.js</files>
  <read_first>
    - src/notifications/__tests__/guards.test.js (full file — the existing grep gate is at :83-90; preserve it verbatim)
    - src/notifications/notificationService.js:165-168 (the plain Car.findById hide-hook re-read the broadcast branch must reuse)
  </read_first>
  <action>
    Add two assertions WITHOUT weakening the existing grep gate (HARD CONSTRAINT — the gate at lines 83-90 asserting
    no `.setOptions(` and no `/\{\s*includeAll(Users|ListingStatuses)/` MUST stay green with the broadcast branch present):
      1. Behavior (Req 6): emit({ type:'new_listing', carId, actorUid }, deps) where Car.findById returns a
         non-active doc (status !== 'active') or null produces ZERO broadcast Notification.create calls AND zero
         fcm.send calls — proving the broadcast branch sits AFTER the existing `if (!visible || visible.status !== 'active') return [];`
         guard and reuses the hide-hook'd `visible` Car (does NOT re-fetch with a bypass).
      2. Source assertion: extend (do not replace) the grep gate so it also asserts the broadcast branch source
         contains the literal `new_listing_broadcast` (confirming the branch exists) while STILL matching zero
         bypass-flag object literals — i.e. the new branch is present yet the gate is still green.
    Mirror the existing DI stub style already in this file (fcm mocked, in-memory Notification stub).
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services && npx jest src/notifications/__tests__/guards.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The pre-existing grep-gate test ("emit pipeline source NEVER passes includeAllUsers...") still PASSES.
    - New behavior test is RED now (no broadcast branch yet) and will go GREEN after 15-04 — the hidden-car case is structurally asserted.
    - The new source assertion for `new_listing_broadcast` is RED now (literal absent), GREEN after 15-04.
  </acceptance_criteria>
  <done>guards.test.js extended; existing grep gate intact; new broadcast hide-hook + presence assertions added (RED until 15-04).</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold users-prefs.test.js (RED) — PUT /api/users/:uid persists notificationPrefs (Req 5 / R-02)</name>
  <files>backend(carEx-services): __tests__/users-prefs.test.js</files>
  <read_first>
    - __tests__/userLanguage.test.js (full file — VERBATIM-handler + mongodb-memory-server + supertest pattern to MIRROR exactly)
    - server.js:542-563 (the PUT /api/users/:uid handler whose allowlist OMITS notificationPrefs — the bug R-02 fixes)
    - src/models/User.js:25-34 (notificationPrefs sub-object the test asserts persistence against)
  </read_first>
  <action>
    Create a supertest + mongodb-memory-server handler test mirroring userLanguage.test.js. The test reproduces the
    FIXED PUT /api/users/:uid handler VERBATIM (the allowlisted-notificationPrefs version 15-02 ships) so the
    integration assertions exercise that handler code path directly. Because the inlined handler ALREADY contains the
    fix, this file is GREEN immediately — it is the executable SPEC that 15-02 Task 3's server.js edit must match
    byte-for-byte-in-behavior (not a RED gate against the current server.js). Write these tests:
      - "persists notificationPrefs" (Req 5): PUT with body { notificationPrefs: { newListingEnabled: false } }
        results in a User doc with notificationPrefs.newListingEnabled === false.
      - "partial patch does not clobber siblings" (R-02): seed a user with savedSearchEnabled:true; PUT only
        { notificationPrefs: { newListingEnabled: false } }; assert savedSearchEnabled STAYS true (dot-path $set,
        not whole-subobject overwrite).
      - "allowlist rejects unknown pref keys" (V5 mass-assignment / threat T-15-01): PUT
        { notificationPrefs: { hacked: 'x', muteAll: 'not-a-bool' } } persists neither `hacked` nor a non-boolean muteAll.
      - "IDOR-safe: keys on req.params.uid, ignores body uid" (V4): PUT to /api/users/u-1 with body
        { uid: 'u-2', notificationPrefs:{...} } updates u-1 only; u-2 is untouched.
    Use the known pref keys allowlist: muteAll, savedSearchEnabled, watchEnabled, newListingEnabled, quietHours, dailyCap.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services && npx jest __tests__/users-prefs.test.js 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File loads (mongodb-memory-server boots, User model requires cleanly) and runs assertions.
    - The four named tests exist; they encode the dot-path $set + allowlist + IDOR behavior R-02 requires.
    - This file is the executable spec for 15-02 Task 3 (server.js fix): it inlines the fixed handler and is GREEN immediately. 15-02 Task 3's done criteria require the real server.js handler to reproduce this behavior.
  </acceptance_criteria>
  <done>users-prefs.test.js exists, boots in-memory Mongo, and encodes notificationPrefs persistence + allowlist + IDOR assertions per R-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile client → PUT /api/users/:uid | Untrusted body crosses here; notificationPrefs sub-object could carry arbitrary fields |
| emit() event → broadcast audience | actor/saved-search uids must be excluded server-side; client never computes audience |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering (mass-assignment) | PUT /api/users/:uid notificationPrefs | mitigate | users-prefs.test.js asserts allowlist + type-check (only known boolean/number keys persist; "hacked"/non-bool rejected) — locks the spec 15-02 implements |
| T-15-02 | Information disclosure (self-notify) | broadcast audience | mitigate | broadcast.test.js "audience excludes actor" asserts seller never in recipients |
| T-15-03 | Tampering (TOCTOU hidden listing) | emit hide-hook re-read | mitigate | guards.test.js extension asserts non-active car → 0 broadcast rows/pushes, branch reuses `visible` |
| T-15-04 | Elevation/IDOR | PUT /api/users/:uid | mitigate | users-prefs.test.js "IDOR-safe" asserts update keys on req.params.uid, body uid ignored |
</threat_model>

<verification>
- `npx jest src/notifications/__tests__/broadcast.test.js` reaches assertions and is RED (no load error).
- `npx jest src/notifications/__tests__/guards.test.js` — existing grep gate green; new broadcast assertions present.
- `npx jest __tests__/users-prefs.test.js` boots in-memory Mongo and runs the prefs/allowlist/IDOR assertions.
- No `.only`, no watch flags in any new/edited test.
</verification>

<success_criteria>
All three test files exist in the backend repo, load without syntax/import errors, and encode the canonical `-t` assertions from 15-VALIDATION.md. broadcast.test.js + the guards.test.js additions are RED against current code (no broadcast branch); the existing guards grep gate stays GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/15-broadcast-new-listing-notifications/15-01-SUMMARY.md` (in the carEx planning tree). Note that the test files were committed in the BACKEND repo (carEx-services), with the backend commit SHA.
</output>
