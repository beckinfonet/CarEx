---
phase: 15-broadcast-new-listing-notifications
plan: 03
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - "backend(carEx-services): src/notifications/translations.js"
autonomous: true
requirements: ["Req 7"]
repo: backend
repo_path: /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services

must_haves:
  truths:
    - "A push_new_listing category title + canonical body exists in RU and EN with identical key sets"
    - "An in-app new_listing copy entry exists in RU and EN (distinct from new_match) with NO {param} tokens"
    - "Broadcast copy contains no listing PII (no make/model/price/seller)"
  artifacts:
    - path: "src/notifications/translations.js"
      provides: "RU+EN broadcast push + in-app copy following the push_* pattern"
      contains: "push_new_listing"
  key_links:
    - from: "fcm.send({ titleKey:'new_listing' })"
      to: "renderGenericPush('new_listing')"
      via: "push_new_listing key lookup (push_ prefix auto-applied)"
      pattern: "push_new_listing"
---

<objective>
Add the RU/EN broadcast copy (Req 7 / D-08) to the backend translations: a generic, PII-free `push_new_listing`
category (lock-screen push) plus an in-app `new_listing` entry distinct from the saved-search `new_match` copy.
Backend-repo-only, single file — runs in parallel with 15-02.

Purpose: 15-04's broadcast branch renders push copy via `fcm.send({ titleKey:'new_listing' })` →
`renderGenericPush('new_listing')` and writes in-app rows keyed `new_listing`. Those keys must exist first.
Output: edited `translations.js`. Auto-covered by the existing `__tests__/push-copy-parity.test.js` parity guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**SPLIT-REPO:** Backend repo only — `cd /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services`
(branch `main`). Commit there. Do NOT touch the carEx working tree.
</execution_context>

<context>
@.planning/phases/15-broadcast-new-listing-notifications/15-SPEC.md
@.planning/phases/15-broadcast-new-listing-notifications/15-RESEARCH.md

<interfaces>
<!-- Ground-truth from translations.js this session. -->

RU block (translations.js:26-62): in-app entries `new_match:{title,body}`, `price_drop`, `booked`, `sold`, ...
  push entries `push_new_match:{title:'Новый вариант по поиску', body:'Откройте, чтобы посмотреть.'}`,
  `push_price_drop:{...}`.
EN block (mirror, :86-109): same keys, English values.

CRITICAL: KEYS_BY_EVENT.new_listing TODAY maps to new_match keys
  (notificationService.js:54 → new_listing: { titleKey:'new_match', bodyKey:'new_match' }). That mapping is for the
  saved-search fan-out and STAYS. The BROADCAST branch (15-04) will use its OWN literal keys ('new_listing') for the
  in-app row + push — it does NOT go through KEYS_BY_EVENT.new_listing. So this plan adds NEW keys named exactly
  `new_listing` (in-app) and `push_new_listing` (push) without disturbing the existing new_match mapping.

renderGenericPush(key, lang) auto-prefixes `push_` — fcm.send({ titleKey:'new_listing' }) resolves push_new_listing.

PII/parity contract: zero `{param}` interpolation tokens in push copy; RU and EN key sets must be IDENTICAL.
  __tests__/push-copy-parity.test.js scans all push_* keys and FAILS the build on key drift or any `{param}` token.
  __tests__/notification-translations-parity.test.js enforces RU/EN in-app key parity.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add push_new_listing + in-app new_listing copy (RU + EN, PII-free, parity-clean)</name>
  <files>backend(carEx-services): src/notifications/translations.js</files>
  <read_first>
    - src/notifications/translations.js:26-62 (RU in-app new_match block + push_new_match/push_price_drop pattern)
    - src/notifications/translations.js:86-109 (EN mirror block)
    - __tests__/push-copy-parity.test.js (the parity + no-{param} guard that auto-covers the new push_ key)
    - 15-RESEARCH.md Pattern 5 (the exact copy strings to use)
  </read_first>
  <action>
    In BOTH the RU and EN blocks, add two new entries following the existing pattern, with EQUAL key sets across
    languages and ZERO `{param}` tokens (D-08 PII-free, lock-screen safe — no make/model/price/seller):
      - Push (category title + one canonical body), keyed `push_new_listing`:
          RU: { title: 'Новые объявления', body: 'Появились новые авто. Откройте, чтобы посмотреть.' }
          EN: { title: 'New listings', body: 'New cars just landed. Open to take a look.' }
      - In-app (distinct from new_match, still PII-free), keyed `new_listing`:
          RU: { title: 'Новые объявления', body: 'Появились новые авто. Откройте, чтобы посмотреть.' }
          EN: { title: 'New listings', body: 'New cars just landed. Open to take a look.' }
    Tone is neutral/informational (D-09 — NOT personality-tier-aware; server push has no tier signal). Use KGS/
    Kyrgyzstan-appropriate, Russia-neutral wording (no geo/regulatory terms — copy is generic so this is naturally
    satisfied). Do NOT alter the existing new_match/KEYS_BY_EVENT.new_listing mapping — these are purely additive keys.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services && npx jest __tests__/push-copy-parity.test.js __tests__/notification-translations-parity.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `push-copy-parity.test.js` and `notification-translations-parity.test.js` both GREEN (RU/EN key sets equal, no `{param}` token in push_new_listing).
    - grep finds `push_new_listing` in BOTH the RU and EN blocks; grep finds an in-app `new_listing` key in both.
    - No make/model/price/seller token anywhere in the new copy.
  </acceptance_criteria>
  <done>translations.js has RU+EN push_new_listing and in-app new_listing entries; parity tests GREEN; new_match mapping untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-rendered push copy → device lock screen | Copy is displayed pre-unlock; must carry no PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Information disclosure (PII on lock screen) | push_new_listing copy | mitigate | Generic param-free copy (no make/model/price/seller); push-copy-parity.test.js bans `{param}` tokens — verified |
</threat_model>

<verification>
- `npx jest __tests__/push-copy-parity.test.js __tests__/notification-translations-parity.test.js` GREEN.
- grep confirms push_new_listing + new_listing keys in both RU and EN blocks; no PII tokens.
</verification>

<success_criteria>
translations.js carries RU-first/EN-parity push_new_listing (push) and new_listing (in-app) copy with zero interpolation tokens and no listing PII; both parity tests pass; the existing new_match → KEYS_BY_EVENT mapping is unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/15-broadcast-new-listing-notifications/15-03-SUMMARY.md` (carEx planning tree). Record the BACKEND repo commit SHA.
</output>
