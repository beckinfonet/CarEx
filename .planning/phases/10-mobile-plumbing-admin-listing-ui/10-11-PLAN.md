---
phase: 10
plan: 11
type: execute
wave: 5
depends_on: [10-06, 10-08]
files_modified:
  - src/components/moderation/TypedConfirmationModal.tsx
  - src/screens/CarDetailsScreen.tsx
  - src/constants/translations.ts
  - src/components/moderation/__tests__/TypedConfirmationModal.test.tsx
  - src/screens/__tests__/CarDetailsScreen.admin.test.tsx
autonomous: true
gap_closure: true
closes_gaps:
  - "SC #1 + LUI-02 — Delete action presents a destructive-red confirmation dialog with appropriate copy"
requirements: [LUI-02]
must_haves:
  truths:
    - "Admin tapping Delete on a listing sees confirmation copy that says the LISTING (not the seller's profile) will be permanently deleted — i.e. the rendered body string maps to a new translation key `typedConfirmListingDeleteBody`, not `typedConfirmWarningBodyDelete`."
    - "Admin sees a hint that says 'Type the listing title {title} to confirm' — i.e. the rendered hint resolves from a new key `typedConfirmListingHint` with the {title} placeholder substituted by the listing title (NOT 'Type the user's email {email}…')."
    - "Admin sees a placeholder string referencing a listing-shaped example (e.g. '2018 Toyota Camry'), not 'email@example.com'."
    - "TypedConfirmationModal exposes additive override props (`bodyKey?`, `hintKey?`, `placeholderKey?`) — defaulting to the existing user-domain action map so Plan 10-06 / Plan 5 user-mod call sites remain byte-identical (no behavior change for `delete_profile` / `revoke_role` / `permanently_banned` without overrides)."
    - "CarDetailsScreen mounts TypedConfirmationModal with the three listing-specific keys; existing sentinel-match flow (case-insensitive trimmed equality) is unchanged because targetEmail still carries the listing title."
    - "Three new translation keys exist with RU+EN parity in `src/constants/translations.ts` — minimal on-touch addition matching Plan 10-06's precedent (full Phase 11 LQUAL-01 sweep is deferred)."
  artifacts:
    - path: "src/components/moderation/TypedConfirmationModal.tsx"
      provides: "Additive `bodyKey?` / `hintKey?` / `placeholderKey?` props; existing BODY_KEY_FOR_ACTION map remains the default for user-domain actions"
      contains: "bodyKey"
    - path: "src/constants/translations.ts"
      provides: "Three new RU+EN entries: typedConfirmListingDeleteBody, typedConfirmListingHint, typedConfirmListingPlaceholder"
      contains: "typedConfirmListingDeleteBody"
    - path: "src/screens/CarDetailsScreen.tsx"
      provides: "TypedConfirmationModal mount passes the three listing override keys instead of the user-domain default mapping"
      contains: "typedConfirmListingDeleteBody"
    - path: "src/components/moderation/__tests__/TypedConfirmationModal.test.tsx"
      provides: "Locks new override-prop behavior + regression-locks existing user-domain default mapping"
    - path: "src/screens/__tests__/CarDetailsScreen.admin.test.tsx"
      provides: "Extends T10 (or adds T15) to assert TypedConfirmationModal receives the listing override keys at mount time"
  key_links:
    - from: "CarDetailsScreen TypedConfirmationModal mount (lines ~1162-1180)"
      to: "TypedConfirmationModal bodyKey / hintKey / placeholderKey props"
      via: "JSX prop pass-through"
      pattern: "bodyKey=\"typedConfirmListingDeleteBody\""
    - from: "TypedConfirmationModal body/hint/placeholder render"
      to: "translations.ts typedConfirmListing* keys"
      via: "T[bodyKey ?? BODY_KEY_FOR_ACTION[action]] / T[hintKey ?? 'typedConfirmHint'] / T[placeholderKey ?? 'typedConfirmInputPlaceholder']"
      pattern: "typedConfirmListing"
    - from: "Existing user-mod surfaces (AdminModerationScreen delete_profile / revoke_role / permanently_banned mounts)"
      to: "TypedConfirmationModal default BODY_KEY_FOR_ACTION + typedConfirmHint + typedConfirmInputPlaceholder"
      via: "no override props passed — default branches preserve byte-identical behavior"
      pattern: "(no bodyKey / no hintKey / no placeholderKey on existing mounts)"
  forbidden:
    - "Do NOT remove or rename the existing `BODY_KEY_FOR_ACTION` map (Plan 5 user-mod call sites depend on its three-action mapping for default behavior)"
    - "Do NOT change the DestructiveAction union — adding a fourth `'delete_listing'` variant is an alternative shape but the chosen approach is override-props (additive), keeping the union scoped to user-domain destructive actions per Phase 4 D-07 sibling-domain discipline"
    - "Do NOT regress the existing 8 TypedConfirmationModal tests — the default mapping test ('warning body switches by action') MUST continue passing without modification"
    - "Do NOT add the override props as required (must be optional with sensible defaults so user-mod call sites need zero changes)"
    - "Do NOT introduce a global i18n sweep (Phase 11 LQUAL-01) — only the three new keys for this listing-delete copy are added in this plan"
    - "Do NOT change the sentinel-match semantics in TypedConfirmationModal — the `targetEmail` prop continues to carry the listing title, and case-insensitive trimmed equality stays as the match rule (D-08a)"
    - "Do NOT use `Bash(cat << 'EOF')` for any file changes — Edit tool only on existing files"
---

<objective>
Fix CR-01: TypedConfirmationModal currently renders user-profile-delete copy (`typedConfirmWarningBodyDelete` → "The provider profile will be permanently deleted") and an email-shaped hint + email placeholder when CarDetailsScreen mounts it for a LISTING delete with `action='delete_profile'` + `targetEmail={listingTitle}`. This is operationally dangerous — an admin reading the modal believes they are deleting the seller's account, not the listing.

Resolution: Add three additive override props to TypedConfirmationModal — `bodyKey?`, `hintKey?`, `placeholderKey?` — so listing-domain mounts can supply listing-specific translation keys without widening the `DestructiveAction` union. Plumb the resolution path through the new props with the existing user-domain maps as defaults. Add three new RU+EN translation keys. Update CarDetailsScreen's mount to pass the three new keys. Extend tests on both component and screen test files.

Purpose: Close the CR-01 BLOCKER from Phase 10's VERIFICATION.md. Without this fix, success criterion #1 + LUI-02 remain partial — admins see misleading copy while confirming a destructive action. The fix is strictly additive (no breaking changes to user-mod call sites) and matches the same additive-prop pattern Plan 10-06 already established for the `keyboardType?` prop, so future maintainers see a consistent extension pattern.

Output: ~30 lines changed in TypedConfirmationModal.tsx (3 props + 3 resolution lines), 6 new entries in translations.ts (3 keys × RU+EN), 3 props added to the CarDetailsScreen mount JSX, ~4 new tests in TypedConfirmationModal.test.tsx, 1-2 new assertions in CarDetailsScreen.admin.test.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-CONTEXT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-REVIEW.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-VERIFICATION.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-06-SUMMARY.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-08-SUMMARY.md

<interfaces>
<!-- The exact current shape of TypedConfirmationModal (after Plan 10-06's keyboardType addition). Override props mirror this pattern. -->

From src/components/moderation/TypedConfirmationModal.tsx (current):
  export type DestructiveAction = 'delete_profile' | 'revoke_role' | 'permanently_banned';

  export interface TypedConfirmationModalProps {
    visible: boolean;
    action: DestructiveAction;
    targetEmail: string;             // For listing flows: passes listing title via this prop (D-08 sentinel)
    submitting?: boolean;
    onConfirm: () => void;
    onClose: () => void;
    keyboardType?: KeyboardTypeOptions;  // Plan 10-06 — additive
  }

  const BODY_KEY_FOR_ACTION: Record<DestructiveAction, string> = {
    delete_profile: 'typedConfirmWarningBodyDelete',
    revoke_role: 'typedConfirmWarningBodyRevoke',
    permanently_banned: 'typedConfirmWarningBodyPermaBan',
  };

  // Current resolution lines (the ones this plan overrides):
  const hint = T.typedConfirmHint?.replace('{email}', targetEmail) ?? targetEmail;
  // <Text style={styles.warningBody}>{T[BODY_KEY_FOR_ACTION[action]]}</Text>
  // placeholder={T.typedConfirmInputPlaceholder}

From src/screens/CarDetailsScreen.tsx (current mount, lines 1162-1179):
  {typedConfirmVisible && fetchedCar && pendingDeletePayload && (
    <TypedConfirmationModal
      visible={true}
      action="delete_profile"             // ← This stays (no new union variant)
      targetEmail={listingTitle}          // ← This stays (sentinel)
      keyboardType="default"              // ← Plan 10-06's additive prop
      onConfirm={...}
      onClose={...}
      // bodyKey="typedConfirmListingDeleteBody"           ← NEW (this plan)
      // hintKey="typedConfirmListingHint"                 ← NEW (this plan)
      // placeholderKey="typedConfirmListingPlaceholder"   ← NEW (this plan)
    />
  )}

From src/constants/translations.ts (current — both blocks have these keys at lines 470-480 RU / 1163-1171 EN):
  RU block has: typedConfirmHint ("Введите email пользователя {email} для подтверждения"),
                typedConfirmInputPlaceholder ("email@example.com"),
                typedConfirmWarningBodyDelete, typedConfirmWarningBodyRevoke, typedConfirmWarningBodyPermaBan.
  EN block has the EN equivalents at corresponding indices.
  Both blocks need THREE new keys added side-by-side: typedConfirmListingDeleteBody, typedConfirmListingHint, typedConfirmListingPlaceholder.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `bodyKey?` / `hintKey?` / `placeholderKey?` override props to TypedConfirmationModal and add 3 new RU+EN translation keys</name>
  <files>src/components/moderation/TypedConfirmationModal.tsx, src/constants/translations.ts</files>
  <read_first>
    - src/components/moderation/TypedConfirmationModal.tsx (current shape — lines 11-38 props + union, line 30-34 BODY_KEY_FOR_ACTION map, line 58 hint resolution, line 78 body render, line 86 placeholder render)
    - src/constants/translations.ts (current structure — RU block typedConfirm* keys at lines 470-480; EN block at lines 1163-1171; the file uses a flat object with RU and EN top-level entries)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-CONTEXT.md (D-07 two-modal stack; D-08 listing title as sentinel; reason recommends override-props approach for Phase 11 LQUAL-03 grep-ability)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-REVIEW.md CR-01 section (lines 92-141 — exact diagnostic + fix shape; the recommended (b) approach mirrors what this task implements)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-VERIFICATION.md gaps[1] (`reason` and `missing` blocks — the verifier's recommended fix shape)
    - src/components/moderation/__tests__/TypedConfirmationModal.test.tsx (current 8 tests — note Test 5 "warning body switches by action" uses JSON.stringify substring match for the three existing default keys; must NOT regress)
  </read_first>
  <action>
    Step 1 — Extend `TypedConfirmationModalProps` in `src/components/moderation/TypedConfirmationModal.tsx`:
    - Add three OPTIONAL props after the existing `keyboardType?` line (preserve declaration order: `visible`, `action`, `targetEmail`, `submitting?`, `onConfirm`, `onClose`, `keyboardType?`, `bodyKey?`, `hintKey?`, `placeholderKey?`):
      - `bodyKey?: string` — overrides which `T[...]` key is rendered as the warning body. Default behavior unchanged: when undefined, falls back to `BODY_KEY_FOR_ACTION[action]`.
      - `hintKey?: string` — overrides which `T[...]` key is used for the hint (with the existing `{email}` placeholder substitution applied AFTER lookup). Default: `'typedConfirmHint'`.
      - `placeholderKey?: string` — overrides which `T[...]` key is used as the input placeholder. Default: `'typedConfirmInputPlaceholder'`.
    - Add a JSDoc comment above each prop in the same style as the existing `keyboardType` doc (lines 20-26) — reference Plan 10-11 CR-01 fix + Phase 11 LQUAL-03 grep-bait.

    Step 2 — Destructure the new props in the function signature with sensible defaults:
    ```
    bodyKey, hintKey, placeholderKey,
    ```
    Add to the existing destructure block (lines 36-39). No default values in the destructure — defaults are applied at the resolution site so `BODY_KEY_FOR_ACTION[action]` remains the user-domain fallback for `bodyKey`.

    Step 3 — Update the three resolution sites:
    - Hint (line 58): change `T.typedConfirmHint?.replace('{email}', targetEmail) ?? targetEmail` to `(T[hintKey ?? 'typedConfirmHint'] ?? '').replace('{email}', targetEmail) ?? targetEmail`. NOTE: this preserves the existing `{email}` substitution token — listing-flow callers still pass `targetEmail={listingTitle}` and the new `typedConfirmListingHint` RU+EN strings use `{title}` semantics in the human-readable copy but the substitution token in the value remains `{email}` to avoid a second template-engine change. ALTERNATIVE if cleaner: use `{title}` as the substitution token in the new listing keys AND extend the replace logic to handle both placeholders (`(T[hintKey] ?? '').replace('{email}', targetEmail).replace('{title}', targetEmail)`). Executor's discretion — document the choice in the SUMMARY.
    - Body (line 78): change `<Text style={styles.warningBody}>{T[BODY_KEY_FOR_ACTION[action]]}</Text>` to `<Text style={styles.warningBody}>{T[bodyKey ?? BODY_KEY_FOR_ACTION[action]]}</Text>`.
    - Placeholder (line 86): change `placeholder={T.typedConfirmInputPlaceholder}` to `placeholder={T[placeholderKey ?? 'typedConfirmInputPlaceholder']}`.

    Step 4 — Add the three new keys to `src/constants/translations.ts` in BOTH the RU and EN top-level blocks. Add them adjacent to the existing `typedConfirmWarningBodyPermaBan` line (after the existing user-domain typed-confirm copy) so a grep `typedConfirm` returns a contiguous block.
    - RU values:
      - `typedConfirmListingDeleteBody: 'Объявление будет удалено навсегда. История заказов сохраняется.'`
      - `typedConfirmListingHint: 'Введите название объявления {title} для подтверждения'`
      - `typedConfirmListingPlaceholder: '2018 Toyota Camry'`
    - EN values:
      - `typedConfirmListingDeleteBody: 'This listing will be permanently deleted. Order history is preserved.'`
      - `typedConfirmListingHint: 'Type the listing title {title} to confirm'`
      - `typedConfirmListingPlaceholder: '2018 Toyota Camry'`
    - NOTE on substitution token: if executor chose Option A in Step 3 (preserve `{email}` token in code), then the human-readable strings here should still use the human-friendly `{title}` placeholder — the code's `.replace('{email}', targetEmail)` won't match `{title}` and the literal `{title}` will show in the UI. THAT IS A BUG. Either (a) change the new RU/EN values to use `{email}` as the placeholder token (matches current code, ugly to read), or (b) use Option B in Step 3 and extend the replace to also handle `{title}`. RECOMMEND Option B for clarity; both options pass the acceptance criteria so executor picks.

    Step 5 — Verify the RU+EN parity: `grep -c "typedConfirmListingDeleteBody\|typedConfirmListingHint\|typedConfirmListingPlaceholder" src/constants/translations.ts` MUST return 6 (3 keys × 2 blocks).

    Step 6 — Do NOT modify `DestructiveAction` union, `BODY_KEY_FOR_ACTION` map, sentinel-match semantics (`normalized` / `targetNormalized` / `matches`), the `dirty` flag, `borderColor`, the `handleConfirm` gate, or any styles. All other behavior remains byte-identical.
  </action>
  <verify>
    <automated>npx jest src/components/moderation/__tests__/TypedConfirmationModal.test.tsx --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bodyKey\?" src/components/moderation/TypedConfirmationModal.tsx` returns ≥ 1 (prop declared as optional in the interface).
    - `grep -c "hintKey\?" src/components/moderation/TypedConfirmationModal.tsx` returns ≥ 1.
    - `grep -c "placeholderKey\?" src/components/moderation/TypedConfirmationModal.tsx` returns ≥ 1.
    - `grep -c "BODY_KEY_FOR_ACTION" src/components/moderation/TypedConfirmationModal.tsx` returns ≥ 2 (declaration + usage in default branch — pattern preserved).
    - `grep -c "typedConfirmListingDeleteBody" src/constants/translations.ts` returns exactly 2 (one RU, one EN).
    - `grep -c "typedConfirmListingHint" src/constants/translations.ts` returns exactly 2.
    - `grep -c "typedConfirmListingPlaceholder" src/constants/translations.ts` returns exactly 2.
    - Existing 8 TypedConfirmationModal tests continue to pass with zero modification.
    - `npx jest src/components/moderation/__tests__/TypedConfirmationModal.test.tsx --no-coverage` exits 0.
    - `npx tsc --noEmit -p .` exits 0 (no new TypeScript errors introduced).
  </acceptance_criteria>
  <done>
    TypedConfirmationModal exposes additive `bodyKey?` / `hintKey?` / `placeholderKey?` props. Defaults preserve user-mod call site behavior byte-identically. Three new RU+EN translation keys exist with parity. Existing tests stay green. TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire the new override keys into CarDetailsScreen's TypedConfirmationModal mount and extend tests on both component + screen test files</name>
  <files>src/screens/CarDetailsScreen.tsx, src/components/moderation/__tests__/TypedConfirmationModal.test.tsx, src/screens/__tests__/CarDetailsScreen.admin.test.tsx</files>
  <read_first>
    - src/screens/CarDetailsScreen.tsx lines 1162-1179 (current TypedConfirmationModal mount JSX — the JSX block this task modifies)
    - src/screens/CarDetailsScreen.tsx lines 477-535 (handleListingActionSubmit — for context on what fires after the typed-confirm succeeds; no changes here in this plan, but the executor must NOT touch this handler)
    - src/components/moderation/TypedConfirmationModal.tsx (after Task 1 — the new override props are now available)
    - src/components/moderation/__tests__/TypedConfirmationModal.test.tsx (after Task 1 — current 8 tests + 2 keyboardType tests from Plan 10-06; this task adds ~4 new tests)
    - src/screens/__tests__/CarDetailsScreen.admin.test.tsx test T10 (lines 426-458 — current Delete two-modal stack test; this task extends with assertion that the override keys are passed)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-REVIEW.md CR-01 §"Issue" (the misleading copy diagnostic — guides what the new tests must assert)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-CONTEXT.md D-07 (two-modal stack — this plan keeps the existing two-modal flow intact)
  </read_first>
  <action>
    Step 1 — In `src/screens/CarDetailsScreen.tsx`, update the TypedConfirmationModal mount JSX (currently lines 1162-1179) to pass the three new override props:
    ```
    <TypedConfirmationModal
      visible={true}
      action="delete_profile"
      targetEmail={listingTitle}
      keyboardType="default"
      bodyKey="typedConfirmListingDeleteBody"
      hintKey="typedConfirmListingHint"
      placeholderKey="typedConfirmListingPlaceholder"
      onConfirm={...}
      onClose={...}
    />
    ```
    The `action="delete_profile"` stays — D-07 two-modal stack semantics are preserved; the union does not widen (per planner's discretion documented in must_haves.forbidden). The three new props override the user-domain default copy at render time.

    Step 2 — Add a code comment above the mount block referencing this plan + CR-01:
    ```
    // Plan 10-11 CR-01 fix — pass listing-specific copy via override props so admin
    // sees "This listing will be permanently deleted" instead of the user-profile
    // delete copy. Sentinel match semantics unchanged (targetEmail still carries
    // listingTitle; case-insensitive trimmed equality per D-08a).
    ```

    Step 3 — Add ~4 new tests to `src/components/moderation/__tests__/TypedConfirmationModal.test.tsx` AFTER the existing `keyboardType` tests (currently lines 132-143). Use the same `makeProps` / `render` helpers (lines 10-44) and the existing JSON.stringify substring assertion pattern (lines 88-103):

    Test A — body override key:
    ```
    test('bodyKey override renders listing body key (CR-01 — Plan 10-11)', () => {
      const tree = render({ action: 'delete_profile', bodyKey: 'typedConfirmListingDeleteBody' });
      const json = JSON.stringify(tree.toJSON());
      expect(json).toContain('typedConfirmListingDeleteBody');
      expect(json).not.toContain('typedConfirmWarningBodyDelete'); // ← KEY assertion: user-profile copy is GONE
    });
    ```

    Test B — bodyKey default unchanged (regression-lock for user-mod):
    ```
    test('bodyKey default preserves user-domain BODY_KEY_FOR_ACTION mapping (no override = no behavior change)', () => {
      const tree = render({ action: 'delete_profile' }); // No bodyKey
      const json = JSON.stringify(tree.toJSON());
      expect(json).toContain('typedConfirmWarningBodyDelete'); // ← User-mod call sites continue to render this
    });
    ```

    Test C — hintKey override:
    ```
    test('hintKey override renders listing hint key (CR-01 — Plan 10-11)', () => {
      const tree = render({ targetEmail: '2018 Toyota Camry', hintKey: 'typedConfirmListingHint' });
      const json = JSON.stringify(tree.toJSON());
      expect(json).toContain('typedConfirmListingHint');
    });
    ```

    Test D — placeholderKey override (assert the resolved key is referenced; the mock useLanguage Proxy returns the key as the value):
    ```
    test('placeholderKey override sets the TextInput placeholder via the new key', () => {
      const tree = render({ placeholderKey: 'typedConfirmListingPlaceholder' });
      const input = tree.root.findByType(TextInput);
      expect(input.props.placeholder).toBe('typedConfirmListingPlaceholder');
    });
    ```

    Step 4 — Extend test T10 in `src/screens/__tests__/CarDetailsScreen.admin.test.tsx` (currently at lines 426-458, the Delete two-modal stack test) to assert that when the TypedConfirmationModal mount renders, it receives `bodyKey="typedConfirmListingDeleteBody"`. Add after the existing `typedConfirm` discovery line (~line 449-451):
    ```
    // Plan 10-11 CR-01 fix — assert listing-specific override props reach the modal
    expect(typedConfirm.props.bodyKey).toBe('typedConfirmListingDeleteBody');
    expect(typedConfirm.props.hintKey).toBe('typedConfirmListingHint');
    expect(typedConfirm.props.placeholderKey).toBe('typedConfirmListingPlaceholder');
    ```

    Step 5 — Do NOT modify `handleListingActionSubmit` (CarDetailsScreen.tsx:477-535), any other JSX in CarDetailsScreen, the FIXTURE_* test constants, or the test file's mock setup blocks. The change is strictly scoped to the TypedConfirmationModal JSX mount + co-located test extensions.
  </action>
  <verify>
    <automated>npx jest src/components/moderation/__tests__/TypedConfirmationModal.test.tsx src/screens/__tests__/CarDetailsScreen.admin.test.tsx --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "typedConfirmListingDeleteBody" src/screens/CarDetailsScreen.tsx` returns ≥ 1 (mount passes the override key).
    - `grep -c "typedConfirmListingHint" src/screens/CarDetailsScreen.tsx` returns ≥ 1.
    - `grep -c "typedConfirmListingPlaceholder" src/screens/CarDetailsScreen.tsx` returns ≥ 1.
    - `grep -c 'typedConfirmWarningBodyDelete' src/screens/CarDetailsScreen.tsx` returns 0 (the mount no longer relies on the user-domain default key for body resolution — it passes its own override).
    - `grep -c 'bodyKey:.*typedConfirmListingDeleteBody\|bodyKey)\.toBe' src/screens/__tests__/CarDetailsScreen.admin.test.tsx` returns ≥ 1 (test asserts the prop reaches the modal).
    - `npx jest src/components/moderation/__tests__/TypedConfirmationModal.test.tsx --no-coverage` exits 0 with at least 12 passing tests (8 existing + 2 keyboardType + ≥ 4 new).
    - `npx jest src/screens/__tests__/CarDetailsScreen.admin.test.tsx --no-coverage` exits 0 with all existing 13 tests still passing PLUS the extended T10 assertions.
    - `npx tsc --noEmit -p .` exits 0.
  </acceptance_criteria>
  <done>
    CarDetailsScreen mounts TypedConfirmationModal with the three listing override keys. Admin reads listing-delete copy when confirming a listing delete. Component tests lock both the override behavior AND the user-mod default behavior. Screen test asserts the override keys reach the mounted modal. All 13 existing admin tests stay green. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
- `grep -c 'typedConfirmWarningBodyDelete' src/screens/CarDetailsScreen.tsx` returns 0 (CR-01 mount no longer relies on user-domain default key for body resolution).
- `grep -c 'typedConfirmListingDeleteBody\|typedConfirmListingHint\|typedConfirmListingPlaceholder' src/screens/CarDetailsScreen.tsx` returns exactly 3 (one per override key).
- `grep -c 'typedConfirmListingDeleteBody\|typedConfirmListingHint\|typedConfirmListingPlaceholder' src/constants/translations.ts` returns exactly 6 (RU+EN parity).
- `grep -c 'bodyKey\?\|hintKey\?\|placeholderKey\?' src/components/moderation/TypedConfirmationModal.tsx` returns ≥ 3 (props declared).
- `npx jest src/components/moderation/__tests__/TypedConfirmationModal.test.tsx --no-coverage` exits 0.
- `npx jest src/screens/__tests__/CarDetailsScreen.admin.test.tsx --no-coverage` exits 0.
- `npx tsc --noEmit -p .` exits 0.
- The full mobile moderation suite still passes: `npx jest src/components/moderation/__tests__/ src/screens/__tests__/CarDetailsScreen.admin.test.tsx src/screens/__tests__/AdminModerationScreen.tabs.test.tsx src/screens/__tests__/SellCarScreen.adminEdit.test.tsx --no-coverage` exits 0.
</verification>

<success_criteria>
- VERIFICATION gap "SC #1 + LUI-02 — Delete action presents a destructive-red confirmation dialog with appropriate copy" closes from `failed` → `verified`.
- Phase 10 LUI-02 flips from `BLOCKED` → `SATISFIED` in re-verification.
- Admin reading the modal sees "This listing will be permanently deleted" (EN) or "Объявление будет удалено навсегда" (RU) — NO mention of "provider profile" or "user account".
- Hint reads "Type the listing title 2018 Toyota Camry to confirm" (or RU equivalent) — NO mention of "email".
- Placeholder shows "2018 Toyota Camry" (or another listing-shaped example) — NO mention of "email@example.com".
- Existing Phase 5 user-mod TypedConfirmationModal mounts (delete_profile / revoke_role / permanently_banned) continue to render their original RU+EN copy without modification.
- No regression in the 13 existing CarDetailsScreen admin tests; no regression in the 8 existing TypedConfirmationModal tests + 2 keyboardType tests from Plan 10-06.
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-plumbing-admin-listing-ui/10-11-SUMMARY.md`. The summary MUST include:
- Which option was chosen in Task 1 Step 3 for the {email}/{title} placeholder substitution (Option A: preserve {email} token, Option B: extend replace to handle both)
- Final RU + EN strings for the three new keys as actually written to translations.ts
- Confirmation that the existing 13 CarDetailsScreen admin tests + 8 TypedConfirmationModal tests + 2 keyboardType tests still pass alongside the new ~4 tests added by this plan
- Test count after this plan: TypedConfirmationModal ≥ 12 tests, CarDetailsScreen.admin.test.tsx still 13 tests with extended T10
</output>
