---
phase: 10
plan: 09
type: execute
wave: 4
depends_on: [10-04]
files_modified:
  - src/types/navigation.ts
  - src/screens/SellCarScreen.tsx
  - src/screens/__tests__/SellCarScreen.adminEdit.test.tsx
autonomous: true
requirements: [LUI-02]
must_haves:
  truths:
    - "D-01: SellCarScreen reuses for admin Edit via `route.params?.adminEdit?: boolean` flag — no AdminEditListingScreen (anti-pattern explicitly rejected per CONTEXT deferred)"
    - "navigation.ts: RootStackParamList.SellCar widens to { carId?: string; adminEdit?: boolean } | undefined (additive — pre-existing optional carId preserved)"
    - "D-01 gates bypassed when adminEdit === true: (a) seller-status APPROVED autofill gate at line ~76; (b) seller-status APPROVED car-fetch gate at line ~88; (c) ownership check inside car-fetch body at line ~94; (d) GatedScreenWrapper around the form body at line ~501 (Pitfall 5); (e) OTP / phone-verification gates (if any exist outside the seller-status path — confirm via grep)"
    - "Pitfall 4: ALL `sellerStatus === 'APPROVED'` occurrences in SellCarScreen.tsx must be patched OR explicitly accept the adminEdit OR branch — partial patching leaves admin on a blank form"
    - "D-01 submit endpoint swap: when adminEdit && carId → ModerationService.adminEditListing(carId, structured); otherwise existing seller PUT /api/cars/:id or create POST /api/cars paths unchanged"
    - "Anti-pattern: NO inline FormData rebuilding in the SellCarScreen submit branch — Plan 09 calls adminEditListing with a structured { fields, existingImageUrls, newFiles } object; ModerationService assembles multipart (CONTEXT + Pitfall 9 — single source of multipart truth)"
    - "On submit success in adminEdit branch: Alert.alert success → navigation.goBack() so admin returns to CarDetailsScreen; per RESEARCH Discretion: focus listener or refresh hint causes CarDetailsScreen to re-fetch (Plan 08 already re-fetches on focus or on navigation params change — verify and adjust if needed)"
    - "DEBT-03 deferred: NO wholesale refactor of SellCarScreen's 1527-line file — conditional branches only, <50 net added lines outside the imports + submit branch"
  artifacts:
    - path: "src/types/navigation.ts"
      provides: "SellCar param shape widened with adminEdit?: boolean"
      contains: "adminEdit?: boolean"
    - path: "src/screens/SellCarScreen.tsx"
      provides: "adminEdit flag wiring: 4+ gate bypasses + submit endpoint swap + ListingModerationError catch branch"
      contains: "route.params?.adminEdit"
    - path: "src/screens/__tests__/SellCarScreen.adminEdit.test.tsx"
      provides: "Screen tests for all 4 gate bypasses + endpoint swap + error handling"
  key_links:
    - from: "src/screens/SellCarScreen.tsx submit branch"
      to: "ModerationService.adminEditListing"
      via: "structured { fields, existingImageUrls, newFiles } input — service assembles FormData centrally"
      pattern: "ModerationService\\.adminEditListing"
    - from: "Plan 08 navigation.navigate('SellCar', { carId, adminEdit: true })"
      to: "SellCarScreen route.params destructure"
      via: "RootStackParamList type"
      pattern: "route\\.params\\?\\.adminEdit"
  forbidden:
    - "Creating a new AdminEditListingScreen.tsx file (D-01 explicit reject; REQUIREMENTS Out of Scope reaffirms)"
    - "Refactoring SellCarScreen.tsx beyond adding conditional branches (DEBT-03 deferred)"
    - "Inline-rebuilding FormData inside SellCarScreen's submit branch (CONTEXT anti-pattern + Pitfall 9 + RESEARCH §Don't Hand-Roll — multipart layout duplication risk)"
    - "Patching only a subset of `sellerStatus === 'APPROVED'` sites (Pitfall 4 — partial patching leaves admin on blank form)"
    - "Forgetting the GatedScreenWrapper bypass at line ~501 (Pitfall 5 — full-screen overlay if admin themselves has restrictedFeatures.create_listing)"
---

<objective>
Wire the `adminEdit?: boolean` route flag into `SellCarScreen.tsx` so an admin tapping Edit on `CarDetailsScreen` (Plan 08) lands on the existing seller form with all 4+ seller-only gates bypassed and the submit endpoint swapped to `ModerationService.adminEditListing(carId, structured)`.

Purpose: D-01 locks the reuse decision (no separate AdminEditListingScreen — explicitly rejected per CONTEXT). Phase 10 keeps SellCarScreen's 1527-line size (DEBT-03 deferred) and adds <50 lines of conditional branches. Pitfall 4 names the failure mode if only one gate is patched: admin lands on a blank form because at least one of the `sellerStatus === 'APPROVED'` predicates still excludes them. Pitfall 5 names the GatedScreenWrapper overlay trap. The submit branch uses structured input (Claude's Discretion recommendation per CONTEXT) so multipart layout assembly stays centralized in `ModerationService.adminEditListing` (Plan 04 + Pitfall 9).

Output: A 1-line widening to `RootStackParamList.SellCar` in `navigation.ts` + targeted conditional-branch additions inside `SellCarScreen.tsx` + a co-located screen test file proving all 4 gates are bypassed and the endpoint swap fires.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-CONTEXT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-04-SUMMARY.md

<interfaces>
<!-- Existing SellCarScreen gate effects + submit branch are the EXACT integration points. -->
<!-- ModerationService.adminEditListing from Plan 04 assembles multipart centrally. -->

From src/types/navigation.ts (existing — line 4):
  SellCar: { carId?: string } | undefined;

  After Plan 09:
  SellCar: { carId?: string; adminEdit?: boolean } | undefined;

From src/screens/SellCarScreen.tsx (existing — confirm via Read before editing):
  - Line ~30-31: const carId = route.params?.carId; const isEditMode = !!carId;
  - Line ~75-79: useEffect autofill gate on user.sellerStatus === 'APPROVED'
  - Line ~87-148: useEffect car-fetch gate on user?.sellerStatus === 'APPROVED' + ownership check (sellerId !== user.localId)
  - Line ~437-458: submit try/catch — isEditMode ? apiClient.put(/api/cars/:id) : apiClient.post(/api/cars)
  - Line ~499-509: GatedScreenWrapper capability="create_listing" wraps the body
  - Any OTP gates: grep for 'isPhoneVerified' or 'otpVerified' or 'verifyPhone' to enumerate

From src/services/moderation/ModerationService.ts (Plan 04):
  adminEditListing(carId: string, input: AdminEditListingInput): Promise<ListingActionResponse>
  - AdminEditListingInput = { fields: {...full set of editable fields}, existingImageUrls?: string[], newFiles?: Array<{uri,type?,name?}> }
  - Throws ListingModerationError on 4xx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen RootStackParamList.SellCar type</name>
  <files>src/types/navigation.ts</files>
  <read_first>
    - src/types/navigation.ts (FULL — confirm SellCar shape at line 4 + alphabetization convention)
  </read_first>
  <action>In `src/types/navigation.ts`, locate the `SellCar` entry (currently `SellCar: { carId?: string } | undefined;`). Widen to `SellCar: { carId?: string; adminEdit?: boolean } | undefined;`. Pure additive change — no other route entries touched, no import changes.</action>
  <verify>
    <automated>grep -nE "SellCar:.*adminEdit\\?: boolean" src/types/navigation.ts</automated>
  </verify>
  <done>Grep returns exactly 1 match showing the widened type. `npx tsc --noEmit` (or `npm run lint` if no tsc script — verify the project's check command) reports zero type errors for the navigation type.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 RED screen tests for SellCarScreen adminEdit flow</name>
  <files>src/screens/__tests__/SellCarScreen.adminEdit.test.tsx</files>
  <read_first>
    - src/screens/SellCarScreen.tsx (FULL — enumerate ALL `sellerStatus === 'APPROVED'` occurrences via in-file grep, list line numbers in test file comments so Task 3 patches every one)
    - src/screens/__tests__/AdminModerationScreen.test.tsx (existing screen-test conventions — settle() pattern, Proxy useLanguage mock, mock service modules)
    - src/services/moderation/ModerationService.ts (Plan 04 — confirm adminEditListing signature)
    - src/components/moderation/GatedScreenWrapper.tsx (existing — confirm capability prop + bypass mechanic)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-VALIDATION.md rows 10-08-01..03
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md Pitfall 4 + Pitfall 5 (lines 587-604)
  </read_first>
  <behavior>
    - Setup: mock `useRoute` returning `{ params: { carId: 'car_abc', adminEdit: true } }` for adminEdit tests; mock `apiClient.get(\`/api/cars/car_abc\`)` returning a fixture Car payload (with admin moderationBadge attached per Phase 9 D-07); mock `ModerationService.adminEditListing` as `jest.fn().mockResolvedValue({ ok: true, listing: { _id: 'car_abc', status: 'active' }, action: {...} })`; mock `useAuth` returning `{ user: { localId: 'admin_xyz', sellerStatus: null }, isAdmin: true }` — note `sellerStatus: null` proves adminEdit bypasses the gate (otherwise the test would pass for the wrong reason).
    - Test 1 (Pitfall 4 — autofill gate bypassed): with `adminEdit: true` + `user.sellerStatus = null`, screen renders without `checkProfileAndAutofill` being called (mock the function and assert called 0 times). Plan 09's source change adds an early-return `if (adminEdit) return;` at the top of the autofill useEffect.
    - Test 2 (Pitfall 4 — car-fetch gate bypassed): with `adminEdit: true` + `user.sellerStatus = null`, assert `apiClient.get('/api/cars/car_abc')` IS called (despite sellerStatus !== 'APPROVED'). Without the gate patch, this call wouldn't fire and the form would render blank.
    - Test 3 (Pitfall 4 — ownership check skipped): with `adminEdit: true` AND the fetched Car's `sellerId !== user.localId` (admin editing someone else's listing), assert NO `Alert.alert('Not authorized')` fires and `navigation.goBack` is NOT called.
    - Test 4 (Pitfall 5 — GatedScreenWrapper bypassed): with `adminEdit: true`, query the rendered tree for `<GatedScreenWrapper>` element with `capability="create_listing"` — it should NOT be present (or should be present but with the capability check bypassed via an `enabled={false}` prop or equivalent). Acceptance: the form body (TextInput for description, image picker, submit button) IS rendered even when admin has `restrictedFeatures: ['create_listing']` mock.
    - Test 5 (OTP/phone-verification gate bypassed): if SellCarScreen has any `if (!isPhoneVerified)` or `isPhoneVerified === false`-style gate that hides the submit button, assert with `adminEdit: true` the submit button IS interactive. If no such gate exists in the current code (verify via Read), mark this test as `it.skip` with a comment referencing the relevant `grep` finding.
    - Test 6 (endpoint swap on submit): with `adminEdit: true`, populate the form with fixture values, tap Submit → assert `ModerationService.adminEditListing` called once with `('car_abc', { fields: { ...spec'd fields }, existingImageUrls: [...], newFiles: [...] })`. CRITICAL: assert `apiClient.put('/api/cars/car_abc', ...)` was NOT called (the seller PUT path) and `apiClient.post('/api/cars', ...)` was NOT called (the seller CREATE path) — the swap is exclusive.
    - Test 7 (success → navigation.goBack): adminEdit submit succeeds → `Alert.alert` shows success → tapping OK calls `navigation.goBack()`. Mock the Alert via `jest.spyOn(Alert, 'alert')`.
    - Test 8 (error → ListingModerationError handled): mock `adminEditListing` to reject with `new ListingModerationError('invalid_make', undefined, undefined, undefined, undefined, undefined, 400)` → assert `Alert.alert` called with a message containing `'invalid_make'` (or the mapped translation key); navigation does NOT go back (admin stays on the form to fix the issue).
    - Test 9 (error → 404 listing_not_found): mock reject with `new ListingModerationError('listing_not_found', ..., 404)` → assert Alert.alert + navigation.goBack().
    - Test 10 (negative control — non-adminEdit path unchanged): with `route.params = { carId: 'car_xyz' }` (no adminEdit), `user.sellerStatus = 'APPROVED'`, `user.localId === fetchedCar.sellerId` → assert existing seller flow: `apiClient.put('/api/cars/car_xyz', ...)` IS called on submit; `ModerationService.adminEditListing` is NOT called. This is the regression-lock for the existing seller path.
    - Test 11 (FormData centralization): inspect the `adminEditListing` call's second argument — assert it has shape `{ fields, existingImageUrls?, newFiles? }` (structured object) NOT a `FormData` instance. Locks the centralization invariant — if a future PR inline-builds FormData in the screen, the test fails.
  </behavior>
  <action>Create `src/screens/__tests__/SellCarScreen.adminEdit.test.tsx`. Use the settle() pattern + mock pattern from existing screen tests. Document in a top-of-file comment the list of `sellerStatus === 'APPROVED'` line numbers found by `grep -n "sellerStatus === 'APPROVED'" src/screens/SellCarScreen.tsx` (Task 3 must patch ALL of them per Pitfall 4). File MUST fail because SellCarScreen does not yet read `route.params?.adminEdit`.</action>
  <verify>
    <automated>npx jest src/screens/__tests__/SellCarScreen.adminEdit.test.tsx --bail 2>&1 | tail -30</automated>
  </verify>
  <done>11 tests present; all fail at the gate-bypass / endpoint-swap assertions (Tests 1-9 fail; Test 10 negative-control should pass already since it doesn't change existing behavior; Test 11 fails by virtue of the missing call). RED gate confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire adminEdit gate bypasses + endpoint swap (GREEN)</name>
  <files>src/screens/SellCarScreen.tsx</files>
  <read_first>
    - src/screens/SellCarScreen.tsx (FULL — Task 2's grep findings; Pitfall 4 says enumerate every `sellerStatus === 'APPROVED'` and add the OR-adminEdit clause to each)
    - src/screens/__tests__/SellCarScreen.adminEdit.test.tsx (Task 2 contract — Tests 1-11)
    - src/components/moderation/GatedScreenWrapper.tsx (Pitfall 5 — confirm whether the component already accepts an `enabled?: boolean` short-circuit prop; if not, Plan 09 uses inline conditional return per RESEARCH recommendation lines 600-604)
    - src/services/moderation/ModerationService.ts (Plan 04 AdminEditListingInput shape)
    - src/services/moderation/errors.ts (Plan 01 ListingModerationError shape)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md §"src/screens/SellCarScreen.tsx (MODIFIED)" KEEP/CHANGE list
  </read_first>
  <action>
  Edit `src/screens/SellCarScreen.tsx`. Apply ONLY conditional branches — DO NOT refactor (DEBT-03 deferred). Net added lines should be <50 outside the imports + submit branch swap.

  Imports — add:
  - `import { ModerationService } from '../services/moderation/ModerationService';`
  - `import { ListingModerationError } from '../services/moderation/errors';`

  Route-param destructure — after the existing `const carId = route.params?.carId; const isEditMode = !!carId;` (~line 30-31), append:
  - `const adminEdit = route.params?.adminEdit ?? false;`

  Patch ALL `sellerStatus === 'APPROVED'` predicates per Pitfall 4. For each occurrence enumerated in Task 2's top-of-file comment, change:
  - `if (user && user.sellerStatus === 'APPROVED')` → `if (!adminEdit && user && user.sellerStatus === 'APPROVED')` for the autofill gate (we want the autofill skipped under adminEdit per RESEARCH line 1237).
  - `if (carId && user?.sellerStatus === 'APPROVED')` → `if (carId && (adminEdit || user?.sellerStatus === 'APPROVED'))` for the car-fetch gate.
  - The dependency arrays of those useEffects MUST add `adminEdit` so React re-runs them when the flag changes (typically not needed in practice — Plan 08 always pushes a fresh mount — but safety).

  Inside the car-fetch useEffect body, locate the ownership check: `if (c.sellerId !== user?.localId) { Alert.alert(...); navigation.goBack(); return; }`. Wrap in `if (!adminEdit && c.sellerId !== user?.localId) { ... }` — admin bypasses the ownership check (D-01).

  GatedScreenWrapper bypass (Pitfall 5) — the existing `<GatedScreenWrapper capability="create_listing">` wraps the entire body. Refactor MINIMALLY: extract the body JSX into a local variable `const body = (<View>...</View>)` (just rename the existing return-body expression; do NOT re-architect), then return `return adminEdit ? body : <GatedScreenWrapper capability="create_listing">{body}</GatedScreenWrapper>;`. If extracting the body is infeasible without a large refactor, use the alternative: add an `enabled?: boolean` prop to GatedScreenWrapper that short-circuits to `<>{children}</>` when false, then use `<GatedScreenWrapper capability="create_listing" enabled={!adminEdit}>{body}</GatedScreenWrapper>` — pick whichever has the smaller diff footprint (planner's discretion).

  Submit branch — locate the existing try block (~line 437): currently `if (isEditMode) { await apiClient.put(...) } else { await apiClient.post(...) }`. Insert a NEW first branch BEFORE `if (isEditMode)`:
  ```
  if (adminEdit && carId) {
    await ModerationService.adminEditListing(carId, {
      fields: { /* spread the form's editable fields — makeId, modelId, trimLevel, wheelbase, fuel, currency, description, bodyType, engine, transmission, drivetrain, mpg, condition, exteriorColor, interiorColor, interiorMaterial, phoneNumber, telegramUsername, year, price, mileage, seats, doors, knownIssues */ },
      existingImageUrls,
      newFiles: images.map((img) => ({ uri: img.uri, type: img.type, name: img.fileName })),
    });
    Alert.alert(t.success, 'Listing updated by admin', [{ text: 'OK', onPress: () => navigation.goBack() }]);
  } else if (isEditMode) {
    // existing seller PUT path — byte-identical
  } else {
    // existing create POST path — byte-identical
  }
  ```
  CRITICAL: keep the existing `isEditMode` and create branches BYTE-IDENTICAL (other than dropping into `else if`/`else`). The structured-input shape is per Plan 04's `AdminEditListingInput` interface — do NOT inline-build a FormData here (Pitfall 9 + CONTEXT anti-pattern).

  Catch branch — extend the existing `catch (error: any) { ... }`: add a branch BEFORE the generic Alert: `if (error instanceof ListingModerationError) { Alert.alert(t.error, error.code); if (error.code === 'listing_not_found') navigation.goBack(); return; }`. Existing seller-path Alert remains for non-ListingModerationError cases.

  OTP gates — per Task 2 Test 5: if a phone-verification gate exists, wrap with `if (!adminEdit && ...)` so admin bypasses it. If none exists, no source change needed for this point (Test 5 is `it.skip`'d in Task 2).
  </action>
  <verify>
    <automated>npx jest src/screens/__tests__/SellCarScreen.adminEdit.test.tsx --bail</automated>
  </verify>
  <done>All 11 tests pass. `grep -c "route.params?.adminEdit" src/screens/SellCarScreen.tsx` returns `≥ 1`. `grep -nE "sellerStatus === 'APPROVED'" src/screens/SellCarScreen.tsx | wc -l` — every occurrence is now adjacent to an `adminEdit` reference (verify by `grep -B1 -A1 "sellerStatus === 'APPROVED'" src/screens/SellCarScreen.tsx` and confirming each context block contains either `!adminEdit` or `adminEdit ||`). `grep -c "ModerationService.adminEditListing" src/screens/SellCarScreen.tsx` returns `1`. Lint passes. Net added lines (verify via `git diff --stat`): under 60.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 08 navigation → SellCarScreen route params | Type-checked via the widened `RootStackParamList.SellCar`. The `adminEdit` flag is a client-side UI mode switch only — it does NOT grant any backend privilege. The backend's `requireAdmin` + `denySelfModerationListing` middleware on `PATCH /api/admin/moderation/listings/:carId` are the sole authority. |
| SellCarScreen submit branch → ModerationService.adminEditListing | Same Bearer-token flow as Plan 04; backend rejects with `cannot_moderate_own_listing` (400) or `403 forbidden` if the caller is not actually an admin, regardless of what `adminEdit` says client-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation (mobile-trust-isAdmin) | `adminEdit` route flag | mitigate | The flag is a UI mode switch only — it tells SellCarScreen to skip seller-only gates and call the admin endpoint instead of the seller endpoint. The admin endpoint is independently auth-checked by backend `requireAdmin` (Phase 7 LSEC-02). A non-admin who somehow navigated to SellCar with `adminEdit: true` would get a 403 from the backend. Test 8 + 9 exercise the 4xx error paths. |
| T-10-02 | Spoofing (admin session loss on error) | submit catch branch | mitigate | The catch explicitly catches `ListingModerationError` and surfaces it via Alert.alert — never routes through the user-suspension flow. Plan 05's clientListing409.test.ts already locks this at the transport layer; Plan 09's tests confirm at the screen layer. |
| T-10-multipart | Tampering (multipart shape divergence) | submit branch | mitigate | Plan 09 calls `ModerationService.adminEditListing(carId, { fields, existingImageUrls, newFiles })` with structured input; the service module (Plan 04) is the SINGLE place that assembles FormData and sets the explicit `'Content-Type': 'multipart/form-data'` header (Pitfall 9). Test 11 grep-locks this — any future inline-FormData regression in SellCarScreen breaks the test. |
| T-10-leak | Information Disclosure (admin edits own listing) | bypassed ownership check | accept | D-01 explicit: admin can edit any listing (that's the whole point). Backend `denySelfModerationListing` rejects own-listing edit with `cannot_moderate_own_listing` 400, surfaced via Test 8 Alert. Admin sees the rejection explicitly rather than silently. |
</threat_model>

<verification>
- `npx jest src/screens/__tests__/SellCarScreen.adminEdit.test.tsx --bail` — all 11 tests green
- Full mobile suite: `npm test` — no regression on existing seller flow (Test 10 negative-control proves this)
- `grep -c "adminEdit" src/screens/SellCarScreen.tsx` returns `≥ 5` (route destructure + gate predicates + GatedScreenWrapper conditional + submit branch)
- `grep -c "ModerationService.adminEditListing" src/screens/SellCarScreen.tsx` returns `1` (single call site)
- `grep -nE "sellerStatus === 'APPROVED'" src/screens/SellCarScreen.tsx`: each match must appear in an expression that ALSO references `adminEdit` within the same logical/&&-chain
- `grep -nE "new FormData\\(\\)" src/screens/SellCarScreen.tsx`: any matches must be in the EXISTING seller paths only (not the new adminEdit branch). If the existing seller paths build FormData inline, that is pre-existing code (DEBT-03 — out of scope for Plan 09).
- ESLint: `npm run lint -- src/screens/SellCarScreen.tsx src/types/navigation.ts` passes
- `npm run lint` (full): passes
</verification>

<success_criteria>
- Plan 08's `navigation.navigate('SellCar', { carId, adminEdit: true })` lands on a functional admin edit form: form pre-fills with existing listing data (car-fetch fires regardless of admin's own sellerStatus), all gates are bypassed, the form body is visible (no GatedScreenWrapper overlay).
- Submit calls `ModerationService.adminEditListing` with structured input; multipart assembly is centralized in the service module (Plan 04 location).
- Backend 400 `cannot_moderate_own_listing` / `invalid_field` / `no_changes` errors surface as Alert.alert without losing the form context; 404 `listing_not_found` pops back to CarDetails.
- Existing seller flows (Edit own listing, Create new listing) remain functionally unchanged (Test 10 regression-lock).
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-plumbing-admin-listing-ui/10-09-SUMMARY.md` summarizing: 1-line navigation type change, list of `sellerStatus === 'APPROVED'` line numbers patched (Pitfall 4 audit), GatedScreenWrapper bypass approach chosen (inline conditional vs `enabled` prop), test file (11 tests), and confirmation that all grep invariants hold + the regression-lock test (Test 10) passes.
</output>
