---
phase: 10
plan: 10
type: execute
wave: 5
depends_on: [10-03, 10-04, 10-07]
files_modified:
  - src/screens/AdminModerationScreen.tsx
  - src/screens/__tests__/AdminModerationScreen.tabs.test.tsx
autonomous: true
requirements: [LUI-04]
must_haves:
  truths:
    - "D-09: AdminModerationScreen gains top-level Users|Listings tab control — no new route, no new screen file (widen-existing-surface, mirrors v1.0 Phase 5 D-03 AdminManagementScreen repurpose)"
    - "D-12: admin lands on Users tab by default (preserves muscle memory); tab switch via ChipButton-style segmented control reusing existing chip styles from AdminModerationScreen.tsx:597-611"
    - "Listings tab body: search bar (raw TextInput + Submit button per Phase 5 plan-05-11 UAT lesson — NOT per-keystroke debounce; useDebouncedValue is deleted), filter chip row (All | Active | Suspended | Archived | Deleted single-select), paginated FlatList of AdminListingRow"
    - "AdminListingRow visual: thumbnail (firstPhotoUrl) + buildListingTitle(row) + price + SeverityBadge (status); row tap navigates to CarDetailsScreen via navigation.navigate('CarDetails', { carId: row._id }) per D-10"
    - "Per-row Recover button ONLY when row.status === 'deleted' (LUI-04 explicit); tap opens ListingRestoreModal (Claude's Discretion recommendation per CONTEXT — consistency with CarDetailsScreen Restore flow) → fires ModerationService.restoreListing(carId, body?) → optimistic row update (status flip + Recover button removal)"
    - "Pitfall 7: NEW listingsAbortRef = useRef<AbortController>(null) — DO NOT reuse the existing user-tab abortRef; tab-switch + search overlap on a shared ref aborts the wrong request"
    - "Anti-pattern: NO separate `recoverListing` route — Recover calls the same restoreListing as CarDetailsScreen (Phase 8 D-B: restore is the single path back to active)"
    - "Default page size 25 (matches Phase 5 D-12 precedent + Plan 03 backend default)"
  artifacts:
    - path: "src/screens/AdminModerationScreen.tsx"
      provides: "Users|Listings tab structure + Listings tab body (search + chips + paginated FlatList + Recover)"
      contains: "ListingStatusFilter"
    - path: "src/screens/__tests__/AdminModerationScreen.tabs.test.tsx"
      provides: "GREEN tests for tab control + Listings tab + Recover row action + Pitfall-7 abortRef isolation"
  key_links:
    - from: "AdminModerationScreen Listings tab"
      to: "ModerationService.searchListings (Plan 04)"
      via: "apiClient.get backed by Plan 03 backend GET endpoint"
      pattern: "ModerationService\\.searchListings"
    - from: "Listings tab row Recover button"
      to: "ListingRestoreModal (Plan 07) → ModerationService.restoreListing"
      via: "modal mount + onSubmit callback"
      pattern: "ListingRestoreModal"
    - from: "Row tap"
      to: "CarDetailsScreen (Plan 08 admin badge surface)"
      via: "navigation.navigate('CarDetails', { carId: row._id })"
      pattern: "navigate.\\('CarDetails'"
  forbidden:
    - "Reusing the existing user-tab abortRef for Listings searches (Pitfall 7 — separate listingsAbortRef MUST be a distinct useRef instance)"
    - "Re-introducing useDebouncedValue or any per-keystroke search debounce (deleted in Phase 5 plan-05-11; RESEARCH §State of the Art deprecation)"
    - "Adding a new screen file like AdminListingsScreen.tsx (D-09 explicit: widen the existing screen, no new route)"
    - "Adding a separate recoverListing route or service method (Phase 8 D-B + CONTEXT anti-pattern: restore is the single path back to active)"
    - "Sharing useState buckets between user and listing tabs (state should be parallel — distinct listings, listingsNextCursor, listingsLoading, listingsQuery, etc. so tab-switch doesn't pollute the other tab)"
---

<objective>
Extend `AdminModerationScreen.tsx` with a top-level `Users | Listings` tab control and a fully wired Listings tab — search bar, filter chips, paginated FlatList, per-row Recover action — calling `ModerationService.searchListings` (Plan 04) backed by the Plan 03 cross-repo GET endpoint.

Purpose: LUI-04 (admin-only Deleted listings filter view) + ROADMAP success criterion #4. D-09 widens the existing screen rather than fragmenting nav (mirrors v1.0 Phase 5 D-03 AdminManagementScreen repurpose). The Recover row action reuses `ListingRestoreModal` (Plan 07) and `ModerationService.restoreListing` (Plan 04) — same code path as CarDetailsScreen's Restore (Plan 08), proving Phase 8 D-B's "single path back to active" design end-to-end.

Output: Substantial additive edits to `AdminModerationScreen.tsx` (~250-300 lines: parallel state bucket + tab control JSX + Listings tab body + AdminListingRow renderer + Recover handler) + a co-located screen integration test file. Existing Users tab functionality must remain byte-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-CONTEXT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-03-SUMMARY.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-04-SUMMARY.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-07-SUMMARY.md

<interfaces>
<!-- Existing user-tab pattern is the EXACT shape to port. -->
<!-- Plan 04 ModerationService.searchListings + Plan 03 backend GET supply the data. -->

From src/screens/AdminModerationScreen.tsx (existing — confirm via Read before editing):
  - Lines 89-101: user-tab state hooks (query, submittedQuery, roleFilter, stateFilter, users, nextCursor, loading, refreshing, loadError, abortRef)
  - Lines 128-165: runSearch with AbortController + isAbortError guard
  - Lines 173-185: fetchNextPage infinite-scroll handler
  - Lines 597-611: ChipButton component (reusable for both filter chips AND tab control)
  - Lines 630-650: chip/chipActive/chipText styles (reusable)
  - Lines 86-91: comment block documenting "submit-driven search, NOT debounced" — Phase 10 honors

From src/services/moderation/ModerationService.ts (Plan 04):
  searchListings(query: SearchListingsQuery, config?: { signal?: AbortSignal }): Promise<SearchListingsResult>
  - SearchListingsQuery = { status?, q?, cursor?, limit? }
  - SearchListingsResult = { rows: ListingSearchItem[], nextCursor: string | null }
  - ListingSearchItem = { _id, status, makeName?, modelName?, year?, price?, firstPhotoUrl?, sellerId, createdAt, moderatedAt?, moderationReason?, listingId? }
  - restoreListing(carId, body?): for Recover action

From src/components/moderation/ListingRestoreModal.tsx (Plan 07):
  <ListingRestoreModal visible carId onSubmit onClose />

From src/components/moderation/SeverityBadge.tsx (existing):
  - Severity color map (Phase 5 substrate); Plan 10 reuses for the status badge on each row

From src/utils/listingTitle.ts (Plan 02):
  buildListingTitle(row): string

From src/components/CarCard.tsx (reference, NOT reuse — too heavy for admin list per RESEARCH §Standard Stack):
  - Phase 10 builds a slim AdminListingRow inline OR as a new component
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED tests for AdminModerationScreen Listings tab</name>
  <files>src/screens/__tests__/AdminModerationScreen.tabs.test.tsx</files>
  <read_first>
    - src/screens/__tests__/AdminModerationScreen.test.tsx (existing — settle() pattern + Proxy useLanguage mock + mocked ModerationService + mocked apiClient conventions)
    - src/screens/AdminModerationScreen.tsx (FULL — confirm current state hook positions + ChipButton at lines 597-611 + chip styles at lines 630-650)
    - src/services/moderation/ModerationService.ts (Plan 04 — searchListings + restoreListing signatures)
    - src/components/moderation/ListingRestoreModal.tsx (Plan 07 — confirm prop shape)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-VALIDATION.md rows 10-09-01..04
  </read_first>
  <behavior>
    - Setup: mock `ModerationService.searchListings` returning `{ rows: [{ _id: 'car_001', status: 'active', makeName: 'Toyota', modelName: 'Camry', year: 2018, price: 18000, firstPhotoUrl: 'http://test/1.jpg', sellerId: 's_a', createdAt: '2026-05-01T00:00:00Z' }, { _id: 'car_002', status: 'deleted', makeName: 'Honda', modelName: 'Civic', year: 2019, price: 16000, firstPhotoUrl: null, sellerId: 's_b', createdAt: '2026-04-15T00:00:00Z', moderatedAt: '2026-05-20T00:00:00Z', moderationReason: 'duplicate' }], nextCursor: 'cursor_b64' }`. Mock `ModerationService.searchUsers` returning empty so the existing user tab doesn't crash. Mock `useNavigation`/`useRoute`/`useLanguage`/`useAuth` per existing screen-test conventions.
    - Test 1 (D-09 tab control + D-12 default Users): on initial mount, query for `testID="tab-users"` and `testID="tab-listings"` returns both elements; the Users tab is marked active (e.g., aria-selected or via testID prefix); `ModerationService.searchUsers` was called at least once on mount; `searchListings` was called 0 times.
    - Test 2 (tab switch fires Listings search): tap `testID="tab-listings"` → `ModerationService.searchListings` was called once with `{ status: undefined, q: '', cursor: undefined, limit: 25 }` (or equivalent default-query shape).
    - Test 3 (Listings tab body — filter chips render): after tab switch, query for testIDs `listing-filter-all`, `listing-filter-active`, `listing-filter-suspended`, `listing-filter-archived`, `listing-filter-deleted` — all 5 chips render.
    - Test 4 (status filter → service call): tap `testID="listing-filter-deleted"` → assert latest `searchListings` call had `{ status: 'deleted', ... }`.
    - Test 5 (search submit — NOT debounced): type "Toyota" into `testID="listings-search-input"`; assert `searchListings` was NOT called again immediately (no per-keystroke fire); tap `testID="listings-search-submit"` (or press the submit-on-enter equivalent) → assert latest `searchListings` call had `q: 'Toyota'`.
    - Test 6 (rows render): after Listings tab loads with the 2-row fixture, query rows by testID `listing-row-car_001` and `listing-row-car_002` — both present; row text contains `2018 Toyota Camry` and `2019 Honda Civic` (sub-string match via buildListingTitle).
    - Test 7 (per-row Recover ONLY for deleted): assert `testID="listing-row-recover-car_002"` IS present (status: 'deleted'); `testID="listing-row-recover-car_001"` is NOT present (status: 'active'). LUI-04 explicit.
    - Test 8 (Recover flow): tap `listing-row-recover-car_002` → `ListingRestoreModal` opens (query for its testID); type "post-appeal", tap Confirm → assert `ModerationService.restoreListing` called once with `('car_002', { note: 'post-appeal' })`; assert the row visually updates (optimistic — status flips to active, Recover button disappears in the next render).
    - Test 9 (row tap → CarDetails navigation): tap `testID="listing-row-car_001"` (not the Recover button — the row body itself) → assert `navigation.navigate` called with `('CarDetails', { carId: 'car_001' })` per D-10.
    - Test 10 (Pitfall 7 — independent abortRefs): switch tabs rapidly Users → Listings → Users → Listings; assert no AbortError fires from the Users tab's outstanding request when Listings tab's search is invoked. Specifically: after the rapid switch, `searchUsers.mock.calls.length` and `searchListings.mock.calls.length` should each show their own search lifecycle without cross-aborts (proving distinct AbortControllers). Use jest fake timers + flushPromises to interleave the tab switches deterministically.
    - Test 11 (pagination): mock `searchListings` to return `nextCursor !== null` for the first call; trigger `onEndReached` on the FlatList; assert next `searchListings` call had `cursor: <previousNextCursor>`.
    - Test 12 (no separate recoverListing route — single-method invariant): in-test source grep — `fs.readFileSync` of `src/screens/AdminModerationScreen.tsx` + `src/services/moderation/ModerationService.ts` and assert neither file contains `recoverListing` (regex). Phase 8 D-B + CONTEXT anti-pattern.
    - Test 13 (existing user tab regression-lock): on default mount (Users tab active), `searchUsers` was called → assert returned rows render in the user-tab body (positive control proving Plan 10 didn't break the existing surface).
  </behavior>
  <action>Create `src/screens/__tests__/AdminModerationScreen.tabs.test.tsx`. Mirror the settle()-pattern + Proxy-useLanguage-mock + mocked-service-modules conventions from the existing `AdminModerationScreen.test.tsx` test file. File MUST fail because AdminModerationScreen lacks the new tab + Listings body.</action>
  <verify>
    <automated>npx jest src/screens/__tests__/AdminModerationScreen.tabs.test.tsx --bail 2>&1 | tail -30</automated>
  </verify>
  <done>13 tests present; all fail at the tab/Listings-body assertions (Tests 1-11). Test 12 grep guards pass already (no recoverListing yet). Test 13 negative-control passes if existing surface still renders. RED gate confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Users|Listings tab control + Listings tab body to AdminModerationScreen.tsx (GREEN)</name>
  <files>src/screens/AdminModerationScreen.tsx</files>
  <read_first>
    - src/screens/AdminModerationScreen.tsx (FULL — re-confirm state hook positions, runSearch shape, ChipButton component, chip styles, FlatList + RefreshControl + onEndReached + EmptyState plumbing)
    - src/services/moderation/ModerationService.ts (Plan 04 — searchListings + restoreListing signatures and types)
    - src/components/moderation/ListingRestoreModal.tsx (Plan 07 — prop shape)
    - src/components/moderation/SeverityBadge.tsx (existing — confirm `state` prop type + severity map to extend for listing statuses if needed)
    - src/utils/listingTitle.ts (Plan 02 — buildListingTitle)
    - src/screens/__tests__/AdminModerationScreen.tabs.test.tsx (Task 1 contract)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md §"src/screens/AdminModerationScreen.tsx (MODIFIED — Users | Listings tabs)" KEEP/CHANGE list
  </read_first>
  <action>
  Edit `src/screens/AdminModerationScreen.tsx`. ALL existing user-tab code stays byte-identical EXCEPT for the addition of the `scopeTab` switch around the render body. Net added lines: ~200-300 (tab control + parallel state + Listings body + AdminListingRow + Recover handler).

  Imports — add:
  - `import { buildListingTitle } from '../utils/listingTitle';`
  - `import { ListingRestoreModal } from '../components/moderation/ListingRestoreModal';`
  - Extend the ModerationService import to also pull in `searchListings`-related types if exported: `import { ModerationService, ListingSearchItem, SearchListingsQuery } from '../services/moderation/ModerationService';` (or import them separately if naming clashes; check Plan 04 final exports)
  - Ensure `Image` from RN is imported (for thumbnail rendering) — likely already imported via the existing user-tab UserAvatar pattern; if not, add.

  Tab type + state — append after the existing user-tab state hooks (~line 102):
  - `type ScopeTab = 'users' | 'listings';`
  - `const [scopeTab, setScopeTab] = useState<ScopeTab>('users');` (D-12 default Users)
  - `type ListingStatusFilter = 'all' | 'active' | 'suspended' | 'archived' | 'deleted';`
  - `const LISTING_STATUS_FILTER_OPTIONS: Array<{ value: ListingStatusFilter; key: string }> = [{ value: 'all', key: 'listingStatusFilterAll' }, { value: 'active', key: 'listingStatusFilterActive' }, { value: 'suspended', key: 'listingStatusFilterSuspended' }, { value: 'archived', key: 'listingStatusFilterArchived' }, { value: 'deleted', key: 'listingStatusFilterDeleted' }];` (module-level; or const inside component if matching local convention)

  Parallel state bucket for the Listings tab (Pitfall 7 — distinct from user-tab):
  - `const [listingsQuery, setListingsQuery] = useState('');`
  - `const [listingsSubmittedQuery, setListingsSubmittedQuery] = useState('');`
  - `const [listingsStatusFilter, setListingsStatusFilter] = useState<ListingStatusFilter>('all');`
  - `const [listings, setListings] = useState<ListingSearchItem[]>([]);`
  - `const [listingsNextCursor, setListingsNextCursor] = useState<string | null>(null);`
  - `const [listingsLoading, setListingsLoading] = useState(false);`
  - `const [listingsRefreshing, setListingsRefreshing] = useState(false);`
  - `const [listingsLoadError, setListingsLoadError] = useState(false);`
  - `const [listingsLoadingMore, setListingsLoadingMore] = useState(false);`
  - `const listingsAbortRef = useRef<AbortController | null>(null);` (Pitfall 7 — DISTINCT instance, NOT the existing abortRef)
  - `const [recoverTarget, setRecoverTarget] = useState<ListingSearchItem | null>(null);`

  Listings tab handlers — `buildListingsQuery(cursor?)` + `runListingsSearch(resetList)` + `fetchNextListingsPage()` + `onRefreshListings()` — port the user-tab equivalents (`runSearch` lines 128-165 + `fetchNextPage` lines 173-185 + `onRefresh`) substituting: `ModerationService.searchListings(buildListingsQuery(cursor))` calls; `{ status: listingsStatusFilter === 'all' ? undefined : listingsStatusFilter, q: listingsSubmittedQuery || undefined, cursor, limit: 25 }` as the query shape; `listingsAbortRef` (NOT `abortRef`); `setListings` / `setListingsNextCursor` / `setListingsLoading` / `setListingsLoadError` for state updates. Mirror the AbortError guard verbatim. Trigger `runListingsSearch(true)` from a `useEffect` watching `[scopeTab, listingsSubmittedQuery, listingsStatusFilter]` — fires on tab switch + on submitted-query change + on status-filter change.

  Recover handler `handleRecoverListing(row, body)` — optimistic update:
  - `const prevListings = listings;`
  - `setListings((curr) => curr.map((l) => l._id === row._id ? { ...l, status: 'active' } : l));` (optimistic flip)
  - `setRecoverTarget(null);`
  - try `await ModerationService.restoreListing(row._id, body ?? {});` — on success, the listing should disappear from the "deleted" filter naturally (filter rerun next refresh — Phase 10 keeps it simple per CONTEXT discretion). On error: `setListings(prevListings);` + `Alert.alert(t.error, err.code ?? 'restore failed');` (no ListingModerationError special-casing here per CONTEXT discretion for the simpler list-view path; Phase 11 may add richer error UX if UAT requests).

  AdminListingRow — define inline (or as a sibling const above the component):
  ```
  const AdminListingRow: React.FC<{ row: ListingSearchItem; onPress: () => void; onRecoverPress?: () => void; }> = ({ row, onPress, onRecoverPress }) => (
    <TouchableOpacity testID={`listing-row-${row._id}`} style={styles.listingRow} onPress={onPress} accessibilityRole="button" accessibilityLabel={buildListingTitle(row)}>
      {row.firstPhotoUrl ? <Image source={{ uri: row.firstPhotoUrl }} style={styles.listingThumb} /> : <View style={[styles.listingThumb, styles.listingThumbPlaceholder]} />}
      <View style={styles.listingRowBody}>
        <Text style={styles.listingTitle} numberOfLines={1}>{buildListingTitle(row)}</Text>
        {row.price && <Text style={styles.listingPrice}>${row.price.toLocaleString()}</Text>}
        <SeverityBadge state={mapListingStatusToSeverityState(row.status)} />
      </View>
      {row.status === 'deleted' && onRecoverPress && (
        <TouchableOpacity testID={`listing-row-recover-${row._id}`} style={styles.recoverButton} onPress={onRecoverPress} accessibilityLabel="Recover listing">
          <Text style={styles.recoverButtonText}>{T.listingActionRestore ?? 'Recover'}</Text>
        </TouchableOpacity>
      )}
    </TouchableOpacity>
  );
  ```
  `mapListingStatusToSeverityState(status)` — small helper that maps `'active' → 'active'`, `'suspended' → 'feature_limited'` (warning palette), `'archived' → 'blocked_with_review'` (neutral palette), `'deleted' → 'permanently_banned'` (destructive palette). Use whichever SeverityBadge state values map cleanly to the desired severity color (RESEARCH §Standard Stack Alternatives Considered table); document the mapping in a comment.

  JSX — wrap the existing render body in a scope-aware structure. The existing screen render returns something like:
  ```
  <SafeAreaView>
    <Header />
    <SearchBar />
    <FilterChips />
    <FlatList ... />
  </SafeAreaView>
  ```
  Refactor to:
  ```
  <SafeAreaView>
    <Header />
    <View style={styles.scopeTabRow}>
      <ChipButton testID="tab-users" label={T.tabUsers ?? 'Users'} active={scopeTab === 'users'} onPress={() => setScopeTab('users')} />
      <ChipButton testID="tab-listings" label={T.tabListings ?? 'Listings'} active={scopeTab === 'listings'} onPress={() => setScopeTab('listings')} />
    </View>
    {scopeTab === 'users' ? (
      <UsersTabBody />   // existing JSX block — extract to a local const or keep inline
    ) : (
      <ListingsTabBody />
    )}
    {/* Recover modal at the end of the tree */}
    {recoverTarget && (
      <ListingRestoreModal visible={true} carId={recoverTarget._id} onSubmit={(body) => handleRecoverListing(recoverTarget, body)} onClose={() => setRecoverTarget(null)} />
    )}
  </SafeAreaView>
  ```
  Where `<ListingsTabBody />` is JSX (not a separate component) containing: a search row with a `<TextInput testID="listings-search-input" value={listingsQuery} onChangeText={setListingsQuery} onSubmitEditing={() => setListingsSubmittedQuery(listingsQuery.trim())} placeholder={T.listingsSearchPlaceholder ?? 'Search listings'} />` and a `<TouchableOpacity testID="listings-search-submit" onPress={() => setListingsSubmittedQuery(listingsQuery.trim())}>Search</TouchableOpacity>`, a chip row mapping `LISTING_STATUS_FILTER_OPTIONS` to `<ChipButton testID={\`listing-filter-${opt.value}\`} ...>`, and a `<FlatList data={listings} keyExtractor={(item) => item._id} renderItem={({ item }) => <AdminListingRow row={item} onPress={() => navigation.navigate('CarDetails', { carId: item._id })} onRecoverPress={item.status === 'deleted' ? () => setRecoverTarget(item) : undefined} />} refreshControl={<RefreshControl refreshing={listingsRefreshing} onRefresh={onRefreshListings} />} onEndReached={fetchNextListingsPage} onEndReachedThreshold={0.5} ListEmptyComponent={listingsLoading ? <ActivityIndicator /> : <EmptyState icon={Archive} title={T.listingsEmpty ?? 'No listings'} />} />`.

  StyleSheet additions: `scopeTabRow` (flexDirection: 'row', gap: SIZES.spacingSm, paddingHorizontal: SIZES.padding, paddingTop: SIZES.spacingSm), `listingRow` (flexDirection: 'row', alignItems: 'center', padding: SIZES.padding, borderBottomWidth: 1, borderBottomColor: COLORS.border), `listingThumb` (width: 60, height: 60, borderRadius: SIZES.borderRadius / 2, backgroundColor: COLORS.searchBackground), `listingThumbPlaceholder` (justifyContent: 'center', alignItems: 'center'), `listingRowBody` (flex: 1, marginLeft: SIZES.spacingSm), `listingTitle` (TYPOGRAPHY.body, color: COLORS.textPrimary), `listingPrice` (TYPOGRAPHY.caption, color: COLORS.textSecondary), `recoverButton` (paddingHorizontal: SIZES.spacingMd, paddingVertical: SIZES.spacingSm, borderRadius: SIZES.radiusSm, backgroundColor: COLORS.accent), `recoverButtonText` (TYPOGRAPHY.captionBold, color: COLORS.background).

  DO NOT modify any existing user-tab handler / state / styles other than wrapping them in the `scopeTab === 'users'` branch. Confirm via diff that the user-tab is byte-identical structurally (just moved inside a conditional).
  </action>
  <verify>
    <automated>npx jest src/screens/__tests__/AdminModerationScreen.tabs.test.tsx --bail && npx jest src/screens/__tests__/AdminModerationScreen.test.tsx --bail</automated>
  </verify>
  <done>All 13 tests from Task 1 pass. Existing AdminModerationScreen.test.tsx (user-tab tests) still passes (Test 13 regression-lock confirmed). `grep -c "listingsAbortRef" src/screens/AdminModerationScreen.tsx` returns `≥ 2` (declaration + use sites). `grep -c "recoverListing" src/screens/AdminModerationScreen.tsx src/services/moderation/ModerationService.ts` returns `0` (anti-pattern guard). `grep -c "useDebouncedValue" src/screens/AdminModerationScreen.tsx` returns `0` (Phase 5 plan-05-11 deletion preserved). ESLint passes. Full mobile suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Listings tab → backend GET /api/admin/moderation/listings | Bearer-token via apiClient; backend chain `verifyIdToken + requireAdmin + listingModerationRateLimiter` (Plan 03). The `setOptions({ includeAllListingStatuses: true })` bypass is backend-side (Plan 03) — mobile cannot tamper with it. |
| Recover row action → backend PATCH /api/admin/moderation/listings/:carId/restore | Same chain as the bottom-sheet Restore in CarDetailsScreen; backend `denySelfModerationListing` rejects self-recover with 400 surfaced via Alert. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation (mobile-trust-isAdmin) | AdminModerationScreen tab gate | mitigate | Listings tab visibility is gated only by the screen-level admin check (this screen is already only reachable from AdminDashboard which gates on `useAuth().isAdmin`). The backend `requireAdmin` middleware on the Plan-03 GET endpoint is the authoritative authorization — a non-admin somehow reaching this screen would see 403 on the listings request. |
| T-10-03 | Information Disclosure (PII via search) | searchListings q param | mitigate (backend) | Plan 03 backend whitelists q-search to `makeName/modelName/listingId` only. Mobile blindly forwards `q` — no client-side widening risk. |
| T-10-04 | Information Disclosure (admin sees only active rows) | searchListings response | mitigate (backend) | Plan 03 backend wires the `includeAllListingStatuses: true` bypass; mobile relies on backend default. |
| T-10-cursor | Tampering (cursor injection from mobile) | pagination | mitigate (backend) | Plan 03's `decodeCursor` returns `undefined` on parse failure and the route returns empty `{ rows: [], nextCursor: null }`. Mobile passes the cursor verbatim and doesn't manufacture them. |
| T-10-abortRef | DoS (cross-tab AbortController race) | parallel state buckets | mitigate | Pitfall 7: distinct `listingsAbortRef` instance; Test 10 exercises rapid tab-switch and asserts no cross-aborts. |
| T-10-recover-route | Repudiation (separate "recover" path drifts from "restore" audit) | Recover row action | mitigate | Plan 10 reuses `ModerationService.restoreListing` — single audit trail across CarDetails Restore + Listings tab Recover. Test 12 grep-asserts no `recoverListing` symbol exists anywhere. |
</threat_model>

<verification>
- `npx jest src/screens/__tests__/AdminModerationScreen.tabs.test.tsx --bail` — all 13 tests green
- `npx jest src/screens/__tests__/AdminModerationScreen.test.tsx --bail` — existing user-tab tests still green (regression-lock)
- Full mobile suite: `npm test` — no other regressions
- `grep -c "listingsAbortRef\|listingsAbortRef.current" src/screens/AdminModerationScreen.tsx` returns `≥ 3` (declaration + abort call before new search + null reset)
- `grep -c "recoverListing" src/screens/AdminModerationScreen.tsx src/services/moderation/ModerationService.ts` returns `0`
- `grep -c "useDebouncedValue" src/screens/AdminModerationScreen.tsx` returns `0`
- `grep -c "scopeTab === 'users'" src/screens/AdminModerationScreen.tsx` returns `≥ 1` (Users branch wrapper)
- `grep -c "scopeTab === 'listings'" src/screens/AdminModerationScreen.tsx` returns `≥ 1` (Listings branch wrapper)
- `grep -c "buildListingTitle" src/screens/AdminModerationScreen.tsx` returns `≥ 1` (row title rendering)
- ESLint: `npm run lint -- src/screens/AdminModerationScreen.tsx` passes
</verification>

<success_criteria>
- ROADMAP success criterion #4 satisfied: soft-deleted listings surface in the admin-only Listings tab when the Deleted filter chip is active; each deleted row has a Recover action; default buyer browse hides them (already enforced by Phase 9 hide-hook — Plan 10 doesn't touch buyer paths).
- Plan-03 backend GET + Plan-04 service method + Plan-07 ListingRestoreModal + the new Listings tab render an end-to-end loop: search → filter → row tap → CarDetails (Plan 08 admin badge surface) OR Recover → restoreListing → optimistic flip.
- The existing Users tab is functionally unchanged; the only structural diff is the outer `scopeTab` switch.
- Phase 10 closes with all 5 ROADMAP success criteria covered: #1 (Plan 08), #2 (Plans 04+08), #3 (Plans 06+08), #4 (Plans 03+04+07+10), #5 (Plans 05+08 — 409 surfaces via Alert/banner in CarDetails on cart-add or checkout flows; the cart-side buyer banner is Phase 11).
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-plumbing-admin-listing-ui/10-10-SUMMARY.md` summarizing: lines added to AdminModerationScreen.tsx (~300), new state hooks (11 — parallel bucket + recoverTarget), tab control + Listings body + AdminListingRow + Recover handler, test file (13 tests), and confirmation of all 5 ROADMAP success criteria across Phase 10 plans.
</output>
