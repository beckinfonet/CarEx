---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/moderation/errors.ts
  - src/services/moderation/__tests__/listingErrors.test.ts
autonomous: true
requirements: [LMOB-01, LMOB-02]
must_haves:
  truths:
    - "D-14: ListingModerationError exists as a SEPARATE class from ModerationError, sibling in src/services/moderation/errors.ts"
    - "Per RESEARCH §Code Examples line 699 recommendation: code union covers 10 explicit codes plus `| string` escape hatch (D-14 + invalid_make + invalid_model)"
    - "ListingModerationError carries listingStatus, reasonCategory, banner, refundId, refundFailed, httpStatus context fields (D-14 verbatim)"
    - "Anti-pattern guard (S7): ModerationError discriminator NOT widened to accept listing codes (Phase 4 D-07 boundary preserved)"
  artifacts:
    - path: "src/services/moderation/errors.ts"
      provides: "ListingModerationError class (sibling to existing ModerationError)"
      contains: "export class ListingModerationError"
    - path: "src/services/moderation/__tests__/listingErrors.test.ts"
      provides: "Wave 0 + GREEN tests for code-union shape + context fields + sibling-class invariant"
  key_links:
    - from: "src/services/moderation/errors.ts"
      to: "ListingModerationError sibling class"
      via: "same file as ModerationError"
      pattern: "export class ListingModerationError extends Error"
  forbidden:
    - "Widening ModerationError union to accept any listing code (Phase 4 D-07 boundary; grep -A2 \"class ModerationError\" must show no listing_not_available / listing_not_found / cannot_moderate_own_listing / already_in_state / not_moderated entries)"
    - "Introducing a third file for ListingModerationError (D-14 + Claude's Discretion default: co-locate in errors.ts so both classes audit together)"
---

<objective>
Land the substrate error class for Phase 10's listing-domain moderation surface: a sibling `ListingModerationError` next to the existing `ModerationError` in `src/services/moderation/errors.ts`, plus a Wave-0 GREEN test file proving the code-union shape and the sibling-class invariant.

Purpose: Every subsequent Phase 10 plan that touches the new `ModerationService` listing methods (Plan 04), the per-method catch wrappers, the `CarDetailsScreen` error banner (Plan 08), and the cart/checkout 409 surface depends on this class existing first. Sibling shape (NOT widening `ModerationError`) keeps user-domain and listing-domain audit boundaries clean for Phase 11 LQUAL-03 security review (per CONTEXT D-14 + RESEARCH §Pattern S7).

Output: An additive change to `errors.ts` (new class appended below `ModerationError`) plus a co-located unit test file under `__tests__/listingErrors.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-CONTEXT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md

<interfaces>
<!-- The existing ModerationError class is the EXACT pattern to mirror. Read errors.ts -->
<!-- Phase 4 D-07 scoped ModerationError to user-suspension codes; Phase 10 must NOT widen it. -->

From src/services/moderation/errors.ts (existing, 12 lines):
  export class ModerationError extends Error {
    constructor(
      public code: 'account_suspended' | 'provider_suspended' | 'user_not_found' | 'deprecated' | string,
      public status?: string,
      public reasonCategory?: string,
      public note?: string,
      public httpStatus?: number,
    ) {
      super(`ModerationError: ${code}`);
      this.name = 'ModerationError';
    }
  }

From .planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md §Code Examples (lines 658-699):
  - 10-code explicit union (D-14's 8 plus invalid_make + invalid_model per A4 recommendation)
  - listingStatus: 'suspended' | 'archived' | 'deleted'
  - banner: { titleKey, bodyKey, severity: 'warning' | 'neutral' | 'destructive' }
  - refundId + refundFailed (Phase 9 D-12..D-15 confirm-booking refund surface)
  - httpStatus number
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Wave 0 RED tests for ListingModerationError shape</name>
  <files>src/services/moderation/__tests__/listingErrors.test.ts</files>
  <read_first>
    - src/services/moderation/errors.ts (current ModerationError sibling shape — Phase 10 mirrors but does NOT modify the existing class)
    - src/services/moderation/__tests__/errors.test.ts (existing ModerationError test patterns — name + super message + code preservation conventions)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md §Code Examples (lines 656-700) for the 10-code union + context-field list
  </read_first>
  <behavior>
    - Test 1: `new ListingModerationError('listing_not_available')` sets `error.code === 'listing_not_available'`, `error.name === 'ListingModerationError'`, `error.message === 'ListingModerationError: listing_not_available'`, and `error instanceof Error` is true
    - Test 2: Each of the 10 explicit code-union members (`listing_not_available`, `listing_not_found`, `cannot_moderate_own_listing`, `already_in_state`, `not_moderated`, `invalid_field`, `no_changes`, `invalid_payload`, `invalid_make`, `invalid_model`) constructs without TypeScript error and the resulting `error.code` round-trips verbatim (use a `forEach` over an array literal so the test is data-driven)
    - Test 3: An unknown code (`'some_future_code'`) still constructs (the `| string` escape hatch) and `error.code === 'some_future_code'`
    - Test 4: All context fields preserved when supplied: `new ListingModerationError('listing_not_available', 'suspended', 'spam', { titleKey: 'k', bodyKey: 'b', severity: 'warning' }, 're_abc', false, 409)` — assert `listingStatus === 'suspended'`, `reasonCategory === 'spam'`, `banner.severity === 'warning'`, `refundId === 're_abc'`, `refundFailed === false`, `httpStatus === 409`
    - Test 5: All context fields default to `undefined` when not supplied (constructor with code only)
    - Test 6 (sibling-class invariant): A `ListingModerationError` is NOT `instanceof ModerationError` and vice versa (cross-import both from `../errors` and assert both directions return false) — guards Phase 4 D-07 boundary at runtime
  </behavior>
  <action>Create `src/services/moderation/__tests__/listingErrors.test.ts`. Import both `ListingModerationError` and `ModerationError` from `../errors`. Group the 6 tests above inside one `describe('ListingModerationError', () => { ... })`. The file MUST fail to compile right now because `ListingModerationError` does not yet exist — that is the RED state. Use the data-driven `forEach` pattern from `src/services/moderation/__tests__/errors.test.ts` (read it first to match style). Do NOT add new dependencies; jest preset already covers TS.</action>
  <verify>
    <automated>npx jest src/services/moderation/__tests__/listingErrors.test.ts --bail 2>&1 | grep -E "Cannot find|FAIL"</automated>
  </verify>
  <done>Test file exists at the path above; running the command shows either a TS compile error mentioning `ListingModerationError` or a jest `FAIL` for all 6 tests (this is the desired RED gate before Task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append ListingModerationError class to errors.ts (GREEN)</name>
  <files>src/services/moderation/errors.ts</files>
  <read_first>
    - src/services/moderation/errors.ts (current full file — Phase 10 appends; existing `ModerationError` class must remain byte-identical)
    - src/services/moderation/__tests__/listingErrors.test.ts (the RED tests from Task 1 — they define the contract)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md §Code Examples (lines 660-690) for the exact constructor parameter order
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md §"src/services/moderation/errors.ts (MODIFIED — sibling error class)" for KEEP/CHANGE list
  </read_first>
  <action>Append a new `export class ListingModerationError extends Error` BELOW the existing `ModerationError` class in `src/services/moderation/errors.ts`. Constructor signature, in order: `public code: 'listing_not_available' | 'listing_not_found' | 'cannot_moderate_own_listing' | 'already_in_state' | 'not_moderated' | 'invalid_field' | 'no_changes' | 'invalid_payload' | 'invalid_make' | 'invalid_model' | string`, then `public listingStatus?: 'suspended' | 'archived' | 'deleted'`, then `public reasonCategory?: string`, then `public banner?: { titleKey: string; bodyKey: string; severity: 'warning' | 'neutral' | 'destructive' }`, then `public refundId?: string`, then `public refundFailed?: boolean`, then `public httpStatus?: number`. Body: `super(\`ListingModerationError: ${code}\`); this.name = 'ListingModerationError';`. Do NOT modify the existing `ModerationError` class — not its union, not its name, not its message format. Per D-14 + RESEARCH A4 recommendation, include `invalid_make` and `invalid_model` in the explicit union (10 codes total).</action>
  <verify>
    <automated>npx jest src/services/moderation/__tests__/listingErrors.test.ts --bail && grep -c "class ModerationError" src/services/moderation/errors.ts && grep -c "class ListingModerationError" src/services/moderation/errors.ts</automated>
  </verify>
  <done>All 6 tests from Task 1 pass (GREEN); `grep -c "class ModerationError"` returns `1`; `grep -c "class ListingModerationError"` returns `1`; file contains two sibling class declarations and the existing `ModerationError` class is byte-identical to its pre-task content.</done>
</task>

<task type="auto">
  <name>Task 3: Lock anti-pattern guard — assert no listing codes in ModerationError union</name>
  <files>src/services/moderation/__tests__/listingErrors.test.ts</files>
  <read_first>
    - src/services/moderation/errors.ts (final state after Task 2)
    - src/services/moderation/__tests__/listingErrors.test.ts (current 6-test file from Task 1)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md §Pattern S7 "Sibling-Component + Sibling-Error-Class Discipline" — grep invariants section
  </read_first>
  <action>Append a 7th test inside the existing `describe('ListingModerationError', ...)` block: `it('does not widen ModerationError to accept listing codes (sibling discipline guard)', () => { ... })`. The test reads `errors.ts` from disk via `fs.readFileSync(path.join(__dirname, '..', 'errors.ts'), 'utf8')`, extracts the substring between `class ModerationError` and the closing `}` of that class (use a simple regex `/class ModerationError[\s\S]*?\n\}/`), and asserts that the extracted block does NOT contain any of the literal substrings: `'listing_not_available'`, `'listing_not_found'`, `'cannot_moderate_own_listing'`, `'already_in_state'`, `'not_moderated'`, `'invalid_make'`, `'invalid_model'`. Use Node's `fs` + `path` modules (already available in jest). Per RESEARCH §Anti-Pattern Guardrails: "No widening of ModerationError discriminator". This test fails at the source level if a future maintainer accidentally edits the wrong class.</action>
  <verify>
    <automated>npx jest src/services/moderation/__tests__/listingErrors.test.ts --bail</automated>
  </verify>
  <done>All 7 tests pass; the new sibling-discipline guard test fails fast on any future regression that adds listing codes into the `ModerationError` union literal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile client ↔ backend admin API | Bearer-token-authenticated; backend `verifyIdToken + requireAdmin` is the only authority. Phase 10 substrate (this plan) introduces no new boundary — it only defines the typed error shape Phase 10's downstream plans throw on backend rejection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation | `ListingModerationError` consumers | mitigate (cross-plan) | This plan adds only the class; mobile `isAdmin` for UI gating handled in Plan 08 / Plan 10. Backend `requireAdmin` (Phase 7) is the authoritative check on every endpoint that throws these codes. No code in this plan reads `isAdmin`. |
| T-10-06 | Repudiation (audit blur) | `ListingModerationError` discriminator | mitigate | Sibling class (NOT widened `ModerationError`) keeps user-domain and listing-domain audit-trail tests independently grep-able for Phase 11 LQUAL-03. Task 3 enforces this at the source level: any future edit that adds a listing code into the `ModerationError` union breaks `listingErrors.test.ts:it('does not widen ModerationError ...')`. |
</threat_model>

<verification>
- All 7 tests in `src/services/moderation/__tests__/listingErrors.test.ts` pass on a clean checkout: `npx jest src/services/moderation/__tests__/listingErrors.test.ts --bail`
- `grep -c "class ListingModerationError" src/services/moderation/errors.ts` returns exactly `1`
- `grep -c "class ModerationError" src/services/moderation/errors.ts` returns exactly `1` (existing class preserved, not widened)
- The full mobile suite remains green: `npm test` (no regression on existing `errors.test.ts` or `ModerationService.test.ts`)
</verification>

<success_criteria>
- `ListingModerationError` is importable as `import { ListingModerationError } from '../errors'` from anywhere under `src/services/moderation/`.
- The class accepts the full 10-code explicit union (D-14's 8 + invalid_make + invalid_model) plus an arbitrary string code, and constructs with all 7 context fields preserved (including refund-related fields for Phase 9 D-12..D-15 cart/checkout surface).
- The sibling-discipline guard fires if anyone accidentally widens `ModerationError` instead of using the new sibling class.
- Plan 04 (extending `ModerationService` with the 5 write methods) is unblocked — it can `import { ListingModerationError } from './errors'` and wrap axios errors with a typed throw.
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-plumbing-admin-listing-ui/10-01-SUMMARY.md` summarizing: lines added to `errors.ts`, full test file path + test count (7), and the grep invariants now enforced (`class ModerationError` x1, `class ListingModerationError` x1, anti-pattern guard test).
</output>
