---
phase: 10
plan: 06
type: execute
wave: 3
depends_on: [10-02, 10-04]
files_modified:
  - src/components/moderation/ListingModerationBottomSheet.tsx
  - src/components/moderation/__tests__/ListingModerationBottomSheet.test.tsx
autonomous: true
requirements: [LUI-01, LUI-02, LUI-03]
must_haves:
  truths:
    - "D-LUI-01/02: 4 visually-distinct action rows (Edit pencil-neutral / Suspend warning-orange / Archive gray-neutral / Delete destructive-red) when listing is active"
    - "D-LUI-03: Status-aware swap — when moderationBadge present (non-active listing), bottom sheet shows reasonCategory chip + single Restore button (NOT 4 actions)"
    - "Specifics from CONTEXT: bottom sheet renders a count/age subtle pill — e.g., 'Suspended since YYYY-MM-DD' derived from moderationBadge.moderatedAt — above the Restore button (Phase 5 specifics echo)"
    - "Sibling component (NOT generalization of QuickActionSheet) per Pattern S7 — keeps listing-action vocabulary grep-able for Phase 11 LQUAL-03"
    - "Pattern S2: Modal + transparent + animationType='slide' + outer Pressable overlay + inner Pressable sheet + useSafeAreaInsets bottom padding (verbatim shape from QuickActionSheet.tsx:61-145)"
    - "Pure presentational — no ModerationService calls, no axios; parent owns optimistic-flip + rollback (Plan 08 handles)"
    - "Header text comes from buildListingTitle(car) per Plan 02 — locks Pitfall 6 single-source-of-truth invariant"
  artifacts:
    - path: "src/components/moderation/ListingModerationBottomSheet.tsx"
      provides: "Bottom-sheet modal with status-aware body (4 actions OR Restore + chip)"
      contains: "ListingModerationBottomSheet"
    - path: "src/components/moderation/__tests__/ListingModerationBottomSheet.test.tsx"
      provides: "GREEN tests for both branches + visual distinction + sibling-discipline guard"
  key_links:
    - from: "ListingModerationBottomSheet"
      to: "buildListingTitle(car) from Plan 02"
      via: "import from src/utils/listingTitle"
      pattern: "buildListingTitle\\("
    - from: "ListingModerationBottomSheet onSelect"
      to: "Plan 08 CarDetailsScreen handler"
      via: "single-arg callback emitting ListingModerationAction"
      pattern: "onSelect:"
  forbidden:
    - "Importing ModerationService in this component (pure presentational; parent fires the call per Phase 5 D-08 + Pattern S7 — grep -c 'ModerationService' on this file MUST return 0)"
    - "Importing or extending QuickActionSheet.tsx (sibling discipline per D-04/S7 — generalization invites cross-domain drift)"
    - "Reading isAdmin / useAuth in this component (parent gates visibility; this component just renders when visible=true per the Plan 5 D-08 pattern)"
    - "Inline concat of `${car.year} ${car.makeName} ${car.modelName}` (Pitfall 6 — must call buildListingTitle for sentinel-parity with TypedConfirmationModal target in Plan 08)"
---

<objective>
Build the new `ListingModerationBottomSheet` component — a sibling of `QuickActionSheet` that renders 4 action rows (Edit / Suspend / Archive / Delete) for active listings and swaps to a single Restore button + reasonCategory chip + "Suspended since DATE" pill for non-active listings.

Purpose: LUI-01 (admin badge tap → bottom sheet open) + LUI-02 (4 visually distinct actions) + LUI-03 (Restore swap on non-active) all originate here. Plan 08 (CarDetailsScreen wiring) mounts this component; Plan 10 (AdminModeration Listings tab) reuses it if needed. Keeping the component pure presentational (no service calls, no AuthContext reads) keeps the optimistic-flip + rollback responsibility cleanly in the parent screen per Phase 5 D-08 + Pattern S7.

Output: One new component file under `src/components/moderation/` + one new co-located test file. ~150 lines of component code (mirroring QuickActionSheet's 211-line footprint with simpler logic since no dual-role complexity).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-CONTEXT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-02-SUMMARY.md

<interfaces>
<!-- QuickActionSheet.tsx is the EXACT modal shape to mirror as a sibling. -->
<!-- buildListingTitle from Plan 02 supplies the header string. -->

From src/components/moderation/QuickActionSheet.tsx (existing, 211 lines — see Modal+overlay shape at lines 61-145, ActionRow at lines 148-167, StyleSheet at lines 169-210):
  - Imports: React; Modal/View/Text/TouchableOpacity/StyleSheet/Pressable from RN; useSafeAreaInsets from react-native-safe-area-context; lucide icons; COLORS/SIZES/TYPOGRAPHY from theme; useLanguage
  - Modal visible transparent animationType="slide" onRequestClose
  - Outer <Pressable style={styles.overlay} onPress={onClose}> + inner <Pressable style={[styles.sheet, { paddingBottom: insets.bottom + SIZES.spacingSm }]} onPress={() => {}}>
  - <View style={styles.handle} /> grab handle
  - Internal ActionRow component (icon + label + disabled + onPress + testID)
  - testID props on every actionable row

From src/utils/listingTitle.ts (Plan 02):
  export function buildListingTitle(car: ListingTitleSource): string

From .planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md §Architecture Patterns Pattern 3 (lines 386-433) + §Code Examples (lines 1026-1041):
  export type ListingModerationAction = 'edit' | 'suspend' | 'archive' | 'delete' | 'restore';
  export interface ListingModerationBottomSheetProps {
    visible: boolean;
    listingTitle: string;
    moderationBadge?: {
      status: 'suspended' | 'archived' | 'deleted';
      reasonCategory?: string;
      moderationReason?: string;
      moderatedBy?: string;
      moderatedAt?: string;
    };
    onSelect: (action: ListingModerationAction) => void;
    onClose: () => void;
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED tests for ListingModerationBottomSheet (both branches)</name>
  <files>src/components/moderation/__tests__/ListingModerationBottomSheet.test.tsx</files>
  <read_first>
    - src/components/moderation/QuickActionSheet.tsx (existing — confirm testID convention + ActionRow shape)
    - src/components/moderation/__tests__/QuickActionSheet.test.tsx (existing test patterns under the same directory — mirror imports + render harness; if none exist for QuickActionSheet specifically, mirror the AdminModerationScreen test under src/screens/__tests__/)
    - src/utils/listingTitle.ts (Plan 02 — confirm buildListingTitle is callable from the test fixture)
    - src/constants/translations.ts (confirm currently-shipping translation keys; if listing-specific keys don't exist yet, Plan 06 uses EN placeholders inline + Phase 11 LQUAL-01 audits later per CONTEXT deferred)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-VALIDATION.md rows 10-05-01..03 + 10-06-01 for required test cases
  </read_first>
  <behavior>
    - Block A (active branch — LUI-01/02): render `<ListingModerationBottomSheet visible={true} listingTitle="2018 Toyota Camry" moderationBadge={undefined} onSelect={mock} onClose={mock} />`. Assert: (1) 4 action rows render with testIDs `listing-action-edit`, `listing-action-suspend`, `listing-action-archive`, `listing-action-delete`; (2) NO row with testID `listing-action-restore` is present; (3) NO reason-category chip is rendered; (4) tapping `listing-action-suspend` calls `onSelect('suspend')` exactly once; (5) tapping `listing-action-edit` calls `onSelect('edit')` exactly once; (6) the listing title `2018 Toyota Camry` is rendered in the header
    - Block B (visual distinction — LUI-02): for each of the 4 active-branch rows, assert the row's icon component is the spec'd Lucide glyph (`Pencil` for edit, `Shield` for suspend, `Archive` for archive, `Trash2` for delete) AND the icon color matches: Edit = `COLORS.accent` (neutral), Suspend = `COLORS.warning` (orange), Archive = `COLORS.textSecondary` (neutral gray), Delete = `COLORS.destructive` (red). Use `react-test-renderer` `findByProps` queries on each ActionRow's icon prop, OR assert via `testID` of the icon wrapper and inspect inline styles.
    - Block C (restore branch — LUI-03): render with `moderationBadge={{ status: 'suspended', reasonCategory: 'spam', moderatedAt: '2026-05-29T12:00:00.000Z' }}`. Assert: (1) row with testID `listing-action-restore` IS present; (2) NONE of `listing-action-edit/suspend/archive/delete` are present (mutually exclusive); (3) the reason-category text `spam` (or its translation key) is rendered as a chip; (4) the "moderated at" pill renders a human-readable date derived from `moderatedAt` (substring match `'2026'` is sufficient — exact format is Claude's Discretion); (5) tapping the restore row calls `onSelect('restore')` exactly once.
    - Block D (close behavior): tapping the overlay (`testID="listing-sheet-overlay"`) calls `onClose` exactly once; tapping the inner sheet body does NOT call `onClose` (Pressable bubbling stop per Pattern S2).
    - Block E (sibling-discipline guard): in-test `fs.readFileSync` of `src/components/moderation/ListingModerationBottomSheet.tsx` and assert the file does NOT contain `'ModerationService'` (grep substring; 0 occurrences — pure presentational); does NOT contain `'useAuth'` (parent gates visibility); does NOT contain `\`${car.year} ${car.makeName} ${car.modelName}\``-shaped template literal (Pitfall 6 — must call buildListingTitle elsewhere; this component receives the pre-built string as a prop).
  </behavior>
  <action>Create `src/components/moderation/__tests__/ListingModerationBottomSheet.test.tsx`. Use `react-test-renderer` (existing devDep). Mock `useLanguage` with a `Proxy`-based stable identity (mirror Phase 5 plan-05-10 lesson) returning the requested key as its own value when the actual key isn't present (so `t.listingActionSuspend` → `'listingActionSuspend'` if not yet in translations). Also wrap renders in `<SafeAreaProvider>` (existing convention from QuickActionSheet tests; if SafeAreaProvider is not present in test infra, use the `react-native-safe-area-context/jest/mock` recipe and document inline). File MUST fail to compile (module not yet created) — RED gate.</action>
  <verify>
    <automated>npx jest src/components/moderation/__tests__/ListingModerationBottomSheet.test.tsx --bail 2>&1 | grep -E "FAIL|Cannot find"</automated>
  </verify>
  <done>Test file exists and fails (compile-error on missing component module). All 5 blocks present in `describe` structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ListingModerationBottomSheet.tsx (GREEN)</name>
  <files>src/components/moderation/ListingModerationBottomSheet.tsx</files>
  <read_first>
    - src/components/moderation/QuickActionSheet.tsx (FULL — copy Modal+overlay+Pressable structure, ActionRow internal component, StyleSheet verbatim; substitute vocabulary)
    - src/utils/listingTitle.ts (Plan 02 — note that this component DOES NOT call buildListingTitle directly; it RECEIVES the pre-built listingTitle as a prop. The parent (Plan 08) calls buildListingTitle and passes the result down)
    - src/constants/theme.ts (confirm COLORS.warning, COLORS.destructive, COLORS.accent, COLORS.textSecondary exist; if `COLORS.warning` is not in palette, document the addition needed and use the closest existing semantic token + comment)
    - src/components/moderation/__tests__/ListingModerationBottomSheet.test.tsx (Task 1's RED tests — contract)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md §"src/components/moderation/ListingModerationBottomSheet.tsx (NEW)" KEEP/CHANGE list
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md §Code Examples lines 382-433
  </read_first>
  <action>
  Create `src/components/moderation/ListingModerationBottomSheet.tsx`.

  Imports header (mirror QuickActionSheet.tsx:1-7 — adapt icons): `import React from 'react'; import { Modal, View, Text, TouchableOpacity, StyleSheet, Pressable } from 'react-native'; import { useSafeAreaInsets } from 'react-native-safe-area-context'; import { Pencil, Shield, Archive, Trash2, RotateCcw } from 'lucide-react-native'; import { COLORS, SIZES, TYPOGRAPHY } from '../../constants/theme'; import { useLanguage } from '../../context/LanguageContext';`. DO NOT import `useAuth` (parent gates visibility). DO NOT import `ModerationService` (pure presentational). DO NOT import or extend `QuickActionSheet` (sibling discipline).

  Exports:
  - `export type ListingModerationAction = 'edit' | 'suspend' | 'archive' | 'delete' | 'restore';`
  - `export interface ListingModerationBottomSheetProps { visible: boolean; listingTitle: string; moderationBadge?: { status: 'suspended' | 'archived' | 'deleted'; reasonCategory?: string; moderationReason?: string; moderatedBy?: string; moderatedAt?: string; }; onSelect: (action: ListingModerationAction) => void; onClose: () => void; }`
  - `export const ListingModerationBottomSheet: React.FC<ListingModerationBottomSheetProps> = ({ visible, listingTitle, moderationBadge, onSelect, onClose }) => { ... };`

  Body shape (mirror QuickActionSheet.tsx:61-145 verbatim with substitutions):
  - `const insets = useSafeAreaInsets();`
  - `const { t } = useLanguage(); const T = t as unknown as Record<string, string>;` (Pattern S5)
  - `const isActive = !moderationBadge;` (the admin path: when listing is active, Phase 9 D-07 omits `moderationBadge`)
  - Return JSX: `<Modal visible={visible} transparent animationType="slide" onRequestClose={onClose}><Pressable testID="listing-sheet-overlay" style={styles.overlay} onPress={onClose}><Pressable style={[styles.sheet, { paddingBottom: insets.bottom + SIZES.spacingSm }]} onPress={() => {}}>...</Pressable></Pressable></Modal>`
  - Inner sheet content: `<View style={styles.handle} />`; header with `<Text style={styles.headerTitle} numberOfLines={1}>{listingTitle}</Text>` (use `listingTitle` prop verbatim — DO NOT inline concat from a `car` prop per Pitfall 6).
  - Branch on `isActive`:
    - Active: 4 `<ActionRow testID="listing-action-edit" icon={<Pencil size={20} color={COLORS.accent} />} label={T.listingActionEdit ?? 'Edit'} onPress={() => onSelect('edit')} />` (then suspend with `Shield` + `COLORS.warning`, archive with `Archive` + `COLORS.textSecondary`, delete with `Trash2` + `COLORS.destructive`). Use the `?? 'EnglishFallback'` pattern since Phase 11 LQUAL-01 audits RU/EN parity later.
    - Non-active: render a small `<View style={styles.reasonChip}><Text style={styles.reasonChipText}>{moderationBadge.reasonCategory ?? T.listingReasonOther}</Text></View>`; render the "moderated at" subtle pill `<Text style={styles.moderatedAtPill}>{moderationBadge.moderatedAt ? `Since ${new Date(moderationBadge.moderatedAt).toISOString().slice(0, 10)}` : ''}</Text>` (CONTEXT specifics — Phase 5 echo); then the Restore `<ActionRow testID="listing-action-restore" icon={<RotateCcw size={20} color={COLORS.accent} />} label={T.listingActionRestore ?? 'Restore'} onPress={() => onSelect('restore')} />`.
  - Cancel row at bottom (mirror QuickActionSheet:135-138): `<TouchableOpacity style={styles.cancelRow} onPress={onClose} testID="listing-sheet-cancel"><Text style={styles.cancelText}>{T.modalCancel ?? 'Cancel'}</Text></TouchableOpacity>`.

  Internal ActionRow component (mirror QuickActionSheet:148-167): inline functional component with the same testID/accessibility props.

  StyleSheet (inline at bottom per CLAUDE.md "Code Style"): copy QuickActionSheet's overlay/sheet/handle/header/actionRow/actionLabel/cancelRow/cancelText styles VERBATIM with renamed `headerEmail → headerTitle`. ADD `reasonChip` (paddingHorizontal: SIZES.spacingMd, paddingVertical: SIZES.spacingSm/2, borderRadius: SIZES.radiusSm, alignSelf: 'flex-start', backgroundColor: COLORS.searchBackground, marginVertical: SIZES.spacingSm) + `reasonChipText` (TYPOGRAPHY.caption, color: COLORS.textPrimary) + `moderatedAtPill` (TYPOGRAPHY.caption, color: COLORS.textTertiary, alignSelf: 'flex-start', marginBottom: SIZES.spacingSm) styles. ZERO hardcoded hex values (CLAUDE.md StyleSheet & Theming rule); use `COLORS.*` exclusively (excepting neutrals like `'rgba(0,0,0,0.6)'` for overlay, which mirrors QuickActionSheet:170).
  </action>
  <verify>
    <automated>npx jest src/components/moderation/__tests__/ListingModerationBottomSheet.test.tsx --bail</automated>
  </verify>
  <done>All 5 test blocks (A-E) pass. `grep -c "buildListingTitle\|ModerationService\|useAuth" src/components/moderation/ListingModerationBottomSheet.tsx` returns `0` (sibling-discipline guards). `grep -c "testID=\"listing-action-" src/components/moderation/ListingModerationBottomSheet.tsx` returns `5` (edit + suspend + archive + delete + restore). File is ~150-200 lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none for this plan) | Pure presentational component — no I/O, no auth checks, no service calls. The parent (Plan 08) reads `useAuth().isAdmin` to gate visibility and owns the optimistic-flip + rollback responsibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation (mobile-trust-isAdmin) | `ListingModerationBottomSheet` | mitigate (cross-plan) | Component never reads `useAuth` / `isAdmin`. Parent's `visible` prop controls render. Test Block E grep-asserts `useAuth` is absent. |
| T-10-06 | Repudiation (cross-domain enum drift) | bottom-sheet vocabulary | mitigate | Sibling component (NOT extending `QuickActionSheet`); Test Block E grep-asserts `ModerationService` is absent and no inline car-template-literal (Pitfall 6). Keeps the user-domain `QuickActionSheet` and listing-domain `ListingModerationBottomSheet` independently auditable for Phase 11 LQUAL-03. |
</threat_model>

<verification>
- `npx jest src/components/moderation/__tests__/ListingModerationBottomSheet.test.tsx --bail` — all 5 blocks green
- Full mobile suite: `npm test` — no regression on QuickActionSheet or other moderation components
- `grep -nE "(ModerationService|useAuth)" src/components/moderation/ListingModerationBottomSheet.tsx | wc -l` returns `0`
- `grep -nE "buildListingTitle|listingTitle.*\\$\\{.*makeName" src/components/moderation/ListingModerationBottomSheet.tsx | wc -l` returns `0` (component receives pre-built `listingTitle` as a prop — no inline concat)
- `grep -c "testID=\"listing-action-" src/components/moderation/ListingModerationBottomSheet.tsx` returns `5`
- ESLint: `npm run lint -- src/components/moderation/ListingModerationBottomSheet.tsx` passes
</verification>

<success_criteria>
- Plan 07 (reason + restore modals) can compose alongside this component in Plan 08 (CarDetailsScreen).
- Plan 08 mounts `<ListingModerationBottomSheet visible={bottomSheetVisible} listingTitle={buildListingTitle(fetchedCar)} moderationBadge={fetchedCar?.moderationBadge} onSelect={handleListingSheetSelect} onClose={...} />` and the visual + interaction contract is met.
- Plan 10 (AdminModeration Listings tab) MAY reuse the same component for its per-row inline interactions (Claude's Discretion); the prop shape supports both use cases.
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-plumbing-admin-listing-ui/10-06-SUMMARY.md` summarizing: new component file (~150 LOC), test file (5 blocks), and confirmation that sibling-discipline grep invariants hold.
</output>
