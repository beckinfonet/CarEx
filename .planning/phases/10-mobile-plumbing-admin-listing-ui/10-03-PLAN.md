---
phase: 10
plan: 03
type: execute
wave: 1
depends_on: []
cross_repo: true
files_modified:
  - ../backend-services/carEx-services/src/moderation/listingRouter.js
  - ../backend-services/carEx-services/src/moderation/listingService.js
  - ../backend-services/carEx-services/src/moderation/listingSchemas.js
  - ../backend-services/carEx-services/src/moderation/__tests__/listingRouter.search.test.js
autonomous: true
requirements: [LUI-04]
must_haves:
  truths:
    - "D-11: new GET /api/admin/moderation/listings endpoint mounted on existing listingRouter (server.js:925 chain — verifyIdToken + requireAdmin + listingModerationRateLimiter unchanged)"
    - "D-11: Car.find(filter).setOptions({ includeAllListingStatuses: true }) — Phase 9 D-01 bypass required so deleted/suspended/archived rows are visible"
    - "D-11: response shape { rows: ListingSearchItem[], nextCursor: string | null } mirrors SearchUsersResult / admin/router.js:93-206"
    - "Pitfall 10: q substring search whitelisted to makeName / modelName / listingId prefix ONLY — never description / phoneNumber / telegramUsername (PII guard)"
    - "Pitfall 8 + RESEARCH A3: uniform sort { createdAt: -1, _id: -1 } across all status filters (no moderatedAt sort in v1; defer to v1.2 if UAT demands)"
    - "searchListingsQuerySchema is .strict() Zod (Phase 7 D-09 mandatory whitelist discipline; rejects unknown query params with 400 invalid_payload)"
    - "Cross-repo plan: all 4 files live in ../backend-services/carEx-services; mobile work in Plan 04+ stubs the call until this lands"
  artifacts:
    - path: "../backend-services/carEx-services/src/moderation/listingRouter.js"
      provides: "GET / handler appended; existing 5 PATCH routes byte-identical"
      contains: "router.get('/'"
    - path: "../backend-services/carEx-services/src/moderation/listingService.js"
      provides: "searchListings({status,q,cursor,limit}) function returning {rows,nextCursor}"
      contains: "searchListings"
    - path: "../backend-services/carEx-services/src/moderation/listingSchemas.js"
      provides: "searchListingsQuerySchema (Zod .strict)"
      contains: "searchListingsQuerySchema"
    - path: "../backend-services/carEx-services/src/moderation/__tests__/listingRouter.search.test.js"
      provides: "integration tests (auth chain + hide-hook bypass + PII guard + cursor pagination)"
  key_links:
    - from: "../backend-services/carEx-services/src/moderation/listingRouter.js"
      to: "listingService.searchListings"
      via: "require('./listingService')"
      pattern: "service\\.searchListings"
    - from: "listingService.searchListings"
      to: "Car model with hide-hook bypass"
      via: "Car.find(filter).setOptions({ includeAllListingStatuses: true })"
      pattern: "includeAllListingStatuses: true"
  forbidden:
    - "Adding description / phoneNumber / telegramUsername / email to the searchListings $or clause (Pitfall 10 PII trawl risk; RESEARCH §T-10-03)"
    - "Omitting setOptions({ includeAllListingStatuses: true }) — without it Phase 9 hide-hook filters non-active rows, defeating LUI-04 (RESEARCH §T-10-04)"
    - "Mounting verifyIdToken / requireAdmin inline on the new route — they're already on the parent mount at server.js:925; double-mounting breaks rate-limit keying"
    - "Adding denySelfModerationListing middleware — irrelevant for a list/read endpoint (only PATCH routes use it)"
    - "Threading a second cursor encoder for `moderatedAt` (Pitfall 8) — v1 uniformly sorts by createdAt DESC; revisit in v1.2 if needed"
---

<objective>
Land the cross-repo backend slice for Phase 10: a new `GET /api/admin/moderation/listings?status=&q=&cursor=&limit=` endpoint that powers the mobile Listings tab in Plan 10. The endpoint is a near-verbatim port of the existing `GET /api/admin/users/search` (`../backend-services/carEx-services/src/admin/router.js:93-206`) with `User` → `Car` substitutions and the Phase 9 hide-hook bypass added.

Purpose: Phase 8 deferred the listing-list endpoint pending UI need; Plan 10 (Listings tab in `AdminModerationScreen`) is exactly that need. Without this endpoint, LUI-04 cannot ship. This plan is in Wave 1 because it has zero dependencies on the mobile substrate — backend and mobile substrate develop in parallel.

Output: One new route, one new service function, one new Zod schema, one new integration test file — all in `../backend-services/carEx-services/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-CONTEXT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md
@.planning/phases/09-backend-read-time-toctou-enforcement/09-CONTEXT.md

<interfaces>
<!-- Existing v1.0 searchUsers endpoint is the EXACT shape to port. -->
<!-- Existing listingRouter middleware chain at server.js:925 is unchanged. -->
<!-- Phase 9 D-01 hide hook on Car requires the includeAllListingStatuses bypass token. -->

From ../backend-services/carEx-services/server.js:925 (mount chain — unchanged):
  app.use('/api/admin/moderation/listings', verifyIdToken, requireAdmin, listingModerationRateLimiter, listingRouter);

From ../backend-services/carEx-services/src/admin/router.js:93-206 (searchUsers — the shape to port):
  router.get('/users/search', verifyIdToken, requireAdmin, async (req, res) => {
    // validation: invalid_q / q_too_long / invalid_role / invalid_state / invalid_cursor
    // filter construction: { $or: [{ email: { $regex: ... }}, { firebaseUid: { $regex: '^...' }}] }
    // cursor: { createdAt: { $lt: ... } } OR { createdAt: ..., _id: { $lt: ... } }
    // sort + limit+1 + hasMore + nextCursor encode
    return res.status(200).json({ users, nextCursor });
  });

From ../backend-services/carEx-services/src/moderation/listingSchemas.js (existing — restoreListingSchema is the sibling shape):
  const restoreListingSchema = z.object({ note: noteField }).strict();

From ../backend-services/carEx-services/src/moderation/listingRouter.js:143-160 (existing Phase 8 PATCH route — Zod safeParse + handleListingServiceError convention):
  router.patch('/:carId/suspend', denySelfModerationListing, async (req, res) => {
    const parsed = schemas.suspendListingSchema.safeParse(req.body || {});
    if (!parsed.success) return res.status(400).json({ error: 'invalid_payload', issues: parsed.error.issues });
    // ...
  });
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Wave 0 RED tests for GET /api/admin/moderation/listings</name>
  <files>../backend-services/carEx-services/src/moderation/__tests__/listingRouter.search.test.js</files>
  <read_first>
    - ../backend-services/carEx-services/src/moderation/__tests__/listingRouter.suspend.test.js (existing Phase 8 test — same test harness, supertest pattern, mongodb-memory-server replica-set fixture path)
    - ../backend-services/carEx-services/src/admin/router.js (lines 93-206 — `searchUsers` shape to mirror in test structure: auth-chain cases + cursor cases + filter cases)
    - ../backend-services/carEx-services/src/models/Car.js (Phase 9 hide-hook with includeAllListingStatuses bypass — tests must verify the bypass is load-bearing)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-VALIDATION.md (rows 10-10-01..05 — five tests this file must cover)
  </read_first>
  <behavior>
    - Block 1 (auth chain): (a) missing Bearer returns 401; (b) admin Bearer returns 200; (c) non-admin authenticated Bearer returns 403 from `requireAdmin`
    - Block 2 (response shape): admin GET returns `{ rows: [...], nextCursor: null }` shape for empty DB (rows is array, nextCursor is null not undefined)
    - Block 3 (hide-hook bypass — Pitfall 4 / T-10-04): seed 4 Cars with status `active`, `suspended`, `archived`, `deleted`; admin GET with no status filter returns ALL 4 rows; assertion fails if the count is less than 4 — proves `setOptions({ includeAllListingStatuses: true })` is wired
    - Block 4 (status filter): seed mixed; admin GET `?status=deleted` returns ONLY the deleted row; same for `suspended`, `archived`, `active` (4 separate assertion cases)
    - Block 5 (cursor pagination): seed 5 Cars with sequential `createdAt`; admin GET with `?limit=2` returns 2 rows + `nextCursor !== null`; GET with `?limit=2&cursor=<nextCursor>` returns the next 2 rows; final page returns `nextCursor === null`
    - Block 6 (q whitelist — Pitfall 10 / T-10-03): seed two Cars — one with `makeName: 'Toyota'` `description: 'unique_telltale_string_xyz'` and `phoneNumber: '555-0101'`; admin GET `?q=Toyota` returns the Toyota row; GET `?q=unique_telltale_string_xyz` returns 0 rows (description NOT in search); GET `?q=555` returns 0 rows (phone NOT in search); GET `?q=<listingId-prefix>` of the seeded row returns the row (listingId prefix IS in search)
    - Block 7 (Zod .strict): GET `?status=invalid_state` returns 400; GET `?limit=foo` returns 400 (z.coerce.number rejects non-numerics); GET `?unknown=field` returns 400 (`.strict()` rejects unknown keys)
    - Block 8 (invalid cursor): GET `?cursor=garbage` returns `{ rows: [], nextCursor: null }` (defensive — invalid cursor treated as empty per RESEARCH lines 1166-1168)
  </behavior>
  <action>Create the test file under `../backend-services/carEx-services/src/moderation/__tests__/listingRouter.search.test.js`. Build a minimal Express app inside `beforeAll` that mounts the existing `listingRouter` behind a fake auth middleware mirroring the Phase 7/8 test pattern (set `req.admin = { uid, email }` based on a custom header so tests can flip between admin/non-admin without real Firebase tokens). Use the existing `mongoReplSet.js` fixture (lines per Phase 2 D-02 `__tests__/_helpers/mongoReplSet.js` precedent). Seed Cars via the loose-schema `mongoose.model('Car')` pattern used in `__tests__/listingRouter.suspend.test.js`. Tests MUST initially fail because `router.get('/', ...)` does not yet exist on `listingRouter.js`. Group all 8 blocks inside one `describe('GET /api/admin/moderation/listings', () => { ... })`. Use `supertest` (already devDep).</action>
  <verify>
    <automated>cd ../backend-services/carEx-services && npx jest src/moderation/__tests__/listingRouter.search.test.js --bail 2>&1 | grep -E "FAIL|Cannot"</automated>
  </verify>
  <done>Test file exists and FAILS at the GET route handler (Express returns 404 since no GET / route is mounted yet). RED gate confirmed for Task 2/3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add searchListingsQuerySchema + searchListings service function (GREEN substrate)</name>
  <files>
    ../backend-services/carEx-services/src/moderation/listingSchemas.js
    ../backend-services/carEx-services/src/moderation/listingService.js
  </files>
  <read_first>
    - ../backend-services/carEx-services/src/moderation/listingSchemas.js (existing — restoreListingSchema pattern + .strict() discipline)
    - ../backend-services/carEx-services/src/moderation/listingService.js (existing — `suspendListing` / `restoreListing` patterns for module shape + module.exports footer)
    - ../backend-services/carEx-services/src/admin/router.js (lines 36-57 encodeCursor / decodeCursor / escapeRegex helpers — port verbatim into listingService.js)
    - ../backend-services/carEx-services/src/admin/router.js (lines 122-200 — filter construction + cursor predicate + limit+1 hasMore pattern)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md §"Backend GET /api/admin/moderation/listings" (lines 1099-1222 for the full target shape)
  </read_first>
  <action>
  Edit `../backend-services/carEx-services/src/moderation/listingSchemas.js`: append `const searchListingsQuerySchema = z.object({ status: z.enum(['active','suspended','archived','deleted']).optional(), q: z.string().max(128).optional(), cursor: z.string().optional(), limit: z.coerce.number().int().positive().max(100).optional() }).strict();` and extend the `module.exports` block to include `searchListingsQuerySchema`. Use `z.coerce.number()` for `limit` because query-string values are strings (existing file precedent for coerce).

  Edit `../backend-services/carEx-services/src/moderation/listingService.js`: (a) at module top, add three private helpers — `escapeRegex(str)` returning `String(str).replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&')`; `encodeCursor(item)` returning `Buffer.from(JSON.stringify({ createdAt: item.createdAt.toISOString(), _id: item._id.toString() }), 'utf8').toString('base64')` (null-safe — return `null` when item is falsy); `decodeCursor(cursor)` wrapping `JSON.parse(Buffer.from(cursor, 'base64').toString('utf8'))` in try/catch — return `null` for empty input, return `undefined` on parse failure (so the route can distinguish "no cursor" from "invalid cursor"). (b) add `async function searchListings({ status, q, cursor, limit = 25 })` that: builds an empty `filter = {}` + `andClauses = []`; if `status` present sets `filter.status = status`; if `q?.trim().length > 0` builds `filter.$or = [{ makeName: { $regex: escapeRegex(q.trim()), $options: 'i' }}, { modelName: { $regex: escapeRegex(q.trim()), $options: 'i' }}, { listingId: { $regex: '^' + escapeRegex(q.trim()) }}]` — ONLY these 3 fields per Pitfall 10; explicitly NOT description / phoneNumber / telegramUsername / email; decodes cursor and on `undefined` returns `{ rows: [], nextCursor: null }` (invalid cursor → empty); on a valid cursor pushes `{ $or: [{ createdAt: { $lt: cursor.createdAt }}, { createdAt: cursor.createdAt, _id: { $lt: cursor._id }}] }` into `andClauses`; if `andClauses.length > 0` sets `filter.$and = andClauses`; runs `await Car.find(filter).setOptions({ includeAllListingStatuses: true }).sort({ createdAt: -1, _id: -1 }).limit(limit + 1).lean()` — the `setOptions` call is LOAD-BEARING per Phase 9 D-01; maps rows to the `ListingSearchItem` shape `{ _id: c._id.toString(), status, makeName, modelName, year, price, firstPhotoUrl: Array.isArray(c.imageUrls) && c.imageUrls.length > 0 ? c.imageUrls[0] : null, sellerId, createdAt, moderatedAt, moderationReason, listingId }`; returns `{ rows: items, nextCursor: hasMore ? encodeCursor(items[items.length - 1]) : null }`. (c) append `module.exports.searchListings = searchListings;` to file footer.

  Tests from Task 1 still fail (no route mounted) — Task 3 closes the loop.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && node -e "const s = require('./src/moderation/listingSchemas'); console.log(typeof s.searchListingsQuerySchema);" && node -e "const s = require('./src/moderation/listingService'); console.log(typeof s.searchListings);"</automated>
  </verify>
  <done>Both module require calls log `object` (Zod schema) and `function` respectively; existing exports unchanged (`grep -c "restoreListingSchema" listingSchemas.js` unchanged from baseline; `grep -c "suspendListing" listingService.js` unchanged from baseline). The schema rejects unknown keys via `.strict()` and the service is callable with no route yet (Task 3 wires it).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mount router.get('/') handler on listingRouter (GREEN — all tests pass)</name>
  <files>../backend-services/carEx-services/src/moderation/listingRouter.js</files>
  <read_first>
    - ../backend-services/carEx-services/src/moderation/listingRouter.js (existing — Phase 8 PATCH routes lines 143-285 are the convention for safeParse + try/catch + console.error)
    - ../backend-services/carEx-services/src/admin/router.js (lines 93-110 for the route signature; lines 203-206 for the catch + 500 envelope)
    - ../backend-services/carEx-services/src/moderation/__tests__/listingRouter.search.test.js (Task 1's RED tests — they define the contract)
  </read_first>
  <action>Append a NEW `router.get('/', async (req, res) => { ... })` handler to `listingRouter.js`, positioned BEFORE the existing parameterized PATCH routes so Express does not mis-route. Handler body: (a) `const parsed = schemas.searchListingsQuerySchema.safeParse(req.query || {});` — if `!parsed.success` return `res.status(400).json({ error: 'invalid_payload', issues: parsed.error.issues });`; (b) wrap `const result = await service.searchListings(parsed.data); return res.json(result);` in try/catch; (c) catch logs `console.error('[GET /api/admin/moderation/listings] error', err)` and returns `res.status(500).json({ error: 'internal_error' })`. DO NOT inline `verifyIdToken` or `requireAdmin` on this route — the parent mount at `server.js:925` already provides them. DO NOT add `denySelfModerationListing` (read-only endpoint). Existing 5 PATCH routes MUST remain byte-identical (no edits below the `router.get('/')` insertion).</action>
  <verify>
    <automated>cd ../backend-services/carEx-services && npx jest src/moderation/__tests__/listingRouter.search.test.js --bail</automated>
  </verify>
  <done>All 8 test blocks from Task 1 pass. `grep -c "router.get('/'" src/moderation/listingRouter.js` returns `1`. `grep -c "router.patch" src/moderation/listingRouter.js` is unchanged from baseline (existing 5 PATCH routes intact). `grep -c "includeAllListingStatuses" src/moderation/listingService.js` returns `≥ 1` (bypass wired). `grep -c "description\\|phoneNumber\\|telegramUsername" src/moderation/listingService.js` inside the `searchListings` `$or` clause returns `0` (use targeted grep — Pitfall 10 PII guard).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile admin client → backend `/api/admin/moderation/listings` | Bearer-token-authenticated; `verifyIdToken` cryptographically validates the Firebase JWT, `requireAdmin` enforces admin claim, `listingModerationRateLimiter` caps at 30/15min/admin. This plan adds NO new middleware — only the new GET route handler inherits the existing chain. |
| backend route handler → MongoDB | Mongoose ODM with `.lean()` reads + the Phase 9 D-01 `pre(/^find/)` hide-hook bypass. The bypass is the load-bearing risk surface — incorrect use exposes non-active rows to non-admin contexts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation | `GET /api/admin/moderation/listings` mount | mitigate | Mount at `server.js:925` already enforces `verifyIdToken + requireAdmin`. Task 3 explicitly does NOT inline these middlewares on the route (double-mount would break rate-limit keying). Test Block 1 verifies 401/403 reach the client. |
| T-10-03 | Information Disclosure (PII trawl via search) | `searchListings` `$or` clause | mitigate | Task 2 explicitly whitelists the 3 search fields (`makeName`, `modelName`, `listingId` prefix). Test Block 6 seeds Cars with `description: 'unique_telltale_string_xyz'` and `phoneNumber: '555-0101'` and asserts `q=unique_telltale_string_xyz` + `q=555` both return 0 rows. Pitfall 10 + `forbidden` block in frontmatter prevents accidental field-set widening. |
| T-10-04 | Information Disclosure / Authorization (hide-hook silent filter) | `searchListings` `Car.find` chain | mitigate | Task 2 wires `.setOptions({ includeAllListingStatuses: true })` per Phase 9 D-01. Test Block 3 seeds 4 Cars across all 4 statuses and asserts admin sees all 4 rows in the unfiltered response — proving the bypass is wired. Without this, admin sees only `active` rows and LUI-04 fails silently. |
| T-10-cursor | Tampering (cursor injection) | `decodeCursor` | mitigate | Base64+JSON decode wrapped in try/catch; invalid cursor returns `undefined` and the route translates that to `{ rows: [], nextCursor: null }` (defensive empty). Test Block 8 verifies. |
| T-10-rate | DoS (excessive search calls) | route mount | accept (existing) | `listingModerationRateLimiter` caps at 30/15min/admin (Phase 7 D-04). Phase 10 inherits — no new mitigation required. |
</threat_model>

<verification>
- `cd ../backend-services/carEx-services && npx jest src/moderation/__tests__/listingRouter.search.test.js --bail` — all 8 blocks green
- Full backend suite: `cd ../backend-services/carEx-services && npm test` — no regressions on Phase 7/8/9 tests
- Grep invariants in `listingService.js`: `grep -c "includeAllListingStatuses" src/moderation/listingService.js` returns `≥ 1`; `grep -nE "description|phoneNumber|telegramUsername|email" src/moderation/listingService.js | wc -l` returns `0` (PII fields absent from the file entirely)
- Grep invariant on `listingRouter.js`: `grep -c "router.get('/'" src/moderation/listingRouter.js` returns `1`; existing PATCH count unchanged
- Existing Phase 8 routes still pass: `cd ../backend-services/carEx-services && npx jest src/moderation/__tests__/listingRouter.suspend.test.js src/moderation/__tests__/listingRouter.archive.test.js src/moderation/__tests__/listingRouter.delete.test.js src/moderation/__tests__/listingRouter.restore.test.js src/moderation/__tests__/listingRouter.edit.test.js --bail`
</verification>

<success_criteria>
- Mobile Plan 04 can call `apiClient.get('/api/admin/moderation/listings', { params })` and receive `{ rows, nextCursor }` against either a running local backend or against this plan's mocks.
- Admin sees all 4 listing statuses in the unfiltered response; non-admin gets 403; PII search via `description`/`phoneNumber` returns 0 rows.
- Cursor pagination works end-to-end (page-1 returns `nextCursor`; page-2 with that cursor returns the next batch; final page returns `nextCursor: null`).
- The new endpoint adds zero new env vars, zero new dependencies, zero new middleware — pure additive code on the existing chain.
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-plumbing-admin-listing-ui/10-03-SUMMARY.md` summarizing: 4 files changed, 1 new route mounted, test count (8 blocks), and the cross-repo commit SHA for inclusion in the v1.1 LIST-SECURITY.md merge gate (Phase 11). Note: this plan commits to the backend repo `../backend-services/carEx-services/` — use that working directory for the commit.
</output>
