---
phase: 10
plan: 04
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/services/moderation/ModerationService.ts
  - src/services/moderation/__tests__/listingMethods.test.ts
autonomous: true
requirements: [LMOB-01]
must_haves:
  truths:
    - "D-LMOB-01: Five new write methods + one new read method ALL live in src/services/moderation/ModerationService.ts (extend, not split; no AuthService methods touched per anti-pattern guardrail)"
    - "adminEditListing accepts STRUCTURED input { fields, existingImageUrls?, newFiles? } — service assembles multipart; SellCarScreen call site (Plan 09) stays readable (Claude's Discretion recommendation per CONTEXT)"
    - "adminEditListing sets explicit headers: { 'Content-Type': 'multipart/form-data' } per RESEARCH Pitfall 9 (axios + RN FormData polyfill needs explicit header)"
    - "All 5 write methods wrap axios errors via toListingModerationError(error) helper → throw ListingModerationError (Plan 01 substrate)"
    - "searchListings (read) does NOT throw ListingModerationError — raw axios error surfaces to caller's EmptyState; isAbortError guard prevents console.error on cancellation (RESEARCH lines 916-921; mirrors Pattern S1 + Plan 05-03 searchUsers convention)"
    - "PATCH path shape: /api/admin/moderation/listings/:carId/<suspend|archive|delete|restore> (Phase 8 D-01); adminEditListing PATCH /api/admin/moderation/listings/:carId (no action segment)"
    - "Anti-pattern guardrail (RESEARCH §): grep -c 'suspendListing\\|archiveListing\\|deleteListing\\|restoreListing\\|adminEditListing' src/services/AuthService.ts returns 0"
  artifacts:
    - path: "src/services/moderation/ModerationService.ts"
      provides: "Extended with 5 write methods + searchListings + toListingModerationError helper"
      contains: "adminEditListing"
    - path: "src/services/moderation/__tests__/listingMethods.test.ts"
      provides: "GREEN tests for 6 methods — paths, bodies, multipart Content-Type, error wrapping"
  key_links:
    - from: "src/services/moderation/ModerationService.ts"
      to: "src/services/http/client.ts apiClient"
      via: "import { apiClient } from '../http/client' (existing module-top import)"
      pattern: "apiClient\\.(patch|get)"
    - from: "src/services/moderation/ModerationService.ts write methods"
      to: "src/services/moderation/errors.ts ListingModerationError"
      via: "throw toListingModerationError(error)"
      pattern: "toListingModerationError"
  forbidden:
    - "Adding listing-mod methods to src/services/AuthService.ts (anti-pattern guardrail — grep MUST return 0)"
    - "Inline-rebuilding multipart FormData in SellCarScreen's submit branch (Plan 09 will use structured input — adminEditListing assembles FormData centrally; CONTEXT anti-pattern + Pitfall 9)"
    - "Throwing ListingModerationError from searchListings (read method) — let raw axios error surface; RESEARCH lines 916-921 documents why (500-class errors are infra/dev bugs, not domain rejections)"
    - "Calling apiClient.put for adminEditListing (Phase 8 D-01 uses PATCH for all 5 listing-mod endpoints; PUT is the seller-only path on /api/cars/:id)"
---

<objective>
Extend the existing `ModerationService` object module with 5 new write methods (`adminEditListing`, `suspendListing`, `archiveListing`, `deleteListing`, `restoreListing`) and 1 new read method (`searchListings`) — all delegating to the Phase 8 backend write endpoints and the Plan 03 cross-repo GET endpoint via the existing `apiClient`.

Purpose: This is the LMOB-01 substrate. Every Phase 10 mobile UI surface (Plans 08 CarDetails, 09 SellCar adminEdit, 10 AdminModeration Listings tab) calls these 6 methods. Per CONTEXT §Anti-Pattern Warnings: NO listing methods in `AuthService.ts`; NO third axios interceptor (Pitfall 1); per-method catch wraps via `toListingModerationError` produce typed `ListingModerationError` throws (Plan 01 substrate). `searchListings` deliberately keeps the bare `throw error` to let raw axios errors surface to the screen's `EmptyState` (RESEARCH lines 916-921).

Output: Additive edits to `ModerationService.ts` (new section under existing methods, no edits to existing methods) + a co-located test file with GREEN coverage of all 6 new methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-CONTEXT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-01-SUMMARY.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md

<interfaces>
<!-- The existing ModerationService module (320 lines) is the EXACT pattern to extend. -->
<!-- Plan 01 added ListingModerationError — Plan 04 wraps axios errors via toListingModerationError → throw it. -->

From src/services/moderation/ModerationService.ts (existing, sections at lines 168 / 281 / 320):
  export const ModerationService = {
    // --- Admin writes ---
    suspend: async (targetUid, body) => { try { const r = await apiClient.post(...); return r.data; } catch (e) { console.error('Failed to suspend user', e); throw e; } },
    unsuspend: async (targetUid, body = {}) => { try { const r = await apiClient.patch(`/api/admin/moderation/${targetUid}/unsuspend`, body); return r.data; } catch (e) { console.error('Failed to unsuspend user', e); throw e; } },
    // ... 5 more existing methods ...
    // --- Reads ---
    searchUsers: async (query, config) => { try { const r = await apiClient.get('/api/admin/users/search', { params: query, signal: config?.signal }); return r.data; } catch (e) { if (isAbortError(e)) throw e; console.error('Failed to search users', e); throw e; } },
    getHistory: async (...) => { ... },
  };

  function isAbortError(err) {
    if (axios.isCancel?.(err)) return true;
    const name = (err)?.name;
    return name === 'CanceledError' || name === 'AbortError';
  }

From src/services/moderation/errors.ts (after Plan 01):
  export class ListingModerationError extends Error { /* 10-code union + 6 context fields per Plan 01 */ }

From .planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md §Code Examples (lines 824-925):
  - Path shapes per method
  - toListingModerationError helper signature
  - AdminEditListingInput structured shape (fields + existingImageUrls + newFiles)

From .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md:
  - D-01: 5 listing-mod endpoints (PATCH /api/admin/moderation/listings/:carId[/<action>])
  - D-02: success response shape { ok: true, listing, action }
  - D-D: Edit multipart shape — same form layout as seller PUT /api/cars/:id (multer.array('images', 25) + existingImageUrls JSON array)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED tests for 6 new ModerationService listing methods</name>
  <files>src/services/moderation/__tests__/listingMethods.test.ts</files>
  <read_first>
    - src/services/moderation/__tests__/ModerationService.test.ts (existing — jest.mock('../../http/client') pattern; describe/test structure to mirror)
    - src/services/moderation/__tests__/ModerationService.searchUsers.test.ts (existing — pagination + AbortSignal test conventions)
    - src/services/moderation/ModerationService.ts (existing — confirm import paths and helper signatures before mocking)
    - src/services/moderation/errors.ts (post-Plan-01 — confirm ListingModerationError importable for `instanceof` assertions)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-VALIDATION.md (rows 10-02-01..04 — four test scenarios this file must cover)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md §Code Examples (lines 824-925) for exact path strings and body shapes
  </read_first>
  <behavior>
    - Block A — `suspendListing(carId, body)`: (1) calls `apiClient.patch` with path `/api/admin/moderation/listings/<carId>/suspend` and body `{ reasonCategory: 'spam', note: 'duplicate' }`; (2) returns `response.data` verbatim on success; (3) on 400 `{ error: 'already_in_state' }` throws an `instanceof ListingModerationError` with `code === 'already_in_state'` and `httpStatus === 400`
    - Block B — `archiveListing(carId, body)`: same 3 assertions with path `/archive` and `code === 'invalid_payload'` on the error case
    - Block C — `deleteListing(carId, body)`: same 3 assertions with path `/delete` and `code === 'cannot_moderate_own_listing'` on the error case (Phase 8 D-04)
    - Block D — `restoreListing(carId, body?)`: (1) calls `apiClient.patch` with path `/restore` and body `{ note: 'mistaken delete' }`; (2) calling with no body argument still POSTs an empty `{}` body (default param); (3) error case throws `ListingModerationError` with `code === 'not_moderated'`
    - Block E — `adminEditListing(carId, structured)`: (1) builds a `FormData` instance, appends each scalar field via `formData.append(k, String(v))`, appends `knownIssues` as `JSON.stringify(value)`, appends `existingImageUrls` as `JSON.stringify(input.existingImageUrls)`, appends each `newFiles[i]` as `{ uri, type, name }`; (2) calls `apiClient.patch(\`/api/admin/moderation/listings/<carId>\`, formData, { headers: { 'Content-Type': 'multipart/form-data' } })` — assert the headers object literal; (3) error case (mock 400 `{ error: 'invalid_make' }`) throws `ListingModerationError` with `code === 'invalid_make'` and `httpStatus === 400`; (4) error context fields preserved: mock backend includes `listingStatus: 'suspended'` + `reasonCategory: 'spam'` → assert these survive through `toListingModerationError`
    - Block F — `searchListings(query, { signal })`: (1) calls `apiClient.get('/api/admin/moderation/listings', { params: query, signal })`; (2) returns `{ rows: [], nextCursor: null }` defensively when `response.data` lacks `rows`; (3) `axios.isCancel` / `CanceledError` / `AbortError` re-thrown as-is WITHOUT a `ListingModerationError` wrap (mirrors Pattern S1 isAbortError guard); (4) non-cancel error re-thrown as-is (raw axios error — NOT wrapped — per RESEARCH 916-921)
    - Block G — anti-pattern grep guard: in-test source assertion using `fs.readFileSync` of `src/services/AuthService.ts` — `expect(content).not.toMatch(/suspendListing|archiveListing|deleteListing|restoreListing|adminEditListing|searchListings/)` (CONTEXT anti-pattern; mirrors Phase 5 plan-05-09 MOB-01 grep guard)
  </behavior>
  <action>Create `src/services/moderation/__tests__/listingMethods.test.ts`. Use `jest.mock('../../http/client', () => ({ apiClient: { patch: jest.fn(), get: jest.fn() } }))` for the mock pattern (mirror `ModerationService.test.ts`). Import `apiClient` and cast as `(apiClient.patch as jest.Mock)` in assertions. For Block E's FormData assertion, capture the first `patch` call's second argument and assert it `instanceof FormData` (note: React Native jest preset provides global FormData); to verify field assembly without depending on FormData internals, replace FormData with a small stub in jest setup OR assert the call signature only (assertion (2)). Use `it.each` for the 6-method-shared assertions when reasonable. File MUST fail initially because the 6 new methods don't exist on `ModerationService` yet.</action>
  <verify>
    <automated>npx jest src/services/moderation/__tests__/listingMethods.test.ts --bail 2>&1 | grep -E "FAIL|TypeError|is not a function"</automated>
  </verify>
  <done>Test file exists; all blocks fail because the 6 methods are not on `ModerationService` (TypeError "...is not a function"). RED gate confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append the 6 new methods + toListingModerationError helper (GREEN)</name>
  <files>src/services/moderation/ModerationService.ts</files>
  <read_first>
    - src/services/moderation/ModerationService.ts (FULL current file — existing 7 methods, isAbortError helper at line 34, section comments at lines 168/281)
    - src/services/moderation/errors.ts (post-Plan-01 ListingModerationError)
    - src/services/moderation/__tests__/listingMethods.test.ts (the RED contract from Task 1)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md §"src/services/moderation/ModerationService.ts (MODIFIED — extend, don't split)" KEEP/CHANGE list + Pattern S1
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md §Code Examples (lines 824-925) for the EXACT method bodies
  </read_first>
  <action>
  Edit `src/services/moderation/ModerationService.ts`:

  1. At the top of the file with the existing interfaces, add type exports: `ListingReasonCategory` (`'spam' | 'policy_violation' | 'fraud' | 'inactive_seller' | 'other'`), `SuspendListingBody`/`ArchiveListingBody`/`DeleteListingBody` (`{ reasonCategory: ListingReasonCategory; note?: string }`), `RestoreListingBody` (`{ note?: string }`), `AdminEditListingInput` (`{ fields: { makeId?, modelId?, trimLevel?, wheelbase?, fuel?, currency?, description?, bodyType?, engine?, transmission?, drivetrain?, mpg?, condition?, exteriorColor?, interiorColor?, interiorMaterial?, phoneNumber?, telegramUsername?: string; year?, price?, mileage?, seats?, doors?: number; knownIssues?: string[] }; existingImageUrls?: string[]; newFiles?: Array<{ uri: string; type?: string; name?: string }> }`), `ListingActionResponse` (`{ ok: true; listing: { _id, status, moderatedBy?, moderatedAt?, lastEditedBy?, lastEditedAt? }; action: { _id, action: 'suspend'|'archive'|'delete'|'restore'|'edit', fromStatus, toStatus, createdAt } }`), `ListingSearchItem` (`{ _id, status, makeName?, modelName?, year?, price?, firstPhotoUrl?, sellerId, createdAt, moderatedAt?, moderationReason?, listingId? }`), `SearchListingsQuery` (`{ status?, q?, cursor?, limit? }`), `SearchListingsResult` (`{ rows: ListingSearchItem[], nextCursor: string | null }`). Use the exact field lists from RESEARCH §Code Examples lines 710-808.

  2. Below the existing `isAbortError` helper (near line 38), add a new module-private helper `function toListingModerationError(error: unknown): ListingModerationError` that extracts `axiosErr.response?.data` and returns `new ListingModerationError(data?.error ?? 'unknown', data?.listingStatus, data?.reasonCategory, data?.banner, data?.refundId, data?.refundFailed, axiosErr.response?.status)`. Import `ListingModerationError` from `./errors` at the top of the file (the existing top-of-file already imports from `./errors` — extend, do NOT add a duplicate import line).

  3. Append a NEW section comment `// --- Listing moderation writes ---` AFTER the existing methods (before the closing `}` of the `ModerationService` object literal). Add the 5 methods in this order: `adminEditListing`, `suspendListing`, `archiveListing`, `deleteListing`, `restoreListing`. EACH follows the existing `unsuspend` skeleton (try / `apiClient.patch` / return response.data / catch / `console.error('Failed to <verb> listing', error)`) EXCEPT the catch block does `throw toListingModerationError(error)` instead of `throw error`. Path strings: `\`/api/admin/moderation/listings/${carId}\`` (adminEditListing), then `/suspend`, `/archive`, `/delete`, `/restore` suffixes. `restoreListing` default body: `body: RestoreListingBody = {}`. `adminEditListing` body assembly: build `FormData` per RESEARCH lines 829-846 — iterate `Object.entries(input.fields)`, skip `undefined` values, JSON.stringify `knownIssues`, JSON.stringify `existingImageUrls`, append each `newFiles[i]` as `{ uri, type, name }` using `@ts-ignore RN FormData accepts {uri,type,name}` comment. CRITICAL: pass `{ headers: { 'Content-Type': 'multipart/form-data' } }` as the third `apiClient.patch` arg (Pitfall 9).

  4. Append section comment `// --- Listing moderation reads ---` and the `searchListings` method. Body mirrors the existing `searchUsers` (lines 260-279): try / `apiClient.get('/api/admin/moderation/listings', { params: query, signal: config?.signal })` / defensive return `{ rows: Array.isArray(data.rows) ? data.rows : [], nextCursor: data.nextCursor ?? null }` / catch / `if (isAbortError(error)) throw error;` / `console.error('Failed to search listings', error)` / `throw error` (raw — NOT `toListingModerationError`).

  5. Preserve all 7 existing methods byte-identical (no edits to `suspend`, `unsuspend`, `revokeRole`, `restoreRole`, `editProfile`, `deleteProviderProfile`, `searchUsers`, `getHistory`). Existing section comments stay.
  </action>
  <verify>
    <automated>npx jest src/services/moderation/__tests__/listingMethods.test.ts --bail && npx jest src/services/moderation/__tests__/ModerationService.test.ts src/services/moderation/__tests__/ModerationService.searchUsers.test.ts src/services/moderation/__tests__/ModerationService.getHistory.test.ts --bail</automated>
  </verify>
  <done>All Task 1 tests pass (GREEN). All pre-existing ModerationService tests still pass (no regression). `grep -c "adminEditListing\|suspendListing\|archiveListing\|deleteListing\|restoreListing\|searchListings" src/services/moderation/ModerationService.ts` returns `≥ 6`. `grep -c "toListingModerationError" src/services/moderation/ModerationService.ts` returns `≥ 6` (one helper definition + 5 write-method call sites). `grep -c "throw error" src/services/moderation/ModerationService.ts` includes the bare `searchListings` throw and the existing `searchUsers`/`getHistory` throws.</done>
</task>

<task type="auto">
  <name>Task 3: Lock anti-pattern guards via filesystem assertions</name>
  <files>src/services/moderation/__tests__/listingMethods.test.ts</files>
  <read_first>
    - src/services/AuthService.ts (current — must NOT contain any listing-mod method names)
    - src/services/http/client.ts (current — must NOT add a third response interceptor)
    - src/services/moderation/__tests__/listingMethods.test.ts (current state after Task 1+2)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md §Anti-Pattern Guardrails table (lines 1501-1518) — grep invariants
  </read_first>
  <action>Append three additional `it()` assertions to `listingMethods.test.ts` under a new `describe('anti-pattern guards', () => { ... })` block. Test 1: read `src/services/AuthService.ts` via `fs.readFileSync` and assert the content does NOT match `/suspendListing|archiveListing|deleteListing|restoreListing|adminEditListing|searchListings/` (CONTEXT MOB-01 mirror). Test 2: read `src/services/http/client.ts` and assert the count of `interceptors.response.use` substrings equals exactly `2` (no third interceptor added per Pitfall 1 / T-10-02). Test 3: read `src/services/moderation/errors.ts` and assert that the substring between `class ModerationError` and the first `}` AFTER it does NOT contain any of the listing codes (`listing_not_available`, `listing_not_found`, `cannot_moderate_own_listing`, `already_in_state`, `not_moderated`) — duplicates Plan 01 Task 3 but locks the invariant against accidental edits across both plans.</action>
  <verify>
    <automated>npx jest src/services/moderation/__tests__/listingMethods.test.ts --bail</automated>
  </verify>
  <done>All 3 anti-pattern guards pass; combined `listingMethods.test.ts` contains the 6 method test blocks + 3 anti-pattern guards. `grep -c "interceptors.response.use" src/services/http/client.ts` returns `2` from the shell as well.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile UI (Plans 08/09/10) → ModerationService | TypeScript interfaces enforce body shape; service is the only entry to backend listing-mod endpoints from mobile (no direct axios calls from screens). |
| ModerationService → backend `/api/admin/moderation/listings/*` | Bearer-token request via `apiClient` (existing chain: Bearer + 401 refresh + 403 user-suspension interceptor — none of these fire on listing 409/403/400 per T-10-02 + Plan 05 regression tests). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation | `ModerationService.adminEditListing/suspendListing/archiveListing/deleteListing/restoreListing` | mitigate | Mobile NEVER pre-checks `isAdmin` to skip these calls (D-02). Backend `requireAdmin` + `denySelfModerationListing` are the authority. Plan 08's CarDetails wiring shows the badge unconditionally for admin users; backend rejection surfaces via `ListingModerationError` banner. |
| T-10-02 | Spoofing (admin session loss via wrong interceptor) | `client.ts` 403 interceptor | mitigate | Task 3 Test 2 asserts `interceptors.response.use` count remains `2` — no third interceptor added for listing errors. Plan 05 adds regression tests that 409 `listing_not_available` + 403 `cannot_moderate_own_listing` do NOT log the admin out. |
| T-10-03 | Information Disclosure (PII leak via search) | `searchListings` | mitigate (backend) | Plan 03 backend whitelists q-search fields. Mobile blindly forwards the `q` param. |
| T-10-04 | Information Disclosure (admin sees only active rows) | `searchListings` | mitigate (backend) | Plan 03 backend wires `setOptions({ includeAllListingStatuses: true })`. Mobile doesn't pass the flag — backend defaults to admin-sees-all. |
| T-10-06 | Repudiation (audit blur across user/listing domains) | `toListingModerationError` helper | mitigate | The helper throws ONLY `ListingModerationError` (never widens `ModerationError`). Task 3 Test 3 enforces at the source level. |
| T-10-multipart | Tampering (admin Edit upload silently serialized as JSON) | `adminEditListing` | mitigate | Pitfall 9: explicit `headers: { 'Content-Type': 'multipart/form-data' }` on the `apiClient.patch` call. Task 1 Block E assertion (2) captures the headers literal — regression fails the test. |
</threat_model>

<verification>
- `npx jest src/services/moderation/__tests__/listingMethods.test.ts --bail` — all blocks (A-G + anti-pattern guards) green
- `npx jest src/services/moderation/__tests__/ --bail` — full ModerationService suite still green (no regression on 7 existing methods)
- `grep -c 'suspendListing\|archiveListing\|deleteListing\|restoreListing\|adminEditListing\|searchListings' src/services/AuthService.ts` returns `0`
- `grep -c "interceptors.response.use" src/services/http/client.ts` returns `2`
- `grep -c "toListingModerationError" src/services/moderation/ModerationService.ts` returns `≥ 6` (helper + 5 write methods)
- `grep -c "throw toListingModerationError" src/services/moderation/ModerationService.ts` returns exactly `5` (NOT in searchListings)
- `grep -c "Content-Type.*multipart/form-data" src/services/moderation/ModerationService.ts` returns `1` (only in adminEditListing)
</verification>

<success_criteria>
- Plans 08, 09, 10 can import all 6 methods from `ModerationService` and call them with typed inputs.
- A 4xx response from any of the 5 write endpoints produces a typed `ListingModerationError` with the code preserved verbatim.
- A 5xx response (or network failure) on `searchListings` surfaces as a raw axios error to the Listings tab's `EmptyState` (no spurious `ListingModerationError` wrap that would imply a domain rejection).
- A cancelled `searchListings` request (via AbortController) re-throws the AbortError WITHOUT a `console.error` log (matches existing `searchUsers` convention).
- The LMOB-01 grep invariant holds: `AuthService.ts` is byte-identical to its pre-Phase-10 state with respect to listing-mod method names.
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-plumbing-admin-listing-ui/10-04-SUMMARY.md` summarizing: lines added to `ModerationService.ts`, list of new exports (types + methods), test file size, and confirmation that all 4 grep invariants hold.
</output>
