---
phase: 10
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/listingTitle.ts
  - src/utils/__tests__/listingTitle.test.ts
autonomous: true
requirements: [LUI-02]
must_haves:
  truths:
    - "D-08: Sentinel = listing title = `${year} ${makeName} ${modelName}` (matches Phase 9 D-05 backend thin-payload concatenation order)"
    - "D-08a: Case-insensitive + whitespace-trimmed match between typed input and canonical title"
    - "D-08b: Fall back to makeId / modelId literal strings when denormalized make/model name fields are missing (extreme legacy data)"
    - "Pitfall 6: single shared `buildListingTitle(car)` helper — bottom sheet header AND TypedConfirmationModal target both consume the SAME string (no per-call-site concat divergence)"
  artifacts:
    - path: "src/utils/listingTitle.ts"
      provides: "buildListingTitle + matchesListingTitleSentinel pure functions"
      contains: "export function buildListingTitle"
    - path: "src/utils/__tests__/listingTitle.test.ts"
      provides: "GREEN tests for title format + fallback + case/whitespace match"
  key_links:
    - from: "src/utils/listingTitle.ts"
      to: "Plan 08 CarDetailsScreen + Plan 06 ListingModerationBottomSheet"
      via: "import { buildListingTitle } from '../../utils/listingTitle'"
      pattern: "buildListingTitle\\("
  forbidden:
    - "Per-call-site concat (Pitfall 6) — any consumer that templates `${car.year} ${car.makeName} ${car.modelName}` inline instead of importing buildListingTitle breaks the sentinel match invariant. Grep `${.*\\.year.*\\.make.*\\.model}` across src/ to find regressions."
    - "Adding `useVehicleCatalog` round-trip from inside the helper (D-08b explicit) — helper stays a pure synchronous function with no I/O"
---

<objective>
Land a tiny pure-function module that locks the canonical listing-title concatenation and the case-insensitive + trimmed match used by Phase 10's typed-confirmation sentinel.

Purpose: Pitfall 6 (RESEARCH §Common Pitfalls) names this exact regression — different render sites concatenate `${make} ${model}` vs `${year} ${make} ${model}` vs `${make} ${model} ${year}`, so the admin reads one string from the bottom-sheet header but the `TypedConfirmationModal` matches against a different one → mismatch forever. A single helper makes that impossible: both the header and the sentinel match call the same function on the same Car object.

Output: `src/utils/listingTitle.ts` with two named exports + a GREEN test file co-located under `src/utils/__tests__/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-CONTEXT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md

<interfaces>
<!-- passwordPolicy.ts is the existing pure-function-module pattern to mirror. -->

From src/utils/passwordPolicy.ts (existing, 22 lines):
  export const PASSWORD_MIN_LENGTH = 7;
  export type PasswordRequirementId = 'length' | 'uppercase' | 'number' | 'symbol';
  export interface PasswordRequirementCheck { id: PasswordRequirementId; met: boolean; }
  export function getPasswordRequirementChecks(password: string): PasswordRequirementCheck[] { ... }
  export function passwordMeetsPolicy(password: string): boolean { ... }

From .planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md (D-08 + Pitfall 6):
  - Canonical concat: `${year} ${makeName} ${modelName}` (Phase 9 D-05 backend thin-payload format)
  - Match rule (D-08a): input.trim().toLowerCase() === canonical.trim().toLowerCase()
  - Fallback (D-08b): if makeName/modelName missing, use makeId/modelId as the literal substitute
  - Existing CarCard.tsx:34 uses `{data.make} {data.model}` (NO year) — Phase 10 deliberately diverges
  - Existing CarDetailsScreen.tsx:559 uses `{car.make} {car.model} {trimLevel?} {car.year}` — Phase 10 deliberately diverges
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Wave 0 RED tests for buildListingTitle + matchesListingTitleSentinel</name>
  <files>src/utils/__tests__/listingTitle.test.ts</files>
  <read_first>
    - src/utils/passwordPolicy.ts (existing pure-function module shape to mirror)
    - src/utils/__tests__/moderationErrorKeyMap.test.ts (existing co-located test format reference for src/utils tests)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md §Common Pitfalls "Pitfall 6: Listing-Title Sentinel Concatenation Doesn't Match What Admin Reads" + D-08/D-08a/D-08b
  </read_first>
  <behavior>
    - Test 1 (canonical happy path): `buildListingTitle({ year: 2018, makeName: 'Toyota', modelName: 'Camry' })` returns `'2018 Toyota Camry'`
    - Test 2 (whitespace trim): `buildListingTitle({ year: 2018, makeName: '  Toyota  ', modelName: ' Camry ' })` returns `'2018 Toyota Camry'` (no double spaces, no leading/trailing whitespace)
    - Test 3 (fallback D-08b): `buildListingTitle({ year: 2018, makeId: 'mk_001', modelId: 'md_023' })` (no makeName/modelName) returns `'2018 mk_001 md_023'`
    - Test 4 (partial fallback): `buildListingTitle({ year: 2018, makeName: 'Toyota', modelId: 'md_023' })` returns `'2018 Toyota md_023'` (per-field fallback)
    - Test 5 (year missing): `buildListingTitle({ makeName: 'Toyota', modelName: 'Camry' })` returns `'Toyota Camry'` (no leading "undefined" or empty token)
    - Test 6 (everything missing): `buildListingTitle({})` returns `''` (empty string, NOT a crash)
    - Test 7 (sentinel exact match): `matchesListingTitleSentinel('2018 Toyota Camry', { year: 2018, makeName: 'Toyota', modelName: 'Camry' })` returns `true`
    - Test 8 (sentinel case-insensitive D-08a): `matchesListingTitleSentinel('2018 toyota CAMRY', sameCar)` returns `true`
    - Test 9 (sentinel whitespace-trimmed D-08a): `matchesListingTitleSentinel('  2018 Toyota Camry  ', sameCar)` returns `true`
    - Test 10 (sentinel mismatch): `matchesListingTitleSentinel('2019 Toyota Camry', sameCar)` returns `false`
    - Test 11 (sentinel empty input): `matchesListingTitleSentinel('', sameCar)` returns `false` (empty input never matches even when canonical is also empty — defensive)
  </behavior>
  <action>Create `src/utils/__tests__/listingTitle.test.ts`. Import `buildListingTitle` and `matchesListingTitleSentinel` from `../listingTitle`. Type the `car` argument as a minimal structural type inline (e.g., `{ year?: number; makeName?: string; modelName?: string; makeId?: string; modelId?: string }`) — keep tests decoupled from the full Car type. Group the 11 tests under one `describe('listingTitle', () => { ... })` with nested `describe('buildListingTitle', ...)` and `describe('matchesListingTitleSentinel', ...)`. File MUST fail to compile right now (module does not exist) — this is the RED gate.</action>
  <verify>
    <automated>npx jest src/utils/__tests__/listingTitle.test.ts --bail 2>&1 | grep -E "Cannot find|FAIL"</automated>
  </verify>
  <done>Test file exists and fails (compile-error or jest FAIL on all 11 tests) confirming RED state before Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement listingTitle.ts pure module (GREEN)</name>
  <files>src/utils/listingTitle.ts</files>
  <read_first>
    - src/utils/__tests__/listingTitle.test.ts (the RED contract from Task 1)
    - src/utils/passwordPolicy.ts (mirror this style — named exports, types at top, no React, no project imports)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md §"src/utils/listingTitle.ts (NEW)" KEEP/CHANGE list
  </read_first>
  <action>Create `src/utils/listingTitle.ts`. Export an `interface ListingTitleSource` with optional fields `year?: number; makeName?: string; modelName?: string; makeId?: string; modelId?: string` (this lets the helper accept either the full admin Car doc or the Phase 9 D-05 thin payload). Export `function buildListingTitle(car: ListingTitleSource): string` that: (a) computes `year = car.year != null ? String(car.year) : ''`, (b) `make = (car.makeName ?? car.makeId ?? '').trim()`, (c) `model = (car.modelName ?? car.modelId ?? '').trim()`, (d) joins `[year, make, model]` with single spaces, filtering out empty/whitespace tokens via `.filter(Boolean)` so the result never contains double spaces or trailing whitespace. Export `function matchesListingTitleSentinel(input: string, car: ListingTitleSource): boolean` that: (a) computes `canonical = buildListingTitle(car).trim().toLowerCase()`, (b) computes `typed = String(input ?? '').trim().toLowerCase()`, (c) returns `typed.length > 0 && typed === canonical`. NO React imports, NO axios imports, NO `useVehicleCatalog` import (D-08b explicit — no I/O). File is pure TS module.</action>
  <verify>
    <automated>npx jest src/utils/__tests__/listingTitle.test.ts --bail</automated>
  </verify>
  <done>All 11 tests pass. File exports `buildListingTitle` + `matchesListingTitleSentinel` + `ListingTitleSource` interface. Running `grep -c "useVehicleCatalog\|axios\|apiClient" src/utils/listingTitle.ts` returns `0` (no I/O).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none for this plan) | Pure synchronous helper with no I/O and no external data crossing — operates only on the `car` object the caller already holds in memory. The sentinel match itself is not a security control (it is friction-with-purpose, per D-08a + RESEARCH §T-10-05); the authoritative reject is backend `cannot_moderate_own_listing` / `denySelfModerationListing`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering (case-variant bypass) | `matchesListingTitleSentinel` | accept | Friction-with-purpose by design (D-08a): admin types the title they just read to confirm Delete intent. Case-insensitive + trimmed comparison reduces UX friction without weakening any security control — the backend `denySelfModerationListing` middleware + `requireAdmin` are the actual authority. No mitigation needed beyond documenting this disposition. |
| T-10-06 (relates) | Repudiation (test-coverage drift) | `buildListingTitle` consumers | mitigate (cross-plan) | This plan + Plan 06 (bottom sheet) + Plan 08 (CarDetails wiring) all consume the same `buildListingTitle` import. Plan 08's `<acceptance_criteria>` includes a grep invariant that the typed-confirm-target and the sheet-header text both come from `buildListingTitle(car)` — preventing the inline-concat divergence Pitfall 6 names. |
</threat_model>

<verification>
- `npx jest src/utils/__tests__/listingTitle.test.ts --bail` — all 11 tests green
- `grep -c "useVehicleCatalog\|axios\|apiClient\|react" src/utils/listingTitle.ts` returns `0` (pure module, no project-code dependencies)
- The file is ≤ 50 lines (it is genuinely tiny — Pattern-S analog `passwordPolicy.ts` is 22 lines; this should be similar)
</verification>

<success_criteria>
- Plan 06 (bottom sheet) and Plan 08 (CarDetailsScreen) can `import { buildListingTitle, matchesListingTitleSentinel } from '../../utils/listingTitle'` (or `'../utils/listingTitle'` from screens) and rely on the same concatenation across both call sites.
- Pitfall 6 is structurally impossible after this plan: the sentinel target and the bottom-sheet header receive byte-identical strings because they call the same function on the same `car` argument.
- Phase 10 picks up zero new runtime dependencies — pure standard-library TS.
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-plumbing-admin-listing-ui/10-02-SUMMARY.md` summarizing: file added, test count (11), and confirmation that `grep` invariants (no I/O imports, pure module) hold.
</output>
