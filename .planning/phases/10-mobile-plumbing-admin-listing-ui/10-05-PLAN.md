---
phase: 10
plan: 05
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/services/http/__tests__/clientListing409.test.ts
  - src/screens/CarDetailsScreen.tsx
autonomous: true
requirements: [LMOB-02, LUI-01]
must_haves:
  truths:
    - "D-13 + LMOB-02: client.ts 403 interceptor matches `error === 'account_suspended'` ONLY — listing 409/403/400 errors all pass through without firing the user-suspension flow"
    - "RESEARCH A6 (LOAD-BEARING): CarDetailsScreen line 112 currently uses bare `axios.get(${API_URL}/api/cars/${carId})` — Bearer header not sent → admin path treated as non-admin by backend → moderationBadge omitted. Plan 05 migrates this single call to `apiClient.get(\\`/api/cars/${carId}\\`)` so the Bearer header is attached and Phase 9 D-07 admin payload fires."
    - "Anti-pattern guardrail: no third response interceptor added to client.ts (Pitfall 1 — `grep -c \"interceptors.response.use\" src/services/http/client.ts` returns exactly 2)"
    - "ModerationError NOT thrown on listing 409 (T-10-02 + RESEARCH §Pattern S7) — the existing 403 interceptor's `error === 'account_suspended'` guard prevents widening; this plan adds regression tests proving the inverse"
  artifacts:
    - path: "src/services/http/__tests__/clientListing409.test.ts"
      provides: "Regression tests: 409 listing_not_available / 403 cannot_moderate_own_listing do NOT trigger logout"
    - path: "src/screens/CarDetailsScreen.tsx"
      provides: "axios.get → apiClient.get migration on the listing detail fetch (line ~112)"
      contains: "apiClient.get"
  key_links:
    - from: "src/screens/CarDetailsScreen.tsx line ~112"
      to: "apiClient.get('/api/cars/${carId}')"
      via: "Bearer + 401 refresh chain"
      pattern: "apiClient\\.get\\(.\\/api\\/cars"
  forbidden:
    - "Adding a third `apiClient.interceptors.response.use(...)` to src/services/http/client.ts (Pitfall 1 / T-10-02 — re-introduces loop-guard footgun Phase 4 D-11 fixed)"
    - "Editing the existing 403 `account_suspended` interceptor's discriminator (LMOB-02 invariant lives at client.ts:103 — Plan 05 reads-only this file)"
    - "Migrating any other axios.* call in CarDetailsScreen — Plan 05 scope is the ONE line at ~112 (deep-link fetch). Other axios.* sites in this file are intentionally deferred to follow-up debt cleanup"
---

<objective>
Land the LMOB-02 regression-test substrate and the LOAD-BEARING `axios.get → apiClient.get` migration in `CarDetailsScreen.tsx` line ~112.

Purpose: LMOB-02 is the invariant "listing 409/403/400 errors NEVER trigger the existing 403 user-suspension interceptor". This plan locks it with two regression tests (T-10-02 mitigation) BEFORE Plan 08 wires the bottom-sheet error banner — so any future drift in the interceptor's `error === 'account_suspended'` discriminator fails fast. The same plan migrates the CarDetailsScreen deep-link fetch from bare `axios.get` to `apiClient.get` (RESEARCH §Assumption A6 + Pitfall: Phase 9 D-07 admin `moderationBadge` field is omitted unless the request carries the Bearer header — without this migration, Plan 08's status banner cannot render even for confirmed admins). Both touches are tiny (~5 lines of source + a new test file) and self-contained, but they unblock Plans 08 + 10's bottom-sheet wiring.

Output: New regression test file under `src/services/http/__tests__/clientListing409.test.ts` + a one-line code change to `CarDetailsScreen.tsx` (with the corresponding import already present).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-CONTEXT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md
@.planning/phases/04-mobile-plumbing-mobile/04-CONTEXT.md

<interfaces>
<!-- Existing client.ts 403 interceptor (LMOB-02 invariant) — DO NOT modify in this plan. -->
<!-- Existing CarDetailsScreen.tsx line ~112 uses bare axios.get — THIS migrates to apiClient.get. -->

From src/services/http/client.ts (existing 403 interceptor at ~line 98-126):
  apiClient.interceptors.response.use(
    (r) => r,
    async (error) => {
      if (error?.response?.status === 403 && error?.response?.data?.error === 'account_suspended') {
        // ↑ KEY DISCRIMINATOR — listing errors pass through because data.error !== 'account_suspended'
        if (config._skipModerationInterceptor) return Promise.reject(error);
        config._skipModerationInterceptor = true;
        const refresher = getModerationRefreshListener();
        if (refresher) await refresher();
        throw new ModerationError(error.response.data.error, error.response.data.status, ...);
      }
      return Promise.reject(error);
    }
  );

From src/screens/CarDetailsScreen.tsx (line ~112, current):
  axios.get(`${API_URL}/api/cars/${carId}`)
    .then(res => { /* setFetchedCar(...) */ });

  // → migrate to:
  apiClient.get(`/api/cars/${carId}`)
    .then(res => { /* setFetchedCar(...) */ });

  // Note: apiClient is already imported in many sibling screens; CarDetailsScreen.tsx may need to add: import { apiClient } from '../services/http/client';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED regression tests — listing 409/403 do NOT trigger logout</name>
  <files>src/services/http/__tests__/clientListing409.test.ts</files>
  <read_first>
    - src/services/http/client.ts (FULL file — confirm the 403 interceptor's exact discriminator string + the existing 401 refresh interceptor + the loop-guard pattern)
    - src/services/moderation/errors.ts (post-Plan-01 — confirm BOTH ModerationError AND ListingModerationError importable)
    - .planning/phases/04-mobile-plumbing-mobile/04-CONTEXT.md (Phase 4 D-09/D-10/D-11 establishes the existing 403 interceptor behavior; the regression tests assert the inverse — listing errors do NOT touch the same code path)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-VALIDATION.md rows 10-03-01..03 for the three regression cases this file must cover
  </read_first>
  <behavior>
    - Setup: import `apiClient` from `'../client'`. Use `axios-mock-adapter` (already present as devDep — check `package.json` first; if absent, mock at the adapter level via `jest.spyOn(apiClient.defaults.adapter, ...)` instead). Register a mock `moderationRefreshListener` via `setModerationRefreshListener` (the helper exported from `client.ts` per Phase 4 D-09) and use a `jest.fn()` so the test can assert it was NOT called.
    - Test 1 (LMOB-02 — 409 listing_not_available): mock `apiClient.post('/api/cars/anything', ...)` to respond `409 { error: 'listing_not_available', listingStatus: 'suspended', reasonCategory: 'spam' }`; invoke the call; assert: (a) the rejection is a plain axios error (NOT `instanceof ModerationError` and NOT `instanceof ListingModerationError` — the latter is only thrown by Plan 04's service wrappers, not by the client interceptor); (b) `moderationRefreshListener` was called 0 times; (c) `error.response.status === 409`.
    - Test 2 (LMOB-02 — 403 cannot_moderate_own_listing): mock `apiClient.patch('/api/admin/moderation/listings/x/suspend', ...)` to respond `403 { error: 'cannot_moderate_own_listing' }`; assert (a) rejection is plain axios error (NOT a ModerationError throw), (b) `moderationRefreshListener` was called 0 times, (c) `error.response.status === 403` and `error.response.data.error === 'cannot_moderate_own_listing'`.
    - Test 3 (positive control — 403 account_suspended DOES trigger): mock any path → `403 { error: 'account_suspended', status: 'permanently_banned' }`; assert (a) `moderationRefreshListener` was called exactly once, (b) the rejection IS `instanceof ModerationError` with `code === 'account_suspended'`. This test guards against regressions that would silently make the 403 interceptor too broad OR too narrow.
    - Test 4 (anti-pattern guard — interceptor count): read `src/services/http/client.ts` via `fs.readFileSync` and assert that `match(/interceptors\\.response\\.use/g)?.length === 2` exactly (existing 403 + existing 401 refresh; no third for listing errors per Pitfall 1).
    - Test 5 (anti-pattern guard — discriminator preserved): in the same file, assert the substring `"data.error === 'account_suspended'"` (or `"data?.error === 'account_suspended'"`) appears at least once — locking the LMOB-02 invariant against accidental discriminator drift (e.g., a future maintainer trying to also catch `'listing_not_available'`).
  </behavior>
  <action>Create `src/services/http/__tests__/clientListing409.test.ts`. Mirror the existing client test patterns under `src/services/http/__tests__/` if any exist; otherwise structure as `describe('apiClient interceptor — LMOB-02 invariant', () => { ... })`. For mocking strategy: prefer `axios-mock-adapter` if listed in `package.json` devDeps; fallback to overriding `apiClient.defaults.adapter` with a `jest.fn()` returning canned responses. Use `setModerationRefreshListener(jest.fn())` (or the exact helper name in client.ts) to install the listener-spy. Verify on a CLEAN checkout: Tests 1-3 should pass IMMEDIATELY (they assert existing behavior); Tests 4-5 should also pass immediately. This plan does NOT modify `client.ts`; the tests lock the invariant for downstream plans.</action>
  <verify>
    <automated>npx jest src/services/http/__tests__/clientListing409.test.ts --bail</automated>
  </verify>
  <done>All 5 tests pass on the current `client.ts` (no source changes needed). Future PRs that introduce a third interceptor or widen the discriminator now break in CI. `grep -c "interceptors.response.use" src/services/http/client.ts` returns `2` from the shell as well.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate CarDetailsScreen line ~112 from axios.get to apiClient.get</name>
  <files>src/screens/CarDetailsScreen.tsx</files>
  <read_first>
    - src/screens/CarDetailsScreen.tsx (FULL current top — confirm whether `apiClient` is already imported; check all `axios.get`/`axios.post` call sites via grep, then identify the ONE at the deep-link car fetch around line 108-130 per RESEARCH A6)
    - src/services/http/client.ts (confirm `apiClient` export name and import path)
    - src/screens/SellCarScreen.tsx lines 87-138 (sibling screen that uses `apiClient.get(\`/api/cars/${carId}\`)` correctly — the exact target shape to mirror)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md §Assumption A6 (lines 1333-1335) for the why
  </read_first>
  <action>In `src/screens/CarDetailsScreen.tsx`: (1) if `apiClient` is not already imported, add `import { apiClient } from '../services/http/client';` near the existing service imports at the top of the file (do NOT alphabetize unless the file already does — match the local convention). (2) Locate the deep-link fetch around line 108-130: `axios.get(\`${API_URL}/api/cars/${carId}\`)`. Replace the call expression with `apiClient.get(\`/api/cars/${carId}\`)` — note the path is RELATIVE (no `${API_URL}` prefix; `apiClient` has `baseURL` set). The rest of the `.then(res => { ... })` chain stays byte-identical (same `res.data` access, same `setFetchedCar` call, same `.catch` / `.finally`). (3) DO NOT touch any OTHER `axios.*` call in the file (other call sites are intentionally out of scope for this plan; future debt-cleanup phase handles them). (4) If after the change `import axios from 'axios'` is no longer referenced anywhere in the file, REMOVE the now-unused import to keep ESLint clean. If `axios` is still referenced elsewhere in the file (e.g., for `axios.isCancel`), KEEP the import.</action>
  <verify>
    <automated>grep -n "axios.get(\`\${API_URL}/api/cars" src/screens/CarDetailsScreen.tsx | wc -l | tr -d ' '; grep -n "apiClient.get(\`/api/cars" src/screens/CarDetailsScreen.tsx | wc -l | tr -d ' '</automated>
  </verify>
  <done>First grep returns `0` (the old `axios.get` call site is gone); second grep returns `≥ 1` (new `apiClient.get(\`/api/cars/${...}\`)` call exists). `npx jest src/screens/__tests__/CarDetailsScreen*.test.tsx --bail` passes if any pre-existing tests exist (run; ignore "no tests found" — Plan 08 adds the screen tests).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile → backend `/api/cars/:id` | After Task 2, the deep-link fetch carries the Bearer header (`apiClient` request interceptor). Backend now sees admin caller and Phase 9 D-07 attaches `moderationBadge` for non-active listings. Before Task 2, the bare axios call lacked the header → admin saw thin-payload (Phase 9 D-05) regardless of admin claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Spoofing (admin session loss via wrong interceptor on listing error) | `client.ts` 403 interceptor | mitigate | Tests 1+2 in `clientListing409.test.ts` assert that listing 409/403 do NOT trigger the moderationRefreshListener; Test 3 is a positive control proving the interceptor still fires on `account_suspended`; Tests 4+5 lock the interceptor count and discriminator string at the source level. Any future plan that widens the interceptor breaks these tests. |
| T-10-A6 | Information Disclosure (admin path silently degraded to thin payload) | `CarDetailsScreen` deep-link fetch | mitigate | Task 2 migrates the call to `apiClient.get` so the Bearer header is attached; backend's status-aware GET (Phase 9 D-08) now returns the admin payload with `moderationBadge` for admin viewers, enabling Plan 08's status banner + bottom-sheet wiring. |
| T-10-01 | Elevation (mobile-trust-isAdmin) | `CarDetailsScreen` migration | mitigate (cross-plan) | The migration only changes the transport, not the auth model. Backend `requireAdmin` (on admin endpoints) and the status-aware GET's role inspection remain the single source of truth. Mobile `isAdmin` still gates UI visibility only (Plan 08). |
</threat_model>

<verification>
- `npx jest src/services/http/__tests__/clientListing409.test.ts --bail` — 5 tests green
- `grep -nE "axios\\.get\\(.\\\\\\$\\{API_URL\\}/api/cars" src/screens/CarDetailsScreen.tsx | wc -l` returns `0` (old call removed)
- `grep -nE "apiClient\\.get\\(.\\/api\\/cars" src/screens/CarDetailsScreen.tsx | wc -l` returns `≥ 1` (new call exists)
- `grep -c "interceptors.response.use" src/services/http/client.ts` returns `2` (interceptor count invariant)
- Full mobile suite: `npm test` — no regressions
</verification>

<success_criteria>
- Plan 08 (CarDetailsScreen wiring) can rely on `fetchedCar.moderationBadge` being populated for admin viewers of non-active listings (the Bearer header is now attached; backend Phase 9 D-07 fires correctly).
- LMOB-02 regression suite locks the invariant at the test level — any future PR that introduces a third response interceptor, widens the 403 discriminator, or routes listing errors through the user-suspension flow breaks `clientListing409.test.ts` in CI.
- No new dependencies; no new state-management or networking libs (CLAUDE.md compliance).
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-plumbing-admin-listing-ui/10-05-SUMMARY.md` summarizing: new test file (5 tests), one-line source change in CarDetailsScreen.tsx, and confirmation of the 5 grep invariants (interceptor count, discriminator preservation, axios.get removed, apiClient.get added, full suite green).
</output>
