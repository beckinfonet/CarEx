---
phase: 10
plan: 08
type: execute
wave: 4
depends_on: [10-04, 10-05, 10-06, 10-07]
files_modified:
  - src/screens/CarDetailsScreen.tsx
  - src/screens/__tests__/CarDetailsScreen.admin.test.tsx
autonomous: true
requirements: [LUI-01, LUI-02, LUI-03, LMOB-02]
must_haves:
  truths:
    - "D-LUI-01: Moderate badge (ShieldAlert icon, COLORS.warning) shown in headerRight ONLY when useAuth().isAdmin === true; tap opens ListingModerationBottomSheet"
    - "D-02 (unconditional badge): badge shows even when admin views their OWN listing — backend rejects with cannot_moderate_own_listing; mobile surfaces banner per D-15 (no client-side isOwner hide)"
    - "D-17: status banner above actionButtonsRow renders moderationBadge fields (status + reasonCategory chip + moderationReason free-text + setBy admin info) — admin-only; admin variant per CONTEXT, distinct from Phase 11 LBUY-01 buyer variant"
    - "D-15: cannot_moderate_own_listing AND already_in_state surface as INLINE banner (errorBanner state); listing_not_found surfaces as Alert.alert + navigation.goBack(); generic errors → Alert.alert with mapped code"
    - "D-16: optimistic flip on Suspend/Archive/Delete/Restore — snapshot BOTH status AND moderationBadge (Pitfall 2); rollback restores BOTH on error; success merges authoritative response.listing"
    - "D-07 two-modal stack: Delete escalation — reason modal stays mounted; TypedConfirmationModal overlays with keyboardType='default' (Pitfall 3) + listing-title sentinel via buildListingTitle(fetchedCar) (Pitfall 6)"
    - "Edit action routes to navigation.navigate('SellCar', { carId, adminEdit: true }) (Plan 09 wires the receiving end)"
    - "Sentinel target = buildListingTitle(fetchedCar) — SAME string the bottom-sheet header displays (Pitfall 6 single-source-of-truth)"
  artifacts:
    - path: "src/screens/CarDetailsScreen.tsx"
      provides: "Admin Moderate badge + bottom-sheet mount + status banner + error banner + optimistic-flip handler + Delete escalation"
      contains: "ListingModerationBottomSheet"
    - path: "src/screens/__tests__/CarDetailsScreen.admin.test.tsx"
      provides: "Screen integration tests: badge gating, status banner, optimistic+rollback, error surfacing, axios→apiClient migration sanity"
  key_links:
    - from: "CarDetailsScreen Moderate badge"
      to: "useAuth().isAdmin"
      via: "conditional render in headerRight"
      pattern: "isAdmin && "
    - from: "CarDetailsScreen handleListingActionSubmit"
      to: "ModerationService.<action>Listing"
      via: "string-keyed method lookup or switch"
      pattern: "ModerationService\\.(suspend|archive|delete|restore)Listing"
    - from: "Delete escalation in handler"
      to: "TypedConfirmationModal with keyboardType='default' + matchesListingTitleSentinel"
      via: "two-modal stack — reason modal mounted, typed-confirm overlays"
      pattern: "keyboardType=\"default\""
  forbidden:
    - "Hiding Moderate badge when admin views their own listing (D-02 — backend is authority; client must NOT pre-check isOwner)"
    - "Optimistically flipping only `status` without also flipping `moderationBadge` (Pitfall 2 — 200ms gap of \"status changed but no banner\" looks broken)"
    - "Using ${fetchedCar.year} ${fetchedCar.makeName} ${fetchedCar.modelName} inline (Pitfall 6 — MUST call buildListingTitle(fetchedCar) so the sheet header and the sentinel target are byte-identical)"
    - "Passing keyboardType='email-address' (or omitting the prop) when opening TypedConfirmationModal for listing-title sentinel (Pitfall 3 — iOS no-spacebar trap)"
    - "Calling ModerationService.<action>Listing without try/catch + rollback (D-16 — every action MUST have the snapshot/flip/restore triple)"
    - "Adding a third response interceptor or otherwise routing listing 409/403 through the user-suspension flow (LMOB-02 — Plan 05 regression tests catch this)"
    - "Editing the existing axios.* call sites in CarDetailsScreen beyond the Plan-05 deep-link fetch (out of scope — DEBT-* cleanup)"
---

<objective>
Wire the admin moderation surface onto `CarDetailsScreen.tsx`: a `ShieldAlert` "Moderate" badge in `headerRight` gated on `useAuth().isAdmin`, a `ListingModerationBottomSheet` mount that shows 4 actions for active listings and a Restore button for non-active, a status banner above `actionButtonsRow` rendering Phase 9 D-07 `moderationBadge`, an error banner for `cannot_moderate_own_listing` + `already_in_state`, `Alert.alert` for `listing_not_found`, the optimistic-flip + rollback handler, and the D-07 two-modal stack for Delete (reason modal stays mounted while TypedConfirmationModal overlays with `keyboardType='default'` + the listing-title sentinel).

Purpose: This plan delivers ROADMAP success criteria #1 (admin sees Moderate badge → bottom sheet of 4 actions + status banner), #2 (Suspend/Archive/Delete/Restore submits update the on-screen banner without restart), and #3 (Restore replaces the 4 actions on non-active listings). Plan 10 still ships #4 (Listings tab) and Plan 09 ships the Edit-path receiving side. Per CONTEXT D-15 + LMOB-02 + D-16, the error/optimistic semantics live HERE — Plans 06/07 keep components pure presentational.

Output: Substantial additive edits to `CarDetailsScreen.tsx` (~150-200 lines of JSX + state + handlers) + a co-located screen integration test file under `src/screens/__tests__/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-CONTEXT.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-04-SUMMARY.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-05-SUMMARY.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-06-SUMMARY.md
@.planning/phases/10-mobile-plumbing-admin-listing-ui/10-07-SUMMARY.md
@.planning/phases/09-backend-read-time-toctou-enforcement/09-CONTEXT.md

<interfaces>
<!-- All upstream substrates ready by Wave 3 close. Plan 08 composes them. -->

From src/services/moderation/ModerationService.ts (Plan 04):
  - suspendListing(carId, body): ListingActionResponse
  - archiveListing(carId, body): ListingActionResponse
  - deleteListing(carId, body): ListingActionResponse
  - restoreListing(carId, body?): ListingActionResponse
  - Each throws ListingModerationError on 4xx

From src/services/moderation/errors.ts (Plan 01):
  export class ListingModerationError { code, listingStatus?, reasonCategory?, banner?, refundId?, refundFailed?, httpStatus? }

From src/utils/listingTitle.ts (Plan 02):
  export function buildListingTitle(car): string
  export function matchesListingTitleSentinel(input, car): boolean   // Used by TypedConfirmationModal's match logic? — actually TypedConfirmationModal already does case-insensitive trimmed equality; Plan 08 passes listingTitle string as targetEmail prop and the existing match logic handles it.

From src/components/moderation/ListingModerationBottomSheet.tsx (Plan 06):
  - <ListingModerationBottomSheet visible listingTitle moderationBadge? onSelect onClose />

From src/components/moderation/ListingModerationReasonModal.tsx (Plan 07):
  - <ListingModerationReasonModal visible action carId listingTitle onSubmit onClose />
  - onSubmit({ reasonCategory, note? })

From src/components/moderation/ListingRestoreModal.tsx (Plan 07):
  - <ListingRestoreModal visible carId onSubmit onClose />
  - onSubmit({ note? })

From src/components/moderation/TypedConfirmationModal.tsx (after Plan 07 keyboardType prop):
  - <TypedConfirmationModal visible action targetEmail keyboardType? onConfirm onClose />
  - keyboardType default 'email-address'; pass 'default' for listing-title sentinel

From src/screens/CarDetailsScreen.tsx (current, post-Plan-05):
  - apiClient.get(`/api/cars/${carId}`) at deep-link fetch
  - fetchedCar state shape includes Phase 9 D-07 admin moderationBadge field
  - Existing state hooks at lines 23-49
  - headerRight icon area near line 451 (Share/Edit icons live here)
  - actionButtonsRow rendered near line 597+

From .planning/phases/10-mobile-plumbing-admin-listing-ui/10-RESEARCH.md §Code Examples lines 973-1093:
  - Full integration sketch — state hooks, badge JSX, status banner JSX, error banner JSX, modal mount block, handleListingActionSubmit shape
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED screen tests for CarDetailsScreen admin surface</name>
  <files>src/screens/__tests__/CarDetailsScreen.admin.test.tsx</files>
  <read_first>
    - src/screens/__tests__/AdminModerationScreen.test.tsx (existing screen test patterns — settle() helper for React 19 act-hang, Proxy-based useLanguage mock, mock ModerationService + apiClient + useAuth conventions)
    - src/screens/CarDetailsScreen.tsx (FULL current — confirm state hook positions + headerRight area + actionButtonsRow position + existing axios.isCancel usage so the test mock doesn't break it)
    - src/services/moderation/ModerationService.ts (Plan 04 — confirm new method names for mocking)
    - src/services/moderation/errors.ts (post-Plan 01 — confirm ListingModerationError constructor signature for mock-error fixtures)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-VALIDATION.md rows 10-07-01..05 for required test cases
  </read_first>
  <behavior>
    - Setup: mock `ModerationService` (jest.mock with all 6 new methods returning promises), mock `useAuth` with both `isAdmin: true` and `isAdmin: false` variants per test, mock `useLanguage` with stable Proxy returning key-as-value fallback (per Phase 5 plan-05-10 mock-hygiene lesson), mock `apiClient` to canned-respond to GET `/api/cars/:id` with a fixture, mock `useNavigation` + `useRoute` for `{ carId: 'car_abc' }`.
    - Test 1 (admin badge gate): render with `isAdmin=false` → query for `testID="moderate-badge"` returns null. Re-render with `isAdmin=true` (separate test) → query returns the element.
    - Test 2 (badge unconditional — D-02): render with `isAdmin=true` AND a `fetchedCar.sellerId === currentUser.localId` (admin viewing their own listing) → `testID="moderate-badge"` STILL renders (no client-side isOwner hide).
    - Test 3 (badge tap opens sheet): with `isAdmin=true`, tap `testID="moderate-badge"` → query for `testID="listing-sheet-overlay"` returns the rendered overlay.
    - Test 4 (status banner — D-17): render with `isAdmin=true` and `fetchedCar.moderationBadge = { status: 'suspended', reasonCategory: 'spam', moderationReason: 'duplicate listing', moderatedBy: 'admin_xyz', moderatedAt: '2026-05-29T12:00:00Z', banner: { titleKey: 'k', bodyKey: 'b', severity: 'warning' } }` → query for `testID="admin-status-banner"` returns the element; banner text contains `'suspended'` (or T.listingStatusSuspended), `'spam'`, `'duplicate listing'`, `'admin_xyz'` (sub-string matches).
    - Test 5 (active listing — no banner): render with `isAdmin=true` and `fetchedCar.moderationBadge = undefined` (active listing) → `testID="admin-status-banner"` returns null; bottom-sheet (when opened) shows 4 actions, NOT Restore.
    - Test 6 (optimistic suspend flip — D-16): tap moderate badge → tap Suspend → reason modal opens; select `spam`, tap Confirm → assert (a) `fetchedCar.status` (or rendered state element) flips to `suspended` IMMEDIATELY; (b) `ModerationService.suspendListing` is called once with `('car_abc', { reasonCategory: 'spam' })` (note empty/no `note` since not typed); (c) `setBottomSheetVisible(false)` — overlay no longer rendered.
    - Test 7 (rollback on error — D-16): same setup as Test 6, but `ModerationService.suspendListing` mock rejects with `new ListingModerationError('already_in_state', undefined, undefined, undefined, undefined, undefined, 400)` → assert (a) badge reverts to `undefined`/active; (b) `testID="admin-error-banner"` renders with code mapped (literal `'already_in_state'` substring sufficient for Phase 10; final RU/EN copy is Phase 11 LQUAL-01); (c) Alert.alert is NOT fired for `already_in_state` (D-15 inline banner only).
    - Test 8 (cannot_moderate_own_listing → inline banner): trigger Suspend → mock rejects with `new ListingModerationError('cannot_moderate_own_listing', ..., 403)` → assert `testID="admin-error-banner"` renders; Alert.alert NOT fired (D-15).
    - Test 9 (listing_not_found → Alert.alert): trigger Suspend → mock rejects with `new ListingModerationError('listing_not_found', ..., 404)` → assert Alert.alert called; `navigation.goBack()` called (D-15).
    - Test 10 (Delete two-modal stack — D-07): tap moderate badge → tap Delete → reason modal opens; select `fraud`, tap Confirm → reason modal stays mounted AND TypedConfirmationModal renders WITH `keyboardType="default"` (assert via prop snapshot) AND `targetEmail` prop set to `'2018 Toyota Camry'` (the buildListingTitle output for the fixture car); type wrong sentinel "wrong-title" → Confirm disabled; type "2018 Toyota Camry" → Confirm enabled; tap Confirm → assert `ModerationService.deleteListing('car_abc', { reasonCategory: 'fraud' })` called once; assert reason modal AND typed-confirm both unmount.
    - Test 11 (Restore flow — LUI-03): render with `fetchedCar.moderationBadge.status = 'archived'` → tap badge → bottom sheet shows Restore button (not 4 actions); tap Restore → ListingRestoreModal opens; type "post-appeal", tap Confirm → assert `ModerationService.restoreListing('car_abc', { note: 'post-appeal' })` called once; optimistic flip to active.
    - Test 12 (Edit routes to SellCar with adminEdit flag): tap badge → tap Edit → assert `navigation.navigate('SellCar', { carId: 'car_abc', adminEdit: true })` called once (Plan 09 wires the receiving end; Plan 08 just asserts navigation contract).
    - Test 13 (Pitfall 6 single-source-of-truth grep guard): `fs.readFileSync` of `src/screens/CarDetailsScreen.tsx`, find both the bottom-sheet mount block AND the TypedConfirmationModal mount block; assert BOTH use `buildListingTitle(fetchedCar)` (string match) and NEITHER inlines `${fetchedCar.year}` template-literal for the title (regex `/\\$\\{.*\\.year.*\\.make.*\\.model\\}/` returns no match).
  </behavior>
  <action>Create `src/screens/__tests__/CarDetailsScreen.admin.test.tsx`. Use the `settle()` helper pattern from `AdminModerationScreen.test.tsx` (sync `act(() => TestRenderer.create(...))` + outside-act `setImmediate` pump + bookending sync `act`) for any await-driven assertions. Mock all upstream service modules. File MUST fail to compile/run because CarDetailsScreen.tsx does not yet contain any of the new admin features.</action>
  <verify>
    <automated>npx jest src/screens/__tests__/CarDetailsScreen.admin.test.tsx --bail 2>&1 | tail -30</automated>
  </verify>
  <done>Test file exists with 13 tests; all fail because CarDetailsScreen lacks the new wiring. RED gate confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Moderate badge + status banner + bottom-sheet mount</name>
  <files>src/screens/CarDetailsScreen.tsx</files>
  <read_first>
    - src/screens/CarDetailsScreen.tsx (FULL — confirm current state hook positions and JSX layout; note the headerRight area around line 451 and actionButtonsRow around line 597)
    - src/components/moderation/ListingModerationBottomSheet.tsx (Plan 06 — confirm prop shape)
    - src/components/moderation/SeverityBadge.tsx (existing — for status banner styling)
    - src/utils/listingTitle.ts (Plan 02 — buildListingTitle for header + sentinel)
    - src/context/AuthContext.tsx (confirm useAuth().isAdmin is boolean — Assumption A5)
    - src/screens/__tests__/CarDetailsScreen.admin.test.tsx (Task 1 contract)
    - .planning/phases/10-mobile-plumbing-admin-listing-ui/10-PATTERNS.md §"src/screens/CarDetailsScreen.tsx (MODIFIED — admin Moderate badge + bottom sheet + banners)" KEEP/CHANGE list
  </read_first>
  <action>
  Edit `src/screens/CarDetailsScreen.tsx`. Tasks below are INTERLEAVED carefully — do NOT touch any existing axios.* call (other than the Plan-05 migrated apiClient.get), do NOT remove or rename existing state hooks.

  Imports — add to existing import block (do not duplicate):
  - `import { ShieldAlert, X } from 'lucide-react-native';` (existing lucide icons may already include some — extend, do not duplicate)
  - `import { ListingModerationBottomSheet } from '../components/moderation/ListingModerationBottomSheet';`
  - `import { ListingModerationReasonModal, ListingReasonAction } from '../components/moderation/ListingModerationReasonModal';`
  - `import { ListingRestoreModal } from '../components/moderation/ListingRestoreModal';`
  - `import { TypedConfirmationModal } from '../components/moderation/TypedConfirmationModal';`
  - `import { ModerationService } from '../services/moderation/ModerationService';`
  - `import { ListingModerationError } from '../services/moderation/errors';`
  - `import { buildListingTitle } from '../utils/listingTitle';`
  - Ensure `useAuth` is already imported (it is — destructure `isAdmin` alongside the existing `user`).

  State hooks — append after the existing state declarations (~line 49):
  - `const { isAdmin } = useAuth() as unknown as { isAdmin: boolean; user: typeof user };` (or extract from existing destructure pattern — match the file's local convention; if `useAuth` is destructured once at the top, extend that one destructure to include `isAdmin`)
  - `const [bottomSheetVisible, setBottomSheetVisible] = useState(false);`
  - `const [reasonModalAction, setReasonModalAction] = useState<ListingReasonAction | null>(null);`
  - `const [restoreModalVisible, setRestoreModalVisible] = useState(false);`
  - `const [typedConfirmVisible, setTypedConfirmVisible] = useState(false);`
  - `const [pendingDeletePayload, setPendingDeletePayload] = useState<{ reasonCategory: string; note?: string } | null>(null);`
  - `const [errorBanner, setErrorBanner] = useState<string | null>(null);`

  Computed value: `const listingTitle = fetchedCar ? buildListingTitle(fetchedCar) : '';` — placed alongside other derived values in the function body. This is the SINGLE source of truth (Pitfall 6) for both the bottom-sheet header AND the TypedConfirmationModal sentinel target.

  Handler `handleListingActionSubmit(action, body)` — define inside the component (use `useCallback` if other handlers do; otherwise plain arrow function):
  - `const prevBadge = fetchedCar?.moderationBadge ?? null; const prevStatus = fetchedCar?.status ?? 'active';`
  - `const nextStatus = action === 'restore' ? 'active' : action === 'delete' ? 'deleted' : action === 'suspend' ? 'suspended' : 'archived';`
  - Optimistic: `setFetchedCar((c) => c ? { ...c, status: nextStatus, moderationBadge: nextStatus === 'active' ? undefined : { status: nextStatus, reasonCategory: body?.reasonCategory, moderatedAt: new Date().toISOString() } } : c);` (snapshot BOTH fields per Pitfall 2)
  - `setBottomSheetVisible(false); setReasonModalAction(null); setRestoreModalVisible(false);`
  - try block: lookup `const fn = { suspend: ModerationService.suspendListing, archive: ModerationService.archiveListing, delete: ModerationService.deleteListing, restore: ModerationService.restoreListing }[action];` → `const result = await fn(carId, body ?? {});` → on success `setFetchedCar((c) => c ? { ...c, ...result.listing } : c);` (merge authoritative response)
  - catch block: rollback `setFetchedCar((c) => c ? { ...c, status: prevStatus, moderationBadge: prevBadge ?? undefined } : c);` then `if (err instanceof ListingModerationError) { if (err.code === 'cannot_moderate_own_listing' || err.code === 'already_in_state') { setErrorBanner(err.code); } else if (err.code === 'listing_not_found') { Alert.alert(t.error, 'Listing not found'); navigation.goBack(); } else { Alert.alert(t.error, err.code); } } else { Alert.alert(t.error, 'Unexpected error'); }`

  Sheet-select handler `handleSheetSelect(action)`:
  - `if (action === 'edit') { setBottomSheetVisible(false); navigation.navigate('SellCar', { carId, adminEdit: true }); }`
  - `else if (action === 'restore') { setBottomSheetVisible(false); setRestoreModalVisible(true); }`
  - `else { setBottomSheetVisible(false); setReasonModalAction(action as ListingReasonAction); }`

  JSX additions — three insertions inside the existing return tree:

  Insertion A — admin Moderate badge in headerRight (alongside existing Share/Edit at ~line 451):
  ```
  {isAdmin && (
    <TouchableOpacity testID="moderate-badge" style={styles.iconButton} onPress={() => setBottomSheetVisible(true)} accessibilityLabel="Moderate listing">
      <ShieldAlert size={24} color={COLORS.warning} />
    </TouchableOpacity>
  )}
  ```

  Insertion B — admin status banner ABOVE actionButtonsRow (insert BEFORE the actionButtonsRow JSX block):
  ```
  {isAdmin && fetchedCar?.moderationBadge && (
    <View testID="admin-status-banner" style={[styles.adminStatusBanner, styles[`adminBanner_${fetchedCar.moderationBadge.banner?.severity ?? 'neutral'}`]]}>
      <Text style={styles.adminBannerStatus}>{fetchedCar.moderationBadge.status}</Text>
      {fetchedCar.moderationBadge.reasonCategory && <Text style={styles.adminBannerChip}>{fetchedCar.moderationBadge.reasonCategory}</Text>}
      {fetchedCar.moderationBadge.moderationReason && <Text style={styles.adminBannerReason}>{fetchedCar.moderationBadge.moderationReason}</Text>}
      {fetchedCar.moderationBadge.moderatedBy && <Text style={styles.adminBannerSetBy}>by {fetchedCar.moderationBadge.moderatedBy}</Text>}
    </View>
  )}
  ```

  Insertion C — error banner (place near the status banner; render whenever errorBanner is set):
  ```
  {errorBanner && (
    <View testID="admin-error-banner" style={styles.adminErrorBanner}>
      <Text style={styles.adminErrorBannerText}>{errorBanner}</Text>
      <TouchableOpacity onPress={() => setErrorBanner(null)} accessibilityLabel="Dismiss error"><X size={16} color={COLORS.textPrimary} /></TouchableOpacity>
    </View>
  )}
  ```

  Modal mount block — append at END of return tree (right before the closing SafeAreaView):
  ```
  <ListingModerationBottomSheet visible={bottomSheetVisible} listingTitle={listingTitle} moderationBadge={fetchedCar?.moderationBadge} onSelect={handleSheetSelect} onClose={() => setBottomSheetVisible(false)} />

  {reasonModalAction && fetchedCar && (
    <ListingModerationReasonModal visible={true} action={reasonModalAction} carId={carId} listingTitle={listingTitle} onSubmit={(payload) => {
      if (reasonModalAction === 'delete') { setPendingDeletePayload(payload); setTypedConfirmVisible(true); }
      else { handleListingActionSubmit(reasonModalAction, payload); }
    }} onClose={() => setReasonModalAction(null)} />
  )}

  {typedConfirmVisible && fetchedCar && pendingDeletePayload && (
    <TypedConfirmationModal visible={true} action="delete_profile" targetEmail={listingTitle} keyboardType="default" onConfirm={() => { setTypedConfirmVisible(false); setReasonModalAction(null); handleListingActionSubmit('delete', pendingDeletePayload); setPendingDeletePayload(null); }} onClose={() => { setTypedConfirmVisible(false); setPendingDeletePayload(null); }} />
  )}

  {restoreModalVisible && fetchedCar && (
    <ListingRestoreModal visible={true} carId={carId} onSubmit={(body) => handleListingActionSubmit('restore', body)} onClose={() => setRestoreModalVisible(false)} />
  )}
  ```

  StyleSheet — add at bottom of the existing StyleSheet.create call: `adminStatusBanner` (paddingHorizontal: SIZES.padding, paddingVertical: SIZES.spacingSm, marginHorizontal: SIZES.padding, marginTop: SIZES.spacingSm, borderRadius: SIZES.borderRadius), three severity variants `adminBanner_warning` / `adminBanner_neutral` / `adminBanner_destructive` (background colors from COLORS.moderation palette — use existing tokens; if missing, fall back to COLORS.warning/COLORS.textSecondary/COLORS.destructive at low opacity), `adminBannerStatus` / `adminBannerChip` / `adminBannerReason` / `adminBannerSetBy` (TYPOGRAPHY tokens), `adminErrorBanner` (flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', paddingHorizontal: SIZES.padding, paddingVertical: SIZES.spacingSm, marginHorizontal: SIZES.padding, marginTop: SIZES.spacingSm, borderRadius: SIZES.borderRadius, backgroundColor: COLORS.errorBackground ?? COLORS.destructive), `adminErrorBannerText` (color: COLORS.textPrimary, flex: 1).
  </action>
  <verify>
    <automated>npx jest src/screens/__tests__/CarDetailsScreen.admin.test.tsx --bail</automated>
  </verify>
  <done>All 13 tests from Task 1 pass (GREEN). Lint passes: `npm run lint -- src/screens/CarDetailsScreen.tsx`. `grep -c "buildListingTitle(fetchedCar)" src/screens/CarDetailsScreen.tsx` returns `≥ 2` (sheet header listingTitle + sentinel target both derive from the same call — Pitfall 6). `grep -c "keyboardType=\"default\"" src/screens/CarDetailsScreen.tsx` returns `1`. `grep -c "moderate-badge\|admin-status-banner\|admin-error-banner\|listing-sheet-overlay" src/screens/CarDetailsScreen.tsx` returns the expected testIDs in the source. Full mobile suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CarDetailsScreen → ModerationService writes | Bearer-token via apiClient; backend `requireAdmin` + `denySelfModerationListing` enforce authorization. Mobile `isAdmin` only gates UI visibility (the Moderate badge); the bottom-sheet action still hits the backend which is the sole authority. |
| CarDetailsScreen status banner reads | Sourced from Phase 9 D-07 `moderationBadge` payload (admin-only field — only present when backend has identified the caller as admin via `verifyIdToken + requireAdmin`). Buyer / non-admin paths never see this field; banner is render-guarded by both `isAdmin` AND `moderationBadge` presence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation (mobile-trust-isAdmin) | Moderate badge gate + bottom sheet | mitigate | Mobile `isAdmin` from AuthContext gates UI visibility only — clicking the badge sends a Bearer request that the backend `requireAdmin` middleware (Phase 7 LSEC-02) cryptographically verifies. D-02 unconditional-badge design ensures own-listing rejection is explicit (cannot_moderate_own_listing 400) rather than silent. Tests 1+2 lock the gate behavior. |
| T-10-02 | Spoofing (admin session loss via listing 403) | error-handler branch on `cannot_moderate_own_listing` | mitigate | `ListingModerationError` is a SEPARATE class from `ModerationError` — the handler explicitly catches it and surfaces an INLINE banner (D-15) without ever calling `refreshUser` or `logout`. Plan 05's clientListing409.test.ts regression suite locks the interceptor invariant at the transport layer; Plan 08's Test 8 locks it at the handler layer. |
| T-10-03 | Information Disclosure (PII via status banner) | status banner | mitigate | Banner is render-guarded by `isAdmin === true` AND `moderationBadge` presence. Phase 9 D-05 thin payload for non-admin viewers does NOT include `moderationBadge`, so even if `isAdmin` were spoofed client-side, the banner would render nothing meaningful. Phase 11 LBUY-01 handles the buyer-facing variant separately. |
| T-10-05 | Tampering (case-variant sentinel bypass) | TypedConfirmationModal Delete escalation | accept | Friction-with-purpose per D-08a; backend `denySelfModerationListing` + rate limiter are the authoritative checks. Test 10 verifies the sentinel match works (correct typing succeeds) and the wrong-input path keeps Confirm disabled. |
| T-10-keyboard | Usability-DoS (iOS no-spacebar for listing-title) | TypedConfirmationModal mount in handler | mitigate | Insertion C passes `keyboardType="default"` explicitly; Task 1 Test 10 asserts this prop value. Pitfall 3 root cause closed. |
| T-10-optimistic | Tampering (banner-status divergence) | optimistic-flip handler | mitigate | Snapshot pattern captures BOTH `status` AND `moderationBadge`; rollback restores both. Pitfall 2 root cause closed. Test 7 specifically asserts rollback restores the badge correctly. |
</threat_model>

<verification>
- `npx jest src/screens/__tests__/CarDetailsScreen.admin.test.tsx --bail` — all 13 tests green
- `npx jest src/services/http/__tests__/clientListing409.test.ts --bail` — Plan-05 regression suite still green (no listing-flow regression touching the user-suspension interceptor)
- Full mobile suite: `npm test`
- `grep -c "buildListingTitle(fetchedCar)" src/screens/CarDetailsScreen.tsx` returns `≥ 2` (Pitfall 6 single-source-of-truth)
- `grep -c "keyboardType=\"default\"" src/screens/CarDetailsScreen.tsx` returns `1` (Pitfall 3 mitigation visible)
- `grep -c "if (isAdmin)" src/screens/CarDetailsScreen.tsx` (or equivalent `{isAdmin &&`) returns `≥ 2` (badge + banner gates)
- `grep -nE "isOwner.*isAdmin|isAdmin.*isOwner" src/screens/CarDetailsScreen.tsx | wc -l` returns `0` (D-02: no client-side isOwner-hide)
- ESLint: `npm run lint -- src/screens/CarDetailsScreen.tsx` passes
</verification>

<success_criteria>
- ROADMAP success criteria #1, #2, #3 satisfied on the admin path: badge shows for admins, bottom sheet has 4 actions for active listings + Restore for non-active, status banner reflects current state, all 4 write actions update the banner without app restart (via optimistic flip + backend confirmation).
- Plan 09 (SellCarScreen adminEdit) is unblocked — `navigation.navigate('SellCar', { carId, adminEdit: true })` is wired here.
- The error banner + Alert.alert split (D-15) keeps `cannot_moderate_own_listing` and `already_in_state` non-modal (admin can keep working) while making `listing_not_found` a hard-stop (admin probably navigated from stale data; back to safety).
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-plumbing-admin-listing-ui/10-08-SUMMARY.md` summarizing: lines added to CarDetailsScreen.tsx (~200), new state hooks (6), new handlers (2), new JSX insertions (3 — badge / status banner / error banner) + modal mount block, test file (13 tests), and confirmation that all grep invariants hold (no isOwner-hide, single buildListingTitle source, keyboardType="default" visible).
</output>
