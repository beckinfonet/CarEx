---
phase: 13-fcm-push-transport-native
plan: 05
type: execute
wave: 4
depends_on: [13-04]
files_modified:
  - src/constants/translations.ts
  - src/components/notifications/WatchButton.tsx
  - src/components/notifications/SaveSearchBar.tsx
  - src/components/notifications/__tests__/prePrompt.test.tsx
  - src/screens/NotificationSettingsScreen.tsx
  - .planning/phases/13-fcm-push-transport-native/13-HUMAN-UAT.md
autonomous: false
requirements: [NPRF-06]
must_haves:
  truths:
    - "Push permission is NEVER requested on launch — only on the first Watch OR first Save-search (D-04)"
    - "A soft in-app pre-prompt with 'Включить' / 'Не сейчас' precedes the OS dialog (D-06)"
    - "'Не сейчас' (Not now) means never auto-re-ask; the flag persists in AsyncStorage (D-05)"
    - "NotificationSettingsScreen shows live OS permission status and deep-links to OS Settings when off (D-09/D-10/D-11)"
    - "If push is denied, the Phase-12 in-app center stays fully functional — no dead-end (NPRF-07)"
  artifacts:
    - path: "src/components/notifications/WatchButton.tsx"
      provides: "pre-prompt trigger on first successful watch"
      contains: "prePrompt"
    - path: "src/screens/NotificationSettingsScreen.tsx"
      provides: "live OS permission status row + openSettings recovery"
      contains: "hasPermission"
    - path: ".planning/phases/13-fcm-push-transport-native/13-HUMAN-UAT.md"
      provides: "real-device checklist for NPUSH-01/03/06/07"
      min_lines: 20
  key_links:
    - from: "src/components/notifications/WatchButton.tsx / SaveSearchBar.tsx"
      to: "messaging().requestPermission() + PushService.registerToken"
      via: "soft pre-prompt to OS dialog to on-grant register"
      pattern: "requestPermission|prePrompt"
    - from: "src/screens/NotificationSettingsScreen.tsx recovery row"
      to: "Linking.openSettings()"
      via: "tap when status is off"
      pattern: "openSettings"
---

<objective>
Add the contextual soft permission pre-prompt (fire-once on first Watch/Save-search), the denied-permission recovery row on NotificationSettingsScreen (live OS status + deep-link to OS Settings), the RU/EN-parity copy, and the real-device HUMAN-UAT checklist that gates the phase.

Purpose: NPRF-06 (pre-prompt, never on launch) and the NPRF-07 recovery surface. This UX respects App-Store norms (no launch prompt, no re-nagging after deny) while keeping the in-app center as the no-dead-end fallback. It also produces 13-HUMAN-UAT.md — the device checklist for criteria that cannot run in Jest (NPUSH-01/03/06/07).
Output: pre-prompt flow with fire-once unit test, settings recovery row reflecting live status, parity-safe translations, and the device UAT checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-fcm-push-transport-native/13-CONTEXT.md
@.planning/phases/13-fcm-push-transport-native/13-RESEARCH.md
@.planning/phases/13-fcm-push-transport-native/13-PATTERNS.md
@.planning/phases/13-fcm-push-transport-native/13-VALIDATION.md

<interfaces>
<!-- Reuse, do NOT re-explore: -->
<!-- WatchButton.tsx handlePress ~65-83 (createSubscription success path = pre-prompt insertion point); SaveSearchBar.tsx handleSave ~127. -->
<!-- useLanguage().t i18n; existing keys: notNow at translations.ts:956 (RU) / :2014 (EN). New keys need RU+EN parity (jest parity scanner). -->
<!-- NotificationSettingsScreen.tsx toggleRow shape ~287-296; on-mount loader useEffect(loadSubs) ~125. -->
<!-- Live status: messaging().hasPermission() returns AuthorizationStatus.AUTHORIZED|PROVISIONAL. Recovery: Linking.openSettings(). -->
<!-- PushService.registerToken from 13-04 (call on grant). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Translations + fire-once pre-prompt on first Watch/Save-search</name>
  <files>src/constants/translations.ts, src/components/notifications/WatchButton.tsx, src/components/notifications/SaveSearchBar.tsx, src/components/notifications/__tests__/prePrompt.test.tsx</files>
  <read_first>
    - src/constants/translations.ts (notif_* block ~1037-1046 RU / ~2091-2100 EN; notNow ~956/~2014; the parity scanner this table feeds)
    - src/components/notifications/WatchButton.tsx (handlePress ~65-83, createSubscription success path)
    - src/components/notifications/SaveSearchBar.tsx (handleSave ~127)
    - src/components/notifications/__tests__/WatchButton.test.tsx and SaveSearchBar.test.tsx (test harness precedent)
    - .planning/phases/13-fcm-push-transport-native/13-RESEARCH.md (Contextual Permission Pre-Prompt section; D-04/D-05/D-06)
    - .planning/phases/13-fcm-push-transport-native/13-PATTERNS.md (WatchButton/SaveSearchBar section)
  </read_first>
  <behavior>
    - Test: the FIRST successful Watch shows the pre-prompt; a SECOND Watch (flag already set) does NOT show it again.
    - Test: a successful Save-search ALSO satisfies the single ask — after a Watch fired it, Save-search does not re-show, and vice versa (D-04 single ask covers both).
    - Test: tapping "Не сейчас" persists a dismissed flag in AsyncStorage so the prompt never auto-re-shows (D-05).
    - Test: tapping "Включить" calls the permission request path (mock RNFB messaging().requestPermission) and, on grant, calls PushService.registerToken.
    - Test: pre-prompt is NEVER shown at mount/launch — only on a subscription success.
  </behavior>
  <action>
    Add RU-first translation keys with EN parity: pushPrePromptTitle, pushPrePromptBody (D-06 plain/functional, e.g. RU "Включить уведомления, чтобы не пропустить новые совпадения?"), pushEnable ("Включить"); reuse existing notNow ("Не сейчас"). Add a small shared pre-prompt helper/flag (AsyncStorage key, e.g. push_preprompt_seen) used by both WatchButton (handlePress ~65-83) and SaveSearchBar (handleSave ~127): on the FIRST successful createSubscription, if the flag is unset, show a soft in-app modal (D-04, single ask covers both controls). On "Включить" call messaging().requestPermission() plus Android POST_NOTIFICATIONS, then on grant call PushService.registerToken; on "Не сейчас" set the AsyncStorage flag and never auto-re-ask (D-05). Set the seen flag on any resolution so it fires exactly once. ANTI-PATTERN: never request permission at launch. Copy is plain RU-first (D-06), NOT UNHINGED voice. Write prePrompt.test.tsx per the behavior block, mocking RNFB plus AsyncStorage.
  </action>
  <verify>
    <automated>npx jest src/components/notifications/__tests__/prePrompt.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - npx jest src/components/notifications/__tests__/prePrompt.test.tsx passes (fire-once + Not-now-persists + Включить-requests-then-registers).
    - New keys present in BOTH RU and EN (parity scanner stays green via npm test).
    - Test asserts no permission request at component mount/launch (prompt only on subscription success).
    - Covers NPRF-06.
  </acceptance_criteria>
  <done>Pre-prompt fires once across both controls, persists Not-now, and registers on grant; copy has RU/EN parity.</done>
</task>

<task type="auto">
  <name>Task 2: Denied-permission recovery row on NotificationSettingsScreen</name>
  <files>src/constants/translations.ts, src/screens/NotificationSettingsScreen.tsx</files>
  <read_first>
    - src/screens/NotificationSettingsScreen.tsx (toggleRow shape ~287-296; on-mount loader useEffect(loadSubs) ~125 — the focus/mount precedent)
    - src/screens/__tests__/NotificationSettingsScreen.test.tsx (test harness precedent)
    - src/constants/translations.ts (key placement near notificationSettings/cadenceInstant)
    - .planning/phases/13-fcm-push-transport-native/13-RESEARCH.md (iOS APNs Code Examples — hasPermission/openSettings; D-09/D-10/D-11)
    - .planning/phases/13-fcm-push-transport-native/13-PATTERNS.md (NotificationSettingsScreen section)
  </read_first>
  <action>
    Add RU/EN-parity keys: pushStatusOn, pushStatusOff, pushEnableInSettings (e.g. RU "Push-уведомления: выключены — включить в настройках"). Add a row mirroring the existing toggleRow layout (~287-296) that reads live OS permission via messaging().hasPermission() (enabled = AUTHORIZED or PROVISIONAL, D-11) and renders pushStatusOn/pushStatusOff. When OFF, the row is tappable and calls Linking.openSettings() (D-10 — the app cannot re-trigger the native dialog after a deny, so deep-link to OS Settings; D-09 — this lives ONLY here, no nagging banner in the feed). Re-read live status on screen focus / AppState resume (mirror the on-mount loader ~125 plus add a focus or AppState listener) so returning from OS Settings refreshes the row. NPRF-07: this screen and the Phase-12 in-app center remain fully functional regardless of permission state — no dead-end.
  </action>
  <verify>
    <automated>npx jest src/screens/__tests__/NotificationSettingsScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: NotificationSettingsScreen.tsx contains hasPermission and openSettings; translations.ts contains pushStatusOff in RU and EN.
    - When permission is off, the recovery row is tappable and routes to OS Settings (D-10); when on, it shows the on-status (D-11).
    - The recovery surface exists ONLY on NotificationSettingsScreen, not in the feed (D-09).
    - Existing NotificationSettingsScreen tests still pass.
  </acceptance_criteria>
  <done>Live-status push row reflects OS permission and deep-links to Settings when off; in-app center unaffected by denial.</done>
</task>

<task type="auto">
  <name>Task 3: Real-device HUMAN-UAT checklist (NPUSH-01/03/06/07)</name>
  <files>.planning/phases/13-fcm-push-transport-native/13-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/13-fcm-push-transport-native/13-VALIDATION.md (Manual-Only Verifications table — the four device behaviors and their instructions)
    - .planning/phases/13-fcm-push-transport-native/13-RESEARCH.md (Validation Architecture — the real-device caveat)
    - .planning/phases/11-*/11-HUMAN-UAT.md if present (house UAT checklist format precedent)
  </read_first>
  <action>
    Create 13-HUMAN-UAT.md as a real-device checklist with one signed-off item per non-automatable criterion, each with exact steps and a PASS condition: (1) NPUSH-01 — Release archive (use_frameworks! :linkage => :static) installs and RUNS on a real device AND a Stripe test checkout completes (cross-reference 13-SPIKE-RESULT.md). (2) NPUSH-03 — trigger a saved-search/Watch match; confirm the device receives a push whose lock-screen body has NO make/model/price/seller/location (D-08b). (3) NPUSH-06 — send a push in each app state (foreground, background, quit) and confirm each is handled. (4) NPUSH-07 — kill the app, tap the push, confirm the correct CarDetails opens via the linking config (cold-start). Include a sign-off line and note the phase gate: full Jest suite green in BOTH repos PLUS this checklist signed before /gsd-verify-work (per 13-VALIDATION.md sampling rate).
  </action>
  <verify>
    <automated>test -f .planning/phases/13-fcm-push-transport-native/13-HUMAN-UAT.md && grep -q "NPUSH-07" .planning/phases/13-fcm-push-transport-native/13-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 13-HUMAN-UAT.md exists with checklist items for NPUSH-01, 03, 06, 07, each with steps + PASS condition + a sign-off line.
    - The NPUSH-03 item explicitly checks for absence of PII on the lock screen (D-08b).
  </acceptance_criteria>
  <done>Device UAT checklist exists covering all four non-automatable criteria; phase gate documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Execute the real-device UAT and sign off the phase gate</name>
  <what-built>
    The complete push transport: spike-proven build, RNFB installed, backend send loop, token lifecycle, 3-state routing, contextual pre-prompt, and denied-recovery UX.
  </what-built>
  <how-to-verify>
    Run 13-HUMAN-UAT.md end-to-end on a real device:
    1. Confirm the Release archive runs + Stripe checkout works (NPUSH-01 — already passed in the spike; re-confirm the shipped build).
    2. Trigger an instant saved-search/Watch event; confirm a push arrives with a PII-safe generic body (NPUSH-03/08 — no make/model/price/seller/location).
    3. Tap a push in foreground, background, and quit states; confirm each routes correctly (NPUSH-06).
    4. From a killed app, tap a push; confirm the correct CarDetails opens (NPUSH-07).
    5. First Watch/Save-search shows the soft pre-prompt (never on launch); "Не сейчас" never re-asks; NotificationSettingsScreen reflects live status and deep-links to OS Settings when off (NPRF-06/07).
    6. Confirm full Jest suite is green in BOTH repos (mobile + backend).
    Sign off each item in 13-HUMAN-UAT.md.
  </how-to-verify>
  <acceptance_criteria>
    - All 13-HUMAN-UAT.md items signed PASS on a real device.
    - Full Jest suite green in both repos.
    - Phase gate satisfied per 13-VALIDATION.md before /gsd-verify-work.
  </acceptance_criteria>
  <resume-signal>Type "uat passed" once every device checklist item is signed off and both test suites are green.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User intent ↔ OS permission | Permission must be requested only with explicit contextual consent, never silently on launch. |
| Denied state ↔ app usability | A denial must not strand the user; the in-app center remains the functional channel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Repudiation / UX-abuse | permission prompt timing | mitigate | Never request on launch; fire-once on first subscription; "Not now" persisted so the user is not re-nagged (NPRF-06, App-Store-norm aligned). |
| T-13-05-02 | Denial of Service (self) | denied-permission dead-end | mitigate | Recovery row deep-links to OS Settings; in-app center stays fully functional (NPRF-07) — no dead-end. |
| T-13-05-03 | Information Disclosure | lock-screen body (device-verified) | mitigate | HUMAN-UAT NPUSH-03 explicitly verifies no make/model/price/seller/location reaches the lock screen (D-08b), closing the loop on the backend generic-copy work in 13-02. |
</threat_model>

<verification>
- prePrompt.test.tsx green; NotificationSettingsScreen tests green; mobile npm test suite green.
- hasPermission + openSettings present in NotificationSettingsScreen; new keys RU/EN parity.
- 13-HUMAN-UAT.md exists and all items signed PASS on a real device.
</verification>

<success_criteria>
NPRF-06 (contextual fire-once pre-prompt, never on launch, "Not now" persists) and the NPRF-07 recovery surface (live OS status + deep-link to Settings, no dead-end) are implemented and unit-tested. The real-device HUMAN-UAT checklist for NPUSH-01/03/06/07 is produced and signed off — closing the phase gate.
</success_criteria>

<output>
After completion, create `.planning/phases/13-fcm-push-transport-native/13-05-SUMMARY.md`.
</output>
