---
phase: 13-fcm-push-transport-native
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/Podfile
  - ios/carEx/carEx.entitlements
  - ios/carEx/Info.plist
  - .planning/phases/13-fcm-push-transport-native/13-SPIKE-RESULT.md
autonomous: false
requirements: [NPUSH-01]
user_setup:
  - service: apple-developer
    why: "Release-signed build on a real iOS device is the spike's only valid pass bar (D-02). Simulator cannot prove APNs or static-framework Release behavior."
    dashboard_config:
      - task: "Provide a real iOS device + valid Apple developer signing for a Release archive install"
        location: "Xcode → Signing & Capabilities (real device connected)"

must_haves:
  truths:
    - "A pre-frameworks rollback checkpoint (Podfile.lock + Pods state) is committed BEFORE the linkage switch"
    - "iOS Release archive builds with use_frameworks! :linkage => :static and the existing Stripe + fmt/C++17 post_install hooks intact"
    - "The Release archive runs on a real iOS device and a Stripe test checkout still completes"
    - "The notifee-vs-RNFB-built-in-display decision (D-03) is recorded in 13-SPIKE-RESULT.md"
    - "The spike is bounded to a hard 2-day timebox, which is the abort trigger (D-01)"
    - "If the spike cannot pass within the 2-day timebox, the repo is reverted to the rollback checkpoint and the abort is recorded (D-01/D-02)"
  artifacts:
    - path: "ios/Podfile"
      provides: "Static-frameworks linkage + $RNFirebaseAsStaticFramework with Stripe/fmt hooks preserved"
      contains: "linkage => :static"
    - path: ".planning/phases/13-fcm-push-transport-native/13-SPIKE-RESULT.md"
      provides: "Spike pass/fail decision, exact working incantation, notifee decision (D-03), abort record if failed"
      min_lines: 20
  key_links:
    - from: "ios/Podfile"
      to: "stripe-react-native + fmt post_install hooks"
      via: "preserved post_install block"
      pattern: "OTHER_CPLUSPLUSFLAGS|c\\+\\+17"
---

<objective>
Run the timeboxed (2-day, D-01) iOS Podfile `use_frameworks! :linkage => :static` spike that gates ALL of Phase 13. Prove a Release archive builds AND runs on a real iOS device with the existing Stripe + fmt/C++17 post_install hooks intact and Stripe checkout still working. Record the notifee decision (D-03). If it cannot pass within the timebox → ABORT & REVERT (D-02).

Purpose: This is milestone risk #1. Adding `@react-native-firebase` forces iOS static frameworks, which collides with the existing Stripe + RN 0.83 prebuilt-core native build. No downstream native/mobile plan may run until this passes. There is NO fallback device path — if a real-device Release build cannot be proven, the phase aborts (D-02) and Phase-12's in-app center ships as the only channel.
Output: A working static-frameworks `ios/Podfile`, push entitlements/Info.plist edits, and `13-SPIKE-RESULT.md` recording the pass/fail decision, the exact working incantation, and the notifee decision — OR a clean revert to the rollback checkpoint with the abort recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-fcm-push-transport-native/13-CONTEXT.md
@.planning/phases/13-fcm-push-transport-native/13-RESEARCH.md
@.planning/phases/13-fcm-push-transport-native/13-PATTERNS.md

# Memory: [[firebase_sdk_split]] — RNFB native SDK is exactly what this spike de-risks.
# RESEARCH §Common Pitfalls 1-3 and §iOS APNs + Entitlements are the load-bearing detail.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the pre-frameworks rollback checkpoint</name>
  <files>.planning/phases/13-fcm-push-transport-native/13-SPIKE-RESULT.md</files>
  <read_first>
    - ios/Podfile (current `ENV['USE_FRAMEWORKS']` toggle at lines ~11-15 and the `post_install` Stripe/fmt hooks at ~34-49 — these MUST survive the switch)
    - ios/Podfile.lock (the pre-frameworks lockfile state to capture)
    - .planning/phases/13-fcm-push-transport-native/13-RESEARCH.md (§Runtime State Inventory — "rollback checkpoint must capture pre-frameworks Podfile.lock")
    - .planning/phases/13-fcm-push-transport-native/13-CONTEXT.md (D-02 abort/revert path)
  </read_first>
  <action>
    Establish a committed rollback point BEFORE touching linkage (D-02). Confirm working tree is clean (or stash unrelated version-bump churn in `ios/carEx.xcodeproj/project.pbxproj` / `android/version.properties`). Record the current git SHA, the current `ios/Podfile.lock` hash, and the exact pre-switch Podfile toggle state into a new `13-SPIKE-RESULT.md` under a "Rollback Checkpoint" heading (SHA, timebox start timestamp per D-01, revert command `git checkout <SHA> -- ios/Podfile ios/Podfile.lock` plus `pod deintegrate && pod install`). Commit this checkpoint so a clean revert target exists. Do NOT yet change linkage.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-fcm-push-transport-native/13-SPIKE-RESULT.md && grep -q "Rollback Checkpoint" .planning/phases/13-fcm-push-transport-native/13-SPIKE-RESULT.md && git log --oneline -1 -- ios/Podfile.lock >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - 13-SPIKE-RESULT.md exists with a "Rollback Checkpoint" section recording the pre-frameworks git SHA, Podfile.lock state, timebox start timestamp, and the exact revert command.
    - The checkpoint is committed (a known-good SHA to revert to exists).
  </acceptance_criteria>
  <done>A committed, documented pre-frameworks rollback target exists; linkage is still unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Switch to static frameworks and resolve the three native-build landmines</name>
  <files>ios/Podfile, ios/carEx/carEx.entitlements, ios/carEx/Info.plist, .planning/phases/13-fcm-push-transport-native/13-SPIKE-RESULT.md</files>
  <read_first>
    - ios/Podfile (the `post_install` block; the fmt `CLANG_CXX_LANGUAGE_STANDARD=c++17` hook and the stripe-react-native `OTHER_CPLUSPLUSFLAGS -Wno-error=enum-redeclared...` hook at ~34-49 MUST be preserved verbatim)
    - ios/carEx/carEx.entitlements (existing `com.apple.developer.associated-domains` entry — the edit point/shape for adding `aps-environment`)
    - ios/carEx/Info.plist (confirm `UIBackgroundModes` is absent; add `remote-notification`)
    - .planning/phases/13-fcm-push-transport-native/13-RESEARCH.md (§Common Pitfalls 1 FollyConvert.h, 2 prebuilt-core, 3 device/Release; §iOS APNs + Entitlements; A2/A3 assumptions)
    - .planning/phases/13-fcm-push-transport-native/13-PATTERNS.md (ios/Podfile + entitlements/Info.plist sections)
  </read_first>
  <action>
    Enable `use_frameworks! :linkage => :static` (via the existing `ENV['USE_FRAMEWORKS']` toggle seam at ~Podfile:11-15) and add `$RNFirebaseAsStaticFramework = true`. Run `cd ios && RCT_USE_PREBUILT_RNCORE=0 pod install` (Pitfall 2 / A2 — the RN 0.83 prebuilt-core vs static-linkage workaround MUST be proven, not assumed; a `pod deintegrate` first may be required when toggling linkage). Resolve Pitfall 1 (`stripe-react-native` `react/utils/FollyConvert.h not found`): apply the minimal header-search-path / post_install re-export fix and PRESERVE the existing fmt c++17 + stripe enum-redeclared hooks. Add `aps-environment` (development) to `carEx.entitlements` mirroring the existing associated-domains entry, and add `UIBackgroundModes` → `remote-notification` to `Info.plist`. Enable Push Notifications + Background Modes capabilities in Xcode. Confirm A3 (whether the `RCTReactNativeFactory` AppDelegate needs manual APNs registration forwarding) and record the finding. Record every exact incantation (env flags, deintegrate, header fix) in 13-SPIKE-RESULT.md under "Working Incantation". iOS target is already 15.1 (≥15) — no change needed. Do NOT yet install RNFB JS packages (that is plan 13-03, gated on this spike passing); for the spike, add only the pods needed to reproduce the static-frameworks build, or stage RNFB pods solely to prove the build and note it.
  </action>
  <verify>
    <automated>grep -q "linkage => :static" ios/Podfile && grep -Eq "OTHER_CPLUSPLUSFLAGS|enum-redeclared" ios/Podfile && grep -q "aps-environment" ios/carEx/carEx.entitlements && grep -q "remote-notification" ios/carEx/Info.plist && grep -q "Working Incantation" .planning/phases/13-fcm-push-transport-native/13-SPIKE-RESULT.md</automated>
  </verify>
  <acceptance_criteria>
    - Podfile uses `linkage => :static`, declares `$RNFirebaseAsStaticFramework = true`, and STILL contains the fmt c++17 + stripe enum-redeclared post_install hooks (grep proves both present).
    - `pod install` completes with `RCT_USE_PREBUILT_RNCORE=0` and `FollyConvert.h` resolves (no `'react/utils/FollyConvert.h' file not found`).
    - `carEx.entitlements` contains `aps-environment`; `Info.plist` contains `UIBackgroundModes` → `remote-notification`.
    - 13-SPIKE-RESULT.md "Working Incantation" records the exact env flags, deintegrate steps, header fix, and the A2/A3 findings.
  </acceptance_criteria>
  <done>Static-frameworks `pod install` succeeds with Stripe's FollyConvert resolving and the existing hooks preserved; push entitlements/Info.plist added; exact incantation recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Real-device Release archive + Stripe-checkout verification (the spike gate)</name>
  <what-built>
    Static-frameworks `ios/Podfile` + `$RNFirebaseAsStaticFramework` + `RCT_USE_PREBUILT_RNCORE=0` build, with Stripe's FollyConvert resolved and the fmt/Stripe post_install hooks preserved; push entitlements and Background Modes added.
  </what-built>
  <how-to-verify>
    This is the D-02 hard gate — Release archive on a REAL device, NOT simulator (Pitfall 3). The 2-day timebox (D-01) is the abort trigger.
    1. Build a Release archive: `npm run ios:archive` (or Xcode Product → Archive with Release config).
    2. Install the archived build on a real iOS device (Apple developer signing required — no simulator).
    3. Launch the app; confirm it RUNS (does not crash on launch under static frameworks).
    4. Complete a Stripe TEST checkout end-to-end (createPaymentIntent → Stripe sheet → confirmBooking). Confirm it still works.
    5. Decide and RECORD in 13-SPIKE-RESULT.md (D-03): does RNFB built-in display suffice for Phase 13's generic background/quit pushes, or is `@notifee/react-native` needed? (RESEARCH recommends defaulting to RNFB built-in display, no notifee, for this phase.)
    6. PASS = archive builds + runs on device + Stripe checkout works. Otherwise → ABORT.
    If FAILED or timebox exceeded: run the documented revert (`git checkout <checkpoint-SHA> -- ios/Podfile ios/Podfile.lock` + `pod deintegrate && pod install`), record the abort + reason in 13-SPIKE-RESULT.md, and STOP the phase (do NOT extend the timebox; do NOT pivot transports — D-02).
  </how-to-verify>
  <acceptance_criteria>
    - HUMAN-UAT: Release archive runs on a real device AND a Stripe test checkout completes (NPUSH-01 / 13-HUMAN-UAT NPUSH-01 item).
    - 13-SPIKE-RESULT.md records PASS/FAIL, the notifee decision (D-03), and — if FAIL — the abort reason and confirmation the repo was reverted to the checkpoint.
  </acceptance_criteria>
  <resume-signal>Type "spike passed" to unlock the rest of Phase 13, or "spike aborted" to trigger the D-02 revert-and-stop path.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App ↔ APNs | The aps-environment entitlement + Background Modes authorize this app to receive remote notifications; misconfiguration silently drops pushes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | ios/Podfile post_install (Stripe/fmt hooks) | mitigate | Preserve the existing fmt c++17 + stripe enum-redeclared hooks through the linkage switch; grep-verify both survive. Regressing them breaks Stripe checkout (payment integrity). |
| T-13-01-02 | Denial of Service | iOS Release build under static frameworks | mitigate | Spike proves a real-device Release archive runs; the committed rollback checkpoint + D-02 abort path prevent shipping a broken build. |
| T-13-01-03 | Information Disclosure | aps-environment scope | accept | Development aps-environment is appropriate for the spike; production value is set at release build time. No data exposure during the spike. |
</threat_model>

<verification>
- `grep "linkage => :static" ios/Podfile` present; Stripe/fmt hooks still present.
- `aps-environment` in entitlements; `remote-notification` in Info.plist.
- 13-SPIKE-RESULT.md records rollback checkpoint, working incantation, real-device PASS/FAIL, and the D-03 notifee decision.
- Real-device Release archive runs + Stripe checkout works (HUMAN-UAT NPUSH-01).
</verification>

<success_criteria>
NPUSH-01 satisfied: the static-frameworks switch produces a Stripe-intact Release build that runs on a real device, committed behind a rollback checkpoint, with the notifee decision recorded — OR a clean, documented abort+revert per D-02. This plan's PASS is the precondition for waves 2-4.
</success_criteria>

<output>
After completion, create `.planning/phases/13-fcm-push-transport-native/13-01-SUMMARY.md`. If the spike aborted (D-02), the SUMMARY must state the phase stops here and the in-app center ships as the only channel for this milestone.
</output>
