---
phase: 13-fcm-push-transport-native
plan: 04
type: execute
wave: 3
depends_on: [13-01, 13-02, 13-03]
files_modified:
  - src/services/push/PushService.ts
  - src/services/push/__tests__/PushService.test.ts
  - src/context/AuthContext.tsx
  - index.js
  - App.tsx
files_modified_note: "Touches AuthContext.tsx + App.tsx; no other Wave-3 plan touches these — runs alone in its wave."
autonomous: true
requirements: [NPUSH-04, NPUSH-06, NPUSH-07]
must_haves:
  truths:
    - "Device tokens register on login and signup, refresh on onTokenRefresh, and unregister on logout"
    - "The logout unregister fires BEFORE currentIdTokenRef clears (line ~439) so it carries a valid Bearer"
    - "setBackgroundMessageHandler is registered at module scope in index.js, before AppRegistry.registerComponent"
    - "Foreground (onMessage), background (onNotificationOpenedApp), and quit/cold-start (getInitialNotification) taps all route data.deeplink"
    - "Push HTTP lives in a new PushService (NOT AuthService) — MOB-01 guardrail"
  artifacts:
    - path: "src/services/push/PushService.ts"
      provides: "registerToken/unregisterToken via apiClient (no uid in body)"
      contains: "apiClient"
    - path: "index.js"
      provides: "background message handler at module scope"
      contains: "setBackgroundMessageHandler"
    - path: "App.tsx"
      provides: "3-state tap routing effect"
      contains: "getInitialNotification"
  key_links:
    - from: "src/context/AuthContext.tsx logout()"
      to: "PushService.unregisterToken"
      via: "fires BEFORE currentIdTokenRef.current = null (line ~439)"
      pattern: "unregisterToken"
    - from: "App.tsx tap effect"
      to: "existing linking config (CarDetails/SearchResults)"
      via: "navigationRef.navigate from data.deeplink"
      pattern: "getInitialNotification|onNotificationOpenedApp"
---

<objective>
Wire the mobile transport: a new PushService (device-token HTTP), AuthContext token lifecycle (register/refresh/unregister with the logout ordering fix), the index.js background handler, and the App.tsx 3-state tap routing. GATED on the spike (13-01) and consuming the backend routes (13-02) and RNFB install (13-03).

Purpose: NPUSH-04/06/07 — the client half. The two highest-risk traps live here: the logout-ordering Bearer trap (unregister before the idToken ref clears) and the cold-start handler registration (background handler at module scope, getInitialNotification on mount). Push HTTP must stay OFF AuthService (MOB-01).
Output: PushService + green logout-ordering unit test; AuthContext lifecycle hooks; index.js background handler; App.tsx 3-state routing via navigationRef (sidesteps the Android carex://search manifest gap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-fcm-push-transport-native/13-RESEARCH.md
@.planning/phases/13-fcm-push-transport-native/13-PATTERNS.md

<interfaces>
<!-- Reuse, do NOT re-explore: -->
<!-- NotificationService split shape (src/services/notifications/NotificationService.ts:26-39 header; :189-214 thin verb+path method): copy for PushService. -->
<!-- apiClient (src/services/http/client.ts:88-95 request interceptor attaches Bearer from tokenProvider; :139-183 401 refresh). DELETE-with-body goes via config.data. -->
<!-- AuthContext seams: login ~385-412, signup ~414-433, logout ~435 with currentIdTokenRef.current = null at ~439 (THE ORDERING TRAP), mount effect ~242-352, refreshGenerationRef ~449 stale-after-await guard. -->
<!-- App.tsx: linking config ~81-96 (Home '', CarDetails 'listing/:carId', SearchResults 'search'); in-tree effect mount precedent AppStateRefreshEffect ~68-78; navigationRef. -->
<!-- index.js: current 4-line AppRegistry registrar. -->
<!-- Backend routes from 13-02: POST /api/notifications/device-tokens {token,platform,appVersion}; DELETE /api/notifications/device-tokens/:token (uid from Bearer). -->
<!-- Live permission read: messaging().hasPermission() returns AuthorizationStatus.AUTHORIZED|PROVISIONAL. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PushService + logout-ordering unit test</name>
  <files>src/services/push/PushService.ts, src/services/push/__tests__/PushService.test.ts</files>
  <read_first>
    - src/services/notifications/NotificationService.ts (header comment ~26-39 citing MOB-01; thin verb+path method ~189-214; DELETE-body note ~24)
    - src/services/moderation/ModerationService.ts (the original split precedent)
    - src/services/http/client.ts (apiClient request/401 interceptors ~88-95 / ~139-183)
    - src/services/notifications/__tests__/NotificationService.test.ts (test harness precedent for a thin service)
    - .planning/phases/13-fcm-push-transport-native/13-PATTERNS.md (PushService section + MOB-01 guardrail)
  </read_first>
  <behavior>
    - Test: registerToken({token,platform,appVersion}) calls apiClient.post('/api/notifications/device-tokens', body) and sends NO uid in the body.
    - Test: unregisterToken(token) issues a DELETE for that token (body via config.data if DELETE-with-body).
    - Test (logout-ordering logic): a helper that captures the FCM token and fires unregister can complete using the captured token, NOT depending on currentIdTokenRef being non-null at call time — proving the ordering fix is testable in isolation.
    - Test: errors are logged and re-thrown (mirror NotificationService catch shape).
  </behavior>
  <action>
    Create src/services/push/PushService.ts mirroring NotificationService EXACTLY (header cites MOB-01; all methods are thin apiClient verb wrappers with try/catch console.error then throw). Methods: registerToken({ token, platform, appVersion }) hits POST /api/notifications/device-tokens (NO uid in body — backend derives from Bearer); unregisterToken(token) hits DELETE /api/notifications/device-tokens/:token (or DELETE-with-body via config.data). MOB-01: this HTTP MUST live here, NOT in AuthService.ts (a grep gate forbids device/notification HTTP in AuthService). Write PushService.test.ts per the behavior block, mocking apiClient. Logout-ordering correctness is exercised here at the logic level (NPUSH-04); real-device token behavior is HUMAN-UAT.
  </action>
  <verify>
    <automated>npx jest src/services/push/__tests__/PushService.test.ts</automated>
    <automated>test "$(grep -i -E 'device-?token|registerToken|unregisterToken' src/services/AuthService.ts | grep -vc '^[[:space:]]*//')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - npx jest src/services/push/__tests__/PushService.test.ts passes.
    - MOB-01 grep gate: zero device-token/register/unregister matches in AuthService.ts (excluding comments).
    - registerToken sends no uid in the body (asserted in test).
    - Covers the client logic half of NPUSH-04.
  </acceptance_criteria>
  <done>PushService exists off AuthService; logout-ordering logic unit-tested green.</done>
</task>

<task type="auto">
  <name>Task 2: AuthContext token lifecycle (register/refresh/unregister with ordering fix)</name>
  <files>src/context/AuthContext.tsx</files>
  <read_first>
    - src/context/AuthContext.tsx (login ~385-412, signup ~414-433, logout ~435 with currentIdTokenRef.current = null at ~439 THE TRAP, mount effect ~242-352 listener-subscribe precedent, refreshGenerationRef ~449 stale-after-await guard)
    - src/services/push/PushService.ts (registerToken/unregisterToken from Task 1)
    - .planning/phases/13-fcm-push-transport-native/13-PATTERNS.md (AuthContext section — register-on-login seam, the ORDERING TRAP, token-refresh listener pattern)
    - .planning/phases/13-fcm-push-transport-native/13-RESEARCH.md (§Device-Token Lifecycle, Pitfall 4)
  </read_first>
  <action>
    Register: after setUser in login (~385-412) and signup (~414-433), acquire the FCM token via messaging().getToken() and call PushService.registerToken({ token, platform: Platform.OS, appVersion }). Guard: only attempt if push permission is already granted (do NOT trigger the OS dialog here — that is 13-05's contextual pre-prompt; permission-on-launch is an anti-pattern). Refresh: subscribe messaging().onTokenRefresh once in the mount effect (~242-352, mirroring the existing setIdTokenRefreshListener subscribe-once precedent) and re-register on fire. Unregister (THE ORDERING FIX, NPUSH-04 / Pitfall 4): in logout (~435), CAPTURE the FCM token and fire PushService.unregisterToken(token) BEFORE currentIdTokenRef.current = null at line ~439 (so the request interceptor still has a valid Bearer); use the refreshGenerationRef pattern if any await could resolve post-logout. All RNFB calls are spike-gated (only valid because 13-01 passed).
  </action>
  <verify>
    <automated>npx jest src/context/__tests__/ 2>/dev/null; grep -n "unregisterToken" src/context/AuthContext.tsx && awk '/unregisterToken/{u=NR} /currentIdTokenRef.current = null/{n=NR} END{exit !(u>0 && u<n)}' src/context/AuthContext.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source/order assertion: the unregisterToken call appears at an EARLIER line than `currentIdTokenRef.current = null` in logout (awk ordering check passes) — the Bearer-still-valid invariant (Pitfall 4).
    - login and signup both call PushService.registerToken after session is saved; registration does NOT trigger the OS permission dialog.
    - onTokenRefresh is subscribed once on mount and re-registers.
    - Existing AuthContext/Notification context tests still pass.
  </acceptance_criteria>
  <done>Token lifecycle wired with the logout unregister provably firing before the idToken ref clears.</done>
</task>

<task type="auto">
  <name>Task 3: index.js background handler + App.tsx 3-state tap routing</name>
  <files>index.js, App.tsx</files>
  <read_first>
    - index.js (current 4-line AppRegistry registrar)
    - App.tsx (linking config ~81-96; in-tree effect mount precedent AppStateRefreshEffect ~68-78; navigationRef; provider order ~106-113)
    - src/components/notifications/WatchButton.tsx (~63 car?._id || car?.id || carId precedent — the deeplink car-id rule)
    - .planning/phases/13-fcm-push-transport-native/13-PATTERNS.md (index.js + App.tsx sections; Android cold-start gotcha / Pitfall 5)
    - .planning/phases/13-fcm-push-transport-native/13-RESEARCH.md (§Pattern 1 background handler, §Pattern 2 three-state routing)
  </read_first>
  <action>
    index.js: register messaging().setBackgroundMessageHandler(...) at MODULE SCOPE, BEFORE AppRegistry.registerComponent (NPUSH-06 — the OS spins the JS context with no React tree for background/quit data messages). Keep the handler MINIMAL/headless; do NOT do display/route work there. App.tsx: mount an in-tree effect inside the AuthProvider subtree (mirror AppStateRefreshEffect ~68-78) that handles all 3 states per RESEARCH §Pattern 2 — getInitialNotification() ONCE on mount (quit/cold-start tap, guard against double-handling), onNotificationOpenedApp() (background tap), onMessage() (foreground). Each reads data.deeplink and routes through the EXISTING linking whitelist (CarDetails 'listing/:carId', SearchResults 'search' — do NOT widen). Pitfall 5: route push taps via in-JS navigationRef.navigate (parsing data.deeplink) rather than Linking.openURL, to sidestep the missing carex://search Android intent-filter. The deeplink itself is built server-side with car._id || car.id || carId ([[car_id_field_unreliable]]) — never bare car.id. Real-device 3-state + cold-start behavior is HUMAN-UAT (NPUSH-06/07).
  </action>
  <verify>
    <automated>grep -q "setBackgroundMessageHandler" index.js && grep -Eq "getInitialNotification|onNotificationOpenedApp|onMessage" App.tsx && npx jest App.test 2>/dev/null; true</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: setBackgroundMessageHandler is in index.js at module scope before AppRegistry.registerComponent.
    - Source assertion: App.tsx mounts an effect calling getInitialNotification + onNotificationOpenedApp + onMessage, routing via navigationRef.navigate through the existing linking targets (no new linking screens added).
    - Real-device verification deferred to 13-HUMAN-UAT items NPUSH-06 (3 states) and NPUSH-07 (cold-start tap → CarDetails).
  </acceptance_criteria>
  <done>Background handler registered at module scope; 3-state in-JS tap routing wired through the existing linking whitelist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Logged-out device ↔ backend | After logout the device must stop receiving the prior user's pushes; requires an authorized unregister. |
| Push data.deeplink ↔ navigation | Untrusted deeplink string must route only to whitelisted screens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Information Disclosure | logout token unregister | mitigate | Unregister fires BEFORE currentIdTokenRef clears (awk ordering check) so the DELETE carries a valid Bearer; stale token cannot keep receiving pushes (Pitfall 4 / NPUSH-04). |
| T-13-04-02 | Tampering | data.deeplink routing | mitigate | Route only through the existing linking whitelist (CarDetails/SearchResults); do not widen; navigationRef.navigate parses known routes only. |
| T-13-04-03 | Elevation (IDOR) | registerToken | mitigate | No uid in body; backend derives uid from the verified Bearer (PushService reuses apiClient interceptor). |
</threat_model>

<verification>
- PushService.test.ts green; MOB-01 grep gate clean on AuthService.ts.
- awk ordering check: unregisterToken before currentIdTokenRef clear in logout.
- index.js background handler at module scope; App.tsx 3-state routing present.
- Mobile `npm test` suite stays green.
</verification>

<success_criteria>
NPUSH-04 (lifecycle register/refresh/unregister with the ordering fix), NPUSH-06 (3-state handling + background handler at index top), and NPUSH-07 (data.deeplink routing through existing linking) are wired on the client. Device-only behaviors (NPUSH-06/07 real-device taps) are queued for 13-HUMAN-UAT in plan 13-05.
</success_criteria>

<output>
After completion, create `.planning/phases/13-fcm-push-transport-native/13-04-SUMMARY.md`.
</output>
