---
phase: 13-fcm-push-transport-native
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - package.json
  - ios/Podfile.lock
  - android/build.gradle
  - android/app/build.gradle
  - android/app/google-services.json
  - android/app/src/main/AndroidManifest.xml
autonomous: false
requirements: [NPUSH-02, NPUSH-03]
user_setup:
  - service: firebase
    why: "Android needs google-services.json and iOS needs the APNs .p8 auth key uploaded to Firebase — both are Firebase-console artifacts Claude cannot generate."
    env_vars: []
    dashboard_config:
      - task: "Download google-services.json for the Android app and place it at android/app/google-services.json"
        location: "Firebase Console → Project Settings → Your apps (Android) → google-services.json"
      - task: "Upload the APNs .p8 auth key (with Key ID + Team ID) to Firebase Cloud Messaging"
        location: "Firebase Console → Project Settings → Cloud Messaging → Apple app configuration → APNs Authentication Key"

must_haves:
  truths:
    - "@react-native-firebase/app and /messaging are installed at exactly 24.1.0 (locked-step)"
    - "iOS pods install cleanly under static frameworks with RCT_USE_PREBUILT_RNCORE=0 after RNFB is added"
    - "Android google-services plugin is applied and google-services.json is present"
    - "POST_NOTIFICATIONS is declared and a default notification channel meta-data is set"
    - "APNs .p8 is uploaded to Firebase (human console step) — verified delivering on a real device"
  artifacts:
    - path: "package.json"
      provides: "RNFB app+messaging deps at 24.1.0"
      contains: "@react-native-firebase/messaging"
    - path: "android/app/build.gradle"
      provides: "google-services plugin applied at bottom"
      contains: "com.google.gms.google-services"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "POST_NOTIFICATIONS + default channel meta-data"
      contains: "POST_NOTIFICATIONS"
  key_links:
    - from: "android/build.gradle"
      to: "android/app/build.gradle"
      via: "google-services classpath → apply plugin"
      pattern: "com.google.gms:google-services"
---

<objective>
Install RNFB 24.1.0 (app + messaging, locked-step), apply Android google-services + POST_NOTIFICATIONS + default channel, and finalize iOS APNs (the entitlements/Info.plist were added in the spike). Human-gated console artifacts (google-services.json, APNs .p8) are explicit checkpoints.

Purpose: NPUSH-02/03 — the dependency + platform config layer that turns the proven static-frameworks build into a push-capable app. GATED on the 13-01 spike passing (no RNFB install before the static-frameworks build is proven — [[firebase_sdk_split]]).
Output: RNFB installed at 24.1.0, Android push config wired, APNs uploaded and verified delivering on a real device.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-fcm-push-transport-native/13-RESEARCH.md
@.planning/phases/13-fcm-push-transport-native/13-PATTERNS.md
@.planning/phases/13-fcm-push-transport-native/13-SPIKE-RESULT.md

# RESEARCH §Standard Stack (24.1.0 verified), §Android Setup, §iOS APNs + Entitlements.
# targetSdkVersion = 36 (≥33) → POST_NOTIFICATIONS is runtime-gated (resolves A4).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install RNFB 24.1.0 + pod install under static frameworks</name>
  <files>package.json, ios/Podfile.lock</files>
  <read_first>
    - package.json (current deps; confirm RNFB is absent — this is the first native Firebase dependency)
    - ios/Podfile (static-frameworks linkage + $RNFirebaseAsStaticFramework added by the 13-01 spike)
    - .planning/phases/13-fcm-push-transport-native/13-SPIKE-RESULT.md ("Working Incantation" — the exact env flags/deintegrate steps proven in the spike)
    - .planning/phases/13-fcm-push-transport-native/13-RESEARCH.md (§Standard Stack install block)
  </read_first>
  <action>
    Install `@react-native-firebase/app@24.1.0` and `@react-native-firebase/messaging@24.1.0` (LOCKED-STEP — identical versions; do not let one float). Run `cd ios && RCT_USE_PREBUILT_RNCORE=0 pod install` per the spike's recorded Working Incantation. Confirm `FollyConvert.h` still resolves and Stripe pods are intact (the spike already proved the combination). Do NOT add `@notifee/react-native` unless 13-SPIKE-RESULT.md's D-03 decision says it is required (RESEARCH default: RNFB built-in display, no notifee).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const a=(p.dependencies||{});if(a['@react-native-firebase/app']!=='24.1.0'||a['@react-native-firebase/messaging']!=='24.1.0'){process.exit(1)}" && grep -q "RNFBApp\|react-native-firebase" ios/Podfile.lock</automated>
  </verify>
  <acceptance_criteria>
    - package.json pins both `@react-native-firebase/app` and `/messaging` to exactly `24.1.0`.
    - `ios/Podfile.lock` references the RNFB pods (pod install succeeded under static frameworks).
    - notifee added ONLY if 13-SPIKE-RESULT.md D-03 requires it.
  </acceptance_criteria>
  <done>RNFB 24.1.0 installed locked-step; iOS pods resolve under static frameworks.</done>
</task>

<task type="auto">
  <name>Task 2: Android google-services plugin + POST_NOTIFICATIONS + default channel</name>
  <files>android/build.gradle, android/app/build.gradle, android/app/src/main/AndroidManifest.xml</files>
  <read_first>
    - android/build.gradle (buildscript dependencies block ~13-17; `targetSdkVersion = 36` confirms POST_NOTIFICATIONS is runtime-gated, A4 resolved)
    - android/app/build.gradle (apply plugin lines ~1-3; plugin goes at the BOTTOM)
    - android/app/src/main/AndroidManifest.xml (existing `uses-permission` line ~4 and intent-filters ~28-45 — listing/stripe-redirect hosts; carex://search is NOT declared, Pitfall 5)
    - .planning/phases/13-fcm-push-transport-native/13-PATTERNS.md (android/build.gradle + AndroidManifest sections)
    - .planning/phases/13-fcm-push-transport-native/13-RESEARCH.md (§Android Setup, Pitfall 5/6)
  </read_first>
  <action>
    In `android/build.gradle` buildscript dependencies, add `classpath("com.google.gms:google-services:4.4.4")`. In `android/app/build.gradle`, add `apply plugin: "com.google.gms.google-services"` at the BOTTOM. In `AndroidManifest.xml`: add `<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>` next to INTERNET; add the default-channel `<meta-data android:name="com.google.firebase.messaging.default_notification_channel_id" android:value="..."/>` inside `<application>`. Pitfall 5: do NOT add a `carex://search` intent-filter — instead the mobile tap routing (plan 13-04) routes push taps via in-JS `navigationRef.navigate` to sidestep the manifest gap; leave the manifest hosts unchanged except the additions above. (google-services.json placement is the next checkpoint.)
  </action>
  <verify>
    <automated>grep -q "com.google.gms:google-services" android/build.gradle && grep -q "com.google.gms.google-services" android/app/build.gradle && grep -q "POST_NOTIFICATIONS" android/app/src/main/AndroidManifest.xml && grep -q "default_notification_channel_id" android/app/src/main/AndroidManifest.xml</automated>
  </verify>
  <acceptance_criteria>
    - google-services classpath in `android/build.gradle`; plugin applied at the bottom of `android/app/build.gradle`.
    - `POST_NOTIFICATIONS` permission + default-channel meta-data present in the manifest.
    - No new `carex://search` intent-filter added (in-JS routing handles it per Pitfall 5).
  </acceptance_criteria>
  <done>Android push config wired; POST_NOTIFICATIONS + default channel declared.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Place google-services.json + upload APNs .p8 to Firebase</name>
  <what-built>
    Android google-services plugin is applied (awaits the config JSON); iOS aps-environment + Background Modes were added in the spike (13-01).
  </what-built>
  <how-to-verify>
    These are Firebase-console artifacts Claude cannot generate.
    1. Download `google-services.json` for the Android app from Firebase Console → Project Settings → Your apps (Android), and place it at `android/app/google-services.json`. Confirm: `test -f android/app/google-services.json`.
    2. Upload the APNs `.p8` auth key (with Key ID + Team ID) to Firebase Console → Project Settings → Cloud Messaging → Apple app configuration. (Preferred over `.p12`.)
    3. Confirm the FCM API is enabled and the service account has send scope (RESEARCH A1 — also flagged in 13-02 user_setup).
    4. After placement, run an Android debug build to confirm the google-services plugin processes the JSON without error.
  </how-to-verify>
  <acceptance_criteria>
    - `android/app/google-services.json` exists (file present).
    - APNs .p8 uploaded to Firebase (console-confirmed) — enables NPUSH-03 real-device delivery, verified in the 13-HUMAN-UAT NPUSH-03 item (after mobile wiring in 13-04).
  </acceptance_criteria>
  <resume-signal>Type "firebase configured" once google-services.json is placed and the APNs .p8 is uploaded.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App ↔ FCM/APNs | google-services.json + APNs .p8 authorize this app's push channel; misconfig drops pushes silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Spoofing | APNs .p8 / google-services.json | accept | Standard Firebase-console artifacts; .p8 scoped to this app's bundle/Team ID. Not committed secrets beyond what google-services.json already is (client config). |
| T-13-03-02 | Information Disclosure | POST_NOTIFICATIONS runtime gate | mitigate | targetSdk 36 → runtime permission required; request happens contextually via the pre-prompt (13-05), never silently on launch. |
| T-13-03-03 | Denial of Service | locked-step version drift | mitigate | app+messaging pinned to identical 24.1.0; node check fails the build if they diverge. |
</threat_model>

<verification>
- RNFB app+messaging both exactly 24.1.0; iOS pods resolve under static frameworks.
- Android google-services plugin applied; POST_NOTIFICATIONS + default channel declared.
- google-services.json present; APNs .p8 uploaded (console).
</verification>

<success_criteria>
NPUSH-02 (RNFB locked-step install, Android google-services + POST_NOTIFICATIONS + default channel, iOS target ≥15) and NPUSH-03 (APNs .p8 uploaded; entitlements/capabilities configured) are complete — the app is push-capable, pending the mobile wiring in 13-04 to actually receive/route.
</success_criteria>

<output>
After completion, create `.planning/phases/13-fcm-push-transport-native/13-03-SUMMARY.md`.
</output>
