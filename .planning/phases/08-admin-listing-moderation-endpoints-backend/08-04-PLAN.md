---
phase: 08-admin-listing-moderation-endpoints-backend
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - ../backend-services/carEx-services/src/moderation/listingService.js
  - ../backend-services/carEx-services/src/moderation/listingRouter.js
  - ../backend-services/carEx-services/__tests__/listing-moderation/deleteListing.test.js
autonomous: true
requirements: [LADM-04]

must_haves:
  truths:
    - "PATCH /api/admin/moderation/listings/:carId/delete on an active listing returns 200 with { ok, listing, action } and Car.status flips to 'deleted'"
    - "After successful delete-soft, Car.countDocuments({ _id: carId }) === 1 — the document IS NOT removed from MongoDB (LADM-04 critical invariant)"
    - "One ListingModerationAction row is inserted per successful delete with action='delete', fromStatus=<current>, toStatus='deleted'"
    - "Same-state (already deleted) returns 400 already_in_state"
    - "Cross-state (suspended → delete, archived → delete) succeeds per D-B open matrix"
  artifacts:
    - path: "../backend-services/carEx-services/src/moderation/listingService.js"
      provides: "deleteListing function body filled — soft-delete only (does NOT call deleteOne)"
      contains: "toStatus: 'deleted'"
    - path: "../backend-services/carEx-services/src/moderation/listingRouter.js"
      provides: "PATCH /:carId/delete route"
      contains: "router.patch('/:carId/delete'"
    - path: "../backend-services/carEx-services/__tests__/listing-moderation/deleteListing.test.js"
      provides: "Delete-soft service integration tests"
      contains: "countDocuments"
  key_links:
    - from: "../backend-services/carEx-services/src/moderation/listingRouter.js"
      to: "../backend-services/carEx-services/src/moderation/listingService.js"
      via: "service.deleteListing call"
      pattern: "service\\.deleteListing"
---

<objective>
Implement LADM-04 — the Soft-Delete endpoint. Same canonical shape as Suspend/Archive but with target status `'deleted'` and audit action `'delete'`. CRITICAL invariant: the Car document is NOT removed from MongoDB — only `status` flips to `'deleted'`. The phrase "delete-soft" means "tomb status, document survives." Plan 08-05's Restore can flip the same document back to `'active'`.

Purpose: Ship the third state-transition endpoint with the LADM-04 soft-delete invariant explicitly tested via `Car.countDocuments` post-call.

Output: 3 file modifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-01-PLAN.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-02-PLAN.md

<interfaces>
<!-- Same substrate as Plan 08-02 / 08-03. -->

deleteListingSchema (Plan 08-01) — same shape as suspend/archive: { reasonCategory, note? }.strict()

D-02 thin projection unchanged.

LADM-04 invariant (REQUIREMENTS.md): "Does NOT remove the document from the database." The handler MUST NOT call deleteOne/findOneAndDelete/deleteMany — only Car.updateOne($set: { status: 'deleted' }).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill listingService.deleteListing — soft-delete pattern (status flip only, no doc removal)</name>
  <files>../backend-services/carEx-services/src/moderation/listingService.js</files>
  <read_first>
    - ../backend-services/carEx-services/src/moderation/listingService.js (post-Plan-08-03 state — suspendListing + archiveListing implemented; deleteListing/restoreListing/editListing still throw not_implemented)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md (§ "11. deleteListing.test.js" — INVERTED-assertion call-out: v1.0 deleteProviderProfile hard-deletes, Phase 8 soft-deletes; assert doc PRESENT)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md (D-01 — "Document NOT removed from DB")
    - .planning/REQUIREMENTS.md (LADM-04 — exact wording: "Does NOT remove the document from the database")
  </read_first>
  <behavior>
    - `deleteListing({ adminUid, adminEmail, carId, reasonCategory, note })`:
      1. Pre-transaction read with both bypass options; throw `listing_not_found` on null
      2. Throw `already_in_state` if `current.status === 'deleted'`
      3. Inside `withTransaction`: insert audit row with `action: 'delete'`, `fromStatus: current.status`, `toStatus: 'deleted'`, reasonCategory, reasonNote, fieldDiff: null (array form)
      4. `Car.updateOne($set: { status: 'deleted', moderationReason: reasonCategory, moderationNote: note ?? null, moderatedBy: adminUid, moderatedAt: new Date() })` — NO call to `Car.deleteOne`/`findOneAndDelete`/`deleteMany` anywhere in this function
      5. Return D-02 thin projection
    - Critical invariant: `await Car.countDocuments({ _id: carId })` returns 1 BOTH before and after a successful `deleteListing` call
  </behavior>
  <action>
    Replace the `deleteListing` stub in `listingService.js`. Body is a copy of `archiveListing` (just landed in Plan 08-03) with these exact changes:
      - target literal: `'deleted'`
      - Audit `action` literal: `'delete'`
      - Inline header comment in the function explains: "LADM-04 SOFT-DELETE: document survives. This handler MUST NOT call Car.deleteOne / Car.deleteMany / Car.findOneAndDelete. The document remains in MongoDB with status='deleted' so Plan 08-05's restoreListing can flip it back to 'active'. The pre(/^find/) hide hook Phase 9 will land filters non-active listings from public reads; admin reads bypass via .setOptions({ includeAllListingStatuses: true })."
      - Defensive arg check identical to Suspend
    Do NOT touch any other function in `listingService.js`.
    Do NOT add any `Car.deleteOne` / `Car.deleteMany` / `Car.findOneAndDelete` / `Car.collection.deleteOne` reference anywhere — the function only does `Car.updateOne` for the status flip.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && node -e "const src = require('fs').readFileSync('./src/moderation/listingService.js', 'utf8'); const dm = src.match(/async function deleteListing[\\s\\S]*?\\n\\}/); if (!dm) { console.error('FAIL: deleteListing body not found'); process.exit(1); } const body = dm[0]; const checks = [{re: /toStatus: 'deleted'/, msg: 'toStatus literal'}, {re: /action: 'delete'/, msg: 'action literal'}, {re: /session\\.withTransaction/, msg: 'withTransaction'}, {re: /ListingModerationAction\\.create\\(\\[/, msg: 'array-form create'}, {re: /Car\\.updateOne/, msg: 'Car.updateOne'}, {re: /already_in_state/, msg: 'same-state code'}]; for (const c of checks) { if (!c.re.test(body)) { console.error('FAIL: ' + c.msg); process.exit(1); } } if (/Car\\.deleteOne|Car\\.deleteMany|Car\\.findOneAndDelete/.test(body)) { console.error('FAIL: deleteListing MUST NOT call any Car remove API (LADM-04 soft-delete invariant)'); process.exit(1); } console.log('OK: deleteListing soft-delete invariant locked');"</automated>
  </verify>
  <done>
    - `deleteListing` body matches the Archive shape with deleted/delete substitution
    - Static check proves NO `Car.deleteOne` / `Car.deleteMany` / `Car.findOneAndDelete` reference exists in the function body
    - suspendListing + archiveListing bodies untouched
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add PATCH /:carId/delete route to listingRouter.js</name>
  <files>../backend-services/carEx-services/src/moderation/listingRouter.js</files>
  <read_first>
    - ../backend-services/carEx-services/src/moderation/listingRouter.js (post-Plan-08-03 state — `/ping` + Suspend + Archive routes mounted) — file you are about to modify
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-02-PLAN.md (Task 2) — pattern source
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md (D-01 endpoint surface)
  </read_first>
  <behavior>
    - PATCH /:carId/delete route added below Archive
    - Mirror of Suspend/Archive: Zod parse with `schemas.deleteListingSchema` → service.deleteListing → handleListingServiceError on error
    - `denySelfModerationListing` mounted in front
    - NO multer
  </behavior>
  <action>
    Edit `src/moderation/listingRouter.js`. Add the Delete route immediately below the Archive route. Substitutions from the Archive template (just landed in Plan 08-03):
      - URL: `'/:carId/delete'`
      - Schema: `schemas.deleteListingSchema`
      - Service: `service.deleteListing({...})` — same payload shape (adminUid, adminEmail, carId, reasonCategory, note)
      - Tag: `'delete'`
    No new top-of-file requires. No KNOWN_LISTING_ERRORS additions.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && node -e "const src = require('fs').readFileSync('./src/moderation/listingRouter.js', 'utf8'); const checks = [{re: /router\\.patch\\('\\/:carId\\/suspend'/, msg: 'suspend route preserved'}, {re: /router\\.patch\\('\\/:carId\\/archive'/, msg: 'archive route preserved'}, {re: /router\\.patch\\('\\/:carId\\/delete'/, msg: 'delete route added'}, {re: /service\\.deleteListing/, msg: 'dispatches to deleteListing'}, {re: /schemas\\.deleteListingSchema/, msg: 'uses delete schema'}, {re: /router\\.get\\('\\/ping'/, msg: '/ping preserved'}]; for (const c of checks) { if (!c.re.test(src)) { console.error('FAIL: ' + c.msg); process.exit(1); } } if (/upload\\.array/.test(src)) { console.error('FAIL: multer must NOT be on Delete route'); process.exit(1); } console.log('OK: Delete route added cleanly');" && npx jest __tests__/listing-moderation/listingModerationRateLimiter.test.js --silent 2>&1 | tail -3</automated>
  </verify>
  <done>
    - Delete route mounted; Suspend + Archive + `/ping` routes byte-identical
    - Phase 7 rate-limit test still green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Land deleteListing.test.js — happy + soft-delete invariant + cross-state + same-state</name>
  <files>../backend-services/carEx-services/__tests__/listing-moderation/deleteListing.test.js</files>
  <read_first>
    - ../backend-services/carEx-services/__tests__/listing-moderation/suspendListing.test.js (post-Plan-08-02 state) — sibling shape
    - ../backend-services/carEx-services/__tests__/moderation/deleteProviderProfile.test.js (lines 55-100) — v1.0 hard-delete analog (INVERTED: v1.0 asserts doc gone; Phase 8 asserts doc PRESENT)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md (§ "11. deleteListing.test.js")
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md (D-16 deleteListing test catalog)
  </read_first>
  <behavior>
    - ≥ 5 tests green:
      1. happy path active→deleted: audit row has action='delete', fromStatus='active', toStatus='deleted', reasonCategory='spam'; Car has status='deleted'
      2. **Soft-delete invariant (LADM-04 CRITICAL):** after the happy-path call, `await Car.countDocuments({ _id: carId })` returns 1 AND `await Car.findById(carId).setOptions({ includeAllListingStatuses: true, includeAllUsers: true }).lean()` returns a non-null doc with `status === 'deleted'` AND original `sellerId` preserved AND original `createdAt` preserved
      3. same-state deleted→delete: throws `already_in_state`; audit count unchanged
      4. cross-state suspended→delete: succeeds (D-B open matrix); audit fromStatus='suspended'
      5. cross-state archived→delete: succeeds; audit fromStatus='archived'
  </behavior>
  <action>
    Create `__tests__/listing-moderation/deleteListing.test.js` as a near-clone of `suspendListing.test.js` (Plan 08-02). Substitutions:
      - `service.suspendListing` → `service.deleteListing`
      - `'suspended'` literal → `'deleted'` in seed status + assertions
      - `'suspend'` → `'delete'` in audit-row action assertions
    ADD a dedicated **soft-delete invariant** test block (this is the LADM-04 distinguishing assertion):
      ```
      test('LADM-04 soft-delete invariant: doc survives delete-soft', async () => {
        const carId = await seedCar({ sellerId: 'seller-x', status: 'active', createdAt: <fixed Date> });
        const preCount = await Car.countDocuments({ _id: carId });
        expect(preCount).toBe(1);

        await service.deleteListing({ adminUid: 'admin-1', adminEmail: 'a@x', carId, reasonCategory: 'spam', note: null });

        const postCount = await Car.countDocuments({ _id: carId });
        expect(postCount).toBe(1);

        const persisted = await Car.findById(carId).setOptions({ includeAllListingStatuses: true, includeAllUsers: true }).lean();
        expect(persisted).not.toBeNull();
        expect(persisted.status).toBe('deleted');
        expect(persisted.sellerId).toBe('seller-x');
        // createdAt preserved (only audit fields + status updated)
      });
      ```
    The other 4 tests follow the Suspend test shape with target-status substitution. Drop the missing-reasonCategory defensive test if it's redundant with Plan 08-02's coverage.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/deleteListing.test.js --silent 2>&1 | tail -10</automated>
  </verify>
  <done>
    - ≥ 5 tests green in deleteListing.test.js including the LADM-04 soft-delete invariant assertion
    - Phase 7 + Plans 08-01..03 tests still green
    - `Car.countDocuments({ _id: carId })` returns 1 after successful delete (proven by Test 2)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Inherited unchanged from Plan 08-02 (same mount chain + same middleware composition).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 (Delete variant) | Elevation of Privilege | Delete route | mitigate | `denySelfModerationListing` in front |
| T-08-02 (Delete variant) | Tampering / Repudiation | deleteListing service | mitigate | Same withTransaction audit-then-Car pattern as Suspend/Archive |
| T-08-10 | Data Destruction | LADM-04 invariant | mitigate | Task 1 static check forbids `Car.deleteOne/deleteMany/findOneAndDelete` in deleteListing; Task 3 test 2 asserts `Car.countDocuments === 1` post-call. The function does soft-delete by design: future S3-orphan/hard-delete tooling is out of Phase 8 scope (deferred per CONTEXT.md) |
</threat_model>

<verification>
1. `cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/ --silent 2>&1 | tail -5` — Phase 7 (33) + Plans 08-01..04 all green
2. `grep -c "router.patch('/:carId/" ../backend-services/carEx-services/src/moderation/listingRouter.js` = 3 (Suspend + Archive + Delete)
3. `grep -c "Car.deleteOne\\|Car.deleteMany\\|Car.findOneAndDelete" ../backend-services/carEx-services/src/moderation/listingService.js` = 0 (LADM-04 invariant — no remove APIs anywhere in the service)
4. `grep -c "toStatus: 'deleted'" ../backend-services/carEx-services/src/moderation/listingService.js` ≥ 1
</verification>

<success_criteria>
- LADM-04 endpoint live and tested
- Soft-delete invariant proven: document survives in MongoDB after a successful delete call
- No Car-remove APIs introduced (grep gate at zero)
- Phase 7 + 08-01..03 regressions preserved
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-listing-moderation-endpoints-backend/08-04-SUMMARY.md` documenting:
- Confirmation that no Car remove API was introduced anywhere
- The LADM-04 invariant test's exact assertions and the seeded fields whose preservation was verified
- Test count delta
</output>
