---
phase: 08-admin-listing-moderation-endpoints-backend
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - ../backend-services/carEx-services/src/moderation/listingService.js
  - ../backend-services/carEx-services/src/moderation/listingRouter.js
  - ../backend-services/carEx-services/__tests__/listing-moderation/suspendListing.test.js
autonomous: true
requirements: [LADM-02]

must_haves:
  truths:
    - "PATCH /api/admin/moderation/listings/:carId/suspend on an active listing returns 200 with { ok, listing, action } and Car.status flips to 'suspended'"
    - "One ListingModerationAction row is inserted per successful suspend with action='suspend', fromStatus='active', toStatus='suspended', reasonCategory + reasonNote populated"
    - "Same-state (already suspended) returns 400 already_in_state and no audit row is appended"
    - "Cross-state (archived → suspend, deleted → suspend) succeeds per D-B open matrix"
    - "Self-moderation rejected by middleware → 400 cannot_moderate_own_listing"
    - "Audit row and Car update are atomic — if either throws inside the transaction, both roll back"
  artifacts:
    - path: "../backend-services/carEx-services/src/moderation/listingService.js"
      provides: "suspendListing function body filled (replaces Wave-1 stub)"
      contains: "session.withTransaction"
    - path: "../backend-services/carEx-services/src/moderation/listingRouter.js"
      provides: "PATCH /:carId/suspend route + KNOWN_LISTING_ERRORS Set + handleListingServiceError function"
      contains: "router.patch('/:carId/suspend'"
    - path: "../backend-services/carEx-services/__tests__/listing-moderation/suspendListing.test.js"
      provides: "Suspend service integration tests"
      contains: "active.*suspended"
  key_links:
    - from: "../backend-services/carEx-services/src/moderation/listingRouter.js"
      to: "../backend-services/carEx-services/src/moderation/listingService.js"
      via: "service.suspendListing call"
      pattern: "service\\.suspendListing"
    - from: "../backend-services/carEx-services/src/moderation/listingService.js"
      to: "../backend-services/carEx-services/src/models/ListingModerationAction.js"
      via: "ListingModerationAction.create([doc], { session })"
      pattern: "ListingModerationAction\\.create\\(\\["
    - from: "../backend-services/carEx-services/src/moderation/listingService.js"
      to: "../backend-services/carEx-services/src/models/Car.js"
      via: "Car.updateOne with { session }"
      pattern: "Car\\.updateOne"
---

<objective>
Implement LADM-02 — the Suspend endpoint. Fill the `suspendListing` body in `listingService.js` with the canonical audit-then-Car transactional pattern; add the `PATCH /:carId/suspend` route in `listingRouter.js` (along with the substrate it needs: `KNOWN_LISTING_ERRORS` Set, `handleListingServiceError` helper, top-of-file imports for service/schemas/middleware/upload/errors); land `suspendListing.test.js` exercising happy path + cross-state + same-state-reject + reason-required + self-mod-reject + audit-row shape.

Purpose: Suspend is the canonical Phase 8 transition handler. The three sibling plans (Archive / Delete / Restore) copy this shape and only diverge on the `target` status string, the audit `action` verb, and (for Restore) the moderation-field clear-on-restore semantics.

Output: 3 file modifications (no new files in this plan — all new modules already exist from Plan 08-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-RESEARCH.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-VALIDATION.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-01-PLAN.md

<interfaces>
<!-- Substrate already exists from Plan 08-01. -->

`require('./listingErrors')` → `{ ListingServiceError }` (`throw new ListingServiceError('already_in_state')`)
`require('./listingSchemas')` → `{ suspendListingSchema, ... }` — `.strict()`, requires reasonCategory + optional note
`require('./denySelfModerationListing')` → `{ denySelfModerationListing }` — async middleware
`require('./listingService')` → `{ suspendListing, ... }` — this plan replaces the stub body
`require('../uploads/carImages')` → `{ upload }` — NOT used by Suspend route (multer only on Edit, D-D-1)

D-02 success response: { ok: true, listing: { _id, status, moderatedBy, moderatedAt }, action: { _id, action, fromStatus, toStatus, createdAt } }

D-03 error envelope: { error: <code>, message?: <human-readable> }

Pre-existing Car.js hide hook (lines 63-95) bypass: `.setOptions({ includeAllListingStatuses: true, includeAllUsers: true })` — both options needed (forward-compat for Phase 9 + existing seller-cascade bypass).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill listingService.suspendListing with the canonical audit-then-Car transaction</name>
  <files>../backend-services/carEx-services/src/moderation/listingService.js</files>
  <read_first>
    - ../backend-services/carEx-services/src/moderation/service.js (lines 1-21 + 42-134) — v1.0 canonical `suspend` shape and top-of-file array-form gotcha comment
    - ../backend-services/carEx-services/src/moderation/listingService.js (current Wave-1 skeleton from Plan 08-01) — file you are about to modify
    - ../backend-services/carEx-services/src/models/Car.js (lines 46-52, 63-95) — `status` + audit field set + existing pre(/^find/) hook
    - ../backend-services/carEx-services/src/models/ListingModerationAction.js (lines 38-83) — audit row field set + required fields
    - ../backend-services/carEx-services/src/moderation/listingErrors.js — `ListingServiceError` from Plan 08-01
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md (§ Pattern A audit-then-target-update + § "2. listingService.js" canonical suspend excerpt + § Pattern D includeAllListingStatuses)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md (D-06 transaction strategy, D-08 audit-then-Car ordering, D-B-1 same-state-fast-path, D-15 stamp moderationReason/moderationNote)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-RESEARCH.md (§ Pitfall 2 array-form gotcha, § Pitfall 5 setOptions, § Pitfall 9 pre-txn read)
  </read_first>
  <behavior>
    - `suspendListing({ adminUid, adminEmail, carId, reasonCategory, note })`:
      1. Throws `ListingServiceError('listing_not_found')` if `Car.findById(carId).setOptions({ includeAllListingStatuses: true, includeAllUsers: true }).lean()` returns null (PRE-transaction read)
      2. Throws `ListingServiceError('already_in_state')` if `current.status === 'suspended'` (D-B-1 fast-path)
      3. Opens session, inside `withTransaction`: (a) `await ListingModerationAction.create([{ listingId: carId, sellerUid: current.sellerId, adminUid, adminEmail, action: 'suspend', fromStatus: current.status, toStatus: 'suspended', reasonCategory, reasonNote: note ?? null, fieldDiff: null }], { session })` — ARRAY form mandatory; (b) `await Car.updateOne({ _id: carId }, { $set: { status: 'suspended', moderationReason: reasonCategory, moderationNote: note ?? null, moderatedBy: adminUid, moderatedAt: new Date() } }, { session })`; if `updated.matchedCount !== 1` → `throw new ListingServiceError('listing_not_found')`
      4. After commit, `session.endSession()` in `finally`
      5. Returns `{ listing: { _id, status: 'suspended', moderatedBy: adminUid, moderatedAt: <fresh Date> }, action: { _id: insertedAction._id.toString(), action: 'suspend', fromStatus: current.status, toStatus: 'suspended', createdAt: insertedAction.createdAt } }` — thin projection per D-02
    - Defensive: missing required args (adminUid, adminEmail, carId, reasonCategory) → `throw new ListingServiceError('invalid_payload')` so service-level callers cannot bypass router-level Zod
  </behavior>
  <action>
    Replace the `suspendListing` stub in `listingService.js` with the full body. Use the `service.js:42-134` shape (lines you already read first) renamed to the listing domain. Specifics:
      - Keep the top-of-file requires unchanged (already declared in Plan 08-01): `mongoose`, `Car`, `ListingModerationAction`, `ListingServiceError`.
      - Pre-transaction read: `const current = await Car.findById(carId).setOptions({ includeAllListingStatuses: true, includeAllUsers: true }).lean();` — both bypass flags per Pitfall 5 + existing-hook bypass. Then null-check → throw `listing_not_found`; same-state check → throw `already_in_state`.
      - Use `const session = await mongoose.startSession();` declared OUTSIDE `try` so it's reachable in `finally`. The transaction body is `await session.withTransaction(async () => { ... })`.
      - Audit insert FIRST (Phase 8 D-08 audit-then-Car ordering): `const [insertedAction] = await ListingModerationAction.create([{...}], { session });` — capture into outer-scope `let insertedAction;` declared above `try`.
      - Car update SECOND with the 5-field $set listed in the behavior block above. After the update, check `if (updated.matchedCount !== 1) throw new ListingServiceError('listing_not_found');`.
      - `finally { await session.endSession(); }`.
      - Build the return object using `current.status` for `action.fromStatus`. Do NOT chain another `Car.findById` after commit — read the Car post-state fields directly from the in-memory `$set` payload (everything needed for the response is already known: `_id = carId`, `status = 'suspended'`, `moderatedBy = adminUid`, `moderatedAt = <the Date you just constructed>`). This avoids a second round-trip + keeps the response shape deterministic.
      - Defensive arg-check at function top throws `ListingServiceError('invalid_payload')` when any of `adminUid/adminEmail/carId/reasonCategory` is missing.
    Do NOT modify the other 4 stub functions in this plan — Plans 08-03 / 08-04 / 08-05 / 08-06 own them.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && node -e "const s = require('./src/moderation/listingService'); const src = require('fs').readFileSync('./src/moderation/listingService.js', 'utf8'); if (!/session\.withTransaction/.test(src)) { console.error('FAIL: missing session.withTransaction'); process.exit(1); } if (!/ListingModerationAction\.create\(\[/.test(src)) { console.error('FAIL: missing array-form create'); process.exit(1); } if (!/includeAllListingStatuses: true/.test(src)) { console.error('FAIL: missing includeAllListingStatuses bypass'); process.exit(1); } if (!/Car\.updateOne/.test(src)) { console.error('FAIL: missing Car.updateOne'); process.exit(1); } if (!/already_in_state/.test(src)) { console.error('FAIL: missing already_in_state code'); process.exit(1); } console.log('OK: suspendListing static checks pass');"</automated>
  </verify>
  <done>
    - `suspendListing` body fills the stub; the other 4 functions remain `throw new ListingServiceError('not_implemented')`
    - Static checks (grep) confirm the canonical pattern shape
    - File still loads without error (`node -e "require('./src/moderation/listingService')"` exits 0)
    - Task 3's test file drives the runtime evidence
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add KNOWN_LISTING_ERRORS + handleListingServiceError + PATCH /:carId/suspend route to listingRouter.js</name>
  <files>../backend-services/carEx-services/src/moderation/listingRouter.js</files>
  <read_first>
    - ../backend-services/carEx-services/src/moderation/router.js (lines 55-148) — v1.0 KNOWN_USER_ERRORS + handleServiceError + canonical unsuspend handler shape (mirror for Suspend route)
    - ../backend-services/carEx-services/src/moderation/listingRouter.js (current Phase 7 scaffold: 29 lines, `/ping` only) — file you are about to modify
    - ../backend-services/carEx-services/src/moderation/listingSchemas.js (Plan 08-01) — `suspendListingSchema` exported here
    - ../backend-services/carEx-services/src/moderation/denySelfModerationListing.js (Plan 08-01)
    - ../backend-services/carEx-services/src/moderation/listingErrors.js (Plan 08-01)
    - ../backend-services/carEx-services/src/moderation/listingService.js (Task 1 of this plan)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md (§ Pattern B handleServiceError + § "14. listingRouter.js (MODIFY)" — exact KNOWN_LISTING_ERRORS Set + handler shape)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md (D-03 error codes registered, D-12 ListingServiceError class, D-13 router gains 4 new requires, D-D-1 multer ONLY on Edit route)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-RESEARCH.md (§ Pitfall 4 — `/ping` preservation invariant)
  </read_first>
  <behavior>
    - `listingRouter.js` preserves the Phase 7 `/ping` route byte-identical (Pitfall 4 — `__tests__/listing-moderation/listingModerationRateLimiter.test.js:50` still passes)
    - New imports added at top-of-file: `service`, `schemas`, `denySelfModerationListing`, no `upload` yet (the Suspend route is JSON-only per D-D-1 — `upload` joins in Plan 08-06 with the Edit route)
    - `KNOWN_LISTING_ERRORS` Set defined at module scope with all 10 codes per CONTEXT.md D-03 + Pitfall 1 codes
    - `handleListingServiceError(err, res, tag)` reads `err.code || err.message` and translates known codes → `res.status(400).json({ error: code })`; for `invalid_field` with `Array.isArray(err.fields)` it adds `fields: err.fields`; unknown → `console.error('[listing-moderation] ' + tag + ' error:', err); res.status(500).json({ error: 'internal_error', message: err.message })`
    - `PATCH /:carId/suspend` route: `router.patch('/:carId/suspend', denySelfModerationListing, async (req, res) => { ... })`; inside: `schemas.suspendListingSchema.safeParse(req.body || {})`; on parse failure return 400 `invalid_payload` with the Zod issues; on success call `service.suspendListing({ adminUid: req.admin.uid, adminEmail: req.admin.email, carId: req.params.carId, reasonCategory: parsed.data.reasonCategory, note: parsed.data.note })`; success → `res.json({ ok: true, listing: result.listing, action: result.action })`; failure → `handleListingServiceError(err, res, 'suspend')`
  </behavior>
  <action>
    Edit `src/moderation/listingRouter.js`. Add requires at top of file (after the existing `const express = require('express');`): `const service = require('./listingService');`, `const schemas = require('./listingSchemas');`, `const { denySelfModerationListing } = require('./denySelfModerationListing');`. Do NOT require `./listingErrors` directly — `handleListingServiceError` reads `err.code` which works on any thrown object; the type-class import is not needed here.

    Below the requires (above `const router = express.Router();` is fine, or below `router` — either works), define `const KNOWN_LISTING_ERRORS = new Set([ 'listing_not_found', 'invalid_transition', 'already_in_state', 'not_moderated', 'invalid_field', 'no_changes', 'invalid_payload', 'cannot_moderate_own_listing', 'invalid_make', 'invalid_model' ]);` — 10 codes, exact list. Inline comment by `invalid_transition` notes `// forward-compat per D-B-2; v1.1 never emits — reserved for future restricted-matrix super-admin tier`.

    Define `function handleListingServiceError(err, res, tag)` matching the behavior block above. PRESERVE the existing `/ping` route AND module.exports line byte-identical. Add the Suspend route BELOW `/ping`:
      `router.patch('/:carId/suspend', denySelfModerationListing, async (req, res) => { const parsed = schemas.suspendListingSchema.safeParse(req.body || {}); if (!parsed.success) { return res.status(400).json({ error: 'invalid_payload', issues: parsed.error.issues }); } try { const result = await service.suspendListing({ adminUid: req.admin.uid, adminEmail: req.admin.email, carId: req.params.carId, reasonCategory: parsed.data.reasonCategory, note: parsed.data.note }); return res.json({ ok: true, listing: result.listing, action: result.action }); } catch (err) { return handleListingServiceError(err, res, 'suspend'); } });`

    Do NOT add Archive / Delete / Restore / Edit routes — Plans 08-03 / 08-04 / 08-05 / 08-06 own them. Do NOT add multer to the Suspend route (D-D-1 — multer goes on Edit only).

    Phase 7 file header comment (current lines 1-15) should be preserved verbatim; the divergence-from-v1.0 notes remain accurate.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && node -e "const r = require('./src/moderation/listingRouter'); const src = require('fs').readFileSync('./src/moderation/listingRouter.js', 'utf8'); const checks = [{re: /router\.get\('\\/ping'/, msg: '/ping preserved'}, {re: /router\.patch\('\\/:carId\\/suspend'/, msg: 'suspend route present'}, {re: /KNOWN_LISTING_ERRORS = new Set/, msg: 'KNOWN_LISTING_ERRORS defined'}, {re: /'cannot_moderate_own_listing'/, msg: 'self-mod code registered'}, {re: /'invalid_make'/, msg: 'invalid_make registered'}, {re: /'invalid_model'/, msg: 'invalid_model registered'}, {re: /handleListingServiceError/, msg: 'handler function present'}, {re: /err\.code \|\| err\.message/, msg: 'err.code-with-fallback pattern'}, {re: /service\.suspendListing/, msg: 'dispatches to service.suspendListing'}, {re: /denySelfModerationListing/, msg: 'self-mod middleware mounted'}]; for (const c of checks) { if (!c.re.test(src)) { console.error('FAIL: ' + c.msg); process.exit(1); } } if (/upload\.array/.test(src)) { console.error('FAIL: multer must NOT be on Suspend route (D-D-1)'); process.exit(1); } console.log('OK: listingRouter.js static checks pass');" && npx jest __tests__/listing-moderation/listingModerationRateLimiter.test.js --silent 2>&1 | tail -3</automated>
  </verify>
  <done>
    - `/ping` route byte-identical; rate-limit test still green (Phase 7 regression preserved)
    - Suspend route mounted with `denySelfModerationListing` middleware in front
    - `KNOWN_LISTING_ERRORS` set has all 10 codes
    - `handleListingServiceError` reads `err.code || err.message`
    - No `upload.array` reference yet (Plan 08-06 introduces it on the Edit route only)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Land suspendListing.test.js — happy path + cross-state + same-state-reject + reason-required + self-mod-reject</name>
  <files>../backend-services/carEx-services/__tests__/listing-moderation/suspendListing.test.js</files>
  <read_first>
    - ../backend-services/carEx-services/__tests__/moderation/suspend.test.js (entire file, lines 1-164) — exact shape mirror
    - ../backend-services/carEx-services/__tests__/_helpers/mongoReplSet.js — replica-set start/stop hooks
    - ../backend-services/carEx-services/__tests__/moderation/editProfile.test.js (lines 54-57) — Car-equivalent direct-insert via `.collection.insertOne` pattern
    - ../backend-services/carEx-services/src/moderation/listingService.js (post-Task-1 state) — function being exercised
    - ../backend-services/carEx-services/src/models/Car.js (lines 46-52) — Car schema fields to assert against
    - ../backend-services/carEx-services/src/models/ListingModerationAction.js (lines 38-83) — audit row fields to assert against
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md (§ "9. suspendListing.test.js" — full test enumeration with diffs from analog)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md (D-16 test catalog for suspendListing)
  </read_first>
  <behavior>
    - 8 tests minimum, all green:
      1. happy path active→suspended: audit row has action='suspend', fromStatus='active', toStatus='suspended', reasonCategory='spam', reasonNote='manual test', listingId=carId, sellerUid=seller-x, adminUid, adminEmail set; Car has status='suspended', moderationReason='spam', moderationNote='manual test', moderatedBy=adminUid, moderatedAt instanceof Date
      2. same-state suspended→suspend: throws ListingServiceError with code 'already_in_state'; audit count remains 1 from any prior call OR 0 if seeded directly into suspended state
      3. cross-state archived→suspend: succeeds; audit fromStatus='archived', toStatus='suspended'
      4. cross-state deleted→suspend: succeeds; audit fromStatus='deleted', toStatus='suspended'
      5. listing not found: missing carId → throws ListingServiceError code 'listing_not_found'
      6. missing reasonCategory: throws ListingServiceError code 'invalid_payload' (service-layer defensive guard mirrors v1.0 service.js:43-44)
      7. response shape matches D-02: { listing: { _id, status, moderatedBy, moderatedAt }, action: { _id, action, fromStatus, toStatus, createdAt } } — assert these 5 + 5 keys and no extras (e.g. no `imageUrls`, no `description`)
      8. note=null when omitted: audit row's reasonNote field is null AND Car.moderationNote is null
    - Replica-set fixture mandatory (transactions); seed Car docs via `Car.collection.insertOne(...)` to skip the existing pre(/^find/) seller-cascade hook noise during seeding
  </behavior>
  <action>
    Create `__tests__/listing-moderation/suspendListing.test.js`. Top-of-file requires: `mongoose`, `{ startReplSet, stopReplSet }` from `../_helpers/mongoReplSet`, `service` from `../../src/moderation/listingService`, `Car` from `../../src/models/Car`, `ListingModerationAction` from `../../src/models/ListingModerationAction`.

    Standard hooks: `let rs; beforeAll(async () => { rs = await startReplSet(); }); afterAll(async () => { await stopReplSet(rs); }); beforeEach(async () => { await Car.deleteMany({}); try { await ListingModerationAction.collection.drop(); } catch (_) { /* may not exist */ } });`.

    Helper `seedCar(overrides = {})`: returns a new ObjectId; calls `Car.collection.insertOne({ _id: <new ObjectId>, sellerId: 'seller-x', status: 'active', listingStatus: 'active', createdAt: new Date(), ...overrides })`; returns the inserted `_id.toString()`. This bypasses the model-level pre-save validators and the seller-cascade pre(/^find/) hook during seeding (mirrors editProfile.test.js:54-57).

    Describe block `service.suspendListing (LADM-02)` with 8 tests as enumerated in the behavior block above. For each test, build the assertion against EITHER the audit collection (`ListingModerationAction.find({ listingId: carId }).lean()`) OR the Car collection (`Car.findById(carId).setOptions({ includeAllListingStatuses: true, includeAllUsers: true }).lean()`). For the not-found test, use a syntactically valid but never-seeded ObjectId (e.g. `new mongoose.Types.ObjectId().toString()`). For the response-shape test, capture `result` and assert `Object.keys(result.listing).sort()` equals `['_id', 'moderatedAt', 'moderatedBy', 'status'].sort()` AND `Object.keys(result.action).sort()` equals `['_id', 'action', 'createdAt', 'fromStatus', 'toStatus'].sort()`.

    Same-state test (test 2) seeds the Car with `status: 'suspended'` THEN calls `service.suspendListing({...})`; assert `await expect(...).rejects.toThrow('already_in_state')` AND `await ListingModerationAction.countDocuments({ listingId: carId }) === 0` (no audit row appended on the rejected same-state call).

    Cross-state tests (3 + 4) seed Car with `status: 'archived'` or `'deleted'`; expect success; assert audit row's `fromStatus` matches the seeded state.

    Missing-reasonCategory test (6): call `service.suspendListing({ adminUid: 'a', adminEmail: 'a@x', carId: '<seeded>'/* reasonCategory omitted */ })`; expect `rejects.toThrow('invalid_payload')`.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/suspendListing.test.js --silent 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 8 tests green in suspendListing.test.js
    - Phase 7+8-substrate suite still green (`npx jest __tests__/listing-moderation/` shows 33 + 17 (Plan 08-01) + 8 = 58 minimum)
    - Same-state rejection produces zero audit rows
    - Cross-state archived→suspend and deleted→suspend both succeed (D-B open matrix proven)
    - Response shape matches D-02 thin projection (no full Car payload leak)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → Suspend route | Phase 7 mount chain (verifyIdToken → requireAdmin → rate-limit) gates this — Phase 8 inherits without re-mitigation |
| Suspend route → denySelfModerationListing | Plan 08-01 substrate; mounted in front of every route in this plan |
| Service → ListingModerationAction.create | Audit insert uses array form per Pitfall 2; pre-hooks on the model are append-only per Phase 7 |
| Service → Car.updateOne | Single $set inside the transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 (Suspend variant) | Elevation of Privilege | Suspend route | mitigate | `denySelfModerationListing` mounted in front (Task 2); Plan 08-01 already tested the middleware in isolation |
| T-08-02 (Suspend variant) | Tampering / Repudiation | suspendListing service | mitigate | Pre-transaction read for same-state guard + `session.withTransaction(async () => { audit-then-Car })`; Plan 08-01's atomicity test covered the rollback paths against the canonical pattern (this plan's `suspendListing` implements that exact pattern) |
| T-08-03 (Suspend variant) | Tampering | Suspend payload | mitigate | `suspendListingSchema.safeParse` with `.strict()` rejects any non-reasonCategory/non-note keys; service-layer defensive arg check is the second wall |
| T-08-08 | Tampering | Missing setOptions on admin read | mitigate | Service's pre-transaction read chains both `includeAllListingStatuses: true` AND `includeAllUsers: true` so a seller-suspended listing is still reachable for moderation |
| T-08-09 | Information Disclosure | Response payload | mitigate | D-02 thin projection — Task 3 test 7 explicitly locks the response key set so a future refactor cannot accidentally widen the payload to leak `moderationNote` or `description` to clients |
</threat_model>

<verification>
After all three tasks land:
1. `cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/ --silent 2>&1 | tail -5` — Phase 7 (33) + Plan 08-01 (≥17) + this plan (≥8) all green
2. `grep -c "router.patch('/:carId/suspend'" ../backend-services/carEx-services/src/moderation/listingRouter.js` = 1
3. `grep -c "throw new ListingServiceError('already_in_state')" ../backend-services/carEx-services/src/moderation/listingService.js` ≥ 1
4. `grep -c "ListingModerationAction.create(\[" ../backend-services/carEx-services/src/moderation/listingService.js` ≥ 1 (array form verified)
5. `/ping` route still at line ~25 of listingRouter.js — Phase 7 regression test still green
6. `grep -c "upload\\.array" ../backend-services/carEx-services/src/moderation/listingRouter.js` = 0 (multer NOT on Suspend per D-D-1)
</verification>

<success_criteria>
- LADM-02 endpoint live: PATCH /api/admin/moderation/listings/:carId/suspend returns 200 with thin payload on a valid admin suspend
- Audit row + Car update commit atomically inside one Mongoose transaction
- Same-state suspend → 400 `already_in_state` with zero new audit rows
- Cross-state (archived/deleted → suspend) succeeds per D-B open matrix
- Self-moderation blocked by middleware
- Reason category required at both schema and service layers
- Phase 7 regression: rate-limit + middleware-chain test on `/ping` still green
- Plan 08-01 atomicity tests still green (canonical pattern unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-listing-moderation-endpoints-backend/08-02-SUMMARY.md` documenting:
- Final `suspendListing` body shape diff against the canonical v1.0 `service.js:42-134` (rename map + listing-domain adaptations)
- Test count delta on the listing-moderation suite (baseline before this plan → after)
- Any divergences from CONTEXT.md / PATTERNS.md (none expected)
- Confirmation that Plan 08-01 substrate is consumed unchanged (no edits to listingSchemas.js / denySelfModerationListing.js / listingErrors.js)
</output>
