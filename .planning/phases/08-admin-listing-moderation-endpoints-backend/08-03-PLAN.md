---
phase: 08-admin-listing-moderation-endpoints-backend
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - ../backend-services/carEx-services/src/moderation/listingService.js
  - ../backend-services/carEx-services/src/moderation/listingRouter.js
  - ../backend-services/carEx-services/__tests__/listing-moderation/archiveListing.test.js
autonomous: true
requirements: [LADM-03]

must_haves:
  truths:
    - "PATCH /api/admin/moderation/listings/:carId/archive on an active listing returns 200 with { ok, listing, action } and Car.status flips to 'archived'"
    - "One ListingModerationAction row is inserted per successful archive with action='archive', fromStatus=<current>, toStatus='archived'"
    - "Same-state (already archived) returns 400 already_in_state and no audit row is appended"
    - "Cross-state (suspended → archive, deleted → archive) succeeds per D-B open matrix"
    - "Archive is semantically distinct: the 'inactive_seller' reasonCategory exists exactly so admins can reclassify a non-violation listing without using a punitive Suspend"
  artifacts:
    - path: "../backend-services/carEx-services/src/moderation/listingService.js"
      provides: "archiveListing function body filled (replaces Wave-1 stub)"
      contains: "toStatus: 'archived'"
    - path: "../backend-services/carEx-services/src/moderation/listingRouter.js"
      provides: "PATCH /:carId/archive route"
      contains: "router.patch('/:carId/archive'"
    - path: "../backend-services/carEx-services/__tests__/listing-moderation/archiveListing.test.js"
      provides: "Archive service integration tests"
      contains: "archived"
  key_links:
    - from: "../backend-services/carEx-services/src/moderation/listingRouter.js"
      to: "../backend-services/carEx-services/src/moderation/listingService.js"
      via: "service.archiveListing call"
      pattern: "service\\.archiveListing"
---

<objective>
Implement LADM-03 — the Archive endpoint. Same canonical shape as Suspend (Plan 08-02) but with target status `'archived'` and audit action `'archive'`. Archive is semantically distinct from Suspend — it is non-punitive (for inactive sellers), enabled by the `inactive_seller` reasonCategory enum value that exists exactly for this case.

Purpose: Ship the second of four state-transition endpoints. Reuses the Plan 08-02 pattern verbatim with target-status + action-verb differences.

Output: 3 file modifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-01-PLAN.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-02-PLAN.md

<interfaces>
<!-- Substrate from Plan 08-01 and pattern locked by Plan 08-02. -->

archiveListingSchema (Plan 08-01) — same shape as suspendListingSchema: { reasonCategory: reasonCategoryEnum, note?: string }.strict()

D-02 response shape — same thin projection as Suspend, with status='archived'.

Reason taxonomy: `inactive_seller` is the canonical Archive reason; Phase 8 does NOT enforce category-to-action mapping at the schema layer (admin discretion — D-A-1 permissive validation), but `inactive_seller` is the most common pick for Archive.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill listingService.archiveListing — mirror of suspendListing with target='archived' + action='archive'</name>
  <files>../backend-services/carEx-services/src/moderation/listingService.js</files>
  <read_first>
    - ../backend-services/carEx-services/src/moderation/listingService.js (post-Plan-08-02 state — suspendListing fully implemented) — exact shape to mirror
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-02-PLAN.md (Task 1) — pattern source for this plan
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md (D-B open transition matrix, D-15 stamp moderationReason+moderationNote on all non-Edit/non-Restore)
  </read_first>
  <behavior>
    - `archiveListing({ adminUid, adminEmail, carId, reasonCategory, note })`:
      1. Pre-transaction read with both bypass options; throw `listing_not_found` on null
      2. Throw `already_in_state` if `current.status === 'archived'`
      3. Inside `withTransaction`: insert audit row with `action: 'archive'`, `fromStatus: current.status`, `toStatus: 'archived'`, `reasonCategory`, `reasonNote: note ?? null`, `fieldDiff: null` (array form `.create([doc], { session })`)
      4. `Car.updateOne($set: { status: 'archived', moderationReason: reasonCategory, moderationNote: note ?? null, moderatedBy: adminUid, moderatedAt: new Date() })`
      5. Return D-02 thin projection
  </behavior>
  <action>
    Replace the `archiveListing` stub in `listingService.js`. Body is a copy of `suspendListing` (just landed in Plan 08-02) with these exact changes:
      - `target` variable / target-status literal: `'archived'` (used in audit `toStatus`, the same-state check, and the Car `$set`)
      - Audit `action` literal: `'archive'`
      - Inline header comment in the function explains: "Archive is non-punitive (LADM-03 — inactive_seller reason most common). Per D-B open matrix, allows transitions from active/suspended/deleted → archived. Same-state guard rejects archived→archive."
      - Defensive arg check identical to Suspend: missing adminUid/adminEmail/carId/reasonCategory → `ListingServiceError('invalid_payload')`
    Do NOT modify `suspendListing`, `deleteListing`, `restoreListing`, or `editListing` — they are owned by other plans.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && node -e "const src = require('fs').readFileSync('./src/moderation/listingService.js', 'utf8'); const archiveMatch = src.match(/async function archiveListing[\\s\\S]*?\\n\\}/); if (!archiveMatch) { console.error('FAIL: archiveListing body not found'); process.exit(1); } const body = archiveMatch[0]; const checks = [{re: /session\\.withTransaction/, msg: 'withTransaction in body'}, {re: /ListingModerationAction\\.create\\(\\[/, msg: 'array-form create'}, {re: /toStatus: 'archived'/, msg: 'toStatus literal'}, {re: /action: 'archive'/, msg: 'action literal'}, {re: /already_in_state/, msg: 'same-state code'}, {re: /includeAllListingStatuses: true/, msg: 'bypass option'}]; for (const c of checks) { if (!c.re.test(body)) { console.error('FAIL: ' + c.msg); process.exit(1); } } console.log('OK: archiveListing body static checks pass');"</automated>
  </verify>
  <done>
    - `archiveListing` body matches the Suspend pattern with archived/archive substitutions
    - `suspendListing` body byte-identical to its Plan-08-02 commit
    - Two other stubs (`deleteListing`, `restoreListing`, `editListing`) still throw `not_implemented`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add PATCH /:carId/archive route to listingRouter.js</name>
  <files>../backend-services/carEx-services/src/moderation/listingRouter.js</files>
  <read_first>
    - ../backend-services/carEx-services/src/moderation/listingRouter.js (post-Plan-08-02 state — Suspend route + KNOWN_LISTING_ERRORS + handleListingServiceError landed) — file you are about to modify
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-02-PLAN.md (Task 2) — pattern source
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md (D-01 endpoint surface, D-D-1 multer ONLY on Edit)
  </read_first>
  <behavior>
    - `PATCH /:carId/archive` route added BELOW the Suspend route (added by Plan 08-02)
    - Mirror of the Suspend route's structure: Zod parse with `schemas.archiveListingSchema` → on parse failure 400 invalid_payload → on success call `service.archiveListing(...)` → success 200 with { ok, listing, action } → failure → `handleListingServiceError(err, res, 'archive')`
    - `denySelfModerationListing` mounted in front
    - NO multer (D-D-1 — Edit-only)
    - `/ping` route + Suspend route + KNOWN_LISTING_ERRORS + handleListingServiceError all preserved byte-identical
  </behavior>
  <action>
    Edit `src/moderation/listingRouter.js`. Add the Archive route immediately below the Suspend route. Structure exactly mirrors Suspend with two literal substitutions:
      - URL: `'/:carId/archive'`
      - Schema: `schemas.archiveListingSchema`
      - Service call: `service.archiveListing({...})` with same payload shape as Suspend (`adminUid`, `adminEmail`, `carId`, `reasonCategory`, `note`)
      - Tag string to `handleListingServiceError`: `'archive'`
    No new top-of-file requires — `service`, `schemas`, `denySelfModerationListing` are already imported in Plan 08-02. No additions to `KNOWN_LISTING_ERRORS` (the existing 10 codes cover Archive).
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && node -e "const src = require('fs').readFileSync('./src/moderation/listingRouter.js', 'utf8'); const checks = [{re: /router\\.patch\\('\\/:carId\\/suspend'/, msg: 'suspend route preserved'}, {re: /router\\.patch\\('\\/:carId\\/archive'/, msg: 'archive route added'}, {re: /service\\.archiveListing/, msg: 'dispatches to archiveListing'}, {re: /schemas\\.archiveListingSchema/, msg: 'uses archive schema'}, {re: /router\\.get\\('\\/ping'/, msg: '/ping preserved'}]; for (const c of checks) { if (!c.re.test(src)) { console.error('FAIL: ' + c.msg); process.exit(1); } } if (/upload\\.array/.test(src)) { console.error('FAIL: multer must NOT be on Archive route'); process.exit(1); } console.log('OK: listingRouter.js Archive route static checks pass');" && npx jest __tests__/listing-moderation/listingModerationRateLimiter.test.js --silent 2>&1 | tail -3</automated>
  </verify>
  <done>
    - Archive route mounted; Suspend route + `/ping` byte-identical
    - Phase 7 rate-limit test still green
    - No multer reference yet
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Land archiveListing.test.js — happy + cross-state + same-state + self-mod</name>
  <files>../backend-services/carEx-services/__tests__/listing-moderation/archiveListing.test.js</files>
  <read_first>
    - ../backend-services/carEx-services/__tests__/listing-moderation/suspendListing.test.js (post-Plan-08-02 state) — sibling shape to mirror
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md (§ "10. archiveListing.test.js")
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md (D-16 archiveListing test catalog)
  </read_first>
  <behavior>
    - ≥ 5 tests green:
      1. happy path active→archived: audit row has action='archive', fromStatus='active', toStatus='archived', reasonCategory='inactive_seller'; Car has status='archived', moderationReason='inactive_seller', moderatedBy=adminUid
      2. same-state archived→archive: throws `already_in_state`; no new audit row
      3. cross-state suspended→archive: succeeds; audit fromStatus='suspended'
      4. cross-state deleted→archive: succeeds; audit fromStatus='deleted'
      5. response shape matches D-02 thin projection (same key-set lock as Suspend test 7)
  </behavior>
  <action>
    Create `__tests__/listing-moderation/archiveListing.test.js` as a near-clone of `suspendListing.test.js` (just landed in Plan 08-02). Imports unchanged except the describe block label and the calls into `service.archiveListing`. Required substitutions across the file:
      - `service.suspendListing` → `service.archiveListing`
      - `'suspended'` → `'archived'` in seeds and assertions
      - `'suspend'` → `'archive'` in audit-row assertions
      - `'spam'` → `'inactive_seller'` in the happy-path (idiomatic Archive reason — non-punitive)
      - Keep the response-shape lock test
      - Drop the missing-reasonCategory defensive test if redundant (already exercised by Plan 08-02 service); keep if it adds confidence — author's discretion (the test catalog in D-16 lists 3 tests for archiveListing; adding the response-shape lock + a 4th cross-state brings it to 5).
    Cross-state tests (3 + 4) seed Car with `status: 'suspended'` or `'deleted'`; expect success.
    Same-state test (2) seeds with `status: 'archived'`; expects rejection + zero audit rows.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/archiveListing.test.js --silent 2>&1 | tail -10</automated>
  </verify>
  <done>
    - ≥ 5 tests green in archiveListing.test.js
    - Phase 7 + Plan 08-01 + Plan 08-02 tests still green (regression continuity)
    - Cross-state suspended→archive + deleted→archive both prove the D-B open matrix continues to hold
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Inherited unchanged from Plan 08-02 (same mount chain + same middleware composition).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 (Archive variant) | Elevation of Privilege | Archive route | mitigate | `denySelfModerationListing` in front (same as Suspend) |
| T-08-02 (Archive variant) | Tampering / Repudiation | archiveListing service | mitigate | Same `withTransaction` audit-then-Car pattern as Suspend; Plan 08-01 atomicity test continues to cover the rollback contract because `archiveListing` uses the identical sequence |
| T-08-09 (Archive variant) | Information Disclosure | Archive response | mitigate | D-02 thin projection — Task 3 test 5 locks the response key set |
</threat_model>

<verification>
1. `cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/ --silent 2>&1 | tail -5` — Phase 7 (33) + Plan 08-01 (≥17) + Plan 08-02 (≥8) + this plan (≥5) all green
2. `grep -c "router.patch('/:carId/" ../backend-services/carEx-services/src/moderation/listingRouter.js` = 2 (Suspend + Archive routes mounted; Delete/Restore/Edit not yet)
3. `grep -c "toStatus: 'archived'" ../backend-services/carEx-services/src/moderation/listingService.js` ≥ 1
4. `grep -c "action: 'archive'" ../backend-services/carEx-services/src/moderation/listingService.js` ≥ 1
</verification>

<success_criteria>
- LADM-03 endpoint live and tested
- Same canonical transaction pattern as Suspend; differences confined to target-status + action-verb substitution
- Phase 7 regression preserved; Plans 08-01 + 08-02 regression preserved
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-listing-moderation-endpoints-backend/08-03-SUMMARY.md` documenting:
- Diff against Plan 08-02 patterns (must be substitution-only: 'suspended'→'archived', 'suspend'→'archive')
- Test count delta and any flaky/quarantined tests (none expected)
</output>
