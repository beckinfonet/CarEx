---
phase: 08-admin-listing-moderation-endpoints-backend
plan: 06
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - ../backend-services/carEx-services/src/moderation/listingService.js
  - ../backend-services/carEx-services/src/moderation/listingRouter.js
  - ../backend-services/carEx-services/__tests__/listing-moderation/editListing.test.js
autonomous: true
requirements: [LADM-01]

must_haves:
  truths:
    - "PATCH /api/admin/moderation/listings/:carId (multipart/form-data) accepts the seller-PUT field set MINUS 14 system fields per D-A; returns 200 with { ok, listing, action }"
    - "Edit on a non-active listing (suspended/archived/deleted) succeeds without changing status — audit row has fromStatus === toStatus (D-A-4)"
    - "Car.lastEditedBy and Car.lastEditedAt are stamped on every successful Edit (D-A-3)"
    - "Car.moderatedBy and Car.moderatedAt are NEVER touched by Edit — those reflect last status change, not content edit (D-A-3 distinction)"
    - "Audit row stores per-field changed-only fieldDiff with { before, after } pairs (D-A-2)"
    - "Empty fieldDiff (admin submitted only no-op values) → 400 no_changes (D-06)"
    - "Unknown top-level field in body → 400 invalid_field with `fields: [...]` (D-A-1 Zod .strict())"
    - "Invalid makeId or modelId → 400 invalid_make / invalid_model (D-A) via lazy mongoose.model('VehicleMake' / 'VehicleModel') validation"
    - "imageUrls fieldDiff captures before + after URL arrays (D-A-2 example shape); new uploads merge with kept-existing per seller-PUT pattern (server.js:778-785)"
    - "Edit transactional atomicity preserved — fieldDiff + audit row + Car field-set update all commit-or-rollback together"
  artifacts:
    - path: "../backend-services/carEx-services/src/moderation/listingService.js"
      provides: "editListing function body filled — final stub replaced"
      contains: "fieldDiff"
    - path: "../backend-services/carEx-services/src/moderation/listingRouter.js"
      provides: "PATCH /:carId route with upload.array('images', 25) middleware — multer ONLY on this route (D-D-1)"
      contains: "upload.array"
    - path: "../backend-services/carEx-services/__tests__/listing-moderation/editListing.test.js"
      provides: "Edit service integration tests"
      contains: "fieldDiff"
  key_links:
    - from: "../backend-services/carEx-services/src/moderation/listingRouter.js"
      to: "../backend-services/carEx-services/src/uploads/carImages.js"
      via: "require + upload.array on Edit route"
      pattern: "upload\\.array"
    - from: "../backend-services/carEx-services/src/moderation/listingService.js"
      to: "../backend-services/carEx-services/server.js"
      via: "lazy mongoose.model('VehicleMake' / 'VehicleModel') resolves the models server.js registers at boot"
      pattern: "mongoose\\.model\\('VehicleMake'\\)"
---

<objective>
Implement LADM-01 — the Admin Edit endpoint. This is the heaviest Phase 8 plan because Edit must:
- Mount multer (`upload.array('images', 25)`) on the Edit route ONLY (D-D-1)
- Compute per-field changed-only fieldDiff with the seller-PUT image-merge semantics
- Lazy-resolve VehicleMake / VehicleModel via mongoose.model(...) for makeId/modelId validation (Pitfall 7 — these models are inlined in server.js, not in src/models/)
- Stamp lastEditedBy/lastEditedAt but NOT moderatedBy/moderatedAt (D-A-3 distinction)
- Apply to listings in ANY status (active/suspended/archived/deleted — D-A-4); audit row's fromStatus === toStatus = current status
- Reject empty diff (400 no_changes) and unknown fields (400 invalid_field with `fields: [...]`)

Plan 08-06 closes Phase 8.

Output: 3 file modifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-RESEARCH.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-VALIDATION.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-01-PLAN.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-02-PLAN.md

<interfaces>
editListingSchema (Plan 08-01) — broad whitelist `.strict()`; permissive coercion (z.coerce.number for year/price/mileage/seats/doors); existingImageUrls accepted as JSON-stringified array string

Seller-PUT image-merge pattern (server.js:778-785):
  let imageUrls = car.imageUrls || [];
  if (existingImageUrls) { try { imageUrls = JSON.parse(existingImageUrls); } catch (e) {} }
  const newUrls = req.files ? req.files.map(f => f.location) : [];
  imageUrls = [...imageUrls, ...newUrls];

Seller-PUT makeId/modelId validation pattern (server.js:787-796):
  if (makeId && modelId) {
    const makeDoc = await VehicleMake.findOne({ _id: makeId, isActive: true });
    if (!makeDoc) return 400 invalid_make;
    const modelDoc = await VehicleModel.findOne({ _id: modelId, makeId, isActive: true });
    if (!modelDoc) return 400 invalid_model;
    // re-resolve makeName/modelName from the validated docs
  }

D-A excluded system fields (must NOT appear in editListingSchema's whitelist):
  _id, sellerId, listingId, createdAt, updatedAt, listingStatus, status, moderationReason,
  moderationNote, moderatedBy, moderatedAt, lastEditedBy, lastEditedAt, __v

D-A-2 fieldDiff shape: { fieldName: { before, after }, ... } — changed-only, per-field

D-02 thin response: { listing: { _id, status, moderatedBy, moderatedAt, lastEditedBy, lastEditedAt }, action: { _id, action, fromStatus, toStatus, createdAt } }
  Note: lastEditedBy + lastEditedAt are populated on Edit responses (other handlers omit them)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill listingService.editListing — fieldDiff + image-merge + makeId/modelId validation + lastEditedBy stamp + transactional commit</name>
  <files>../backend-services/carEx-services/src/moderation/listingService.js</files>
  <read_first>
    - ../backend-services/carEx-services/src/moderation/listingService.js (post-Plan-08-05 state — Suspend/Archive/Delete/Restore implemented; editListing is the last stub)
    - ../backend-services/carEx-services/src/moderation/service.js (lines 438-530 — v1.0 editProfile canonical fieldDiff + changeSet computation pattern; lines 307-320 — getProfileModel lazy-resolve pattern to mirror for getVehicleModels)
    - ../backend-services/carEx-services/server.js (lines 763-845 — seller PUT — exact image-merge + makeId/modelId validation source to mirror; lines 99-100 — VehicleMake/VehicleModel inline registration, source of the lazy-model requirement)
    - ../backend-services/carEx-services/src/moderation/listingSchemas.js (Plan 08-01) — editListingSchema field set Phase 8 admin can change
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md (§ "2. listingService.js" canonical Edit handler shape + § "Pattern E" lazy mongoose.model; § "Example 5" getVehicleModels excerpt; § "Example 6" image-merge excerpt)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md (D-A whitelist, D-A-1 zod strict, D-A-2 fieldDiff shape, D-A-3 lastEditedBy stamp + moderatedBy untouched, D-A-4 works on any status, D-D image multipart, D-D-3 imageUrls diff URL-arrays-only)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-RESEARCH.md (§ Pitfall 7 lazy model resolution, § Pitfall 8 middleware ordering for non-active listings, § Pitfall 9 pre-txn read)
  </read_first>
  <behavior>
    - `editListing({ adminUid, adminEmail, carId, fields, uploadedFiles })`:
      1. Defensive arg check: missing adminUid/adminEmail/carId → ListingServiceError('invalid_payload')
      2. Pre-transaction read with both bypass options; throw listing_not_found on null. **Edit applies to ANY status** (D-A-4) — no same-state check, no not_moderated check
      3. If `fields.makeId` is set: validate via `VehicleMake.findOne({ _id: makeId, isActive: true })` lazy-resolved (Pitfall 7); on null throw `invalid_make`. If `fields.modelId` is set: validate `VehicleModel.findOne({ _id: modelId, makeId, isActive: true })`; on null throw `invalid_model`. Captures `makeName`/`modelName` for the changeSet (server.js:792-795 mirror)
      4. Compute image-merge per server.js:778-785: existing = current.imageUrls || []; if fields.existingImageUrls present → JSON.parse (try/catch swallow on parse fail = treat as no change); new URLs from uploadedFiles.map(f => f.location); merged = [...existing-or-kept, ...new]
      5. Compute fieldDiff: for each whitelisted submitted key, compare `current[key]` vs `fields[key]` (with appropriate coercion for the 5 numeric fields and the knownIssues split-string); record `{ before, after }` only when they differ. For imageUrls, compare `current.imageUrls || []` (before) vs merged (after) with JSON.stringify equality (server.js shape). For makeId changes, ALSO record `makeId`, `modelId`, `makeName`, `modelName` in fieldDiff when re-resolved
      6. If fieldDiff is empty (`Object.keys(fieldDiff).length === 0`) → throw `no_changes` (D-06)
      7. Inside withTransaction: insert audit row with action:'edit', fromStatus:current.status, toStatus:current.status (= current — Edit does NOT transition state per D-A-4), reasonCategory:null, reasonNote:null, fieldDiff (the populated object — Mixed type per ListingModerationAction.js:77-81); array form mandatory
      8. Car.updateOne({_id: carId}, {$set: { ...changeSet, lastEditedBy: adminUid, lastEditedAt: new Date() }}, { session }) — changeSet is the same set of keys whose `after` values appear in fieldDiff (D-A-3: stamp lastEditedBy/lastEditedAt; do NOT touch moderatedBy/moderatedAt)
      9. Return D-02 thin projection: { listing: { _id, status: current.status, moderatedBy: current.moderatedBy, moderatedAt: current.moderatedAt, lastEditedBy: adminUid, lastEditedAt: <fresh Date> }, action: { _id, action:'edit', fromStatus: current.status, toStatus: current.status, createdAt } }
    - Unknown-field detection happens AT THE ROUTER (Zod .strict() rejects unknown top-level keys → router converts unrecognized_keys issue to 400 invalid_field with `fields: [...]`). The service does NOT redo the unknown-field check (D-A-1 routes the rejection to the router-layer Zod parse path); but the service may also receive a list of explicitly-disallowed fields if a future migration adds an extra check — for Phase 8 the Zod schema is the authoritative whitelist
  </behavior>
  <action>
    Replace the editListing stub in listingService.js. This is the most complex handler — break it into 4 inline helper sections separated by blank lines + brief comment headers:

    **Section A — pre-transaction read + status irrelevance** (mirrors Suspend's pre-read but DOES NOT do a same-state check):
      - `const current = await Car.findById(carId).setOptions({ includeAllListingStatuses: true, includeAllUsers: true }).lean();`
      - null → throw listing_not_found
      - D-A-4 inline comment: "Edit applies to ANY status — admin may correct content on a moderated listing without restoring it first. Audit row uses fromStatus === toStatus = current.status."

    **Section B — makeId/modelId lazy validation** (mirror server.js:787-796 with Pitfall 7 lazy resolution):
      - Add a module-scope helper `function getVehicleModels() { return { VehicleMake: mongoose.model('VehicleMake'), VehicleModel: mongoose.model('VehicleModel') }; }` placed near the top of `listingService.js` after the requires
      - Inside editListing: declare `let resolvedMakeName, resolvedModelName;`
      - If `fields.makeId` is present: const { VehicleMake } = getVehicleModels(); const makeDoc = await VehicleMake.findOne({ _id: fields.makeId, isActive: true }); if (!makeDoc) throw new ListingServiceError('invalid_make'); resolvedMakeName = makeDoc.name
      - If `fields.modelId` is present AND `fields.makeId` is present: VehicleModel.findOne({ _id: fields.modelId, makeId: fields.makeId, isActive: true }); on null throw invalid_model; resolvedModelName = modelDoc.name
      - If only modelId is present without makeId: matches the seller-PUT permissive behavior (it would only re-resolve when both are present); the Edit service treats this the same way — modelId without makeId is accepted by the Zod schema and propagates into changeSet without re-validation

    **Section C — fieldDiff + changeSet computation** (mirror service.js:438-530 editProfile pattern + server.js image-merge):
      - `const fieldDiff = {}; const changeSet = {};`
      - For each key in `fields` (excluding `existingImageUrls` which is handled separately), compute `before = current[key]; after = fields[key];` — but apply key-specific coercion / nullish-normalization so `'value' vs undefined` doesn't trigger false-positive diffs:
        - For numeric fields (year/price/mileage/seats/doors): both are already coerced to numbers by the schema; compare directly
        - For knownIssues: if `fields.knownIssues` is a string, try `JSON.parse` (mirrors server.js:799-804); fall back to `[fields.knownIssues]`; before is current.knownIssues || []; compare via JSON.stringify
        - For all other string fields: direct === comparison
      - When `before !== after` (or for arrays, when JSON.stringify(before) !== JSON.stringify(after)): `fieldDiff[key] = { before: before ?? null, after }; changeSet[key] = after;`
      - If makeId was re-resolved (Section B): `fieldDiff.makeId = { before: current.makeId, after: fields.makeId }; changeSet.makeId = fields.makeId;` (and analogously for modelId/makeName/modelName when resolved)
      - **Image-merge** (mirror server.js:778-785):
        - `const newUrls = (uploadedFiles || []).map(f => f.location);`
        - `let keptUrls = current.imageUrls || [];`
        - `if (fields.existingImageUrls !== undefined) { try { keptUrls = JSON.parse(fields.existingImageUrls); } catch (_e) { /* swallow — treat as no-change keep */ } }`
        - `const merged = [...keptUrls, ...newUrls];`
        - `if (JSON.stringify(current.imageUrls || []) !== JSON.stringify(merged)) { fieldDiff.imageUrls = { before: current.imageUrls || [], after: merged }; changeSet.imageUrls = merged; }`

    **Section D — empty-diff guard + transaction**:
      - `if (Object.keys(fieldDiff).length === 0) throw new ListingServiceError('no_changes');`
      - Open session; inside withTransaction:
        - `const [insertedAction] = await ListingModerationAction.create([{ listingId: carId, sellerUid: current.sellerId, adminUid, adminEmail, action: 'edit', fromStatus: current.status, toStatus: current.status, reasonCategory: null, reasonNote: null, fieldDiff }], { session });`
        - `const lastEditedAt = new Date();`
        - `const updated = await Car.updateOne({ _id: carId }, { $set: { ...changeSet, lastEditedBy: adminUid, lastEditedAt } }, { session });`
        - `if (updated.matchedCount !== 1) throw new ListingServiceError('listing_not_found');`
      - finally: session.endSession()
      - Return D-02 thin projection — lastEditedBy and lastEditedAt populated; moderatedBy and moderatedAt taken from `current` (Edit does NOT touch them per D-A-3)

    Inline D-A-3 comment by the Car.updateOne block: "D-A-3: Edit stamps lastEditedBy/lastEditedAt but NEVER touches moderatedBy/moderatedAt. moderatedBy reflects last status change (suspend/archive/delete/restore); lastEditedBy reflects last admin content edit."

    Do NOT touch suspendListing, archiveListing, deleteListing, or restoreListing bodies.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && node -e "const fs=require('fs');const src=fs.readFileSync('./src/moderation/listingService.js','utf8');const dm=src.match(/async function editListing[\s\S]*?\n\}/);if(!dm){console.error('FAIL: editListing body not found');process.exit(1);}const body=dm[0];const checks=[[/action: 'edit'/,'action literal'],[/toStatus: current\.status/,'fromStatus===toStatus per D-A-4'],[/fieldDiff/,'fieldDiff computed'],[/no_changes/,'empty-diff guard'],[/invalid_make/,'invalid_make code'],[/invalid_model/,'invalid_model code'],[/lastEditedBy: adminUid/,'D-A-3 stamp lastEditedBy'],[/lastEditedAt/,'D-A-3 stamp lastEditedAt'],[/getVehicleModels/,'lazy VehicleMake/VehicleModel resolution'],[/uploadedFiles/,'multipart uploadedFiles param'],[/existingImageUrls/,'existingImageUrls JSON-parse'],[/session\.withTransaction/,'withTransaction'],[/ListingModerationAction\.create\(\[/,'array-form create']];for(const [re,msg] of checks){if(!re.test(body)){console.error('FAIL: '+msg);process.exit(1);}}if(/moderatedBy: adminUid/.test(body)){console.error('FAIL: editListing must NOT set moderatedBy (D-A-3 distinction)');process.exit(1);}if(/moderatedAt: new Date/.test(body)){console.error('FAIL: editListing must NOT set moderatedAt (D-A-3 distinction)');process.exit(1);}if(/already_in_state/.test(body)){console.error('FAIL: editListing must NOT have same-state guard — Edit valid on any status (D-A-4)');process.exit(1);}console.log('OK: editListing body static checks pass — D-A-3 + D-A-4 + Pitfall 7 locked');" && node -e "const s = require('./src/moderation/listingService'); const expected=['editListing','suspendListing','archiveListing','deleteListing','restoreListing']; for (const k of expected) { if (typeof s[k] !== 'function') { console.error('FAIL: missing ' + k); process.exit(1); } } console.log('OK: all 5 service functions exported');"</automated>
  </verify>
  <done>
    - editListing body matches D-A-2 fieldDiff + D-A-3 stamp/exclude + D-A-4 any-status + image-merge + makeId/modelId lazy validation
    - Static checks prove the body does NOT set moderatedBy/moderatedAt, does NOT have a same-state guard, DOES set lastEditedBy/lastEditedAt
    - Other 4 service functions byte-identical to their prior plan commits
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add PATCH /:carId route with upload.array('images', 25) middleware + Zod unknown-key → invalid_field translation</name>
  <files>../backend-services/carEx-services/src/moderation/listingRouter.js</files>
  <read_first>
    - ../backend-services/carEx-services/src/moderation/listingRouter.js (post-Plan-08-05 state — Suspend + Archive + Delete + Restore routes mounted; multer not yet imported)
    - ../backend-services/carEx-services/src/moderation/router.js (lines 183-212 — v1.0 editProfile router shape: Zod unrecognized_keys → 400 invalid_field with `fields: [...]`)
    - ../backend-services/carEx-services/src/uploads/carImages.js (Plan 08-01) — `upload` instance to import
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md (§ "14. listingRouter.js (MODIFY)" — Imports + edit route excerpt; § Example 3 Phase 8 listingRouter additions)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md (D-D image multipart; D-D-1 multer ONLY on Edit; D-A-1 Zod .strict() unknown-key → invalid_field)
  </read_first>
  <behavior>
    - Edit route mounted: `router.patch('/:carId', upload.array('images', 25), denySelfModerationListing, async (req, res) => {...})` — multer FIRST so multipart parses before the middleware needs req.params.carId; denySelfModerationListing SECOND so the Car-fetch self-mod check fires before the handler
    - `upload` imported at top of file via `const { upload } = require('../uploads/carImages');`
    - Zod parse path: on `parsed.success === false`, scan `parsed.error.issues` for an `unrecognized_keys` issue; if found → 400 `{ error: 'invalid_field', fields: issue.keys || [] }`; otherwise → 400 `{ error: 'invalid_payload', issues: parsed.error.issues }` (v1.0 router.js:183-212 pattern)
    - On parse success: dispatch `service.editListing({ adminUid: req.admin.uid, adminEmail: req.admin.email, carId: req.params.carId, fields: parsed.data, uploadedFiles: req.files || [] })`
    - Success → 200 `{ ok: true, listing, action }`; failure → `handleListingServiceError(err, res, 'editListing')` — handler enriches `invalid_field` with `err.fields` (already wired in Plan 08-02 handler) when service throws an invalid_field too (Phase 8 reserves the option of service-layer field rejection, even though today the Zod schema does all the rejection)
    - /ping + 4 prior routes byte-identical
  </behavior>
  <action>
    Edit src/moderation/listingRouter.js. Add the `upload` import at top of file: `const { upload } = require('../uploads/carImages');` placed adjacent to the other `./listingService` / `./listingSchemas` / `./denySelfModerationListing` requires (added in Plan 08-02). Add the Edit route block BELOW the Restore route block. Block structure:
      `router.patch('/:carId', upload.array('images', 25), denySelfModerationListing, async (req, res) => { const parsed = schemas.editListingSchema.safeParse(req.body || {}); if (!parsed.success) { const unknownIssue = parsed.error.issues.find(i => i.code === 'unrecognized_keys'); if (unknownIssue) { return res.status(400).json({ error: 'invalid_field', fields: unknownIssue.keys || [] }); } return res.status(400).json({ error: 'invalid_payload', issues: parsed.error.issues }); } try { const result = await service.editListing({ adminUid: req.admin.uid, adminEmail: req.admin.email, carId: req.params.carId, fields: parsed.data, uploadedFiles: req.files || [] }); return res.json({ ok: true, listing: result.listing, action: result.action }); } catch (err) { return handleListingServiceError(err, res, 'editListing'); } });`

    KEY DETAIL: middleware order is `upload.array('images', 25)` FIRST, `denySelfModerationListing` SECOND. multer must run before any handler reads `req.body` because multer is what populates req.body from the multipart stream. The self-mod check needs `req.params.carId` (which Express populates from the route pattern before either middleware runs), not req.body, so the ordering is safe.

    No changes to KNOWN_LISTING_ERRORS, handleListingServiceError, or the prior 4 route blocks. /ping byte-identical.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && node -e "const fs=require('fs');const src=fs.readFileSync('./src/moderation/listingRouter.js','utf8');const checks=[[/require\('\.\.\/uploads\/carImages'\)/,'upload import added'],[/router\.patch\('\/:carId'/,'edit route mounted at bare /:carId'],[/upload\.array\('images', 25\)/,'multer mounted on Edit route'],[/service\.editListing/,'dispatches to editListing service'],[/schemas\.editListingSchema/,'uses edit schema'],[/'invalid_field'/,'invalid_field 400 translation'],[/'invalid_payload'/,'invalid_payload 400 fallback'],[/router\.patch\('\/:carId\/suspend'/,'suspend preserved'],[/router\.patch\('\/:carId\/archive'/,'archive preserved'],[/router\.patch\('\/:carId\/delete'/,'delete preserved'],[/router\.patch\('\/:carId\/restore'/,'restore preserved'],[/router\.get\('\/ping'/,'ping preserved']];for(const [re,msg] of checks){if(!re.test(src)){console.error('FAIL: '+msg);process.exit(1);}}const editMatch = src.match(/router\.patch\('\/:carId'[\s\S]*?\}\);/);if(!editMatch){console.error('FAIL: edit block not found');process.exit(1);}const editBody = editMatch[0];const uploadIdx = editBody.indexOf('upload.array');const middlewareIdx = editBody.indexOf('denySelfModerationListing');if(uploadIdx < 0 || middlewareIdx < 0 || uploadIdx >= middlewareIdx){console.error('FAIL: middleware order must be upload.array FIRST, denySelfModerationListing SECOND');process.exit(1);}console.log('OK: Edit route mounted correctly; middleware order verified; prior 4 routes + /ping byte-preserved');" && npx jest __tests__/listing-moderation/listingModerationRateLimiter.test.js --silent 2>&1 | tail -3</automated>
  </verify>
  <done>
    - Edit route mounted with upload.array('images', 25) BEFORE denySelfModerationListing
    - Zod unrecognized_keys → 400 invalid_field with `fields` array translation present
    - All 4 prior routes + /ping byte-identical
    - Phase 7 rate-limit test still green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Land editListing.test.js — fieldDiff + image cases + unknown-field + no-changes + any-status + makeId/modelId validation + D-A-3 stamp distinction</name>
  <files>../backend-services/carEx-services/__tests__/listing-moderation/editListing.test.js</files>
  <read_first>
    - ../backend-services/carEx-services/__tests__/listing-moderation/suspendListing.test.js (Plan 08-02) — fixture pattern, helper functions
    - ../backend-services/carEx-services/__tests__/moderation/editProfile.test.js (entire file) — v1.0 canonical fieldDiff test shape + lazy-model registration dance (lines 16-26)
    - ../backend-services/carEx-services/src/moderation/listingService.js (post-Task-1 state) — function being exercised
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md (§ "13. editListing.test.js" — full 13-test enumeration; § Pattern E lazy model)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md (D-16 editListing test catalog: per-field happy paths + image-add/remove/reorder + unknown-field + empty-diff + any-status + makeId/modelId validation)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-RESEARCH.md (§ Pitfall 7 — VehicleMake/VehicleModel lazy registration dance for tests)
  </read_first>
  <behavior>
    - ≥ 12 tests green covering D-16 catalog:
      1. Single-field text edit: fields={price:11500} on Car with price=12000 → fieldDiff={price:{before:12000,after:11500}}, changeSet applied, Car.price === 11500, other fields untouched, Car.lastEditedBy === adminUid, Car.lastEditedAt fresh Date, Car.moderatedBy NOT touched (preserved from seed)
      2. Multi-field edit: 3+ fields change; fieldDiff has all 3 entries
      3. Image-add: fields.existingImageUrls=JSON.stringify(['s3://a']) + uploadedFiles=[{location:'s3://b'}] → fieldDiff.imageUrls={before:['s3://a'],after:['s3://a','s3://b']}
      4. Image-remove: existing=['a','b'], submitted existingImageUrls=JSON.stringify(['a']), no new uploads → after=['a']
      5. Image-reorder: existing=['a','b'], submitted ['b','a'] → after=['b','a'] (order changed, fieldDiff captures it)
      6. Unknown field at service or router: skipped here (router-layer test covered separately) — service receives Zod-parsed data, so unknown fields should never reach service in production. Instead, exercise the SCHEMA's rejection: `editListingSchema.safeParse({ foo: 'bar' }).success === false` and the failed-issue carries `keys: ['foo']` (this lives in Plan 08-01's schemas.test.js so optional here; if author wants belt-and-braces, add a one-test version)
      7. Empty diff → service throws no_changes: seed Car with price=12000; call editListing with fields={price:12000}; expect rejects.toThrow('no_changes')
      8. Works on suspended listing (D-A-4): seed Car with status='suspended'; call editListing; succeeds; audit row has fromStatus='suspended', toStatus='suspended', action='edit'
      9. Works on archived listing
      10. Works on deleted listing
      11. makeId/modelId validation OK: register loose VehicleMake/VehicleModel models per Pitfall 7 (use the editProfile.test.js:16-26 dance — `if (!mongoose.models.VehicleMake) { mongoose.model('VehicleMake', new mongoose.Schema({}, { strict: false, collection: 'vehicle_makes' })); }`); seed one VehicleMake + one VehicleModel with `isActive: true` referencing the make; call editListing with fields.makeId and fields.modelId; success; fieldDiff captures the change AND Car.makeName/modelName re-resolved per server.js:792-795 pattern
      12. Invalid makeId → 400 invalid_make: makeId points to a non-existent or isActive:false make → rejects.toThrow('invalid_make')
      13. Invalid modelId → 400 invalid_model: makeId valid, modelId points to a model that doesn't belong to that make → rejects.toThrow('invalid_model')
      14. D-A-3 lock test: seed Car with moderatedBy='original-admin', moderatedAt=<fixed Date>; call editListing with fields={price:11500}; assert post-call Car.moderatedBy === 'original-admin' (UNCHANGED) AND Car.moderatedAt === <fixed Date> (UNCHANGED) AND Car.lastEditedBy === <restoring-admin-uid> AND Car.lastEditedAt is a fresh Date >> the seed Date. This locks the D-A-3 stamp distinction at the test layer
    - Replica-set fixture mandatory; seed via Car.collection.insertOne (skips the existing pre(/^find/) hook noise)
  </behavior>
  <action>
    Create __tests__/listing-moderation/editListing.test.js. Imports + replica-set hooks identical to suspendListing.test.js (Plan 08-02). ADD the lazy-model registration dance from editProfile.test.js:16-26 BEFORE requiring the service:
      ```
      const mongoose = require('mongoose');
      if (!mongoose.models.VehicleMake) {
        mongoose.model('VehicleMake', new mongoose.Schema({}, { strict: false, collection: 'vehicle_makes' }));
      }
      if (!mongoose.models.VehicleModel) {
        mongoose.model('VehicleModel', new mongoose.Schema({}, { strict: false, collection: 'vehicle_models' }));
      }
      const VehicleMake = mongoose.model('VehicleMake');
      const VehicleModel = mongoose.model('VehicleModel');
      const service = require('../../src/moderation/listingService');
      ```
    `beforeEach` also clears VehicleMake / VehicleModel collections.

    Helper `seedCar(overrides = {})` returns an ObjectId as in Plan 08-02 with the addition of optional `moderatedBy: 'original-admin'`, `moderatedAt: <fixed Date>`, `imageUrls`, and `price` defaults.

    Implement all 14 tests as enumerated in the behavior block. For test 14 (D-A-3 lock), construct a fixed Date with `const seedDate = new Date('2025-01-01T00:00:00Z');` and assert `persisted.moderatedAt.getTime() === seedDate.getTime()`.

    For test 7 (empty-diff), include a one-line inline comment citing CONTEXT D-06: `// D-06 / D-A-2 — submitting only values equal to the current Car fields produces an empty fieldDiff → 400 no_changes`.

    For test 8/9/10 (any-status), seed the Car with the desired starting `status` field via Car.collection.insertOne; assert post-call `audit.fromStatus === <seed status> && audit.toStatus === <seed status> && audit.action === 'edit'` AND `persisted.status === <seed status>` (Edit does NOT change status per D-A-4).

    For test 11 (makeId/modelId OK), seed a VehicleMake with `_id: <ObjectId>, name: 'Toyota', slug: 'toyota', isActive: true`; seed a VehicleModel with `_id: <ObjectId>, makeId: <same>, name: 'Corolla', slug: 'corolla', isActive: true`; call editListing with the two ObjectIds; assert fieldDiff contains makeId, modelId (and makeName='Toyota', modelName='Corolla' if the service propagates them — confirm via the post-call Car.findById read).
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/editListing.test.js --silent 2>&1 | tail -15</automated>
  </verify>
  <done>
    - ≥ 12 tests green in editListing.test.js
    - D-A-3 stamp distinction test proves moderatedBy / moderatedAt are NOT touched by Edit
    - D-A-4 any-status tests prove Edit succeeds on suspended/archived/deleted listings with fromStatus === toStatus = current
    - makeId/modelId validation tests prove invalid_make and invalid_model error paths
    - Image-add, image-remove, image-reorder all proven via fieldDiff.imageUrls before/after arrays
    - Empty-diff → no_changes proven
    - Full Phase 8 test suite green: Phase 7 (33) + Plans 08-01..06 → expect ≥ 80 total tests
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → multer parsing | Multipart bytes stream through multer-S3 to S3; same boundary as the existing seller PUT |
| Multer body → editListingSchema | Zod .strict() rejects unknown top-level keys at the router |
| editListing service → mongoose.model('VehicleMake'/'VehicleModel') | Lazy resolution — server.js registers these at boot, tests register loose-schema variants before require'ing the service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 (Edit variant) | Elevation of Privilege | Edit route | mitigate | denySelfModerationListing mounted SECOND on the Edit route (multer first to parse multipart, then self-mod check); the middleware fetches Car.sellerId regardless of multipart payload |
| T-08-03 (Edit variant) | Tampering / Privilege Escalation | editListing payload | mitigate | Zod .strict() rejects every system field (status, sellerId, moderatedBy, lastEditedBy, etc.); router-layer unrecognized_keys → 400 invalid_field with `fields: [...]`; service-layer changeSet only ever contains keys whose names came through the schema parse |
| T-08-04 | Tampering | makeId/modelId fake refs | mitigate | Lazy mongoose.model('VehicleMake'/'VehicleModel').findOne validation; 400 invalid_make / invalid_model rejection before any Car.updateOne |
| T-08-07 (Edit variant) | Tampering | Orphan S3 objects on image removal | accept | D-D-4 — mirrors existing seller-PUT behavior. Orphan S3 lifecycle / sweep is deferred per CONTEXT.md `<deferred>` |
| T-08-12 | Tampering | D-A-3 distinction violation | mitigate | Task 1 static check forbids `moderatedBy: adminUid` / `moderatedAt: new Date` in editListing body; Task 3 test 14 asserts post-call invariance |
| T-08-13 | Information Disclosure | Full Car payload leak in Edit response | mitigate | D-02 thin projection; tests assert the response key set equals exactly `{_id, status, moderatedBy, moderatedAt, lastEditedBy, lastEditedAt}` for listing and `{_id, action, fromStatus, toStatus, createdAt}` for action — refactor that widens this fails Test 1's key-set assertion |
</threat_model>

<verification>
After all three tasks land:
1. `cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/ --silent 2>&1 | tail -5` — Phase 7 (33) + Plans 08-01..05 + this plan all green; total ≥ 80 tests
2. `grep -c "router.patch('/:carId" ../backend-services/carEx-services/src/moderation/listingRouter.js` = 5 (Edit + 4 transitions)
3. `grep -c "upload.array" ../backend-services/carEx-services/src/moderation/listingRouter.js` = 1 (Edit route only — D-D-1)
4. `grep -c "lastEditedBy: adminUid" ../backend-services/carEx-services/src/moderation/listingService.js` = 1 (only editListing stamps it)
5. `grep -c "moderatedBy: adminUid" ../backend-services/carEx-services/src/moderation/listingService.js` ≥ 4 (Suspend + Archive + Delete + Restore stamp it; editListing does NOT — D-A-3)
6. `grep -c "getVehicleModels" ../backend-services/carEx-services/src/moderation/listingService.js` ≥ 1 (lazy resolution helper landed)
7. Full Phase 8 suite: `cd ../backend-services/carEx-services && npm test -- __tests__/listing-moderation/ 2>&1 | tail -10` — all green
</verification>

<success_criteria>
- LADM-01 endpoint live: PATCH /api/admin/moderation/listings/:carId (multipart) accepts seller-PUT field set minus system fields
- fieldDiff captures changed-only entries; empty diff → 400 no_changes
- imageUrls before/after arrays diff correctly via the seller-PUT merge pattern
- makeId/modelId lazy validation produces invalid_make / invalid_model on bad refs
- Edit succeeds on listings in any status (active/suspended/archived/deleted) per D-A-4
- D-A-3 stamp distinction proven by direct test: lastEditedBy/lastEditedAt updated, moderatedBy/moderatedAt untouched
- Phase 8 complete: all 5 endpoints live; all 8 Wave-0 tests + 4 endpoint tests green; Phase 7 33 tests still green
- D-D-1 multer scoped to Edit route only (other 4 routes are JSON)
- D-02 thin projection enforced by response key-set assertion
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-listing-moderation-endpoints-backend/08-06-SUMMARY.md` documenting:
- editListing body sections (A pre-read, B make/model validation, C fieldDiff/image-merge, D transaction) and which lines map to which seller-PUT analog
- Test 14 D-A-3 stamp-distinction evidence (seed values for moderatedBy/moderatedAt and post-call assertions proving they didn't change)
- Final Phase 8 file inventory: 5 endpoint routes, 5 service functions, 1 schemas module, 1 middleware, 1 errors class, 8+ test files
- Phase 8 completion summary: all LADM-01..05 satisfied; full suite green; Phase 7 substrate untouched; the listingRouter `/ping` route byte-identical to its Phase 7 shape (last regression check)
</output>
