---
phase: 08-admin-listing-moderation-endpoints-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../backend-services/carEx-services/src/uploads/carImages.js
  - ../backend-services/carEx-services/server.js
  - ../backend-services/carEx-services/src/moderation/listingErrors.js
  - ../backend-services/carEx-services/src/moderation/listingSchemas.js
  - ../backend-services/carEx-services/src/moderation/denySelfModerationListing.js
  - ../backend-services/carEx-services/src/moderation/listingService.js
  - ../backend-services/carEx-services/__tests__/listing-moderation/listingSchemas.test.js
  - ../backend-services/carEx-services/__tests__/listing-moderation/denySelfModerationListing.test.js
  - ../backend-services/carEx-services/__tests__/listing-moderation/listingTransaction.atomicity.test.js
autonomous: true
requirements: [LADM-01, LADM-02, LADM-03, LADM-04, LADM-05]

must_haves:
  truths:
    - "Multer `upload` instance exported from src/uploads/carImages.js with byte-identical S3 config to server.js:43-64"
    - "server.js consumes upload via require — module-load no longer redefines S3Client or multer locally for car-images"
    - "ListingServiceError class exposes { code } so router error-mapping discriminates known codes from generic Errors"
    - "listingSchemas.js exports 5 .strict() Zod schemas plus REASON_CATEGORIES derived from Car.schema.path('moderationReason').enumValues"
    - "denySelfModerationListing middleware: admin === sellerId → 400 cannot_moderate_own_listing; not found → 404 listing_not_found; otherwise next()"
    - "listingService.js exports five named functions (editListing, suspendListing, archiveListing, deleteListing, restoreListing) — substrate-only skeletons that throw NotImplementedError until Wave 2/3 fills them"
    - "Wave 0 test files exist and import the substrate modules (compile-time wiring check); transaction atomicity test exercises a stubbable seam"
  artifacts:
    - path: "../backend-services/carEx-services/src/uploads/carImages.js"
      provides: "Shared multer-S3 upload instance + s3 client"
      exports: ["upload", "s3"]
    - path: "../backend-services/carEx-services/src/moderation/listingErrors.js"
      provides: "ListingServiceError class with .code"
      exports: ["ListingServiceError"]
    - path: "../backend-services/carEx-services/src/moderation/listingSchemas.js"
      provides: "5 Zod schemas + reasonCategoryEnum"
      exports: ["REASON_CATEGORIES", "reasonCategoryEnum", "suspendListingSchema", "archiveListingSchema", "deleteListingSchema", "restoreListingSchema", "editListingSchema"]
    - path: "../backend-services/carEx-services/src/moderation/denySelfModerationListing.js"
      provides: "Per-route self-mod guard middleware"
      exports: ["denySelfModerationListing"]
    - path: "../backend-services/carEx-services/src/moderation/listingService.js"
      provides: "Five-handler skeleton (Wave 2/3 will fill bodies)"
      exports: ["editListing", "suspendListing", "archiveListing", "deleteListing", "restoreListing"]
    - path: "../backend-services/carEx-services/__tests__/listing-moderation/listingSchemas.test.js"
      provides: "Zod schema unit tests"
      contains: ".strict()"
    - path: "../backend-services/carEx-services/__tests__/listing-moderation/denySelfModerationListing.test.js"
      provides: "Middleware unit test"
      contains: "cannot_moderate_own_listing"
    - path: "../backend-services/carEx-services/__tests__/listing-moderation/listingTransaction.atomicity.test.js"
      provides: "Transaction rollback evidence test"
      contains: "withTransaction"
  key_links:
    - from: "../backend-services/carEx-services/server.js"
      to: "../backend-services/carEx-services/src/uploads/carImages.js"
      via: "require"
      pattern: "require\\(['\\\"]\\./src/uploads/carImages['\\\"]\\)"
    - from: "../backend-services/carEx-services/src/moderation/denySelfModerationListing.js"
      to: "../backend-services/carEx-services/src/models/Car.js"
      via: "Car.findById(carId).setOptions({ includeAllListingStatuses: true, includeAllUsers: true }).select('sellerId').lean()"
      pattern: "Car\\.findById"
    - from: "../backend-services/carEx-services/src/moderation/listingSchemas.js"
      to: "../backend-services/carEx-services/src/models/Car.js"
      via: "Car.schema.path('moderationReason').enumValues"
      pattern: "moderationReason"
---

<objective>
Land the Phase 8 substrate: extract the multer-S3 `upload` instance into a shared module, define the `ListingServiceError` class, ship the 5 Zod `.strict()` schemas, ship the `denySelfModerationListing` middleware, ship the `listingService.js` skeleton with five exported async stubs, and create the 3 Wave-0 test files (schemas, middleware, transaction atomicity). Wave 2 (Suspend / Archive / Delete / Restore) and Wave 3 (Edit) consume this substrate without re-litigating any of these decisions.

Purpose: Every Wave 2/3 plan depends on this substrate; landing it as a single Wave 1 plan keeps the dependency tree clean and provides compile-time wiring evidence (each substrate module is import-targeted by at least one test or downstream plan in the same wave-1 commit).

Output: 9 new files in the backend repo + one in-place modification to `server.js`. Zero new package installs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-RESEARCH.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-VALIDATION.md
@.planning/phases/07-listing-schema-security-baseline-backend/07-VERIFICATION.md

<interfaces>
<!-- Phase 7 substrate already in place that Phase 8 consumes verbatim. -->

`Car.schema.path('moderationReason').enumValues` returns
  ['spam', 'policy_violation', 'fraud', 'inactive_seller', 'other']

`Car.schema.path('status').enumValues` returns
  ['active', 'suspended', 'archived', 'deleted']

ListingModerationAction document shape (from src/models/ListingModerationAction.js lines 38-83):
  listingId: String (required)
  sellerUid: String (required)
  adminUid: String (required)
  adminEmail: String (required)
  action: 'suspend' | 'archive' | 'delete' | 'restore' | 'edit' (required)
  fromStatus: 'active' | 'suspended' | 'archived' | 'deleted' (required)
  toStatus:   'active' | 'suspended' | 'archived' | 'deleted' (required)
  reasonCategory: enum 5-value, default null
  reasonNote: String, default null, maxlength 2000
  fieldDiff: Mixed, default null
  createdAt: Date, default Date.now

v1.0 ListingServiceError analog (src/payments/confirmBooking.js:31 — ProviderSuspendedError) uses
  class XError extends Error { constructor(...) { super(msg); this.name = 'XError'; this.<fields> = ...; } }

Mongoose pre-hook (existing Car.js:63-95) bypass option name is `includeAllUsers`. Phase 9 will add
a parallel listing-status hook that reads `includeAllListingStatuses`. Phase 8 admin reads MUST
chain BOTH options so the read is forward-compatible with Phase 9 AND bypasses the existing
seller-cascade hide when the listing's seller has been moderated.

server.js mount line 854 (LOCKED — do NOT modify in any Phase 8 plan):
  app.use('/api/admin/moderation/listings', verifyIdToken, requireAdmin, listingModerationRateLimiter, listingModerationRouter);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract multer-S3 upload to src/uploads/carImages.js and rewire server.js</name>
  <files>../backend-services/carEx-services/src/uploads/carImages.js, ../backend-services/carEx-services/server.js</files>
  <read_first>
    - ../backend-services/carEx-services/server.js (lines 1-78) — current require list + inline S3Client + multer-S3 + uploadAvatar definitions; copy the S3Client and `upload` blocks verbatim
    - ../backend-services/carEx-services/server.js (lines 763-833) — seller PUT consumer; must keep resolving `upload` after extraction
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md (§ "1. src/uploads/carImages.js (NEW — extraction)") — extraction recipe
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-RESEARCH.md (§ "Pitfall 1") — why this extraction is required (server.js does not export `upload`)
  </read_first>
  <behavior>
    - server.js's seller PUT (line 763) still resolves `upload.array('images', 25)` and uploads land in the same S3 bucket with the same `${bodyType}/${timestamp}-${originalname}` key shape
    - `require('./src/uploads/carImages')` returns `{ upload, s3 }` where `upload` is a configured multer instance and `s3` is an S3Client
    - `uploadAvatar` stays inlined in server.js (NOT extracted — different consumer, out of Phase 8 scope)
    - Phase 7 mount line at server.js:854 is byte-identical (zero diff on that line)
  </behavior>
  <action>
    Create `../backend-services/carEx-services/src/uploads/carImages.js`. Top of file requires `@aws-sdk/client-s3` (S3Client), `multer`, `multer-s3`. Construct the S3Client with `region: process.env.AWS_REGION` and `credentials: { accessKeyId: process.env.AWS_ACCESS_KEY_ID, secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY }` — verbatim from server.js:44-50. Construct `upload = multer({ storage: multerS3({ s3, bucket: process.env.AWS_BUCKET_NAME, metadata: function(req,file,cb){cb(null,{fieldName:file.fieldname});}, key: function(req,file,cb){ const folder = req.body.bodyType ? req.body.bodyType.toLowerCase() : 'misc'; cb(null, `${folder}/${Date.now().toString()}-${file.originalname}`); } }) })` — verbatim from server.js:52-64. Export with `module.exports = { upload, s3 };`.

    Edit `../backend-services/carEx-services/server.js`: add `const { upload, s3 } = require('./src/uploads/carImages');` to the top-of-file require block (place it adjacent to the other moderation requires near line 23-25). Delete the inline S3Client construction at lines 44-50 AND the inline `upload = multer({ storage: multerS3({...}) })` block at lines 52-64. Do NOT touch `uploadAvatar` at lines 66-78 (separate config, not in scope). Do NOT modify any `app.use(...)` line, the Mongo connection block, the seller PUT handler at lines 763-845, or the Phase 7 mount at line 854.

    Header comment in the new file documents: "Shared multer-S3 upload instance used by both server.js seller PUT (/api/cars/:id) and listingRouter.js admin Edit (PATCH /api/admin/moderation/listings/:carId). Single source of truth — D-D-2 / Phase 8 Pitfall 1. Bucket, region, key, and metadata functions are byte-identical to the pre-Phase-8 inline construction so seller-PUT S3 keys do not change."
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && node -e "const m = require('./src/uploads/carImages'); if (!m.upload || !m.s3) { console.error('FAIL: missing exports'); process.exit(1); } if (typeof m.upload.array !== 'function') { console.error('FAIL: upload.array not a function'); process.exit(1); } console.log('OK: upload + s3 exported, upload.array is a function');" && grep -c "require('./src/uploads/carImages')" server.js | awk '$1 == 1 { print "OK: server.js requires carImages once" } $1 != 1 { print "FAIL: expected exactly 1 require"; exit 1 }' && grep -c "const upload = multer" server.js | awk '$1 == 1 { print "OK: only uploadAvatar remains (1 multer(...) call left after removing inline car-images upload)"; exit 0 } $1 != 1 { print "FAIL: expected 1 remaining multer(...) call for uploadAvatar"; exit 1 }'</automated>
  </verify>
  <done>
    - `src/uploads/carImages.js` exists and exports `{ upload, s3 }`
    - `server.js` requires the new module once and no longer defines a local `upload` constant for car images
    - `uploadAvatar` remains in server.js (separate consumer; not in scope)
    - `npx jest __tests__/listing-moderation/listingModerationRateLimiter.test.js` still passes (Phase 7 /ping regression check survives — verified at wave merge in Task 3 below; this task does not break it because server.js's mount chain is untouched)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship ListingServiceError + listingSchemas.js + denySelfModerationListing.js + listingService.js skeleton</name>
  <files>../backend-services/carEx-services/src/moderation/listingErrors.js, ../backend-services/carEx-services/src/moderation/listingSchemas.js, ../backend-services/carEx-services/src/moderation/denySelfModerationListing.js, ../backend-services/carEx-services/src/moderation/listingService.js</files>
  <read_first>
    - ../backend-services/carEx-services/src/moderation/schemas.js (lines 1-95) — v1.0 Zod analog; mirror header comment + `.strict()` chaining + per-action schema export shape
    - ../backend-services/carEx-services/src/moderation/denySelfModeration.js (lines 1-36) — v1.0 middleware analog (UID comparison only; Phase 8 must FETCH the Car to learn sellerId)
    - ../backend-services/carEx-services/src/moderation/service.js (lines 1-21) — top-of-file comment block about array-form .create() + lazy model resolution; mirror with listing-domain rename
    - ../backend-services/carEx-services/src/payments/confirmBooking.js (lines 31-40) — class-extends-Error shape with named fields; canonical model for ListingServiceError
    - ../backend-services/carEx-services/src/models/Car.js (lines 46-52, 63-95) — `status` + audit field set + existing seller-cascade pre(/^find/) hook (must NOT be modified)
    - ../backend-services/carEx-services/src/models/ListingModerationAction.js (lines 38-83) — audit row field set
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md (§ "2. listingService.js", § "3. listingSchemas.js", § "4. denySelfModerationListing.js", § "5. listingErrors.js", § "Pattern D") — required pattern source
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md (D-04, D-09, D-10, D-12, D-A whitelist, D-C restore schema) — locked decisions
  </read_first>
  <behavior>
    - `require('./listingErrors')` returns `{ ListingServiceError }`; `new ListingServiceError('listing_not_found')` produces `err.code === 'listing_not_found'` and `err.message === 'listing_not_found'` and `err.name === 'ListingServiceError'`
    - `require('./listingSchemas')` returns `{ REASON_CATEGORIES, reasonCategoryEnum, suspendListingSchema, archiveListingSchema, deleteListingSchema, restoreListingSchema, editListingSchema }` — all 5 schemas chain `.strict()`
    - `REASON_CATEGORIES` is derived from `Car.schema.path('moderationReason').enumValues` at module load (NOT a hardcoded literal array)
    - `denySelfModerationListing` is an async Express middleware: when req.admin.uid === Car.sellerId → 400 `{ error: 'cannot_moderate_own_listing' }` + console.warn; when Car not found → 404 `{ error: 'listing_not_found' }` (does NOT collapse to the self-mod code per D-04); when admin !== sellerId → calls `next()`
    - `require('./listingService')` returns five named async exports: `editListing`, `suspendListing`, `archiveListing`, `deleteListing`, `restoreListing` — each accepts `({ adminUid, adminEmail, carId, ...payload })` and throws `new ListingServiceError('not_implemented')` (Wave 2/3 plans fill the bodies)
  </behavior>
  <action>
    Create `src/moderation/listingErrors.js`. Define `class ListingServiceError extends Error { constructor(code) { super(code); this.name = 'ListingServiceError'; this.code = code; } }`. `module.exports = { ListingServiceError };`. Header comment explains: "Used by listingService.js to throw stable error codes that listingRouter.js maps to 400 responses via KNOWN_LISTING_ERRORS (Phase 8 D-12 — cleanup over v1.0's err.message string-matching pattern)."

    Create `src/moderation/listingSchemas.js`. Require `zod` (z) and `../models/Car`. `const REASON_CATEGORIES = Car.schema.path('moderationReason').enumValues;` then `const reasonCategoryEnum = z.enum(REASON_CATEGORIES);` (D-10 enum-drift lock). `const noteField = z.string().max(2000).optional();`. Define and export:
      - `suspendListingSchema = z.object({ reasonCategory: reasonCategoryEnum, note: noteField }).strict()` — reasonCategory REQUIRED (D-14)
      - `archiveListingSchema` — same shape
      - `deleteListingSchema` — same shape
      - `restoreListingSchema = z.object({ note: noteField }).strict()` — NO reasonCategory (D-C)
      - `editListingSchema = z.object({ ... }).strict()` — broad whitelist per D-A excluding the 14 system fields (`_id`, `sellerId`, `listingId`, `createdAt`, `updatedAt`, `listingStatus`, `status`, `moderationReason`, `moderationNote`, `moderatedBy`, `moderatedAt`, `lastEditedBy`, `lastEditedAt`, `__v`). Permissive validators per D-A-1: `makeId/modelId/trimLevel/wheelbase/fuel/currency/description/bodyType/engine/transmission/drivetrain/mpg/condition/exteriorColor/interiorColor/interiorMaterial/phoneNumber/telegramUsername` all `z.string().optional()`; `year/price/mileage/seats/doors` all `z.coerce.number().int().nonnegative().optional()` (multipart strings → numbers — mirrors seller-PUT `parseInt(year)` at server.js:810); `knownIssues` is `z.union([z.string(), z.array(z.string())]).optional()` (mirrors seller-PUT `JSON.parse(knownIssues)` fallback at server.js:799-804); `existingImageUrls` is `z.string().optional()` (JSON-stringified array from multipart per server.js:779-782).
      Export `{ REASON_CATEGORIES, reasonCategoryEnum, suspendListingSchema, archiveListingSchema, deleteListingSchema, restoreListingSchema, editListingSchema }`.

    Create `src/moderation/denySelfModerationListing.js`. Require `../models/Car`. Export `async function denySelfModerationListing(req, res, next)`: defensively return `next()` if `req.params.carId` or `req.admin?.uid` is missing; in try-block call `await Car.findById(req.params.carId).setOptions({ includeAllListingStatuses: true, includeAllUsers: true }).select('sellerId').lean()` (BOTH bypass options per Phase 9 forward-compat + existing seller-cascade hook bypass); if result is null → return `res.status(404).json({ error: 'listing_not_found' })` (D-04 do-not-leak-existence); if `car.sellerId === req.admin.uid` → `console.warn('[listing-moderation] denied self-moderation attempt by ' + req.admin.uid + ' on listing ' + req.params.carId + ' (sellerId=' + car.sellerId + ') at ' + new Date().toISOString())` then `return res.status(400).json({ error: 'cannot_moderate_own_listing' })` (D-05 log-not-audit); otherwise `return next()`. Wrap in try/catch: catch → `console.error('[denySelfModerationListing] error:', err)` then `return res.status(500).json({ error: 'internal_error' })`. `module.exports = { denySelfModerationListing };`.

    Create `src/moderation/listingService.js`. Top-of-file comment block mirrors v1.0 service.js:1-14 with listing-domain rename: "Moderation service layer for listings. Each handler opens a Mongoose session and runs the audit-row insert + Car mutation inside a single session.withTransaction() so the pair is atomic. NOTE: We use ListingModerationAction.create([doc], { session }) — array form — because Mongoose's single-doc create(doc, { session }) silently ignores { session }, which would land the audit row OUTSIDE the transaction (Pitfall 2)." Requires `mongoose`, `../models/Car`, `../models/ListingModerationAction`, `./listingErrors` (`ListingServiceError`). Export five named async functions: `editListing`, `suspendListing`, `archiveListing`, `deleteListing`, `restoreListing`. Each function signature is `async function suspendListing({ adminUid, adminEmail, carId, reasonCategory, note }) { throw new ListingServiceError('not_implemented'); }` etc. (Wave 2/3 plans replace the body). `editListing({ adminUid, adminEmail, carId, fields, uploadedFiles })` and `restoreListing({ adminUid, adminEmail, carId, note })` have slightly different payload shapes per D-01. `module.exports = { editListing, suspendListing, archiveListing, deleteListing, restoreListing };`.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && node -e "const { ListingServiceError } = require('./src/moderation/listingErrors'); const e = new ListingServiceError('listing_not_found'); if (e.code !== 'listing_not_found') { console.error('FAIL: missing .code'); process.exit(1); } if (e.name !== 'ListingServiceError') { console.error('FAIL: wrong .name'); process.exit(1); } if (!(e instanceof Error)) { console.error('FAIL: not Error subclass'); process.exit(1); } console.log('OK: ListingServiceError');" && node -e "const s = require('./src/moderation/listingSchemas'); const expected = ['REASON_CATEGORIES','reasonCategoryEnum','suspendListingSchema','archiveListingSchema','deleteListingSchema','restoreListingSchema','editListingSchema']; for (const k of expected) { if (s[k] === undefined) { console.error('FAIL: missing ' + k); process.exit(1); } } if (JSON.stringify(s.REASON_CATEGORIES) !== JSON.stringify(['spam','policy_violation','fraud','inactive_seller','other'])) { console.error('FAIL: REASON_CATEGORIES drift: ' + JSON.stringify(s.REASON_CATEGORIES)); process.exit(1); } const r1 = s.suspendListingSchema.safeParse({ reasonCategory: 'spam', note: 'x', foo: 'bar' }); if (r1.success) { console.error('FAIL: .strict() did not reject unknown key'); process.exit(1); } const r2 = s.restoreListingSchema.safeParse({ reasonCategory: 'spam' }); if (r2.success) { console.error('FAIL: restore must reject reasonCategory'); process.exit(1); } console.log('OK: listingSchemas');" && node -e "const m = require('./src/moderation/denySelfModerationListing'); if (typeof m.denySelfModerationListing !== 'function') { console.error('FAIL: not exported'); process.exit(1); } if (m.denySelfModerationListing.constructor.name !== 'AsyncFunction') { console.error('FAIL: not async'); process.exit(1); } console.log('OK: denySelfModerationListing');" && node -e "const s = require('./src/moderation/listingService'); const expected = ['editListing','suspendListing','archiveListing','deleteListing','restoreListing']; for (const k of expected) { if (typeof s[k] !== 'function') { console.error('FAIL: missing ' + k); process.exit(1); } } console.log('OK: listingService skeleton');"</automated>
  </verify>
  <done>
    - All four new modules load without error
    - `ListingServiceError` has `.code`, `.name === 'ListingServiceError'`, and extends `Error`
    - `listingSchemas.REASON_CATEGORIES` is derived from Car schema and equals the 5-value array
    - All 5 schemas reject unknown top-level keys (.strict() proven)
    - `restoreListingSchema` does NOT accept `reasonCategory` (D-C)
    - `denySelfModerationListing` is an async function export
    - `listingService.js` exports five named async functions (all currently throwing `not_implemented`)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Wave-0 tests (listingSchemas.test.js, denySelfModerationListing.test.js, listingTransaction.atomicity.test.js)</name>
  <files>../backend-services/carEx-services/__tests__/listing-moderation/listingSchemas.test.js, ../backend-services/carEx-services/__tests__/listing-moderation/denySelfModerationListing.test.js, ../backend-services/carEx-services/__tests__/listing-moderation/listingTransaction.atomicity.test.js</files>
  <read_first>
    - ../backend-services/carEx-services/__tests__/moderation/schemas.test.js (entire file) — copy header + describe/test shape; replace per-schema bodies
    - ../backend-services/carEx-services/__tests__/moderation/denySelfModeration.test.js (lines 1-63) — minimal Express harness + console-spy regex assertion
    - ../backend-services/carEx-services/__tests__/moderation/suspend.test.js (lines 1-164) — replica-set fixture + beforeAll/afterAll/beforeEach + Car-equivalent insert pattern
    - ../backend-services/carEx-services/__tests__/_helpers/mongoReplSet.js — exports `startReplSet`, `stopReplSet`
    - ../backend-services/carEx-services/__tests__/moderation/editProfile.test.js (lines 16-26, 54-57) — lazy-model registration dance + Broker.collection.insertOne seed pattern (Phase 8 atomicity test uses the same direct-insert pattern for Car)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md (§ "6. listingSchemas.test.js", § "7. denySelfModerationListing.test.js", § "8. listingTransaction.atomicity.test.js") — required pattern source
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-VALIDATION.md (§ Wave 0 Requirements) — required test files
  </read_first>
  <behavior>
    - `npx jest __tests__/listing-moderation/listingSchemas.test.js` exits 0 with at least 8 tests (each schema gets happy-path + .strict() rejection coverage; reasonCategoryEnum derivation lock)
    - `npx jest __tests__/listing-moderation/denySelfModerationListing.test.js` exits 0 with 4 tests covering: self-mod → 400 + console.warn matches regex; non-self → next() called; car not found → 404 listing_not_found; no audit-row write attempted (D-05)
    - `npx jest __tests__/listing-moderation/listingTransaction.atomicity.test.js` exits 0 with 2 stub-failure tests: (a) ListingModerationAction.create throws → Car.status unchanged AND ListingModerationAction.countDocuments === 0; (b) Car.updateOne throws after audit-create → audit count === 0. Because listingService bodies are stubs in Wave 1, the atomicity test directly drives a hand-rolled `withTransaction` block in the test file that mirrors the canonical Phase 8 pattern (audit-then-Car) so the rollback evidence is captured before Wave 2 plans land
  </behavior>
  <action>
    Create `__tests__/listing-moderation/listingSchemas.test.js`. Import the listingSchemas module and Car model. Build describe blocks for each of the 5 schemas plus one block for the `REASON_CATEGORIES` derivation lock. Tests per block:
      - **suspendListingSchema:** happy `{ reasonCategory: 'spam', note: 'x' }` → success; missing `reasonCategory` → failure; unknown key `{ foo: 'bar', ... }` → failure with `unrecognized_keys` issue; note over 2000 chars → failure.
      - **archiveListingSchema, deleteListingSchema:** mirror suspend (one test per: happy, unknown-key, reasonCategory-required).
      - **restoreListingSchema:** happy `{ note: 'x' }` → success; happy `{}` (no note) → success; unknown `{ reasonCategory: 'spam' }` → failure (D-C symmetry — Restore must reject reasonCategory as an unknown key).
      - **editListingSchema:** happy `{ price: 12000, description: 'x' }` → success; permissive coercion `{ price: '12000' }` (string from multipart) → success AND `parsed.data.price === 12000`; unknown field `{ foo: 'bar' }` → failure with `unrecognized_keys` carrying `keys: ['foo']`; system-field reject `{ status: 'suspended' }` → failure with `unrecognized_keys`; `{ moderatedBy: 'admin-x' }` → failure with `unrecognized_keys`.
      - **REASON_CATEGORIES derivation lock:** assert `JSON.stringify(REASON_CATEGORIES) === JSON.stringify(Car.schema.path('moderationReason').enumValues)` (single source of truth lock for D-10).

    Create `__tests__/listing-moderation/denySelfModerationListing.test.js`. Imports: `express`, `supertest`, `mongoose`, `MongoMemoryServer` (plain — middleware does no transactions), `Car` model, and the middleware. `beforeAll` starts plain `MongoMemoryServer.create()` and `mongoose.connect()` to it; `afterAll` disconnects and stops. `beforeEach` does `await Car.deleteMany({})`. Helper `appWith(adminUid)` returns an Express app: `app.use(express.json())` then a middleware injecting `req.admin = { uid: adminUid, email: 'admin@test.local' }` then `app.patch('/:carId', denySelfModerationListing, (req, res) => res.json({ passed: true }))`. Tests:
      1. **Self-moderation rejected (400):** seed `Car.collection.insertOne({ _id: <ObjectId>, sellerId: 'admin-uid-42', status: 'active' })`; PATCH `/<carId>` with admin uid `'admin-uid-42'` → 400 `{ error: 'cannot_moderate_own_listing' }`; console.warn called with regex `/\[listing-moderation\] denied self-moderation attempt by admin-uid-42 on listing .* \(sellerId=admin-uid-42\) at \d{4}-\d{2}-\d{2}T/`; spy on `console.warn` with `jest.spyOn(console, 'warn').mockImplementation()` and restore after.
      2. **Non-self call passes (200):** seed Car with `sellerId: 'seller-x'`; PATCH with admin uid `'admin-y'` → 200 `{ passed: true }`.
      3. **Car not found → 404 listing_not_found:** PATCH with a syntactically valid but absent ObjectId → 404 `{ error: 'listing_not_found' }` (D-04 do-not-leak); verify the response body does NOT contain `'cannot_moderate_own_listing'`.
      4. **Edit-on-moderated-listing passes the middleware:** seed Car with `sellerId: 'seller-x'` AND `status: 'deleted'`; PATCH with admin uid `'admin-y'` → 200 `{ passed: true }` (D-A-4 — Edit valid on any status; middleware must not filter on `status`).

    Create `__tests__/listing-moderation/listingTransaction.atomicity.test.js`. Imports: `mongoose`, `startReplSet`/`stopReplSet` from `../_helpers/mongoReplSet`, `Car` model, `ListingModerationAction` model. `beforeAll` starts replica set + connects; `afterAll` disconnects + stops. `beforeEach` does `await Car.deleteMany({})` and `try { await ListingModerationAction.collection.drop(); } catch (_) {}`. The test file defines a local `runMockSuspend(carId, sellerId, adminUid, { failOn })` helper that opens a Mongoose session and runs `session.withTransaction(async () => { ... })` with the canonical Phase 8 sequence: (1) `await ListingModerationAction.create([{ listingId: carId, sellerUid: sellerId, adminUid, adminEmail: 'admin@test.local', action: 'suspend', fromStatus: 'active', toStatus: 'suspended', reasonCategory: 'spam', reasonNote: null }], { session })`; (2) `await Car.updateOne({ _id: carId }, { $set: { status: 'suspended' } }, { session })`. Tests:
      1. **Audit insert throws → Car unchanged AND no audit row landed:** use `jest.spyOn(ListingModerationAction, 'create').mockImplementationOnce(() => { throw new Error('simulated audit failure'); })` THEN call the helper; assert it rejects; then assert `(await Car.findById(carId).setOptions({ includeAllListingStatuses: true, includeAllUsers: true }).lean()).status === 'active'` and `await ListingModerationAction.countDocuments({ listingId: carId }) === 0`.
      2. **Car.updateOne throws after audit-create → audit row rolled back:** unmocked audit + `jest.spyOn(Car, 'updateOne').mockImplementationOnce(() => { throw new Error('simulated Car update failure'); })` THEN call the helper; assert it rejects; assert `(await Car.findById(carId).setOptions({ includeAllListingStatuses: true, includeAllUsers: true }).lean()).status === 'active'` AND `await ListingModerationAction.countDocuments({ listingId: carId }) === 0` (proves the audit insert was rolled back when Car update threw).
      3. **Positive control (happy path):** call helper without monkey-patches; assert audit count === 1 AND `Car.status === 'suspended'` (proves the transaction can actually commit when nothing throws — guards against a test harness false-negative).
    Restore spies in `afterEach` with `jest.restoreAllMocks()`.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/listingSchemas.test.js __tests__/listing-moderation/denySelfModerationListing.test.js __tests__/listing-moderation/listingTransaction.atomicity.test.js --silent 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All three test files exist and exit 0
    - Combined test count is ≥ 17 (≥10 schema + 4 middleware + 3 atomicity)
    - Phase 7's `__tests__/listing-moderation/` 33 tests still green (run `cd ../backend-services/carEx-services && npm test -- __tests__/listing-moderation/` and confirm: 33 + 17 = 50 minimum)
    - `Car.pre(/^find/)` seller-cascade hook in src/models/Car.js (lines 63-95) is byte-identical (grep `grep -c "this.getOptions().includeAllUsers" src/models/Car.js` = 1)
    - `src/moderation/listingRouter.js` is byte-identical (Wave 2/3 plans modify it — this plan does not touch it)
    - Phase 7 `/ping` route still returns `{ ok: true }` (regression check)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → `verifyIdToken` | Anonymous traffic enters the admin moderation chain; Firebase ID token must validate before any handler runs |
| `requireAdmin` → handler | Admin authority is established server-side from the AdminUser collection; never trust mobile-side `isAdmin` |
| Handler → service | Application code; trust boundary is on argument validation (Zod) |
| Service → Mongoose model writes | Application-layer append-only enforcement on `ListingModerationAction` (Phase 7 substrate guards this) |
| Multer body → S3 | Multipart file bytes stream to S3 via existing upload instance (no Phase 8 change to the boundary itself) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | `denySelfModerationListing` middleware | mitigate | Middleware fetches Car.sellerId on every request and rejects 400 `cannot_moderate_own_listing` when sellerId === admin.uid; Wave-0 test in this plan asserts both the rejection AND the next()-on-non-self path |
| T-08-02 | Tampering / Repudiation | `listingTransaction.atomicity` (canonical pattern) | mitigate | Wave-0 test in this plan exercises the audit-then-Car ordering AND both rollback paths (audit-throws, Car-throws) so Wave 2/3 implementations inherit the atomicity contract via a passing test that already constrains the sequence |
| T-08-03 | Tampering / Privilege Escalation | `editListingSchema` (.strict() whitelist) | mitigate | Zod `.strict()` rejects every system field (`status`, `sellerId`, `moderatedBy`, `lastEditedBy`, etc.) at parse time; Wave-0 test in this plan locks `{ status: 'suspended' }` and `{ moderatedBy: 'x' }` → rejection |
| T-08-05 | Tampering | `ListingModerationAction` append-only | accept | Phase 7 substrate (6 pre-hooks) protects this. Phase 8 inherits without re-mitigation. Defense-in-depth: Wave-0 atomicity test would surface any handler bug that attempts an `updateOne` on an existing audit row |
| T-08-06 | Repudiation | Rejected self-mod attempts | accept | D-05 / v1.0 D-29 — self-mod rejections use `console.warn` only, not audit. Accepted tradeoff: audit collection is reserved for successful state changes; rejected attempts dilute audit signal |
| T-08-07 | Tampering | Phase 9 hide-hook forward-compat | mitigate | All admin reads in this plan's middleware chain `setOptions({ includeAllListingStatuses: true, includeAllUsers: true })` — Phase 9's hook lands without retroactively touching Phase 8 code; existing seller-cascade hook bypass guarantees admin can moderate listings even when the seller has been moderated |
</threat_model>

<verification>
After all three tasks land:
1. `cd ../backend-services/carEx-services && node -e "console.log(require('./src/uploads/carImages').upload.array)"` prints a function (not undefined) — extraction round-trip works
2. `cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/ --silent 2>&1 | tail -5` — full Phase 7+8-substrate suite green; Phase 7 baseline = 33 tests, this plan adds ≥17 tests → expect ≥50
3. `grep -c "const upload = multer" ../backend-services/carEx-services/server.js` = 1 (uploadAvatar only — the car-images upload is now imported)
4. `grep -c "require('./src/uploads/carImages')" ../backend-services/carEx-services/server.js` = 1
5. `grep -c "module.exports" ../backend-services/carEx-services/src/moderation/listingService.js` = 1 (substrate skeleton committed; Wave 2/3 plans will fill bodies but not re-export)
6. `grep -c "ListingServiceError" ../backend-services/carEx-services/src/moderation/listingService.js` ≥ 5 (one throw per stub function — `'not_implemented'` placeholders Wave 2/3 will replace)
</verification>

<success_criteria>
- All 9 new files exist; server.js modified in place
- Wave-0 tests all green (≥ 17 new tests)
- Phase 7 regression: 33/33 still green; `/ping` byte-identical; existing seller-cascade hook untouched
- Zero changes to package.json (no new deps)
- Zero changes to `src/moderation/listingRouter.js`, `src/moderation/listingRateLimit.js`, `src/moderation/listingCapabilities.js`, `src/models/Car.js`, `src/models/ListingModerationAction.js` (Phase 7 substrate preserved)
- Phase 8 substrate ready: Wave 2 plans (Suspend / Archive / Delete / Restore) and Wave 3 plan (Edit) can import `listingService`, `listingSchemas`, `denySelfModerationListing`, `ListingServiceError`, and `upload` without any further substrate work
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-listing-moderation-endpoints-backend/08-01-SUMMARY.md` documenting:
- The exact extraction diff in server.js (line-range removed + line added)
- Which test file in the existing `__tests__/listing-moderation/` directory ended up importing each new module (compile-time wiring evidence)
- Any deviations from this plan, with rationale
- Phase 7 test count before/after this plan (must be 33 → ≥50)
- Confirmation that listingRouter.js / listingRateLimit.js / listingCapabilities.js / Car.js / ListingModerationAction.js are byte-identical (`git diff --stat` shows zero on these files)
</output>
