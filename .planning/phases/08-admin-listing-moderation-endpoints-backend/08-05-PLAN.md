---
phase: 08-admin-listing-moderation-endpoints-backend
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - ../backend-services/carEx-services/src/moderation/listingService.js
  - ../backend-services/carEx-services/src/moderation/listingRouter.js
  - ../backend-services/carEx-services/__tests__/listing-moderation/restoreListing.test.js
autonomous: true
requirements: [LADM-05]

must_haves:
  truths:
    - "PATCH /api/admin/moderation/listings/:carId/restore on a non-active listing returns 200 and Car.status flips to 'active'"
    - "Restore on already-active listing returns 400 not_moderated (NOT already_in_state — distinct code per D-03 / Pitfall 10)"
    - "Car.moderationReason and Car.moderationNote are CLEARED to null on Restore (D-C-1)"
    - "Car.moderatedBy and Car.moderatedAt ARE updated to the Restore admin and timestamp (D-C-2)"
    - "Audit row appended with action='restore', fromStatus=<prior>, toStatus='active', reasonCategory=null (Restore body has no reasonCategory per D-C)"
    - "Original transition audit rows are NEVER edited or removed — restore appends a new row, history is preserved (LADM-05)"
    - "Self-moderation rejected even on Restore (admin can't restore their own listing)"
  artifacts:
    - path: "../backend-services/carEx-services/src/moderation/listingService.js"
      provides: "restoreListing function body filled"
      contains: "toStatus: 'active'"
    - path: "../backend-services/carEx-services/src/moderation/listingRouter.js"
      provides: "PATCH /:carId/restore route"
      contains: "router.patch('/:carId/restore'"
    - path: "../backend-services/carEx-services/__tests__/listing-moderation/restoreListing.test.js"
      provides: "Restore service integration tests"
      contains: "not_moderated"
  key_links:
    - from: "../backend-services/carEx-services/src/moderation/listingRouter.js"
      to: "../backend-services/carEx-services/src/moderation/listingService.js"
      via: "service.restoreListing call"
      pattern: "service\\.restoreListing"
---

<objective>
Implement LADM-05 — the Restore endpoint. Unlike the three transition handlers, Restore:
- Accepts only `{ note?: string }` — no `reasonCategory` (D-C)
- Throws `not_moderated` (NOT `already_in_state`) when called on a listing whose status is already `'active'` — distinct code per D-03 + Pitfall 10
- CLEARS `Car.moderationReason` and `Car.moderationNote` to `null` (D-C-1) inside the transaction
- UPDATES `Car.moderatedBy` and `Car.moderatedAt` to the Restore admin + new timestamp (D-C-2)
- Writes an audit row with `reasonCategory: null` (D-C) — the live Car doc no longer has the moderation reason; the historical reason lives in the prior audit rows (LADM-05's "history never rewritten")

Purpose: Ship the 4th transition endpoint and complete the four state-transition surface. Plan 08-06 (Edit) closes Phase 8.

Output: 3 file modifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-01-PLAN.md
@.planning/phases/08-admin-listing-moderation-endpoints-backend/08-02-PLAN.md

<interfaces>
restoreListingSchema (Plan 08-01) — z.object({ note: noteField }).strict() — ONLY `note` accepted; `reasonCategory` rejected as unknown key (D-C)

D-02 thin projection — Restore returns the same shape; `listing.moderatedBy` and `listing.moderatedAt` reflect the restoring admin (not the prior moderator)

Pitfall 10: Restore handler must throw `not_moderated` (not `already_in_state`) when current.status === 'active' — distinct codes for distinct semantics

Restore appends a new audit row; the existing 6 append-only pre-hooks on ListingModerationAction (Phase 7) ensure prior rows cannot be edited
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill listingService.restoreListing with clear-on-restore semantics + not_moderated code path</name>
  <files>../backend-services/carEx-services/src/moderation/listingService.js</files>
  <read_first>
    - ../backend-services/carEx-services/src/moderation/listingService.js (post-Plan-08-04 state — Suspend + Archive + Delete implemented; Restore + Edit still stubs)
    - ../backend-services/carEx-services/src/moderation/service.js (lines 147-207 — v1.0 unsuspend canonical shape, including the reasonCategory:null audit-row clear-on-restore semantics)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md (§ "12. restoreListing.test.js" — references v1.0 unsuspend mirror)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md (D-C, D-C-1, D-C-2 — restore body shape + field-clear + moderatedBy update; D-03 — not_moderated distinct from already_in_state)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-RESEARCH.md (§ Pitfall 10 — not_moderated NOT already_in_state)
  </read_first>
  <behavior>
    - restoreListing({ adminUid, adminEmail, carId, note }):
      1. Defensive arg check: missing adminUid/adminEmail/carId → ListingServiceError('invalid_payload'). reasonCategory is NOT required for Restore (D-C)
      2. Pre-transaction read with both bypass options; throw 'listing_not_found' on null
      3. Throw ListingServiceError('not_moderated') if current.status === 'active' — NOT already_in_state (Pitfall 10 — distinct semantics)
      4. Inside withTransaction: insert audit row with action:'restore', fromStatus:current.status, toStatus:'active', reasonCategory:null (D-C), reasonNote:note ?? null, fieldDiff:null (array form)
      5. Car.updateOne($set: { status:'active', moderationReason:null, moderationNote:null, moderatedBy:adminUid, moderatedAt:new Date() }) — explicit null clear (D-C-1) AND moderatedBy/moderatedAt update (D-C-2 — Restore IS a state change)
      6. Return D-02 thin projection with status='active' and moderatedBy=adminUid (the restoring admin)
  </behavior>
  <action>
    Replace the restoreListing stub in listingService.js. Structure mirrors suspendListing from Plan 08-02 with these intentional divergences:
      - Signature: async function restoreListing({ adminUid, adminEmail, carId, note }) — no reasonCategory param
      - Defensive arg check: validates adminUid/adminEmail/carId only (not reasonCategory)
      - Same-state guard becomes not_moderated: if (current.status === 'active') throw new ListingServiceError('not_moderated'); — explicitly call this out in an inline comment that states: "Pitfall 10: Restore on already-active throws not_moderated, NOT already_in_state. Distinct codes for distinct semantics — already_in_state is the cross-action no-op (Suspend on suspended), not_moderated is the Restore-specific case."
      - Audit row sets reasonCategory:null (D-C explicit) and action:'restore', toStatus:'active'
      - Car $set has FIVE fields: status:'active', moderationReason:null (D-C-1 clear), moderationNote:null (D-C-1 clear), moderatedBy:adminUid (D-C-2 — restoring admin), moderatedAt:new Date() (D-C-2 — restore timestamp)
      - Inline comment by the $set explains D-C-1 clear-on-restore + D-C-2 update-on-restore semantics so future readers don't try to "optimize" by preserving the prior reason fields
      - Return shape unchanged (D-02 thin projection); listing.moderatedBy === adminUid (restoring admin) and listing.moderatedAt === fresh Date
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && node -e "const fs=require('fs');const src=fs.readFileSync('./src/moderation/listingService.js','utf8');const dm=src.match(/async function restoreListing[\s\S]*?\n\}/);if(!dm){console.error('FAIL: restoreListing body not found');process.exit(1);}const body=dm[0];const checks=[[/toStatus: 'active'/,'toStatus literal'],[/action: 'restore'/,'action literal'],[/reasonCategory: null/,'audit reasonCategory null'],[/not_moderated/,'not_moderated code'],[/moderationReason: null/,'D-C-1 reason clear'],[/moderationNote: null/,'D-C-1 note clear'],[/moderatedBy: adminUid/,'D-C-2 moderatedBy update'],[/session\.withTransaction/,'withTransaction'],[/ListingModerationAction\.create\(\[/,'array-form create']];for(const [re,msg] of checks){if(!re.test(body)){console.error('FAIL: '+msg);process.exit(1);}}if(/already_in_state/.test(body)){console.error('FAIL: restoreListing must NOT use already_in_state code (Pitfall 10)');process.exit(1);}console.log('OK: restoreListing body static checks pass');"</automated>
  </verify>
  <done>
    - restoreListing body matches D-C/D-C-1/D-C-2 semantics with not_moderated (not already_in_state)
    - Static grep proves the body does NOT contain already_in_state (Pitfall 10 lock)
    - Suspend/Archive/Delete bodies untouched
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add PATCH /:carId/restore route to listingRouter.js</name>
  <files>../backend-services/carEx-services/src/moderation/listingRouter.js</files>
  <read_first>
    - ../backend-services/carEx-services/src/moderation/listingRouter.js (post-Plan-08-04 state — Suspend + Archive + Delete routes mounted)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-02-PLAN.md (Task 2) — pattern source
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md (D-01 — Restore body is { note? } only, no reasonCategory)
  </read_first>
  <behavior>
    - PATCH /:carId/restore route added below Delete
    - Calls service.restoreListing with payload { adminUid, adminEmail, carId, note } — no reasonCategory passed (D-C)
    - Uses schemas.restoreListingSchema (Plan 08-01 — accepts only { note? })
    - denySelfModerationListing mounted in front (D-04 applies even on Restore — admin can't restore their own listing)
    - NO multer
  </behavior>
  <action>
    Edit src/moderation/listingRouter.js. Add the Restore route immediately below the Delete route. Block contents:
      - URL: '/:carId/restore'
      - Schema: schemas.restoreListingSchema (Plan 08-01)
      - Middleware in front: denySelfModerationListing
      - On parse failure: return res.status(400).json({ error:'invalid_payload', issues: parsed.error.issues })
      - On parse success: call service.restoreListing({ adminUid: req.admin.uid, adminEmail: req.admin.email, carId: req.params.carId, note: parsed.data.note }) — pass ONLY these four named keys; do NOT include a reasonCategory field anywhere in the dispatch object (D-C)
      - On success: res.json({ ok: true, listing: result.listing, action: result.action })
      - On error: return handleListingServiceError(err, res, 'restore')
    Key difference from Suspend/Archive/Delete: the service call passes ONLY { adminUid, adminEmail, carId, note } — no reasonCategory field. The static-check grep below locks this.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && node -e "const fs=require('fs');const src=fs.readFileSync('./src/moderation/listingRouter.js','utf8');const checks=[[/router\.patch\('\/:carId\/suspend'/,'suspend preserved'],[/router\.patch\('\/:carId\/archive'/,'archive preserved'],[/router\.patch\('\/:carId\/delete'/,'delete preserved'],[/router\.patch\('\/:carId\/restore'/,'restore added'],[/service\.restoreListing/,'dispatches to restoreListing'],[/schemas\.restoreListingSchema/,'uses restore schema'],[/router\.get\('\/ping'/,'ping preserved']];for(const [re,msg] of checks){if(!re.test(src)){console.error('FAIL: '+msg);process.exit(1);}}const rm=src.match(/router\.patch\('\/:carId\/restore'[\s\S]*?\}\);/);if(!rm){console.error('FAIL: restore block not found');process.exit(1);}if(/reasonCategory/.test(rm[0])){console.error('FAIL: restore route MUST NOT pass reasonCategory (D-C)');process.exit(1);}if(/upload\.array/.test(src)){console.error('FAIL: multer must NOT be on Restore route');process.exit(1);}console.log('OK: Restore route added; reasonCategory absent; multer absent');" && npx jest __tests__/listing-moderation/listingModerationRateLimiter.test.js --silent 2>&1 | tail -3</automated>
  </verify>
  <done>
    - Restore route mounted; Suspend + Archive + Delete + /ping byte-identical
    - reasonCategory absent from the restore dispatch block (grep gate)
    - multer absent (grep gate)
    - Phase 7 rate-limit test still green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Land restoreListing.test.js — 3 starting states + not_moderated + field-clear + history-preserved</name>
  <files>../backend-services/carEx-services/__tests__/listing-moderation/restoreListing.test.js</files>
  <read_first>
    - ../backend-services/carEx-services/__tests__/listing-moderation/suspendListing.test.js (Plan 08-02) — fixture + helper shape
    - ../backend-services/carEx-services/__tests__/moderation/unsuspend.test.js (entire file, lines 1-80+) — v1.0 canonical clear-on-restore assertion shape (reasonCategory:null on Car + reasonCategory:null on audit)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-PATTERNS.md (§ "12. restoreListing.test.js" — full test enumeration including history-preservation assertion)
    - .planning/phases/08-admin-listing-moderation-endpoints-backend/08-CONTEXT.md (D-16 restoreListing test catalog + D-C/D-C-1/D-C-2 semantic locks)
  </read_first>
  <behavior>
    - ≥ 7 tests green:
      1. happy path suspended→active: Car.status='active', Car.moderationReason=null (cleared per D-C-1), Car.moderationNote=null, Car.moderatedBy=restoringAdminUid (D-C-2), Car.moderatedAt is fresh Date; audit row has action='restore', fromStatus='suspended', toStatus='active', reasonCategory=null (D-C)
      2. happy path archived→active: same assertions, audit fromStatus='archived'
      3. happy path deleted→active: same assertions, audit fromStatus='deleted'
      4. not_moderated rejection (Pitfall 10): seed Car with status='active'; expect rejects.toThrow('not_moderated'); audit count remains whatever it was (no new audit row added)
      5. History preservation (LADM-05): seed Car with status='suspended' AND seed ONE prior ListingModerationAction row with action='suspend' (representing the original suspend); call restoreListing; assert (a) the original 'suspend' row is BYTE-IDENTICAL (same _id, same fields, same createdAt) and (b) a NEW row with action='restore' has been appended. This proves the audit chain is append-only at the test layer in addition to the schema-layer pre-hooks
      6. listing not_found
      7. Response shape D-02 thin projection lock (5+5 keys, no extras)
  </behavior>
  <action>
    Create __tests__/listing-moderation/restoreListing.test.js as a near-clone of suspendListing.test.js (Plan 08-02). Substitutions:
      - service.suspendListing → service.restoreListing
      - Service payload omits reasonCategory; passes only { adminUid, adminEmail, carId, note }
      - 'suspend' (action) → 'restore'
      - Target status assertion → 'active' (not 'suspended')
      - Add D-C-1 clear assertions: assert persisted.moderationReason === null AND persisted.moderationNote === null AFTER each happy-path call
      - Add D-C-2 update assertions: assert persisted.moderatedBy === restoringAdminUid AND persisted.moderatedAt is fresh
      - Add three separate happy-path test cases for fromStatus='suspended', 'archived', 'deleted' (loop with test.each or three explicit tests — author's discretion)

    Add a dedicated History-Preservation test:
      - Seed Car with status='suspended', moderationReason='spam', moderationNote='original note', moderatedBy='admin-original', moderatedAt=<fixed Date>
      - Seed ONE prior audit row: ListingModerationAction.collection.insertOne({ listingId:carId, sellerUid:'seller-x', adminUid:'admin-original', adminEmail:'orig@x', action:'suspend', fromStatus:'active', toStatus:'suspended', reasonCategory:'spam', reasonNote:'original note', fieldDiff:null, createdAt:<fixed Date> })
      - Capture the original row's _id (from collection.insertOne result.insertedId)
      - Call service.restoreListing({ adminUid:'admin-new', adminEmail:'new@x', carId, note:null })
      - Assert: await ListingModerationAction.countDocuments({ listingId:carId }) === 2 (original suspend + new restore)
      - Assert: the row with the captured _id still exists AND its action === 'suspend' AND its reasonCategory === 'spam' (history NOT rewritten — LADM-05)
      - Assert: a separate row exists with action === 'restore' AND reasonCategory === null

    Add not_moderated test:
      - Seed Car with status='active'
      - await expect(service.restoreListing({ adminUid:'a', adminEmail:'a@x', carId, note:null })).rejects.toThrow('not_moderated')
      - Inline comment in the test cites Pitfall 10
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/restoreListing.test.js --silent 2>&1 | tail -10</automated>
  </verify>
  <done>
    - ≥ 7 tests green in restoreListing.test.js
    - History preservation test proves the original audit row is byte-identical after Restore (LADM-05 — history never rewritten)
    - not_moderated rejection proven (NOT already_in_state)
    - Phase 7 + Plans 08-01..04 tests still green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Inherited unchanged from Plan 08-02.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 (Restore variant) | Elevation of Privilege | Restore route | mitigate | denySelfModerationListing in front — admin cannot restore their own listing |
| T-08-02 (Restore variant) | Tampering / Repudiation | restoreListing service | mitigate | Same withTransaction audit-then-Car pattern; clear-on-restore happens INSIDE the transaction so a partial commit cannot leave the Car with mismatched state vs audit row |
| T-08-05 (Restore variant) | Tampering | History rewriting | mitigate | Restore APPENDS a new audit row; Phase 7's 6 append-only pre-hooks prevent any in-handler bug from editing prior rows; Task 3 test 5 asserts the historical 'suspend' row is byte-identical after Restore |
| T-08-11 | Information Disclosure | Stale moderationReason after restore | mitigate | D-C-1 clear-on-restore ensures the LIVE Car doc reflects current state (active, no reason); the audit log preserves historical reasons for forensic queries |
</threat_model>

<verification>
1. `cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/ --silent 2>&1 | tail -5` — Phase 7 (33) + Plans 08-01..05 all green
2. `grep -c "router.patch('/:carId/" ../backend-services/carEx-services/src/moderation/listingRouter.js` = 4 (Suspend + Archive + Delete + Restore mounted; Edit not yet)
3. `grep -c "not_moderated" ../backend-services/carEx-services/src/moderation/listingService.js` ≥ 1 (Restore throws this distinct code)
4. The restore route block does NOT contain the string "reasonCategory" (grep gate from Task 2)
</verification>

<success_criteria>
- LADM-05 endpoint live and tested
- D-C / D-C-1 / D-C-2 semantics all locked: restoreListingSchema accepts only `note`; Car.moderationReason + moderationNote cleared; Car.moderatedBy + moderatedAt updated to the restoring admin
- Pitfall 10 lock: Restore-on-active throws `not_moderated`, not `already_in_state`
- History preservation: the test directly proves prior audit rows are untouched
- Phase 7 + Plans 08-01..04 regressions preserved
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-listing-moderation-endpoints-backend/08-05-SUMMARY.md` documenting:
- Exact field-clear / field-update list in the restoreListing $set (with comment-grep evidence that the inline rationale comment landed)
- History-preservation test's evidence: prior-row _id round-trip and the count=2 invariant
- Confirmation that Restore route block does NOT include reasonCategory (grep gate at zero)
</output>
