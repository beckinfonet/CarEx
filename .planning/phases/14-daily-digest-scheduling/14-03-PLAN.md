---
phase: 14-daily-digest-scheduling
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - "carEx-services/src/notifications/digest.js"
  - "carEx-services/src/models/Notification.js"
  - "carEx-services/src/notifications/__tests__/digest.test.js"
autonomous: true
requirements: [NDIG-02, NDIG-03]
must_haves:
  truths:
    - "runDigest snapshots only digestPending rows created on or before runStart"
    - "Three daily + two cap-overflow rows for one user produce exactly ONE push with count=5"
    - "A crash mid-run causes neither a double-send nor a drop"
    - "A listing hidden overnight is excluded from the count and not pushed (hide-hook re-check)"
    - "digestPending is cleared only for successfully-sent ids (per-id)"
  artifacts:
    - path: "carEx-services/src/notifications/digest.js"
      provides: "pure, directly-callable runDigest({ now, deps }) flush"
      exports: ["runDigest", "DIGEST_HOUR"]
      contains: "runDigest"
    - path: "carEx-services/src/models/Notification.js"
      provides: "digestRunId claim field on the Notification schema"
      contains: "digestRunId"
  key_links:
    - from: "carEx-services/src/notifications/digest.js runDigest"
      to: "fcm.sendDigest"
      via: "one call per uid with the surviving count"
      pattern: "sendDigest"
    - from: "runDigest hide-hook re-check"
      to: "Car.findById (plain, no bypass)"
      via: "respect-by-omission TOCTOU re-read mirroring notificationService.emit"
      pattern: "findById"
    - from: "runDigest clear step"
      to: "Notification.updateMany digestPending:false on sent ids only"
      via: "per-id clear after successful send"
      pattern: "digestPending"
---

<objective>
Build the crash-safe digest flush — the core of Phase 14 — as `carEx-services/src/notifications/digest.js` (BACKEND repo, sibling) exporting a pure, directly-callable `runDigest({ now, deps })`. Add the `digestRunId` claim field to the `Notification` model.

Purpose: NDIG-02 (the locked crash-safe contract) and NDIG-03 (one push per user) are the phase's reason to exist. The flush snapshots `createdAt <= runStart`, claims with a run marker, groups by uid, re-checks the hide-hook per watch-family row (respect-by-omission), sends ONE push per user via Plan-02's `sendDigest`, and clears `digestPending` only for successfully-sent ids — proving no double-send and no drop across a simulated mid-run crash. `runDigest` is callable WITHOUT the cron so integration tests invoke it directly (the cron registration is Plan 04).
Output: `digest.js` with `runDigest` + `DIGEST_HOUR`; `digestRunId` on the Notification schema; digest.test.js integration tests for snapshot bound, one-push-count, crash no-double-send/no-drop, and hide-hook exclusion all green.

REPO NOTE: all paths relative to the BACKEND repo `/Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services`. Commands run from that repo root.

DOUBLE-SEND HARDENING DECISION (RESEARCH A4 / Pitfall 5 — resolved here): adopt the LOCKED minimal contract — guarantee NO DROP, accept the rare post-send/pre-clear duplicate. Do NOT add a separate digestSent marker. Rationale: single-instance Railway; the window is the narrow gap between sendDigest resolving and the clear updateMany; a once-in-a-blue-moon repeat morning digest after a crash-at-08:00 is acceptable and strictly better than a second durable marker's complexity. Strict zero-duplicate is deferred (pairs naturally with NOTF2-06). State this tradeoff in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-daily-digest-scheduling/14-CONTEXT.md
@.planning/phases/14-daily-digest-scheduling/14-RESEARCH.md
@.planning/phases/14-daily-digest-scheduling/14-VALIDATION.md
@.planning/phases/14-daily-digest-scheduling/14-02-PLAN.md

<interfaces>
<!-- All in carEx-services. -->

Notification model (verified src/models/Notification.js):
- Fields: uid (String, req), kind, titleKey, bodyKey, params, data { deeplink, carId (default null), searchId (default null) }, read, channels, digestPending (Boolean, default false), dedupeKey, createdAt (Date, default Date.now).
- Indexes: {uid,createdAt:-1}, {uid,read:1}, {digestPending:1}, {dedupeKey:1}.
- module.exports = Notification; module.exports.NOTIFICATION_RETENTION_DAYS = 90.
- NO claim/run field exists today — this plan adds digestRunId.

notificationService.emit hide-hook precedent (verified src/notifications/notificationService.js:165-168):
- const visible = await Car.findById(carId);  // PLAIN — NO setOptions/bypass flags
- if (!visible || visible.status !== 'active') return [];  // suppress hidden/non-active
- File header documents a grep-gate convention: ZERO bypass-flag names allowed in the file.
- emit() sets digestPending: cadence === 'daily'; deps injected as { Car, Subscription, Notification, matchSavedSearches, fcm }.

fcm.sendDigest (Plan 02): async sendDigest({ uid, count, lang='RU', data }) -> { ok, delivered }; forwards only data.deeplink; never throws.

Test harness: __tests__/_helpers/mongoReplSet.js (startReplSet/stopReplSet); standalone MongoMemoryServer also fine (no transactions).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add digestRunId claim field to the Notification model</name>
  <read_first>
    - carEx-services/src/models/Notification.js (backend repo) — the file being modified; preserve the existing field set, indexes, and the NOTIFICATION_RETENTION_DAYS export.
  </read_first>
  <action>
    Add a durable claim field `digestRunId: { type: String, default: null }` to the Notification schema (sibling to digestPending). It distinguishes a claimed-but-not-yet-cleared row (crashed prior run) from an unclaimed one and is re-stampable by a later run. Do NOT add a digestSent marker (double-send hardening is intentionally out — see the objective decision). Keep the existing `{digestPending:1}` index (serves both the snapshot scan and the prune). Do NOT alter NOTIFICATION_RETENTION_DAYS or any existing index.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services && node -e "const N=require('./src/models/Notification'); console.log(!!N.schema.path('digestRunId'), N.NOTIFICATION_RETENTION_DAYS)"</automated>
  </verify>
  <acceptance_criteria>
    - `Notification.schema.path('digestRunId')` is defined (claim field present).
    - `NOTIFICATION_RETENTION_DAYS` still === 90 (no regression).
  </acceptance_criteria>
  <done>digestRunId on the schema; retention constant + existing indexes intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement crash-safe runDigest() flush + hide-hook re-check</name>
  <read_first>
    - carEx-services/src/notifications/notificationService.js (backend repo) — the hide-hook re-read precedent (plain Car.findById, suppress on null/non-active) and the deps-injection pattern runDigest must mirror; copy its NO-bypass-flag discipline and grep-gate convention.
    - carEx-services/src/notifications/push/fcm.js (backend repo, post-Plan-02) — sendDigest({ uid, count, lang, data }) signature and its { ok, delivered } return that gates the clear.
    - carEx-services/src/models/Notification.js (backend repo, post-Task-1) — digestPending + digestRunId + createdAt fields the flush operates on.
    - carEx-services/src/models/User.js (backend repo) — User.language (default 'RU') for render-language resolution.
    - carEx-services/__tests__/_helpers/mongoReplSet.js (backend repo) — the DB harness for the integration tests.
  </read_first>
  <behavior>
    - Snapshot: only rows with digestPending:true AND createdAt <= runStart are in the batch; a row created after runStart is excluded (belongs to tomorrow).
    - Claim: one atomic updateMany stamps digestRunId = runStart.toISOString() on the snapshot; re-runnable (a later runStart re-claims leftovers from a crashed run).
    - Group by uid; resolve User.language (default 'RU'); hide-hook re-check each row carrying data.carId via plain Car.findById, dropping rows whose Car is null or status !== 'active'; rows with data.carId === null (saved-search rows) skip the per-car re-check.
    - One sendDigest call per uid with count = surviving rows; data.deeplink = 'carex://notifications' (the Notification-Center route Plan 05 wires on mobile).
    - On sendDigest ok -> updateMany digestPending:false ($unset digestRunId) for ONLY that uid's surviving sent ids (per-id). On !ok -> leave digestPending:true (next morning re-picks; no drop).
    - Crash sim: stub sendDigest to throw/reject for user B AFTER user A is cleared -> A's rows cleared, B's still digestPending:true; a re-run sends B and does NOT re-send A.
  </behavior>
  <action>
    Create `src/notifications/digest.js` exporting `async function runDigest({ now = new Date(), deps = {} } = {})` plus a `DIGEST_HOUR = 8` constant (Plan 04 consumes DIGEST_HOUR; keep it here as the single retune point per D-01). Accept injected deps `{ Notification, Car, User, fcm }` defaulting to the mongoose models / the fcm module so tests can inject mocks. Implement: (1) SNAPSHOT+CLAIM via one updateMany matching digestPending true and createdAt $lte runStart, setting digestRunId, then read the claimed set back with `.lean()`; (2) GROUP by uid with a plain JS reduce (no $group pipeline); (3) per uid resolve language, run the hide-hook re-check using a PLAIN `Car.findById(carId)` (NEVER pass setOptions/includeAllListingStatuses/includeAllUsers — mirror notificationService.js and keep its grep-gate comment), drop non-active; (4) call `fcm.sendDigest` ONCE with uid, count = survivingIds.length, lang, and data.deeplink 'carex://notifications'; (5) on ok clear digestPending false and $unset digestRunId for ONLY survivingIds. Wrap each per-user iteration so one user's send failure never aborts the loop. Do NOT use session.withTransaction (per-doc updateMany atomicity is the design — RESEARCH A1). Do NOT register any cron here (Plan 04). Export runDigest and DIGEST_HOUR.
    Convert the NDIG-02 (crash + snapshot bound), NDIG-03 (one-push-count), and SC4 (hide-hook) todos in digest.test.js into real MongoMemoryReplSet integration tests per the behavior block, injecting a mock fcm.sendDigest.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services && npx jest src/notifications/__tests__/digest.test.js 2>&1 | tail -10 && grep -vE '^[[:space:]]*(//|[*])' src/notifications/digest.js | grep -c 'includeAllListingStatuses\|includeAllUsers\|setOptions'</automated>
  </verify>
  <acceptance_criteria>
    - `require('./src/notifications/digest').runDigest` is a function and `DIGEST_HOUR === 8` (VALIDATION SC1/NDIG-01 — directly callable without the cron).
    - Snapshot bound: a row with createdAt > runStart is NOT cleared/sent (VALIDATION SC3/NDIG-02 snapshot row).
    - One-push-count: 5 digestPending rows for one uid -> sendDigest called once with count:5 (VALIDATION SC2/NDIG-03).
    - Crash sim: A cleared + B retained on B-failure; re-run sends B, not A (VALIDATION SC3/NDIG-02 crash row) — proves no drop and no double-send for the cleared user.
    - Hide-hook: a watch-family row whose Car is now non-active is excluded from the count and not pushed (VALIDATION SC4 hide-hook).
    - Bypass-flag grep gate over non-comment lines of digest.js returns 0 (T-14-03-01).
  </acceptance_criteria>
  <done>digest.js exports runDigest + DIGEST_HOUR; flush is crash-safe (no drop, no double-send for cleared users), snapshot-bounded, one-push-per-user, hide-hook-respecting; integration tests + grep gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| digestPending rows -> push fan-out | Server-trusted claimed rows become a user-facing push; a flush bug double-sends or drops |
| Notification target carId -> hide-hook re-read | An overnight-hidden listing must not surface in the digest (TOCTOU) |
| Car.findById in digest.js | Must NOT bypass the moderation/hide hooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Information disclosure | hide-hook re-check | mitigate | Plain Car.findById with ZERO bypass flags (mirrors notificationService.emit); a grep gate over non-comment lines asserts no includeAllListingStatuses/includeAllUsers/setOptions in digest.js; non-active listing -> row dropped from the count and not pushed (SC4). |
| T-14-03-02 | Tampering / data integrity | claim/clear flush | mitigate | Snapshot bound createdAt<=runStart + per-id claim (digestRunId) + clear-only-sent-ids via per-doc-atomic updateMany (RESEARCH A1) guarantees no drop; the crash-sim test proves cleared users are not re-sent and unsent users are retained. No transaction/advisory lock (single-instance, NOTF2-06 deferred). |
| T-14-03-03 | Repudiation / double-send | post-send/pre-clear crash window | accept | Locked NDIG-02 contract requires only no-drop; the rare duplicate after a crash at exactly 08:00 is accepted (no digestSent marker) — single-instance, low-frequency, strictly better UX than a missed digest. Documented in SUMMARY. |
| T-14-03-04 | Denial of service | per-user loop | mitigate | Each per-uid iteration is wrapped; sendDigest never throws (Plan 02) and one user's failure leaves their rows pending for the next run without aborting the loop. |
</threat_model>

<verification>
- `node -e "require('./src/notifications/digest').runDigest"` is a function; DIGEST_HOUR === 8.
- digest.test.js snapshot-bound, one-push-count, crash, and hide-hook integration tests green.
- Bypass-flag grep gate over digest.js returns 0.
</verification>

<success_criteria>
- runDigest is pure and directly callable (no cron); snapshots createdAt<=runStart; claims via digestRunId.
- Exactly one sendDigest per user with the surviving count; clears digestPending only for sent ids.
- Hide-hook re-check excludes overnight-hidden listings; no drop and no double-send for cleared users across a simulated crash.
</success_criteria>

<output>
After completion, create `.planning/phases/14-daily-digest-scheduling/14-03-SUMMARY.md`.
</output>
