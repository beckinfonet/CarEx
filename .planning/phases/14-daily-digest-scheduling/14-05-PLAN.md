---
phase: 14-daily-digest-scheduling
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - "App.tsx"
  - "__tests__/digestDeeplink.test.tsx"
autonomous: true
requirements: [NDIG-03]
must_haves:
  truths:
    - "A digest push deeplink (carex://notifications) routes to the in-app Notification Center"
    - "The existing listing/:carId and search?... routing is unchanged"
    - "The deeplink whitelist widens by exactly one route (notifications), no more"
  artifacts:
    - path: "App.tsx"
      provides: "a notifications case in routeDeeplink + Notifications in the linking config; routeDeeplink exported for test"
      contains: "Notifications"
  key_links:
    - from: "App.tsx routeDeeplink"
      to: "navigationRef.navigate('Notifications')"
      via: "normalizedPath === 'notifications' branch"
      pattern: "Notifications"
---

<objective>
Wire the digest push's Notification-Center deeplink on the MOBILE side (THIS repo): teach `App.tsx`'s `routeDeeplink` to route `carex://notifications` (the value the backend digest sets as `data.deeplink` in Plan 03) to the in-app `NotificationsScreen`, and add `Notifications` to the `linking` config.

Purpose: D-03 — tapping the daily digest opens the in-app Notification Center (the natural "here's everything" destination for a multi-item bundle), NOT the single-listing `listing/:carId` deeplink. The current `routeDeeplink` whitelist honors ONLY `listing/:carId` and `search?...` (verified App.tsx:156-192); a `carex://notifications` tap currently hits the "Unknown target → ignore" branch and does nothing. This plan closes that exactly-one-route gap so the digest tap lands correctly. This is the ONLY mobile change in Phase 14.
Output: `routeDeeplink` handles `notifications` → `navigationRef.navigate('Notifications')`; `linking.config.screens.Notifications` registered; a unit test proving the route (and that the existing two targets are unchanged).

NOTE: Phase 12 already registered `NotificationsScreen` in the navigator and `Notifications: undefined` in `RootStackParamList` (verified src/types/navigation.ts:26) — no new screen, no param type change needed. The whitelist widening from 2→3 routes is deliberate and security-reviewed below (it diverges from the T-13-04-02 "do not widen" note, which predates D-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-daily-digest-scheduling/14-CONTEXT.md
@.planning/phases/14-daily-digest-scheduling/14-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- THIS mobile repo. -->

App.tsx routeDeeplink (verified App.tsx:135-196): parses a deeplink into "path?query"; for carex:// the host is the first route token (carex://notifications -> normalizedPath 'notifications'). Existing whitelist branches:
- normalizedPath matches /^listing\/([^/?#]+)/  -> navigationRef.navigate('CarDetails', { carId }).
- normalizedPath === 'search' || startsWith('search/') -> navigationRef.navigate('SearchResults', { initialQuery, initialFilters }).
- else -> ignore (whitelist-only). navigationRef is the module-level NavigationContainer ref; routeDeeplink is currently a module-private function (NOT exported).

linking (verified App.tsx:265-280): prefixes ['https://www.carexmarket.com','carex://']; config.screens = { Home:'', CarDetails:'listing/:carId', SearchResults:'search' }. Notifications is NOT present.

navigation types (verified src/types/navigation.ts:26): Notifications: undefined; NotificationSettings: undefined. NotificationsScreen already imported + registered in App.tsx (Phase 12).

PushTapRoutingEffect (App.tsx:209+) already reads remoteMessage.data.deeplink and calls routeDeeplink across all 3 app states (Phase 13) — no change needed; it will route the digest deeplink once routeDeeplink handles it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route carex://notifications to NotificationsScreen + add to linking config</name>
  <read_first>
    - App.tsx (THIS repo, lines ~135-196 and ~265-280) — the file being modified; routeDeeplink's whitelist branches and the linking.config.screens block.
    - src/types/navigation.ts (THIS repo, line ~26) — confirm Notifications: undefined already exists (no param change).
    - __tests__/App.test.tsx (THIS repo) — the existing App test pattern (default-import render) to mirror harness setup (mocks for navigationRef / @react-native-firebase/messaging).
  </read_first>
  <behavior>
    - routeDeeplink('carex://notifications') -> navigationRef.navigate('Notifications') (no params).
    - routeDeeplink('https://www.carexmarket.com/notifications') -> same Notifications navigation (https prefix normalized).
    - routeDeeplink('carex://listing/abc') still -> CarDetails { carId: 'abc' } (unchanged).
    - routeDeeplink('carex://search?make=BMW') still -> SearchResults with filters (unchanged).
    - routeDeeplink('carex://unknownroute') -> still ignored (whitelist remains closed beyond the three allowed routes).
  </behavior>
  <action>
    In App.tsx routeDeeplink, add a branch (after the listing and search branches, before the unknown-target ignore) handling `normalizedPath === 'notifications'` (also accept a trailing slash / sub-path defensively) -> `navigationRef.navigate('Notifications')` with no params. Add `Notifications: 'notifications'` to `linking.config.screens` (so both the OS-cold-start linking layer and the in-app routeDeeplink agree on the path token, matching the listing/search precedent). Do NOT widen beyond this single route — the unknown-target ignore branch stays. EXPORT `routeDeeplink` (change it from module-private to a named export) so the new behavior is unit-testable without a real device; do NOT change its signature or the existing branches. Keep the carId-reliability and PII conventions intact (the notifications route carries no params, so no id handling needed).
    Create `__tests__/digestDeeplink.test.tsx`: mock navigationRef.navigate, call the exported routeDeeplink for each behavior case above, and assert the navigate target/args. Mirror the App.test.tsx mock setup for @react-native-firebase/messaging and the navigation ref.
  </action>
  <verify>
    <automated>npx jest __tests__/digestDeeplink.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - routeDeeplink('carex://notifications') navigates to 'Notifications' (D-03 / NDIG-03 deeplink consumer) — asserted in digestDeeplink.test.tsx.
    - https form of /notifications routes identically.
    - Existing listing and search routing assertions still pass (no regression to the other two whitelist branches).
    - An unknown path (carex://unknownroute) is still ignored (whitelist closed) — T-14-05-01.
    - `linking.config.screens.Notifications === 'notifications'` (grep App.tsx).
  </acceptance_criteria>
  <done>routeDeeplink routes carex://notifications to the in-app Notification Center; linking config updated; existing routes unchanged; unit test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| push data.deeplink -> in-app navigation | An untrusted/unexpected deeplink could navigate to an unintended screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | Elevation of privilege / open-redirect | routeDeeplink whitelist | mitigate | The whitelist widens by EXACTLY one route (notifications), which navigates to a param-free, already-registered authenticated in-app screen; the unknown-target ignore branch is preserved (test asserts an unknown path is still dropped). No params are parsed from the notifications deeplink, so there is no injection surface. |
| T-14-05-02 | Information disclosure | Notifications route params | accept | The notifications route carries no params (no carId/uid/search criteria) — nothing sensitive is encoded in the deeplink; the feed itself is already uid-scoped by NotificationContext. |
</threat_model>

<verification>
- `npx jest __tests__/digestDeeplink.test.tsx` exits 0.
- All five behavior cases asserted; unknown-path still ignored.
- `linking.config.screens.Notifications` present in App.tsx.
</verification>

<success_criteria>
- carex://notifications (and its https form) routes to the in-app Notification Center via routeDeeplink.
- The existing listing/:carId and search?... routes are unchanged; the whitelist gains exactly one route.
- routeDeeplink is exported and unit-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/14-daily-digest-scheduling/14-05-SUMMARY.md`.
</output>
