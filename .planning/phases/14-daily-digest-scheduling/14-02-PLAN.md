---
phase: 14-daily-digest-scheduling
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - "carEx-services/src/notifications/push/fcm.js"
  - "carEx-services/src/notifications/__tests__/digest.test.js"
autonomous: true
requirements: [NDIG-03]
must_haves:
  truths:
    - "A digest push can be sent with the integer count interpolated into the localized title"
    - "The existing param-free send() path is unchanged (PII guarantee preserved)"
    - "sendDigest forwards ONLY data.deeplink, never PII"
  artifacts:
    - path: "carEx-services/src/notifications/push/fcm.js"
      provides: "sendDigest({ uid, count, lang, data }) sibling to send()"
      exports: ["send", "sendDigest"]
      contains: "sendDigest"
  key_links:
    - from: "carEx-services/src/notifications/push/fcm.js sendDigest"
      to: "translations.renderDigest / pluralizeRu"
      via: "renders digest_title with {count} interpolated"
      pattern: "renderDigest|digest_title"
    - from: "sendDigest"
      to: "DeviceToken fan-out + pruneToken"
      via: "reuses the existing sendEachForMulticast machinery"
      pattern: "sendEachForMulticast"
---

<objective>
Add a `sendDigest()` send path to `carEx-services/src/notifications/push/fcm.js` (BACKEND repo, sibling) that renders the count-bearing `digest_title` and fans out ONE push per user — resolving the single biggest Phase-14 risk (RESEARCH Pitfall 1): the existing `send()` strips all params via `renderGenericPush()` and CANNOT interpolate `{count}`.

Purpose: NDIG-03 requires exactly one localized push per user carrying the match count. The existing `send()` is deliberately param-free for PII safety (NPUSH-08) and must stay that way. So a sibling `sendDigest()` is the clean fork (RESEARCH Don't-Hand-Roll option 1) — it reuses the cached-admin, jittered backoff, fan-out isolation, and dead-token prune, but renders the digest copy with the count (a non-PII integer) and forwards only `data.deeplink`.
Output: `fcm.js` exports `sendDigest` alongside `send`; digest.test.js asserts it's called once per user with the right count and forwards only the deeplink.

REPO NOTE: all paths relative to the BACKEND repo `/Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services`. Commands run from that repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-daily-digest-scheduling/14-CONTEXT.md
@.planning/phases/14-daily-digest-scheduling/14-RESEARCH.md
@.planning/phases/14-daily-digest-scheduling/14-VALIDATION.md
@.planning/phases/14-daily-digest-scheduling/14-01-PLAN.md

<interfaces>
<!-- All in carEx-services. -->

fcm.js send() contract (verified src/notifications/push/fcm.js:83-157):
- async send({ uid, titleKey, title, lang='RU', data={} }): fetches DeviceToken.find({ uid }).lean() → tokens; if none returns { ok:true, delivered:0 }.
- Renders GENERIC param-free copy via renderGenericPush(categoryKey, lang) — title/body ignored beyond the category key.
- payloadData = data.deeplink ? { deeplink: String(data.deeplink) } : {} — STRIPS everything except the deeplink.
- message = { notification: { title, body }, data: payloadData }; admin = ensureInitialized(); messaging.sendEachForMulticast({ tokens, ...message }).
- Bounded retry loop (MAX_ATTEMPTS), PRUNE_CODES → pruneToken(token) (DeviceToken.deleteOne), TRANSIENT_CODES → retry, never throws.
- Returns { ok: true, delivered }. module.exports = { send }.
- Helpers in-file: pruneToken(token), ensureInitialized(), sleep, jitteredBackoff, PRUNE_CODES, TRANSIENT_CODES, MAX_ATTEMPTS.

translations.js (after Plan 01): exports renderDigest(lang, count) (or equivalent) → { title, body } with {count} interpolated and RU 3-form applied; plus pluralizeRu.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fcm.sendDigest() with count-bearing localized copy</name>
  <read_first>
    - carEx-services/src/notifications/push/fcm.js (backend repo) — the file being modified; replicate send()'s token fetch, ensureInitialized, sendEachForMulticast loop, PRUNE_CODES/pruneToken, retry/backoff, and the never-throw contract. Reuse the shared in-file helpers; do NOT duplicate them.
    - carEx-services/src/notifications/translations.js (backend repo, post-Plan-01) — renderDigest/pluralizeRu the new path calls to render digest_title with the count.
    - carEx-services/src/notifications/push/fcm.test.js (backend repo) — the firebase-admin mock harness pattern to copy for asserting sendDigest behavior.
  </read_first>
  <behavior>
    - sendDigest({ uid, count: 5, lang: 'RU', data: { deeplink: 'carex://notifications' } }) renders a title via renderDigest('RU', 5) (машин form) and calls messaging.sendEachForMulticast ONCE with all of that uid's tokens.
    - With zero device tokens for the uid → returns { ok: true, delivered: 0 } without calling send (mirrors send()).
    - payloadData forwarded contains ONLY { deeplink } — no count, no uid, no carId in data.
    - A PRUNE_CODES error on a token → pruneToken removes it; sendDigest never throws.
    - Returns { ok: boolean, delivered: number } so the caller (Plan 03) can clear ids only on ok.
  </behavior>
  <action>
    Add `async function sendDigest({ uid, count, lang = 'RU', data = {} } = {})` to fcm.js. Render the digest title/body with the Plan-01 renderDigest(lang, count) (the count is the ONLY param — a non-PII integer, so interpolating it is allowed; do NOT add make/model/price/seller). Reuse the existing token fetch (`DeviceToken.find({ uid }).lean()`), `ensureInitialized()`, the bounded `sendEachForMulticast` retry/backoff loop, and `pruneToken` for PRUNE_CODES — extract a shared internal helper if needed rather than copy-pasting the loop, but do NOT regress the existing `send()` behavior or its module.exports shape. Build `payloadData` exactly like send(): `data.deeplink ? { deeplink: String(data.deeplink) } : {}` — strip all else. Export it: `module.exports = { send, sendDigest }`. Never throw out of sendDigest; return `{ ok, delivered }`.
    Convert the NDIG-03 todo in digest.test.js into a unit test (mocking firebase-admin like fcm.test.js): seed tokens for one uid, call sendDigest({ uid, count: 5, lang: 'RU' }), assert sendEachForMulticast called once, the title is the RU машин-form for 5, and payload data has only { deeplink }.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services && npx jest src/notifications/push/fcm.test.js src/notifications/__tests__/digest.test.js 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `require('./src/notifications/push/fcm').sendDigest` is a function AND `.send` is still exported (assert both) — VALIDATION SC2/NDIG-03.
    - Existing `src/notifications/push/fcm.test.js` stays green (send() unchanged).
    - sendDigest test: one sendEachForMulticast call for a 5-count digest; rendered title is the RU машин form; payload `data` keys are exactly `['deeplink']` (no count/uid/carId) — T-14-02-01 PII assertion.
    - Zero-token uid → `{ ok: true, delivered: 0 }`, no send call.
  </acceptance_criteria>
  <done>fcm.js exports sendDigest with count-bearing copy + deeplink-only payload; send() untouched; both test files green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| digest row count → push payload | The count integer is server-computed; the push crosses to FCM/APNs → device lock screen |
| sendDigest data arg → forwarded payload | Only data.deeplink may cross; everything else must be stripped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Information disclosure | sendDigest payload | mitigate | payloadData built as `data.deeplink ? { deeplink } : {}` exactly like send(); test asserts forwarded data keys === ['deeplink']. Title carries only the integer count (no make/model/price/seller/uid) — PII guarantee identical to NPUSH-08. |
| T-14-02-02 | Denial of service | sendDigest fan-out | mitigate | Reuses send()'s bounded MAX_ATTEMPTS + jittered backoff + never-throw; a bad token is pruned, not retried unbounded; one user's failure does not abort others (caller loops per-uid in Plan 03). |
| T-14-02-03 | Spoofing | uid arg | accept | uid is supplied by the in-process digest flush from claimed Notification rows (server-trusted), never from a request body; no public route exposes sendDigest. |
</threat_model>

<verification>
- fcm.test.js (existing) green — send() unchanged.
- digest.test.js sendDigest unit test green — one push, correct RU form, deeplink-only payload.
</verification>

<success_criteria>
- fcm.js exports `sendDigest` alongside `send`.
- sendDigest renders digest_title with the count, fans out once per user, forwards only data.deeplink, never throws, returns { ok, delivered }.
</success_criteria>

<output>
After completion, create `.planning/phases/14-daily-digest-scheduling/14-02-SUMMARY.md`.
</output>
