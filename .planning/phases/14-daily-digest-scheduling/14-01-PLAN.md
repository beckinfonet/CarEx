---
phase: 14-daily-digest-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "carEx-services/package.json"
  - "carEx-services/package-lock.json"
  - "carEx-services/src/notifications/translations.js"
  - "carEx-services/src/notifications/__tests__/digest.test.js"
autonomous: true
requirements: [NDIG-04, NDIG-03]
must_haves:
  truths:
    - "node-cron@^4.2.1 is an installed backend dependency"
    - "digest_title renders a grammatically-correct RU 3-form count title and an EN plural title"
    - "The existing translations parity test passes with the new digest_* keys"
    - "A digest.test.js scaffold exists and runs (covering NDIG-01..05 + SC1..4 progressively)"
  artifacts:
    - path: "carEx-services/package.json"
      provides: "node-cron dependency declaration"
      contains: "node-cron"
    - path: "carEx-services/src/notifications/translations.js"
      provides: "digest_title (RU 3-form via render-time pluralizeRu helper) + digest_body, RU+EN parity, plus exported pluralizeRu"
      contains: "digest_title"
    - path: "carEx-services/src/notifications/__tests__/digest.test.js"
      provides: "Wave-0 test scaffold for the whole phase"
      contains: "pluralizeRu"
  key_links:
    - from: "carEx-services/src/notifications/translations.js"
      to: "pluralizeRu helper"
      via: "render-time word-form selection on a single {count} template per language"
      pattern: "pluralizeRu"
---

<objective>
Lay the Wave-0 foundation for Phase 14 in the BACKEND repo `carEx-services` (sibling repo, on `main`): install `node-cron`, add the localized `digest_title`/`digest_body` translation keys with a correct RU 3-form pluralization helper, and create the `digest.test.js` scaffold every downstream task will extend.

Purpose: Phase 14 is almost entirely backend. The cron dep, the count-bearing localized copy, and the test harness are prerequisites for the send path (Plan 02) and the flush (Plans 03/04). Per D-04 the RU title must read grammatically for every count (1/3/5/11/0…), and the existing parity test must stay green.
Output: `node-cron` in `package.json`; `digest_*` keys + `pluralizeRu` in `translations.js`; `digest.test.js` scaffold with the pluralization boundary table and the parity assertion green.

REPO NOTE: every path below is RELATIVE TO THE BACKEND REPO `/Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services`. All `npm`/`npx jest` commands run from THAT repo root, NOT this mobile repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-daily-digest-scheduling/14-CONTEXT.md
@.planning/phases/14-daily-digest-scheduling/14-RESEARCH.md
@.planning/phases/14-daily-digest-scheduling/14-VALIDATION.md

<interfaces>
<!-- All in the carEx-services sibling repo. -->

translations.js render/parity contract (verified src/notifications/translations.js):
- TRANSLATIONS = { RU: {...}, EN: {...} }; module.exports = TRANSLATIONS plus named render, renderGenericPush, formatSom, interpolate, SOM_TOKEN.
- Existing generic push keys are leaf objects: push_new_match: { title, body }, push_price_drop, push_booked, push_sold, push_back_available — RU lines 58-62, EN 87-91.
- interpolate(template, params, language) replaces {param} tokens; price-family keys go through formatSom (SOM_TOKEN), everything else String(params[key]).

Parity test contract (verified __tests__/notification-translations-parity.test.js):
- flatten(TRANSLATIONS.RU) vs flatten(TRANSLATIONS.EN): RU/EN key sets MUST be identical (toEqual on sorted keys).
- Every leaf MUST be a non-empty string.
- tokens(str) extracts sorted {param} placeholders; per key the RU and EN placeholder token sets MUST be identical.
- It also asserts push bodies contain no ruble token (/руб|₽|ruble|rouble/i) — KGS som only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install node-cron + scaffold digest.test.js</name>
  <read_first>
    - carEx-services/package.json (backend repo) — confirm node-cron is absent; see the existing test scripts and jest config block.
    - carEx-services/__tests__/_helpers/mongoReplSet.js (backend repo) — the startReplSet/stopReplSet harness the integration tests will reuse.
    - carEx-services/src/notifications/push/fcm.test.js (backend repo) — the existing mock harness pattern for stubbing firebase-admin in notification tests.
  </read_first>
  <action>
    In the BACKEND repo run `npm install node-cron@^4.2.1` (this is the one true new dependency; version 4.2.1 is the verified latest — do NOT pin v3). Confirm `node-cron` appears in `package.json` dependencies and `package-lock.json`.
    Create `src/notifications/__tests__/digest.test.js` as the Wave-0 scaffold. Wire the MongoMemoryReplSet helper in beforeAll/afterAll (standalone MongoMemoryServer is also acceptable since no withTransaction is used — do NOT add transactions). Add an outer describe block `Phase 14 daily digest` with placeholder `test.todo` entries naming each VALIDATION.md row: NDIG-01 cron gate, NDIG-04 DIGEST_HOUR/timezone, NDIG-03 one-push-count, D-04 pluralizeRu boundaries, NDIG-02 crash no-double-send, NDIG-02 snapshot bound, NDIG-05/NDOM-06 90-day prune, NDIG-05 stale-token prune, SC4 hide-hook re-check. Downstream tasks convert these todos into real assertions.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services && node -e "require('node-cron'); console.log('node-cron OK')" && npx jest src/notifications/__tests__/digest.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('node-cron')"` resolves (dependency installed) — ties to VALIDATION SC1/NDIG-01 prerequisite.
    - `package.json` dependencies include `node-cron` at `^4.2.1`.
    - `npx jest src/notifications/__tests__/digest.test.js` exits 0 (scaffold runs; test.todo entries listed).
  </acceptance_criteria>
  <done>node-cron installed; digest.test.js exists and runs green with one todo per VALIDATION row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pluralizeRu helper + digest_* translations (RU 3-form, EN plural, parity-safe)</name>
  <read_first>
    - carEx-services/src/notifications/translations.js (backend repo) — the file being modified; replicate the leaf-object shape of push_* and the render/interpolate/module.exports structure.
    - carEx-services/__tests__/notification-translations-parity.test.js (backend repo) — the parity contract (identical key sets, non-empty leaves, identical {param} token sets per key) the new keys MUST satisfy.
  </read_first>
  <behavior>
    - pluralizeRu(1, [one,few,many]) → one; pluralizeRu(2|3|4) → few; pluralizeRu(0|5|6|...|20) → many.
    - Teen exception: pluralizeRu(11|12|13|14) → many; pluralizeRu(21) → one; pluralizeRu(22|23|24) → few.
    - Rendered RU digest_title for count 1 reads grammatically (машина form), 3 → машины form, 5 → машин form, 11 → машин form, 0 → машин form.
    - Rendered EN digest_title uses simple singular for 1, plural for >1.
    - The parity test stays green: RU and EN digest_title BOTH contain a single {count} placeholder token; digest_body has matching (zero) tokens both sides.
  </behavior>
  <action>
    Add a `pluralizeRu(n, [one, few, many])` helper implementing the standard Russian rule: mod10===1 && mod100!==11 → one; mod10 in 2..4 && (mod100<12 || mod100>14) → few; else → many. Export it via `module.exports.pluralizeRu`.
    Store ONE `digest_title` template per language with a `{count}` token (do NOT store three separate RU keys — that breaks parity set-equality, per RESEARCH Pitfall 2 / A5). RU template carries `{count}` plus the RU 3-form noun resolved at render time by pluralizeRu over the forms машина/машины/машин; EN template `{count}` plus simple singular/plural. Use the plain `push_*` register (NOT the UNHINGED tier, per D-04). Add `digest_body` to RU+EN — a generic line in the existing style (RU "Откройте, чтобы посмотреть." / EN "Open to take a look.") with no {param} tokens. Provide a `renderDigest(lang, count)` (or extend `render`) that applies pluralizeRu and interpolates `{count}` — this is what Plan 02's sendDigest will call. Keep the helper unit-testable: do NOT bake fcm/network into it.
    In digest.test.js convert the D-04 todo into a boundary table asserting the rendered RU form for counts {1,2,4,5,11,14,21,22,0} and EN singular/plural for {1,2}.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services && npx jest src/notifications/__tests__/digest.test.js __tests__/notification-translations-parity.test.js 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The parity test `__tests__/notification-translations-parity.test.js` exits green WITH the new digest_* keys present (VALIDATION row "parity").
    - The D-04 boundary table in digest.test.js passes for counts {1,2,4,5,11,14,21,22,0} (VALIDATION SC2/D-04).
    - `translations.js` exports `pluralizeRu` (assert via `require('../translations').pluralizeRu` typeof function).
    - `digest_title` RU and EN templates each contain exactly one `{count}` token (parity placeholder assertion green).
  </acceptance_criteria>
  <done>pluralizeRu exported and correct on all boundary cases; digest_title/digest_body present RU+EN; parity test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| count integer → rendered push title | The only dynamic value reaching push copy; must be a non-PII integer |
| translation map → parity test | Adding keys must not regress the PII/parity guarantees |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Information disclosure | digest_title interpolation | mitigate | Only the integer `{count}` is interpolated — no make/model/price/seller/uid ever enters the template; renderDigest accepts only (lang, count). Parity test's no-ruble assertion plus a digest-specific assertion that the rendered title contains no non-count param. |
| T-14-01-02 | Tampering | pluralizeRu input | accept | count is computed server-side from claimed rows (Plan 03), never client-supplied; no injection surface. Low risk. |
| T-14-01-03 | Denial of service | node-cron dependency | accept | Pure-JS, zero-dep, mature library; pinned `^4.2.1`; no network/exec surface added by install. |
</threat_model>

<verification>
- `node -e "require('node-cron')"` resolves.
- `npx jest src/notifications/__tests__/digest.test.js` and the parity test both exit 0.
- pluralizeRu boundary table green.
</verification>

<success_criteria>
- node-cron@^4.2.1 installed in the backend repo.
- digest_title (RU 3-form) + digest_body present with RU/EN parity; pluralizeRu exported and correct.
- digest.test.js scaffold runs and lists every Phase-14 VALIDATION row as a todo or (for D-04/parity) a real green assertion.
</success_criteria>

<output>
After completion, create `.planning/phases/14-daily-digest-scheduling/14-01-SUMMARY.md`.
</output>
