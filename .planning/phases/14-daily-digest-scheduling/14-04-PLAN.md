---
phase: 14-daily-digest-scheduling
plan: 04
type: execute
wave: 4
depends_on: ["14-03"]
files_modified:
  - "carEx-services/src/notifications/digest.js"
  - "carEx-services/server.js"
  - "carEx-services/src/notifications/__tests__/digest.test.js"
autonomous: true
requirements: [NDIG-01, NDIG-04, NDIG-05, NDOM-06]
must_haves:
  truths:
    - "The same run prunes notifications older than 90 days"
    - "The same run prunes stale device tokens (non-duplicative with the send-time prune)"
    - "An in-process node-cron job fires at 08:00 Asia/Bishkek via DIGEST_HOUR"
    - "The cron is gated by require.main === module so the test suite never starts it"
  artifacts:
    - path: "carEx-services/src/notifications/digest.js"
      provides: "prune() (90-day notifications + stale tokens) invoked by runDigest"
      contains: "deleteMany"
    - path: "carEx-services/server.js"
      provides: "node-cron registration inside the existing require.main === module block"
      contains: "node-cron"
  key_links:
    - from: "carEx-services/server.js require.main === module block"
      to: "cron.schedule -> runDigest"
      via: "cron expression 0 8 * * * with timezone Asia/Bishkek + noOverlap"
      pattern: "cron.schedule"
    - from: "runDigest prune step"
      to: "Notification.deleteMany / DeviceToken.deleteMany"
      via: "90-day retention cutoff + stale lastSeenAt cutoff"
      pattern: "deleteMany"
---

<objective>
Complete Phase 14 in the BACKEND repo `carEx-services` (sibling, on `main`): add the same-run retention prune to `digest.js` (90-day notifications + stale device tokens) and register the in-process `node-cron` job inside `server.js`'s existing `require.main === module` block.

Purpose: NDIG-05 / NDOM-06 require the cron run to prune dead device tokens and notifications older than 90 days; NDIG-01 / NDIG-04 require an entrypoint-gated cron firing at 08:00 Asia/Bishkek via the `DIGEST_HOUR` constant, never under Jest. The stale-token prune is the EXTRA layer beyond `fcm.send`'s send-time prune (which already deletes FCM-rejected tokens) — it targets tokens that simply went quiet, keyed on `DeviceToken.lastSeenAt` (VERIFIED refreshed on each register/refresh at router.js:315, so age is a valid liveness signal — RESEARCH A2 resolved).
Output: `prune()` invoked at the end of `runDigest`; the cron registered in `server.js` (gated, timezone Asia/Bishkek, noOverlap); tests for the 90-day prune, the stale-token prune, the gate, and the cron expression all green.

REPO NOTE: all paths relative to the BACKEND repo `/Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services`. Commands run from that repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-daily-digest-scheduling/14-CONTEXT.md
@.planning/phases/14-daily-digest-scheduling/14-RESEARCH.md
@.planning/phases/14-daily-digest-scheduling/14-VALIDATION.md
@.planning/phases/14-daily-digest-scheduling/14-03-PLAN.md

<interfaces>
<!-- All in carEx-services. -->

Notification retention (verified src/models/Notification.js): module.exports.NOTIFICATION_RETENTION_DAYS = 90; {uid,createdAt:-1} index serves the prune scan.

DeviceToken (verified src/models/DeviceToken.js): { uid, token (unique), platform, appVersion, createdAt (default Date.now), lastSeenAt (default Date.now) }; {uid:1} index.

Device-token register route (verified src/notifications/router.js:306-329): POST /device-tokens upserts on token with a $set that includes lastSeenAt: new Date() on EVERY register/refresh -> lastSeenAt IS refreshed on each app open (A2 resolved); prune on lastSeenAt age is valid.

fcm.send already prunes FCM-rejected tokens at send time (pruneToken -> DeviceToken.deleteOne) — the cron prune must NOT duplicate that; it targets stale lastSeenAt only.

server.js gate (verified server.js:1505-1514): the only `if (require.main === module) { app.listen(PORT, ...) }` block; module.exports = { app, getCarDetailHandler, createPaymentIntentHandler }; tests `require('./server')` for app WITHOUT a listener.

digest.js (Plan 03): exports runDigest({ now, deps }) + DIGEST_HOUR (=8).

node-cron v4 (installed Plan 01): cron.schedule(expr, asyncTask, { name, timezone, noOverlap }); v4 auto-starts on creation, awaits the async task; recoverMissedExecutions REMOVED in v4 -> no catch-up is the default (satisfies D-02, no code).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add prune() (90-day notifications + stale tokens) to runDigest</name>
  <read_first>
    - carEx-services/src/notifications/digest.js (backend repo, post-Plan-03) — the file being modified; add prune() and invoke it at the end of runDigest.
    - carEx-services/src/models/Notification.js (backend repo) — NOTIFICATION_RETENTION_DAYS (90) export to build the notification cutoff.
    - carEx-services/src/models/DeviceToken.js (backend repo) — lastSeenAt field for the stale-token cutoff.
    - carEx-services/src/notifications/router.js (backend repo, lines ~306-329) — confirm the register route refreshes lastSeenAt (A2) before relying on it; cite it in a code comment.
  </read_first>
  <behavior>
    - A Notification with createdAt 91 days old is deleted; one 89 days old is kept (boundary on NOTIFICATION_RETENTION_DAYS=90).
    - A DeviceToken with lastSeenAt older than TOKEN_STALE_DAYS is deleted; a fresh token is kept.
    - prune runs as part of runDigest (after the flush) and is bounded by precise date cutoffs (never an unbounded deleteMany).
    - prune failure does not throw out of runDigest (wrapped) — a prune error never blocks or re-fires the flush.
  </behavior>
  <action>
    Add a `prune(now, deps)` step to digest.js and call it at the END of runDigest (after the per-user flush). Compute the notification cutoff from NOTIFICATION_RETENTION_DAYS (90) and delete Notifications whose createdAt is before the cutoff (Notification.deleteMany with a $lt date filter). Add a TOKEN_STALE_DAYS constant (set to 90 — a token unseen for ~3 months is abandoned; FCM would also reject it on the next real send) and delete DeviceTokens whose lastSeenAt is before the stale cutoff. Use injected deps (Notification, DeviceToken) so tests can use the in-memory models. Add a code comment citing router.js:315 (lastSeenAt refreshed on register) and stating this is the EXTRA/stale layer, non-duplicative with fcm send-time pruneToken. Wrap prune so its failure is logged but never thrown out of runDigest. Both deleteMany queries MUST be bounded by a date cutoff — no unconditional delete.
    Convert the NDIG-05/NDOM-06 (90-day) and NDIG-05 (stale-token) todos in digest.test.js into integration tests with the boundary rows above.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services && npx jest src/notifications/__tests__/digest.test.js 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 90-day prune: a 91d-old Notification deleted, an 89d-old kept (VALIDATION SC4 / NDIG-05 / NDOM-06).
    - Stale-token prune: a stale-lastSeenAt DeviceToken deleted, a fresh one kept (VALIDATION SC4 / NDIG-05).
    - Both deleteMany calls carry a date-bounded filter (assert no unconditional delete) — T-14-04-01.
    - The flush integration tests from Plan 03 still pass (prune did not regress the flush).
  </acceptance_criteria>
  <done>runDigest prunes 90-day notifications + stale tokens with bounded queries; prune failure is non-fatal; prune tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Register the entrypoint-gated node-cron job in server.js</name>
  <read_first>
    - carEx-services/server.js (backend repo, lines ~1505-1514) — the file being modified; the EXISTING require.main === module block where the cron must register (alongside app.listen), and the module.exports shape tests depend on.
    - carEx-services/src/notifications/digest.js (backend repo, post-Task-1) — runDigest + DIGEST_HOUR to import.
  </read_first>
  <action>
    Inside the EXISTING `if (require.main === module)` block in server.js (next to app.listen), register the cron with cron.schedule using the expression built from DIGEST_HOUR (i.e. `0 8 * * *` when DIGEST_HOUR is 8) and options { name: 'daily-digest', timezone: 'Asia/Bishkek', noOverlap: true }, where the task wraps `await runDigest({ now: new Date() })` in try/catch and logs on failure (a digest failure must NEVER crash the process). Import cron via `require('node-cron')` and `{ runDigest, DIGEST_HOUR }` from `./src/notifications/digest`. Use the v4 signature — do NOT use `{ scheduled:false }` + `.start()` and do NOT reference recoverMissedExecutions (removed in v4; no-catch-up is the default and satisfies D-02 with zero code). The cron registration MUST stay strictly inside the require.main block so `require('./server')` under Jest starts NO scheduler (Pitfall 4). Do NOT mount any router or add any public route — this is a worker.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/backend-services/carEx-services && grep -n "cron.schedule" server.js && node -e "const s=require('./server'); console.log('app-required-no-listener', !!s.app); setTimeout(()=>process.exit(0),300)" && npx jest 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep cron.schedule server.js` matches and the match is inside the require.main block (cron NOT at module top-level) — VALIDATION SC1 / NDIG-01 (Pitfall 4).
    - `require('./server')` returns the app and exits cleanly with NO open cron handle (process exits — no scheduler started under require) — VALIDATION SC1 / NDIG-01.
    - The cron expression derives from DIGEST_HOUR (0 8 * * *) and options carry timezone 'Asia/Bishkek' — VALIDATION SC1 / NDIG-04.
    - Full backend suite `npx jest` exits 0 (no regressions; the parity, emit-guard, and digest tests all green) — VALIDATION per-wave-merge gate.
  </acceptance_criteria>
  <done>node-cron registered inside the entrypoint gate at 08:00 Asia/Bishkek via DIGEST_HOUR; require('./server') starts no scheduler; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prune deleteMany queries | Destructive deletes on live collections; an unbounded filter would destroy live data |
| cron registration vs test require | A misplaced cron would start a live scheduler (and real pushes) under Jest |
| in-process worker | No new public route is exposed — the worker is internal/in-process only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Tampering / data integrity | prune deleteMany | mitigate | Both deletes carry a precise date cutoff ($lt createdAt for 90-day notifications; $lt lastSeenAt for stale tokens); tests assert a fresh/recent row is KEPT, proving the query is bounded and never deletes live data. prune is wrapped so a failure cannot cascade. |
| T-14-04-02 | Elevation of privilege / exposure | cron under Jest | mitigate | Cron registered strictly inside `require.main === module`; a require-the-server test asserts no scheduler/open handle starts (Pitfall 4). |
| T-14-04-03 | Information disclosure / authorization | new endpoint | accept | This phase adds NO public route — the worker is in-process only; no auth surface is introduced (confirmed: no router.mount, no app.use added). |
| T-14-04-04 | Denial of service | cron overlap / failure | mitigate | noOverlap skips a fire still running; the task try/catch prevents a digest error from crashing the process; v4 no-catch-up means a slept-through fire rolls forward (D-02) rather than stampeding on boot. |
</threat_model>

<verification>
- digest.test.js prune integration tests (90-day + stale-token) green; flush tests still green.
- `grep cron.schedule server.js` matches inside the require.main block; `require('./server')` exits with no scheduler.
- Full backend `npx jest` exits 0.
</verification>

<success_criteria>
- runDigest prunes 90-day-old notifications and stale (lastSeenAt-aged) device tokens with date-bounded queries.
- node-cron fires at 08:00 Asia/Bishkek via DIGEST_HOUR, gated by require.main === module, noOverlap on.
- No public route added; full backend suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/14-daily-digest-scheduling/14-04-SUMMARY.md`.
</output>
