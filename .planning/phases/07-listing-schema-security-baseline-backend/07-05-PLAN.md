---
phase: 07-listing-schema-security-baseline-backend
plan: 05
type: execute
wave: 2
depends_on: [07-04]
files_modified:
  - ../backend-services/carEx-services/src/moderation/listingRouter.js
  - ../backend-services/carEx-services/server.js
  - ../backend-services/carEx-services/__tests__/listing-moderation/requireAdmin.listing.middleware.test.js
  - ../backend-services/carEx-services/__tests__/listing-moderation/listingModerationRateLimiter.test.js
autonomous: true
requirements: [LSEC-01, LSEC-02, LSEC-03]
must_haves:
  truths:
    - "A request to `/api/admin/moderation/listings/ping` without a valid Firebase ID token returns 401 with the canonical `{ error: 'unauthenticated', message: 'Missing or invalid idToken' }` envelope (ROADMAP success criterion #2 — first clause)"
    - "A request to `/api/admin/moderation/listings/ping` with a valid Firebase token but a non-admin role returns 403 with the canonical `{ error: 'unauthorized', message: 'Admin access required' }` envelope (ROADMAP success criterion #2 — second clause)"
    - "The 31st listing-moderation action by the same admin within 15 minutes is rejected with 429, `{ error: 'rate_limited', retryAfter }`, and a `Retry-After` header (ROADMAP success criterion #3)"
    - "Exhausting the listing-moderation bucket does NOT block the same admin's user-moderation calls — proven by a supertest that mounts BOTH `/api/admin/moderation` (v1.0) and `/api/admin/moderation/listings` (new) on one minimal app and shows the v1.0 endpoint still returns 200 after the new endpoint is 429-blocked (D-04 separate-bucket invariant)"
    - "New files (`listingRouter.js`, supertests) land under existing `src/moderation/` and `__tests__/listing-moderation/` directories — no new top-level subdirectory created (D-01 file-placement decision)"
    - "`server.js` gains exactly ONE mount line: `app.use('/api/admin/moderation/listings', verifyIdToken, requireAdmin, listingModerationRateLimiter, listingModerationRouter)` — appended after the existing `/api/admin/moderation` mount, middleware chain order is load-bearing (D-03)"
    - "`verifyIdToken` and `requireAdmin` middlewares are REUSED VERBATIM from `src/security/` (no fork, no new copy) — Phase 7 only mounts them on the new listing prefix (D-05 reuse contract)"
  artifacts:
    - path: "../backend-services/carEx-services/src/moderation/listingRouter.js"
      provides: "Express router scaffold with single GET /ping route returning { ok: true } — the LSEC-01/02/03 acceptance shell. Real handlers arrive in Phase 8."
      contains: "router.get('/ping'"
    - path: "../backend-services/carEx-services/server.js"
      provides: "One mount line: app.use('/api/admin/moderation/listings', verifyIdToken, requireAdmin, listingModerationRateLimiter, listingModerationRouter)"
      contains: "/api/admin/moderation/listings"
    - path: "../backend-services/carEx-services/__tests__/listing-moderation/requireAdmin.listing.middleware.test.js"
      provides: "5 supertest assertions covering missing/malformed/invalid Bearer → 401, non-admin → 403, valid admin → 200 + a req.admin.uid propagation spy"
      contains: "describe('/api/admin/moderation/listings/ping (LSEC-01 + LSEC-02)'"
    - path: "../backend-services/carEx-services/__tests__/listing-moderation/listingModerationRateLimiter.test.js"
      provides: "30-then-429 test + per-admin keying test + D-04 separate-bucket cross-prefix test"
      contains: "30 successful pings from admin A, the 31st returns 429"
  key_links:
    - from: "../backend-services/carEx-services/server.js"
      to: "../backend-services/carEx-services/src/moderation/listingRouter.js"
      via: "app.use(prefix, verifyIdToken, requireAdmin, listingModerationRateLimiter, listingModerationRouter) — middleware chain order is load-bearing per D-03"
      pattern: "/api/admin/moderation/listings.*listingModerationRouter"
    - from: "../backend-services/carEx-services/__tests__/listing-moderation/listingModerationRateLimiter.test.js"
      to: "../backend-services/carEx-services/src/moderation/listingRateLimit.js"
      via: "Test imports `listingModerationRateLimiter` for in-chain mount AND calls `.resetKey('listing-admin:<uid>')` in beforeEach for test isolation"
      pattern: "listingModerationRateLimiter\\.resetKey\\("
---

<objective>
Land the LSEC-01/02/03 substrate end-to-end:
1. Create the minimal `listingRouter.js` scaffold (GET `/ping`) — the route that makes the auth-chain testable BEFORE Phase 8's real endpoint handlers arrive.
2. Add ONE mount line to `server.js` wiring `verifyIdToken → requireAdmin → listingModerationRateLimiter → listingModerationRouter` at the `/api/admin/moderation/listings` prefix.
3. Land two supertest files that prove the full chain works: (a) middleware test for 401/403/200 cases, (b) rate-limit test for 30-then-429 + per-admin keying + D-04 separate-bucket invariant.

Purpose: This plan converts Plans 07-04's standalone limiter + the existing v1.0 `verifyIdToken` + `requireAdmin` middlewares into a live chain mounted at a real Express prefix, then proves the chain works via supertests. The `/ping` scaffold exists explicitly so the LSEC-01/02/03 acceptance curls can exercise the full middleware chain end-to-end before any Phase 8 endpoint handler exists.

Output: New router file, one-line server.js edit, two supertest files. After this plan lands, ROADMAP success criteria #2 and #3 are mechanically green; only criterion #1 (migration backfill — Plan 07-06) and criterion #4 (audit append-only — already green from Plan 07-02) remain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-listing-schema-security-baseline-backend/07-CONTEXT.md
@.planning/phases/07-listing-schema-security-baseline-backend/07-PATTERNS.md

<interfaces>
<!-- Source of truth: 07-CONTEXT.md D-01 + D-03 + D-04 + D-06 + 07-PATTERNS.md §2 + §8 + §11 + §12 + Pattern C + Pattern D + Pattern E. -->

Router shape (D-01 — Phase 7 ships ONLY the `/ping` scaffold; real handlers arrive in Phase 8):
- `const express = require('express');`
- `const router = express.Router();`
- `router.get('/ping', (req, res) => { res.json({ ok: true }); });`
- `module.exports = router;`

Per D-01 + 07-PATTERNS.md §2 adaptation notes: keep this file dependency-free — NO service import, NO schemas import, NO ListingModerationAction import, and CRITICALLY NO `router.use(listingModerationRateLimiter)` (that mounts at app level in server.js per D-03 — divergence from v1.0 router-level pattern).

server.js mount line (D-03) — insert IMMEDIATELY after the existing line `app.use('/api/admin/moderation', verifyIdToken, requireAdmin, moderationRouter)` at line 851:
- `app.use('/api/admin/moderation/listings', verifyIdToken, requireAdmin, listingModerationRateLimiter, listingModerationRouter);`
- Middleware order is load-bearing: `verifyIdToken` → `requireAdmin` → `listingModerationRateLimiter` → `listingModerationRouter`. Limiter must come AFTER `requireAdmin` so `req.admin.uid` is populated when `keyGenerator` runs.
- Imports at top of server.js (added near the existing `moderationRouter` import — currently at line 22):
  - `const listingModerationRouter = require('./src/moderation/listingRouter');`
  - `const { listingModerationRateLimiter } = require('./src/moderation/listingRateLimit');`

D-06 response envelopes (LOCKED — must not change so mobile interceptor keeps working):
- 401: `{ error: 'unauthenticated', message: 'Missing or invalid idToken' }`
- 403: `{ error: 'unauthorized', message: 'Admin access required' }`
- 429: `{ error: 'rate_limited', retryAfter: <integer-seconds> }` + `Retry-After: <integer>` header

Middleware test (07-PATTERNS.md §12 + Pattern E): mock `firebase-admin` BEFORE requiring any module using it; mount minimal app in `beforeAll` with `app.use('/api/admin/moderation/listings', verifyIdToken, requireAdmin, listingRouter)` — INTENTIONALLY without `listingModerationRateLimiter` for THIS file so 401/403/200 cases don't accidentally trigger a 429. The rate-limiter is exercised in the OTHER test file.

Rate-limit test (07-PATTERNS.md §11 + D-04): mount BOTH `/api/admin/moderation` (v1.0 router) AND `/api/admin/moderation/listings` (new router) on the same minimal app. The v1.0 `moderationRateLimiter` is installed INSIDE the imported v1.0 router via `router.use(moderationRateLimiter)` at router/router.js:53 — re-mounting it at app-level here would double-apply the same limiter to the v1.0 prefix (each request would tick the bucket twice and 429 at request 16 instead of 31), so the test mounts ONLY `verifyIdToken + requireAdmin + moderationRouter` for the v1.0 prefix and relies on the router-internal limiter for the v1.0 bucket. The new listing-mod chain mounts the limiter at app level per D-03 because `listingRouter.js` is dependency-free (D-01) and does NOT install its own limiter.

Then drive supertest calls through both prefixes. The separate-bucket assertion is: after admin A exhausts the listing bucket (31st call → 429), the SAME admin A's call to `/api/admin/moderation/ping` (v1.0 prefix) still returns 200 because the v1.0 bucket (`admin:<uid>`) is untouched.

Test isolation (Pattern D + 07-PATTERNS.md §11 — limiter state CARRIES OVER between tests via in-memory store): call `listingModerationRateLimiter.resetKey('listing-admin:' + uid)` AND (because the v1.0 limiter is also exercised) `moderationRateLimiter.resetKey('admin:' + uid)` in `beforeEach`. The v1.0 limiter is reachable via `require('../../src/moderation/rateLimit')` even though it's installed inside the v1.0 router — it's the SAME singleton instance because Node module cache is process-wide.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create listingRouter.js scaffold + add server.js mount line + middleware test (LSEC-01/02 chain)</name>
  <files>../backend-services/carEx-services/src/moderation/listingRouter.js, ../backend-services/carEx-services/server.js, ../backend-services/carEx-services/__tests__/listing-moderation/requireAdmin.listing.middleware.test.js</files>
  <read_first>
    <file>../backend-services/carEx-services/src/moderation/router.js (lines 1–22, 47–53, 86–90 — v1.0 router head + mount + /ping pattern per 07-PATTERNS.md §2)</file>
    <file>../backend-services/carEx-services/server.js (lines 845–855 — the existing user-mod mount line at 851 + surrounding comment block; PRESERVATION CONTRACT per 07-PATTERNS.md §8)</file>
    <file>../backend-services/carEx-services/__tests__/moderation/requireAdmin.middleware.test.js (full file — the v1.0 sibling per 07-PATTERNS.md §12)</file>
    <file>.planning/phases/07-listing-schema-security-baseline-backend/07-PATTERNS.md §2 "listingRouter.js" + §8 "server.js" + §12 "requireAdmin.listing.middleware.test.js"</file>
    <file>.planning/phases/07-listing-schema-security-baseline-backend/07-CONTEXT.md §"Backend Code Structure" (D-01, D-02, D-03) + §"Admin Auth + Rate Limiting" (D-05, D-06)</file>
  </read_first>
  <action>
    Three changes in this task — all required for the auth chain to be testable end-to-end:

    **1) Create `../backend-services/carEx-services/src/moderation/listingRouter.js`** per D-01 + 07-PATTERNS.md §2:
    - File header comment block (≤6 lines) mirroring v1.0 router.js header: name the mount path (`app.use('/api/admin/moderation/listings', verifyIdToken, requireAdmin, listingModerationRateLimiter, listingModerationRouter)` in server.js), state that the rate-limiter mounts at app level (D-03 divergence from v1.0's router-level mount — keeps this file dependency-free), and cross-reference forward to Phase 8 for the real handlers.
    - `const express = require('express');`
    - `const router = express.Router();`
    - `router.get('/ping', (req, res) => { res.json({ ok: true }); });` — preserves the v1.0 `/ping` contract.
    - `module.exports = router;`
    - DEPENDENCY DISCIPLINE per D-01: do NOT require `./service`, `./schemas`, `../models/ListingModerationAction`, or `./listingRateLimit`. The router file is dependency-free at the Phase 7 boundary.

    **2) Modify `../backend-services/carEx-services/server.js`** per D-03 + 07-PATTERNS.md §8:
    - Locate the existing `moderationRouter` import at the top of server.js (line ~22 per 07-PATTERNS.md §8). Directly after it (or in the nearest semantically-grouped import block), add TWO new require lines:
      - `const listingModerationRouter = require('./src/moderation/listingRouter');`
      - `const { listingModerationRateLimiter } = require('./src/moderation/listingRateLimit');`
    - Locate the existing mount line at line ~851: `app.use('/api/admin/moderation', verifyIdToken, requireAdmin, moderationRouter);` (the comment block at lines 845–850 applies to both surfaces — keep it).
    - Insert IMMEDIATELY AFTER that existing line, on a new line, exactly: `app.use('/api/admin/moderation/listings', verifyIdToken, requireAdmin, listingModerationRateLimiter, listingModerationRouter);`
    - Middleware order is LOAD-BEARING: `verifyIdToken` → `requireAdmin` → `listingModerationRateLimiter` → `listingModerationRouter`. The limiter must come AFTER `requireAdmin` so `req.admin.uid` is populated when its keyGenerator runs.
    - PRESERVATION CONTRACT: the existing line 851 (the v1.0 user-mod mount) MUST NOT change. Do NOT touch any other line in server.js — specifically, do not re-order or modify any other `app.use()` line.

    **3) Create `../backend-services/carEx-services/__tests__/listing-moderation/requireAdmin.listing.middleware.test.js`** per 07-PATTERNS.md §12:
    - Header block: require `express`, `supertest`, `mongoose`, `MongoMemoryServer` from 'mongodb-memory-server'.
    - `jest.mock('firebase-admin', () => { ... })` BEFORE any module import that uses it (per Pattern E). The mock pattern is verbatim from 07-PATTERNS.md §12 / Pattern E:
      - `verifyIdTokenMock = jest.fn();`
      - `mock = { credential: { cert: jest.fn(() => ({})) }, initializeApp: jest.fn(), auth: jest.fn(() => ({ verifyIdToken: verifyIdTokenMock })) };`
      - `mock.__verifyIdTokenMock = verifyIdTokenMock;`
      - `return mock;`
    - Imports AFTER the jest.mock call: `const admin = require('firebase-admin'); const { verifyIdToken } = require('../../src/security/verifyIdToken'); const { requireAdmin } = require('../../src/security/requireAdmin'); const listingRouter = require('../../src/moderation/listingRouter'); const AdminUser = require('../../src/models/AdminUser');`
    - `beforeAll`: standard MongoMemoryServer connect + `process.env.FIREBASE_SERVICE_ACCOUNT_JSON = JSON.stringify({ project_id: 'test' });` + minimal Express app: `app = express(); app.use(express.json()); app.use('/api/admin/moderation/listings', verifyIdToken, requireAdmin, listingRouter);` — note: NO `listingModerationRateLimiter` in this chain so the 401/403/200 cases don't accidentally hit a 429. The rate-limit test (Task 2) covers the limiter-in-chain case.
    - `afterAll`: disconnect + stop.
    - `describe('/api/admin/moderation/listings/ping (LSEC-01 + LSEC-02)', () => { ... })` containing exactly 5 tests (mirrors v1.0 §12 test list verbatim, all paths swapped to the new prefix):
      1. `'no Authorization header → 401 unauthenticated'` — `request(app).get('/api/admin/moderation/listings/ping')`, expect status 401 + body `{ error: 'unauthenticated', message: 'Missing or invalid idToken' }`.
      2. `'malformed Authorization header → 401 unauthenticated'` — `.set('Authorization', 'Basic abc')`, expect 401.
      3. `'invalid Bearer token (verifyIdToken throws) → 401 unauthenticated'` — `admin.__verifyIdTokenMock.mockRejectedValueOnce(new Error('invalid signature'))`, `.set('Authorization', 'Bearer bad-token')`, expect 401 + body.
      4. `'valid idToken but email not an AdminUser → 403 unauthorized'` — `mockResolvedValueOnce({ uid: 'u1', email: 'notadmin@test.local' })`, expect 403 + body `{ error: 'unauthorized', message: 'Admin access required' }`.
      5. `'valid admin idToken → 200 { ok: true }'` — `AdminUser.create({ email: 'admin@test.local', role: 'admin' })` then `mockResolvedValueOnce({ uid: 'u2', email: 'admin@test.local' })`, expect 200 + body `{ ok: true }`.
    - Add a 6th spy test per 07-PATTERNS.md §12 adaptation notes — `'req.admin.uid propagates from requireAdmin into the route handler'` — install a passthrough route mid-chain that captures `req.admin.uid` to assert it's the resolved uid; confirms the rate-limiter's keyGenerator (Task 2) will see a populated value.

    Per D-20: do NOT boot `server.js` from this test file.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/requireAdmin.listing.middleware.test.js -x</automated>
  </verify>
  <acceptance_criteria>
    - Router source: `grep -c "router.get('/ping'" ../backend-services/carEx-services/src/moderation/listingRouter.js` returns exactly 1
    - Router source: `grep -c "module.exports = router" ../backend-services/carEx-services/src/moderation/listingRouter.js` returns exactly 1
    - Router dependency-free (D-01): `grep -cE "require\('(.+)'\)" ../backend-services/carEx-services/src/moderation/listingRouter.js` returns exactly 1 (express only — no service, no schemas, no models, no limiter)
    - server.js mount: `grep -c "app.use('/api/admin/moderation/listings', verifyIdToken, requireAdmin, listingModerationRateLimiter, listingModerationRouter)" ../backend-services/carEx-services/server.js` returns exactly 1
    - server.js imports: `grep -c "require('./src/moderation/listingRouter')" ../backend-services/carEx-services/server.js` returns exactly 1
    - server.js imports: `grep -c "require('./src/moderation/listingRateLimit')" ../backend-services/carEx-services/server.js` returns exactly 1
    - server.js preservation: `grep -c "app.use('/api/admin/moderation', verifyIdToken, requireAdmin, moderationRouter)" ../backend-services/carEx-services/server.js` returns exactly 1 (the v1.0 user-mod mount line is byte-identical)
    - Test file: `grep -c "describe('/api/admin/moderation/listings/ping (LSEC-01 + LSEC-02)'" ../backend-services/carEx-services/__tests__/listing-moderation/requireAdmin.listing.middleware.test.js` returns exactly 1
    - Test file: `grep -c "test(" ../backend-services/carEx-services/__tests__/listing-moderation/requireAdmin.listing.middleware.test.js` returns at least 6 (5 LSEC + 1 propagation spy)
    - Test file: `grep -c "jest.mock('firebase-admin'" ../backend-services/carEx-services/__tests__/listing-moderation/requireAdmin.listing.middleware.test.js` returns exactly 1
    - Test file isolation: `grep -c "listingModerationRateLimiter" ../backend-services/carEx-services/__tests__/listing-moderation/requireAdmin.listing.middleware.test.js` returns 0 (rate limiter intentionally NOT in this chain per 07-PATTERNS.md §12)
    - Behavior: `cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/requireAdmin.listing.middleware.test.js -x` exits 0 with `Tests: 6 passed` (or 6+)
  </acceptance_criteria>
  <done>
    Router scaffold landed; server.js mount line added with correct middleware order; v1.0 user-mod mount line byte-identical; middleware test file proves all four LSEC-01/02 canonical cases (401 missing/malformed/invalid + 403 non-admin + 200 admin) plus the req.admin.uid propagation spy that confirms the rate-limiter's keyGenerator will work when wired in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create listingModerationRateLimiter.test.js — 30-then-429 + per-admin keying + D-04 separate-bucket proof (LSEC-03)</name>
  <files>../backend-services/carEx-services/__tests__/listing-moderation/listingModerationRateLimiter.test.js</files>
  <read_first>
    <file>../backend-services/carEx-services/__tests__/moderation/acceptance.test.js lines 229–331 (BLOCK 3 — the v1.0 rate-limit canonical test per 07-PATTERNS.md §11)</file>
    <file>.planning/phases/07-listing-schema-security-baseline-backend/07-PATTERNS.md §11 "listingModerationRateLimiter.test.js" (full adaptation notes including the separate-bucket extension)</file>
    <file>.planning/phases/07-listing-schema-security-baseline-backend/07-CONTEXT.md §"Admin Auth + Rate Limiting" (D-04) and §"Testing" (D-19, D-20)</file>
    <file>../backend-services/carEx-services/src/moderation/listingRateLimit.js (post-Plan-07-04 — required so the test can import the limiter + call .resetKey)</file>
    <file>../backend-services/carEx-services/src/moderation/rateLimit.js (v1.0 user-mod limiter — required so the test can call .resetKey on the singleton; the limiter itself is mounted INSIDE the imported v1.0 router via router.use at router.js:53)</file>
    <file>../backend-services/carEx-services/src/moderation/router.js (v1.0 user-mod router — installs `moderationRateLimiter` at router level via `router.use(moderationRateLimiter)` on line 53; the separate-bucket test imports this router for the cross-prefix mount)</file>
  </read_first>
  <action>
    Create `../backend-services/carEx-services/__tests__/listing-moderation/listingModerationRateLimiter.test.js`.

    Structural shape per 07-PATTERNS.md §11 + Pattern D + Pattern E:

    - Top: same firebase-admin mock block as Task 1 (Pattern E verbatim — `verifyIdTokenMock`, `__verifyIdTokenMock` export).
    - Imports (after mock): `express`, `supertest`, `mongoose`, `MongoMemoryServer`, `admin`, `verifyIdToken`, `requireAdmin`, `listingRouter` (the scaffold from Task 1), `{ listingModerationRateLimiter, WINDOW_MS, MAX_REQUESTS }` (from Plan 07-04), `AdminUser`. PLUS for the separate-bucket test: `moderationRouter` from `'../../src/moderation/router'` and `{ moderationRateLimiter }` from `'../../src/moderation/rateLimit'` (the latter only so we can call `.resetKey()` on the v1.0 limiter singleton in beforeEach — we do NOT mount it at app level because the imported v1.0 router already installs it at router level via `router.use(moderationRateLimiter)` at router.js:53).
    - Constants: `const ADMIN_A_UID = 'admin-a-uid'; const ADMIN_A_EMAIL = 'admin-a@test.local'; const ADMIN_C_UID = 'admin-c-uid'; const ADMIN_C_EMAIL = 'admin-c@test.local';` (mirrors v1.0 BLOCK 3 naming).
    - `beforeAll`: standard MongoMemoryServer + FIREBASE_SERVICE_ACCOUNT_JSON env + `await AdminUser.create({ email: ADMIN_A_EMAIL, role: 'admin' }); await AdminUser.create({ email: ADMIN_C_EMAIL, role: 'admin' });` + build the minimal app with BOTH prefixes mounted:
      - `app = express(); app.use(express.json());`
      - `app.use('/api/admin/moderation/listings', verifyIdToken, requireAdmin, listingModerationRateLimiter, listingRouter);` — listing-mod chain mounts the new limiter at app level per D-03 (listingRouter.js is dependency-free and does NOT install its own limiter).
      - `app.use('/api/admin/moderation', verifyIdToken, requireAdmin, moderationRouter);` — v1.0 user-mod chain mounts ONLY verifyIdToken+requireAdmin at app level. The v1.0 `moderationRateLimiter` is already installed INSIDE `moderationRouter` via `router.use(moderationRateLimiter)` at src/moderation/router.js:53. Re-mounting it at app level here would double-apply the same limiter to every v1.0 request (each request would tick the `admin:<uid>` bucket TWICE, causing 429 at request 16 instead of 31, and would break the D-04 separate-bucket test's invariant that the v1.0 bucket is "untouched" by the listing-bucket workload).
    - `afterAll`: disconnect + stop.
    - `beforeEach`: `listingModerationRateLimiter.resetKey('listing-admin:' + ADMIN_A_UID); listingModerationRateLimiter.resetKey('listing-admin:' + ADMIN_C_UID); moderationRateLimiter.resetKey('admin:' + ADMIN_A_UID);` — fresh bucket state for every test per 07-PATTERNS.md §11 (limiter state carries over between tests). The v1.0 limiter is the SAME singleton whether reached via `require('./rateLimit')` here or via `router.use(...)` inside `moderationRouter` — Node module cache is process-wide.

    Three test blocks per 07-PATTERNS.md §11:

    **Test 1 — 30-then-429 with Retry-After header (LSEC-03 primary):**
    ```
    test('30 successful pings from admin A, the 31st returns 429 with retryAfter + Retry-After header', async () => {
      admin.__verifyIdTokenMock.mockResolvedValue({ uid: ADMIN_A_UID, email: ADMIN_A_EMAIL });
      for (let i = 0; i < 30; i++) {
        const r = await request(app).get('/api/admin/moderation/listings/ping').set('Authorization', 'Bearer ok-token');
        expect(r.status).toBe(200);
        expect(r.body).toEqual({ ok: true });
      }
      const blocked = await request(app).get('/api/admin/moderation/listings/ping').set('Authorization', 'Bearer ok-token');
      expect(blocked.status).toBe(429);
      expect(blocked.body.error).toBe('rate_limited');
      expect(typeof blocked.body.retryAfter).toBe('number');
      expect(blocked.body.retryAfter).toBeGreaterThanOrEqual(0);
      expect(blocked.headers['retry-after']).toMatch(/^\d+$/);
    }, 60_000);
    ```

    **Test 2 — per-admin keying (admin C bucket independent of admin A bucket, D-31 carry-forward):**
    ```
    test('per-admin keying: admin C succeeds after admin A exhausts the listing bucket', async () => {
      admin.__verifyIdTokenMock.mockResolvedValue({ uid: ADMIN_A_UID, email: ADMIN_A_EMAIL });
      for (let i = 0; i < 30; i++) {
        const r = await request(app).get('/api/admin/moderation/listings/ping').set('Authorization', 'Bearer ok-token');
        expect(r.status).toBe(200);
      }
      const aBlocked = await request(app).get('/api/admin/moderation/listings/ping').set('Authorization', 'Bearer ok-token');
      expect(aBlocked.status).toBe(429);

      admin.__verifyIdTokenMock.mockResolvedValue({ uid: ADMIN_C_UID, email: ADMIN_C_EMAIL });
      const cRes = await request(app).get('/api/admin/moderation/listings/ping').set('Authorization', 'Bearer ok-token');
      expect(cRes.status).toBe(200);
    }, 60_000);
    ```

    **Test 3 — D-04 SEPARATE BUCKET PROOF (the load-bearing distinguishing assertion vs v1.0):**
    ```
    test('separate buckets: admin A exhausting listing bucket does NOT block same admin on user-mod prefix (D-04)', async () => {
      admin.__verifyIdTokenMock.mockResolvedValue({ uid: ADMIN_A_UID, email: ADMIN_A_EMAIL });
      // Exhaust listing bucket
      for (let i = 0; i < 30; i++) {
        const r = await request(app).get('/api/admin/moderation/listings/ping').set('Authorization', 'Bearer ok-token');
        expect(r.status).toBe(200);
      }
      const listingBlocked = await request(app).get('/api/admin/moderation/listings/ping').set('Authorization', 'Bearer ok-token');
      expect(listingBlocked.status).toBe(429);

      // SAME admin A hits the v1.0 user-mod /ping — the admin: bucket is untouched, should be 200
      const userModRes = await request(app).get('/api/admin/moderation/ping').set('Authorization', 'Bearer ok-token');
      expect(userModRes.status).toBe(200);
      expect(userModRes.body).toEqual({ ok: true });
    }, 60_000);
    ```

    Per D-19, file lives under `__tests__/listing-moderation/`. Per D-20, do NOT boot `server.js` — build the minimal app in `beforeAll` as shown.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/listingModerationRateLimiter.test.js -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "describe(" ../backend-services/carEx-services/__tests__/listing-moderation/listingModerationRateLimiter.test.js` returns at least 1
    - Source: `grep -c "test(" ../backend-services/carEx-services/__tests__/listing-moderation/listingModerationRateLimiter.test.js` returns exactly 3
    - Source: `grep -c "listingModerationRateLimiter.resetKey" ../backend-services/carEx-services/__tests__/listing-moderation/listingModerationRateLimiter.test.js` returns at least 2 (resetKey for admin A and admin C in beforeEach)
    - Source: `grep -c "moderationRateLimiter.resetKey" ../backend-services/carEx-services/__tests__/listing-moderation/listingModerationRateLimiter.test.js` returns at least 1 (v1.0 limiter reset for separate-bucket test)
    - Source: `grep -c "for (let i = 0; i < 30" ../backend-services/carEx-services/__tests__/listing-moderation/listingModerationRateLimiter.test.js` returns at least 3 (one per test that exhausts the bucket)
    - Source: `grep -c "expect(blocked.status).toBe(429)" ../backend-services/carEx-services/__tests__/listing-moderation/listingModerationRateLimiter.test.js` returns at least 1 (LSEC-03 primary)
    - Source: `grep -c "separate buckets" ../backend-services/carEx-services/__tests__/listing-moderation/listingModerationRateLimiter.test.js` returns exactly 1 (D-04 test name marker)
    - Source: `grep -c "jest.mock('firebase-admin'" ../backend-services/carEx-services/__tests__/listing-moderation/listingModerationRateLimiter.test.js` returns exactly 1
    - Source: `grep -c "MongoMemoryServer.create" ../backend-services/carEx-services/__tests__/listing-moderation/listingModerationRateLimiter.test.js` returns exactly 1 (D-20 isolation)
    - Anti-pattern lock (no double-mount of v1.0 limiter): `grep -cE "app\.use\('/api/admin/moderation',[^)]*moderationRateLimiter" ../backend-services/carEx-services/__tests__/listing-moderation/listingModerationRateLimiter.test.js` returns exactly 0 (the v1.0 limiter is installed inside moderationRouter at router.js:53; re-mounting at app-level here would double-tick the bucket and break the D-04 separate-bucket invariant)
    - Behavior: `cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/listingModerationRateLimiter.test.js -x` exits 0 with `Tests: 3 passed`
  </acceptance_criteria>
  <done>
    All three supertest assertions green: 30-then-429 with Retry-After header (LSEC-03 primary), per-admin keying (admin C unaffected by admin A's exhausted bucket), and D-04 separate-bucket proof (admin A's user-mod prefix still returns 200 after listing bucket is exhausted). After this plan lands, ROADMAP success criteria #2 and #3 are mechanically green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet ↔ Express app | Every request to `/api/admin/moderation/listings/*` crosses this boundary. The full middleware chain (`verifyIdToken` → `requireAdmin` → `listingModerationRateLimiter`) is the only validation before the router handler runs. |
| Mounted-prefix collision | `/api/admin/moderation` and `/api/admin/moderation/listings` share a path prefix. Express matches routes whose path actually exists in the mounted router; a `GET /api/admin/moderation/listings/ping` will NOT match the v1.0 user-mod router because that router has no `/listings/ping` route. 07-PATTERNS.md §8 analyzed this and confirms safety. |
| Test-environment firebase-admin mock | The mock returns whatever the test sets via `mockResolvedValueOnce`. In test mode the mock IS the trust boundary; in production the real `firebase-admin.verifyIdToken()` enforces it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Spoofing | client sends arbitrary Bearer string | mitigate | `verifyIdToken` rejects any token that doesn't pass Firebase signature verification; Task 1 test 3 locks the rejection envelope. |
| T-07-05-02 | Elevation of privilege | authenticated non-admin tries to call listing-mod routes | mitigate | `requireAdmin` checks the email against `AdminUser` collection server-side; Task 1 test 4 locks the 403 envelope. LSEC-02 contract explicitly forbids trusting mobile-side `isAdmin`. |
| T-07-05-03 | Denial of service | abusive admin (compromised credential) drains throughput | mitigate | `listingModerationRateLimiter` 30/15min; Task 2 test 1 locks the 31st-call 429 response. |
| T-07-05-04 | Denial of service | shared-bucket attack (admin spends user-mod budget on listing endpoints or vice versa) | mitigate | D-04 separate bucket — Plan 07-04 + Task 2 test 3 PROVE the buckets are independent on a live minimal app. |
| T-07-05-05 | Tampering | middleware order regression (e.g., limiter mounted before requireAdmin) | mitigate | server.js mount line grep acceptance enforces literal middleware order: `verifyIdToken, requireAdmin, listingModerationRateLimiter, listingModerationRouter`. Any reorder breaks the grep. |
| T-07-05-06 | Tampering | someone adds a route to listingRouter.js that bypasses the limiter | accept | App-level mount (D-03) means ALL routes registered on listingRouter run through the limiter — there's no router-level escape. Phase 8 endpoint handlers will be subject to the same chain. |
| T-07-05-07 | Information disclosure | response envelope deviates from D-06 contract (e.g., 401 returns Spanish error message) | mitigate | Acceptance grep on the canonical envelope strings in both verifyIdToken.js and requireAdmin.js source (preserved verbatim from v1.0); Task 1 tests assert envelope byte-equal to D-06 spec. Mobile `apiClient` interceptor depends on these exact envelopes. |
| T-07-05-08 | Spoofing | server.js mount order means longer-prefix `/listings` request could match shorter-prefix `/api/admin/moderation` mount first | mitigate | 07-PATTERNS.md §8 analyzed Express route matching: the shorter-prefix mount only matches if its router has the requested sub-path; `moderationRouter` has no `/listings/ping` route so the request falls through to the longer-prefix mount. Phase 8 endpoint testing will re-validate this property. |
</threat_model>

<verification>
After both tasks complete, run from the mobile-repo root:

```
cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/requireAdmin.listing.middleware.test.js __tests__/listing-moderation/listingModerationRateLimiter.test.js -x
```

Expect: 9+ passing tests (6 middleware + 3 rate-limit), exit 0.

Server boot smoke (manual, OPTIONAL — confirms the server.js mount line doesn't break startup): `cd ../backend-services/carEx-services && node -e "require('./server.js')"` should not throw a syntax or require error (it WILL try to connect to MongoDB and exit if MONGODB_URI is unset — that's fine; the parse + require chain is what we're verifying).
</verification>

<success_criteria>
- `listingRouter.js` exists as a dependency-free scaffold with the `/ping` route.
- `server.js` has exactly one new mount line, correct middleware order, and the existing v1.0 user-mod mount line is byte-identical.
- Middleware test file proves LSEC-01/02 with all 5 canonical cases + req.admin.uid propagation spy.
- Rate-limit test file proves LSEC-03 with 30-then-429 + per-admin keying + D-04 separate-bucket invariant.
- All 9+ tests green; ROADMAP success criteria #2 and #3 mechanically delivered.
</success_criteria>

<output>
After completion, create `.planning/phases/07-listing-schema-security-baseline-backend/07-05-SUMMARY.md`.
</output>
</content>
</invoke>