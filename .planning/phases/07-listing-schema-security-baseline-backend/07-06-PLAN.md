---
phase: 07-listing-schema-security-baseline-backend
plan: 06
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - ../backend-services/carEx-services/scripts/migrate-listing-moderation.js
  - ../backend-services/carEx-services/src/security/ensureBaseline.js
  - ../backend-services/carEx-services/__tests__/listing-moderation/migrate-listing-moderation.test.js
autonomous: true
requirements: [LDATA-04]
must_haves:
  truths:
    - "After `node scripts/migrate-listing-moderation.js` runs against a Car collection seeded with documents missing the `status` field, every Car document has `status: 'active'` (or its pre-existing non-default value) AND the pre-migration count equals the post-migration count (ROADMAP success criterion #1 — primary deliverable of this plan)"
    - "Pre-existing Car documents that already carry a non-default `status` value (e.g., a `suspended` listing inserted before the migration ran a second time) are NOT mutated — the `{ status: { $exists: false } }` filter only matches docs WITHOUT the field (D-18 idempotency)"
    - "On server startup, `ensureBaseline()` logs `[Baseline] {N} listings missing status — run: node scripts/migrate-listing-moderation.js` if any Car docs lack the field, but does NOT auto-migrate (D-17 safety rationale mirroring v1.0 D-30)"
    - "Both new indexes (`{ status: 1 }` and `{ sellerId: 1, status: 1 }`) on the `cars` collection AND all three audit indexes on `listing_moderation_actions` are explicitly created by the migration script's `ensureIndexes()` step so Phase 9 doesn't pay first-query index-build cost in production"
    - "`scripts/migrate-listing-moderation.js` is the one-off, manually-invoked Node script (`node scripts/migrate-listing-moderation.js`) — mirrors the v1.0 `scripts/migrate-moderation.js` structure, is idempotent (D-18), and does two things: (a) backfills every `Car` doc lacking `status` with `status: 'active'` (other moderation fields default via schema), (b) creates the new `{ status: 1 }` and `{ sellerId: 1, status: 1 }` indexes explicitly (D-15 migration-script spec)"
    - "Migration acceptance contract: post-migration `Car.countDocuments({})` MUST equal pre-migration count AND `Car.countDocuments({ status: { $exists: false } })` MUST return 0 — the script logs both counts and exits non-zero if either invariant fails (D-16 acceptance)"
    - "New tests live under `__tests__/listing-moderation/` at the backend repo root, sibling to v1.0's `__tests__/moderation/` directory (D-19 test-directory placement)"
    - "The migration test does NOT boot `server.js` — it builds a minimal harness that imports models/exported migration functions directly (D-20 test-isolation pattern mirroring v1.0 D-36 / Phase 5 Plan 05-10)"
  artifacts:
    - path: "../backend-services/carEx-services/scripts/migrate-listing-moderation.js"
      provides: "Idempotent one-off Node script that backfills `Car.status = 'active'` and syncs indexes on both Car and ListingModerationAction"
      contains: "async function backfillListings()"
    - path: "../backend-services/carEx-services/src/security/ensureBaseline.js"
      provides: "Extended in place — adds the Car.status baseline check alongside the existing User.moderationStatus check"
      contains: "pendingListings"
    - path: "../backend-services/carEx-services/__tests__/listing-moderation/migrate-listing-moderation.test.js"
      provides: "Seed 10 docs (7 missing status, 3 pre-suspended), run backfill, assert counts + idempotency + ensureIndexes creates both new indexes"
      contains: "describe('migrate-listing-moderation"
  key_links:
    - from: "../backend-services/carEx-services/scripts/migrate-listing-moderation.js"
      to: "../backend-services/carEx-services/src/models/Car.js (Plan 07-01)"
      via: "Script calls `Car.updateMany({ status: { $exists: false } }, { $set: { status: 'active' } })` — depends on the `status` field existing in the schema"
      pattern: "Car.updateMany"
    - from: "../backend-services/carEx-services/scripts/migrate-listing-moderation.js"
      to: "../backend-services/carEx-services/src/models/ListingModerationAction.js (Plan 07-02)"
      via: "Script calls `ListingModerationAction.syncIndexes()` to create the 3 audit indexes — depends on the model being registered"
      pattern: "ListingModerationAction.syncIndexes"
    - from: "../backend-services/carEx-services/src/security/ensureBaseline.js"
      to: "../backend-services/carEx-services/src/models/Car.js (Plan 07-01)"
      via: "Baseline check uses `Car.countDocuments({ status: { $exists: false } })` — depends on the `Car` model loading cleanly with the extended schema"
      pattern: "Car.countDocuments"
---

<objective>
Land the LDATA-04 migration substrate end-to-end:
1. Create `scripts/migrate-listing-moderation.js` — idempotent Node script that backfills every Car doc lacking `status` with `status: 'active'` and explicitly syncs the new indexes on both `cars` and `listing_moderation_actions` collections.
2. Extend `src/security/ensureBaseline.js` in place to add a second `countDocuments` check on Car alongside the existing User check — log-only, never auto-migrate (D-17 safety rationale).
3. Land `__tests__/listing-moderation/migrate-listing-moderation.test.js` — seeds 10 Car docs (7 missing status, 3 pre-suspended), runs the exported `backfillListings()` and `ensureIndexes()` functions, asserts pre/post counts match + idempotency + both new indexes created.

Purpose: ROADMAP success criterion #1 ("Every existing listing in the DB has `status: 'active'` after the migration runs and the pre-migration count equals the post-migration count") is mechanically delivered by the migration script. The in-tree jest test is the CI-side proof; the production-deploy step has its own manual run + Railway log verification (per [[backend_deploy_gotcha]] — Railway deploys backend `main` only, so the migration must land on backend `main` before mobile assumes the field is populated).

Output: New migration script, modified `ensureBaseline.js`, new jest test file. All four ROADMAP success criteria become green after this plan + Plan 07-05 land together.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-listing-schema-security-baseline-backend/07-CONTEXT.md
@.planning/phases/07-listing-schema-security-baseline-backend/07-PATTERNS.md

<interfaces>
<!-- Source of truth: 07-CONTEXT.md D-15, D-16, D-17, D-18 + 07-PATTERNS.md §5, §7, §14, Pattern F. -->

Migration script API (D-15 + Pattern F):
- Shebang `#!/usr/bin/env node`
- `require('dotenv').config()` at top
- `const Car = require('../src/models/Car');`
- `const ListingModerationAction = require('../src/models/ListingModerationAction');`
- Exported functions (so the test can call them WITHOUT triggering `main()`):
  - `async function backfillListings()` — runs `Car.updateMany({ status: { $exists: false } }, { $set: { status: 'active' } })`, logs the modifiedCount, returns it. Other moderation fields default via schema (D-15) — do NOT set them here.
  - `async function ensureIndexes()` — calls `await Car.syncIndexes(); await ListingModerationAction.syncIndexes();` then logs `[migrate-listing] indexes synced on cars + listing_moderation_actions`.
- `async function main()` — connects via `process.env.MONGODB_URI` (throw if unset), `dbName: 'CarEx'`, runs `backfillListings()` → `ensureIndexes()`, then D-16 invariant check:
  - `const stillMissing = await Car.countDocuments({ status: { $exists: false } });`
  - `if (stillMissing > 0) { console.error('[migrate-listing] FAIL: ' + stillMissing + ' cars still missing status after backfill'); process.exit(2); }`
  - Disconnects via `mongoose.disconnect()`.
- `if (require.main === module) { main().catch(err => { console.error('[migrate-listing] FAILED:', err); process.exit(1); }); }` (so the test can import the exports without triggering main).
- `module.exports = { backfillListings, ensureIndexes };`

ensureBaseline extension (D-17 + 07-PATTERNS.md §7) — extend in place:
- ADD `const Car = require('../models/Car');` to imports.
- INSERT a second `countDocuments` check INSIDE the existing try block, AFTER the existing User check so a Car-side failure doesn't mask a User-side warning:
  - `const pendingListings = await Car.countDocuments({ status: { $exists: false } });`
  - `if (pendingListings > 0) { console.warn('[Baseline] ' + pendingListings + ' listings missing status — run: node scripts/migrate-listing-moderation.js'); } else { console.log('[Baseline] All listings have status.'); }`
- PRESERVATION CONTRACT: existing `User.countDocuments` check + log/warn shape MUST remain intact. Function name `ensureBaseline` and single `module.exports = { ensureBaseline }` MUST NOT change (server.js:37 calls `await ensureBaseline()`). Top-level try/catch envelope MUST wrap the new check too — v1.0 contract: "this function never throws into the server bootstrap".
- Update the docstring to mention BOTH `migrate-moderation.js` and `migrate-listing-moderation.js`.

Test seed shape per 07-PATTERNS.md §14:
- Seed via `Car.collection.insertOne(...)` (bypasses Mongoose defaults — required to simulate legacy docs without `status`):
  - 7 docs with no `status` field: `{ sellerId: 'seller-{i}', makeName: 'Toyota', modelName: 'Camry' }` for i in 0..6.
  - 3 docs with pre-existing `status: 'suspended'`: `{ sellerId: 'seller-{i}', makeName: 'Honda', modelName: 'Civic', status: 'suspended' }` for i in 7..9.
- Pre-migration: `Car.countDocuments({})` returns 10.
- Run `backfillListings()` — assert it returns 7 (only the missing-status docs updated).
- Post-migration: `Car.countDocuments({})` returns 10 (D-16 invariant — count unchanged); `Car.countDocuments({ status: { $exists: false } })` returns 0 (D-16 hard merge-gate); `Car.countDocuments({ status: 'suspended' })` returns 3 (idempotency proof — pre-suspended docs untouched per D-18).

Test does NOT call `main()` — only the exported functions. `main()` calls `process.exit()` which would kill the test runner.

Test cleanup contract (interaction with Plan 07-02 append-only hooks):
- Plan 07-02 installs 6 Mongoose pre-hooks on `ListingModerationAction` — `pre('deleteMany')` throws `'ListingModerationAction is append-only'`. Calling `ListingModerationAction.deleteMany({})` at the Mongoose model layer is GUARANTEED to throw, not "may throw".
- The documented escape hatch is the raw MongoDB driver: `ListingModerationAction.collection.deleteMany({})` bypasses Mongoose middleware entirely (the `.collection` property returns the underlying native driver Collection instance, which has no awareness of Mongoose pre-hooks).
- For Car (Plan 07-01 — no append-only hooks), the Mongoose-level `Car.deleteMany({})` is fine.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migrate-listing-moderation.js script + extend ensureBaseline.js with the Car.status baseline check</name>
  <files>../backend-services/carEx-services/scripts/migrate-listing-moderation.js, ../backend-services/carEx-services/src/security/ensureBaseline.js</files>
  <read_first>
    <file>../backend-services/carEx-services/scripts/migrate-moderation.js (full file, ~127 lines — the v1.0 template per 07-PATTERNS.md §5)</file>
    <file>../backend-services/carEx-services/src/security/ensureBaseline.js (full file, ~22 lines — the v1.0 file is the analog itself per 07-PATTERNS.md §7)</file>
    <file>.planning/phases/07-listing-schema-security-baseline-backend/07-PATTERNS.md §5 "scripts/migrate-listing-moderation.js" + §7 "src/security/ensureBaseline.js" + Pattern F "Migration script discipline"</file>
    <file>.planning/phases/07-listing-schema-security-baseline-backend/07-CONTEXT.md §"Migration / Backfill" (D-15, D-16, D-17, D-18)</file>
  </read_first>
  <action>
    Two file changes in this task.

    **1) Create `../backend-services/carEx-services/scripts/migrate-listing-moderation.js`** per 07-PATTERNS.md §5 + Pattern F.

    Structure (top-to-bottom):
    - `#!/usr/bin/env node` shebang on line 1.
    - File header comment (3–5 lines): "One-off migration. Idempotent. Run: node scripts/migrate-listing-moderation.js. Backfills Car.status and creates Phase 7 indexes."
    - `require('dotenv').config();`
    - `const mongoose = require('mongoose');`
    - `const Car = require('../src/models/Car');`
    - `const ListingModerationAction = require('../src/models/ListingModerationAction');`
    - `async function backfillListings() { ... }` — body:
      - `const filter = { status: { $exists: false } };`
      - `const patch = { $set: { status: 'active' } };`
      - `const result = await Car.updateMany(filter, patch);`
      - `console.log('[migrate-listing] listings backfilled: ' + result.modifiedCount);`
      - `return result.modifiedCount;`
    - `async function ensureIndexes() { ... }` — body:
      - `await Car.syncIndexes();`
      - `await ListingModerationAction.syncIndexes();`
      - `console.log('[migrate-listing] indexes synced on cars + listing_moderation_actions');`
    - `async function main() { ... }` — body:
      - `const uri = process.env.MONGODB_URI; if (!uri) throw new Error('MONGODB_URI env var is required');`
      - `await mongoose.connect(uri, { dbName: 'CarEx' });`
      - `console.log('[migrate-listing] connected');`
      - `const listingsCount = await backfillListings();`
      - `await ensureIndexes();`
      - D-16 invariant check: `const stillMissing = await Car.countDocuments({ status: { $exists: false } }); if (stillMissing > 0) { console.error('[migrate-listing] FAIL: ' + stillMissing + ' cars still missing status after backfill'); await mongoose.disconnect(); process.exit(2); }`
      - `console.log('[migrate-listing] DONE — listings backfilled: ' + listingsCount);`
      - `await mongoose.disconnect();`
    - `if (require.main === module) { main().catch(err => { console.error('[migrate-listing] FAILED:', err); process.exit(1); }); }` — guard so test imports don't trigger main.
    - `module.exports = { backfillListings, ensureIndexes };`

    DO NOT include a `backfillOrders` function — Phase 7 has no order-side backfill (orders gain a Listing-pause flag only in Phase 9). DO NOT call `main()` from anywhere outside the `require.main === module` guard.

    **2) Extend `../backend-services/carEx-services/src/security/ensureBaseline.js` in place** per 07-PATTERNS.md §7 + D-17.

    Three edits — preserve everything else byte-identical:
    - ADD `const Car = require('../models/Car');` near the existing `const User = require('../models/User');` import.
    - INSIDE the existing try block, AFTER the existing User-check `if/else` block, ADD:
      - `const pendingListings = await Car.countDocuments({ status: { $exists: false } });`
      - `if (pendingListings > 0) { console.warn('[Baseline] ' + pendingListings + ' listings missing status — run: node scripts/migrate-listing-moderation.js'); } else { console.log('[Baseline] All listings have status.'); }`
    - UPDATE the docstring (the JSDoc-style comment above `async function ensureBaseline()`) to mention BOTH scripts: change "run `node scripts/migrate-moderation.js`" reference into something like "run `node scripts/migrate-moderation.js` and/or `node scripts/migrate-listing-moderation.js` deliberately" — keep the existing intent ("never auto-migrates") verbatim.

    PRESERVATION CONTRACT (verified by grep in acceptance):
    - The existing `User.countDocuments({ 'moderationStatus.state': { $exists: false } })` line MUST remain intact (byte-identical to pre-edit).
    - The existing `console.warn('[Baseline] ${pending} users missing moderationStatus — run: node scripts/migrate-moderation.js')` line MUST remain intact.
    - The existing `console.log('[Baseline] All users have moderationStatus.')` line MUST remain intact.
    - The function signature `async function ensureBaseline()` and the export line `module.exports = { ensureBaseline };` MUST NOT change.
    - The top-level `try { ... } catch (err) { ... }` envelope MUST still wrap the new check — the new code goes INSIDE the try block, not as a separate try.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && node -e "const m = require('./scripts/migrate-listing-moderation'); if (typeof m.backfillListings !== 'function') throw new Error('backfillListings not exported'); if (typeof m.ensureIndexes !== 'function') throw new Error('ensureIndexes not exported'); const b = require('./src/security/ensureBaseline'); if (typeof b.ensureBaseline !== 'function') throw new Error('ensureBaseline not exported'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Script source: `grep -c "async function backfillListings" ../backend-services/carEx-services/scripts/migrate-listing-moderation.js` returns exactly 1
    - Script source: `grep -c "async function ensureIndexes" ../backend-services/carEx-services/scripts/migrate-listing-moderation.js` returns exactly 1
    - Script source: `grep -c "module.exports = { backfillListings, ensureIndexes }" ../backend-services/carEx-services/scripts/migrate-listing-moderation.js` returns exactly 1
    - Script source: `grep -c "Car.updateMany" ../backend-services/carEx-services/scripts/migrate-listing-moderation.js` returns exactly 1
    - Script source: `grep -c "status: { \$exists: false }" ../backend-services/carEx-services/scripts/migrate-listing-moderation.js` returns at least 2 (one in backfillListings filter, one in main's D-16 invariant check)
    - Script source: `grep -c "Car.syncIndexes" ../backend-services/carEx-services/scripts/migrate-listing-moderation.js` returns exactly 1
    - Script source: `grep -c "ListingModerationAction.syncIndexes" ../backend-services/carEx-services/scripts/migrate-listing-moderation.js` returns exactly 1
    - Script source: `grep -c "process.exit(2)" ../backend-services/carEx-services/scripts/migrate-listing-moderation.js` returns exactly 1 (D-16 soft-failure exit code per Pattern F)
    - Script source: `grep -c "process.exit(1)" ../backend-services/carEx-services/scripts/migrate-listing-moderation.js` returns exactly 1 (uncaught error exit per Pattern F)
    - Script source: `grep -c "require.main === module" ../backend-services/carEx-services/scripts/migrate-listing-moderation.js` returns exactly 1 (Pattern F guard so tests can import exports)
    - Script anti-pattern: `grep -c "backfillOrders" ../backend-services/carEx-services/scripts/migrate-listing-moderation.js` returns 0 (no order-side backfill — Phase 7 scope)
    - Script anti-pattern: `grep -c "backfillUsers" ../backend-services/carEx-services/scripts/migrate-listing-moderation.js` returns 0 (no user-side backfill — different script)
    - ensureBaseline source: `grep -c "Car.countDocuments({ status: { \$exists: false } })" ../backend-services/carEx-services/src/security/ensureBaseline.js` returns exactly 1
    - ensureBaseline source: `grep -c "pendingListings" ../backend-services/carEx-services/src/security/ensureBaseline.js` returns at least 2 (declaration + log interpolation)
    - ensureBaseline source: `grep -c "migrate-listing-moderation.js" ../backend-services/carEx-services/src/security/ensureBaseline.js` returns at least 1 (in the warn message)
    - ensureBaseline preservation: `grep -c "User.countDocuments({ 'moderationStatus.state': { \$exists: false } })" ../backend-services/carEx-services/src/security/ensureBaseline.js` returns exactly 1 (existing line byte-identical)
    - ensureBaseline preservation: `grep -c "users missing moderationStatus" ../backend-services/carEx-services/src/security/ensureBaseline.js` returns exactly 1 (existing warn message intact)
    - ensureBaseline preservation: `grep -c "module.exports = { ensureBaseline }" ../backend-services/carEx-services/src/security/ensureBaseline.js` returns exactly 1
    - ensureBaseline preservation: `grep -c "async function ensureBaseline()" ../backend-services/carEx-services/src/security/ensureBaseline.js` returns exactly 1
    - ensureBaseline anti-pattern (D-17 safety rationale — log only, never auto-migrate; regex guards imperative call paths, not docstring prose like "never auto-migrates"): `grep -cE "(await|return)[[:space:]]+.*migrate" ../backend-services/carEx-services/src/security/ensureBaseline.js` returns exactly 0 (no `await someMigrate(...)` or `return someMigrate(...)` — the docstring's stated intent "never auto-migrates" has no `await`/`return` prefix and is correctly not matched)
    - Behavior: the `<verify>` node one-liner exits 0 and prints `OK`
  </acceptance_criteria>
  <done>
    Migration script exists with the documented exports + D-16 invariant check + Pattern F discipline (idempotent filter, dual exit codes, require.main guard). ensureBaseline extended in place with the Car.status check; existing User check is byte-identical to pre-edit. Both modules are loadable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create migrate-listing-moderation.test.js — seeds 10 docs, asserts pre/post counts, idempotency, and ensureIndexes creates both new indexes</name>
  <files>../backend-services/carEx-services/__tests__/listing-moderation/migrate-listing-moderation.test.js</files>
  <read_first>
    <file>../backend-services/carEx-services/__tests__/moderation/migrate-moderation.test.js (full file, ~120 lines — the v1.0 sibling per 07-PATTERNS.md §14)</file>
    <file>.planning/phases/07-listing-schema-security-baseline-backend/07-PATTERNS.md §14 "migrate-listing-moderation.test.js" (full adaptation notes including the 10-doc seed pattern)</file>
    <file>../backend-services/carEx-services/scripts/migrate-listing-moderation.js (post-Task-1)</file>
  </read_first>
  <action>
    Create `../backend-services/carEx-services/__tests__/listing-moderation/migrate-listing-moderation.test.js`. Adapt v1.0's `migrate-moderation.test.js` per 07-PATTERNS.md §14.

    Structural shape:
    - Top: require `mongoose`, `MongoMemoryServer`. Declare `let mongo; let Car; let ListingModerationAction; let migrate;`.
    - `beforeAll`:
      - `mongo = await MongoMemoryServer.create(); await mongoose.connect(mongo.getUri());`
      - `Car = require('../../src/models/Car');`
      - `require('../../src/models/ListingModerationAction');` — register so `ensureIndexes` can sync it.
      - `ListingModerationAction = mongoose.model('ListingModerationAction');`
      - `migrate = require('../../scripts/migrate-listing-moderation');`
    - `afterAll`: `await mongoose.disconnect(); await mongo.stop();`
    - `beforeEach`: clean slate between tests. The two cleanup calls have DIFFERENT shapes because they interact differently with Plan 07-02's append-only contract:
      - `await Car.deleteMany({});` — Mongoose-level deleteMany is FINE on Car (Plan 07-01 installs no append-only hooks).
      - `await ListingModerationAction.collection.deleteMany({});` — MUST use the raw MongoDB driver. ListingModerationAction has 6 append-only pre-hooks (Plan 07-02 D-11) including `pre('deleteMany')` which throws `'ListingModerationAction is append-only'`. The `.collection` property returns the underlying native driver Collection instance, which has no awareness of Mongoose middleware — this is the documented escape hatch and the only safe cleanup path in tests. Calling Mongoose-level `ListingModerationAction.deleteMany({})` here would throw and abort every test.

    Three `describe` blocks per 07-PATTERNS.md §14:

    **Describe 1 — backfillListings (LDATA-04 primary):**
    - Test 1: `'backfills 7 docs missing status, leaves 3 pre-suspended docs untouched, post-count equals pre-count (D-16)'`
      - Seed 7 docs via `Car.collection.insertOne({ sellerId: 'seller-' + i, makeName: 'Toyota', modelName: 'Camry' })` for i in 0..6 (bypasses Mongoose defaults — these docs WILL NOT have a `status` field).
      - Seed 3 docs via `Car.collection.insertOne({ sellerId: 'seller-' + i, makeName: 'Honda', modelName: 'Civic', status: 'suspended' })` for i in 7..9.
      - `const preCount = await Car.countDocuments({}); expect(preCount).toBe(10);`
      - `const updated = await migrate.backfillListings(); expect(updated).toBe(7);`
      - `const postCount = await Car.countDocuments({}); expect(postCount).toBe(preCount);` (D-16 invariant)
      - `const stillMissing = await Car.countDocuments({ status: { $exists: false } }); expect(stillMissing).toBe(0);` (D-16 hard merge-gate)
      - `const suspended = await Car.countDocuments({ status: 'suspended' }); expect(suspended).toBe(3);` (D-18 idempotency on pre-existing values — pre-suspended docs untouched)
      - `const active = await Car.countDocuments({ status: 'active' }); expect(active).toBe(7);` (the 7 newly-backfilled docs)

    - Test 2: `'is idempotent on a second run (D-18) — returns 0 modified'`
      - Seed 1 doc: `Car.collection.insertOne({ sellerId: 'seller-x', makeName: 'Ford', modelName: 'F-150' });`
      - First run: `await migrate.backfillListings();` (returns 1, sets status: 'active').
      - Second run: `const second = await migrate.backfillListings(); expect(second).toBe(0);` — proves the `{ status: { $exists: false } }` filter matches zero docs on rerun.

    **Describe 2 — ensureIndexes:**
    - Test 3: `'creates the { status: 1 } and { sellerId: 1, status: 1 } indexes on cars'`
      - `await migrate.ensureIndexes();`
      - `const indexes = await Car.collection.getIndexes();`
      - `const names = Object.keys(indexes);`
      - Assert SOME index name includes `'status'` (the single-field one): `expect(names.some(n => n === 'status_1')).toBe(true);` (or use a looser `n.includes('status')` if exact name varies — the v1.0 test uses the looser form per 07-PATTERNS.md §14 example).
      - Assert SOME index name includes both `sellerId` and `status` (the compound): `expect(names.some(n => n.includes('sellerId') && n.includes('status'))).toBe(true);`.

    - Test 4: `'creates the three audit indexes on listing_moderation_actions'`
      - `await migrate.ensureIndexes();`
      - `const auditIndexes = await ListingModerationAction.collection.getIndexes();`
      - `const auditNames = Object.keys(auditIndexes);`
      - Assert each of the three D-10 indexes exists: one matching `listingId` + `createdAt`, one matching `adminUid` + `createdAt`, one matching `sellerUid` + `createdAt`.

    Per D-19, file lives at `__tests__/listing-moderation/`. Per D-20, do NOT boot `server.js`. CRITICAL per 07-PATTERNS.md §14: do NOT call `migrate.main()` from the test — only the exported `backfillListings` / `ensureIndexes`. Calling `main()` would trigger `process.exit()` and kill the test runner.
  </action>
  <verify>
    <automated>cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/migrate-listing-moderation.test.js -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "describe('migrate-listing-moderation" ../backend-services/carEx-services/__tests__/listing-moderation/migrate-listing-moderation.test.js` returns at least 1
    - Source: `grep -c "test(" ../backend-services/carEx-services/__tests__/listing-moderation/migrate-listing-moderation.test.js` returns at least 4
    - Source: `grep -c "Car.collection.insertOne" ../backend-services/carEx-services/__tests__/listing-moderation/migrate-listing-moderation.test.js` returns at least 10 (7 missing-status seeds + 3 pre-suspended seeds + at least 1 idempotency seed; planner-accepted minimum 10, could be loop-driven)
    - Source: `grep -c "migrate.backfillListings" ../backend-services/carEx-services/__tests__/listing-moderation/migrate-listing-moderation.test.js` returns at least 3 (first run + second-run idempotency)
    - Source: `grep -c "migrate.ensureIndexes" ../backend-services/carEx-services/__tests__/listing-moderation/migrate-listing-moderation.test.js` returns at least 2 (one for cars indexes, one for audit indexes)
    - Source: `grep -c "expect(postCount).toBe(preCount)" ../backend-services/carEx-services/__tests__/listing-moderation/migrate-listing-moderation.test.js` returns exactly 1 (D-16 invariant)
    - Source: `grep -c "expect(stillMissing).toBe(0)" ../backend-services/carEx-services/__tests__/listing-moderation/migrate-listing-moderation.test.js` returns exactly 1 (D-16 merge-gate)
    - Anti-pattern: `grep -c "migrate.main" ../backend-services/carEx-services/__tests__/listing-moderation/migrate-listing-moderation.test.js` returns 0 (per 07-PATTERNS.md §14 — never call main from tests)
    - Cleanup contract (raw-driver path required for append-only model): `grep -c "ListingModerationAction.collection.deleteMany" ../backend-services/carEx-services/__tests__/listing-moderation/migrate-listing-moderation.test.js` returns at least 1 (raw MongoDB driver bypasses the 6 Mongoose append-only pre-hooks installed in Plan 07-02 — the documented escape hatch)
    - Cleanup contract (no Mongoose-level deleteMany on append-only model): `grep -cE "ListingModerationAction\.deleteMany\(" ../backend-services/carEx-services/__tests__/listing-moderation/migrate-listing-moderation.test.js` returns exactly 0 (Mongoose-level deleteMany would throw `'ListingModerationAction is append-only'` from Plan 07-02's `pre('deleteMany')` hook; must use `.collection.deleteMany()` instead — the negative-context lookahead via the literal `.deleteMany(` after the model name excludes the safe `.collection.deleteMany(` path which has `.collection` between the model name and `.deleteMany(`)
    - Source: `grep -c "MongoMemoryServer.create" ../backend-services/carEx-services/__tests__/listing-moderation/migrate-listing-moderation.test.js` returns exactly 1 (D-20 isolation)
    - Behavior: `cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/migrate-listing-moderation.test.js -x` exits 0 with `Tests: 4 passed` (or 4+)
  </acceptance_criteria>
  <done>
    Test file exists with 4+ tests; backfill correctness (7 of 10 docs updated, pre-existing 3 suspended untouched), D-16 invariants (pre/post count equal, zero missing-status post-run), D-18 idempotency (second run returns 0), and both index-creation assertions (cars: status + compound; audit: 3 indexes) all green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator ↔ migration script | The script is operator-invoked (`node scripts/migrate-listing-moderation.js` against production MongoDB). MUST be idempotent so an accidental re-run doesn't corrupt state. |
| Server startup ↔ ensureBaseline | `ensureBaseline()` runs after Mongoose connects in server.js. If its `try/catch` envelope ever throws into the bootstrap, the server fails to start — v1.0 contract "never throws into bootstrap" is critical. |
| Test sandbox ↔ MongoMemoryServer | Jest test uses an in-memory Mongo instance; production Atlas data is never touched by the test path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Tampering | running the migration twice corrupts state | mitigate | Idempotent filter `{ status: { $exists: false } }` matches zero docs on rerun (D-18); Task 2 Test 2 locks `expect(second).toBe(0)`. |
| T-07-06-02 | Denial of service | migration runs against a multi-million-row collection | accept | CarEx production collection is small-to-mid-size (no public count); `updateMany` with `{ $exists: false }` is index-able via the new `{ status: 1 }` index after first run. If scale becomes an issue, batched-write rewrite is a follow-up. |
| T-07-06-03 | Information disclosure | migration logs leak listing IDs or sensitive data | accept | Script logs only counts (modifiedCount, stillMissing), never document fields. Pattern matches v1.0 D-29 logging discipline. |
| T-07-06-04 | Repudiation | operator denies running the migration | accept | Railway deploy logs persist the migration's stdout (timestamped). [[backend_deploy_gotcha]] notes Railway deploys backend `main` only — operator must intentionally run the script, log is the audit trail. |
| T-07-06-05 | Tampering | ensureBaseline's new Car check throws and breaks server startup | mitigate | Existing `try/catch` envelope wraps the new check; v1.0 D-30 contract "never throws into bootstrap" is preserved. Task 1 acceptance grep locks the function signature + export shape unchanged. |
| T-07-06-06 | Spoofing | migration script run against the wrong database (e.g., dev DB instead of prod) | accept | Script reads `MONGODB_URI` from env; operator discipline + Railway environment scoping is the control. Mirrors v1.0 migrate-moderation.js operator-trust model. |
| T-07-06-07 | Tampering | migration script silently truncates `result.modifiedCount` reporting | mitigate | D-16 post-count invariant + Task 2 Test 1 `expect(updated).toBe(7)` independently verify modifiedCount matches the seeded missing-status count. |
</threat_model>

<verification>
After both tasks complete:

```
cd ../backend-services/carEx-services && npx jest __tests__/listing-moderation/migrate-listing-moderation.test.js -x
```

Expect: 4+ passing tests, exit 0.

Smoke (manual, OPTIONAL — does NOT run against production data): `cd ../backend-services/carEx-services && node -e "const m = require('./scripts/migrate-listing-moderation'); console.log('exports:', Object.keys(m));"` prints `exports: [ 'backfillListings', 'ensureIndexes' ]`.

Production deploy sequence (out-of-band, post-merge to backend `main` per [[backend_deploy_gotcha]]):
1. Railway picks up backend `main` merge.
2. On boot, `ensureBaseline()` logs `[Baseline] N listings missing status — run: node scripts/migrate-listing-moderation.js` if any docs lack the field.
3. Operator SSH/Railway-shell into the running instance and runs `node scripts/migrate-listing-moderation.js` manually.
4. Script exits 0; subsequent server boot's `ensureBaseline()` logs `[Baseline] All listings have status.`
</verification>

<success_criteria>
- `migrate-listing-moderation.js` exists as a Pattern F-compliant script: shebang, dotenv, idempotent filter, dual exit codes (1 for uncaught, 2 for D-16 invariant failure), `require.main` guard, exported `backfillListings` + `ensureIndexes`.
- `ensureBaseline.js` extended in place: new Car.status check inside existing try block; existing User check + function signature + export byte-identical to pre-edit.
- New test file proves: (a) 7-of-10 backfill correctness, (b) D-16 pre/post count equality, (c) D-16 zero-missing-status post-run merge-gate, (d) D-18 idempotency on rerun, (e) both Car indexes created, (f) all 3 audit indexes created.
- ROADMAP success criterion #1 mechanically delivered (in-tree CI proof); production-deploy step has its own manual run per the verification section.
</success_criteria>

<output>
After completion, create `.planning/phases/07-listing-schema-security-baseline-backend/07-06-SUMMARY.md`.
</output>
</content>
</invoke>