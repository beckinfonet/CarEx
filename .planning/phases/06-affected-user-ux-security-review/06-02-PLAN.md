---
phase: 06-affected-user-ux-security-review
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - src/constants/translations.ts
autonomous: true
requirements: [AFF-01, AFF-02, AFF-03, AFF-04, QUAL-01]
threat_refs: [T-06-02]
tags: [translations, i18n]
must_haves:
  truths:
    - "32 new keys exist in RU section of TRANSLATIONS"
    - "32 new keys exist in EN section of TRANSLATIONS"
    - "QUAL-01 translation parity test continues to pass (set-equality holds)"
    - "No key count hardcoded — baseline 459 → 491 after Phase 6"
  artifacts:
    - path: "src/constants/translations.ts"
      provides: "Banner severity copy + gate overlay copy matrix + mailto appeal fallback strings"
      contains: "bannerTitleFeatureLimited"
  key_links:
    - from: "__tests__/translation-parity.test.ts"
      to: "src/constants/translations.ts"
      via: "TRANSLATIONS import"
      pattern: "Object.keys\\(TRANSLATIONS"
---

<objective>
Add the 32 new translation keys required by Phase 6 Half A to `src/constants/translations.ts` in both RU and EN. Every key lands in parallel under an `// ---- Phase 6 — Affected-User UX ----` banner comment (same structural precedent as Phase 5 Plan 05-02 established). QUAL-01 set-equality parity test must continue to pass after this plan.

Purpose: Decouple string delivery from component delivery so Wave 2 components can reference stable `t.*` keys without plan-order coupling. The QUAL-01 parity guard from Wave 0 actively protects this plan.
Output: 64 new translation entries (32 × 2 languages), zero behavior change to existing Phase 5 and earlier keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-affected-user-ux-security-review/06-UI-SPEC.md
@.planning/phases/06-affected-user-ux-security-review/06-RESEARCH.md
@.planning/phases/06-affected-user-ux-security-review/06-PATTERNS.md
@.planning/phases/05-admin-moderation-ui-mobile/05-02-SUMMARY.md

<interfaces>
<!-- Existing file shape — RU block starts line 2, EN block starts line 533, closes line 1064 (Phase 5 final state) -->
<!-- Phase 5's moderation block lives at RU ~lines 520-530 and EN ~lines 1055-1063 with a banner comment "// ---- Phase 5 — Admin Moderation UI ----" -->

From src/constants/translations.ts (structure):
```typescript
export const TRANSLATIONS = {
  RU: {
    // ... existing keys ...
    // ---- Phase 5 — Admin Moderation UI (UI-SPEC §10) ----
    severityActive: 'Активен',
    // ... etc
  },
  EN: {
    // ... existing keys mirrored ...
    // ---- Phase 5 — Admin Moderation UI (UI-SPEC §10) ----
    severityActive: 'Active',
    // ... etc
  },
};

export type Translation = typeof TRANSLATIONS.RU;
```

Key-count guard: the current repo has RU=EN=459 keys (verified by Phase 5 Plan 05-11 final state). This plan adds 32 to each side → 491=491.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 6 keys to RU block (32 entries)</name>
  <files>src/constants/translations.ts</files>
  <read_first>
    - src/constants/translations.ts:1-35 (header + RU start structure)
    - src/constants/translations.ts:520-535 (locate existing Phase-5 moderation banner comment + find the end of RU block)
    - .planning/phases/06-affected-user-ux-security-review/06-UI-SPEC.md §Copywriting Contract (exact RU strings — copy verbatim)
    - .planning/phases/06-affected-user-ux-security-review/06-UI-SPEC.md §Clipboard Decision (the revised key list — no appealCopyEmail/appealCopied/appealCancel)
  </read_first>
  <action>
    Insert the following 32 entries in the RU block, at the END of the RU object (just before the closing `},`), preceded by a banner comment. Use EXACT strings from UI-SPEC §Copywriting Contract (per D-05 these are locked):

    ```typescript
        // ---- Phase 6 — Affected-User UX (UI-SPEC §Copywriting) ----

        // Banner severity titles (per D-01 / UI-SPEC §Banner severity copy)
        bannerTitleFeatureLimited: 'Доступ ограничен',
        bannerTitleBlockedWithReview: 'Аккаунт заблокирован',
        bannerTitlePermanentlyBanned: 'Аккаунт заблокирован навсегда',

        // Appeal CTA (banner, blocked_with_review only, per D-07)
        appealCta: 'Обжаловать',

        // Appeal mailto fallback Alert (per D-08 / UI-SPEC Clipboard Decision)
        appealNoMailTitle: 'Почтовое приложение не установлено',
        appealNoMailBody: 'Отправьте письмо на support@carexmarket.com и укажите ваш ID: {uid}',
        appealOk: 'OK',
        appealPlaceholder: '[Напишите ваше сообщение здесь]',

        // Banner expand hints (per D-02)
        expandNote: 'Нажмите, чтобы развернуть',
        collapseNote: 'Нажмите, чтобы свернуть',

        // Gate overlay CTA (feature_limited only, per UI-SPEC §Component 2 card content #4)
        restoreProfile: 'Восстановить профиль',

        // Gate overlay copy — create_listing (capability × severity × field)
        gateCreateListingFeatureLimitedTitle: 'Размещение объявлений ограничено',
        gateCreateListingFeatureLimitedBody: 'Чтобы снова размещать объявления, пройдите повторную проверку профиля.',
        gateCreateListingBlockedTitle: 'Размещение объявлений недоступно',
        gateCreateListingBlockedBody: 'Ваш аккаунт на рассмотрении. Свяжитесь с поддержкой через баннер выше.',
        gateCreateListingBannedTitle: 'Размещение объявлений запрещено',
        gateCreateListingBannedBody: 'Ваш аккаунт заблокирован навсегда.',

        // Gate overlay copy — create_order
        gateCreateOrderFeatureLimitedTitle: 'Бронирование услуг ограничено',
        gateCreateOrderFeatureLimitedBody: 'Восстановите статус аккаунта, чтобы оформлять заказы.',
        gateCreateOrderBlockedTitle: 'Бронирование недоступно',
        gateCreateOrderBlockedBody: 'Ваш аккаунт на рассмотрении. Свяжитесь с поддержкой через баннер выше.',
        gateCreateOrderBannedTitle: 'Бронирование запрещено',
        gateCreateOrderBannedBody: 'Ваш аккаунт заблокирован навсегда.',

        // Gate overlay copy — apply_as_provider (FRONTEND ALIAS per RESEARCH §Capability Contract Verification)
        gateApplyProviderFeatureLimitedTitle: 'Подача заявок ограничена',
        gateApplyProviderFeatureLimitedBody: 'Восстановите статус аккаунта, чтобы подать заявку.',
        gateApplyProviderBlockedTitle: 'Подача заявок недоступна',
        gateApplyProviderBlockedBody: 'Ваш аккаунт на рассмотрении. Свяжитесь с поддержкой через баннер выше.',
        gateApplyProviderBannedTitle: 'Подача заявок запрещена',
        gateApplyProviderBannedBody: 'Ваш аккаунт заблокирован навсегда.',

        // Gate overlay copy — contact_seller
        gateContactSellerFeatureLimitedTitle: 'Связь с продавцом ограничена',
        gateContactSellerFeatureLimitedBody: 'Восстановите статус аккаунта, чтобы связаться с продавцом.',
        gateContactSellerBlockedTitle: 'Связь с продавцом недоступна',
        gateContactSellerBlockedBody: 'Ваш аккаунт на рассмотрении. Свяжитесь с поддержкой через баннер выше.',
        gateContactSellerBannedTitle: 'Связь с продавцом запрещена',
        gateContactSellerBannedBody: 'Ваш аккаунт заблокирован навсегда.',
    ```

    Indentation: match existing file (2 or 4 spaces — preserve what's already there; do not reformat).

    Do NOT add `appealCopyEmail`, `appealCopied`, `appealCancel` — those were STRUCK by UI-SPEC Clipboard Decision (RN 0.83 removed legacy Clipboard; no package to add per CLAUDE.md). Use only `appealOk` for the single Alert button.

    CRITICAL — the reason-category labels (`reasonSpam`, `reasonPolicyViolation`, `reasonFraud`, `reasonOther`) already exist from Phase 5. Do NOT re-add them.
  </action>
  <verify>
    <automated>grep -c "bannerTitleFeatureLimited\|bannerTitleBlockedWithReview\|bannerTitlePermanentlyBanned\|appealCta\|appealNoMailTitle\|appealNoMailBody\|appealOk\|appealPlaceholder\|expandNote\|collapseNote\|restoreProfile\|gateCreateListing\|gateCreateOrder\|gateApplyProvider\|gateContactSeller" src/constants/translations.ts</automated>
  </verify>
  <done>
    - 32 new Phase 6 keys exist in file
    - Grep count of "gateCreateListing" returns exactly 6 (3 severities × 2 fields) per language — 12 total across RU+EN after Task 2
    - Grep count of "gateCreateOrder" returns exactly 6 per language
    - Grep count of "gateApplyProvider" returns exactly 6 per language
    - Grep count of "gateContactSeller" returns exactly 6 per language
    - `grep -c "appealCopyEmail\|appealCopied\|appealCancel" src/constants/translations.ts` returns 0 (struck keys not added)
    - Banner comment `// ---- Phase 6 — Affected-User UX` appears at least once in RU block
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 6 keys to EN block (32 entries) + run parity test</name>
  <files>src/constants/translations.ts, __tests__/translation-parity.test.ts</files>
  <read_first>
    - src/constants/translations.ts (the RU additions from Task 1 — keys must match 1:1)
    - .planning/phases/06-affected-user-ux-security-review/06-UI-SPEC.md §Copywriting Contract (exact EN strings)
  </read_first>
  <action>
    Insert the matching 32 EN entries at the END of the EN block (same structural position as RU), preceded by the same banner comment.

    Use EXACT strings from UI-SPEC §Copywriting Contract:

    ```typescript
        // ---- Phase 6 — Affected-User UX (UI-SPEC §Copywriting) ----

        bannerTitleFeatureLimited: 'Access limited',
        bannerTitleBlockedWithReview: 'Account blocked',
        bannerTitlePermanentlyBanned: 'Account permanently banned',

        appealCta: 'Appeal',

        appealNoMailTitle: 'No mail app installed',
        appealNoMailBody: 'Please email support@carexmarket.com and include your user ID: {uid}',
        appealOk: 'OK',
        appealPlaceholder: '[Your message here]',

        expandNote: 'Tap to expand',
        collapseNote: 'Tap to collapse',

        restoreProfile: 'Restore profile',

        gateCreateListingFeatureLimitedTitle: 'Listing creation restricted',
        gateCreateListingFeatureLimitedBody: 'Re-verify your profile to restore listing access.',
        gateCreateListingBlockedTitle: 'Listing creation unavailable',
        gateCreateListingBlockedBody: 'Your account is under review. Use the Appeal link above to contact support.',
        gateCreateListingBannedTitle: 'Listing creation permanently disabled',
        gateCreateListingBannedBody: 'Your account is permanently banned.',

        gateCreateOrderFeatureLimitedTitle: 'Booking restricted',
        gateCreateOrderFeatureLimitedBody: 'Restore your account status to place orders.',
        gateCreateOrderBlockedTitle: 'Booking unavailable',
        gateCreateOrderBlockedBody: 'Your account is under review. Use the Appeal link above to contact support.',
        gateCreateOrderBannedTitle: 'Booking permanently disabled',
        gateCreateOrderBannedBody: 'Your account is permanently banned.',

        gateApplyProviderFeatureLimitedTitle: 'Provider applications restricted',
        gateApplyProviderFeatureLimitedBody: 'Restore your account status to apply as a provider.',
        gateApplyProviderBlockedTitle: 'Provider applications unavailable',
        gateApplyProviderBlockedBody: 'Your account is under review. Use the Appeal link above to contact support.',
        gateApplyProviderBannedTitle: 'Provider applications permanently disabled',
        gateApplyProviderBannedBody: 'Your account is permanently banned.',

        gateContactSellerFeatureLimitedTitle: 'Contacting seller restricted',
        gateContactSellerFeatureLimitedBody: 'Restore your account status to contact the seller.',
        gateContactSellerBlockedTitle: 'Contacting seller unavailable',
        gateContactSellerBlockedBody: 'Your account is under review. Use the Appeal link above to contact support.',
        gateContactSellerBannedTitle: 'Contacting seller permanently disabled',
        gateContactSellerBannedBody: 'Your account is permanently banned.',
    ```

    After writing, run the QUAL-01 parity test to confirm set-equality holds:
    `npx jest __tests__/translation-parity.test.ts`

    The test MUST pass. If it fails, fix the mismatched key(s) before declaring done. Common failure mode: typo in one side's key name that didn't mirror the other.
  </action>
  <verify>
    <automated>npx jest __tests__/translation-parity.test.ts</automated>
  </verify>
  <done>
    - QUAL-01 parity test passes (RU key set === EN key set)
    - Expected key count: 459 baseline + 32 new = 491 per language (verify via `node -e "const { TRANSLATIONS } = require('./src/constants/translations'); console.log(Object.keys(TRANSLATIONS.RU).length, Object.keys(TRANSLATIONS.EN).length)"` returns `491 491`)
    - Banner comment `// ---- Phase 6 — Affected-User UX` appears at least twice in file (RU + EN)
    - `grep -c "appealCopyEmail\|appealCopied\|appealCancel" src/constants/translations.ts` returns 0
    - `grep -c "bannerTitleFeatureLimited" src/constants/translations.ts` returns 2
    - `grep -c "restoreProfile" src/constants/translations.ts` returns 2
    - `grep -c "gateApplyProvider" src/constants/translations.ts` returns 12 (6 per language)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Translation strings are consumed by end users in their native locale. Body strings interpolate `{uid}` at render time via `String.prototype.replace`.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Injection | Mailto body assembly (per T-06-02 from planning_context) | mitigate | `appealNoMailBody` interpolates `{uid}` via `.replace('{uid}', ...)` — safe literal substring substitution at the consumer (UserStatusBanner — Plan 06-03). The translation string itself contains NO user-controlled content; the substitution happens at call site AFTER user.localId is pulled from AuthContext (not attacker-controlled for this user's own banner). No HTML/URL context here — Alert body is plain text. |

Severity: low. This plan only adds static strings; the actual interpolation security lives in Plan 06-03 (mailto encoding) and is mitigated there via `encodeURIComponent`.
</threat_model>

<verification>
- Final state: 491 RU keys, 491 EN keys, sorted-set-equal
- `npx jest __tests__/translation-parity.test.ts` exits 0
- No struck keys (`appealCopyEmail`, `appealCopied`, `appealCancel`) present
- All 32 new base-names present in both sections
</verification>

<success_criteria>
- QUAL-01 parity test green
- 32 new keys × 2 languages = 64 new entries shipped
- Phase 5 keys byte-identical (no accidental mutation of existing entries)
- Banner-comment convention preserved (single Phase-6 banner per language block)
</success_criteria>

<output>
After completion, create `.planning/phases/06-affected-user-ux-security-review/06-02-SUMMARY.md`
</output>
