# Phase 6 — Continue Here

## Status

**9 of 12 plans complete.** Mobile waves (0-5) landed on `main`. Backend + security waves deferred by operator decision on 2026-04-19.

## What was built

| Plan | Wave | Summary |
|------|------|---------|
| 06-01 | 0 | TDD scaffolds for Banner/Overlay/Wrapper + translation-parity guard |
| 06-02 | 1 | 35 RU + 35 EN translation keys (baseline 459 → 494) |
| 06-03 | 2 | UserStatusBanner (19 real tests green) |
| 06-04 | 2 | FeatureGateOverlay (18 real tests green) |
| 06-05 | 2 | GatedScreenWrapper w/ apply_as_provider alias + all_writes sentinel (14 tests green) |
| 06-06 | 3 | SellCar + ServiceCart + ServiceApplication screen wraps |
| 06-07 | 3 | CarDetails inline contact_seller gate + fade Modal |
| 06-08 | 4 | App.tsx global banner mount + Android LayoutAnimation enable |
| 06-09 | 5 | Jest literal scanner for new moderation components (QUAL-01 complete) |

Full suite: **218 passing / 1 failed** — the single failure is the pre-existing
`App.test.tsx` native-stack `usesNewAndroidHeaderHeightImplementation` deferred
since Phase 5 Plan 11. No regressions introduced by Phase 6.

Requirements complete: **AFF-01, AFF-02, AFF-03, AFF-04, QUAL-01.**
Requirements pending: **QUAL-02 (load test), QUAL-03 (security sign-off).**

## What is pending

### 06-0a — Backend seed 10k users (cross-repo)

`autonomous: false`. Target repo: `backend-services/carEx-services` (separate
from this repo). Operator must:

1. Clone/open `backend-services/carEx-services`
2. Create `scripts/seed-moderation-load.js` per 06-0a-PLAN.md:
   - `insertMany({ ordered: false })` batches of 1000
   - 10k synthetic users distributed across 4 moderation states
   - Idempotent (clean or upsert)
3. Create `scripts/verify-indexes.sh` asserting index presence on
   `moderationStatus.state`, `ModerationAction.targetUid+createdAt`,
   `ModerationAction.adminUid+createdAt`
4. Run against MongoDB Atlas; commit.

### 06-0b — k6 load test harness (cross-repo)

Depends on 06-0a. `autonomous: false`. Operator must:

1. Create `scripts/load-test/admin-search.k6.js` per 06-0b-PLAN.md
2. Auth ONCE in `setup()` using `K6_ADMIN_IDTOKEN` env var (or mint test admin)
3. Threshold: `http_req_duration[p(95)] < 200`
4. Exercise `GET /api/admin/users/search` and `GET /api/admin/moderation/:uid/history`
5. Add `k6` binary output paths to `.gitignore`
6. Run, capture results, commit the harness.

### 06-10 — Security review artifact

`autonomous: false`. Depends on all prior plans including 06-0a + 06-0b.
Operator must write `06-SECURITY.md` with 5 sections (a)-(e) matching
ROADMAP §Phase 6 Success Criterion 6 verbatim:

- a) verifyIdToken runs on every admin route
- b) no `callerUid` body param trusted for authz on any new route
- c) suspend + confirm-booking mutations are transactional
- d) ModerationAction rejects updates/deletes at app layer
- e) no new hardcoded secrets

Each section: Verification / Evidence (grep + test commands) / Verdict. All
5 verdicts must be PASS or the artifact is explicitly operator-blocked.

## Resume commands

```bash
# When ready to finish Phase 6:
/gsd-execute-phase 6                    # picks up from 06-0a (only incompletes remain)
# or, for the cross-repo plans specifically:
cd backend-services/carEx-services
/gsd-execute-plan 06-0a                 # inside the backend repo
```

## Critical anti-patterns (severity: blocking)

None.

## Notes

- Provider stack order in `App.tsx` is preserved per CLAUDE.md contract.
  Banner sits inside `NavigationContainer`, zIndex 9998 < OfflineNotice 9999.
- `apply_as_provider` is a **frontend alias** — it gates when EITHER
  `request_broker_role` OR `request_logistics_role` is in `restrictedFeatures`.
  Do not revert to single-key lookup (documented in `06-RESEARCH.md §Pitfall 6`).
- Translation parity: never hardcode key counts. The parity test uses
  set-equality to tolerate future additions.
