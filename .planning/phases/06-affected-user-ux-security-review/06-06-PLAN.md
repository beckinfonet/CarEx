---
phase: 06-affected-user-ux-security-review
plan: 06
type: execute
wave: 3
depends_on: [06-05]
files_modified:
  - src/screens/SellCarScreen.tsx
  - src/screens/ServiceCartScreen.tsx
  - src/screens/ServiceApplicationScreen.tsx
autonomous: true
requirements: [AFF-04]
threat_refs: [T-06-03]
tags: [screen-integration, wrapping]
must_haves:
  truths:
    - "SellCarScreen wraps its body in GatedScreenWrapper capability='create_listing'"
    - "ServiceCartScreen wraps its body in GatedScreenWrapper capability='create_order'"
    - "ServiceApplicationScreen wraps its body in GatedScreenWrapper capability='apply_as_provider' (NOT per-role)"
    - "Each wrap is a 1-import + 2-JSX-line diff with existing body unchanged"
  artifacts:
    - path: "src/screens/SellCarScreen.tsx"
      provides: "Gated listing creation"
      contains: "GatedScreenWrapper"
    - path: "src/screens/ServiceCartScreen.tsx"
      provides: "Gated service-booking checkout"
      contains: "GatedScreenWrapper"
    - path: "src/screens/ServiceApplicationScreen.tsx"
      provides: "Gated provider applications (broker + logistics via frontend alias)"
      contains: "apply_as_provider"
  key_links:
    - from: "src/screens/SellCarScreen.tsx"
      to: "src/components/moderation/GatedScreenWrapper"
      via: "JSX wrap of SafeAreaView body"
      pattern: "<GatedScreenWrapper capability=\"create_listing\""
    - from: "src/screens/ServiceCartScreen.tsx"
      to: "src/components/moderation/GatedScreenWrapper"
      via: "JSX wrap"
      pattern: "<GatedScreenWrapper capability=\"create_order\""
    - from: "src/screens/ServiceApplicationScreen.tsx"
      to: "src/components/moderation/GatedScreenWrapper"
      via: "JSX wrap"
      pattern: "<GatedScreenWrapper capability=\"apply_as_provider\""
---

<objective>
Wrap the three full-screen gated surfaces (`SellCarScreen`, `ServiceCartScreen`, `ServiceApplicationScreen`) in `GatedScreenWrapper` with the correct capability key. Each modification is a minimal diff: 1 new import + 2 JSX lines (open + close tag) around the existing screen body. No other changes to screen logic.

Purpose: Deliver AFF-04 for the three full-screen gated surfaces named in ROADMAP §Phase 6 Success Criterion 3. `CarDetailsScreen` CTA-only gating is plan 06-07 (separate shape).
Output: Three wrapped screens — pre-existing code byte-identical inside the wrapper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-affected-user-ux-security-review/06-UI-SPEC.md
@.planning/phases/06-affected-user-ux-security-review/06-PATTERNS.md
@src/components/moderation/GatedScreenWrapper.tsx

<interfaces>
Wrapper from Plan 06-05:
```typescript
export const GatedScreenWrapper: React.FC<{
  capability: 'create_listing' | 'create_order' | 'apply_as_provider' | 'contact_seller';
  children: React.ReactNode;
}>;
```

Imports — consistent across all three screens:
```typescript
import { GatedScreenWrapper } from '../components/moderation/GatedScreenWrapper';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap SellCarScreen with capability="create_listing"</name>
  <files>src/screens/SellCarScreen.tsx</files>
  <read_first>
    - src/screens/SellCarScreen.tsx:1-40 (imports)
    - src/screens/SellCarScreen.tsx:496-510 (top-level return JSX — SafeAreaView + header)
    - .planning/phases/06-affected-user-ux-security-review/06-PATTERNS.md §SellCarScreen.tsx (exact diff sketch)
    - src/components/moderation/GatedScreenWrapper.tsx (wrapper API)
  </read_first>
  <action>
    Modify `src/screens/SellCarScreen.tsx`:

    **Step 1 — Add import.** Insert after the last `../components/...` import:
    ```typescript
    import { GatedScreenWrapper } from '../components/moderation/GatedScreenWrapper';
    ```

    **Step 2 — Wrap body JSX.** Locate the top-level return. It currently looks like:
    ```tsx
    return (
      <SafeAreaView style={styles.container}>
        <View style={styles.header}>
          ...
        </View>
        ... rest of screen ...
      </SafeAreaView>
    );
    ```

    Change to:
    ```tsx
    return (
      <SafeAreaView style={styles.container}>
        <GatedScreenWrapper capability="create_listing">
          <View style={styles.header}>
            ...
          </View>
          ... rest of screen ...
        </GatedScreenWrapper>
      </SafeAreaView>
    );
    ```

    - Leave `SafeAreaView` OUTSIDE the wrapper (so insets apply identically regardless of gated state, matching UI-SPEC §Integration Seam)
    - ALL existing JSX between the `<SafeAreaView>` open tag and `</SafeAreaView>` close tag must be included inside the wrapper — do NOT change internal structure or indent levels of the wrapped content beyond adding 2 spaces of indentation
    - Do NOT touch component logic, state, styles, or handlers

    Run `npx tsc --noEmit` after the change — expect zero new TS errors.

    Manual sanity on the rendered layout is a Wave 7 UAT concern, not this task.
  </action>
  <verify>
    <automated>grep -c "GatedScreenWrapper capability=\"create_listing\"" src/screens/SellCarScreen.tsx</automated>
  </verify>
  <done>
    - `grep -c "from '../components/moderation/GatedScreenWrapper'" src/screens/SellCarScreen.tsx` equals 1
    - `grep -c "<GatedScreenWrapper capability=\"create_listing\">" src/screens/SellCarScreen.tsx` equals 1
    - `grep -c "</GatedScreenWrapper>" src/screens/SellCarScreen.tsx` equals 1
    - `npx tsc --noEmit` passes with no new errors related to SellCarScreen
    - `git diff src/screens/SellCarScreen.tsx` shows net additions are approximately 3 lines (1 import + 2 JSX tag lines) — actual line count may vary slightly due to formatting but NO existing logic lines are deleted
  </done>
</task>

<task type="auto">
  <name>Task 2: Wrap ServiceCartScreen and ServiceApplicationScreen</name>
  <files>src/screens/ServiceCartScreen.tsx, src/screens/ServiceApplicationScreen.tsx</files>
  <read_first>
    - src/screens/ServiceCartScreen.tsx:1-80 (imports + top-level return)
    - src/screens/ServiceApplicationScreen.tsx:1-100 (imports + top-level return; this screen dispatches on route.params.type ∈ {broker, logistics})
    - .planning/phases/06-affected-user-ux-security-review/06-PATTERNS.md §ServiceCartScreen.tsx + §ServiceApplicationScreen.tsx
    - .planning/phases/06-affected-user-ux-security-review/06-RESEARCH.md §Pitfall 5 (do NOT branch on route.params.type — single capability="apply_as_provider" via alias)
  </read_first>
  <action>
    **File 1 — `src/screens/ServiceCartScreen.tsx`:**
    Same wrap pattern as Task 1 but with `capability="create_order"`:
    - Add: `import { GatedScreenWrapper } from '../components/moderation/GatedScreenWrapper';`
    - Wrap the inside of the top-level `<SafeAreaView>` (or equivalent root View) with `<GatedScreenWrapper capability="create_order">...</GatedScreenWrapper>`

    **File 2 — `src/screens/ServiceApplicationScreen.tsx`:**
    Same wrap pattern with `capability="apply_as_provider"`:
    - Add: `import { GatedScreenWrapper } from '../components/moderation/GatedScreenWrapper';`
    - Wrap inside top-level `<SafeAreaView>` with `<GatedScreenWrapper capability="apply_as_provider">...</GatedScreenWrapper>`

    **CRITICAL** — do NOT branch on `route.params.type` to choose `request_broker_role` vs `request_logistics_role`. The FRONTEND ALIAS in `GatedScreenWrapper` (Plan 06-05) already resolves `apply_as_provider` to both backend keys transparently. RESEARCH §Pitfall 5 + §Capability Contract Verification Path 1 (recommended) locks this to a single capability key.

    For BOTH files:
    - Leave SafeAreaView + any root-level context providers OUTSIDE the wrapper
    - No logic/style/handler changes
    - `npx tsc --noEmit` must pass after both changes
  </action>
  <verify>
    <automated>grep -c "GatedScreenWrapper capability=\"create_order\"" src/screens/ServiceCartScreen.tsx && grep -c "GatedScreenWrapper capability=\"apply_as_provider\"" src/screens/ServiceApplicationScreen.tsx</automated>
  </verify>
  <done>
    - `grep -c "<GatedScreenWrapper capability=\"create_order\">" src/screens/ServiceCartScreen.tsx` equals 1
    - `grep -c "</GatedScreenWrapper>" src/screens/ServiceCartScreen.tsx` equals 1
    - `grep -c "<GatedScreenWrapper capability=\"apply_as_provider\">" src/screens/ServiceApplicationScreen.tsx` equals 1
    - `grep -c "</GatedScreenWrapper>" src/screens/ServiceApplicationScreen.tsx` equals 1
    - `grep -c "request_broker_role\|request_logistics_role" src/screens/ServiceApplicationScreen.tsx` equals 0 (no per-route-type branch — alias does the work)
    - `grep -c "from '../components/moderation/GatedScreenWrapper'" src/screens/ServiceCartScreen.tsx src/screens/ServiceApplicationScreen.tsx` equals 2
    - `npx tsc --noEmit` passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Screens consume the GatedScreenWrapper contract from Plan 06-05. The wrapper owns the predicate; these plans own only the wrap JSX.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Elevation of privilege (bypass) | Screen-level wrap completeness | mitigate | Backend Phase 3 ENF-01 is the authoritative denial; these wraps are UX only. Risk here is a ScrollView / nested interactive element bypassing `pointerEvents="none"` cascade on Android. Acceptance: all three screens show the Gated wrapper open+close tag exactly once (grep count = 1 each). UAT check on Android is a Manual Verification in 06-VALIDATION.md. |

Severity: low. If a ScrollView inside the wrapped body bypasses pointerEvents, the backend still returns 403. UX degradation, not privilege escalation.
</threat_model>

<verification>
- All three screens wrap with correct capability key
- No per-role branching in ServiceApplicationScreen (alias handles broker+logistics)
- `npx tsc --noEmit` clean
- Pre-existing logic byte-identical (only indentation + wrap tags added)
</verification>

<success_criteria>
- AFF-04 full-screen gates deliver on three of four surfaces
- Minimal diff policy: each screen modified with only import + wrap
- Alias map (Plan 06-05) proves its worth — ServiceApplicationScreen stays single-capability
</success_criteria>

<output>
After completion, create `.planning/phases/06-affected-user-ux-security-review/06-06-SUMMARY.md`
</output>
