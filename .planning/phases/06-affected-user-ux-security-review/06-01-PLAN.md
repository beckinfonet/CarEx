---
phase: 06-affected-user-ux-security-review
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - __tests__/components/moderation/UserStatusBanner.test.tsx
  - __tests__/components/moderation/FeatureGateOverlay.test.tsx
  - __tests__/components/moderation/GatedScreenWrapper.test.tsx
  - __tests__/translation-parity.test.ts
autonomous: true
requirements: [AFF-01, AFF-02, AFF-03, AFF-04, QUAL-01]
threat_refs: []
tags: [tdd, scaffolds, wave-0]
must_haves:
  truths:
    - "Four test files exist before any feature file is written"
    - "Each scaffold imports from the not-yet-existing module path it covers"
    - "Every Phase-6 feature task has a pre-existing automated verify target"
  artifacts:
    - path: "__tests__/components/moderation/UserStatusBanner.test.tsx"
      provides: "Wave-0 scaffold with test.todo entries for banner behavior"
      contains: "test.todo"
    - path: "__tests__/components/moderation/FeatureGateOverlay.test.tsx"
      provides: "Wave-0 scaffold with test.todo entries for overlay rendering"
      contains: "test.todo"
    - path: "__tests__/components/moderation/GatedScreenWrapper.test.tsx"
      provides: "Wave-0 scaffold including all_writes sentinel + alias branches"
      contains: "all_writes"
    - path: "__tests__/translation-parity.test.ts"
      provides: "QUAL-01 parity guard scaffold"
      contains: "translation-parity"
  key_links:
    - from: "__tests__/components/moderation/UserStatusBanner.test.tsx"
      to: "src/components/moderation/UserStatusBanner"
      via: "import path"
      pattern: "from '.*UserStatusBanner'"
    - from: "__tests__/components/moderation/FeatureGateOverlay.test.tsx"
      to: "src/components/moderation/FeatureGateOverlay"
      via: "import path"
      pattern: "from '.*FeatureGateOverlay'"
    - from: "__tests__/components/moderation/GatedScreenWrapper.test.tsx"
      to: "src/components/moderation/GatedScreenWrapper"
      via: "import path"
      pattern: "from '.*GatedScreenWrapper'"
---

<objective>
Create Wave-0 test scaffolds for all three new Phase 6 components and the QUAL-01 translation parity test. Scaffolds use `test.todo` entries and import from not-yet-existing module paths so Wave 2+ plans inherit both a real `<automated>` verify target and a compile-time wiring check. Mirrors the Phase 5 Plan 05-01 scaffold-first pattern.

Purpose: Lock the test contract (behaviors to be implemented) before any feature code. Every behavior called out in UI-SPEC, the capability-key alias contract, the `all_writes` sentinel branch, and the mailto appeal flow surfaces as a named `test.todo` here.
Output: Four jest test files staged for Wave 2/5/later plans to fill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-affected-user-ux-security-review/06-CONTEXT.md
@.planning/phases/06-affected-user-ux-security-review/06-UI-SPEC.md
@.planning/phases/06-affected-user-ux-security-review/06-RESEARCH.md
@.planning/phases/06-affected-user-ux-security-review/06-PATTERNS.md
@.planning/phases/05-admin-moderation-ui-mobile/05-01-SUMMARY.md

<interfaces>
<!-- Reference: src/components/moderation/__tests__/EmptyState.test.tsx (minimal file shape) -->
<!-- Reference: src/components/moderation/__tests__/SeverityBadge.test.tsx (testRenderer + translation mock pattern) -->

From Phase 4 ModerationService.ts:108-122 (moderationStatus shape the scaffolds mock):
```typescript
type ModerationState = 'active' | 'feature_limited' | 'blocked_with_review' | 'permanently_banned';
type ModerationStatus = {
  state: ModerationState;
  reasonCategory?: 'spam' | 'policy_violation' | 'fraud' | 'other' | null;
  note?: string | null;
  setByAdminUid?: string | null;
  setAt?: string | null;
  restrictedFeatures?: string[];
};
```

From UI-SPEC §testID Manifest (scaffolds assert against these IDs):
- `user-status-banner`, `user-status-banner-note`, `user-status-banner-appeal`
- `user-status-banner-icon-feature_limited`, `user-status-banner-icon-blocked_with_review`, `user-status-banner-icon-permanently_banned`
- `feature-gate-overlay-dim`, `feature-gate-overlay-card`, `feature-gate-overlay-cta`
- `gated-screen-wrapper-{capability}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold UserStatusBanner.test.tsx</name>
  <files>__tests__/components/moderation/UserStatusBanner.test.tsx</files>
  <read_first>
    - src/components/moderation/__tests__/SeverityBadge.test.tsx (full file — reuse the TestRenderer + useLanguage mock skeleton)
    - .planning/phases/06-affected-user-ux-security-review/06-UI-SPEC.md §Component 1 + §testID Manifest (establishes the behaviors to scaffold)
    - .planning/phases/06-affected-user-ux-security-review/06-PATTERNS.md §UserStatusBanner.tsx (banner pattern — 13 test.todo entries derived from this)
  </read_first>
  <action>
    Create `__tests__/components/moderation/UserStatusBanner.test.tsx`.

    Imports: `import { UserStatusBanner } from '../../../src/components/moderation/UserStatusBanner';` (path will not resolve until Wave 2 — this is intentional scaffold wiring).

    Describe block: `describe('UserStatusBanner (Phase 6 — AFF-01, AFF-02, AFF-03)', () => { ... })`.

    Add these `test.todo` entries VERBATIM (13 total):

    ```ts
    // AFF-01 — non-dismissable render-when-non-active
    test.todo('returns null when user.moderationStatus.state === "active"');
    test.todo('returns null when user is undefined (signed-out)');
    test.todo('renders with testID user-status-banner when state === "feature_limited"');
    test.todo('renders with testID user-status-banner when state === "blocked_with_review"');
    test.todo('renders with testID user-status-banner when state === "permanently_banned"');

    // AFF-01 — severity palette + icon picks from UI-SPEC
    test.todo('uses palette featureLimited bg + border + AlertTriangle icon when state === "feature_limited"');
    test.todo('uses palette blockedReview bg + border + ShieldAlert icon when state === "blocked_with_review"');
    test.todo('uses palette permaBanned bg + border + Ban icon when state === "permanently_banned"');

    // AFF-02 — reason category + verbatim note
    test.todo('renders localized reason-category chip text for each reasonCategory key (spam/policy_violation/fraud/other)');
    test.todo('renders note verbatim on line 2, numberOfLines=2 when collapsed');
    test.todo('tapping line-2 note Pressable fires LayoutAnimation.configureNext then toggles expanded state (numberOfLines undefined when expanded)');
    test.todo('hides line 2 entirely when note is null or empty string');

    // AFF-03 — Appeal CTA visibility + mailto build
    test.todo('renders Appeal CTA (user-status-banner-appeal) ONLY when state === "blocked_with_review"');
    test.todo('does NOT render Appeal CTA when state === "feature_limited" or "permanently_banned"');
    test.todo('tapping Appeal CTA calls Linking.openURL with mailto:support@carexmarket.com, subject "CarEx moderation appeal — {localId}", and body containing User ID / Reason category / Suspended (setAt) lines encoded via encodeURIComponent');
    test.todo('Linking.openURL.catch fires Alert.alert with appealNoMailTitle + appealNoMailBody (containing uid) + single OK button on mailto failure');
    ```

    Use `jest.mock` calls at top-of-file for `../../../src/context/AuthContext` (exposes `useAuth`), `../../../src/context/LanguageContext` (exposes `useLanguage`), `react-native-safe-area-context` (stub `useSafeAreaInsets`), and `@react-navigation/native` (stub `useFocusEffect`). Keep mocks skeletal — scaffolds don't run assertions yet; they just have to compile.

    Export nothing (test files are side-effect only). File must be syntactically valid TypeScript so `jest --listTests` picks it up.
  </action>
  <verify>
    <automated>npx jest __tests__/components/moderation/UserStatusBanner.test.tsx --listTests</automated>
  </verify>
  <done>
    - File exists at __tests__/components/moderation/UserStatusBanner.test.tsx
    - `grep -c "test.todo" __tests__/components/moderation/UserStatusBanner.test.tsx` returns >= 13
    - `grep -q "from '.*UserStatusBanner'" __tests__/components/moderation/UserStatusBanner.test.tsx`
    - `npx jest --listTests` includes the file without syntax error
    - Each of the 3 severity-icon test.todos is present (grep for AlertTriangle, ShieldAlert, Ban)
    - Mailto test.todo references "encodeURIComponent" and "setAt" literally
  </done>
</task>

<task type="auto">
  <name>Task 2: Scaffold FeatureGateOverlay.test.tsx and GatedScreenWrapper.test.tsx</name>
  <files>__tests__/components/moderation/FeatureGateOverlay.test.tsx, __tests__/components/moderation/GatedScreenWrapper.test.tsx</files>
  <read_first>
    - src/components/moderation/__tests__/EmptyState.test.tsx (full file — minimal TestRenderer shape)
    - .planning/phases/06-affected-user-ux-security-review/06-UI-SPEC.md §Component 2 + §Component 3 (overlay + wrapper contracts)
    - .planning/phases/06-affected-user-ux-security-review/06-PATTERNS.md §GatedScreenWrapper (CAPABILITY_ALIASES + all_writes predicate — CRITICAL)
    - .planning/phases/06-affected-user-ux-security-review/06-RESEARCH.md §Capability Contract Verification + §Pitfall 6
  </read_first>
  <action>
    **File 1: `__tests__/components/moderation/FeatureGateOverlay.test.tsx`**

    Imports: `import { FeatureGateOverlay } from '../../../src/components/moderation/FeatureGateOverlay';` (will not resolve until Wave 2).

    Describe: `describe('FeatureGateOverlay (Phase 6 — AFF-04)', () => { ... })`.

    `test.todo` entries VERBATIM (10):
    ```ts
    test.todo('returns null when user.moderationStatus.state === "active"');
    test.todo('renders dim layer (feature-gate-overlay-dim) with backgroundColor rgba(15, 17, 21, 0.7)');
    test.todo('renders overlay card (feature-gate-overlay-card) with borderLeftWidth 4 and borderLeftColor matching severity palette');
    test.todo('uses AlertTriangle icon for feature_limited severity');
    test.todo('uses ShieldAlert icon for blocked_with_review severity');
    test.todo('uses Ban icon for permanently_banned severity');
    test.todo('looks up title + body from t[gate{Capability}{Severity}Title/Body] — capability-key driven copy per D-05');
    test.todo('renders Restore-profile CTA (feature-gate-overlay-cta) ONLY when severity === "feature_limited"');
    test.todo('does NOT render any CTA when severity === "blocked_with_review" (banner owns the appeal CTA per D-06)');
    test.todo('does NOT render any CTA when severity === "permanently_banned"');
    ```

    **File 2: `__tests__/components/moderation/GatedScreenWrapper.test.tsx`**

    Imports: `import { GatedScreenWrapper } from '../../../src/components/moderation/GatedScreenWrapper';`.

    Describe: `describe('GatedScreenWrapper (Phase 6 — AFF-04 — capability alias + all_writes sentinel)', () => { ... })`.

    `test.todo` entries VERBATIM (12):
    ```ts
    // Pass-through when not gated
    test.todo('returns children unchanged when state === "active"');
    test.todo('returns children unchanged when state === "feature_limited" but capability not in restrictedFeatures');

    // feature_limited — specific capability key path
    test.todo('gates when state === "feature_limited" and restrictedFeatures includes "create_listing" for capability="create_listing"');
    test.todo('gates when state === "feature_limited" and restrictedFeatures includes "create_order" for capability="create_order"');
    test.todo('gates when state === "feature_limited" and restrictedFeatures includes "contact_seller" for capability="contact_seller"');

    // apply_as_provider FRONTEND ALIAS — critical finding (RESEARCH §Capability Contract Verification)
    test.todo('gates capability="apply_as_provider" when restrictedFeatures includes "request_broker_role" (frontend alias)');
    test.todo('gates capability="apply_as_provider" when restrictedFeatures includes "request_logistics_role" (frontend alias)');
    test.todo('does NOT gate capability="apply_as_provider" when restrictedFeatures contains only unrelated keys (e.g. ["request_seller_role"]) — alias map is exactly {request_broker_role, request_logistics_role}');

    // all_writes sentinel branch — critical finding (RESEARCH §Pitfall 6)
    test.todo('gates ALL capabilities when state === "blocked_with_review" and restrictedFeatures === ["all_writes"] (sentinel path)');
    test.todo('gates ALL capabilities when state === "permanently_banned" and restrictedFeatures === ["all_writes"] (sentinel path)');

    // Render structure
    test.todo('wraps children in View with pointerEvents="none" when gated');
    test.todo('renders <FeatureGateOverlay capability={capability} /> as a sibling (not a child) of the pointerEvents=none subtree');
    test.todo('sets testID="gated-screen-wrapper-{capability}" on the outer wrapper View when gated');
    ```

    Both files: mock `../../../src/context/AuthContext` (useAuth) and `../../../src/context/LanguageContext` (useLanguage) skeletally so the files compile. Do NOT import React if unused; if TypeScript complains about unused React, use `// eslint-disable-line @typescript-eslint/no-unused-vars`.

    CRITICAL: the GatedScreenWrapper scaffold MUST include both the `apply_as_provider` alias branches AND the `all_writes` sentinel branches — these correct UI-SPEC's implementation sketch per RESEARCH §Pitfall 6. Any future Wave 2 executor MUST satisfy these test.todos, preventing them from copying UI-SPEC's buggy sketch verbatim.
  </action>
  <verify>
    <automated>npx jest __tests__/components/moderation/FeatureGateOverlay.test.tsx __tests__/components/moderation/GatedScreenWrapper.test.tsx --listTests</automated>
  </verify>
  <done>
    - Both files exist
    - `grep -c "test.todo" __tests__/components/moderation/FeatureGateOverlay.test.tsx` >= 10
    - `grep -c "test.todo" __tests__/components/moderation/GatedScreenWrapper.test.tsx` >= 12
    - `grep -c "all_writes" __tests__/components/moderation/GatedScreenWrapper.test.tsx` >= 2
    - `grep -c "apply_as_provider" __tests__/components/moderation/GatedScreenWrapper.test.tsx` >= 3
    - `grep -c "request_broker_role" __tests__/components/moderation/GatedScreenWrapper.test.tsx` >= 1
    - `grep -c "request_logistics_role" __tests__/components/moderation/GatedScreenWrapper.test.tsx` >= 1
    - Both files appear in `npx jest --listTests` output
  </done>
</task>

<task type="auto">
  <name>Task 3: Scaffold translation-parity.test.ts</name>
  <files>__tests__/translation-parity.test.ts</files>
  <read_first>
    - src/constants/translations.ts (top 30 lines to confirm TRANSLATIONS export shape)
    - .planning/phases/06-affected-user-ux-security-review/06-PATTERNS.md §translation-parity.test.ts (proposed source — copy shape)
    - .planning/phases/06-affected-user-ux-security-review/06-RESEARCH.md §Pattern 6 (set-equality pattern) + §Pitfall 8 (do NOT hardcode key count)
  </read_first>
  <action>
    Create `__tests__/translation-parity.test.ts`.

    Imports: `import { TRANSLATIONS } from '../src/constants/translations';`.

    Describe: `describe('QUAL-01: translation parity', () => { ... })`.

    Because this scaffold must RUN green as soon as Wave 1 adds keys in parallel RU/EN form, the test bodies here are REAL (not test.todo). Use set-equality only (per Pitfall 8 — never hardcode a count):

    ```ts
    describe('QUAL-01: translation parity', () => {
      const ru = Object.keys((TRANSLATIONS as any).RU).sort();
      const en = Object.keys((TRANSLATIONS as any).EN).sort();

      test('RU and EN key sets are identical', () => {
        const onlyInRu = ru.filter(k => !en.includes(k));
        const onlyInEn = en.filter(k => !ru.includes(k));
        expect({ onlyInRu, onlyInEn }).toEqual({ onlyInRu: [], onlyInEn: [] });
      });

      test('every value is a non-empty string', () => {
        for (const lang of ['RU', 'EN'] as const) {
          for (const [key, val] of Object.entries((TRANSLATIONS as any)[lang])) {
            expect(typeof val).toBe('string');
            expect((val as string).trim().length).toBeGreaterThan(0);
          }
        }
      });

      test('no TODO/FIXME/TRANSLATE placeholder values', () => {
        for (const lang of ['RU', 'EN'] as const) {
          for (const [key, val] of Object.entries((TRANSLATIONS as any)[lang])) {
            expect(val).not.toMatch(/^(TODO|FIXME|TRANSLATE)[:\s]/i);
          }
        }
      });
    });
    ```

    DO NOT assert a specific count like `expect(ru.length).toBe(487)`. The pre-Phase-6 baseline is 459 (NOT 455 as UI-SPEC claims); Phase-6 will add 32 → 491. Using set-equality makes the test robust to any count.

    The file must pass AS-IS against current `src/constants/translations.ts` (i.e. Phase 5's committed state with RU=EN sorted-set-equal) so it ships green from Wave 0 and provides a live guard through Wave 1.
  </action>
  <verify>
    <automated>npx jest __tests__/translation-parity.test.ts</automated>
  </verify>
  <done>
    - File exists at __tests__/translation-parity.test.ts
    - `grep -c "TRANSLATIONS" __tests__/translation-parity.test.ts` >= 3
    - `grep -c "Object.keys" __tests__/translation-parity.test.ts` >= 2
    - `grep -c "toEqual" __tests__/translation-parity.test.ts` >= 1
    - `grep -v "expect(ru.length).toBe" __tests__/translation-parity.test.ts` — no hardcoded count assertion
    - `npx jest __tests__/translation-parity.test.ts` exits 0 (tests pass against current baseline)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 6 Wave 0 creates TEST files only — no runtime trust boundaries crossed. The scaffolds themselves are not production code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | Test scaffolds (false-positive lock-in) | accept | Scaffolds use `test.todo` so they produce skipped tests, not false greens. Wave 2+ executors MUST convert todos to real assertions before declaring completion. |

No high-severity threats; this plan creates test files only.
</threat_model>

<verification>
After all 3 tasks complete:
- `npx jest --listTests | grep -E "(UserStatusBanner|FeatureGateOverlay|GatedScreenWrapper|translation-parity)"` lists all 4 files
- `npx jest __tests__/translation-parity.test.ts` exits 0 (real assertions pass against current baseline)
- `npx jest __tests__/components/moderation/UserStatusBanner.test.tsx __tests__/components/moderation/FeatureGateOverlay.test.tsx __tests__/components/moderation/GatedScreenWrapper.test.tsx` reports all as "todo" (no failures, no unexpected passes)
- `grep -rc "test.todo" __tests__/components/moderation/` returns at minimum 35 across the 3 new files
</verification>

<success_criteria>
- Four new test files exist and compile
- `translation-parity.test.ts` passes against current baseline (RU=EN already parity at 459=459)
- Three component scaffolds have >= 35 combined `test.todo` entries covering: AFF-01 render contract, AFF-02 reason + note, AFF-03 mailto + fallback, AFF-04 overlay + wrapper predicate, `apply_as_provider` alias, `all_writes` sentinel
- No implementation code exists yet — the scaffolds import from paths that Wave 2 will create
</success_criteria>

<output>
After completion, create `.planning/phases/06-affected-user-ux-security-review/06-01-SUMMARY.md`
</output>
