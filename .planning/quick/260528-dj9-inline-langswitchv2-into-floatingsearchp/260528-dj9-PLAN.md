---
phase: 260528-dj9-inline-langswitchv2-into-floatingsearchp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/v2/FloatingSearchPill.tsx
  - src/screens/HomeScreenV2.tsx
autonomous: true
requirements:
  - QUICK-DJ9
must_haves:
  truths:
    - "LangSwitchV2 renders on the same horizontal row as FloatingSearchPill, to the right of the filter button."
    - "No new vertical row above GreetingBlock — content position is unchanged versus the pre-d9a baseline."
    - "Existing FloatingSearchPill test still passes (trailing is optional, no test changes)."
    - "Tapping LangSwitchV2 toggles language; tapping search pill navigates; tapping filter button opens filter modal."
  artifacts:
    - path: "src/components/home/v2/FloatingSearchPill.tsx"
      provides: "FloatingSearchPill with optional `trailing` slot rendered as a sibling of the pill inside a row-flex wrapper."
      contains: "trailing?: React.ReactNode"
    - path: "src/screens/HomeScreenV2.tsx"
      provides: "HomeScreenV2 with the LangSwitchV2 row removed and passed via FloatingSearchPill `trailing` prop instead."
      contains: "trailing={<LangSwitchV2"
  key_links:
    - from: "src/screens/HomeScreenV2.tsx"
      to: "src/components/home/v2/FloatingSearchPill.tsx"
      via: "trailing prop carrying <LangSwitchV2 language={language} setLanguage={setLanguage} />"
      pattern: "trailing=\\{<LangSwitchV2"
---

<objective>
Inline LangSwitchV2 into FloatingSearchPill via a new `trailing` slot so the language toggle sits on the SAME horizontal row as the search pill (right of the filter button), eliminating the extra vertical row introduced by the previous fix (260528-d9a).

Purpose: Restore vertical layout density. The previous fix added a flex-end row above GreetingBlock, which pushed all hero/feed content down. User wants zero new vertical space — LangSwitchV2 should ride alongside the pill.

Output:
- `FloatingSearchPill.tsx` gains an optional `trailing` prop and a row-flex wrapper, rendering the trailing node as a sibling AFTER the pill.
- `HomeScreenV2.tsx` removes the `flex-end / paddingTop: 8` wrapper containing LangSwitchV2 and instead passes `trailing={<LangSwitchV2 ... />}` into the FloatingSearchPill JSX.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@src/components/home/v2/FloatingSearchPill.tsx
@src/components/home/v2/__tests__/FloatingSearchPill.test.tsx
@src/screens/HomeScreenV2.tsx
@src/components/home/v2/LangSwitchV2.tsx

<interfaces>
<!-- Concrete contracts the executor needs. No codebase exploration required. -->

Current FloatingSearchPillProps (src/components/home/v2/FloatingSearchPill.tsx:7-11):
  interface FloatingSearchPillProps {
    placeholder: string;
    onPress: () => void;
    onFiltersPress: () => void;
  }

Current wrapper style (src/components/home/v2/FloatingSearchPill.tsx:33-36):
  wrapper: {
    paddingHorizontal: 18,
    paddingTop: 12,
  }

Current pill style (src/components/home/v2/FloatingSearchPill.tsx:37-48): row-flex pill with height 48,
  paddingLeft 18, paddingRight 6, dark bg, V2.borderHi border, V2.radius.pill, gap 10. Internal children
  are Search icon, placeholder Text, and filtersButton TouchableOpacity. These children stay byte-identical.

Existing test (src/components/home/v2/__tests__/FloatingSearchPill.test.tsx) does NOT pass `trailing` and
calls touchables[0] (outer pill) → onPress and touchables[1] (filtersButton) → onFiltersPress. Since
trailing is optional and rendered AFTER the pill, no additional TouchableOpacity is introduced when the
prop is absent, so the test continues to pass without modification.

LangSwitchV2 (src/components/home/v2/LangSwitchV2.tsx:10): exported as a named export.
  <LangSwitchV2 language={language} setLanguage={setLanguage} />
The component already uses `alignSelf: 'flex-end'` internally; that style is harmless inside a row-flex
parent and does not need to be removed.

HomeScreenV2 hook usage (src/screens/HomeScreenV2.tsx:41):
  const { t, language, setLanguage } = useLanguage();
This destructure stays as-is; `language` and `setLanguage` now flow into the trailing JSX.

Existing wrapper to remove (src/screens/HomeScreenV2.tsx:95-97):
  <View style={{ alignItems: 'flex-end', paddingTop: 8 }}>
    <LangSwitchV2 language={language} setLanguage={setLanguage} />
  </View>
This is the FIRST child of the `Header = (<> ... </>)` fragment. Delete these three lines so the
fragment now begins with `<GreetingBlock ... />`.

Existing FloatingSearchPill call site (src/screens/HomeScreenV2.tsx:144-148):
  <FloatingSearchPill
    placeholder={t.searchPlaceholderV2}
    onPress={handleSearchPress}
    onFiltersPress={handleFiltersPress}
  />
Add `trailing={<LangSwitchV2 language={language} setLanguage={setLanguage} />}` as a new prop on this
JSX element. The LangSwitchV2 import at line 23 stays unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add trailing slot to FloatingSearchPill and inline LangSwitchV2 from HomeScreenV2</name>
  <files>src/components/home/v2/FloatingSearchPill.tsx, src/screens/HomeScreenV2.tsx</files>
  <behavior>
    - FloatingSearchPillProps gains an optional `trailing?: React.ReactNode` field.
    - When `trailing` is omitted (existing test case), FloatingSearchPill renders exactly one wrapper View containing one pill TouchableOpacity, which contains the Search icon, placeholder Text, and the filtersButton TouchableOpacity. The existing test (touchables[0] = pill, touchables[1] = filtersButton) continues to pass.
    - When `trailing` is provided, the wrapper View renders the pill TouchableOpacity AND the trailing node as direct flex-row siblings; the pill shrinks to fill remaining space (flex: 1) and the trailing node sits to its right.
    - HomeScreenV2 no longer renders the `<View style={{ alignItems: 'flex-end', paddingTop: 8 }}>` row above GreetingBlock. GreetingBlock is now the first child of the Header fragment.
    - HomeScreenV2 passes `trailing={<LangSwitchV2 language={language} setLanguage={setLanguage} />}` to FloatingSearchPill, so the toggle renders to the right of the filter button on the same row.
    - LangSwitchV2 import stays at HomeScreenV2.tsx line 23. The `{ t, language, setLanguage }` destructure stays at line 41.
    - Tapping LangSwitchV2 still toggles language; tapping the search pill still calls handleSearchPress; tapping the filter button still calls handleFiltersPress.
  </behavior>
  <action>
Make TWO edits, both deterministic. Do NOT touch any other file.

EDIT 1 — `src/components/home/v2/FloatingSearchPill.tsx`:

1a. Extend the props interface (currently lines 7–11) to add an optional `trailing` slot:

    export interface FloatingSearchPillProps {
      placeholder: string;
      onPress: () => void;
      onFiltersPress: () => void;
      trailing?: React.ReactNode;
    }

1b. Extend the component destructure (currently line 14) to include `trailing`:

    export const FloatingSearchPill: React.FC&lt;FloatingSearchPillProps&gt; = ({
      placeholder, onPress, onFiltersPress, trailing,
    }) =&gt; {

1c. Inside the wrapper View, render `{trailing}` as a sibling AFTER the existing pill TouchableOpacity. Do NOT modify any element inside the pill (Search icon, placeholder Text, filtersButton TouchableOpacity stay byte-identical). The JSX block becomes:

    return (
      &lt;View style={styles.wrapper}&gt;
        &lt;TouchableOpacity style={styles.pill} onPress={onPress} activeOpacity={0.85}&gt;
          {/* unchanged: Search icon, placeholder Text, filtersButton */}
        &lt;/TouchableOpacity&gt;
        {trailing}
      &lt;/View&gt;
    );

1d. Update the `wrapper` style (currently lines 33–36) to row-flex with a 10px gap:

    wrapper: {
      paddingHorizontal: 18,
      paddingTop: 12,
      flexDirection: 'row',
      alignItems: 'center',
      gap: 10,
    },

1e. Add `flex: 1` to the `pill` style (currently lines 37–48) so the pill shrinks to leave room for the trailing node:

    pill: {
      flex: 1,
      flexDirection: 'row',
      alignItems: 'center',
      height: 48,
      paddingLeft: 18,
      paddingRight: 6,
      backgroundColor: 'rgba(19,21,27,0.92)',
      borderWidth: 1,
      borderColor: V2.borderHi,
      borderRadius: V2.radius.pill,
      gap: 10,
    },

Do NOT touch `placeholder` or `filtersButton` styles. Do NOT change the Search icon size/color, the placeholder Text props, or the filtersButton's onPress / icon.

EDIT 2 — `src/screens/HomeScreenV2.tsx`:

2a. DELETE the three-line wrapper at the top of the Header fragment (currently lines 95–97):

    &lt;View style={{ alignItems: 'flex-end', paddingTop: 8 }}&gt;
      &lt;LangSwitchV2 language={language} setLanguage={setLanguage} /&gt;
    &lt;/View&gt;

   After deletion, the Header fragment now starts directly with `&lt;GreetingBlock ... /&gt;`.

2b. UPDATE the FloatingSearchPill call site (currently lines 144–148) to add a `trailing` prop:

    &lt;FloatingSearchPill
      placeholder={t.searchPlaceholderV2}
      onPress={handleSearchPress}
      onFiltersPress={handleFiltersPress}
      trailing={&lt;LangSwitchV2 language={language} setLanguage={setLanguage} /&gt;}
    /&gt;

2c. Leave the `LangSwitchV2` import (line 23) AS-IS. Leave the `useLanguage()` destructure (line 41) AS-IS — `language` and `setLanguage` now feed the trailing JSX.

Do NOT touch styles in HomeScreenV2.tsx, any other component import, or any other JSX. Do NOT touch LangSwitchV2.tsx, the FloatingSearchPill test, theme.ts, BottomBar.tsx, v1 HomeScreen.tsx, or any other file.
  </action>
  <verify>
    <automated>cd /Users/beckmaldinVL/development/mobileApps/carEx &amp;&amp; \
      pill_trailing=$(grep -v '^//' src/components/home/v2/FloatingSearchPill.tsx | grep -c 'trailing') &amp;&amp; \
      home_trailing=$(grep -v '^//' src/screens/HomeScreenV2.tsx | grep -c 'trailing') &amp;&amp; \
      home_langswitch=$(grep -v '^//' src/screens/HomeScreenV2.tsx | grep -c 'LangSwitchV2') &amp;&amp; \
      old_row=$(grep -c "alignItems: 'flex-end', paddingTop: 8" src/screens/HomeScreenV2.tsx) &amp;&amp; \
      echo "pill_trailing=$pill_trailing home_trailing=$home_trailing home_langswitch=$home_langswitch old_row=$old_row" &amp;&amp; \
      [ "$pill_trailing" -ge 3 ] &amp;&amp; \
      [ "$home_trailing" -ge 1 ] &amp;&amp; \
      [ "$home_langswitch" -eq 2 ] &amp;&amp; \
      [ "$old_row" -eq 0 ] &amp;&amp; \
      npx jest --testPathPattern FloatingSearchPill --no-watch</automated>
  </verify>
  <done>
    - `FloatingSearchPill.tsx` exposes optional `trailing?: React.ReactNode`, renders it as a sibling of the pill inside a row-flex wrapper with `gap: 10`, and the pill has `flex: 1`. Internal pill children (Search icon, placeholder Text, filtersButton) are unchanged.
    - `HomeScreenV2.tsx` no longer has the `alignItems: 'flex-end', paddingTop: 8` wrapper. The Header fragment starts with GreetingBlock. FloatingSearchPill receives `trailing={<LangSwitchV2 ... />}`.
    - Greps pass: `trailing` ≥ 3 in pill, ≥ 1 in screen; `LangSwitchV2` == 2 in screen (import + JSX); old row string count == 0.
    - `npx jest --testPathPattern FloatingSearchPill --no-watch` is green (the existing test does not pass `trailing`, so behavior with no trailing slot is preserved).
  </done>
</task>

</tasks>

<verification>
- Visual: open HomeScreenV2; LangSwitchV2 sits on the SAME row as the search pill, to the right of the filter button. No extra vertical row above GreetingBlock.
- Functional: tap LangSwitchV2 → language toggles RU ↔ EN; tap pill body → SearchResults navigation; tap filter button → FilterModal opens with type 'Год'.
- Test suite: `npx jest --testPathPattern FloatingSearchPill --no-watch` passes.
- No other v2/v1 component files modified (verify via `git status` shows only the two target files).
</verification>

<success_criteria>
- `src/components/home/v2/FloatingSearchPill.tsx` exports a component accepting optional `trailing?: React.ReactNode`, with row-flex wrapper and `pill: { flex: 1, ... }`.
- `src/screens/HomeScreenV2.tsx` removes the old flex-end row above GreetingBlock and passes the LangSwitchV2 through the new `trailing` prop.
- Existing FloatingSearchPill test passes unchanged.
- No other files in the repo are modified.
</success_criteria>

<output>
After completion, create `.planning/quick/260528-dj9-inline-langswitchv2-into-floatingsearchp/260528-dj9-SUMMARY.md` documenting:
- The two edits made
- Confirmation that the existing FloatingSearchPill test passes
- A note that the LangSwitchV2 toggle is now visually inline with the search pill (no new vertical row)
</output>
