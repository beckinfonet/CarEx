---
phase: 260528-d9a-re-add-ru-en-language-toggle-to-v2-home-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/v2/LangSwitchV2.tsx
  - src/screens/HomeScreenV2.tsx
autonomous: true
requirements:
  - QUICK-D9A-01
must_haves:
  truths:
    - "v2 Home shows a RU|EN pill toggle in the top-right of the Header above GreetingBlock"
    - "Tapping the pill swaps language between RU and EN immediately (same behavior as v1)"
    - "Active language token highlights with V2.blue background + dark text (#04101f); inactive shows V2.textMuted"
    - "Pill chrome (surface + border) uses V2 tokens — no V1 COLORS leak into the v2 tree"
    - "v1 HomeScreen.tsx remains byte-identical (no edits to v1, FloatingSearchPill, or any other file)"
  artifacts:
    - path: "src/components/home/v2/LangSwitchV2.tsx"
      provides: "V2-token-styled RU|EN switch component"
      exports: ["LangSwitchV2"]
    - path: "src/screens/HomeScreenV2.tsx"
      provides: "v2 Home with LangSwitchV2 mounted as first Header child"
      contains: "LangSwitchV2"
  key_links:
    - from: "src/screens/HomeScreenV2.tsx"
      to: "src/components/home/v2/LangSwitchV2.tsx"
      via: "named import + JSX inside Header fragment"
      pattern: "import \\{ LangSwitchV2 \\} from '../components/home/v2/LangSwitchV2'"
    - from: "src/screens/HomeScreenV2.tsx"
      to: "src/context/LanguageContext.tsx"
      via: "useLanguage destructure of language + setLanguage"
      pattern: "const \\{ t, language, setLanguage \\} = useLanguage\\(\\)"
---

<objective>
Re-add the RU|EN language toggle to HomeScreenV2 that was lost during the v2 redesign. v1 Home (HomeScreen.tsx:111-118) has a working pill toggle in its header — v2 Home dropped it. User wants visual + functional parity, restyled to V2 design tokens, owned by a new co-located component (`LangSwitchV2`) so v2 stays self-contained.

Purpose: Restore language switching on v2 without leaking V1 `COLORS` constants into the v2 component tree.
Output: One new component file (`LangSwitchV2.tsx`) + one wiring edit in `HomeScreenV2.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@src/components/home/v2/theme.ts
@src/context/LanguageContext.tsx
@src/screens/HomeScreenV2.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor uses these directly — no codebase exploration needed. -->

From src/context/LanguageContext.tsx (already verified):
- `type Language = 'RU' | 'EN'`
- `useLanguage()` returns `{ language: Language; setLanguage: (lang: Language) => void; t: typeof TRANSLATIONS.RU }`

From src/components/home/v2/theme.ts (V2 tokens — use these only):
- `V2.surface`    = '#13151B'        — pill background
- `V2.border`     = 'rgba(255,255,255,0.06)' — pill border + divider
- `V2.textMuted`  = 'rgba(246,247,251,0.62)' — inactive text
- `V2.blue`       = '#4DA3FF'        — active background

Active text color: `'#04101f'` literal — precedent at src/components/home/v2/FloatingSearchPill.tsx:25 (dark icon on V2.blue chrome). This is the ONLY allowed hex in the new component.

V1 reference shape (DO NOT import — copy structure only):
src/screens/HomeScreen.tsx lines 111-118 (JSX) + lines 307-334 (styles: langSwitch, langText, activeLang, divider).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LangSwitchV2 and mount in HomeScreenV2 Header</name>
  <files>src/components/home/v2/LangSwitchV2.tsx, src/screens/HomeScreenV2.tsx</files>
  <action>
Step A — Create `src/components/home/v2/LangSwitchV2.tsx` as a new file. Import `React` from 'react'; import `View, Text, TouchableOpacity, StyleSheet` from 'react-native'; import `V2` from './theme'. Export a named React.FC `LangSwitchV2` with props interface `LangSwitchV2Props { language: 'RU' | 'EN'; setLanguage: (lang: 'RU' | 'EN') => void }`. The component returns a `TouchableOpacity` with `activeOpacity={0.85}` and `onPress={() => setLanguage(language === 'RU' ? 'EN' : 'RU')}`, wrapping a row: `<Text style={[styles.langText, language === 'RU' && styles.activeLang]}>RU</Text>`, then `<View style={styles.divider} />`, then `<Text style={[styles.langText, language === 'EN' && styles.activeLang]}>EN</Text>`. Define a local `StyleSheet.create` with four keys mirroring v1 shape but with V2 tokens: (1) `langSwitch`: `backgroundColor: V2.surface, flexDirection: 'row', alignItems: 'center', padding: 4, borderRadius: 20, borderWidth: 1, borderColor: V2.border, alignSelf: 'flex-end'`; (2) `langText`: `color: V2.textMuted, fontSize: 12, fontWeight: 'bold', paddingHorizontal: 8, paddingVertical: 4, borderRadius: 16, overflow: 'hidden'`; (3) `activeLang`: `backgroundColor: V2.blue, color: '#04101f'` (the single literal hex permitted; precedent at FloatingSearchPill.tsx:25); (4) `divider`: `width: 1, height: 12, backgroundColor: V2.border, marginHorizontal: 2`. Apply `styles.langSwitch` to the outer TouchableOpacity. Do not import or reference V1 `COLORS`. Do not add `useTypography` (v1 langSwitch is bold default; matches design parity).

Step B — Modify `src/screens/HomeScreenV2.tsx`. Add a new import line directly under the existing v2 imports block (after the FilterModal import on line 22, before the navigation type import): `import { LangSwitchV2 } from '../components/home/v2/LangSwitchV2';`. Change line 40 from `const { t }      = useLanguage();` to `const { t, language, setLanguage } = useLanguage();` (keep alignment loose — Prettier will reformat). In the `Header` JSX fragment (currently starting at line 93 with `<GreetingBlock`), insert as the FIRST child (before `<GreetingBlock`): `<View style={{ alignItems: 'flex-end', paddingTop: 8 }}><LangSwitchV2 language={language} setLanguage={setLanguage} /></View>`. Do not touch anything else — no style changes, no other components, no v1 file.
  </action>
  <verify>
    <automated>grep -c "LangSwitchV2" src/screens/HomeScreenV2.tsx | grep -q "^2$" && grep -c "setLanguage" src/screens/HomeScreenV2.tsx | grep -q "^2$" && grep -q "V2.surface" src/components/home/v2/LangSwitchV2.tsx && grep -q "V2.blue" src/components/home/v2/LangSwitchV2.tsx && grep -q "'#04101f'" src/components/home/v2/LangSwitchV2.tsx && ! grep -q "COLORS" src/components/home/v2/LangSwitchV2.tsx && npx tsc --noEmit 2>&1 | grep -E "HomeScreenV2|LangSwitchV2" | grep -v "^$" ; [ $? -ne 0 ] && echo "PASS: no tsc errors in target files" && npx eslint src/screens/HomeScreenV2.tsx src/components/home/v2/LangSwitchV2.tsx</automated>
  </verify>
  <done>
LangSwitchV2.tsx exists, exports `LangSwitchV2`, uses only V2 tokens + the single '#04101f' literal (no `COLORS` import). HomeScreenV2.tsx destructures `language` + `setLanguage` from `useLanguage()`, imports `LangSwitchV2`, and renders it as the first child of the `Header` fragment wrapped in a flex-end alignment View. `grep -c "LangSwitchV2"` in HomeScreenV2.tsx returns 2 (import + JSX). tsc reports zero errors for either file. eslint passes with zero new errors on both files. v1 HomeScreen.tsx, FloatingSearchPill.tsx, and theme.ts are untouched (`git diff --stat` shows only the two target files).
  </done>
</task>

</tasks>

<verification>
After task completes, manual smoke verification (deferred to user, not blocking):
- Launch app on iOS sim (`npm run ios`), open Home (v2 must be active via Settings → Внешний вид toggle).
- Confirm RU|EN pill appears top-right of the Header, above GreetingBlock greeting line.
- Tap the pill; greeting + listingsCount labels swap RU↔EN immediately.
- Confirm pill chrome matches v2 dark surface (not v1 cardBackground) and active token highlights V2.blue with dark text.
</verification>

<success_criteria>
- `src/components/home/v2/LangSwitchV2.tsx` created, ~40 LOC, V2-token-only.
- `src/screens/HomeScreenV2.tsx` shows LangSwitchV2 rendered top-right of Header.
- `npx tsc --noEmit` and `npx eslint` clean for both touched files.
- `git diff --stat` shows exactly two paths: the new component and the screen.
- No edits to v1 HomeScreen.tsx, FloatingSearchPill.tsx, theme.ts, LanguageContext.tsx, or any other file.
</success_criteria>

<output>
After completion, create `.planning/quick/260528-d9a-re-add-ru-en-language-toggle-to-v2-home-/260528-d9a-01-SUMMARY.md` capturing:
- The exact diff (path + line ranges added).
- Confirmation the v1 path remained untouched.
- A one-line note on the active-text hex `'#04101f'` literal and its FloatingSearchPill precedent (so future readers don't flag it as a token leak).
</output>
