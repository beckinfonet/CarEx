---
phase: 260528-cr4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/HomeScreenV2.tsx
autonomous: true
requirements:
  - CR4-01
must_haves:
  truths:
    - "HomeScreenV2 renders the v1 BottomBar component instead of EditorialDock at the bottom of the screen"
    - "Tapping 'Ещё' in HomeScreenV2 opens the v1 MoreMenu popup (managed by BottomBar's internal state), not navigation to ProfileScreen or LoginScreen"
    - "Tapping 'Главная' navigates Home with clearFilters:true (v1 behavior, inherited from BottomBar)"
    - "Tapping the center sell button navigates to SellCar (v1 behavior, inherited from BottomBar)"
    - "Refresh control, BackHandler unwind, FilterModal, FloatingSearchPill, GreetingBlock, HeroRotator, SmartShelf, and feed rendering all continue to work unchanged on HomeScreenV2"
  artifacts:
    - path: "src/screens/HomeScreenV2.tsx"
      provides: "HomeScreenV2 wired to v1 BottomBar"
      contains: "BottomBar"
  key_links:
    - from: "src/screens/HomeScreenV2.tsx"
      to: "src/components/BottomBar.tsx"
      via: "named import + JSX render"
      pattern: "import \\{ BottomBar \\} from '\\.\\./components/BottomBar'"
---

<objective>
Swap the v2 EditorialDock for the v1 BottomBar in HomeScreenV2 verbatim, per user's UAT decision after the FAB-alignment fix in 260528-cfe failed visual review. The user explicitly approved copy-pasting the v1 dock to ship the v2 home behind the feature toggle without further dock-design iteration.

Purpose: Unblock the homescreen-v2 toggle UAT. The v2 home grid is approved; only the bottom dock is failing. Adopting v1 BottomBar restores the working MoreMenu popup, the centered '+' glyph, and the established label spacing, at the known cost of a minor color seam between V2.bg content and v1 COLORS.background dock (user-accepted).

Output: Modified `src/screens/HomeScreenV2.tsx` with `EditorialDock` import + JSX + `handleMorePress` handler removed, and `BottomBar` imported + rendered in their place. No other files touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/screens/HomeScreenV2.tsx
@src/components/BottomBar.tsx

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->
<!-- No exploration required. -->

From src/components/BottomBar.tsx (the component being adopted):

```typescript
interface BottomBarProps {
  t: any;
}

export const BottomBar = ({ t }: BottomBarProps) => { ... }
```

Usage pattern (from src/screens/HomeScreen.tsx v1): `<BottomBar t={t} />` — single prop, the translations object from `useLanguage()`. BottomBar internally manages MoreMenu visibility via its own `useState`, owns its own `useNavigation` instance, and handles all three actions (Home/SellCar/More) internally. No external handlers needed.

Current EditorialDock usage in HomeScreenV2.tsx (lines 171-176, to be replaced):

```
<EditorialDock
  homeLabel={t.home}
  sellLabel={t.sellCar}
  moreLabel={t.more}
  onMorePress={handleMorePress}
/>
```

Current `handleMorePress` definition (lines 93-96, to be deleted):

```
const handleMorePress = () => {
  if (user) navigation.navigate('Profile');
  else navigation.navigate('Login');
};
```

IMPORTANT: `useAuth` and the `user` destructure on line 42 must REMAIN — they may still be used elsewhere in the file by future work, and removing them is out of scope. Verify after the edit that `user` is either still referenced or the destructure becomes unused (TypeScript will warn but not error). If TS complains about unused `user`, change `const { user } = useAuth();` to `const { } = useAuth();` is NOT acceptable — instead leave the destructure as-is; an unused local is a lint warning, not a blocker, and the user wants the diff minimal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap EditorialDock for v1 BottomBar in HomeScreenV2</name>
  <files>src/screens/HomeScreenV2.tsx</files>
  <action>
    Make exactly three edits to `src/screens/HomeScreenV2.tsx`. No other files in scope.

    Edit 1 — Replace the import on line 22.
    Remove the line: `import { EditorialDock } from '../components/home/v2/EditorialDock';`
    Insert in its place: `import { BottomBar } from '../components/BottomBar';`
    Keep the import grouped with the existing v2 component imports (lines 14-23). The replacement should preserve the surrounding import block ordering.

    Edit 2 — Delete the `handleMorePress` handler (currently lines 93-96).
    Remove the entire block:
    `const handleMorePress = () => {`
    `  if (user) navigation.navigate('Profile');`
    `  else navigation.navigate('Login');`
    `};`
    Do NOT remove anything else in the surrounding handlers — `handleCarPress`, `handleSearchPress`, and `handleFiltersPress` must stay. Do NOT remove `useAuth` or the `const { user } = useAuth();` destructure on line 42. The `user` variable may end up locally unused after this edit; that is acceptable per the planning constraint to keep the diff minimal. If ESLint or TS flag it, leave it — do not "fix" by removing the destructure.

    Edit 3 — Replace the EditorialDock JSX block (currently lines 171-176).
    Remove the entire block:
    `<EditorialDock`
    `  homeLabel={t.home}`
    `  sellLabel={t.sellCar}`
    `  moreLabel={t.more}`
    `  onMorePress={handleMorePress}`
    `/>`
    Insert in its place: `<BottomBar t={t} />`
    The replacement sits between the closing tag of `<FlatList ... />` and the opening tag of `<FilterModal ... />`. Indentation should match the surrounding JSX (six spaces inside the `SafeAreaView`).

    Do NOT modify: StyleSheet block, any state, any effects, FloatingSearchPill, GreetingBlock, ActiveFilterChips, HeroRotator, SmartShelf, BigFeedCard, SmallFeedCard, FeedLoader, FilterModal, the BackHandler effect, the `clearFilters` route-params effect, the slicing of `heroCars`/`shelfCars`/`feedCars`, or any prop on any remaining component.

    Do NOT delete `src/components/home/v2/EditorialDock.tsx` — it stays as dead code per user decision (we may revisit the v2 dock later).
    Do NOT modify `src/components/BottomBar.tsx` — it is adopted as-is.
    Do NOT modify `src/screens/HomeScreen.tsx` (the v1 reference) — read-only.

    Color seam note (informational, no action required): v1 BottomBar uses `COLORS.background` + `COLORS.border` from `src/constants/theme.ts`, while HomeScreenV2 content uses `V2.bg`. There will be a subtle visual seam. The user has accepted this for now.
  </action>
  <verify>
    <automated>
      grep -c "BottomBar" src/screens/HomeScreenV2.tsx | grep -q '^2$' && grep -c "EditorialDock" src/screens/HomeScreenV2.tsx | grep -q '^0$' && grep -c "handleMorePress" src/screens/HomeScreenV2.tsx | grep -q '^0$' && grep -q "^import { BottomBar } from '../components/BottomBar';$" src/screens/HomeScreenV2.tsx && grep -q "<BottomBar t={t} />" src/screens/HomeScreenV2.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "HomeScreenV2\.tsx.*error" | grep -v "is declared but its value is never read" ; test $? -ne 0
    </automated>
  </verify>
  <done>
    - `src/screens/HomeScreenV2.tsx` contains the string `BottomBar` exactly twice: once in the import on (approx) line 22, once in the JSX where EditorialDock used to be (approx line 171).
    - `src/screens/HomeScreenV2.tsx` contains the string `EditorialDock` zero times.
    - `src/screens/HomeScreenV2.tsx` contains the string `handleMorePress` zero times.
    - `npx tsc --noEmit` produces no new errors attributable to HomeScreenV2.tsx other than (acceptable) unused-local warnings for `user`.
    - No other files in the repo are modified by this task.
  </done>
</task>

</tasks>

<verification>
After the task completes, manually sanity-check by running:
- `grep -n "BottomBar\|EditorialDock\|handleMorePress" src/screens/HomeScreenV2.tsx` — expect 2 BottomBar lines, 0 of the others.
- `git diff --stat src/screens/HomeScreenV2.tsx` — expect only HomeScreenV2.tsx in the changed-files list.
- `git diff src/screens/HomeScreenV2.tsx` — expect roughly: −1 import / +1 import, −4 handler lines, −6 JSX lines / +1 JSX line. Net change should be approximately −9 lines.

End-to-end UAT (not gated here, performed after merge to the toggle branch):
1. With the v2 toggle ON in Settings → Внешний вид, open Home.
2. Confirm the bottom bar renders with the v1 layout (rounded buttons, blue accent border, Home / Продать / Ещё labels).
3. Tap "Ещё" → expect the MoreMenu modal to slide in (v1 popup), not a navigation jump.
4. Tap "Главная" → expect Home to refresh with filters cleared.
5. Tap the center sell button → expect navigation to SellCar.
6. Confirm refresh-to-reload still works on the v2 feed.
7. Confirm the BackHandler still unwinds filters on Android (if testing on Android).
</verification>

<success_criteria>
- All three `grep` acceptance checks pass (`BottomBar` count == 2, `EditorialDock` count == 0, `handleMorePress` count == 0).
- TypeScript compiles with no new HomeScreenV2-attributable errors.
- Only `src/screens/HomeScreenV2.tsx` appears in `git diff --stat`.
- Net diff is approximately −9 lines (one-line import swap, four-line handler removal, six-to-one JSX collapse).
</success_criteria>

<output>
After completion, create `.planning/quick/260528-cr4-swap-editorialdock-for-v1-bottombar-in-h/260528-cr4-01-SUMMARY.md` documenting:
- The three edits applied (import / handler / JSX), with line ranges as edited
- Confirmation of the grep acceptance results
- Confirmation that EditorialDock.tsx, BottomBar.tsx, MoreMenu.tsx, and v1 HomeScreen.tsx were not modified
- Any unexpected ESLint/TS warnings (especially around the now-unused `user` destructure) and the decision left in place
- Next step: hand off to UAT against the v2 toggle
</output>
