---
phase: quick-260603-t98
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [T98-V1-REMOVAL]
files_modified:
  # DELETED
  - src/screens/HomeScreen.tsx
  - src/screens/HomeScreenRouter.tsx
  - src/components/CategoryList.tsx
  - src/components/QuickSortFilters.tsx
  - src/components/LatestCarousel.tsx
  - src/components/home/v2/EditorialDock.tsx
  - src/components/home/v2/__tests__/EditorialDock.test.tsx
  - src/components/home/v2/V2InviteBanner.tsx
  - src/context/UIVersionContext.tsx
  - src/context/__tests__/UIVersionContext.test.tsx
  # REFACTORED
  - App.tsx
  - src/screens/SearchResultsRouter.tsx
  - src/screens/AccountSettingsScreen.tsx
  - src/hooks/useTypography.ts
  - src/constants/translations.ts
  # TEST WRAPPER STRIP (UIVersionProvider removal)
  - src/hooks/__tests__/useTypography.test.tsx
  - src/components/home/v2/__tests__/BigFeedCard.test.tsx
  - src/components/home/v2/__tests__/FloatingSearchPill.test.tsx
  - src/components/home/v2/__tests__/GreetingBlock.test.tsx
  - src/components/home/v2/__tests__/HeroCard.test.tsx
  - src/components/home/v2/__tests__/HeroRotator.test.tsx
  - src/components/home/v2/__tests__/ShelfCard.test.tsx
  - src/components/home/v2/__tests__/SmallFeedCard.test.tsx
  - src/components/home/v2/__tests__/SmartShelf.test.tsx

must_haves:
  truths:
    - "App launches and the Home route renders HomeScreenV2 with no V1/V2 toggle anywhere"
    - "AccountSettings shows no appearance/version toggle and no [DEV] reset button"
    - "Search route renders SearchResultsV2 directly, no version check"
    - "V2 typography (Manrope/JetBrainsMono) still applies on V2 screens"
    - "grep for UIVersionContext, useUIVersion, UIVersionProvider, HomeScreenRouter, CategoryList, QuickSortFilters, LatestCarousel, EditorialDock, V2InviteBanner, setVersion, ui_design_version returns ZERO matches"
    - "npm run lint passes and npm test passes"
  artifacts:
    - path: "App.tsx"
      provides: "Home route -> HomeScreenV2 (aliased HomeScreen), no UIVersionProvider"
      contains: "HomeScreenV2"
    - path: "src/hooks/useTypography.ts"
      provides: "V2-hardcoded typography, no useUIVersion dependency"
      contains: "Manrope"
    - path: "src/screens/SearchResultsRouter.tsx"
      provides: "SearchResultsV2 render with no version check"
  key_links:
    - from: "App.tsx"
      to: "HomeScreenV2"
      via: "import aliased as HomeScreen, component={HomeScreen} on Home Stack.Screen"
      pattern: "HomeScreenV2 as HomeScreen"
    - from: "src/hooks/useTypography.ts"
      to: "V2 components"
      via: "returns Manrope/JetBrainsMono unconditionally"
      pattern: "Manrope"
---

<objective>
Remove the old V1 UI entirely and the V1/V2 version toggle. V2 is shipped; hardcode V2 as the only UI with no path back to V1.

Purpose: Eliminate dead V1 code, the UIVersionContext toggle, and the EditorialDock dead V2 code so the codebase reflects the shipped reality.
Output: V1 screens/components deleted, UIVersionContext removed, V2 made the only path. Lint + tests green, zero dangling references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<critical_finding>
The explorer's removal map is accurate but MISSED one dependency that this plan handles:

**`src/hooks/useTypography.ts` imports `useUIVersion`** (line 1, 24). useTypography is V2-only and MUST be preserved, but it currently reads `version` from UIVersionContext. Since V2 is now the only UI, useTypography must be refactored to hardcode V2 typography and DROP the useUIVersion import. This is the unlock that lets UIVersionContext be deleted.

Consequence: **9 V2 test files + the useTypography test wrap their components in `<UIVersionProvider>`** purely because of the (now-removed) useTypography→useUIVersion chain. After useTypography is decoupled, those wrappers reference a deleted module and must be stripped. The 2 dedicated tests (UIVersionContext.test.tsx, EditorialDock.test.tsx) are deleted outright.
</critical_finding>

<interfaces>
Verified against code:

App.tsx — Home route currently `component={HomeScreenRouter}`; SearchResults route `component={SearchResultsRouter}`. Provider stack nests `<UIVersionProvider>` between `<PersonalityProvider>` and `<NavigationContainer>` (App.tsx lines 101 and 137). Route names "Home" and "SearchResults" MUST stay unchanged (RootStackParamList keys + deep link `linking.config.screens.Home`).

src/screens/HomeScreenV2.tsx — named export `HomeScreenV2`. Uses useTypography. Does NOT import V2InviteBanner or EditorialDock.
src/screens/SearchResultsV2.tsx — named export `SearchResultsV2`.

src/types/navigation.ts — `Home: { clearFilters?: boolean } | undefined;` and `SearchResults: { initialQuery: string; ... }`. NO change needed; route param types stay valid.

V2InviteBanner — only live consumer is src/screens/HomeScreen.tsx (V1). Once HomeScreen is deleted, V2InviteBanner is fully dead → delete it. NOT referenced by HomeScreenV2.

translations.ts — appearance toggle keys at RU lines 950-954 and EN lines 1964-1968: appearanceTitle, appearanceClassic, appearanceClassicDesc, appearanceNew, appearanceNewDesc. These become orphaned after AccountSettings toggle removal → remove all 5 from BOTH blocks (RU + EN parity).

AccountSettingsScreen.tsx — toggle UI is the entire `appearanceSection` View (lines ~299-341), including the two radio TouchableOpacitys and the `__DEV__` reset button. Imports to remove: `useUIVersion, UIVersion` (line 11) and the destructure on line 31 (`version: uiVersion, setVersion: setUIVersion`). AsyncStorage import (line 12) is used ONLY by the __DEV__ reset button's `AsyncStorage.removeItem('ui_design_invite_dismissed_v2')` — verify no other AsyncStorage usage in the file before removing the import. Styles appearanceTitle/appearanceRow/appearanceRowTitle/appearanceRowDesc/radio/radioDot become unused → remove from StyleSheet.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead V1 files, EditorialDock, V2InviteBanner, and UIVersionContext (+ their tests)</name>
  <files>
    src/screens/HomeScreen.tsx,
    src/screens/HomeScreenRouter.tsx,
    src/components/CategoryList.tsx,
    src/components/QuickSortFilters.tsx,
    src/components/LatestCarousel.tsx,
    src/components/home/v2/EditorialDock.tsx,
    src/components/home/v2/__tests__/EditorialDock.test.tsx,
    src/components/home/v2/V2InviteBanner.tsx,
    src/context/UIVersionContext.tsx,
    src/context/__tests__/UIVersionContext.test.tsx
  </files>
  <action>
    Delete these 10 files outright (use `git rm` or the filesystem). All are confirmed dead once the toggle is gone:
    - V1 home + router: HomeScreen.tsx, HomeScreenRouter.tsx
    - V1-only components imported solely by HomeScreen.tsx: CategoryList.tsx, QuickSortFilters.tsx, LatestCarousel.tsx
    - Dead V2 code (user wants it gone): EditorialDock.tsx + EditorialDock.test.tsx
    - V2InviteBanner.tsx — verified its only live consumer was HomeScreen.tsx (V1); HomeScreenV2 does NOT use it, so it is fully dead.
    - The toggle context: UIVersionContext.tsx + UIVersionContext.test.tsx
    Do NOT touch App.tsx, useTypography, or any remaining test files yet — those break compilation until Tasks 2 and 3 run. This task intentionally leaves the tree non-compiling; that is fixed by Tasks 2-3.
  </action>
  <verify>
    <automated>test ! -e src/screens/HomeScreen.tsx && test ! -e src/screens/HomeScreenRouter.tsx && test ! -e src/components/CategoryList.tsx && test ! -e src/components/QuickSortFilters.tsx && test ! -e src/components/LatestCarousel.tsx && test ! -e src/components/home/v2/EditorialDock.tsx && test ! -e src/components/home/v2/__tests__/EditorialDock.test.tsx && test ! -e src/components/home/v2/V2InviteBanner.tsx && test ! -e src/context/UIVersionContext.tsx && test ! -e src/context/__tests__/UIVersionContext.test.tsx && echo ALL_DELETED</automated>
  </verify>
  <done>All 10 files no longer exist on disk; command prints ALL_DELETED.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor App.tsx, SearchResultsRouter, and useTypography to hardcode V2</name>
  <files>App.tsx, src/screens/SearchResultsRouter.tsx, src/hooks/useTypography.ts</files>
  <action>
    **App.tsx:**
    - Replace the HomeScreenRouter import (line 14) with: import `HomeScreenV2` from './src/screens/HomeScreenV2' aliased as `HomeScreen` (e.g. `import { HomeScreenV2 as HomeScreen } from './src/screens/HomeScreenV2';`). This keeps the `component={HomeScreen}` reference and the "Home" route name/key unchanged.
    - Update the Home Stack.Screen (line 111) to `component={HomeScreen}`.
    - Remove the `UIVersionProvider` import (line 40) and remove the `<UIVersionProvider>` open/close wrapper tags (lines 101 and 137) from the provider stack. The nesting order of all OTHER providers must stay byte-identical (CartProvider uses useAuth; order matters per CLAUDE.md).
    - Leave SearchResultsRouter import/usage as-is (still a valid route; refactored below).

    **src/screens/SearchResultsRouter.tsx:**
    - Remove the `useUIVersion` import and all version logic. V1 never had a search screen, so this only ever renders SearchResultsV2. Reduce the component to render `<SearchResultsV2 />` directly with no version check, no useEffect redirect, no useNavigation dependency on version. Keep the named export `SearchResultsRouter` so App.tsx's `component={SearchResultsRouter}` keeps working (do NOT rename the route or the export).

    **src/hooks/useTypography.ts:**
    - Remove the `import { useUIVersion } from '../context/UIVersionContext';` and the `const { version } = useUIVersion();` call.
    - Hardcode V2 typography: `display` returns 'Manrope', `mono` returns 'JetBrainsMono-Medium' (the prior `version === 'v2'` branch values). Keep the `weights` object and the `TypographyFamilies` interface unchanged so all V2 consumers keep their contract.
  </action>
  <verify>
    <automated>grep -rn "UIVersionProvider\|HomeScreenRouter\|useUIVersion" App.tsx src/screens/SearchResultsRouter.tsx src/hooks/useTypography.ts | grep -v '^#' | grep -c . | grep -qx 0 && grep -q "HomeScreenV2 as HomeScreen" App.tsx && grep -q "Manrope" src/hooks/useTypography.ts && echo TASK2_OK</automated>
  </verify>
  <done>App.tsx routes Home to HomeScreenV2 (aliased HomeScreen) with no UIVersionProvider; SearchResultsRouter renders SearchResultsV2 with no version check; useTypography returns V2 fonts unconditionally with no useUIVersion import. Command prints TASK2_OK.</done>
</task>

<task type="auto">
  <name>Task 3: Strip toggle UI from AccountSettings, remove orphaned translations, and strip UIVersionProvider test wrappers</name>
  <files>
    src/screens/AccountSettingsScreen.tsx,
    src/constants/translations.ts,
    src/hooks/__tests__/useTypography.test.tsx,
    src/components/home/v2/__tests__/BigFeedCard.test.tsx,
    src/components/home/v2/__tests__/FloatingSearchPill.test.tsx,
    src/components/home/v2/__tests__/GreetingBlock.test.tsx,
    src/components/home/v2/__tests__/HeroCard.test.tsx,
    src/components/home/v2/__tests__/HeroRotator.test.tsx,
    src/components/home/v2/__tests__/ShelfCard.test.tsx,
    src/components/home/v2/__tests__/SmallFeedCard.test.tsx,
    src/components/home/v2/__tests__/SmartShelf.test.tsx
  </files>
  <action>
    **src/screens/AccountSettingsScreen.tsx:**
    - Remove the `import { useUIVersion, UIVersion } from '../context/UIVersionContext';` (line 11) and the destructure `const { version: uiVersion, setVersion: setUIVersion } = useUIVersion();` (line 31).
    - Remove the entire `appearanceSection` block (the `<View style={styles.appearanceSection}>...</View>`, approx lines 299-341) including both radio TouchableOpacitys and the `__DEV__` reset button.
    - The `AsyncStorage` import (line 12) is used ONLY by the deleted __DEV__ button (`AsyncStorage.removeItem('ui_design_invite_dismissed_v2')`). Grep the file to confirm no other AsyncStorage usage remains; if none, remove the import. If other usage exists, keep the import.
    - Remove now-unused styles from the StyleSheet: appearanceSection, appearanceTitle, appearanceRow, appearanceRowTitle, appearanceRowDesc, radio, radioDot (only those that are no longer referenced after the block is gone — verify each with grep before deleting).

    **src/constants/translations.ts:**
    - Remove the 5 orphaned appearance keys from BOTH language blocks (RU lines ~950-954, EN lines ~1964-1968): appearanceTitle, appearanceClassic, appearanceClassicDesc, appearanceNew, appearanceNewDesc. Maintain RU+EN parity — remove the same 5 keys from each block, no orphans left in either language.

    **Test wrapper strip (9 files):** In each of the listed test files, the components are wrapped in `<UIVersionProvider>...</UIVersionProvider>` only because useTypography used to need it. useTypography is now decoupled. Remove the `import { UIVersionProvider } from '...UIVersionContext';` line and unwrap the JSX so the component-under-test renders without the provider wrapper (keep all other test logic, assertions, and any OTHER providers intact). After this, no test imports the deleted UIVersionContext.
  </action>
  <verify>
    <automated>grep -rn "useUIVersion\|UIVersionProvider\|setUIVersion\|appearanceSection\|appearanceClassic\|appearanceNew" src/screens/AccountSettingsScreen.tsx src/constants/translations.ts src/hooks/__tests__/useTypography.test.tsx src/components/home/v2/__tests__ | grep -v '^#' | grep -c . | grep -qx 0 && echo TASK3_OK</automated>
  </verify>
  <done>AccountSettings has no toggle UI/imports; the 5 appearance keys are gone from both RU and EN; all 9 test files render without UIVersionProvider. Command prints TASK3_OK.</done>
</task>

<task type="auto">
  <name>Task 4: Final sweep — lint, test, and zero-dangling-reference grep gate</name>
  <files>(verification only — no source changes unless a failure surfaces a missed reference)</files>
  <action>
    Run the full gate. If any check fails, fix the surfaced reference (a missed import, a stray usage, an unused style/var that lint flags) in the appropriate file from Tasks 1-3, then re-run until all pass.

    1. Run `npm run lint` — must pass (no errors; watch for unused-import/unused-var from the removed toggle code).
    2. Run `npm test` — all tests pass.
    3. Run the dangling-identifier grep sweep over `src` and `App.tsx` (exclude comment lines). Every one of these MUST return zero matches: UIVersionContext, useUIVersion, UIVersionProvider, HomeScreenRouter, CategoryList, QuickSortFilters, LatestCarousel, EditorialDock, V2InviteBanner, setVersion, ui_design_version.
    Note: the SearchResultsRouter export name and AccountSettings file are allowed to remain — only the listed REMOVED identifiers must be gone.
  </action>
  <verify>
    <automated>npm run lint && npm test 2>&1 | tail -20 && MATCHES=$(grep -rn "UIVersionContext\|useUIVersion\|UIVersionProvider\|HomeScreenRouter\|CategoryList\|QuickSortFilters\|LatestCarousel\|EditorialDock\|V2InviteBanner\|setVersion\|ui_design_version" src App.tsx --include="*.ts" --include="*.tsx" | grep -v '^[^:]*:[0-9]*: *//' | grep -c .) && echo "DANGLING_MATCHES=$MATCHES" && [ "$MATCHES" -eq 0 ] && echo GATE_PASS</automated>
  </verify>
  <done>`npm run lint` passes, `npm test` passes, grep sweep prints DANGLING_MATCHES=0 and GATE_PASS. The plan is complete only when all three hold.</done>
</task>

</tasks>

<verification>
- App.tsx Home route renders HomeScreenV2 (aliased HomeScreen); SearchResults renders SearchResultsV2; no UIVersionProvider in the provider stack.
- useTypography returns Manrope/JetBrainsMono-Medium with no UIVersionContext dependency; V2 screens keep their fonts.
- AccountSettings has no appearance toggle and no __DEV__ reset button; AsyncStorage import removed if unused.
- translations.ts: 5 appearance keys removed from both RU and EN (parity preserved).
- 9 V2/hook test files no longer import or wrap with UIVersionProvider; EditorialDock + UIVersionContext tests deleted.
- npm run lint passes, npm test passes, grep sweep returns zero matches for all removed identifiers.
- Signup, login, listing browse, cart, Stripe checkout untouched (no changes to those screens/providers; provider nesting order preserved).
</verification>

<success_criteria>
- V1 UI and the version toggle are fully removed; V2 is the only path with no way back to V1.
- Zero dangling references to: UIVersionContext, useUIVersion, UIVersionProvider, HomeScreenRouter, CategoryList, QuickSortFilters, LatestCarousel, EditorialDock, V2InviteBanner, setVersion, ui_design_version.
- `npm run lint` and `npm test` both green.
- RU+EN translation parity maintained.
- No regression to signup/login/browse/cart/Stripe flows.
</success_criteria>

<output>
After completion, create `.planning/quick/260603-t98-remove-old-v1-ui-version-and-version-tog/260603-t98-SUMMARY.md`
</output>
