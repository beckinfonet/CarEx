---
phase: 260528-c4q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/HomeScreenV2.tsx
autonomous: false
requirements:
  - QUICK-FIX-STICKY-PILL
must_haves:
  truths:
    - "FloatingSearchPill is visible at the top of HomeScreenV2 on initial render"
    - "FloatingSearchPill remains pinned at the top of the screen while the feed FlatList is scrolled down"
    - "FloatingSearchPill remains pinned at the top after pull-to-refresh"
    - "All other v2 home behaviors continue to work: refresh, Android BackHandler filter unwind, FilterModal open/apply, EditorialDock bottom bar"
    - "Tapping the pill still navigates to SearchResults and tapping the filter button still opens FilterModal"
  artifacts:
    - path: "src/screens/HomeScreenV2.tsx"
      provides: "HomeScreenV2 layout with FloatingSearchPill hoisted as a sibling of FlatList inside SafeAreaView"
      contains: "FloatingSearchPill"
  key_links:
    - from: "src/screens/HomeScreenV2.tsx (SafeAreaView)"
      to: "src/components/home/v2/FloatingSearchPill.tsx"
      via: "direct child render outside of FlatList ListHeaderComponent"
      pattern: "FloatingSearchPill"
    - from: "src/screens/HomeScreenV2.tsx (Header fragment)"
      to: "GreetingBlock + ActiveFilterChips + HeroRotator + SmartShelf + feedHeader"
      via: "ListHeaderComponent={Header}"
      pattern: "ListHeaderComponent"
---

<objective>
Make the FloatingSearchPill stick to the top of HomeScreenV2 so it remains visible while the user scrolls the feed.

Purpose: The current behavior places the pill inside the FlatList's ListHeaderComponent fragment, so it scrolls off-screen with the rest of the header content. The mockup and product intent is that the pill is a persistent search/filters entry point pinned at the top of the screen. This is purely a layout fix — no scroll-aware styling, no animation, no behavior changes to the pill itself.

Output: HomeScreenV2.tsx with FloatingSearchPill hoisted to be a direct sibling of FlatList inside SafeAreaView. The pill is rendered once, statically, above the scrolling feed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@src/screens/HomeScreenV2.tsx
@src/components/home/v2/FloatingSearchPill.tsx
@src/components/home/v2/theme.ts

<interfaces>
<!-- Key contracts the executor needs. No exploration required. -->

From src/components/home/v2/FloatingSearchPill.tsx — already imported in HomeScreenV2.tsx (line 14), props unchanged:
```typescript
export interface FloatingSearchPillProps {
  placeholder: string;
  onPress: () => void;
  onFiltersPress: () => void;
}
```

Note: FloatingSearchPill's own wrapper style already includes `paddingHorizontal: 18` and `paddingTop: 12`, so it owns its own outer spacing. No additional wrapper View needed in HomeScreenV2.

Current HomeScreenV2 handlers wired to the pill (preserve verbatim):
```typescript
const handleSearchPress  = () => navigation.navigate('SearchResults', { initialQuery: '' });
const handleFiltersPress = () => { setCurrentFilterType('Год'); setFilterModalVisible(true); };
```

Current structure (HomeScreenV2.tsx lines 98–187):
- `Header` is a JSX fragment that contains FloatingSearchPill as its first child, followed by GreetingBlock, ActiveFilterChips, HeroRotator, SmartShelf, and the inline feedHeader View.
- `Header` is passed as `ListHeaderComponent={Header}` to FlatList, which means it scrolls with the list.

Target structure:
- `Header` fragment NO LONGER contains FloatingSearchPill; everything else (GreetingBlock onward) remains in the fragment.
- A standalone `<FloatingSearchPill ... />` is rendered inside `<SafeAreaView>` immediately AFTER `<StatusBar />` and BEFORE `<FlatList ... />`. This makes it a static sibling of the FlatList, pinned at the top.
- The FlatList's `contentContainerStyle={styles.listContent}` stays unchanged (`paddingHorizontal: 18, paddingBottom: 120`). The pill owns its own padding, so the first scrolling header element (GreetingBlock) renders below it cleanly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hoist FloatingSearchPill out of ListHeaderComponent in HomeScreenV2</name>
  <files>src/screens/HomeScreenV2.tsx</files>
  <behavior>
    - Behavior 1: On HomeScreenV2 mount, FloatingSearchPill renders once at the top of the SafeAreaView, above the FlatList.
    - Behavior 2: When the FlatList content scrolls (feed cards move up), the FloatingSearchPill stays in place visually because it is a sibling of the FlatList, not inside its scrolling header.
    - Behavior 3: Tapping the pill body still calls handleSearchPress → navigation.navigate('SearchResults', { initialQuery: '' }).
    - Behavior 4: Tapping the filter sub-button still calls handleFiltersPress → opens FilterModal with type 'Год'.
    - Behavior 5: GreetingBlock, ActiveFilterChips, HeroRotator, SmartShelf, and the feedHeader text block continue to scroll with the feed (they remain inside ListHeaderComponent).
    - Behavior 6: Pull-to-refresh, Android back-handler filter unwind, EditorialDock, and FilterModal continue to function as before.
  </behavior>
  <action>
    Edit src/screens/HomeScreenV2.tsx ONLY. Do not edit FloatingSearchPill.tsx, theme.ts, or any other file.

    Step 1: Remove the `<FloatingSearchPill ... />` JSX block from the top of the `Header` fragment (currently lines 100–104). The `Header` constant now starts directly with `<GreetingBlock ... />` as its first child. Keep all other children in the fragment unchanged (GreetingBlock, ActiveFilterChips, HeroRotator, SmartShelf, the feedHeader `<View>`).

    Step 2: Inside the `return (...)` block, add a standalone `<FloatingSearchPill />` element as a direct child of `<SafeAreaView>`, positioned immediately AFTER `<StatusBar ... />` and immediately BEFORE `<FlatList ... />`. Pass the exact same three props it currently receives:
      - placeholder={t.searchPlaceholderV2}
      - onPress={handleSearchPress}
      - onFiltersPress={handleFiltersPress}

    Step 3: Do not change `contentContainerStyle={styles.listContent}` — the existing `paddingHorizontal: 18, paddingBottom: 120` is correct. The pill's own wrapper style (paddingHorizontal: 18, paddingTop: 12 inside FloatingSearchPill.tsx) handles its own spacing, so no spacer View is required between the pill and the FlatList. GreetingBlock retains its own internal top padding which provides visual breathing room below the pill.

    Step 4: Do not modify imports — `FloatingSearchPill` is already imported at the top of the file. Do not modify the styles object. Do not modify handlers, hooks, or any other logic.

    Constraints (do NOT do these):
    - Do NOT touch FloatingSearchPill.tsx internals.
    - Do NOT change handleSearchPress / handleFiltersPress wiring (a separate bug-fix task owns navigation behavior).
    - Do NOT introduce scroll listeners, Animated.Value, or any scroll-aware styling.
    - Do NOT add `position: 'absolute'` to the pill — sibling placement inside SafeAreaView is sufficient to keep it pinned because it is rendered outside the scrolling FlatList.
    - Do NOT wrap the pill in an extra `<View>` — it already provides its own wrapper.
  </action>
  <verify>
    <automated>
      # Pill must be referenced exactly twice in the file: once in the import (line 14) and once in the JSX (new sibling location). Previously: 2 (import + Header fragment usage). Same count, different position.
      test "$(grep -v '^//' src/screens/HomeScreenV2.tsx | grep -c 'FloatingSearchPill')" -eq 2 \
        && echo "OK: pill reference count == 2" \
        || (echo "FAIL: expected 2 FloatingSearchPill references"; exit 1)

      # Pill must NOT appear inside the Header constant. Detect by checking that the line that previously held the pill JSX is no longer adjacent to GreetingBlock as siblings inside the Header fragment. We grep for the specific signal: the pill JSX block must be OUTSIDE the const Header = ( ... ) declaration. Use awk to extract the Header constant block and assert FloatingSearchPill is absent from it.
      awk '/const Header = \(/{flag=1} flag{print} /^  \);$/{if(flag){flag=0}}' src/screens/HomeScreenV2.tsx | grep -c 'FloatingSearchPill' | grep -q '^0$' \
        && echo "OK: pill not inside Header fragment" \
        || (echo "FAIL: FloatingSearchPill still inside Header fragment"; exit 1)

      # Pill JSX must appear between StatusBar and FlatList in the return block. Use grep -n to confirm ordering.
      STATUSBAR_LINE=$(grep -n '<StatusBar' src/screens/HomeScreenV2.tsx | tail -1 | cut -d: -f1)
      PILL_LINE=$(grep -n '<FloatingSearchPill' src/screens/HomeScreenV2.tsx | tail -1 | cut -d: -f1)
      FLATLIST_LINE=$(grep -n '<FlatList' src/screens/HomeScreenV2.tsx | tail -1 | cut -d: -f1)
      [ -n "$STATUSBAR_LINE" ] && [ -n "$PILL_LINE" ] && [ -n "$FLATLIST_LINE" ] \
        && [ "$STATUSBAR_LINE" -lt "$PILL_LINE" ] && [ "$PILL_LINE" -lt "$FLATLIST_LINE" ] \
        && echo "OK: pill sits between StatusBar and FlatList" \
        || (echo "FAIL: pill not positioned between StatusBar and FlatList (SB=$STATUSBAR_LINE PILL=$PILL_LINE FL=$FLATLIST_LINE)"; exit 1)

      # ESLint must pass on the changed file.
      npx eslint src/screens/HomeScreenV2.tsx
    </automated>
  </verify>
  <done>
    - HomeScreenV2.tsx compiles with no TypeScript or ESLint errors.
    - The pill JSX no longer appears inside the `const Header = ( ... )` fragment.
    - A `<FloatingSearchPill ... />` is rendered as a sibling of FlatList inside SafeAreaView, positioned between `<StatusBar />` and `<FlatList />`.
    - The pill receives the same three props (`placeholder`, `onPress`, `onFiltersPress`) with the same values it had before.
    - No other file is modified.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — sticky pill on device/sim</name>
  <what-built>FloatingSearchPill hoisted out of the FlatList header into a sibling slot inside SafeAreaView so it stays pinned at the top of HomeScreenV2 while the feed scrolls.</what-built>
  <how-to-verify>
    1. Run the app on iOS sim or Android emulator (`npm run ios` or `npm run android`).
    2. If you are not already on HomeScreenV2, switch the "Внешний вид" toggle in Settings to v2.
    3. Land on the v2 Home screen. Confirm FloatingSearchPill is visible at the top with the search placeholder text and the filter button on the right.
    4. Scroll the feed down with a slow drag, then with a fast flick. EXPECTED: the pill stays pinned at the top of the screen and does NOT scroll up with the GreetingBlock / HeroRotator / SmartShelf / feed cards.
    5. Confirm GreetingBlock, ActiveFilterChips, HeroRotator, SmartShelf, and the "Для тебя / Ещё предложения" feed header still scroll with the feed (i.e., they leave the screen as you scroll).
    6. Tap the pill body. EXPECTED: navigates to SearchResults (existing behavior — do NOT verify navigation correctness here; separate bug fix).
    7. Tap the filter button (right-side blue circle with sliders icon). EXPECTED: FilterModal opens with the "Год" tab.
    8. Pull down to refresh. EXPECTED: refresh indicator appears, pill stays in place throughout.
    9. (Android only) Apply a filter, then press the hardware back button. EXPECTED: filter unwinds; pill remains visible the whole time.
    10. Confirm the EditorialDock bottom bar still renders unchanged.
    11. Confirm no visual overlap between the pill and GreetingBlock at scroll-top — if there is awkward crowding, report it; otherwise approve.
  </how-to-verify>
  <resume-signal>Type "approved" if the pill stays sticky and all listed behaviors pass. Otherwise, describe the specific issue (e.g., "pill scrolls with header", "overlap with GreetingBlock", "filter modal does not open").</resume-signal>
</task>

</tasks>

<verification>
- Static checks: grep invariants in Task 1 confirm the pill was hoisted (not duplicated, not deleted, not left inside Header).
- ESLint passes on the modified file.
- Human verification (Task 2) confirms sticky behavior, scroll behavior of remaining header elements, tap handlers, refresh, Android back-handler, FilterModal, and EditorialDock.
</verification>

<success_criteria>
- FloatingSearchPill is rendered as a direct child of SafeAreaView, between StatusBar and FlatList, in HomeScreenV2.tsx.
- The pill remains visually pinned at the top of the screen during all scroll interactions (slow scroll, fast flick, pull-to-refresh).
- All other HomeScreenV2 behaviors are unchanged: GreetingBlock/HeroRotator/SmartShelf/feed cards scroll, refresh works, Android back-handler unwinds filters, FilterModal opens via filter button, EditorialDock renders at the bottom, navigation to SearchResults still fires on pill tap.
- No regressions to FloatingSearchPill.tsx, theme.ts, or any other file.
</success_criteria>

<output>
After completion, create `.planning/quick/260528-c4q-make-floatingsearchpill-sticky-at-top-of/260528-c4q-01-SUMMARY.md`.
</output>
