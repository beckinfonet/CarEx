---
phase: quick-260528-h2r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/v2/V2InviteBanner.tsx
  - src/utils/greetingSubject.ts
  - src/components/home/v2/GreetingBlock.tsx
  - src/screens/HomeScreenV2.tsx
  - src/constants/translations.ts
autonomous: true
requirements:
  - HOME-V2-POLISH-BANNER-MARGIN
  - HOME-V2-POLISH-SMART-GREET

must_haves:
  truths:
    - "V2InviteBanner wrapper has visible bottom margin separating it from the search/filter bar below"
    - "Greeting on HomeScreenV2 renders firstName + city when both available (e.g. 'Доброе утро · Becky · Москва')"
    - "Greeting falls back to firstName only when timezone city is unknown"
    - "Greeting falls back to city only when user.firstName is missing"
    - "Greeting shows just time-of-day (no trailing ' · ') when neither firstName nor city are available"
    - "HomeScreenV2 no longer references t.moscow hardcoded for the greeting kicker"
    - "translations.ts maintains RU/EN parity for every newly added city key"
  artifacts:
    - path: "src/components/home/v2/V2InviteBanner.tsx"
      provides: "Banner wrapper with marginBottom"
      contains: "marginBottom"
    - path: "src/utils/greetingSubject.ts"
      provides: "getCityFromTimezone + buildGreetingSubject helpers"
      exports:
        - "getCityFromTimezone"
        - "buildGreetingSubject"
    - path: "src/components/home/v2/GreetingBlock.tsx"
      provides: "Greeting block that accepts optional subject and omits the divider when absent"
      contains: "subject"
    - path: "src/screens/HomeScreenV2.tsx"
      provides: "Home screen wires useAuth, derives timezone city, passes computed subject to GreetingBlock"
      contains: "useAuth"
    - path: "src/constants/translations.ts"
      provides: "RU + EN city name keys (cityMoscow, citySaintPetersburg, ...) covering the major IANA Russia/Europe timezones"
      contains: "cityMoscow"
  key_links:
    - from: "src/screens/HomeScreenV2.tsx"
      to: "src/utils/greetingSubject.ts"
      via: "import { getCityFromTimezone, buildGreetingSubject }"
      pattern: "from '\\.\\./utils/greetingSubject'"
    - from: "src/screens/HomeScreenV2.tsx"
      to: "src/context/AuthContext"
      via: "import { useAuth } and read user?.firstName"
      pattern: "useAuth\\(\\)"
    - from: "src/screens/HomeScreenV2.tsx"
      to: "src/components/home/v2/GreetingBlock.tsx"
      via: "subject={subject} prop (replaces city={t.moscow})"
      pattern: "subject="
    - from: "src/components/home/v2/GreetingBlock.tsx"
      to: "rendered text"
      via: "conditional ' · ' divider"
      pattern: "subject"
---

<objective>
Home V2 polish — ship two small UX fixes on the V2 home screen.

1. Add `marginBottom: 12` to the V2InviteBanner wrapper so the "Новый дизайн доступен" banner has breathing room from the search/filter pill below it.
2. Replace the hardcoded `city={t.moscow}` kicker in HomeScreenV2 with a smart subject that combines the user's first name with a timezone-derived city when both are available, gracefully falling back when one or both are missing.

Purpose: Polish the V2 home shell after the merge — banner currently kisses the search pill, and every user sees "Москва" regardless of who they are or where they live.

Output:
- Banner wrapper has visible bottom margin
- Greeting renders firstName + city (or firstName / city / neither) without ever crashing
- No new native deps, no geolocation library, no auth/cart/payments touched
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/components/home/v2/V2InviteBanner.tsx
@src/components/home/v2/GreetingBlock.tsx
@src/screens/HomeScreenV2.tsx
@src/constants/translations.ts
@src/context/AuthContext.tsx

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->
<!-- Do NOT re-explore — these are the facts. -->

From src/components/home/v2/GreetingBlock.tsx (current):
- Props interface `GreetingBlockProps` currently requires `city: string` and renders `{timeOfDay} · {city}` unconditionally at line 28.
- After this plan, prop is renamed `city` → `subject?: string` (optional). Render becomes conditional: with subject, render `{timeOfDay} · {subject}`; without, render `{timeOfDay}` only.

From src/screens/HomeScreenV2.tsx:
- Line 14: `import { GreetingBlock } from '../components/home/v2/GreetingBlock';`
- Line 30-35: `timeOfDayKey(t)` already returns localized time-of-day string.
- Line 41: destructures `{ t, language, setLanguage } = useLanguage()` — needs to ALSO destructure `user` from a new `useAuth()` call.
- Line 95-102: GreetingBlock is rendered with `city={t.moscow}` — this is the only call site to change.
- Line 1: `import React, { useEffect, useState, useRef } from 'react';` — must add `useMemo`.

From src/context/AuthContext.tsx:
- `useAuth()` returns `{ user: any, ... }` (user is typed `any`).
- `user?.firstName` is the read pattern used by AccountSettingsScreen.tsx:46 and SellCarScreen.tsx:155.

From src/constants/translations.ts:
- RU block ends around line 1100; EN block starts around line 1150 and ends around line ~2200.
- Existing key `moscow: 'Москва'` (line 627) and `moscow: 'Moscow'` (line 1255). LEAVE THIS KEY UNTOUCHED — it is consumed by SearchResultsV2.tsx:117 as `t.moscowAndRegion`/`t.moscow`.
- Add NEW keys: `cityMoscow`, `citySaintPetersburg`, `cityKaliningrad`, `citySamara`, `cityYekaterinburg`, `cityOmsk`, `cityNovosibirsk`, `cityKrasnoyarsk`, `cityIrkutsk`, `cityYakutsk`, `cityVladivostok`, `cityMagadan`, `cityKamchatka`. All 13 keys must appear in BOTH RU and EN blocks (CLAUDE.md i18n rule: RU-first with EN parity).

From src/components/home/v2/V2InviteBanner.tsx:
- Lines 65-71: `styles.wrapper` has `marginHorizontal: 16, marginTop: 8, padding: 12, borderRadius: 14, backgroundColor: V2.surface, borderWidth: 1, borderColor: 'rgba(77,163,255,0.32)'`.
- Add `marginBottom: 12` inline with `marginTop: 8` (same line is fine).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bottom margin to V2InviteBanner wrapper</name>
  <files>src/components/home/v2/V2InviteBanner.tsx</files>
  <action>
  In `styles.wrapper` (lines 65-71), add `marginBottom: 12` to the existing style block. Keep the existing `marginTop: 8` value. Single-character/single-property edit; do not refactor the styles object or touch anything else in the file.

  Suggested final shape of line 67:
  `marginHorizontal: 16, marginTop: 8, marginBottom: 12, padding: 12,`

  Naming/code-style: follow existing inline `StyleSheet.create` convention (multiple style props per line). No new constants needed; `12` is a literal pixel value consistent with the V2 spacing already used in the file.
  </action>
  <verify>
    <automated>grep -c "marginBottom: 12" src/components/home/v2/V2InviteBanner.tsx</automated>
  </verify>
  <done>
  `grep -c "marginBottom: 12" src/components/home/v2/V2InviteBanner.tsx` returns 1 (or more). `npm run lint -- src/components/home/v2/V2InviteBanner.tsx` passes. App still bundles and renders the banner when V1 is the active version and invite has not been dismissed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add greetingSubject util + translations for timezone-derived city names</name>
  <files>src/utils/greetingSubject.ts, src/constants/translations.ts</files>
  <action>
  Create `src/utils/greetingSubject.ts` with two named exports:

  1. `getCityFromTimezone(timeZone: string, t: any): string | null`
     - Maps an IANA timezone string (e.g. `Europe/Moscow`) to a localized city name pulled from the translations object.
     - Internal `TIMEZONE_TO_CITY_KEY: Record<string, string>` constant (UPPER_SNAKE_CASE per CLAUDE.md) maps each supported IANA zone to a translation key:
       - `'Europe/Moscow'` → `'cityMoscow'`
       - `'Europe/Saint_Petersburg'` → `'citySaintPetersburg'`
       - `'Europe/Kaliningrad'` → `'cityKaliningrad'`
       - `'Europe/Samara'` → `'citySamara'`
       - `'Asia/Yekaterinburg'` → `'cityYekaterinburg'`
       - `'Asia/Omsk'` → `'cityOmsk'`
       - `'Asia/Novosibirsk'` → `'cityNovosibirsk'`
       - `'Asia/Krasnoyarsk'` → `'cityKrasnoyarsk'`
       - `'Asia/Irkutsk'` → `'cityIrkutsk'`
       - `'Asia/Yakutsk'` → `'cityYakutsk'`
       - `'Asia/Vladivostok'` → `'cityVladivostok'`
       - `'Asia/Magadan'` → `'cityMagadan'`
       - `'Asia/Kamchatka'` → `'cityKamchatka'`
     - Lookup key in the map; if absent OR if `t[key]` is falsy, return `null`. Wrap the lookup in try/catch defensively — if anything throws (e.g. `t` is undefined at boot), return `null`.

  2. `buildGreetingSubject({ firstName, city }: { firstName?: string | null; city?: string | null }): string`
     - Trim both inputs. Treat empty string as missing.
     - When both present: return `` `${firstName} · ${city}` ``.
     - When only firstName: return `firstName`.
     - When only city: return `city`.
     - When neither: return empty string `''`.

  File-top JSDoc explaining the purpose (per CLAUDE.md "Function purpose if not self-evident" comment convention).

  Then update `src/constants/translations.ts` — add the 13 new city keys to BOTH the RU and EN blocks. Place them in a contiguous block right after the existing `moscow:` line in each language (line ~627 RU, line ~1255 EN) so they are grep-discoverable as a unit. Do NOT modify the existing `moscow:` / `moscowAndRegion:` keys (still consumed by SearchResultsV2).

  RU values:
    - `cityMoscow: 'Москва'`
    - `citySaintPetersburg: 'Санкт-Петербург'`
    - `cityKaliningrad: 'Калининград'`
    - `citySamara: 'Самара'`
    - `cityYekaterinburg: 'Екатеринбург'`
    - `cityOmsk: 'Омск'`
    - `cityNovosibirsk: 'Новосибирск'`
    - `cityKrasnoyarsk: 'Красноярск'`
    - `cityIrkutsk: 'Иркутск'`
    - `cityYakutsk: 'Якутск'`
    - `cityVladivostok: 'Владивосток'`
    - `cityMagadan: 'Магадан'`
    - `cityKamchatka: 'Петропавловск-Камчатский'`

  EN values:
    - `cityMoscow: 'Moscow'`
    - `citySaintPetersburg: 'Saint Petersburg'`
    - `cityKaliningrad: 'Kaliningrad'`
    - `citySamara: 'Samara'`
    - `cityYekaterinburg: 'Yekaterinburg'`
    - `cityOmsk: 'Omsk'`
    - `cityNovosibirsk: 'Novosibirsk'`
    - `cityKrasnoyarsk: 'Krasnoyarsk'`
    - `cityIrkutsk: 'Irkutsk'`
    - `cityYakutsk: 'Yakutsk'`
    - `cityVladivostok: 'Vladivostok'`
    - `cityMagadan: 'Magadan'`
    - `cityKamchatka: 'Petropavlovsk-Kamchatsky'`

  No new dependency, no native module, no auth/cart/payments touched.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p . 2>&1 | grep -E "(greetingSubject|translations)" | grep -v "^$" | wc -l | xargs -I{} test {} -eq 0 && echo OK</automated>
  </verify>
  <done>
  `src/utils/greetingSubject.ts` exists and exports both `getCityFromTimezone` and `buildGreetingSubject`. `grep -c "cityMoscow:" src/constants/translations.ts` returns 2 (one per language). `grep -c "cityKamchatka:" src/constants/translations.ts` returns 2. The existing `moscow:` key still appears exactly 2 times (`grep -c "^    moscow:" src/constants/translations.ts` returns 2 — unchanged). `npx tsc --noEmit -p .` completes with no errors attributable to the new file or translations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire smart subject into GreetingBlock + HomeScreenV2</name>
  <files>src/components/home/v2/GreetingBlock.tsx, src/screens/HomeScreenV2.tsx</files>
  <action>
  **Part A — `src/components/home/v2/GreetingBlock.tsx`:**

  Rename the `city: string` prop to `subject?: string` in `GreetingBlockProps`. Update the JSDoc above it to read: `/** Optional contextual subject for the kicker (e.g. "Becky · Москва"). When omitted, only the time-of-day is shown. */`.

  In the destructure on line 22, rename `city` → `subject`.

  Change the kicker render (line 28) from:
  ```
  {timeOfDay} · {city}
  ```
  to a conditional that omits the divider entirely when `subject` is falsy or empty after trim. Use a small inline expression — no helper function needed. Trim safety: `subject?.trim()` then check truthiness.

  Suggested implementation: build `const kicker = subject && subject.trim() ? `${timeOfDay} · ${subject}` : timeOfDay;` then render `{kicker}` inside the existing `<Text>`.

  Leave the surrounding markup, styles, chip row, and `trailing` prop untouched.

  **Part B — `src/screens/HomeScreenV2.tsx`:**

  - Line 1: extend the React import to include `useMemo`.
  - Add a new import line below the existing `useLanguage` import (line 8 region): `import { useAuth } from '../context/AuthContext';`
  - Add a new import line near the other helper imports: `import { getCityFromTimezone, buildGreetingSubject } from '../utils/greetingSubject';`
  - Inside the `HomeScreenV2` component, after the `useLanguage()` line (line 41), add `const { user } = useAuth();`.
  - Compute the subject via memo (place above the `Header` JSX, after the existing route effect):
    ```
    const subject = useMemo(() => {
      let tz: string | null = null;
      try { tz = Intl.DateTimeFormat().resolvedOptions().timeZone; } catch { tz = null; }
      const city = tz ? getCityFromTimezone(tz, t) : null;
      return buildGreetingSubject({ firstName: user?.firstName, city });
    }, [user?.firstName, t]);
    ```
  - Replace the `city={t.moscow}` prop on `<GreetingBlock ... />` (line 97) with `subject={subject}`. Leave all other GreetingBlock props (timeOfDay, headline, listingsCount, listingsNoun, trailing) untouched.

  Do not modify any other JSX, the `handleCarPress` / `handleSearchPress` / `handleFiltersPress` handlers, or the `BottomBar` / `FilterModal` blocks. No new third-party dep is added; `Intl` is available on Hermes (RN 0.83) per known-facts.

  Run `npm test` afterwards to ensure no regressions in the existing test suite. There are no targeted tests for HomeScreenV2 (this is V2 polish, not Phase 5/6 moderation work) — the global suite passing is enough.
  </action>
  <verify>
    <automated>grep -c "subject" src/components/home/v2/GreetingBlock.tsx</automated>
  </verify>
  <done>
  `grep -c "subject" src/components/home/v2/GreetingBlock.tsx` returns 4+ (prop name, destructure, conditional expression, JSDoc). `grep -c "t\.moscow" src/screens/HomeScreenV2.tsx` returns 0 (the hardcoded reference is gone). `grep -c "useAuth" src/screens/HomeScreenV2.tsx` returns 2 (import + call). `grep -c "buildGreetingSubject\|getCityFromTimezone" src/screens/HomeScreenV2.tsx` returns 3 (1 import line containing both + 2 call sites). `npx tsc --noEmit -p .` completes with no errors attributable to HomeScreenV2 or GreetingBlock. `npm test` completes with no NEW failures vs. baseline (pre-existing App.test.tsx navigation-stack failure documented in Phase 5 Plan 05-11 SUMMARY is allowed). `npm run lint` passes for the two touched files.
  </done>
</task>

</tasks>

<verification>
End-to-end quick check (manual, after Tasks 1-3 land):

1. iOS sim or Android emulator, fresh metro start: `npm run start:reset` then `npm run ios` (or `npm run android`).
2. Sign in (or use existing dev session). On the V1 HomeScreen, confirm the "Новый дизайн доступен" banner now has a visible gap between its bottom edge and the search/filter pill (or whatever follows it). It should no longer kiss the next UI element.
3. Tap "Try V2" (or use the version toggle) to switch to HomeScreenV2.
4. Confirm the greeting kicker reads `Доброе утро · {firstName} · Москва` (assuming sim is in `Europe/Moscow` and user has a firstName) — or one of the four documented fallback states.
5. Force the four fallback states (each at least once):
   - **Both present:** signed-in user with firstName, sim TZ = Europe/Moscow → `Доброе утро · Becky · Москва`.
   - **firstName only:** sim TZ set to `America/New_York` (not in map) → `Доброе утро · Becky`.
   - **City only:** log out (or use account with no firstName) + sim TZ = Europe/Moscow → `Доброе утро · Москва`.
   - **Neither:** log out + sim TZ = `America/New_York` → `Доброе утро` (no trailing ` · `).
6. Toggle language EN: kicker now reads `Good morning · Becky · Moscow` (etc.).

Automated check (must pass):
```
grep -c "marginBottom: 12" src/components/home/v2/V2InviteBanner.tsx              # >= 1
grep -c "t\.moscow" src/screens/HomeScreenV2.tsx                                    # 0
grep -c "subject" src/components/home/v2/GreetingBlock.tsx                          # >= 4
grep -c "cityMoscow:" src/constants/translations.ts                                 # 2
grep -c "cityKamchatka:" src/constants/translations.ts                              # 2
npx tsc --noEmit -p .                                                               # exit 0
npm run lint                                                                        # exit 0
npm test                                                                            # no new failures
```
</verification>

<success_criteria>
- [ ] V2InviteBanner wrapper has `marginBottom: 12` (separates banner from search pill)
- [ ] `src/utils/greetingSubject.ts` exists and exports `getCityFromTimezone` + `buildGreetingSubject`
- [ ] `src/constants/translations.ts` contains the 13 new `city*` keys in BOTH RU and EN blocks (RU/EN parity per CLAUDE.md)
- [ ] Existing `moscow` / `moscowAndRegion` translation keys are unchanged (SearchResultsV2 still works)
- [ ] `GreetingBlock` accepts optional `subject` prop and omits the ` · ` divider when subject is empty
- [ ] `HomeScreenV2` imports `useAuth`, computes subject via `useMemo`, passes `subject={subject}` to `GreetingBlock`
- [ ] No `t.moscow` references remain in `HomeScreenV2.tsx`
- [ ] No new dependencies in `package.json` (no geolocation lib, no Intl polyfill, no new state-management/HTTP lib)
- [ ] `npx tsc --noEmit` passes (no new TypeScript errors)
- [ ] `npm run lint` passes
- [ ] `npm test` passes (no new failures vs baseline)
- [ ] All four greeting fallback states render correctly on device (manual check)
- [ ] Banner margin visually verified on both V1 and V2 (manual check)
</success_criteria>

<output>
After completion, create `.planning/quick/260528-h2r-home-v2-polish-banner-margin-smart-greet/260528-h2r-SUMMARY.md` summarizing:
- The 3 commits produced (or 2, if Tasks 1+2 combined)
- Final state of `npx tsc --noEmit`, `npm run lint`, `npm test`
- Any deviations from the plan (and why)
- Screenshot or text description of the four fallback states confirmed on device
</output>
