---
phase: quick-260603-i4w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/v2/ListingStatusBadge.tsx
  - src/components/home/v2/SmallFeedCard.tsx
  - src/components/home/v2/BigFeedCard.tsx
autonomous: true
requirements: [QUICK-i4w]

must_haves:
  truths:
    - "A booked listing in SearchResultsV2 shows a 'Booked' badge on the card photo"
    - "A booked listing in HomeScreenV2 feed shows a 'Booked' badge on the card photo"
    - "A sold listing shows a 'Sold' badge on the card photo"
    - "An active listing shows NO badge"
    - "Badge label respects RU/EN language via existing t.booked/t.sold keys"
  artifacts:
    - path: "src/components/home/v2/ListingStatusBadge.tsx"
      provides: "Presentational status pill rendered over a feed-card photo"
      contains: "ListingStatusBadge"
    - path: "src/components/home/v2/SmallFeedCard.tsx"
      provides: "Small feed card with badge in photoWrap + listingStatus on interface"
      contains: "ListingStatusBadge"
    - path: "src/components/home/v2/BigFeedCard.tsx"
      provides: "Big feed card with badge in photoWrap + listingStatus on interface"
      contains: "ListingStatusBadge"
  key_links:
    - from: "src/components/home/v2/SmallFeedCard.tsx"
      to: "src/components/home/v2/ListingStatusBadge.tsx"
      via: "rendered inside photoWrap with status={car.listingStatus}"
      pattern: "ListingStatusBadge status=\\{car.listingStatus\\}"
    - from: "src/components/home/v2/BigFeedCard.tsx"
      to: "src/components/home/v2/ListingStatusBadge.tsx"
      via: "rendered inside photoWrap with status={car.listingStatus}"
      pattern: "ListingStatusBadge status=\\{car.listingStatus\\}"
---

<objective>
Booked/sold V2 feed listings currently show NO status indicator because `SmallFeedCard` and `BigFeedCard` never render one (only v1 `CarCard` does). A user's booked test listing appears in `Все авто` (SearchResultsV2) and the home feed (HomeScreenV2) with no "Booked" label.

This plan adds a small presentational `ListingStatusBadge` (V2-styled, sits ON the photo, top-left) and renders it inside the `photoWrap` of both V2 feed cards.

Purpose: Operators and buyers can see at a glance which V2-feed listings are booked or sold, matching the v1 `CarCard` behavior.
Output: One new component + two card edits. No screen, hook, backend, or translation changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/home/v2/theme.ts
@src/components/home/v2/SmallFeedCard.tsx
@src/components/home/v2/BigFeedCard.tsx

<interfaces>
<!-- Existing v1 badge pattern (CarCard.tsx ~22-33) — replicate semantics, restyle with V2 tokens: -->
<!--   status = data.listingStatus || 'active'; render only if 'booked' || 'sold'; -->
<!--   label = status === 'sold' ? t.sold : t.booked. -->

<!-- i18n hook (project convention): -->
import { useLanguage } from '../../../context/LanguageContext';
const { t } = useLanguage();   // t.booked, t.sold already exist in RU + EN — DO NOT add keys

<!-- V2 tokens (src/components/home/v2/theme.ts) available for styling: -->
V2.green     // '#67E8B6'  — use for SOLD
V2.ember     // '#F2BD98'  — warm amber, use for BOOKED
V2.emberBd   // 'rgba(255,176,128,0.30)'
V2.radius.pill  // 999
V2.bg        // '#08090C'  — dark text color for legibility over the solid pill fill

<!-- Confirmed: listingStatus already flows to both cards. useHomeListings.ts ~44 maps -->
<!-- `listingStatus: car.listingStatus || 'active'` then spreads `...car`, so items passed -->
<!-- as car={item} in SearchResultsV2 (~173-181) and HomeScreenV2 (~279-285) carry listingStatus. -->
<!-- NO call-site / screen edits needed. -->

<!-- SmallFeedCard photoWrap (line 31-33): <View style={styles.photoWrap}> wraps OptimizedImage. -->
<!--   styles.photoWrap = { width: 124, aspectRatio: 1 } — badge is absolute top-left inside it. -->
<!-- BigFeedCard photoWrap (line 49-79): holds image + absolute-fill gradient + heart + bottomBlock. -->
<!--   Insert badge as a sibling after OptimizedImage (so it sits above the gradient/photo, top-left, -->
<!--   clear of the top-right heart and the bottom bottomBlock). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ListingStatusBadge V2 component</name>
  <files>src/components/home/v2/ListingStatusBadge.tsx</files>
  <action>
Create a small presentational component `ListingStatusBadge` with prop `status?: string`.
Return `null` unless `status === 'booked' || status === 'sold'` (mirror CarCard semantics; treat undefined/'active' as no-badge).
Use `useLanguage()` from `../../../context/LanguageContext` for the label: `t.sold` when sold, else `t.booked`. DO NOT add new translation keys.
Render an absolute-positioned pill at top-left of its parent (`position: 'absolute', top: 8, left: 8`), using V2 tokens from `./theme`:
  - Booked: solid/high-opacity warm fill (V2.ember as backgroundColor) with a subtle border (V2.emberBd).
  - Sold: solid fill V2.green.
  - Both: borderRadius V2.radius.pill, small horizontal/vertical padding (e.g. paddingHorizontal 8, paddingVertical 3).
Text must be legible over arbitrary photos: use dark text (V2.bg / '#08090C') on the solid fill — NOT a low-opacity wash. Font small (~10-11px, fontWeight '800', letterSpacing slightly negative) to match the V2 editorial aesthetic. Use the existing typography hook if convenient (`useTypography().display`) but plain text styling is acceptable; keep it small and unobtrusive.
StyleSheet.create at bottom of file per project convention.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -i 'ListingStatusBadge' ; test $? -ne 0</automated>
  </verify>
  <done>File exists exporting `ListingStatusBadge`; returns null for active/undefined; renders V2-styled pill with dark text for booked (ember) and sold (green); uses t.booked/t.sold; no new translation keys; tsc reports no errors referencing this file.</done>
</task>

<task type="auto">
  <name>Task 2: Render badge in both V2 feed cards + extend interfaces</name>
  <files>src/components/home/v2/SmallFeedCard.tsx, src/components/home/v2/BigFeedCard.tsx</files>
  <action>
In BOTH files:
  1. Import the new badge: `import { ListingStatusBadge } from './ListingStatusBadge';`
  2. Add `listingStatus?: string;` to the card car interface (`SmallFeedCardCar` and `BigFeedCardCar`).
SmallFeedCard: inside `<View style={styles.photoWrap}>` (line ~31), render `<ListingStatusBadge status={car.listingStatus} />` as a sibling AFTER the `<OptimizedImage />` so it overlays the image at top-left. photoWrap already clips with `overflow: 'hidden'` on the parent row — badge top-left is clear of card content.
BigFeedCard: inside `<View style={styles.photoWrap}>`, render `<ListingStatusBadge status={car.listingStatus} />` immediately AFTER the `<OptimizedImage />` (and after/around the LinearGradient is fine, but place it so it sits at top-left, clear of the top-right `heartButton` and the bottom `bottomBlock`). Top-left at top:8/left:8 avoids both.
DO NOT edit SearchResultsV2.tsx or HomeScreenV2.tsx — `listingStatus` already flows via the spread `car` object from useHomeListings. DO NOT touch v1 CarCard, HeroCard, or ShelfCard.
  </action>
  <verify>
    <automated>grep -c 'ListingStatusBadge status={car.listingStatus}' src/components/home/v2/SmallFeedCard.tsx src/components/home/v2/BigFeedCard.tsx && grep -c 'listingStatus?: string' src/components/home/v2/SmallFeedCard.tsx src/components/home/v2/BigFeedCard.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -iE 'SmallFeedCard|BigFeedCard' ; test $? -ne 0</automated>
  </verify>
  <done>Both cards import and render `<ListingStatusBadge status={car.listingStatus} />` inside photoWrap; both interfaces declare `listingStatus?: string`; no edits to screens or other cards; tsc reports no errors for these files.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.json` passes (no new errors in the three touched files).
- `npm run lint` clean for the three files.
- Manual (out of scope to automate, optional): with a booked test listing, SearchResultsV2 and HomeScreenV2 small cards show an amber "Booked" pill top-left on the photo; a sold listing shows a green "Sold" pill; active listings show nothing.
- No call-site edits in SearchResultsV2.tsx / HomeScreenV2.tsx (listingStatus already flows via spread).
</verification>

<success_criteria>
- New `ListingStatusBadge` renders a legible top-left pill (dark text on solid V2.ember/booked or V2.green/sold) over feed-card photos.
- Returns null for active/undefined status.
- Both `SmallFeedCard` and `BigFeedCard` render the badge in their `photoWrap` and accept `listingStatus?: string`.
- No translation keys added; uses existing t.booked / t.sold.
- No backend, hook, screen, v1 CarCard, HeroCard, or ShelfCard changes.
- HeroCard / ShelfCard left untouched — noted as a possible follow-up for full V2 coverage.
</success_criteria>

<output>
After completion, create `.planning/quick/260603-i4w-v2-feed-card-status-badge/260603-i4w-SUMMARY.md`
</output>
