---
phase: quick-260603-nl5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constants/translations.ts
  - src/components/moderation/ListingModerationBottomSheet.tsx
  - src/screens/CarDetailsScreen.tsx
  - src/screens/__tests__/CarDetailsScreen.admin.test.tsx
autonomous: true
requirements: [NL5-01, NL5-02, NL5-03]

must_haves:
  truths:
    - "When an admin views their OWN listing, the moderation bottom sheet shows NO action rows (Edit/Suspend/Archive/Delete/Restore) and instead shows an explanatory note."
    - "When an admin views someone ELSE's listing, all existing moderation actions still appear exactly as before."
    - "The ShieldAlert moderate badge still renders for any admin (own listing or not) — gating unchanged."
    - "The admin error banner shows human-readable translated copy for cannot_moderate_own_listing and already_in_state, and falls back to the raw code for unknown codes."
    - "All new strings have RU + EN parity."
  artifacts:
    - path: "src/components/moderation/ListingModerationBottomSheet.tsx"
      provides: "Owner-aware bottom sheet body (isOwner prop suppresses actions, shows note)"
    - path: "src/screens/CarDetailsScreen.tsx"
      provides: "isOwner passed to sheet + error-code-to-translation mapping in the banner"
    - path: "src/constants/translations.ts"
      provides: "New RU+EN keys: listingModerationOwnerNote, errorCannotModerateOwnListing, errorAlreadyInState"
  key_links:
    - from: "src/screens/CarDetailsScreen.tsx"
      to: "ListingModerationBottomSheet"
      via: "isOwner prop"
      pattern: "isOwner=\\{"
    - from: "src/screens/CarDetailsScreen.tsx errorBanner block"
      to: "translations"
      via: "code-to-copy lookup with raw-code fallback"
      pattern: "cannot_moderate_own_listing"
---

<objective>
Fix the admin self-moderation UX on CarDetailsScreen. When the admin is also the listing owner, the backend rejects all 5 listing-moderation actions with `cannot_moderate_own_listing`, but the UI still offers those actions and renders the raw error code. Three mobile-only changes:

1. Hide all 5 moderation actions in the bottom sheet for owners; show a translated explanatory note instead.
2. Map known error codes (`cannot_moderate_own_listing`, `already_in_state`) to translated human-readable copy in the admin error banner, falling back to the raw code for unknown codes.
3. Add the supporting RU+EN translation keys.

Purpose: Stop offering actions that always 400, and stop leaking raw error codes to the operator.
Output: Owner-aware bottom sheet, friendly error banner, new translation keys, and updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
Bottom sheet current props (src/components/moderation/ListingModerationBottomSheet.tsx):
```typescript
export interface ListingModerationBottomSheetProps {
  visible: boolean;
  listingTitle: string;
  moderationBadge?: { status; reasonCategory?; moderationReason?; moderatedBy?; moderatedAt?; };
  onSelect: (action: ListingModerationAction) => void;
  onClose: () => void;
}
```
The sheet reads `const { t } = useLanguage();` then `const T = t as unknown as Record<string, string>;` and uses `T.someKey ?? 'fallback'`. Body branches on `isActive = !moderationBadge`: active → 4 ActionRows (edit/suspend/archive/delete), non-active → reasonChip + since pill + Restore ActionRow. A Cancel row always renders below.

CarDetailsScreen (src/screens/CarDetailsScreen.tsx):
- `isOwner` defined ~line 87: `const isOwner = car?.sellerId && user?.localId === car.sellerId;`
- Sheet rendered ~line 1174 with props visible/listingTitle/moderationBadge/onSelect/onClose.
- errorBanner block ~line 711 renders `{errorBanner}` raw inside `<Text style={styles.adminErrorBannerText}>`.
- `errorBanner` state holds the error CODE string (set via `setErrorBanner(err.code)` ~line 532). Control flow in `handleListingActionSubmit` (~485-543) must stay unchanged — only the banner RENDER changes.

translations.ts: two language objects, RU first (modalCancel ~line 464), EN second (modalCancel ~line 1451). New keys go into BOTH blocks. RU is primary per CLAUDE.md.
</interfaces>

Test file (src/screens/__tests__/CarDetailsScreen.admin.test.tsx):
- Uses a Proxy mock for `t` that returns the KEY name for any property access: `new Proxy({}, { get: (_t, k) => String(k) })`. So in tests, `t.errorCannotModerateOwnListing` evaluates to the string `"errorCannotModerateOwnListing"`, and a raw-code fallback returns the code itself.
- Default fixture: `sellerId: 'seller-99'`, default admin auth `user.localId: 'admin-1'` → isOwner FALSE for most tests (sheet behavior unchanged for them).
- T8 (~line 370) "cannot_moderate_own_listing → INLINE banner, NO Alert" asserts `JSON.stringify(tree.toJSON())).toContain('cannot_moderate_own_listing')`. After this change the banner renders the MAPPED key string, not the raw code — this assertion must be updated.
- T7 (~line 338) "already_in_state" asserts `rendered).toContain('already_in_state')` — same update needed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RU+EN translation keys</name>
  <files>src/constants/translations.ts</files>
  <action>
Add three new keys to BOTH the RU block (primary, ~near line 464 modalCancel) and the EN block (~near line 1451 modalCancel), keeping RU/EN parity (NL5-03):
- `listingModerationOwnerNote`: explains the admin cannot moderate their own listing and should use the status controls (mark sold) or the owner edit below. RU primary, EN parity. Use CarEx audience tone (RU-first, plain operator-facing copy; no Russia-specific regulatory terms).
- `errorCannotModerateOwnListing`: human-readable copy for the `cannot_moderate_own_listing` code (e.g. RU: "Нельзя модерировать собственное объявление." / EN: "You can't moderate your own listing.").
- `errorAlreadyInState`: human-readable copy for the `already_in_state` code (e.g. RU: "Объявление уже в этом статусе." / EN: "Listing is already in that state.").
Place them near the existing modal/admin copy with a short grouping comment. Do not modify or reorder existing keys.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i translations || echo "no translations type errors"</automated>
  </verify>
  <done>All three keys exist in both RU and EN blocks with matching key names; tsc reports no errors in translations.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Owner-aware bottom sheet + translated error banner</name>
  <files>src/components/moderation/ListingModerationBottomSheet.tsx, src/screens/CarDetailsScreen.tsx</files>
  <action>
Bottom sheet (NL5-01): Add an optional `isOwner?: boolean` field to `ListingModerationBottomSheetProps`. In the render, when `isOwner` is true, render NEITHER the active 4-action branch NOR the non-active Restore branch. Instead render an explanatory note `<Text>` using `T.listingModerationOwnerNote ?? '<fallback EN>'`, wrapped in a `View` styled consistently with the existing `restoreBody`/note spacing. Give the note a `testID="listing-owner-note"`. Keep the header (listingTitle) and the Cancel row unchanged. The branch order should be: if `isOwner` → note; else if `isActive` → 4 actions; else → Restore body. Add any needed style to the StyleSheet at the bottom (use COLORS/SIZES/TYPOGRAPHY, no hardcoded hex).

CarDetailsScreen — pass the prop (NL5-01): At the `<ListingModerationBottomSheet ... />` render (~line 1174), add `isOwner={!!isOwner}`. Do NOT touch the `isAdmin &&` badge gate (~line 587) — it stays gated only on isAdmin.

CarDetailsScreen — error banner mapping (NL5-02): In the `errorBanner` block (~line 711), replace the raw `{errorBanner}` text with a mapped value. Build a lookup: `cannot_moderate_own_listing` → `t.errorCannotModerateOwnListing`, `already_in_state` → `t.errorAlreadyInState`, and for any other/unknown code fall back to the raw `errorBanner` string so nothing breaks. Render the mapped string in the existing `<Text style={styles.adminErrorBannerText}>`. Do NOT change `handleListingActionSubmit` control flow — `setErrorBanner(err.code)` still stores the code; only the render maps it.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "ListingModerationBottomSheet|CarDetailsScreen" || echo "no type errors in touched files"</automated>
  </verify>
  <done>Sheet hides all 5 actions and shows the note when isOwner is true; non-owner admin behavior unchanged; banner renders translated copy for the two known codes and raw code otherwise; isOwner prop wired; badge gate untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Update affected Jest assertions</name>
  <files>src/screens/__tests__/CarDetailsScreen.admin.test.tsx</files>
  <action>
The Proxy `t` mock returns the key name as the value. After Task 2, the banner renders the MAPPED key string instead of the raw code:
- T8 (~line 394): change `expect(JSON.stringify(tree.toJSON())).toContain('cannot_moderate_own_listing')` to assert the rendered tree contains the mapped key `'errorCannotModerateOwnListing'`. Keep the `admin-error-banner` presence assertion and `alertSpy` not-called assertion unchanged.
- T7 (~line 365): change `expect(rendered).toContain('already_in_state')` to assert `'errorAlreadyInState'`. Keep the inline-banner-present and no-Alert assertions unchanged.
Add ONE new test (e.g. T16) asserting owner-suppression of actions: set `setMockAuth({ user: { localId: 'seller-99' }, isAdmin: true })` (matches FIXTURE_ACTIVE_CAR.sellerId so isOwner is true), mount, tap the moderate badge to open the sheet, then assert `findAllByTestID(tree.root, 'listing-action-edit').length).toBe(0)` (and suspend/archive/delete/restore all 0) and `findAllByTestID(tree.root, 'listing-owner-note').length).toBeGreaterThan(0)`. Do not weaken any other existing assertion. Note: T2 already uses seller-99 admin but only checks the badge renders — leave it as-is.
  </action>
  <verify>
    <automated>npx jest src/screens/__tests__/CarDetailsScreen.admin.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>The full CarDetailsScreen.admin test suite passes, including updated T7/T8 and the new owner-suppression test.</done>
</task>

</tasks>

<verification>
- `npx jest src/screens/__tests__/CarDetailsScreen.admin.test.tsx` passes (all tests green).
- `npx tsc --noEmit` reports no new errors in the three touched source files.
- `npm run lint` clean on touched files.
- Manual reasoning: non-owner admin sheet is byte-for-byte behaviorally identical (no isOwner → original branches).
</verification>

<success_criteria>
- Owner admin: bottom sheet shows only the note + Cancel (zero action rows).
- Non-owner admin: all original moderation actions present.
- Moderate badge still renders for any admin.
- Error banner shows translated copy for the two known codes, raw code for unknown.
- RU+EN parity for all new keys.
- Backend untouched; control flow in handleListingActionSubmit unchanged.
</success_criteria>

<output>
After completion, create `.planning/quick/260603-nl5-admin-self-mod-ux/260603-nl5-SUMMARY.md`
</output>
