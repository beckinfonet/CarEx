---
phase: quick-260607-c2i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - App.tsx
  - android/app/src/main/java/com/carex/market/MainApplication.kt
  - src/components/notifications/WatchButton.tsx
  - src/components/notifications/__tests__/WatchButton.test.tsx
  - src/hooks/useHomeListings.ts
  - .planning/quick/260607-c2i-fix-pr12-push-findings/260607-c2i-deferred-items.md
autonomous: true
requirements: [NPUSH-06, NPUSH-07, NPUSH-02, NSUB-04]

must_haves:
  truths:
    - "Tapping a carex://listing/:carId push (price_drop/booked/sold/back_available) opens CarDetails for that car"
    - "Tapping a carex://search?<criteria> push opens SearchResults with make/model/price/year/bodyType filters applied"
    - "Android push notifications render on the branded carex_default channel (not the unbranded FCM fallback)"
    - "Returning to CarDetails for an already-watched car shows the active Watching state and does not POST a duplicate subscription"
    - "A saved-search deeplink carrying bodyType (e.g. SUV) filters the home list by that body category"
    - "The iOS aps-environment development->production swap is documented in the deferred release-prep checklist"
  artifacts:
    - path: "App.tsx"
      provides: "routeDeeplink mirroring NotificationsScreen.routeNotification parsing"
      contains: "carex"
    - path: "android/app/src/main/java/com/carex/market/MainApplication.kt"
      provides: "carex_default NotificationChannel creation in onCreate"
      contains: "createNotificationChannel"
    - path: "src/components/notifications/WatchButton.tsx"
      provides: "watch-state hydration from listSubscriptions on mount"
      contains: "listSubscriptions"
    - path: ".planning/quick/260607-c2i-fix-pr12-push-findings/260607-c2i-deferred-items.md"
      provides: "release-prep note for aps-environment swap"
      contains: "aps-environment"
  key_links:
    - from: "App.tsx routeDeeplink"
      to: "navigationRef.navigate CarDetails"
      via: "regex matching carex://listing/:carId"
      pattern: "listing"
    - from: "WatchButton mount effect"
      to: "NotificationService.listSubscriptions"
      via: "useEffect hydration keyed on watchKey"
      pattern: "listSubscriptions"
---

<objective>
Fix 5 ultrareview findings on PR #12 (Phase 13 push notifications). Each is an
independent atomic correctness fix in a different file, ordered by blast radius.
The deeplink-parsing reference implementation already exists and works:
`routeNotification` in `src/screens/NotificationsScreen.tsx` (exported). Mirror it.

Purpose: push-tap routing, Android channel branding, watch-state correctness, and
saved-search filtering all currently fail silently — they regress the core Phase 13
value (alerts that open the right screen) without throwing.
Output: corrected App.tsx, MainApplication.kt, WatchButton.tsx, useHomeListings.ts,
and a deferred release-prep note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- The CORRECT reference parser to mirror in Finding 1. Already exported. -->
From src/screens/NotificationsScreen.tsx:
- STRING_FILTER_KEYS = ['makeId', 'modelId', 'bodyType'] (carried as-is)
- NUMERIC_FILTER_KEYS = ['priceMin', 'priceMax', 'yearMin', 'yearMax'] (Number() + NaN guard)
- listing match uses regex: /^carex:\/\/listing\/([^/?#]+)/ then navigate('CarDetails', { carId: decodeURIComponent(match[1]) })
- search uses deeplink.startsWith('carex://search'), parses query into initialFilters, navigate('SearchResults', { initialQuery, ...(filters ? { initialFilters } : {}) })
- exported signature: routeNotification(notification: NotificationItem, navigate): void

From src/services/notifications/NotificationService.ts:
- listSubscriptions: () => Promise<Subscription[]>  (already unwraps the { items: [...] } envelope)
- deleteSubscription: (id: string) => Promise<any>
- interface Subscription { _id: string; kind: 'watch'|'saved_search'; carId?: string; active: boolean; ... }

From src/hooks/useHomeListings.ts:
- CATEGORIES (src/constants/mockData.ts): { id, name } RU labels — 1 Sedan, 2 SUV/RV, 3 Passenger, 4 Truck, 5 Special
- filteredCars already case-insensitive substring-matches selectedCategory.name against car.bodyType (both directions, plus an id===2 'suv' special case)
- normalizeInitialFilters already carries src.bodyType -> next.bodyType but NOTHING reads it, and selectedCategory is never seeded from initialFilters
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix App.tsx routeDeeplink (carex:// listing + search parsing)</name>
  <files>App.tsx</files>
  <action>
Rewrite routeDeeplink (~lines 87-140) to mirror NotificationsScreen.routeNotification
for the carex:// scheme, fixing both defects.

DEFECT (a) carex://listing no-op: current code strips the scheme then slices off the first
path segment, so carex://listing/abc collapses to segments[0]='abc' and neither branch
matches. Replace segment-slicing for the listing case with the SAME regex the reference uses:
match /^carex:\/\/listing\/([^/?#]+)/ then navigationRef.navigate('CarDetails',
{ carId: decodeURIComponent(match[1]) }).

DEFECT (b) search filters dropped: current code reads params.get('initialFilters') as a JSON
blob, but the backend encodes discrete URLSearchParams. For deeplink.startsWith('carex://search'),
parse the query and build initialFilters by iterating the SAME key sets as the reference:
string keys ['makeId','modelId','bodyType'] carried as-is, numeric keys
['priceMin','priceMax','yearMin','yearMax'] coerced via Number() with a NaN guard. Pass
initialQuery from params.initialQuery || '' and include initialFilters only when at least one
key is present.

PRESERVE: (1) the existing https-URL handling — https://www.carexmarket.com/listing/abc already
works (the dotted host absorbs the first slash); do NOT regress the https path. (2) the
unknown->no-op whitelist behavior (only CarDetails + SearchResults route). (3) the
navigationRef.isReady() guard and the try/catch.

PREFER reuse over duplication: routeNotification is exported but its signature is
(notification: NotificationItem, navigate). If you can cleanly call it by wrapping
{ data: { deeplink } } and passing navigationRef.navigate, do that to avoid a second copy of
the carex:// parsing — but only if it keeps the https branch working. If wrapping is awkward,
extract a shared parseDeeplinkTarget(deeplink) helper used by both files; otherwise duplicate
the carex:// branches faithfully. Do not widen the whitelist. No new deps.
  </action>
  <verify>
    <automated>npx jest src/screens/__tests__/NotificationsScreen.test.tsx --silent && npx eslint App.tsx</automated>
  </verify>
  <done>routeDeeplink routes carex://listing/:carId to CarDetails and carex://search?makeId=&priceMin=... to SearchResults with initialFilters populated; https variant still works; unknown targets no-op; lint clean. App.tsx no longer JSON-parses params.get('initialFilters').</done>
</task>

<task type="auto">
  <name>Task 2: Create carex_default Android notification channel in MainApplication.kt</name>
  <files>android/app/src/main/java/com/carex/market/MainApplication.kt</files>
  <action>
The manifest declares com.google.firebase.messaging.default_notification_channel_id =
carex_default (AndroidManifest.xml lines 20-22) but the channel is created nowhere (notifee was
rejected in the D-03 spike). On Android 8+ (API 26 / O) a channel must be registered before use,
so every push currently downgrades to FCM's unbranded fallback channel.

In MainApplication.kt onCreate(), BEFORE loadReactNative(this), register the channel
idempotently (createNotificationChannel is a no-op when the id already exists). Guard with
Build.VERSION.SDK_INT >= Build.VERSION_CODES.O. Use:
- id: string literal "carex_default" (must exactly match the manifest value)
- name: the app name via getString(R.string.app_name) (resolves to "CarEx: Marketplace")
- importance: NotificationManager.IMPORTANCE_DEFAULT
Then get the system service via getSystemService(NotificationManager::class.java) and call
createNotificationChannel(channel).

Add imports: android.app.NotificationChannel, android.app.NotificationManager, android.os.Build.
(R is com.carex.market.R — package already matches.) Keep the existing reactHost lazy block and
super.onCreate() untouched; only prepend the channel registration ahead of loadReactNative(this).
  </action>
  <verify>
    <automated>grep -q createNotificationChannel android/app/src/main/java/com/carex/market/MainApplication.kt && grep -q '"carex_default"' android/app/src/main/java/com/carex/market/MainApplication.kt && grep -q 'Build.VERSION_CODES.O' android/app/src/main/java/com/carex/market/MainApplication.kt && echo CHANNEL_OK</automated>
  </verify>
  <done>MainApplication.kt onCreate creates the carex_default channel (IMPORTANCE_DEFAULT, app-name label) under an SDK_INT >= O guard, before loadReactNative; grep prints CHANNEL_OK. Full gradle build is heavy — static grep verification is sufficient here; native runtime confirmed at next Android UAT.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hydrate WatchButton state from existing subscriptions (dedup)</name>
  <files>src/components/notifications/WatchButton.tsx, src/components/notifications/__tests__/WatchButton.test.tsx</files>
  <behavior>
    - On mount with a watchKey, calls NotificationService.listSubscriptions(); if an active watch subscription has carId === watchKey, setWatching(true) and caches that subscription _id.
    - No matching subscription -> stays watching=false (shows t.watchCta), createSubscription still works on tap.
    - Remount for an already-watched car does NOT call createSubscription on mount (no duplicate POST).
    - setState after unmount is guarded (mounted flag) — no act/leak warning.
  </behavior>
  <action>
watching is useState(false) with no hydration (WatchButton.tsx:63), so navigating away from
CarDetails and back remounts the button showing the inactive label for an already-watched car;
tapping POSTs a duplicate { kind:'watch', carId } (backend has no Subscription-row dedup). This
was flagged as WR-03 in 12-REVIEW.md and never addressed.

Add a mount useEffect (depend on watchKey) that:
1. Returns early if !watchKey.
2. Uses a `let mounted = true` guard; cleanup sets mounted = false.
3. Calls NotificationService.listSubscriptions(), finds the first sub where
   sub.kind === 'watch' && sub.active && String(sub.carId) === String(watchKey).
4. If found and still mounted: setWatching(true) and store sub._id in a useRef<string|null>
   so a future unwatch can target it via deleteSubscription. (Do NOT implement an unwatch tap in
   this task — just cache the id so the active-state contract is complete; handlePress already
   early-returns when watching is true, which is the correct no-duplicate behavior.)
5. Wrap in try/catch — a failed hydration logs via console.error and leaves watching=false (fail
   open to the existing tap flow); never throw on mount.

Do not change the watchKey resolver (car?._id || car?.id || carId — load-bearing, NSUB-04/D-04).
No new user-facing strings (reuse t.watchCta / t.watchCtaActive), so no translation changes.

Update WatchButton.test.tsx: the existing jest.mock for NotificationService only stubs
createSubscription — add listSubscriptions: jest.fn().mockResolvedValue([]) to the mock object
and reset it in beforeEach so existing tests stay green. Add ONE new test: when listSubscriptions
resolves an active watch sub matching the car's id, the button renders the active label and a
subsequent press does NOT call createSubscription (assert createSubscription not called).
  </action>
  <verify>
    <automated>npx jest src/components/notifications/__tests__/WatchButton.test.tsx --silent</automated>
  </verify>
  <done>WatchButton hydrates watching from listSubscriptions on mount; remounting an already-watched car shows the active state and does not POST a duplicate subscription; new + existing WatchButton tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: Apply bodyType filter on saved-search deeplink in useHomeListings</name>
  <files>src/hooks/useHomeListings.ts</files>
  <action>
normalizeInitialFilters carries src.bodyType -> next.bodyType (useHomeListings.ts:83-85, with a
"future consumer" comment) but filteredCars never reads activeFilters.bodyType and
selectedCategory is never seeded from initialFilters — so an SUV saved-search deeplinks with
make/model/price/year applied but ALL body types shown.

Pick the cleaner fix (option a is preferred — it reuses the existing, tested category-matching
path rather than adding a parallel predicate):

(a) In normalizeInitialFilters, when src.bodyType is present, reverse-lookup CATEGORIES by name
(case-insensitive substring, mirroring the existing matchesCategory logic: category.name vs
bodyType in either direction, plus the id===2 'suv' special case) to resolve a category id, and
return it as a new selectedCategory field on the result object. Then in useHomeListings, seed
the selectedCategory useState from seed.selectedCategory (currently hard-initialized to null at
line 114). Keep carrying next.bodyType too (harmless). Update the function return type and the
non-canonical-keys early-return to include selectedCategory: null so the shape is consistent. The
CR-03 byte-equality guarantee for the no-canonical-keys path must hold (when no canonical key is
present, selectedCategory stays null and activeFilters is returned unchanged).

OR (b) add an activeFilters.bodyType branch in filteredCars alongside the Year/Price branches,
using the same case-insensitive substring match (both directions) against car.bodyType.

Whichever you choose, the no-deeplink home path must remain unaffected (no behavior change when
initialFilters is undefined or carries no canonical keys). No new deps, no new strings.
  </action>
  <verify>
    <automated>npx jest src/hooks/__tests__/useHomeListings.test.tsx --silent && npx eslint src/hooks/useHomeListings.ts</automated>
  </verify>
  <done>A saved-search deeplink carrying bodyType filters the home list by that body category; the no-deeplink home path is unchanged (CR-03 byte-equality holds); existing useHomeListings tests pass and lint is clean.</done>
</task>

<task type="auto">
  <name>Task 5: Document iOS aps-environment release-prep swap (doc-only)</name>
  <files>.planning/quick/260607-c2i-fix-pr12-push-findings/260607-c2i-deferred-items.md</files>
  <action>
ios/carEx/carEx.entitlements line 5 sets aps-environment = development, and both Debug and
Release configs in project.pbxproj point at this same file, so an App Store archive would ship
the sandbox APNs entitlement. This is the correct value for current dev/TestFlight, so do NOT
change it. Already a known release-prep TODO (13-PATTERNS.md:236).

This is a doc-only task. Create the deferred-items file recording the release-prep action,
paralleling the existing Stripe pk_live swap note. Include:
- File + line: ios/carEx/carEx.entitlements (aps-environment, currently development)
- The required action: swap development -> production before the App Store (production) archive;
  development stays correct for dev and TestFlight builds.
- Why it is deferred: both Debug and Release pbxproj configs reference the single entitlements
  file, so there is no per-config split today; flipping the value now would break dev/TestFlight
  push.
- Cross-reference the parallel Stripe pk_test -> pk_live + Railway sk_live release swap already
  tracked in STATE.md, so all production-archive prerequisites live in one checklist.

Only escalate to an actual code change (a clean per-config entitlements split in project.pbxproj)
if reading the pbxproj reveals it is trivial; otherwise leave it doc-only as specified.
  </action>
  <verify>
    <automated>test -f .planning/quick/260607-c2i-fix-pr12-push-findings/260607-c2i-deferred-items.md && grep -qi 'aps-environment' .planning/quick/260607-c2i-fix-pr12-push-findings/260607-c2i-deferred-items.md && echo DEFERRED_OK</automated>
  </verify>
  <done>Deferred-items file exists documenting the aps-environment development->production swap as a pre-App-Store-archive release-prep step, cross-referencing the Stripe live-key swap; entitlements value left unchanged.</done>
</task>

</tasks>

<verification>
- npx jest src/screens/__tests__/NotificationsScreen.test.tsx src/components/notifications/__tests__/WatchButton.test.tsx src/hooks/__tests__/useHomeListings.test.tsx --silent  (all green; no regressions)
- npx eslint App.tsx src/components/notifications/WatchButton.tsx src/hooks/useHomeListings.ts  (clean)
- grep verifications for Tasks 2 (CHANNEL_OK) and 5 (DEFERRED_OK) pass
- No new dependencies added (package.json unchanged); no new hardcoded keys; no user-facing string changes (so no RU/EN parity work needed)
</verification>

<success_criteria>
All 5 findings closed:
1. carex://listing and carex://search push taps route correctly (App.tsx mirrors routeNotification)
2. carex_default Android channel registered natively before loadReactNative
3. WatchButton hydrates watch state on mount — no duplicate subscription POST on remount
4. bodyType saved-search deeplink filters the home list by category
5. aps-environment swap documented in deferred release-prep checklist (value unchanged)
Existing test suite shows no regressions; lint clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260607-c2i-fix-pr12-push-findings/260607-c2i-SUMMARY.md`
</output>
